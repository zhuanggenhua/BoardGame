# 山屋惊魂怪物运行时 E2E 截图验收

## 命令

- `node --max-old-space-size=4096 scripts/infra/run-e2e-single.mjs isolated e2e/betrayal/monster-runtime.e2e.ts`
- 结果：`1 passed`
- 本轮最终确认命令：`npm run test:e2e:ci:file -- e2e/betrayal/monster-runtime.e2e.ts`
- 本轮最终确认结果：`1 passed`

## 截图核对

- 截图时间：`2026/6/26 23:23:12-13`
- 持有区验收口径：玩家必须能直接识别自己持有什么牌。`绳索`、`手电筒` 必须显示正面裁片；`预兆书` 当前缺正面，只允许显示弱化牌背底纹 + 大号对象名 + `缺正面`，不得用统一牌背让玩家猜。
- 地图 token 验收口径：玩家和怪物必须落在房间 tile 上。玩家面板、怪物卡、右侧摘要只能辅助说明，不能替代地图上的玩家指示物或怪物 token。

### 01 玩家与怪物同场

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-monster-runtime\01-山屋惊魂-运行时-玩家与怪物同场.png`
- 实际看到：左侧持有区里 `绳索`、`手电筒` 已经是可识别正面裁片，不再是两张同类牌背；`预兆书` 显示为缺正面的诚实状态。
- 实际看到：两个怪物直接显示在对应房间牌中央，右侧不再用怪物摘要面板代替地图承载；我能直接指出狼人位于 `大楼梯`，幽灵位于 `逼仄的通道`。
- 实际看到：怪物 token 直接使用怪物自身素材本体，当前已经去掉额外底座、顶部短名签和重复名字标签，只保留红色描边语义。
- 实际看到：探索者也在房间牌上显示为同对象 token 素材，当前已经去掉 `你` 标签和重复名字文本；自己是绿色描边，队友为黑色描边。
- 实际看到：房间内承载关系已回到玩家左、怪物右；单边存在时居中，多对象沿竖向堆叠。
- 实际看到：队友仍在右侧功能按钮下方，底部动作条和右侧牌堆没有被怪物态挤歪。
- 实际看到：左下持有区里的 `绳索`、`手电筒` 保持竖向卡面比例，当前比早前被压成横短卡的版本更接近设计稿。
- 验收结论：玩家与怪物同场的运行时承载已回到地图 / 房间 tile，本轮不再以侧栏说明替代棋盘事实；当前截图已经足以支撑“对某个房间里的怪物使用物品/移动接近它”的读图描述。

### 02 使用物品后

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-monster-runtime\02-山屋惊魂-怪物同场-使用物品后.png`
- 实际看到：左侧持有区里的 `绳索` 已出现 `已用` 标记，说明这一步不是只改变按钮高亮，而是物品本身进入本回合已用状态。
- 实际看到：右上 `MOVE` 从 `3` 变成 `4`，和绳索带来的移动增益一致；底部 `使用` 动作变暗，下一步推荐动作切到 `移动`。
- 实际看到：狼人仍在 `大楼梯`，幽灵仍在 `逼仄的通道`，怪物没有因为使用物品后的状态刷新从地图上消失。
- 验收结论：同场怪物状态下，使用物品后的 UI 能同时保留玩家状态变化、物品使用状态和怪物所在房间。

### 03 移动选目标

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-monster-runtime\03-山屋惊魂-怪物同场-移动选目标.png`
- 实际看到：点击 `移动` 后，底部动作变成 `取消移动`，说明当前进入移动选目标状态。
- 实际看到：可达房间出现绿色目标点，至少能看清 `逼仄的通道` 与 `画廊` 两个可点目标；目标点没有被房间牌互相压住。
- 实际看到：狼人位于 `大楼梯`，幽灵位于 `逼仄的通道`，玩家能基于棋盘判断“要接近幽灵就点带目标点的逼仄的通道”。
- 验收结论：怪物同场下的移动目标选择可读，玩家可以从截图里直接判断下一步点击哪个房间。

### 04 移动后

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-monster-runtime\04-山屋惊魂-怪物同场-移动后.png`
- 实际看到：左侧当前位置已经从 `大楼梯` 变为 `二层平台`，右上 `MOVE` 从 `4` 回到 `3`，说明移动真实结算并消耗了移动值。
- 实际看到：当前玩家的绿色描边探索者 token 落在移动后的房间区域，不是只改了左侧文字。
- 实际看到：狼人仍在 `大楼梯`，幽灵仍在 `逼仄的通道`，移动后仍能说清两个怪物的位置。
- 实际看到：右侧队友仍在功能按钮下方，且出现“同房间”状态，说明移动后队友关系没有被动作条或怪物显示挤坏。
- 验收结论：怪物同场下的移动链路可用；当前截图能支撑“先使用绳索增加移动，再进入移动模式，点击目标房间完成位移，并继续知道怪物在哪”的玩家操作描述。

## 回归补充

- 本轮后续复跑已把 `betrayal-explorer-figure-token-*` 和 `betrayal-monster-board-token-*` 本体纳入 E2E 断言，不再只证明房间容器存在。
- 这轮截图验收口径已改为：token 必须优先保持素材原形态和原宽高比，靠描边语义区分自己 / 队友 / 怪物，而不是额外套一层通用壳。
