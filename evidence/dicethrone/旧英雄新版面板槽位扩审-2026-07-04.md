# DiceThrone 旧英雄新版面板槽位扩审

## 范围

- 问题对象：旧六角色升级牌在新版玩家面板上的覆盖槽位、点击槽位和高亮反查。
- 真相来源：`evidence/dicethrone/旧英雄新版面板槽位合同-2026-07-04.md` 的玩家板图面合同。
- 对照入口：各旧英雄 `cards.ts` 中所有 `type='upgrade'` 且执行 `replaceAbility(targetAbilityId)` 的升级牌。
- 消费链：`abilityOverlayHelpers.ts` 的升级牌覆盖槽位、`abilitySlotMapping.ts` 的点击/高亮反查槽位。

## 本轮结论

- 这次不是只看僧侣单点。已用运行时卡牌数据扫描旧六角色全部 `58` 条升级替换效果。
- 扩审结果：当前代码下没有继续命中新的旧六角色槽位错配；僧侣此前错配的 `大乘拳法 II -> combo` 与 `花开见佛 II -> lightning` 已归入全量合同测试。
- 新增组件合同测试会逐张升级牌断言：玩家板图面合同存在、升级牌覆盖槽位一致、点击/高亮反查槽位一致。

## 漏审复盘

- 漏审类型：真相源消费顺序错误 + 同类扩审不足。
- 具体原因：之前把代码里的基础技能名、旧共享槽位语义和底图物理槽位混用，没有强制逐张读取升级牌 `replaceAbility(targetAbilityId)` 后再和玩家板中文槽位标题对齐。
- 规范回代：`.spec/skills/dicethrone-hero-intake/SKILL.md` 已补充旧英雄 / 共享面板槽位迁移门禁，要求发现一张升级牌槽位错配时横扫同英雄、同批次和同一共享槽位消费链。

## 扩审对象

| 角色 | 升级替换效果数 | 当前结论 |
| --- | ---: | --- |
| 僧侣 | 10 | 已对齐合同 |
| 野蛮人 | 10 | 已对齐合同 |
| 火法 | 10 | 已对齐合同 |
| 月精灵 | 10 | 已对齐合同 |
| 暗影盗贼 | 8 | 已对齐合同 |
| 圣骑士 | 10 | 已对齐合同 |

## 后续保护

- 组件测试：`旧六角色全部升级牌应按玩家板图面合同覆盖同一物理槽位`。
- E2E：`老派系升级牌稳定态应与新派系一致：进入技能槽而不是留在弃牌堆` 已覆盖僧侣本次错配风险槽位。

## 2026-07-04 后续扩展说明

- 这份文档是旧六角色阶段的扩审记录，不再代表当前最终覆盖范围。
- 后续已升级为全英雄替换型升级牌合同测试：扫描 `HERO_CARDS_MAP` 全部英雄所有 `replaceAbility` 升级牌，当前验收门槛为 `108` 张。
- 当前单一运行时合同来源已收敛到 `abilitySlotMapping.ts`，`abilityOverlayHelpers.ts` 只查询升级牌并委托槽位合同，不再维护第二份完整槽位表。
- 最新全量证据见 `evidence/dicethrone/升级牌槽位全量回图审计-2026-07-04.md`。
