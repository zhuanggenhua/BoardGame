# 后台反馈一键复制改为 AI 诊断包的 E2E 证据

## 验证目标

验证后台反馈页点击“一键复制”后：

- 复制内容改为多段 AI 诊断包，而不是过度压缩的单行摘要
- 诊断包同时包含工单信息、客户端上下文、错误堆栈、操作日志原文、状态快照原文
- 原始状态快照仍可通过旁边 `JSON` 按钮单独复制

## 执行命令

```bash
npm run test:e2e:ci:file -- admin-feedback.e2e.ts "反馈页可展示分诊上下文并复制 AI 诊断包"
```

## 结果

- 结果：通过
- 断言覆盖：
  - 复制文本与剪贴板内容一致
  - 文本不以 `{` 开头，但明确包含多段标题和换行
  - 文本包含反馈 ID、游戏、反馈原文、截图索引、客户端上下文、错误上下文、错误堆栈
  - 文本包含操作日志摘要、操作日志原文、状态快照摘要、状态快照原文
  - 预览框 `textarea` 带 `wrap="off"`

## 截图证据

- 绝对路径：`D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\_shared\admin-feedback-ai-diagnostic-packet.png`

![后台反馈 AI 诊断包复制截图](../test-results/evidence-screenshots/_shared/admin-feedback-ai-diagnostic-packet.png)

## 人工观察

- 右侧详情面板的复制按钮已经进入绿色 `已复制` 状态，旁边独立 `JSON` 按钮仍然保留，说明现在是一键诊断包复制而不是替换掉原始 JSON 入口。
- 预览区标题已经变成 `AI 诊断包`，正文第一行清晰可见 `# AI 排障诊断包`，下一行就是面向 AI 的排障说明，不再是旧版一行压缩摘要。
- 预览正文里已经能肉眼看到 `## 1. 工单信息`、`- 反馈ID`、`- 时间` 等分段内容，说明复制文本默认就是结构化长文本，管理员不需要再手工补贴上下文。

## 结论

- 右侧详情面板顶部仍是“一键复制 + JSON”双按钮，但点击“一键复制”后，预览区已经显示为多段 `AI 排障诊断包`，而不是旧的一行压缩摘要。
- 诊断包正文可直接看到 `工单信息 / 证据索引 / 客户端上下文 / 错误上下文 / 操作日志原文 / 状态快照原文` 这些标题，说明复制内容已经具备完整证据链。
- 预览区高度明显比旧版更高，代码块区域可以直接滚动查看，说明现在默认就是面向定位 bug 的长文本，而不是需要管理员再手工补贴额外证据。
