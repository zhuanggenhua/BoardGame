# PR #121 冲突解决与有效改动提取记录

## 1. 背景

- PR：`#121`，补齐动作英雄、返时者、异形变体、青少年、怨灵捕手玩法
- 主线基线：`115a67d4c69bdf2c3b29e43ca10c30735028e0ee`
- PR head：`868f36aa8996a38d88ed6446ecd4daf2d69fd82e`
- 共同祖先：`553dbb4ecedd9ee49cd89626e786b48a0ec5a90a`
- 触发命令：`git merge --no-commit --no-ff 868f36aa8996a38d88ed6446ecd4daf2d69fd82e`

共同祖先之后，主线已有大量后续提交。主线侧的新增测试、文档、证据、资源和其他游戏实现不属于 PR 作者删除；PR head 相对共同祖先也没有真实文件删除候选。因此本次只从 PR head 提取其真实新增/有意修改，不按旧分支相对当前主线的差异整份覆盖。

## 2. 合并范围核对

- PR 相对共同祖先：28 个文件，约 `5509` 行新增、`1377` 行删除。
- PR 相对共同祖先的真实文件删除：0 个。
- 合并结果相对主线的文件删除：0 个。
- 主线相对共同祖先包含 458 个文件的后续漂移；这些内容保留在合并结果中，未作为 PR 删除处理。
- 主线已有的 Munchkin 测试、DiceThrone/召唤师战争测试、在线 AI 测试以及 PR #119/#120 审计证据均未被删除。

## 3. 冲突文件与逐块裁决

### `src/games/smashup/domain/commands.ts`

- 两个冲突块只是使用天赋校验目标参数的写法差异。
- 保留主线当前作用域已经解构出的 `targetBaseIndex`、`targetMinionUid`，没有退回 PR 旧版本的 `command.payload.*` 直接读取写法。
- PR 在该文件其他非冲突区域的有效玩法改动随合并保留；没有用旧分支整份文件覆盖主线。
- 这样处理避免把主线已经采用的当前命令契约回退，同时保留 PR 新增派系对天赋/能力校验的接入。

### `src/games/smashup/domain/events.ts`

- 保留主线在共同祖先之后新增的 Munchkin 事件音效定义和事件类型常量。
- PR head 对应区域没有新增内容，空冲突侧不构成删除意图。
- PR 新增派系使用的事件定义若位于非冲突区域，按 PR delta 保留。

### `src/games/smashup/domain/index.ts`

- 同时保留主线的 Munchkin 特殊卡描述符导入，以及 PR 新增的 `pregameDraft` 相关导入。
- 该文件采用双方导入块合并，未接受任何一侧的整份旧文件。
- 结果同时支持主线既有 Munchkin 运行时规则和 PR 的五派系预选阵营流程。

### `src/games/smashup/domain/reduce.ts`

- 保留主线在共同祖先之后新增的 Munchkin 事件类型导入和完整归约分支；PR head 对应区域没有这些事件实现，不能据此判断主线功能应删除。
- 在附着行动卡离场冲突处，保留主线的 `placeAttachedActionLeavingPlay` 处理，并保留“随从回手时指定附着行动卡随同回手”的当前语义。
- 未恢复 PR 旧版直接拼接个人弃牌堆的实现，因为它会绕过主线已有的离场处理和附着卡去重/归属逻辑。
- PR 在其他区域对五派系玩法的事件消费和状态归约改动按真实 delta 保留。

### `src/games/smashup/domain/types.ts`

- 保留主线的 Munchkin 事件联合类型和事件接口；这些内容是主线共同祖先之后新增的有效契约，PR head 没有对应实现，不能当成 PR 删除。
- 同时保留 PR head 相对共同祖先新增的类型内容以及主线已有的 `WraithrustlersHqBonusUpdatedEvent` 等类型。
- 未采用 PR 旧版 `types.ts` 整份文件，避免丢失主线新增事件契约。

### `src/games/smashup/domain/reducer.ts`（严格审计提示：结果等于父 2）

- 该文件没有产生文本冲突，但严格单边审计发现合并结果与 PR 父提交完全一致，因此单独核对了主线 delta，不能只以“无冲突”带过。
- 主线相对共同祖先的 17 行改动，是把 `targetBaseIndex` / `targetMinionUid` 从命令透传给能力执行上下文；PR 父提交在相同位置已经包含这组改动。父 1 与父 2 的文件差异中没有这两个参数的差异，说明这部分主线有效内容已等价存在于 PR 侧。
- PR 父提交相对共同祖先另外承载了本 PR 的有效功能：从牌库出牌、禁用/确认阵营命令、按预选结果创建牌组和基地，以及对应选秀流程。合并结果保留这些新增内容。
- 因此这里不是“整份接受旧分支文件”，而是双方对主线目标参数透传已一致、PR 另有新增职责时的等价单边结果。若判断错误，最可能丢失的行为是天赋/基地能力的第二步目标无法传给能力执行器；当前三方 diff 和定向测试已覆盖该透传链路。

## 4. PR 实际吸收内容

本次合并保留 PR 明确带来的：

- 动作英雄、返时者、异形变体、青少年、怨灵捕手五个派系的卡牌、能力和数据注册。
- 五派系预选阵营流程（`pregameDraft`）及参与池。
- 对应领域 reducer/command/ability 接线的有效代码块。
- 新增和更新的 Smash Up 单元测试、集成测试和 E2E 测试。
- 对应基地图、素材清单、OpenSpec 任务和 PR closeout 证据。
- 五派系基地交互标题、额外出牌选项和预选参与池分组的中英文翻译。

没有吸收的内容只有旧分支相对当前主线缺失的主线后续文件，以及冲突块中会回退主线现有契约或附着行动处理的旧代码。

## 5. 回归与行为变化登记

- 原 PR 目标问题：补齐上述五个派系的玩法、数据、预选流程和验证覆盖。
- 本次额外发现的真实回归：合并后定向测试在加载卡牌目录时发现 PR 新增的基地扩展池仍引用主线已经改名收口的 `NEW_ROUND_TABLE_KNIGHTS_BASES` / `NEW_GOBLINS_BASES`；已按当前真实导出改为 `ROUND_TABLE_KNIGHTS_BASES` / `GOBLINS_BASES`。继续验证时确认 PR 新增的牌库额外出牌测试仍按旧的“一次选牌即可完成”口径断言，而当前运行时要求玩家继续选择目标基地，即使只有一个合法基地也不自动确认；已补齐第二次目标响应，未修改领域行为。同时，冲突处理中发现 PR 旧版附着行动卡离场代码会绕过主线最新的离场处理；已保留主线处理，未引入该回退。
- 仅业务口径/规则变化：额外出牌交互保持当前项目规则：选牌和选目标是两个独立点击步骤，单目标也不自动跳过目标选择；本次只同步测试流程。预选阵营行为属于 PR 原目标范围。

## 6. 验证记录

- 冲突标记扫描：通过，无 `<<<<<<<` / `=======` / `>>>>>>>`。
- 合并结果删除检查：通过，无文件删除。
- 定向测试：通过，3 个测试文件、68 个测试全部通过。
- TypeScript：`npx tsc --noEmit --pretty false` 通过。
- 改动 TS/TSX 文件 ESLint：通过，0 errors；相对主线未新增 warning（基线与当前均为 78）。
- i18n 缺失 key：已补齐本 PR 新增的 29 个中英文 key；主线已有且未被本 PR 修改的 2 条 warning 已登记到 `scripts/verify/i18n-warning-baseline.json`，不改变运行时行为。
- 构建：`npm run build -- --minify false --configLoader bundle` 通过；产物清理按脚本完成。
- 全量 ESLint：默认堆大小运行时 OOM；提高 Node 堆上限后在 4 分钟内超时，均未输出 lint error。该资源限制已如实保留，不能将其表述为全量 ESLint 通过。
- 合并审计：普通审计 13 个混合结果、1 个等价单边结果；严格审计按预期因 `reducer.ts` 等于父 2 返回失败，单边原因已在上节单独记录。

## 7. 结果

- 当前状态：冲突已按三方差异解决，定向验证、TypeScript、构建、改动文件 ESLint 和 i18n 检查已通过；待 pre-push 门禁完成后推送收口。
- 合并提交：当前隔离 worktree 的合并提交（本次门禁修复一并 amend）。
- 推送目标：目标仓库 `main`（PR head fork 无写权限时按已确认 fallback 收口）。
