# 七大恨区域拓扑单一真相真实页面证据

## 本轮目标

- 验证当前运行时预览页直接读取单一真相工作区，而不是旧 formal 结果反向回灌。
- 验证 `喀喇沁部` 在运行时中保持独立区域，不再被 `土默特部` 吞并。
- 验证当前仍存在的共享正式印刷区只剩文档已确认的 3 组，不包含 `喀喇沁部`。

## 真实入口

- 页面入口：`http://127.0.0.1:4275/dev/qidahen-runtime-preview?workspace=runtime-single-truth-current`
- 工作区：`runtime-single-truth-current`
- 页面数据来源：当前仓库 `runtime-region-mask-regions.json` 与 `runtime-region-graph.json` 复制出的验收工作区

## 关键截图

- `D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\_shared\qidahen-区域拓扑-真实运行时预览-喀喇沁独立区域.png`

## 我实际看到什么

### 01 主地图命名区

- 地图上能同时看到 `土默特部` 和 `喀喇沁部` 两个独立标签，位置分别落在不同闭合区，不是同一块区域共用一个名字。
- 页面顶部统计显示 `中心 37 / 通路 81 / 缺中心 0 / 共享印刷区 3 / 缺权威指引 0`，说明运行时节点与通路已经完整生成，没有出现新的无中心脏区。

### 02 右侧正式共图块审计

- 右侧正式共图块审计只列出 3 组共享正式区：`敖汉部 -> 敖汉部 / 辽西`、`辽北 -> 辽北 / 辽东`、`顺天 -> 蓟镇 / 顺天`。
- 审计列表里没有 `喀喇沁部`，这直接说明它不再被算进共享正式区，也没有再并回 `土默特部`。

### 03 页面真实性

- 本轮先用真实图片查看工具打开了这张截图，再执行了 `npm run verify:open-image -- "<绝对路径>"`，脚本返回的 `RESOLVED_IMAGE` 和 `OPENED_IMAGE` 与上面的绝对路径一致。
- 该图来自真实运行中的开发页面，不是脚本后画的辅助图，也不是静态拼接图。

## 是否达到验收标准

- 达到 `3.3` 验收标准。
- 当前真实页面已证明：`喀喇沁部` 保持独立运行时区域，没有再被 `土默特部` 吞并；共享正式区只剩文档与映射中明确保留的 3 组。
