# 山屋惊魂首剧本 E2E 截图验收

## 命令

- `node scripts/infra/run-e2e-command.mjs ci e2e/betrayal/first-scenario.e2e.ts`
- 结果：`1 passed`

## 截图核对

### 01 第一剧本 haunt 运行时

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-first-scenario\01-山屋惊魂-第一剧本-haunt运行时.png`
- 实际看到：页面已经进入真实 `Haunt` 阶段，顶部主状态不再误写成“恶兆前”。
- 实际看到：当前玩家是 `杰登·琼斯`，剩余移动为 `4`，说明第一剧本已从恶兆前真实切进 haunt，而不是停留在旧代表态。
- 实际看到：中央房间簇、右侧牌堆/弃牌、底部动作、左侧属性和持有区都还在同一张真实运行时页面里，没有切到单独的“剧本结算页”或假页面。
- 验收结论：这张图证明“首个预兆后进入真实 haunt”已经在真实页面链路上成立；角色选择到恶兆前的前置仍由 `evidence/betrayal-basic-flow/` 负责。

### 02 玩家参考卡帮助面板

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-first-scenario\02-山屋惊魂-玩家参考卡-帮助面板.png`
- 实际看到：右侧小书本入口现在打开的是玩家参考卡，而不是一整块“剧本说明面板”。
- 实际看到：帮助面板确实来自真实运行时页面，顶部有翻面与收起，中央是官方参考卡素材，不是伪造 HTML 说明卡。
- 验收结论：帮助入口已经收成一个轻量图标入口，符合“帮助只做小入口，不在主 UI 常驻解释”的方向。

### 03 运行时当前牌桌

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-first-scenario\03-山屋惊魂-运行时-当前牌桌.png`
- 对应设计稿：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\docs\games\betrayal\design\generated\betrayal-runtime-prehaunt-board-v4.png`
- 实际看到：运行时已回到 `v4` 五区关系：左侧探索者与持有物、中央房间主视区、右侧牌堆/弃牌、底部五个动作、顶部短状态都存在。
- 实际看到：左侧属性区现在带有 `当前属性` 标头和当前玩家名签，房间里的当前玩家 token 也带 `你` 标记；不用来回猜“左侧是谁、棋盘上哪个是我”。
- 实际看到：右上角旧的齿轮入口已经去掉；通用悬浮球仍保留在真实截图里，没有再被测试样式偷偷隐藏。
- 实际看到：顶部旧齿轮入口删除后，原来的右侧空列也已经释放，运行时 header 没有再保留设置入口占位。
- 实际看到：右侧队友已经回到功能按钮下方，不再掉到底部动作条下面。
- 实际看到：持有区已改成 `物品 / 预兆` 直接分组；这张图里 3 张物品完整可见，预兆区进入内部滚动承载，不再额外重复一张选中大卡挤压首屏。
- 实际看到：持有区不再是“整排牌背”；当前能看到 `手电筒` 和 `狩猎短刀` 的真实物品正面裁片。`绳索` 与 `预兆书` 当前没有已确认正面素材，运行时显示真实牌背 + 名称，没有再用 marker 或错误图集冒充正面。
- 实际看到：玩家位置已经落在房间牌上，用探索者自身真实肖像做棋子化承载；当前仓库尚未找到独立官方 figure 素材，所以这属于同对象真实素材代用，不是已接入官方独立棋子。
- 实际看到：如果让我基于图描述操作，我可以直接说“当前玩家在大楼梯；要移动就先点底部 `移动`，再点棋盘上出现目标标记的可达房间；要使用物品就先在左侧持有区选物品，再点底部 `使用`；帮助入口是右侧小书本，不是常驻说明卡。”
- 实际看到：旧截图里的“可点击预演 / 当前目标 / 下一步 / 大块刚发生面板”已经不再常驻在主 UI。
- 实际看到：中央主视区现在是三行房间簇，不再把房间牌压成一条细竖列；首屏能同时看到上层、一层、地下的关键房间关系。
- 实际看到：房间牌之间不再上下互相压住，底部房间没有被行动栏裁掉。
- 实际看到：房间牌面不再重复铺可见房间名、楼层说明和身份标签，主牌面只保留真实素材和最小悬浮操作入口。
- 实际看到：每个房间都有单独预览入口，右侧也保留了帮助卡与当前房间入口，其中当前房间入口已经从放大镜换成房屋图标，不再和底部探索动作混淆。
- 实际看到：属性条的低值区现在只保留一个红色致命位，不再出现“双红点”。
- 实际看到：中央地图仍可拖拽/滚动，作为后续房间数扩大时的承载方式。
- 素材修正：运行时房间牌已改为从原始房间图集裁剪显示，不再使用 `contact-11-1425-1426.jpg` 低清索引图裁片；本轮截图已重新核对，房间清晰度恢复，未见旧截图里的异常黄色上边框。
- 连接语义修正：房间之间不再额外绘制黄色/绿色“门块”覆盖层；连接关系只由房间素材自身的门缝与相邻关系表达，避免重复造门。
- 验收结论：运行时流程通过，当前首屏已经比上一版更适合阅读和操作；这轮收住了悬浮球、设置入口、持有区分组和属性危险位这几处回归点，但地图簇与左侧密度仍可继续向 `v4` 收敛。

### 04 终局

- 路径：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\evidence\betrayal-first-scenario\04-山屋惊魂-终局-幸存者胜利.png`
- 对应设计稿：`D:\gongzuo\webgame\BoardGame\.worktrees\betrayal\docs\games\betrayal\design\generated\betrayal-endgame-style-b.png`
- 实际看到：终局页显示剧本结果为胜利，幸存者列表、叛徒败退、中央剧本卡和统计区都可见。
- 实际看到：幸存者和叛徒条目使用完整探索者素材，没有裁成圆形头像。
- 实际看到：左侧幸存者条目和右侧叛徒条目现在都有了更完整的横条结果板边界，不再只是松散贴在两侧的实现列表。
- 实际看到：终局里的目标、结果、奖励、叛徒败退和统计图标已经换成探索者牌、叛徒牌背、房间牌、预兆/事件牌、参考卡等真实素材，不再使用 emoji 假图标。
- 实际看到：中央结果区已改成“深色外框 + 旧纸内板”的双层结构，纸面不再是单层浅色块，分栏线和顶部短装饰也开始接近设计稿语法。
- 实际看到：本轮重新压回了终局整体高度，`重赛 / 大厅 / 日志` 三个底部动作按钮已经能在 `1600x900` 截图里完整显示，不再被底边裁掉。
- 实际看到：这张终局图能明确告诉玩家“第一剧本已经结束，幸存者胜利，接下来可重赛 / 回大厅 / 看日志”，不存在不知道下一步去哪的问题。
- 实际看到：左右两侧分区仍然保留结果板语法，但这轮已经去掉“整区大盒子”的感觉，更多靠条目铭牌、纸面和统计板自身承重。
- 实际看到：左侧幸存者条目已经从普通实现列表收成更接近结果板条目的黑底条带，角色名、属性珠和生还徽记比上一版更像设计稿里的记录牌。
- 实际看到：本轮又把幸存者、叛徒和统计区的边框厚度补了一笔，右下统计现在也更像挂在同一张结果板上的记录区，不再只是三列轻数字。
- 实际看到：本轮正式通过前，这条测试曾多次命中“页面没有正常显示”的保护层；现在 `betrayal` 专用 helper 会先刷新重试并重新等 harness，再继续状态注入，最终成功落到真实终局页。
- 实际看到：终局截图继续直接截真实 `betrayal-endgame-screen` 主容器，规避了整页截图在 Chromium 上的崩溃点，同时没有改成替代页面或假容器。
- 与设计稿差距：中央纸面的旧化纹理和印章层次已经更清楚，但左右分区的铜边装饰、幸存者/叛徒条目的精致度和顶部横幅的厚重感仍比目标稿更克制，尚未完全贴齐。
- 验收结论：首剧本终局流程可用，颜色方向已回正；视觉仍是继续收敛中的实现稿，不能说已完全对齐终局设计稿。

## 备注

- 四张截图均来自 `/play/betrayal` 真实页面链路和 Playwright 状态注入，不是静态 HTML、组件孤立截图或生图替代品。
- 本文件只记录第一剧本分段 E2E；角色选择到运行时的基本流程已拆到 `evidence/betrayal-basic-flow/`，本用例不重复执行前置。
- 本轮在持有物放大层修正后又重新跑过第一剧本 E2E，并复看了 `03-山屋惊魂-运行时-当前牌桌.png` 与 `04-山屋惊魂-终局-幸存者胜利.png`；运行时和终局没有被该改动带歪。
- 本文件当前截图已覆盖终局中央结果纸面的加厚装裱、旧纸纹理、内框和分栏线增强；这张图仍未达到设计稿完全高保真，但不再处于“待复验”状态。
- 本轮截图已经通过 `view_image` 肉眼核对；结论是“第一剧本已经能从真实 haunt 运行时进入幸存者终局，运行时素材已回到原始图集，房间簇可读性过关，但三屏仍未全部达到设计稿高保真验收”。
- 当前剩余视觉缺口：终局中央纸面还要继续收旧纸层次与边缘语法；运行时虽然已经把队友压回右侧按钮下方、释放设置入口空列、让持有区按 `物品 / 预兆` 直接分组，并把玩家/怪物落回地图房间承载，但整体分区重量和地图房间簇仍可继续向 `v4` 设计稿收敛。
