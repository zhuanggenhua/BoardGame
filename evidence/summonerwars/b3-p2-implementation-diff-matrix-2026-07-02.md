# 召唤师战争 B3 P2 实现对照首轮矩阵（2026-07-02）

## 目的

- 承接 `b3-p2-rule-text-lock-matrix-2026-07-02.md` 的 12 个 `locked-规则原文已锁` 对象。
- 本文件只做“官方合同子句 → 当前实现入口 → 现有证据/缺口”的首轮分流。
- 本轮不回图片/OCR、不重新录入规则；若发现合同缺字段、对象归属冲突或官方原文不够支撑实现判断，再把对应对象降级为 `blocked` 或 `disputed`。

## 续接门禁

- `locked` 后继续默认进入实现对照、测试补证、真实入口证据或 L3/L4 补证。
- 不得因为实现对照还没做完，就回到图片/OCR 层重复确认已锁合同。
- 数据录入质量仍是前提：若实现对照暴露合同缺字段、来源冲突或对象归属不清，先回写合同状态，再补录入合同；不得在实现审计中临时猜规则。

## 首轮分流矩阵

| 对象 | 中文承载卡 | 官方合同要点 | 当前实现入口 | 现有测试/证据 | 首轮分流 | 后续动作 |
| --- | --- | --- | --- | --- | --- | --- |
| `feed_beast` | 巨食兽 | 攻击阶段结束；本回合未摧毁单位；强制弃相邻友方或自弃 | `abilities-goblin.ts:165` 定义 `onPhaseEnd` + `usesPerTurn: 1` + `feed_beast_check`；`abilityHelpers.ts:145` 按击杀记录拦截；`systems.ts:1513` 建二选一交互；`executors/goblin.ts:63` 结算自弃或弃相邻友方 | `abilities-phase-triggered.test.ts` 覆盖吃友方、自弃、已击杀不触发、自动推进；`interaction-chain-comprehensive.test.ts` 覆盖自弃、吞噬相邻友方、缺目标负向，并新增阶段结束真实 halt、吞噬相邻友方收口、重复响应不二次弃置、再次推进阶段进入 magic 且不残留交互断言；目标测试 `feed_beast\|喂养野兽` 通过，4 passed / 113 skipped | `match-with-L4-proof` | 已完成 L4：攻击阶段结束真实入口、重复响应和阶段推进收口均已补证；暂不改机制 |
| `revive_undead` | 雷塔勒斯 | 每回合一次；召唤阶段；可选；加 2 伤害；从己方弃牌堆取亡灵单位并放在相邻格 | `abilities.ts:312` 定义；`abilityHelpers.ts:139` 要求弃牌堆存在亡灵；`systems.ts:648` 建选弃牌堆亡灵单位交互，`systems.ts:3119` 建相邻空格选择；`abilities.ts:367` 改用完整 `isCellEmpty` 校验单位/建筑占格；`executors/necromancer.ts:13` 先防御校验亡灵、相邻、空格，再自伤 2 并从弃牌堆召唤 | 新增 `abilities-necromancer-execute.test.ts` 执行器防御断言：非亡灵、非相邻、非空格均不自伤、不召唤；新增 `interaction-chain-comprehensive.test.ts` 验证层断言：非亡灵、非相邻、有单位或建筑占格均拒绝；本轮新增真实交互链 L4 断言，覆盖发动技能后选弃牌堆亡灵、选相邻空格、自伤 2、从弃牌堆召唤落位、弃牌堆移除和重复响应不二次执行；目标测试均通过 | `fixed-with-L4-proof` | 已最小修复并补齐 L4：执行器不再先自伤后查目标，验证层不再把有建筑格误判为空；真实交互链目标测试 `revive_undead\|复活亡灵` 通过 7 passed / 115 skipped，相邻执行器回归通过 5 passed / 17 skipped |
| `ancestral_bond` | 阿布亚·石 | 移动后；可选；3 格内友方单位；目标 +1 充能；自身所有充能移给目标 | `abilities-barbaric.ts:32` 定义；`systems.ts:1850` 建目标交互；`executors/barbaric.ts:21` 目标 +1，并把自身全部充能转移给目标 | `abilities-barbaric.test.ts` 覆盖主体流程；`interaction-chain-comprehensive.test.ts` 覆盖完整 payload、超距、选择自己负向；本轮新增移动后真实入口断言，覆盖移动后生成可跳过目标选择、只列入 3 格内友方目标、确认后目标 +1 并接收自身全部充能、重复响应不二次执行 | `match-with-L4-proof` | 已完成 L4：移动后真实入口和重复响应补证通过；未改机制实现 |
| `vanish` | 思尼克斯 | 每回合一次；攻击阶段；可选；与友方 0 费用单位交换位置 | `abilities-goblin.ts:29` 定义 `usesPerTurn: 1`；校验攻击阶段、友方、0 费用、不能自己；`systems.ts` 在未带目标时生成 `activated_ability_target` 选目标交互；`executors/goblin.ts:13` 发交换事件 | `abilities-goblin.test.ts` 覆盖基础流程；`interaction-chain-comprehensive.test.ts` 覆盖 0 费友方交换、非 0 费、敌方、非攻击阶段负向；本轮新增真实入口断言，覆盖无目标发动后只列入友方 0 费用单位、排除非 0 费友方和敌方 0 费用单位、确认后交换位置、交互收口、重复响应不二次交换、同回合二次使用拒绝；目标测试 `vanish\|狡黠\|神出鬼没` 通过，5 passed / 125 skipped | `match-with-L4-proof` | 已完成 L4：真实入口、目标过滤、交换落位、重复响应和同回合次数门禁均已补证；未改机制实现 |
| `blood_rune` | 布拉夫 | 攻击阶段开始；强制二选一：花 1 魔力充能，或给本单位 1 伤害 | `abilities-goblin.ts:93` 定义 `onPhaseStart` + `blood_rune_choice`；校验 charge 路径需 1 魔力；`systems.ts:1633` 阶段开始创建 damage/charge 选择并通过动态选项生成器在交互弹出时按最新魔力重算；`systems.ts:2994` 响应后执行 `blood_rune`；`executors/goblin.ts:36` 结算伤害或充能 | `abilities-phase-triggered.test.ts` / `abilities-goblin.test.ts` 覆盖主体；`interaction-chain-comprehensive.test.ts` 覆盖魔力不足 charge 拒绝、damage 路径可用、进入攻击阶段创建强制二选一、无魔力时只保留自伤并自动结算；本轮新增多个布拉夫同一攻击阶段开始时共享魔力顺序消费断言，确认第一个花唯一魔力充能后第二个按最新魔力状态自动自伤 | `fixed-with-L4-proof` | 已最小修复：血符文入队交互不再保留旧魔力选项，弹出时重算；目标测试 `blood_rune\|血符文` 通过 5 passed / 116 skipped，相邻哥布林执行器回归通过 3 passed / 48 skipped，阶段自动能力组合回归通过 10 passed / 111 skipped |
| `frost_axe` | 寒冰锻造师 | 移动后；可选；自充能或花 1 充能附加到 3 格内友方士兵；被附加士兵攻击时特殊面等于两个普通命中 | `abilities-frost.ts:239` 定义；`systems.ts:3098` 承接选择；`executors/frost.ts:98` 自充能或花 1 充能并附加到友方士兵；`execute.ts:517` 在被附加士兵攻击时把 special 按 2 个命中计入 | 新增多充能 attach 路径断言，证明 2 充能时只消耗 1 个；本轮新增移动后真实入口到攻击消费的 L4 断言，覆盖移动后交互附加、只消耗 1 充能、寒冰锻造师从棋盘移除并附加到友方士兵、被附加士兵攻击时 special 按 2 个命中结算；`interaction-chain-comprehensive.test.ts` frost_axe 目标测试通过 10 passed / 114 skipped；`abilities-frost.test.ts` 目标回归通过 1 passed / 36 skipped | `fixed-with-L4-proof` | 已最小修复并补齐 L4：充能消耗已为 `delta: -1`，真实移动附加到攻击消费链路已补证 |
| `grab` | 部落抓附手 | 友方单位从与本单位相邻处开始移动；移动后可把本单位放到该友方单位相邻格 | `abilities-goblin.ts:290` 定义；`execute.ts` 在友方单位从抓附手相邻格开始移动后发 `GRAB_FOLLOW_REQUESTED`；`systems.ts` 将请求转成 `grab_follow` 位置选择交互，并提供跳过；`executors/goblin.ts:126` 验证目标空格并移动抓附手 | `abilities-goblin.test.ts` 覆盖相邻开始移动触发、非相邻不触发、跟随、被占据拒绝；`abilities-goblin-execute.test.ts` 覆盖执行路径；本轮新增 `interaction-chain-comprehensive.test.ts` 真实入口断言，覆盖友方从相邻处移动后生成跟随选择、确认后只移动一次、交互收口、重复响应不二次移动；目标测试 `grab\|抓附` 通过，1 passed / 128 skipped | `match-with-L4-proof` | 已完成 L4：移动后真实交互入口、跟随落位、重复响应拒绝均已补证；未改机制实现 |
| `healing` | 圣殿牧师 | 攻击友方士兵或英雄前；可弃 1 手牌；若支付，本次攻击不加伤害，按普通命中和特殊面治疗 | `abilities-paladin.ts:173` 定义 `beforeAttack`；校验手牌、友方士兵/英雄；`systems.ts:624` 创建攻击前选牌/跳过交互；`executors/paladin.ts:81` 弃牌并设置治疗模式；`execute.ts:421` 治疗模式独立结算友方治疗；`reduce.ts:200` 在攻击后清理治疗模式 | `abilities-paladin-new.test.ts` 覆盖弃牌进入治疗模式、攻击携带 beforeAttack、治疗替代伤害、攻击后清理、目标限制；本轮新增真实攻击前交互 L4 断言，覆盖选牌弃牌、治疗模式只作用本次攻击、治疗落地、攻击后清理、重复响应不二次治疗/弃牌 | `match-with-L4-proof` | 已完成 L4：真实攻击前选牌、治疗结算、攻击后清理和重复响应补证通过；未改机制实现 |
| `holy_arrow` | 城塞弓箭手 | 攻击前；展示并弃任意数量互不相同单位牌；每张 +1 魔力，本次攻击 +1 战力 | `abilities-paladin.ts:278` 定义 `beforeAttack`；校验单位牌、不同名、不能弃同名自身；`systems.ts:576` 创建攻击前多选手牌交互并按卡名去重；`execute.ts` 的攻击前分支加魔力、弃牌并通过 `beforeAttackBonus` 只影响本次攻击；`executors/paladin.ts:50` 的直接激活路径已移除永久充能写入 | `abilities-paladin-execute.test.ts` 已补最小断言：弃牌不产生 `UNIT_CHARGED`、不残留 `boosts`、攻击骰数只按本次攻击加成计算；本轮新增真实攻击前多选 L4 断言，覆盖同名候选去重、排除事件卡/同名自身、确认后只弃已选不同名单位牌、魔力 +2、攻击骰数本次 +2、不写永久充能、重复响应不二次弃牌/加成；同步修正 `abilities-paladin.test.ts` 旧断言为“不留下永久战力”；目标测试均通过 | `fixed-with-L4-proof` | 已最小修复并补齐 L4：圣光箭只保留本次攻击临时加成；真实多选交互边界和旧测试口径均已对齐 |
| `illusion` | 心灵巫女 | 移动阶段开始；可选；3 格内士兵；复制目标能力直到本回合结束 | `abilities-trickster.ts:462` 定义 `onPhaseStart`；校验士兵、3 格内、非自身；`systems.ts:1669` 在移动阶段开始生成可跳过士兵目标选择；`executors/trickster.ts:65` 复制目标当前有效能力；`reduce.ts:262` 回合切换清理 `tempAbilities` | `abilities-trickster-execute.test.ts` 覆盖复制、士兵/距离/阶段限制、回合切换清理和去重；`interaction-chain-comprehensive.test.ts` 已补移动阶段开始真实交互、目标过滤、确认后复制能力、交互收口和重复响应不二次复制；目标测试通过 9 passed / 123 skipped | `match-with-L4-proof` | 已补 L4：阶段开始真实入口、可跳过士兵目标选择、确认后复制能力、重复响应拒绝均有目标测试证据；未改机制实现 |
| `structure_shift` | 斯瓦拉 | 移动后；可选；3 格内友方建筑；Force 目标 1 格 | `abilities-frost.ts:64` 定义；`systems.ts:1887` 选建筑并二步选位置；`systems.ts:3044` 选择相邻空格；`executors/frost.ts:23` 验证友方建筑/活体结构、3 格、相邻空格后推移；`helpers.ts` 已记录 Force 可上/下/左/右选择且普通 Force 被单位和建筑阻挡 | `abilities-frost.test.ts`、`interaction-chain-comprehensive.test.ts` 覆盖完整 payload、缺位置、非移动阶段、非友方建筑、超距、非当前玩家、活体结构正负；本轮新增移动后两步真实入口 L4 断言，覆盖移动后生成可跳过目标选择、只列入 3 格内友方建筑、排除远处友方建筑和敌方建筑，二步只列入相邻空格并排除占用格，确认后建筑移动、`UNIT_PUSHED` 仅 1 次、重复响应不二次移动；同步修正旧测试中“移动后能力应消耗攻击行动”的过期期望；目标测试通过 7 passed / 120 skipped | `match-with-L4-proof` | 已完成 L4：真实移动后两步入口、Force 1 相邻空格候选、建筑移动落地和重复响应补证；未改机制实现 |
| `spirit_bond` | 祖灵法师 | 移动后；二选一：自身 +1 充能，或花 1 充能给 3 格内友方单位 +1 充能；卡面未写 may | `abilities-barbaric.ts:318` 定义 `usesPerTurn: 1`；`systems.ts:1805` 建 self/transfer 强制选择，已移除无合同依据的 skip；`executors/barbaric.ts:156` 自充或花 1 给目标充能 | `abilities-barbaric.test.ts` 覆盖自充、转移、同回合二次拒绝、无充能/超距负向；`interaction-chain-comprehensive.test.ts` 已补移动后强制二选一且不提供跳过、真实入口转移路径、交互收口和重复响应不二次转移；目标测试通过 9 passed / 123 skipped | `fixed-with-L4-proof` | 已最小修复并补齐 L4：祖灵交流移动后不提供跳过，真实移动后入口能结算 transfer，重复响应拒绝且不二次转移 |

## 首轮结论

- B3 已按 `locked` 合同进入实现对照，没有回到图片/OCR。
- `frost_axe` 已从 `suspected-gap` 推进到 `fixed-with-L4-proof`：最小失败测试确认当前实现会清空全部充能，修复后目标测试通过；本轮追加移动后真实交互附加到攻击消费的 L4 断言，确认 special 按 2 个命中结算。
- `holy_arrow` 已从 `proof-needed` 推进到 `fixed-with-L4-proof`：最小失败测试确认旧实现会写入永久充能并存在双算/残留风险，修复后目标测试通过；本轮追加真实攻击前多选交互 L4 断言，并修正旧圣骑士测试中“圣光箭写永久战力”的过期期望。
- `illusion` 已从 `proof-needed` 推进到 `match-with-L4-proof`：补齐移动阶段开始真实交互、目标过滤、确认复制和重复响应拒绝证据，执行/清理链路已有目标测试覆盖。
- `spirit_bond` 已从 `proof-needed` 推进到 `fixed-with-L4-proof`：最小断言确认移动后交互不应提供跳过，并补齐真实入口转移路径、收口和重复响应不二次转移证据。
- B3 本批 12 个对象首轮实现对照已完成：`blood_rune`、`revive_undead`、`frost_axe`、`holy_arrow`、`spirit_bond` 为 `fixed-with-L4-proof`；`feed_beast`、`ancestral_bond`、`grab`、`healing`、`illusion`、`structure_shift`、`vanish` 已推进到 `match-with-L4-proof`。
- `feed_beast` 已完成阶段结束真实入口 L4 补证：强制二选一交互、吞噬相邻友方、重复响应拒绝、阶段继续推进均有目标测试证据；`ancestral_bond` 已完成移动后真实入口 L4 补证：移动后目标交互、跳过选项、3 格内友方目标过滤、充能转移落地和重复响应拒绝均有目标测试证据；`grab` 已完成移动后真实入口 L4 补证：友方从相邻处移动后生成跟随选择、确认后跟随落位、交互收口和重复响应拒绝均有目标测试证据；`healing` 已完成攻击前真实选牌到治疗后清理 L4 补证：选牌弃牌、治疗落地、攻击后清理和重复响应拒绝均有目标测试证据；`structure_shift` 已完成移动后两步真实入口 L4 补证：友方建筑目标过滤、Force 1 相邻空格候选、占用格排除、建筑移动落地和重复响应拒绝均有目标测试证据；`vanish` 已完成攻击阶段真实入口 L4 补证：目标过滤、交换落位、重复响应拒绝和同回合次数门禁均有目标测试证据。
