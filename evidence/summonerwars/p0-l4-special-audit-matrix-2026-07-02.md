# 召唤师战争 P0 L4 专项补审矩阵（2026-07-02）

## 目的

- 承接“所有漏审都全面补审”：P0 四个 `locked` 对象虽然已补真实在线入口，但不能因此冒充 L4 全面通过。
- 本文件只记录 L4 专项缺口：乐观确认、重连、重复事件回放、队列/交互恢复。
- P0 不回录入层；真相源沿用已锁定卡图/子句合同。

## P0 L4 对象矩阵

| 对象 | 中文名 | 当前已证等级 | L4 仍缺什么 | 最小补审入口 | 禁止误判 |
| --- | --- | --- | --- | --- | --- |
| `intimidate` | 雌狮「威势」 | L4 重连/回放已补：攻击敌方单位后 1 个充能点，刷新后仍为 1 个充能点，UI 不重复显示 | 已补：服务端确认后刷新/重连不重放历史充能事件；状态仍为 `{ boosts: 1, attackCount: 1, hasAttacked: true }` | 已通过在线 E2E：攻击后刷新同一房间，核对单位充能点和 UI 充能点仍只显示一次 | 可作为雌狮对象级 L4 证据；不能外推其它威势对象 |
| `imposing` | 贾穆德「威势」 | L4 重连/回放已补：攻击敌方单位后 1 个充能点，刷新后仍为 1 个充能点，UI 不重复显示 | 已补：服务端确认后刷新/重连不重放历史充能事件；状态仍为 `{ boosts: 1, attackCount: 1, hasAttacked: true }` | 已通过在线 E2E：攻击后刷新同一房间，核对单位充能点和 UI 充能点仍只显示一次 | 可作为贾穆德对象级 L4 证据；不能只用雌狮证据代表贾穆德 |
| `fortress_power` | 瑟拉·艾德温「城塞之力」 | L4 重连/回放已补：选牌入口可见；未完成选择刷新后仍恢复同一选牌入口；完成选择后刷新不重复回手 | 已补：刷新/重连后选牌入口恢复；完成选择后刷新不重复创建选牌入口、不重复把同一张城塞单位拿回手牌 | 已通过在线 E2E：触发选牌入口后刷新同一房间，选择弃牌堆城塞单位回手后再次刷新验证手牌/弃牌堆不重复变化 | 可作为「城塞之力」对象级 L4 证据；不外推其它 custom/后续选择对象 |
| `judgment` | 城塞圣武士「裁决」 | L4 重连/回放已补：3 个特殊符号抓 3 张牌，刷新后仍只抓 3 张 | 已补：服务端事件回放后不重复抓牌；刷新/重连后手牌不再 +3；抓牌事件仍为 1 条 | 已通过在线 E2E：攻击后刷新同一房间，核对手牌、牌库、抓牌事件只变化一次 | 可作为「裁决」L4 已补证据；不能外推其它 P0 对象 |

## L4 验收口径

- 必须回到真实在线房间或等价在线状态恢复路径。
- 每个对象至少证明一个“不重复结算”的负向场景。
- 涉及交互的对象还要证明 pending/interaction 恢复或关闭正确。
- 截图或状态证据必须来自当前 worktree 的真实运行位点。

## 下一步

1. 「裁决」已补 L4 重连/回放断言。
2. 「威势」族已补 L4 重连/回放断言；雌狮和贾穆德各自保留对象级证据，不互相代替。
3. P0 四个 locked 对象的 L4 重连/回放专项均已补；下一步转入 P1/P2/P3/P4 的逐字规则锁定队列。

