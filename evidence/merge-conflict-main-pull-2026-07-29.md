# main pull 双边内容吸收记录（2026-07-29）

## 触发背景

- 本地提交：`0d2f44eba`（扩展山屋惊魂剧本、The Gang 规则重启与 AI 反馈）
- 远端基线：`d2d85fd0372a6cdd9681c1c83b276891e55e995a`
- merge commit：`bdfc90a93221b0a8b30e344904466b58739d9528`
- pre-push 门禁识别 `.gitignore` 为双侧重叠改动文件，要求补充吸收说明。

## 文件裁定

| 文件 | 本地侧有效内容 | 远端侧有效内容 | 最终裁定 |
| --- | --- | --- | --- |
| `.gitignore` | 根目录 HTTP / 临时下载探针产物忽略规则：`/temp-*.bin`、`/temp-*.html`、`/temp-*.json`、`/temp-*.txt` | Smash Up 新批次资源随仓提交白名单：波利尼西亚航海家、新圆桌骑士、新哥布林等卡图 / 基地图路径 | 两边都保留；本地规则只约束根目录临时文件，不影响远端 Smash Up 正式资源白名单 |

## 验证口径

- `.gitignore` 当前同时包含 Smash Up 正式资源白名单和根目录 `temp-*` 临时文件忽略规则。
- 这不是单边覆盖；远端资源白名单用于允许正式素材入库，本地忽略规则用于防止临时 HTTP 探针文件误入库，两者职责不同且不冲突。
- 后续 push 门禁应能通过 merge conflict guard 的 evidence 检查。
