# Task Plan: SmashUp yuanhou effect atom / shared seam completion audit（2026-05-21）

> 当前 worktree `D:\gongzuo\webgame\BoardGame\.worktrees\smashup-yuanhou-factions` 的正式计划入口。下方旧计划均为历史上下文，不作为本轮任务入口。

## Goal

沿 `C:\Users\zhuagenbao\.codex\.omx+\state\long-term-task\smashup-yuanhou-faction-effect-audit-2026-05-16.json` 的 `last_verified / next_actions` 持续推进 SmashUp yuanhou 四派系 effect atom 全链路语义重审与 shared seam completion audit；当前主线已收束到 `borrowed ongoing / sourceController queued runtime / owner-source provenance`，优先筛 `metadata.sourceControllerId` 已存在但 runtime consumer / modifier / restriction / base-total-power 仍只读 `ownerId` 的最小真红灯，避免再被活跃 `goal` 的旧 objective 或本文件旧 transport 标题拉回已闭环的 `The Spy Who Ditched Me / waiting overlay` 残项。

## Constraints

- 活跃 `goal.objective` 当前仍是旧文案，但工具不支持原地改写 objective；执行锚点一律以长期状态 JSON 的 `last_verified / next_actions` 为准。
- 不回扫已闭环对象级 family：`Spy / Secret Agent / The Spy Who Ditched Me / Portal Room / Time Box / PromptOverlay / MeFirstOverlay`，也不再把旧 transport / playerView 顶层标题当当前主线。
- 每轮必须同步回写 evidence 与长期状态，但不得把 scoped direct gate 误写成整条 runtime 已完成。
- 当前 worktree 很脏，只允许触碰本轮 direct gate、evidence 与状态文件。

## Acceptance Checklist

- [x] S0 修正当前 worktree 的计划入口，使其与长期状态 JSON 的 operational target 对齐。
- [ ] S1 继续沿 `borrowed ongoing / sourceController queued runtime / owner-source provenance` 查找仍只读 `ownerId` 的 runtime consumer / modifier / restriction 最小红灯或旁路非对称。
- [x] S2 对新增缺口补最小实现或测试，并跑聚焦验证命令。
- [x] S3 回写 `evidence/smashup/smashup-in-progress-effect-atom-audit-2026-05-15.md` 与长期状态 JSON，明确已验证事实和下一格。
- [ ] S4 保持“有真实进展但不误报总完成”的收口口径。

## Current Status

- 2026-05-25 最新已验证：Skeletons 又命中两条 `trueOwnerId: playerId` sibling 真红灯，并已从 focused 红灯转绿。第一条是 [`skeletons.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/skeletons.ts>) 的 `handleSkeletonsReturnedOne()`：P0 打出 owner=1 的 borrowed `skeletons_returned_one` 并选择自埋时，buried card 的 `trueOwnerId` 被写成控制者 `0`。第二条是同文件 `buildDiscardBuryEvents()`：P0 弃牌堆中 owner=1 的 borrowed minion 经 `skeletons_place_em_down` 埋葬后，`trueOwnerId` 同样被写成 `0`。现已最小修复为：`Returned One` 按 `buriedFrom` 从 live hand/discard/play 回查真实 owner，其中 play 来源扫描 base minions / base ongoing / attached actions；Skeletons 共用 discard-bury helper 从当前玩家弃牌堆 live card 回查 owner。验证：`pnpm vitest run src/games/smashup/__tests__/newFactionAbilities.test.ts -t "skeletons_returned_one 自埋 borrowed"` => 首轮 failed，修复后 `1 file passed, 1 passed`；`-t "skeletons_place_em_down 埋葬 borrowed|skeletons_returned_one 自埋 borrowed"` => `1 file passed, 2 passed`；邻近 `-t "skeletons_returned_one 可把自己埋葬|skeletons_returned_one 自埋 borrowed|skeletons_returned_one 被挖掘|skeletons_place_em_down 从弃牌堆|skeletons_place_em_down 埋葬 borrowed|skeletons_spooky_scary|skeletons_revenant"` => `1 file passed, 8 passed`；`npx eslint src/games/smashup/abilities/skeletons.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` => 0 errors，40 warnings（既有 unused/any 噪音）。该格只闭合 Skeletons Returned One 自埋 borrowed play-source 与 Skeletons discard-bury helper 的 true owner provenance，不外推 Titans Sphinx、所有 bury caller、所有 discard/deck 来源或整个长期任务完成。
- 2026-05-25 最新已验证：`base_ossuary` 又命中一条新的 `borrowed discard-bury trueOwner provenance` 真红灯，并已从 focused 红灯转绿。按 next_actions 离开 Ancient Egyptians 后，扫描仍直接写 `trueOwnerId: playerId` 的 producer，命中 [`baseAbilities_expansion.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities_expansion.ts>) 的 `base_ossuary` interaction handler：P0 弃牌堆里持有 owner=1 的 borrowed `skeletons_returned_one`，Ossuary 埋葬后 `trueOwnerId` 被写成控制者 `0`。现已最小修复为：该 handler 从当前玩家弃牌堆 live card 回查 owner，再传入 `buildBuryCardEvents()`；不改 shared bury reducer，也不扩大到其它 discard-bury producer。验证：`pnpm vitest run src/games/smashup/__tests__/expansionBaseAbilities.test.ts -t "base_ossuary"` => 修复后 `1 file passed, 2 passed`；邻近 `-t "base_ossuary|base_innsmouth_base_choose_card|base_inventors_salon|base_greenhouse"` => `1 file passed, 13 passed`。该格只闭合 Ossuary 从当前玩家弃牌堆埋葬 borrowed 卡的 true owner provenance，不外推所有 discard-bury producer、Skeletons Returned One、Titans Sphinx、或整个长期任务已完成。
- 2026-05-25 最新已验证：`ancient_egyptians_you_can_take_it_with_you` 又命中一条新的 `borrowed self-bury action trueOwner provenance` 真红灯，并已从 focused 红灯转绿。前一批 hand-bury prompt 收口后，继续按 `trueOwnerId: playerId` 扫到 [`ancient_egyptians.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ancient_egyptians.ts>) 的 `ancientEgyptiansBurySelfOnPlay()`：borrowed `You Can Take It With You(owner='1')` 从 P0 手牌真实打出并自埋后，`BuriedCardOnBase.trueOwnerId` 被写成控制者 `0`。现已最小修复为：该 self-bury onPlay helper 按 live hand owner 回查 `ctx.cardUid`，不改 shared `buildBuryCardEvents()`，也不扩大到 discard/deck 来源。验证：`pnpm vitest run src/games/smashup/__tests__/expansionAbilities.test.ts -t "ancient_egyptians_you_can_take_it_with_you 自埋 borrowed"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近组合 `expansionAbilities.test.ts + newBaseAbilities.test.ts -t "ancient_egyptians_pyramid_engineer talent|ancient_egyptians_lost_knowledge|ancient_egyptians_seal_the_tomb|ancient_egyptians_you_can_take_it_with_you 自埋 borrowed|base_pyramids 在出牌阶段|base_pyramids 埋葬 borrowed"` => `2 files passed, 7 passed`。该格只闭合 Ancient Egyptians self-bury action 从当前手牌打出时的 true owner provenance，不外推所有 `buriedFrom:'play'`、所有自埋行动、或整个长期任务已完成。
- 2026-05-25 最新已验证：`ancient_egyptians_lost_knowledge`、`ancient_egyptians_seal_the_tomb` 与 `base_pyramids` 三条相邻 hand-bury caller 已补 dedicated focused green evidence。前一格修复已经把 [`ancient_egyptians.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ancient_egyptians.ts>) 里的 Pyramid Engineer / Lost Knowledge / Seal the Tomb / Pyramids 从当前玩家手牌埋葬出口统一改成 live hand owner 回查；本轮不再只靠代码阅读，而是在 [`expansionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionAbilities.test.ts>) 和 [`newBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newBaseAbilities.test.ts>) 补三条 focused gate，分别证明 Lost Knowledge、Seal the Tomb、Pyramids 埋葬 borrowed 手牌时 `trueOwnerId` 保留真实 owner `1`，`controllerId` 仍是当前玩家 `0`。验证：`pnpm vitest run src/games/smashup/__tests__/expansionAbilities.test.ts src/games/smashup/__tests__/newBaseAbilities.test.ts -t "ancient_egyptians_lost_knowledge 埋葬 borrowed|ancient_egyptians_seal_the_tomb 埋葬 borrowed|base_pyramids 埋葬 borrowed"` => `2 files passed, 3 passed`；邻近组合 `-t "ancient_egyptians_pyramid_engineer talent|ancient_egyptians_lost_knowledge|ancient_egyptians_seal_the_tomb|base_pyramids 在出牌阶段|base_pyramids 埋葬 borrowed"` => `2 files passed, 6 passed`。该批只闭合 Ancient Egyptians 当前手牌来源 hand-bury sibling 的 focused evidence，不外推 discard/deck 来源、所有 bury caller、或整个长期任务已完成。
- 2026-05-25 最新已验证：`skeletons_lord_of_bones`、`skeletons_grave_goods` 与 `ancient_egyptians_pyramid_engineer` 又命中同一类 `borrowed hand bury trueOwner provenance` 真红灯，并已从 focused 红灯转绿。先复跑交接里提到的 `borrowed skeletons_lord_of_bones`，当前实际不是假红，而是 `CARD_BURIED.trueOwnerId` 被写成控制者 `0`；最小修复为 [`skeletons.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/skeletons.ts>) 在 `handleSkeletonsLordOfBonesBury()` 响应时从当前手牌按 `cardUid` 回查真实 owner。随后新增 [`expansionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionAbilities.test.ts>) gate `ancient_egyptians_pyramid_engineer talent 埋葬 borrowed 手牌时应保留真实 owner`，首轮同样 failed；[`ancient_egyptians.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ancient_egyptians.ts>) 现已把 Pyramid Engineer / Lost Knowledge / Seal the Tomb / Pyramids 这些从当前手牌埋葬的 handler 统一改成回查 live hand owner，而不是直接写 `playerId`。最后继续验证 `borrowed skeletons_grave_goods 首次埋葬 borrowed 手牌时应保留真实 trueOwnerId`，首轮同样 failed，现已把 `skeletons_grave_goods` 首次埋葬和 bonus 额外埋葬两个 hand-bury 出口都改成 live hand owner。验证：`newFactionAbilities.test.ts -t "skeletons_lord_of_bones 天赋可选择从手牌埋葬|borrowed skeletons_lord_of_bones"` => 修复后 `1 file passed, 2 passed`；`expansionAbilities.test.ts -t "ancient_egyptians_pyramid_engineer talent 埋葬 borrowed 手牌时应保留真实 owner"` => 首轮 failed，修复后 `1 file passed, 1 passed`；`newFactionAbilities.test.ts -t "borrowed skeletons_grave_goods 首次埋葬 borrowed 手牌时应保留真实 trueOwnerId|skeletons_grave_goods 首次埋葬后若只剩埋葬牌应直接进入挖掘分支|skeletons_grave_goods 额外埋葬时可选择不同基地"` => `1 file passed, 3 passed`；相邻组合 `expansionAbilities.test.ts + newFactionAbilities.test.ts -t "ancient_egyptians_pyramid_engineer talent|ancient_egyptians_seal_the_tomb|skeletons_lord_of_bones"` => `2 files passed, 5 passed`。该格只闭合从当前玩家手牌选择 borrowed 卡埋葬时的 true owner provenance，不外推所有 bury caller、discard/deck 来源、或整个长期任务已完成。
- 2026-05-25 最新已验证：`vampire_fledgling_vampire_pod` 又命中一条新的 `global hand/discard source controller provenance + buried trueOwner provenance` 真红灯，并已从 focused 红灯转绿。新 gate `borrowed Fledgling Vampire POD 从当前玩家手牌埋葬时，仍应保留真实 trueOwnerId` 首轮不是单点失败，而是顺着真链连续暴露 2 层断口：一，[`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 的 `locateGlobalSources()` 仍把 hand/discard/deck source 的 `controllerId` 写成 `card.owner`，导致 destroyer 手牌里的 borrowed `Fledgling POD` 根本不会被 `playerContext:'sourceController'` 的 global trigger 选成 source；二，修完 global source 定位后，[`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 `vampireFledglingPodBuryBasePromptProgram` 又把 `CARD_BURIED.trueOwnerId` 写成当前玩家 `playerId='1'`，而不是 borrowed 卡真实 owner `0`。现已最小修复为：只把 `locateGlobalSources()` 的 hand/discard/deck 三处 `controllerId` 改成当前区位玩家 `player.id`，并让 `vampireFledglingPodBurySourcePromptProgram` 在进入 bury-base prompt 前透传所选卡真实 owner，再由 `vampireFledglingPodBuryBasePromptProgram` 写回 `trueOwnerId`；不扩到其它 global trigger family、其它 bury caller、或 shared bury reducer。验证：focused `vampiresPod.test.ts -t "borrowed Fledgling Vampire POD 从当前玩家手牌埋葬时，仍应保留真实 trueOwnerId"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `vampiresPod.test.ts + reactionQueueEventPlayerContext.test.ts -t "Fledgling Vampire POD|queued onMinionDestroyed 全局 Vampire POD|Dinner Date POD|Vampire Summon Wolves"` => `2 files passed, 11 passed`。该格当前只记成 dedicated green evidence，不外推所有 global hand/discard trigger、所有 bury caller、或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`vampire_dinner_date_pod` 又命中一条新的 `queued onMinionAffected / destroyer provenance / sourceController runtime` 真红灯，并已从 focused 红灯转绿。新 gate `sourceController queued onMinionAffected trigger 处理 borrowed Dinner Date POD 时，destroyerId 仍应归控制者` 首轮直接坐实：[`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 `vampireDinnerDatePodDestroyHost()` 虽然已按 `playerContext:'sourceController'` 正确入队，但 resolve 时 `buildValidatedDestroyEvents(...)` 仍把 `destroyerId` 写成 `attachment.ownerId='1'`，导致 borrowed `Dinner Date POD` 在控制者 P0 的 affect 真链上消灭宿主时，销毁归属被洗回真实 owner。现已最小修复为：只把这一条 callback 改成按 `attachment.metadata?.sourceControllerId ?? attachment.ownerId` 计算 `destroyerId`；不扩到 `Dinner Date` 非 POD、`Summon Wolves`、`The Count`、或其它 Vampires sibling。验证：focused `reactionQueueEventPlayerContext.test.ts -t "sourceController queued onMinionAffected trigger 处理 borrowed Dinner Date POD 时，destroyerId 仍应归控制者"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueEventPlayerContext.test.ts + vampiresPod.test.ts -t "Dinner Date POD|dinner_date_pod|borrowed Dinner Date"` => `2 files passed, 5 passed`。该格当前只记成 dedicated green evidence，不外推整个 Vampires、所有 `onMinionAffected` destroy producer、或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`trickster_hideout_pod` 又命中一条新的 `borrowed base ongoing interceptor controller provenance` 真红灯，并已从 focused 红灯转绿。新 gate `borrowed Hideout POD 应按控制者而不是真实 owner 阻止其他玩家把随从移动到此基地` 首轮直接坐实：[`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 的 `trickster_hideout_pod` 移动拦截器仍只按 `hideout.ownerId` 判“是不是其他玩家”，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Hideout POD` 会错误放行 P1 把随从移动进基地，同时反向阻止控制者 P0。现已最小修复为：只把这一个 interceptor 改成按 `hideout.metadata?.sourceControllerId ?? hideout.ownerId` 判断控制者；不扩到 `hideout_pod` talent、基础版 `Hideout` 保护、或其它 Tricksters sibling。验证：focused `baseFactionOngoing.test.ts -t "borrowed Hideout POD 应按控制者而不是真实 owner 阻止其他玩家把随从移动到此基地"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `baseFactionOngoing.test.ts -t "trickster_hideout|borrowed Hideout|Hideout POD|POD 版会阻止其他玩家把随从移动到此基地|POD 版允许拥有者把自己的随从移动到此基地|POD 版不沿用旧版行动牌保护"` => `1 file passed, 8 passed`。该格当前只记成 dedicated green evidence，不外推整个 Tricksters、所有 interceptor consumer、或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`zombie_overrun` 又命中一条新的 `borrowed base ongoing restriction controller provenance` 真红灯，并已从 focused 红灯转绿。新 gate `borrowed zombie_overrun 应按控制者而不是真实 owner 限制其他玩家打随从到此基地` 首轮直接坐实：[`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 的 `zombieOverrunRestriction()` 仍只按 `overrun.ownerId` 判“其他玩家”，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `zombie_overrun` 会错误放行真实 owner P1 打随从到基地，同时反向拦住控制者 P0。现已最小修复为：只把这一条 restriction 改成按 `overrun.metadata?.sourceControllerId ?? overrun.ownerId` 判断控制者；不扩到 `zombieOverrunSelfDestruct()`、其它 Zombies sibling、或 shared restriction framework。验证：focused `zombieInteractionChain.test.ts -t "borrowed zombie_overrun 应按控制者而不是真实 owner 限制其他玩家打随从到此基地"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `zombieInteractionChain.test.ts + reactionQueueSourceRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts + wizard-neophyte-ongoing.test.ts -t "zombie_overrun|Zombie Overrun|Overrun|泛滥横行"` => `4 files passed, 8 passed`。该格当前只记成 dedicated green evidence，不外推整个 Zombies、所有 restriction consumer、或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`baseAbilities.ts` 又命中一条新的 shared `ninja_infiltrate base-ability gate controller provenance` 真红灯，并已从 focused 红灯转绿。新 gates `borrowed Infiltrate 由控制者控制时，应阻止 Laboratorium 给控制者打出的首个随从放指示物`、`borrowed Infiltrate 由控制者控制时，应阻止 Moot Site 给控制者打出的首个随从 +2 临时力量`、`borrowed Infiltrate 由控制者控制时，应阻止 Golem Schloß 给冠军的随从放指示物`、`borrowed Infiltrate 由控制者控制时，应阻止 Mountains of Madness 让控制者自己打出的随从拥有者抽疯狂卡` 首轮都直接坐实：[`baseAbilities.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities.ts>) 的这组 `ninja_infiltrate` gate 仍只按 `o.ownerId` 判“谁能忽略基地能力”，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Infiltrate` 无法替控制者 P0 屏蔽 `Laboratorium / Moot Site / Golem Schloß / Mountains of Madness`。现已最小修复为：只把这四条 gate 统一改成 `metadata.sourceControllerId ?? ownerId`，其中 `Mountains of Madness` 额外改为按 played minion `controller` 判是否受基地能力影响，但继续按 minion `owner` 决定若未被屏蔽时谁抽疯狂卡；不扩到其它基地能力、restriction 或 shared base-ability framework。验证：focused `newBaseAbilities.test.ts -t "borrowed Infiltrate 由控制者控制时，应阻止 Laboratorium|borrowed Infiltrate 由控制者控制时，应阻止 Moot Site|borrowed Infiltrate 由控制者控制时，应阻止 Golem Schlo"` => 首轮 `2 failed`，补第三条后 `1 file passed, 3 passed`；focused `newOngoingAbilities.test.ts -t "borrowed Infiltrate 由控制者控制时，应阻止 Mountains of Madness"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `newBaseAbilities.test.ts + baseAbilityIntegration.test.ts + baseAbilitiesPrompt.test.ts -t "borrowed Infiltrate|Castle Blood|The Hill|Laboratorium|Moot Site"` => `3 files passed, 4 passed`。该格当前只记成 shared base-ability gate green evidence，不外推整个 `sourceControllerId` 主线、所有基地能力、或整个长期任务已完成。
- 2026-05-25 最新已验证：`ongoingModifiers.ts::registerOngoingPowerModifier()` 又命中一条新的 shared `base ongoing ownerMinions/opponentMinions controller provenance` 真红灯，并已从 focused 红灯转绿。新 gates `borrowed 睡眠孢子应按控制者而不是真实 owner 压低对手随从` 与 `borrowed 旋转弹头发射器应按控制者而不是真实 owner 给控制者随从 +2` 首轮都直接坐实：[`ongoingModifiers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingModifiers.ts>) 的声明式 helper 在 `target='opponentMinions' | 'ownerMinions' | 'firstOwnerMinion'` 三个分支里仍只按 `action.ownerId` 统计 ongoing 归属，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `killer_plant_sleep_spores / steampunk_rotary_slug_thrower` 会把控制权语义洗回真实 owner。现已最小修复为：只在这一处 shared helper 内新增 `getControllerId = metadata.sourceControllerId ?? ownerId`，并让上述三个分支统一按控制者计数；不扩到其它 scoring helper、base ability gate、restriction 或非声明式 callback。验证：focused `ongoingModifiers.test.ts -t "borrowed 睡眠孢子应按控制者而不是真实 owner 压低对手随从|borrowed 旋转弹头发射器应按控制者而不是真实 owner 给控制者随从"` => 首轮 `2 failed`，修复后 `1 file passed, 2 passed`；邻近 `ongoingModifiers.test.ts -t "睡眠孢子|旋转弹头发射器|Shipwreck Cove|Becalmed Shores|Door to the Beyond|Full Moon|borrowed steampunk_aggromotive"` => `1 file passed, 12 passed`。该格当前只记成 shared modifier green evidence，不外推整个 `sourceControllerId` 主线、所有 base ability gate、或整个长期任务已完成。
- 2026-05-25 最新已验证：`cthulhu_complete_the_ritual` 又命中一条新的 `queued onTurnStart / per-instance source selection / multi-instance base-reset` 真红灯，并已从 focused 红灯转绿。新 gate `queued onTurnStart trigger 处理两张 borrowed cthulhu_complete_the_ritual 时，应逐实例入队并各自清场换基地` 首轮直接坐实：[`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 旧注册没有 `perInstance:true`，`collectTriggers(...)` 只排出 1 条 `sourceDefId='cthulhu_complete_the_ritual'` trigger。现已最小修复为：只给 `cthulhu_complete_the_ritual` 的 `onTurnStart` trigger 补 `perInstance:true`，不扩大到其它 Cthulhu sibling 或 shared queue framework。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "queued onTurnStart trigger 处理两张 borrowed cthulhu_complete_the_ritual 时，应逐实例入队并各自清场换基地"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueSourceRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts + newOngoingAbilities.test.ts + expansionOngoing.test.ts -t "complete_the_ritual|Complete the Ritual|cthulhu_complete_the_ritual|完成仪式"` => `3 files passed, 5 passed, 1 file skipped`。该格当前只记成 dedicated green evidence，不外推整个 Cthulhu、所有 turn-boundary ongoing、所有多实例 base-reset callback，或整个 queued runtime 主线已完成。
- 2026-05-25 最新已验证：`steampunk_difference_engine` 又命中一条新的 `queued onTurnEnd / per-instance source runtime / multi-instance same-def isolation` 真红灯，并已从 focused 红灯转绿。新 gate `queued onTurnEnd trigger 处理同基地两张 borrowed steampunk_difference_engine 时，应逐实例入队并各自给控制者抽牌` 首轮直接坐实：[`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 旧注册没有 `perInstance:true`，`collectTriggers(...)` 只排出 1 条 `sourceDefId='steampunk_difference_engine'` trigger；而 runtime callback `steampunkDifferenceEngineTrigger()` 又只会扫到第一张同名 ongoing 就 `return`，不会按 `sourceCardUid` 精确结算当前实例。现已最小修复为：只给 `steampunk_difference_engine` 的 `onTurnEnd` trigger 补 `perInstance:true`，并把 callback 改成优先按 `ctx.sourceCardUid` 精确定位当前实例后各抽 1 张。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "queued onTurnEnd trigger 处理同基地两张 borrowed steampunk_difference_engine 时，应逐实例入队并各自给控制者抽牌"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueSourceRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts + expansionOngoing.test.ts + specialInteractionChain.test.ts -t "difference_engine|Difference Engine|steampunk_difference_engine|差分机"` => `4 files passed, 7 passed`。该格当前只记成 dedicated green evidence，不外推整个 Steampunks、所有 turn-boundary ongoing、所有 same-def multi-instance callback，或整个 queued runtime 主线已完成。
- 2026-05-25 最新已验证：`killer_plant_entangled` 又命中一条新的 `queued onTurnStart / per-instance source runtime / multi-instance same-def isolation` 真红灯，并已从 focused 红灯转绿。新 gate `queued onTurnStart trigger 处理同基地两张 borrowed killer_plant_entangled 时，应逐实例入队并各自自毁回真实 owner discard` 首轮直接坐实：[`killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>) 旧注册没有 `perInstance:true`，`collectTriggers(...)` 只排出 1 条 `sourceDefId='killer_plant_entangled'` trigger；而 runtime callback `killerPlantEntangledDestroyTrigger()` 又只会 `find()` 每个基地第一张同名 `Entangled`，没有按 `sourceCardUid` 精确结算当前实例。现已最小修复为：只给 `killer_plant_entangled / killer_plant_entangled_pod` 的 `onTurnStart` trigger 补 `perInstance:true`，并把 callback 改成优先按 `ctx.sourceCardUid` 精确定位当前实例后再自毁，`ONGOING_DETACHED.ownerId` 继续保留真实 owner。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "queued onTurnStart trigger 处理同基地两张 borrowed killer_plant_entangled 时，应逐实例入队并各自自毁回真实 owner discard"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueSourceRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts + newOngoingAbilities.test.ts -t "entangled|Entangled|killer_plant_entangled"` => `3 files passed, 9 passed`。该格当前只记成 dedicated green evidence，不外推整个 Killer Plants、所有 turn-boundary ongoing、所有 same-def multi-instance callback，或整个 queued runtime 主线已完成。
- 2026-05-25 最新已验证：`zombie_overrun` 又命中一条新的 `queued onTurnStart / per-instance source runtime / multi-instance same-def isolation` 真红灯，并已从 focused 红灯转绿。新 gate `queued onTurnStart trigger 处理同基地两张 borrowed zombie_overrun 时，应逐实例入队并各自自毁回真实 owner discard` 首轮直接坐实：[`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 旧注册没有 `perInstance:true`，`collectTriggers(...)` 只排出 1 条 `sourceDefId='zombie_overrun'` trigger；而 runtime callback `zombieOverrunSelfDestruct()` 又按全场同名 ongoing 扫描，不按 `sourceCardUid` 精确结算当前实例。现已最小修复为：只给 `zombie_overrun / zombie_overrun_pod` 的 `onTurnStart` trigger 补 `perInstance:true`，并把 callback 改成必须按 `ctx.sourceCardUid + ctx.sourceBaseIndex` 定位当前实例后再自毁，`ONGOING_DETACHED.ownerId` 继续保留真实 owner。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "queued onTurnStart trigger 处理同基地两张 borrowed zombie_overrun 时，应逐实例入队并各自自毁回真实 owner discard"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueSourceRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts + zombieInteractionChain.test.ts -t "zombie_overrun|Zombie Overrun|Overrun|泛滥横行"` => `3 files passed, 6 passed`。该格当前只记成 dedicated green evidence，不外推整个 Zombies、所有 turn-boundary ongoing、所有 same-def multi-instance callback，或整个 queued runtime 主线已完成。
- 2026-05-25 最新已验证：`borrowed mermaids_desert_island` 又命中一条新的 `queued onTurnStart / sourceController runtime / true-owner discard provenance` 真红灯，并已从 focused 红灯转绿。新 gate `borrowed Mermaid Desert Island 应按控制者回合开始自毁，但进入真实拥有者弃牌堆` 首轮直接坐实：queued trigger 已按 `sourceController` 正常入队，`sourceCardUid='desert-island-borrowed-a'`、`ownerPlayerId='0'` 都正确，但 [`mermaids.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts>) 的 `mermaidsDesertIslandOnTurnStart()` 在 runtime detach 时仍把 `ONGOING_DETACHED.payload.ownerId` 直接写成 `ctx.sourceControllerId='0'`，导致 borrowed `Desert Island` 会被送进控制者弃牌堆，而不是真实拥有者 P1 的弃牌堆。现已最小修复为：只在这一个 callback 内按 `ctx.sourceCardUid` 从 live `minion.attachedActions` 回找真实 attached action owner，查不到才 fallback 到 `ctx.sourceControllerId`；不扩到 `Shipwreck Cove`、其它 Mermaids sibling 或 shared onTurnStart framework。验证：focused `reactionQueueEventPlayerContext.test.ts -t "borrowed Mermaid Desert Island 应按控制者回合开始自毁，但进入真实拥有者弃牌堆"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueEventPlayerContext.test.ts + smashup.smoke.test.ts -t "Desert Island|desert_island|Mermaid Desert Island"` => `1 file passed, 2 passed, 1 file skipped`。该格当前只记成 dedicated green evidence，不外推整个 Mermaids、所有 attached-action turn-start self-destruct callback，或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`borrowed ninjas_invisible_ninja` 又命中一条新的 `queued onTurnStart / sourceController runtime / titan lookup provenance` 真红灯，并已从 focused 红灯转绿。新 gate `sourceController queued onTurnStart trigger 在 borrowed Invisible Ninja 上仍应把 prompt 交给当前控制者，并保留 start-turn metadata` 首轮直接坐实：[`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `invisibleNinjaOnTurnStart()` 仍按 `candidate.ownerId === ctx.playerId` 找泰坦；当 `ownerId='1'、controllerId='0'` 的 borrowed `Invisible Ninja` 在 P0 回合开始进入 queued runtime resolve 时，交互与 `invisibleNinjaStartTurn` metadata 都直接丢失。现已最小修复为：executor 优先按 `ctx.sourceCardUid` 回找 live titan，只有 direct caller 缺 `sourceCardUid` 时才 fallback 到 `controllerId === ctx.playerId`；并保留 `defId === 'ninjas_invisible_ninja'` 防御。验证：focused `reactionQueueEventPlayerContext.test.ts -t "sourceController queued onTurnStart trigger 在 borrowed Invisible Ninja 上仍应把 prompt 交给当前控制者，并保留 start-turn metadata"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueEventPlayerContext.test.ts + smashup.smoke.test.ts -t "Invisible Ninja|invisible_ninja"` => `1 file passed, 4 passed, 1 file skipped`。该格当前只记成 dedicated green evidence，不外推整个 Titans、整个 Ninjas、所有 onTurnStart titan trigger，或整个 owner/source 主线已完成。
- 2026-05-25 最新已验证：`borrowed world_champs_bewitched` 又命中一条新的 `attached-return / queued runtime / true-owner provenance` 真红灯，并已从 focused 红灯转绿。新 gate `borrowed world_champs_bewitched 在宿主回手后的 queued runtime resolve 中仍应保留真实 owner` 首轮不是单点失败，而是顺着真链连续暴露出 3 层断口：一，[`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 的 `processReturnToHandTriggers()` 在 `MINION_RETURNED` 宿主回手分支里，把 borrowed attached action 的 `sourceControllerId/ownerPlayerId` 洗回宿主玩家；二，修完 collector 后，[`world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `worldChampsBewitchedTransferOnLeave()` continuation 仍把 runtime `ownerId` 直接取成 `ctx.sourceControllerId`；三，修完 continuation 后，[`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `SU_EVENTS.ONGOING_ATTACHED` 在 borrowed reattach 时虽然已经重新附着成功，但没有同时从真实 owner 弃牌堆移除该卡，形成“基地已重新附着，弃牌堆仍残留同一 UID”的双写。现已最小修复为：`reducer.ts` 里 attached action 回手 collector 改成优先读 `metadata.sourceControllerId ?? sourcePlayerId ?? ownerId`；`world_champs.ts` continuation 改为优先按 `sourceCardUid` 从 live state 回找真实 owner，查不到才 fallback 到 `triggerMinion/sourceControllerId`；`reduce.ts` 的 `ONGOING_ATTACHED` 在 `sourcePlayerId !== ownerId` 时同时从控制者区与真实 owner 区清理旧卡位。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "borrowed world_champs_bewitched 在宿主回手后的 queued runtime resolve 中仍应保留真实 owner"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueSourceRuntimeContext.test.ts + newFactionAbilities.test.ts + baseScoring.test.ts -t "world_champs_bewitched|Bewitched|borrowed world_champs_bewitched|queued onCardReturnedToHand per-instance trigger 在对手回手宿主时仍应把 world_champs_bewitched 的转移权交给行动拥有者|MINION_RETURNED 真链回手宿主时也应把 world_champs_bewitched attached source 入队"` => `3 files passed, 10 passed`；`git diff --check -- src/games/smashup/domain/reducer.ts src/games/smashup/abilities/world_champs.ts src/games/smashup/domain/reduce.ts src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推整个 World Champs、所有 attached return family、所有 queued runtime provenance，或整个长期任务已完成。
- 2026-05-25 最新已验证：`bury.ts` 又命中一条新的 `uncover action prompt-state core-vs-prefix-events carry-through` 真红灯，并已从 focused 红灯转绿。新 gate `bury.uncoverBuriedCard 翻开 fairies_enchantment 后响应 minus 时，应在 prompt state 上看到已附着的 ongoing 并写入 metadata` 首轮直接坐实：`uncoverBuriedCard()` 翻开 `fairies_enchantment` 后虽然已创建 `fairies_enchantment` prompt，但在同一个 prompt state 上响应 `minus` 时只产生 `SYS_INTERACTION_RESOLVED`，没有重新发出带 `metadata.fairiesEnchantmentMode='minus'` 的 `ONGOING_ATTACHED`。根因不是 `fairies.ts` 本体，而是 [`bury.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/bury.ts>) 的 `executeUncoveredAction()` / `handleUncoverOngoingPickTargetMinion()` 在构造了前置 `ACTION_PLAYED + ONGOING_ATTACHED` 之后，仍把旧 `currentState.core` 直接传给 onPlay executor；而 `fairies_enchantment` 的 prompt resolve 明确要从 `state.core.bases[baseIndex].ongoingActions` 里回找自己，于是 prompt state 漂出了旧 core。现已最小修复为：仅在 `bury.ts` 本地新增 `previewEventsOnMatchState(...)`，让 uncover action/ongoing 两个入口在 executor 返回 `matchState` 时，把前置 `ACTION_PLAYED/ONGOING_ATTACHED` 与 executor 自己的 emitted events preview 回返回的 `matchState.core`；不扩到 shared `systems.ts`，也不改普通 minion uncover 分支。验证：focused `expansionAbilities.test.ts -t "bury.uncoverBuriedCard 翻开 fairies_enchantment 后响应 minus 时，应在 prompt state 上看到已附着的 ongoing 并写入 metadata"` => 首轮 failed，修复后 `1 file passed, 1 passed`；整文件 `expansionAbilities.test.ts` => `1 file passed, 34 passed`；整文件 `buryEngine.test.ts` => `1 file passed, 14 passed`；`git diff --check -- src/games/smashup/domain/bury.ts src/games/smashup/__tests__/expansionAbilities.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推所有 bury/uncover caller、所有 ongoing prompt family，或整个长期任务已完成。
- 2026-05-25 最新已验证：`ancient_egyptians_seal_the_tomb` 命中一条新的 `runtime prompt + currentState/event preview carry-through` 真红灯，并已从 focused 红灯转绿。新 gate `ancient_egyptians_seal_the_tomb 真实 uncover 多选若先翻开随从再翻开 Blessing of Anubis，后者也应看到新翻开的随从` 首轮直接坐实：`Seal the Tomb` 的 `uncover` 多选响应里，先翻开 `buried-mummy` 再翻开 `buried-blessing` 后，最终 `buried-mummy.tempPowerModifier === 0` 而不是 `2`。根因不是 `Blessing of Anubis` 本体，而是 [`ancient_egyptians.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ancient_egyptians.ts>) 的 `ancientEgyptiansSealTheTombUncoverPromptProgram.onResolve()` 在 for-loop 里只把 `result.state` 往后传，没有把前一张翻开的领域事件先 preview 到 `currentState.core`；于是第二张 `Blessing of Anubis` 看不到第一张刚翻开的随从。现已最小修复为：仅在该 caller 内新增 `applyPreviewEventsToMatchState(...)`，每轮 `uncoverBuriedCard(...)` 后都先把 `result.events` reduce 到 `result.state.core` 再继续下一张选择；不扩到 shared `systems.ts`，也不改 `bury.ts` 的通用翻开合同。验证：focused `expansionAbilities.test.ts -t "ancient_egyptians_seal_the_tomb 真实 uncover 多选若先翻开随从再翻开 Blessing of Anubis，后者也应看到新翻开的随从"` => 首轮 failed，修复后 `1 file passed, 1 passed`；整文件 `expansionAbilities.test.ts` => `1 file passed, 33 passed`；`git diff --check -- src/games/smashup/abilities/ancient_egyptians.ts src/games/smashup/__tests__/expansionAbilities.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推所有 Ancient Egyptians、所有 bury/uncover caller、所有 `currentState + events` multi-step resolver，或整个 runtime prompt family 已完成。
- 2026-05-25 最新已验证：`trickster_hideout_pod_swap` 命中一条新的 `runtime prompt + emitted DECK_REORDERED + side-state carry-through` 真红灯，并已从 focused 红灯转绿。现有 handler 级测试早已证明“从牌库交换时把 Hideout 洗回牌库”本身成立，但新 focused gate `从牌库交换时经过真实 SYS_INTERACTION_RESPOND 收口后，不应因 DECK_REORDERED 丢失 ongoing 交换结果` 首轮直接坐实：真实响应收口后，P0 牌库只剩 `['d-extra']`，`oa1` 没有进手牌也没有留在牌库，说明 [`systems.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/systems.ts>) 的 `mergePromptResultCoreWithPreEventState(...)` 在 `DECK_REORDERED` 这类只描述牌库顺序、不描述换位来源区的事件上，把 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) `tricksterHideoutPodSwapPromptProgram.onResolve()` 预改的 `players[playerId].deck/hand` 整块抹回了 `coreBeforeHandler`。现已最小修复为：helper 只在 emitted events 纯为 `DECK_REORDERED` 时，按事件里的 `playerId` 精确保留 `result.state.core.players[*].hand/deck`；不扩大到其它带抽牌/弃牌/回手的混合事件族。验证：focused `ongoingTalent.test.ts -t "从牌库交换时经过真实 SYS_INTERACTION_RESPOND 收口后，不应因 DECK_REORDERED 丢失 ongoing 交换结果|从牌库交换时把藏身处洗回牌库，而不是回手|从手牌交换时把藏身处回到手牌，并继续给出消灭选项|从手牌交换被他人拥有的持续战术时，换进基地的 ongoing 仍应保留真实 ownerId"` => `1 file passed, 4 passed`；整文件 `ongoingTalent.test.ts` => `1 file passed, 29 passed`；`git diff --check -- src/games/smashup/domain/systems.ts src/games/smashup/__tests__/ongoingTalent.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推所有 `DECK_REORDERED` producer、所有牌库换位 talent、或整个 runtime prompt family 已完成。
- 2026-05-25 最新已验证：`bear_cavalry_general_ivan_pod` 的真实 prompt `yes` 分支已补回 “每回合限一次” 记录，不再把这格误判成 `systems.ts` 还要继续扩 shared merge。focused gate `bear_cavalry_general_ivan_pod 在真实 prompt 响应 yes 后应保留每回合限一次记录，并阻止同回合第二次触发` 首轮先坐实：旧 `bearCavalryGeneralIvanPodPromptProgram.onResolve` 只手改 `matchState.core.specialLimitUsed`，没有发已有 reducer 支持的 `SU_EVENTS.SPECIAL_LIMIT_USED`，导致 `SYS_INTERACTION_RESPOND` 收口后 `resolved.finalState.core.specialLimitUsed === undefined`。现已最小修复为：该 caller 改为 emitted `SPECIAL_LIMIT_USED(limitGroup=bear_cavalry_general_ivan_pod_0)`，而不是继续给 `mergePromptResultCoreWithPreEventState(...)` 扩新特判。验证：focused `newOngoingAbilities.test.ts -t "bear_cavalry_general_ivan_pod 在真实 prompt 响应 yes 后应保留每回合限一次记录，并阻止同回合第二次触发"` => `1 file passed, 1 passed`；邻近 `newOngoingAbilities.test.ts -t "bear_cavalry_"` => `1 file passed, 27 passed`；`git diff --check -- src/games/smashup/abilities/bear_cavalry.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推整个 Bear Cavalry、所有 prompt-side state carry-through caller，或整个 runtime prompt family 已完成。
- 2026-05-25 最新已验证：`bear_cavalry_superiority_pod` 新补的 direct gate 已明确降级为 dedicated green evidence，不是下一条待修红灯。真实 `USE_TALENT -> SYS_INTERACTION_RESPOND(draw)` 链上，旧实现虽然仍同时返回 `CARDS_DRAWN` 与 `matchState.core` 对 `metadata.superiorityProtect` 的预改，但 focused gate `bear_cavalry_superiority_pod 在真实 prompt 响应 draw 后应关闭保护标记并正常摸牌` 复跑已确认当前合同成立：`draw` 分支会正常摸 1 张牌，且不会继续把 `protect` 模式悬挂成保护。验证：focused `newOngoingAbilities.test.ts -t "bear_cavalry_superiority_pod 在真实 prompt 响应 draw 后应关闭保护标记并正常摸牌"` => `1 file passed, 1 passed`。该格当前只记成 dedicated green evidence，不外推整个 Bear Cavalry、所有 `events + matchState` prompt resolver，或整个 runtime prompt family 已完成；下一步继续离开 `General Ivan POD / Superiority POD` 这两个已确认对象，回到 still-open inventory。
- 2026-05-25 最新已验证：`reactionSession.ts` 的 queued trigger runtime player 恢复已补齐 `eventPlayerId` 语义，`playerContext:'sourceController'` 的 queued `onActionPlayed` 不再把 `ctx.playerId` 洗回 source owner；`Secret Agent` 真链已从共享红灯转绿，并新增 `reactionQueueEventPlayerContext.test.ts` 专项回归。
- 2026-05-25 最新已验证：两个此前仍有怀疑的 caller 已降为 dedicated green evidence，而不是开放红灯：
  - `miskatonic.ts::appendResolvedActionAbility(...)` 没有丢失 `special_madness` 后续交互链。
  - `yuanhou.ts::buildDiscardActionPlayEvents(...) / resolveItsAstoundingAction(...)` 没有吞掉 `令人震惊` 打出后继续创建 runtime prompt 的分支。
- 2026-05-25 最新浏览器证据：`Time Walk` 与 `Secret Agent` 都已补 scoped L3，本地真实入口已覆盖：
  - `Time Walk`：抽 2、把自己沉到牌库底、授予本回合额外随从与额外行动额度，并真实消费这两类额度。
  - `Secret Agent`：P0 真实打出 `Stasis Field` 后，弃牌选择权回到行动玩家本人，且候选只含剩余手牌，不含刚打出的行动。
- 2026-05-25 当前整文件状态：`yuanhouFactionAbilities.test.ts` 已从此前 `8 -> 6 -> 2` 的剩余红灯继续收口到 `249 passed`。最后两条并不是真业务红灯：`Cellular Bonding -> Stasis Field` 用例踩中了 `sharks_megalodon` 自带 `beforeScoring` 真触发器，`Biolab me-first restriction` 则仍在手工拼旧 `legacy responseWindow`，与当前 frame-backed reaction session 合同脱节。
- 2026-05-25 当前结论：这一轮已经证明“不是死循环”，因为同一主文件已全绿；但这只代表 `yuanhouFactionAbilities.test.ts` 这份四派系代表性行为单测已清空，不代表整个长期 `effect atom / shared seam completion audit` 已完成。
- 2026-05-25 下一格聚焦：沿 `post-process producer / queued runtime context / true-owner provenance` 继续筛其它 handbuilt producer 与 runtime consumer 的非对称，优先看 `onCardReturnedToHand / onMinionDiscardedFromBase / onTurnStart / onTurnEnd` 家族里 borrowed attached/ongoing 仍可能把 `metadata.sourceControllerId`、`sourcePlayerId` 或 live owner provenance 洗回 `ownerId` 的 sibling；不回扫已转绿的 `world_champs_bewitched`、`Stasis Field`、`Secret Agent`，也不被旧 `waiting overlay` objective 文案带回 transport 残项。

## Current Focus

- `skeletons_lord_of_bones / skeletons_grave_goods / ancient_egyptians_pyramid_engineer` 的真实红灯已修绿，`ancient_egyptians_lost_knowledge / seal_the_tomb / base_pyramids` 三条相邻 hand-bury caller 已有 dedicated focused green evidence，`ancient_egyptians_you_can_take_it_with_you` 自埋 borrowed action 与 `base_ossuary` discard-bury borrowed card 也已转绿；下一步继续沿 `trueOwnerId: playerId` / 当前区位玩家写入最终 owner 的方向筛其它 still-open sibling，优先看 Skeletons Returned One / discard-bury helper、Titans Sphinx，或其它手写 producer，不回扫刚转绿的 `Lord of Bones / Grave Goods / Pyramid Engineer / Lost Knowledge / Seal the Tomb / Pyramids / You Can Take It With You / Ossuary / Fledgling POD`。
- `vampire_fledgling_vampire_pod` 这条 `global hand/discard source controller provenance + buried trueOwner` seam 已闭合，不再把 `locateGlobalSources()` 的 zone-controller 计算或 `vampireFledglingPodBuryBasePromptProgram` 当 still-open residual；下一步继续横向筛其它 global hand/discard/deck trigger 与 bury/return caller，优先看仍把 source controller 写成 `card.owner`、或把最终 `trueOwnerId/ownerId` 写成当前玩家区位的 sibling，不回扫刚转绿的 `Fledgling POD / Dinner Date POD / Hideout POD / Zombie Overrun`。
- `vampire_dinner_date_pod` 这条 `queued onMinionAffected / destroyer provenance` seam 已闭合，不再把 `vampireDinnerDatePodDestroyHost()` 当 still-open residual；下一步继续横向筛其它 `playerContext:'sourceController'` 已正确入队、但 runtime destroy/detach/discard producer 仍把 `destroyerId`、`ownerId` 或 live provenance 洗回 `ownerId` 的 sibling，优先看 `onMinionAffected / onMinionDestroyed / onMinionDiscardedFromBase / onCardReturnedToHand` 家族，不回扫刚转绿的 `Dinner Date POD / Zombie Overrun / Hideout POD`。
- `zombie_overrun` 这条 `queued onTurnStart / per-instance source runtime / same-def multi-instance isolation` seam 已闭合，不再把 `zombieOverrunSelfDestruct()` 当 still-open residual；下一步继续横向筛其它 `playerContext:'sourceController'` 或 turn-boundary callback 里仍未标 `perInstance`、且 resolve 时还按全场同名 source 扫描的 sibling，优先看 `onTurnStart / onTurnEnd` 家族，不回扫刚转绿的 `Zombie Overrun / Mermaid Desert Island / Invisible Ninja / Bewitched / Stasis Field`。
- `borrowed mermaids_desert_island` 这条 `queued onTurnStart / true-owner discard provenance` seam 已闭合，不再把 `mermaidsDesertIslandOnTurnStart()` 当 still-open residual；下一步继续横向筛同 family 里仍是 `playerContext:'sourceController'`、但 runtime detach/discard owner 仍可能洗回控制者的 sibling，优先看 `onTurnStart / onTurnEnd / onCardReturnedToHand / onMinionDiscardedFromBase` 家族，不回扫刚转绿的 `Mermaid Desert Island / Invisible Ninja / Bewitched / Stasis Field`。
- `borrowed Invisible Ninja` 这条 `queued onTurnStart / sourceCardUid lookup` seam 已闭合，不再把 `titans.ts::invisibleNinjaOnTurnStart()` 当 still-open residual；下一步继续横向筛其它仍注册 `playerContext:'sourceController'`、但 callback/executor 仍直接按 `ownerId` 找 source titan/ongoing 的 sibling，优先看 `onTurnStart / onTurnEnd / onCardReturnedToHand / onMinionDiscardedFromBase` 家族，不回扫刚转绿的 `Invisible Ninja / Bewitched / Stasis Field`。
- `processReturnToHandTriggers()` 这条 `Bewitched` true-owner provenance seam 已闭合，不再把 `MINION_RETURNED -> queued trigger -> runtimePrompt -> ONGOING_ATTACHED` 这条已转绿的 World Champs sibling 当 still-open residual；下一步继续横向筛其它手写 post-process producer 与 queued runtime consumer，优先找 borrowed attached/ongoing 在回手、离场、回合边界和重新附着时仍把 controller/owner provenance 混掉的最小真红灯。
- `bury.ts` 的 uncover action prompt-state seam 已闭合，不再把“前置 `ACTION_PLAYED/ONGOING_ATTACHED` 没 preview 回 prompt state core”当 still-open residual；下一步继续横向筛其它“返回 prompt/matchState，但返回态 core 仍停在 prefix events 之前”的 sibling，优先看 `fairies_enchantment / ornate_dome / 其它 buried ongoing onPlay` 是否还有 direct gate 缺口，不回扫刚转绿的 `fairies_enchantment from buried / Seal the Tomb`。
- `cowboys_stagecoach` 的 mixed dynamic/static move 这格已从 focused 红灯转成 scoped green evidence：当前新的 shared 真缝不是 `Cowboys` 自己又漏了某张牌，而是 [`systems.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/systems.ts>) 的 `mergePromptResultCoreWithPreEventState(...)` 之前只保 `timedPowerModifiers`，会把 [`cowboys.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cowboys.ts>) `relocateStagecoachStaticCards(...)` 已搬走的 `ongoingActions / buriedCards` 在 emitted `MINION_MOVED` 存在时抹回去。现已最小修复为：helper 再按 emitted events 影响面精确保留 base 静态区 side-state；只有当 emitted events 不包含 `ONGOING_ATTACHED/DETACHED`、`CARD_BURIED`、`BURIED_CARD_*` 这类会自己改 `ongoingActions / buriedCards` 的事件时，才把 `result.state.core.bases[*].ongoingActions / buriedCards` 透传回去。验证：focused `newFactionAbilities.test.ts -t "cowboys_stagecoach 可把同一基地上至多两个己方随从移动到另一个基地|cowboys_stagecoach 也可搬运基地上的持续行动和埋葬牌|cowboys_stagecoach 同时搬运随从与基地持续行动时，不应因随从移动事件而丢失持续行动的真实迁移|cowboys_stagecoach 应按控制者而不是真实 owner 搬运基地上的 borrowed 持续行动|cowboys_stagecoach 也可搬运你控制的泰坦|fairies_glymmer 对其他随从的 -4 力量会在你的下回合开始时结束|fairies_glymmer 对自己的 \\+1 力量也会在你的下回合开始时结束"` => `1 file passed, 7 passed`；整文件 `newFactionAbilities.test.ts` => `1 file passed, 281 passed`。该格当前只记成 dedicated green evidence，不外推所有 base static-zone prompt caller、所有 `ongoingActions / buriedCards` side-state，或整个 ability runtime family 已完成；下一步继续横向筛其它“runtime prompt 会预改 `core` 且同时返回 events”的 caller，不回扫已闭合的 `Glymmer / Stagecoach`。
- `fairies_glymmer` 的 until-next-turn restore 这格已从 `newFactionAbilities.test.ts` 唯一真红灯转成 scoped green evidence：根因不是 `fairies.ts` 的 `addPermanentPower(-4)` 失效，而是 [`systems.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/systems.ts>) 在 ability runtime prompt resolve 且 `emittedEvents.length > 0` 时，把 `result.state.core` 整块回退成 `coreBeforeHandler`，把 [`fairies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/fairies.ts>) `appendTimedPowerModifier(...)` 写入的 `timedPowerModifiers` 抹掉了；于是目标随从虽已被真实减成 0 战力，但 `TURN_STARTED(turnNumber=3)` 没有计时条目可回退。现已最小修复为：只新增 `mergePromptResultCoreWithPreEventState(...)`，在需要保留 pre-event core 的 prompt 分支上，仅透传 `result.state.core.timedPowerModifiers`，其余 core 字段仍回退到 `coreBeforeHandler`，避免 domain events 双应用。随后继续把 `Glymmer` 的 self-bonus sibling 补成 direct gate：`fairies_glymmer 对自己的 +1 力量也会在你的下回合开始时结束`，确认同一 helper 的另一条分支也被当前补丁覆盖。验证：focused `newFactionAbilities.test.ts -t "fairies_glymmer 对其他随从的 -4 力量会在你的下回合开始时结束|fairies_glymmer 对自己的 \\+1 力量也会在你的下回合开始时结束"` => `1 file passed, 2 passed`；整文件 `newFactionAbilities.test.ts` => `1 file passed, 280 passed`；`git diff --check -- src/games/smashup/domain/systems.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` => 仅 LF/CRLF warning。该格当前只记成 dedicated green evidence，不外推所有 non-event core side-state、所有 timed effect producer，或整个 ability runtime family 已完成；下一步继续横向筛其它 runtime prompt 在 emitted events 时仍依赖 `matchState.core` side-state 的 sibling，优先从“会直接预改 `core` 且同时返回 events”的 runtime prompt caller 继续下钻，不回扫已闭合的 `Glymmer` 两分支。
- `tornados_ripped_off` 的 borrowed Fairies Enchantment 迁移链已经收口成 scoped green evidence：前一轮 focused 红点实际是测试夹具把目标随从选成了会被同控制者 `source-minion` 额外加 1 的 `robot_microbot_alpha`，把 `fairies_enchantment` 的 `-1` 抵消掉；改成稳定宿主 `sharks_mako` 后，`metadata/sourcePlayerId` 迁移与移动后 modifier 一起转绿。验证：`shayuFactionAbilities.test.ts -t "龙卷风：扯走把 borrowed Fairies Enchantment 移到另一基地后，应保留 metadata 与 sourcePlayerId|龙卷风：扯走把 borrowed 附着行动移到 Brownie 身上时，应保留 sourcePlayerId 并把 queued 事件玩家留给真正行动玩家"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/__tests__/shayuFactionAbilities.test.ts src/games/smashup/abilities/tornados.ts` => passed（仅 LF/CRLF warning）。这格只记成 dedicated green evidence，不外推整个 Tornados/Fairies，或整条 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `world_champs_high_speed_chase` 又收掉一条 borrowed reattach controller metadata seam：[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed world_champs_high_speed_chase 应按控制者而不是真实 owner 转移行动并移动随从且+3` 后，首轮先坐实 `world_champs.ts` 的 source-base 查找还在按 `ownerId` 回捞 ongoing；修完这层后，剩余唯一失败又进一步把真缺口钉死在 reattach provenance：动作本身、移动和 `+3` 都已经成立，但转移后的 `USE_TALENT` 复用校验却从预期的“本回合天赋已使用”退回成“只能使用自己的持续行动卡天赋”，说明 live state 重新附着后把 borrowed controller 语义洗回了 true owner。现已最小修复为：只在 [`world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `world_champs_high_speed_chase_base` reattach payload 上补 `sourcePlayerId` 与 `metadata: ongoing.metadata`，让 [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 重新附着时继续保住 `metadata.sourceControllerId`。验证：focused `newFactionAbilities.test.ts -t "borrowed world_champs_high_speed_chase 应按控制者而不是真实 owner 转移行动并移动随从且\+3"` => `1 file passed, 1 passed`；邻近 `newFactionAbilities.test.ts -t "world_champs_high_speed_chase"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/abilities/world_champs.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格当前只记成 dedicated green evidence，不外推整个 World Champs、所有 move/reattach caller、所有 `ONGOING_ATTACHED` producer，或整个 owner/source 主线已完成；下一步继续横向筛 still-open handbuilt `ONGOING_ATTACHED` producer / runtime consumer，优先找 live state 已依赖 `metadata.sourceControllerId`、但 reattach/move 后仍可能丢 provenance 的 sibling。
- 当前最新已验证 seam：
  - `trickster_flame_trap_pod` 又收掉一条 borrowed turn-start prompt controller seam：[`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Flame Trap POD 应按控制者而不是真实 owner 在控制者回合开始弹出 breakpoint prompt` 后，首轮直接坐实 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 的 `onTurnStart` callback 仍只按 `trap.ownerId !== trigCtx.playerId` 判“是不是你的回合开始”，导致 queued trigger 虽然正常入队，resolve 后却没有任何 interaction。现已最小修复为：只把这一个过滤点统一改成 `trap.metadata?.sourceControllerId ?? trap.ownerId`，不碰 `onMinionPlayed` 的自毁/消灭链，也不扩到 `Pay the Piper`、`Brownie` 或其它 Tricksters sibling。验证：focused `reactionQueueEventPlayerContext.test.ts -t "borrowed Flame Trap POD 应按控制者而不是真实 owner 在控制者回合开始弹出 breakpoint prompt"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `reactionQueueEventPlayerContext.test.ts -t "Flame Trap POD"` => `1 file passed, 2 passed`。该格当前只记成 dedicated green evidence，不外推整个 Tricksters、所有 onTurnStart prompt callback，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `ninja_infiltrate / ninja_assassination` 又收掉两条 borrowed controller seam：[`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Ninja Infiltrate 应按控制者而不是真实 owner 在控制者回合开始自毁` 与 `borrowed Ninja Assassination 应按控制者而不是真实 owner 在控制者回合结束消灭宿主` 后，首轮两条都直接坐实 [`ninjas.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ninjas.ts>) 的 callback 本体仍只按 `ownerId` 过滤，导致 queued trigger 虽然正常入队，resolve 后却只留下 `TRIGGER_CONSUMED`。现已最小修复为：`ninja_infiltrate` 的自毁判定改成 `metadata.sourceControllerId ?? ownerId`，`ninja_assassination` 的触发归属与 `destroyerId` 也统一改按控制者；不扩到 `onPlay`、`Disguise`、`Smoke Bomb` 之外的其它 Ninjas sibling。验证：focused `reactionQueueEventPlayerContext.test.ts -t "borrowed Ninja Infiltrate|borrowed Ninja Assassination"` => 首轮 `2 failed`，修复后 `1 file passed, 2 passed`；邻近 `reactionQueueEventPlayerContext.test.ts -t "Ninja Infiltrate|Ninja Assassination"` => `1 file passed, 4 passed`。该组当前只记成 dedicated green evidence，不外推整个 Ninjas、所有 attached action turn-boundary callback，或整个 owner/source 主线已完成；下一步离开 `ninjas.ts`，继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `ninja_smoke_bomb` 又收掉一组 borrowed controller seam：[`baseFactionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseFactionOngoing.test.ts>) 新增 focused gate `borrowed Smoke Bomb 应按控制者而不是真实 owner 保护控制者的随从不受其他玩家行动影响` 后，首轮直接坐实 [`ninjas.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ninjas.ts>) 的 protection checker 仍只按 `bomb.ownerId` 判“其他玩家”；[`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Ninja Smoke Bomb 应按控制者而不是真实 owner 在控制者回合开始自毁` 后，也首轮坐实 queued trigger 已入队但 callback 仍按 `attached.ownerId !== trigCtx.playerId` 跳过 detach。现已最小修复为：只把这两个 runtime consumer 都统一改成 `metadata.sourceControllerId ?? ownerId`，不扩到 `ninja_infiltrate / ninja_assassination / ninja_disguise` 或其它 Ninjas sibling。验证：focused 两条各自从 failed 转成 `1 file passed, 1 passed`；邻近 `baseFactionOngoing.test.ts -t "ninja_smoke_bomb|Smoke Bomb"` => `1 file passed, 4 passed`；邻近 `reactionQueueEventPlayerContext.test.ts -t "Ninja Smoke Bomb|ninja_smoke_bomb"` => `1 file passed, 2 passed`。该格当前只记成 dedicated green evidence，不外推整个 Ninjas、所有 attached-action protection、所有 onTurnStart 自毁 callback，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `innsmouth_in_plain_sight` 又收掉一条 borrowed protection-checker controller seam：[`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增 focused gate `borrowed in_plain_sight 应按控制者而不是真实 owner 保护控制者的低力量随从不受其他玩家影响` 后，首轮直接坐实 [`innsmouth.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/innsmouth.ts>) 的 `innsmouthInPlainSightChecker()` 仍只按 `sight.ownerId` 判“保护谁”和“谁算其他玩家”。现已最小修复为：只把该 checker 的控制者判定统一改成 `sight.metadata?.sourceControllerId ?? sight.ownerId`，不碰原版/POD 的力量判断分支，也不扩到 `return_to_the_sea` 或其他 Innsmouth sibling。验证：focused `expansionOngoing.test.ts -t "borrowed in_plain_sight 应按控制者而不是真实 owner 保护控制者的低力量随从不受其他玩家影响"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `expansionOngoing.test.ts -t "innsmouth_in_plain_sight|borrowed in_plain_sight"` => `1 file passed, 5 passed`。该格当前只记成 dedicated green evidence，不外推整个 Innsmouth、所有 affect protection、所有 POD/base 力量分支，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `trickster_hideout` 又顺手收掉一条 exact-defId protection alias bleed：在 borrowed `Hideout` 本体修完后，宽子集验证把 `POD 版不沿用旧版行动牌保护` 旧红点重新带了出来。回读 [`utils.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/utils.ts>) 后已坐实根因：`matchesDefId()` 会把 `_pod` 视为同名，因此 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 里基础版 `trickster_hideout` protection checker 用 `matchesDefId(...,'trickster_hideout')` 去找来源时，会把 `trickster_hideout_pod` 一起吞进来。现已最小修复为：只把这条基础版 checker 在 base/attached 两处来源识别改成精确 `defId === 'trickster_hideout'`，不碰 `hideout_pod` 自己的 talent/interceptor，也不动别的 Tricksters card。验证：`baseFactionOngoing.test.ts -t "保护同基地己方随从不受对手行动卡影响|borrowed Hideout 打在基地上|borrowed Hideout 贴在随从上|不保护敌方随从|POD 版不沿用旧版行动牌保护"` => `1 file passed, 5 passed`。该格当前只记成 dedicated green evidence，不外推所有 `_pod` alias、所有 Tricksters protection、或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `trickster_hideout` 又收掉一条 borrowed action-protection controller seam：[`baseFactionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseFactionOngoing.test.ts>) 新增 focused gate `borrowed Hideout 打在基地上时，应按控制者而不是真实 owner 保护控制者的随从` 与 `borrowed Hideout 贴在随从上时，应按控制者而不是真实 owner 保护控制者的随从` 后，首轮两条都直接坐实 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 的 `trickster_hideout` protection checker 在 base/attached 两条入口里都仍只按 `ownerId` 判受保护随从与敌我来源。现已最小修复为：只把这两处判定统一改成 `metadata.sourceControllerId ?? ownerId`，不碰 `hideout_pod`、talent、Brownie 或其它 Tricksters sibling。验证：focused `baseFactionOngoing.test.ts -t "borrowed Hideout"` => 首轮 `2 failed`，修复后 `1 file passed, 2 passed`；收窄到 `保护同基地己方随从不受对手行动卡影响|borrowed Hideout 打在基地上|borrowed Hideout 贴在随从上|不保护敌方随从` => `1 file passed, 4 passed`。中途宽过滤词 `trickster_hideout|borrowed Hideout` 会带出已知旧红点 `POD 版不沿用旧版行动牌保护`；它是当前文件里的既有 residual，不算这轮 `trickster_hideout` borrowed seam 的回归，也不算本轮已完成项。该格当前只记成 dedicated green evidence，不外推 `hideout_pod`、所有 protection alias、所有 Tricksters，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `killer_plant_entangled` 又收掉一条 borrowed protection-checker controller seam：[`newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增 focused gate `borrowed Entangled 在只有控制者随从而没有真实 owner 随从时，也应按控制者触发保护` 后，首轮直接坐实 [`killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>) 的 `killerPlantEntangledChecker()` 仍只按 `entangled.ownerId` 判“这里有你的随从”。现已最小修复为：只把这条 checker 的控制者判定改成 `entangled.metadata?.sourceControllerId ?? entangled.ownerId`，不碰 self-destruct callback，也不扩到 `protectedFromEntangled` 的 affect-source 语义、其它 Killer Plants sibling、或 shared protection framework。验证：focused `newOngoingAbilities.test.ts -t "borrowed Entangled 在只有控制者随从而没有真实 owner 随从时，也应按控制者触发保护"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `newOngoingAbilities.test.ts -t "borrowed Entangled|killer_plant_entangled"` => `1 file passed, 6 passed`。该格当前只记成 dedicated green evidence，不外推整个 Killer Plants、所有 move protection、所有 affect-source 语义，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `killer_plant_choking_vines` 又收掉一条 borrowed attached-action turn-start callback seam：[`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增 focused gate `borrowed Choking Vines 应按控制者而不是真实 owner 在控制者回合开始消灭宿主` 后，首轮直接坐实 [`killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>) 的 `killerPlantChokingVinesTrigger()` 仍只按 `attached.ownerId !== ctx.playerId` 判“是不是你的回合开始”。现已最小修复为：只把这条 callback 的控制者判定统一改成 `attached.metadata?.sourceControllerId ?? attached.ownerId`，不扩到 `deep_roots / overgrowth / entangled`、其它 Killer Plants sibling、或 shared onTurnStart framework。验证：focused `expansionOngoing.test.ts -t "borrowed Choking Vines 应按控制者而不是真实 owner 在控制者回合开始消灭宿主"` => 首轮 failed，修复后 `1 file passed, 1 passed`；邻近 `expansionOngoing.test.ts -t "killer_plant_choking_vines|borrowed Choking Vines"` => `1 file passed, 2 passed`。该格当前只记成 dedicated green evidence，不外推整个 Killer Plants、所有 attached-action turn-start callback、所有 destroy producer，或整个 owner/source 主线已完成；下一步继续横向筛同型 runtime consumer / restriction / candidate-builder seam。
- 当前最新已验证 seam：
  - `fairies_magic_ward` 又收掉一条横向 borrowed restriction seam：[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed fairies_magic_ward 应按控制者而不是真实 owner 限制其他玩家在此基地打行动` 后，直接坐实 [`fairies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/fairies.ts>) 的 `fairiesMagicWardRestrictionChecker()` 仍只按 `action.ownerId !== ctx.playerId` 判“其他玩家”。现已最小修复为：restriction 本体统一改按 `metadata.sourceControllerId ?? ownerId` 判控制者，不扩到 `fairies_ladybug`、`fairies_enchantment`、其它 Fairies prompt 或 shared restriction registry。验证：focused `newFactionAbilities.test.ts -t "borrowed fairies_magic_ward 应按控制者而不是真实 owner 限制其他玩家在此基地打行动"` => `1 file passed, 1 passed`；邻近 `newFactionAbilities.test.ts -t "fairies_magic_ward|borrowed fairies_magic_ward|fairies_ladybug"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/abilities/fairies.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` 待本轮状态回写后统一复核。该格当前只记成 dedicated green evidence，不外推整个 Fairies、所有 restriction consumer 或整个 owner/source 主线已完成；下一步继续横向筛同型 restriction / provider / candidate-builder seam。
- 当前最新已验证 seam：
  - `cyborg_apes_missing_uplink / shapeshifters_cellular_bonding(copied missing_uplink) / cyborg_apes_flying_monkey` 又收掉 3 条同文件段的 borrowed controller seam：[`yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts>) 新增 focused gate `borrowed 丢失中继应按控制者而不是真实 owner 在控制者回合结束抽牌`、`borrowed 细胞结合复制丢失中继后也应按控制者而不是真实 owner 触发抽牌`、`borrowed 飞猴在计分后应把移动选择权交给控制者而不是真实 owner` 后，直接坐实 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `cyborgApesMissingUplink()`、`shapeshiftersCellularBondingMissingUplink()` 仍按 `action.ownerId` 计数实例，而 `cyborgApesFlyingMonkeyAfterScoring()` 仍把 prompt player 发给 `action.ownerId`。现已最小修复为：两条 Missing Uplink consumer 的实例计数都统一改按 `getOngoingActionControllerId(action)` 聚合，Flying Monkey 的 prompt player 改成 `getOngoingActionControllerId(action)`；不改 draw 落点、detach/discard 落点或整条 afterScoring move 链其它语义。验证：focused `yuanhouFactionAbilities.test.ts -t "borrowed 丢失中继|borrowed 细胞结合复制丢失中继|丢失中继在拥有者回合结束从 attachedActions 触发抽牌|细胞结合复制丢失中继后能从附着行动触发抽牌|丢失中继在多 owner 混挂时只聚合当前拥有者的实例|丢失中继在额外回合结束时也应按拥有者实例数抽牌"` => `1 file passed, 6 passed`；focused `yuanhouFactionAbilities.test.ts -t "电子猿：飞猴在计分后按玩家选择另一基地并可跳过自动移动|电子猿：borrowed 飞猴在计分后应把移动选择权交给控制者而不是真实 owner"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/abilities/yuanhou.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` 通过。该组只闭合这 3 条 Cyborg Apes / Cellular Bonding runtime consumer seam，不外推所有 turn-end draw trigger、所有 afterScoring prompt owner 或整个 yuanhou 长期任务已完成。
- 当前最新已验证 seam：
  - `super_spies_mindraker` 又收掉一条 borrowed restriction seam：[`yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts>) 新增 focused gate `borrowed Mindraker 应按控制者而不是真实 owner 在计分窗口阻止其他玩家向该基地打行动` 后，直接坐实 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `registerRestriction('super_spies_mindraker','play_action',...)` 仍只按 `action.ownerId !== ctx.playerId` 判“其他玩家”。现已最小修复为：restriction 本体统一改按 `getOngoingActionControllerId(action) !== ctx.playerId`，不扩到 `Mole` 额外行动长链或其它 scoring-window restriction。验证：focused `yuanhouFactionAbilities.test.ts -t "超级间谍：心灵扳机只在该基地计分窗口阻止其他玩家向该基地打行动|超级间谍：borrowed Mindraker 应按控制者而不是真实 owner 在计分窗口阻止其他玩家向该基地打行动"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/abilities/yuanhou.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` 通过。该格只闭合 borrowed `Mindraker` 的 scoring-window restriction seam，不外推 `Mole` 真实计分窗口链或整个 Super Spies 已完成。
- 当前最新已验证 seam：
  - `cyborg_apes_monkey_on_your_back / cyborg_apes_going_bananas / cyborg_apes_shielding` 又收掉一组同文件段 borrowed controller seam：[`yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts>) 新增 3 条 focused gate `borrowed 猴子在你的背上应按控制者找到宿主并在结算后回到真实拥有者牌库底`、`borrowed Going Bananas 应按控制者而不是真实 owner 移除其他玩家控制的基地与附着行动`、`borrowed 护盾应按控制者而不是真实 owner 摧毁宿主上其他玩家控制的行动` 后，直接坐实 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 里 `locateMonkeyOnYourBackHost()`、`cyborgApesGoingBananas()`、`cyborgApesShielding()` 都仍把 runtime 控制者洗回 `ownerId`。现已最小修复为：只补一个局部 helper `getOngoingActionControllerId(...)`，并让这 3 个 consumer 统一按 `metadata.sourceControllerId ?? ownerId` 判控制者，保留 detach / deck-bottom 的 true owner 落点不变。验证：focused `yuanhouFactionAbilities.test.ts -t "电子猿：为了香蕉只摧毁其他玩家打到指定基地和仆从上的行动|电子猿：borrowed Going Bananas 应按控制者而不是真实 owner 移除其他玩家控制的基地与附着行动|电子猿：护盾打出时摧毁该仆从上其他玩家的行动并保留己方行动|电子猿：borrowed 护盾应按控制者而不是真实 owner 摧毁宿主上其他玩家控制的行动|电子猿：护盾持续保护宿主上的其他行动不受对手行动影响|电子猿：borrowed 猴子在你的背上应按控制者找到宿主并在结算后回到真实拥有者牌库底|电子猿：猴子在你的背上天赋摧毁同基地敌方低力量随从并把本卡放到底"` => `1 file passed, 7 passed`；`git diff --check -- src/games/smashup/abilities/yuanhou.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` 通过。该组只闭合这 3 条 Cyborg Apes runtime consumer 的 borrowed controller seam，不外推 `super_spies_mindraker`、`Mole` 计分窗口链或整条 yuanhou 长期任务已完成。
- 当前最新已验证 seam：
  - `trickster_enshrouding_mist borrowed turn-transition extra-minion controller seam` 已从新红灯升为 scoped green：[`archmageE2E.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/archmageE2E.test.ts>) 新增 focused gate `borrowed 隐蔽迷雾应按控制者而不是真实 owner 在进入 playCards 时给控制者基地限定额外随从` 后，首轮直接坐实 [`domain/index.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/index.ts>) 在回合切入 `playCards` 时仍写着 `ongoing.ownerId === playerId`，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `隐蔽迷雾` 不会给 P0 发 `baseLimitedMinionQuota[0]`。现已最小修复为：该 turn-transition 分支改按 `ongoing.metadata?.sourceControllerId ?? ongoing.ownerId` 判控制者，不扩到 `trickster_enshrouding_mist_pod`、其它 startTurn/playCards extra quota 逻辑或通用 phase-transition helper。验证：focused `archmageE2E.test.ts --testNamePattern "隐蔽迷雾 E2E|borrowed 隐蔽迷雾"` => `1 file passed, 2 passed`；邻近 `baseFactionOngoing.test.ts --testNamePattern "trickster_enshrouding_mist|迷雾笼罩"` => `1 file passed, 3 passed`。该格只闭合 borrowed `trickster_enshrouding_mist` 在 turn transition -> playCards 的 extra-minion controller seam，不外推所有 Tricksters、所有 banked extra minion grant、或整个 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `kaiju_gorgodzolla_special borrowed action-count controller seam` 已从新红灯升为 scoped green：[`smashup.smoke.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/smashup.smoke.test.ts>) 新增 focused gate `borrowed 哥佐拉 special 应按控制者而不是真实 owner 统计本基地的基地与附着战术` 后，首轮直接坐实 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `getOwnActionCountOnBase()` 仍纯按 `action.ownerId === playerId` 统计“你在这里的行动牌”，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed 基地 ongoing 与 borrowed attached action 都不会给 P0 的 `Gorgodzolla` special 计数，`validate(ACTIVATE_SPECIAL)` 直接拒绝。现已最小修复为：base ongoing 与 attached action 两条计数分支都统一改按 `metadata.sourceControllerId ?? ownerId` 判控制者，不扩到其它 Kaiju/Titans special 或 generic action counter helper。验证：focused `smashup.smoke.test.ts --testNamePattern "哥佐拉满足本基地有你至少两个战术后可通过 special 从牌库旁进场，并统计附着战术|borrowed 哥佐拉 special 应按控制者而不是真实 owner 统计本基地的基地与附着战术"` => `1 file passed, 2 passed`；邻近 `smashup.smoke.test.ts --testNamePattern "哥佐拉"` => `1 file passed, 5 passed`。该格只闭合 borrowed `Gorgodzolla` special 的 action-count controller seam，不外推所有 Titans、所有“你有几个行动牌”统计器、或整个 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `frankenstein_grave_situation borrowed destroy-replacement controller seam` 已从新红灯升为 scoped green：[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed grave_situation 应按控制者而不是真实 owner 让控制者的被消灭随从回到拥有者手牌` 后，首轮直接坐实 [`frankenstein.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/frankenstein.ts>) 的 replacement callback 同时把 `hasGraveSituation` 与“你的随从被消灭”两处都绑在 `playerId/ownerId`：既只认 `action.ownerId === playerId`，又要求 `minion.controller === playerId`。对 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Grave Situation` 来说，这会让 P0 控制、P0 拥有的宿主被消灭时完全不返回。现已最小修复为：改用 `ctx.triggerMinion ?? live base lookup` 求被消灭宿主的 `controllerId`，并统一按 `metadata.sourceControllerId ?? ownerId` 与该 `controllerId` 对齐；`toPlayerId` 仍保持 `minion.owner`，不改“回拥有者手牌”的既有语义。验证：focused `newFactionAbilities.test.ts --testNamePattern "borrowed grave_situation|borrowed german_engineering|德国工程学：在该基地打出随从后应给该随从\+1指示物"` => `1 file passed, 3 passed`；`git diff --check -- frankenstein.ts + newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Grave Situation` 的 destroy replacement controller seam，不外推整个 Frankenstein、所有 replacement trigger、或整个 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `frankenstein_german_engineering borrowed base ongoing controller seam` 已从新红灯升为 scoped green：[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed german_engineering 应按控制者而不是真实 owner 在该基地给控制者打出的随从+1指示物` 后，首轮直接坐实 [`frankenstein.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/frankenstein.ts>) 的 `isFrankensteinGermanEngineeringEligible()` 仍只按 `action.ownerId === playerId` 判“你在该基地打出随从后”的归属；`ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `German Engineering` 在 P0 真实 `PLAY_MINION` 链上完全不给 `m1` 加指示物。现已最小修复为：eligibility 统一改按 `metadata.sourceControllerId ?? ownerId` 判控制者，不扩到 `grave_situation`、`uberserum` 以外 sibling。验证：focused `newFactionAbilities.test.ts --testNamePattern "borrowed german_engineering|德国工程学：在该基地打出随从后应给该随从\+1指示物"` => `1 file passed, 2 passed`；相邻 `reactionQueueEventPlayerContext.test.ts --testNamePattern "German Engineering"` => `1 file passed, 1 passed`；`git diff --check -- frankenstein.ts + newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `German Engineering` 的 base ongoing controller seam，不外推所有 Frankenstein、所有 onMinionPlayed base ongoing、或整个 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `frankenstein_uberserum borrowed attached-action sourceController/runtime controller seam` 已从新红灯升为 scoped green，而且这次先后坐实了 queue 与 callback 两层缺口：[`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Frankenstein Uberserum 应按控制者而不是真实 owner 在控制者回合开始给宿主加 1 标记` 后，首轮直接证明 borrowed attached action 连 queued trigger 都没有；原因不在 `Uberserum` 本体，而是 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 的 `locateSources()` 对 attached action 仍把 `controllerId` 固定成 `attached.ownerId`。补完这层后，第二轮又直接坐实 [`frankenstein.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/frankenstein.ts>) 的 `frankenstein_uberserum` callback 仍只按 `action.ownerId === playerId` 判“谁的回合开始加标记”，导致 executor 只吐 `TRIGGER_CONSUMED` 不加 counter。现已最小修复为：1) `locateSources()` 的 attached action `controllerId` 改按 `metadata.sourceControllerId ?? sourcePlayerId ?? ownerId`；2) `frankenstein_uberserum` callback 也改按同一口径判控制者；`hostControllerId` 保持宿主控制者，不把 attached action 默认 sourceController 误改成 host controller。验证：focused `reactionQueueEventPlayerContext.test.ts --testNamePattern "borrowed Frankenstein Uberserum|sourceController queued onTurnStart trigger 仍应只在拥有者回合开始让 Frankenstein Uberserum 给宿主加 1 标记|German Engineering"` => `1 file passed, 3 passed`；`git diff --check -- frankenstein.ts + ongoingEffects.ts + reactionQueueEventPlayerContext.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Uberserum` 的 attached-action sourceController/runtime controller seam，不外推所有 attached action、所有 onTurnStart trigger、或整个 Frankenstein 已完成。
- 当前最新已验证 seam：
  - `borrowed fairies_enchantment` 的二段 resolve/runtime attach provenance 已从新红灯升为 scoped green：[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed fairies_enchantment 选择 -1 模式后仍应更新 metadata 并保留 sourcePlayerId` 后，首轮直接坐实 [`fairies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/fairies.ts>) 的 `fairiesEnchantmentPromptProgram.onResolve()` 只发出 `SYS_INTERACTION_RESOLVED`，不会重发带 metadata 的 `ONGOING_ATTACHED`。最小根因有两层：prompt context 没带原卡 `uid`，二段 resolve 只能用 `action.ownerId === playerId` 回捞宿主 ongoing；借打他人拥有的 `fairies_enchantment` 时这条 lookup 直接回空。其次，即便能找到 attached action，旧回写事件也不会把 borrowed `sourcePlayerId` 带回 `ONGOING_ATTACHED`。现已最小修复为：`fairiesEnchantmentProgram` 把 `attachedCardUid: ctx.cardUid` 带进 continuation；二段 resolve 按 `uid` 精确找回当前 ongoing，并从 `attached.metadata.sourcePlayerId/sourceControllerId` 恢复 borrowed provenance 后写回新的 `ONGOING_ATTACHED`。验证：focused `newFactionAbilities.test.ts --testNamePattern "fairies_enchantment 选择 -1 模式后会写入 metadata 并降低基地上随从力量|borrowed fairies_enchantment 选择 -1 模式后仍应更新 metadata 并保留 sourcePlayerId|fairies_enchantment 在丛林之灵在场时会先执行已选分支，再给剩余分支与跳过并记录 both 模式"` => `1 file passed, 3 passed`；相邻 `newFactionAbilities.test.ts --testNamePattern "fairies_enchantment|fairies_ladybug|fairies_tinx"` => `1 file passed, 5 passed`；`git diff --check -- fairies.ts + newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 `borrowed fairies_enchantment` 的二段 resolve lookup/provenance seam，不外推所有 Fairies、所有二段 prompt、所有 borrowed ongoing attach 或整个 owner/source 主线已完成。
- 当前最新已验证 seam：
  - `effect-contract queued interaction / The Bride targetUid+selectedTargetUids footprint seam` 已从新红灯升为 scoped green：[`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate `continuationContext.selectedTargetUids 数组应映射到多张 minion footprint，而不是只记当前第二次分支选中的目标` 后，首轮直接坐实 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 只写出 `titan:bride-titan`，既漏了 `selectedTargetUids=['first-target-a']`，也没把当前 option 的 `targetUid='second-target-a'` 记进 footprint。最小根因不是 generic `*Uid` 漏网，而是 `sourceId='titan_frankenstein_the_bride_start_choose_target'` 这条 prompt 用 source-specific 的 `targetUid/selectedTargetUids` 表达 live 目标占用面。现已只在 `deriveFootprintFromInteraction(...)` 为该 `sourceId` 补 source-specific 映射：把 option/continuation/runtime continuation 里的 `targetUid` 与 `selectedTargetUids` 记为 `minion`，并把 `titanUid` 额外补成 `cardInstance` witness；不扩到 generic `targetUid` 或其它 Titans prompt。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "continuationContext.selectedTargetUids 数组应映射到多张 minion footprint，而不是只记当前第二次分支选中的目标|continuationContext.playedHandUids 数组应映射到多张 cardInstance footprint，而不是只记当前第二次手牌候选"` => `1 file passed, 2 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 68 passed`；`git diff --check -- reactionResources.ts + reactionQueueOrdering.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 `The Bride` 这条 source-specific queued interaction footprint seam，不外推 generic `targetUid`、所有 Titans prompt 或整个 `reactionResources` family 已完成。
- 当前最新已验证 seam：
  - `world_champs_bewitched destroy->discard duplicate queued-transfer seam` 已从 open sibling 升为 scoped green：旧 [`world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `worldChampsBewitchedTransferOnLeave()` 同时挂在 `onMinionDestroyed + onMinionDiscardedFromBase`，而 `processDestroyTriggers()` 会在 destroy 真链里先后排这两拍；新增 focused gate `world_champs_bewitched 在 destroy -> processDestroyTriggers 真链里不应因同一宿主同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双起转移交互` 后，首轮直接坐实同一宿主会排出两条 `sourceDefId='world_champs_bewitched'` 的 queued trigger。第一刀只在 callback 补 discard guard 没有效果，反证重复发生在 queue 阶段而不是执行阶段；随后最小修复为：保留 callback guard，同时新增 `canQueueWorldChampsBewitchedLeaveTrigger()` 并挂到 `world_champs_bewitched` 的 trigger 注册上，让 `timing==='onMinionDiscardedFromBase' && sourceEventId =~ /^minion-discarded-from-base:/` 时直接不入队，不扩到 return-to-hand fallback、prompt handler 或其它 World Champs sibling。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_bewitched 在 destroy -> processDestroyTriggers 真链里不应因同一宿主同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双起转移交互|world_champs_bewitched 宿主回手时应给行动控制者创建 per-instance 转移交互"` => `1 file passed, 2 passed`。该格只闭合 `Bewitched` 的 destroy/discard duplicate queued-transfer seam，不外推整个 World Champs、所有 attached-action leave trigger 或整个 destroy/discard family 已完成。
  - `world_champs_samurai_chan destroy->discard duplicate draw seam` 已从 open sibling 升为 scoped green：旧 [`world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `worldChampsSamuraiChanTrigger()` 同时挂在 `onMinionDestroyed + onMinionDiscardedFromBase`，而 callback 本体没有区分 `processDestroyTriggers()` 派生出来的 discard trigger；新增 focused gate `world_champs_samurai_chan 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽` 后，首轮直接红灯坐实同一张 `Samurai Chan` 会产出两条 `CARDS_DRAWN`。随后最小修复为：补 `isDestroyPipelineDiscardTrigger(ctx)`，并让 `worldChampsSamuraiChanTrigger()` 在 `timing==='onMinionDiscardedFromBase' && sourceEventId =~ /^minion-discarded-from-base:/` 时跳过 discard 分支，把 draw 只留给 `onMinionDestroyed` 一次；不扩到 `Bewitched / Mummy / Sheriff` 或其它 World Champs sibling。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_samurai_chan 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|world_champs_samurai_chan 因基地计分从场上进入弃牌堆后会抽一张牌"` => `1 file passed, 2 passed`。该格只闭合 `Samurai Chan` 的 destroy/discard duplicate draw seam，不外推整个 World Champs、所有 self-source dual-timing trigger 或整个 destroy/discard family 已完成。
  - `giant_ants_death_on_six_legs destroy->discard duplicate counter seam` 已从 open sibling 升为 scoped green：旧 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `giantAntsDeathOnSixLegsBeforeDiscard()` 同时挂在 `onMinionDestroyed + onMinionDiscardedFromBase`，而 callback 本体没有区分 `processDestroyTriggers()` 派生出来的 discard trigger，导致同一名己方随从在 destroy -> owner discard 真链里会给 `Death on Six Legs` 泰坦加两次指示物。现已在 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `giant_ants_death_on_six_legs 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双加指示物`，首轮直接红灯坐实同一批事件会产出两条 `TITAN_POWER_COUNTER_ADDED(reason='giant_ants_death_on_six_legs')`。随后最小修复为：`giantAntsDeathOnSixLegsBeforeDiscard()` 增加 `isDestroyPipelineDiscardTrigger(ctx)` 守卫，在 `timing==='onMinionDiscardedFromBase' && sourceEventId =~ /^minion-discarded-from-base:/` 时跳过 discard 分支，把 destroy 真链的加标记只留给 `onMinionDestroyed` 一次；不扩到 `special/talent`、其它 Titan 或其它 Giant Ants sibling。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "giant_ant_worker_pod 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双重 replay|giant_ants_death_on_six_legs 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双加指示物"` => `1 file passed, 2 passed`。该格只闭合 `Death on Six Legs` 的 destroy/discard duplicate counter seam，不外推整个 Giant Ants、所有 Titan trigger 或整个 destroy/discard family 已完成。
  - `giant_ant_worker_pod destroy->discard duplicate replay seam` 已从 open sibling 升为 scoped green：旧 [`giant_ants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/giant_ants.ts>) 的 `giantAntWorkerPodReplayTrigger()` 同时挂在 `onMinionDestroyed + onMinionDiscardedFromBase`，而 callback 本体没有区分 `processDestroyTriggers()` 派生出来的 discard trigger，导致同一只 `giant_ant_worker_pod` 在 destroy -> owner discard 真链里会排出两次 replay source。现已在 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `giant_ant_worker_pod 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双重 replay`，首轮直接红灯坐实 `TRIGGER_QUEUED` 里同一 `sourceDefId='giant_ant_worker_pod'` 会排两次。随后最小修复为：`giantAntWorkerPodReplayTrigger()` 增加 `isDestroyPipelineDiscardTrigger(ctx)` 守卫，在 `timing==='onMinionDiscardedFromBase' && sourceEventId =~ /^minion-discarded-from-base:/` 时跳过 discard 分支，把 destroy 真链的 replay 只留给 `onMinionDestroyed` 一次；不扩到 `giant_ants_death_on_six_legs`、`drone`、`who_wants_to_live_forever` 或其它 Giant Ants sibling。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "giant_ant_worker_pod 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双重 replay|giant_ant_worker_pod 在宿主进入弃牌堆后仍会通过 queued discard trigger 提示从弃牌堆打到另一基地"` => `1 file passed, 2 passed`。该格只闭合 `Worker POD` 的 destroy/discard duplicate replay seam，不外推整个 Giant Ants、所有 dual-timing trigger 或整个 destroy/discard family 已完成。
  - `bear_cavalry_bear_necessities_pod borrowed turn-start self-destruct still keyed by ownerId` 又命中一条新的 Bear Cavalry turn-boundary seam，而且这次不是 queued metadata，而是 `playerContext:'sourceController'` 已经转绿后，runtime callback 本体仍只按 true owner 判“是不是你的下回合开始”。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 的 `bearCavalryBearNecessitiesPodTurnStart()` 仍直接用 `a.ownerId === ctx.playerId` 过滤要自毁的口粮 POD，因此 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed 口粮 POD 在 P0 回合开始时不会 detach。现已在 [`newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增 focused gate `borrowed bear_cavalry_bear_necessities_pod 应按控制者而不是真实 owner 在下回合开始时自毁`，首轮直接红灯坐实 P0 回合开始没有任何 `ONGOING_DETACHED`。随后最小修复为：`bearCavalryBearNecessitiesPodTurnStart()` 改按 `a.metadata?.sourceControllerId ?? a.ownerId` 判控制者，detach 仍保留真实 `ownerId` 进弃牌堆，不扩到 restriction、额外出牌限制、`Superiority POD` 或其它 Bear Cavalry sibling。验证：focused `newOngoingAbilities.test.ts --testNamePattern "bear_cavalry_bear_necessities_pod 应按控制者而不是真实 owner 在下回合开始时自毁|拥有者下回合开始时会销毁已激活的口粮POD|bear_cavalry_bear_necessities_pod 限制"` => 首轮 `1 failed, 3 passed`，修复后 `1 file passed, 4 passed`。该格只闭合 borrowed `Bear Necessities POD` 的 turn-start self-destruct controller seam，不外推所有 Bear Cavalry、所有 onTurnStart trigger、所有 POD ongoing self-destruct，或整个 owner/source 主线已完成。
  - `bear_cavalry_superiority / bear_cavalry_superiority_pod borrowed protection + pod alias override still keyed by ownerId` 又命中两条新的 Bear Cavalry protection seam，而且这次不只是 borrowed controller 问题，还连带翻出 `_pod` 自动别名覆盖显式 POD protection 的旧行为缝。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 的 `bearCavalrySuperiorityChecker()` 与 `bearCavalrySuperiorityPodProtection()` 仍只按 `ownerId` 判“你的随从”；[`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `TURN_STARTED` 对 ongoing/attached `talentUsed` 清理也仍只读 `ownerId`；与此同时 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 的 protection `_pod` 自动别名即使在 `bear_cavalry_superiority_pod` 已显式注册 protection 的前提下，仍会把普通版 `bear_cavalry_superiority` checker 继承到 `_pod`，导致 `protect=false` 或 `talentUsed=false` 时也会继续被普通版逻辑兜底保护。现已在 [`newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增 focused gates `borrowed bear_cavalry_superiority 应按控制者而不是真实 owner 保护己方随从` 与 `borrowed bear_cavalry_superiority_pod 的 protect 模式也应按控制者而不是真实 owner 保护己方随从`，首轮直接红灯坐实 borrowed controller seam；随后扩大到 `bear_cavalry_superiority 保护|superiority_pod 保护模式` 子集，又坐实旧 `protect/draw` 生命周期本体也坏着：`protect` 分支在控制者下回合开始后仍不失效，`draw` 分支仍继续保护。随后最小修复为：`bearCavalrySuperiorityChecker()` 与 `bearCavalrySuperiorityPodProtection()` 都统一按 `metadata.sourceControllerId ?? ownerId` 判控制者；`reduce.ts` 的 `TURN_STARTED` 对 ongoing/attached `talentUsed` 清理也改按同一口径；`ongoingEffects.ts` 的 protection `_pod` alias 生成收紧为“只要 POD 已显式注册同类型 protection，就不再继承普通版 alias”。验证：focused `newOngoingAbilities.test.ts --testNamePattern "borrowed bear_cavalry_superiority|bear_cavalry_superiority 保护|superiority_pod 保护模式"` => 首轮 `2 failed, 4 passed`，修复后 `1 file passed, 6 passed`；`git diff --check -- src/games/smashup/abilities/bear_cavalry.ts src/games/smashup/domain/reduce.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Superiority` protection seam 与 `Superiority POD` protect/draw 生命周期 seam，不外推所有 Bear Cavalry、所有 `_pod` alias、所有 protection family，或整个 owner/source 主线已完成。
  - `bear_cavalry_high_ground borrowed destroy trigger still keyed by ownerId` 又命中一条新的 runtime trigger seam，而且这次不是 POD queued 路径，而是非 POD `High Ground` 的 direct `onMinionMoved` trigger 仍在按 true owner 判敌我与 destroyer。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 在 `bearCavalryHighGroundTrigger()` 中直接用 `ongoing.ownerId` 比较 `movedMinion.controller`，也按 `ongoing.ownerId` 检查基地上是否有“己方”随从、并把 destroyer 写成 `ongoing.ownerId`；因此 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `bear_cavalry_high_ground` 面对 P1 移入随从时不会触发消灭。现已在 [`newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增 focused gate `borrowed bear_cavalry_high_ground 应按控制者而不是真实 owner 消灭移入的对手随从`，首轮直接红灯坐实没有 `MINION_DESTROYED`。随后最小修复为：`bearCavalryHighGroundTrigger()` 统一按 `ongoing.metadata?.sourceControllerId ?? ongoing.ownerId` 判控制者、基地上“己方”随从和 destroyer，不扩到 `High Ground POD / Superiority / Bear Necessities` 等 sibling，也不重构全局 ongoing schema。验证：focused `newOngoingAbilities.test.ts --testNamePattern "borrowed bear_cavalry_high_ground|bear_cavalry_high_ground 触发|High Ground"` => 首轮 failed 后 `1 file passed, 3 passed`；`git diff --check -- src/games/smashup/abilities/bear_cavalry.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `High Ground` 非 POD destroy trigger seam，不外推所有 Bear Cavalry、所有 onMinionMoved trigger、所有 destroyer 归属，或整个 owner/source 主线已完成。
  - `bear_cavalry_bear_necessities borrowed opponent-action target collection still keyed by ownerId` 又命中一条新的 runtime target-selection seam，而且这次不是 queued trigger，而是 Black Bear Cavalry 的“对手行动”候选生成与单目标直执行旁路仍在按 true owner 判“是不是其他玩家的行动”。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 的 `collectBearNecessitiesTargets()` 与 `bearCavalryBearNecessities()` 单目标 actionTargets 旁路都只看 `a.ownerId/o.ownerId !== playerId`，因此 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed ongoing 会被错误当成对手行动自动 detach。现已在 [`onDestroyAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/onDestroyAbilities.test.ts>) 新增 focused gate `bear_cavalry_bear_necessities 不应把自己控制但真实 owner 不同的 borrowed ongoing 当成对手行动目标`，首轮直接红灯坐实唯一 borrowed ongoing 会被 `ONGOING_DETACHED`。随后最小修复为：`collectBearNecessitiesTargets()` 与 `bearCavalryBearNecessities()` 的单目标 actionTargets 旁路都统一按 `metadata.sourceControllerId ?? ownerId` 判行动控制者，不扩到 `High Ground / Superiority / Bear Necessities POD` 等 sibling，也不重构全局 ongoing schema。验证：focused `onDestroyAbilities.test.ts --testNamePattern "bear_cavalry_bear_necessities 不应把自己控制但真实 owner 不同的 borrowed ongoing 当成对手行动目标|onDestroy 基础设施|bear_necessities"` => 首轮 failed 后 `1 file passed, 3 passed`；`git diff --check -- src/games/smashup/abilities/bear_cavalry.ts src/games/smashup/__tests__/onDestroyAbilities.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Bear Necessities` 的 opponent-action target collection seam，不外推所有 Bear Cavalry、所有 destroy action、所有 opponent-action candidate builder，或整个 owner/source 主线已完成。
  - `steampunk_change_of_venue / steampunk_captain_ahab borrowed candidate generation still keyed by ownerId` 又命中两条新的 Steampunks sibling seam，而且这次不是 queued metadata 或 onPlay destroy，而是 prompt 候选生成与 `validateUse` 入口仍只按 true owner 判“你的持续行动/你的目标基地”。旧 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 的 `buildChangeOfVenueOngoingOptions()` 只把 `ownerId === playerId` 的 base ongoing / attached ongoing 放进候选，因此 `change_of_venue` 面对 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed ongoing 时甚至起不来选择 prompt；`buildCaptainAhabBaseOptions()` 与 `registerAbilityProgram('steampunk_captain_ahab').validateUse` 也都只看 `ownerId`，因此拥有 borrowed ongoing 的基地不会被亚哈船长视为合法目标。现已在 [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增两个 focused gate：`steampunk_change_of_venue: 应允许取回由自己控制但真实 owner 不同的 borrowed ongoing` 与 `borrowed ongoing 也应被视为 captain_ahab 的可选目标基地`，首轮分别直接红灯坐实“没有 `steampunk_change_of_venue` prompt”和“没有 `MINION_MOVED` 目标”。随后最小修复为：`buildChangeOfVenueOngoingOptions()`、`buildCaptainAhabBaseOptions()` 与 `captain_ahab.validateUse` 统一按 `metadata.sourceControllerId ?? ownerId` 识别 ongoing 控制者，不扩到 replay owner 归属、回手归属或其它 Steampunks sibling，也不重构全局 ongoing schema。验证：focused `expansionOngoing.test.ts --testNamePattern "borrowed ongoing 也应被视为 captain_ahab 的可选目标基地|由自己控制但真实 owner 不同的 borrowed ongoing|captain_ahab|change_of_venue"` => 首轮 failed 后 `1 file passed, 10 passed`；扩大到 `ornate_dome + change_of_venue + captain_ahab + steampunk_mechanic_target: 目标基地被对手 ornate_dome 封锁时不再附着` 子集 => `1 file passed, 15 passed`；`npx eslint src/games/smashup/abilities/steampunks.ts src/games/smashup/__tests__/expansionOngoing.test.ts` => `0 errors`；`git diff --check -- src/games/smashup/abilities/steampunks.ts src/games/smashup/__tests__/expansionOngoing.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Change of Venue` 的候选生成与 borrowed `Captain Ahab` 的目标基地判定 seam，不外推 replay 回手归属、所有 Steampunks sibling、所有 prompt candidate builder 或整个 owner/source 主线已完成。
  - `steampunk_ornate_dome / borrowed restriction + onPlay consumer still keyed by ownerId` 又命中一条新的 runtime restriction/onPlay seam，而且这次不是 queued metadata，而是 Steampunks 本体的 restriction consumer 与 onPlay destroy branch 仍在按 true owner 判“谁是其他玩家”。旧 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 的 `steampunkOrnateDomeChecker()` 只看 `dome.ownerId`，因此 borrowed `steampunk_ornate_dome(ownerId='1', metadata.sourceControllerId='0')` 会错误放行 P1 在该基地打行动；`steampunkOrnateDomeOnPlay()` 也只看 `ongoing.ownerId/a.ownerId`，因此同控制者的 borrowed ally ongoing 会被误当成“其他玩家行动”一起摧毁。现已在 [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增两个 focused gate：`borrowed ornate_dome 应按控制者而不是真实 owner 限制其他玩家打行动` 与 `borrowed ornate_dome onPlay 应按控制者而不是真实 owner 仅摧毁其他玩家行动`，首轮分别直接红灯坐实 `false !== true` 与误 detach `ally-borrowed`。随后最小修复为：`steampunkOrnateDomeChecker()`、`steampunkOrnateDomeOnPlay()` 的 base ongoing / attached action 分支都统一按 `metadata.sourceControllerId ?? ownerId` 判控制者，不扩到 `change_of_venue / captain_ahab / infiltrate` 等 sibling，也不重构全局 ongoing schema。验证：focused `expansionOngoing.test.ts --testNamePattern "borrowed ornate_dome|steampunk_ornate_dome: 华丽穹顶限制|华丽穹顶限制"` => 首轮 failed 后 `1 file passed, 4 passed`；扩大到相邻 `steampunk_mechanic_target: 目标基地被对手 ornate_dome 封锁时不再附着` 的子集 => `1 file passed, 5 passed`；`npx eslint src/games/smashup/abilities/steampunks.ts src/games/smashup/__tests__/expansionOngoing.test.ts` => `0 errors`；`git diff --check -- src/games/smashup/abilities/steampunks.ts src/games/smashup/__tests__/expansionOngoing.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Ornate Dome` 的 restriction/onPlay controller seam，不外推所有 Steampunks、所有 restriction、所有 onPlay destroy 分支，或整个 owner/source 主线已完成。
  - `commands.ts / USE_TALENT ongoing validation still keyed by ownerId` 又命中一条新的 runtime consumer seam，而且 live state 已经持久化 `metadata.sourceControllerId` 却仍在命令验证层被 true owner 挡掉。旧 [`commands.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/commands.ts>) 在 `USE_TALENT` 校验 ongoing 天赋时直接用 `ongoing.ownerId !== command.playerId` 判“只能使用自己的持续行动卡天赋”，因此 borrowed ongoing 即使 live metadata 已表明控制者是当前玩家，也会被拒绝。现已在 [`commandsValidation.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/commandsValidation.test.ts>) 新增 focused gate `allows borrowed ongoing talent when metadata.sourceControllerId matches the acting player`，用 `trickster_hideout_pod(ownerId='1', metadata.sourceControllerId='0')` 坐实这类 talent 会被命令验证层错误挡掉；随后最小修复为：`USE_TALENT` ongoing 分支优先按 `ongoing.metadata?.sourceControllerId ?? ongoing.ownerId` 识别控制者，不扩到 `USE_TALENT` 以外的命令族，也不重构整体 command schema。验证：focused `commandsValidation.test.ts --testNamePattern "allows borrowed ongoing talent when metadata.sourceControllerId matches the acting player|trickster_hideout_pod talent|持续行动卡天赋"` => `1 file passed, 2 passed`；`npx eslint src/games/smashup/domain/commands.ts src/games/smashup/__tests__/commandsValidation.test.ts` => `0 errors, 1 existing warning`。该格只闭合 borrowed ongoing talent 的 command-validation controller seam，不外推所有 ongoing talent、所有 command validator、所有 borrowed runtime consumer，或整个 owner/source 主线已完成。
  - `steampunk_aggromotive / borrowed ongoing base-power modifier still keyed by ownerId` 又命中一条新的 deeper seam，而且这次不是 queue metadata，而是 modifier/runtime consumer 本体仍在按 true owner 计总力量。旧 [`ongoing_modifiers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ongoing_modifiers.ts>) 的 `registerBasePowerModifier('steampunk_aggromotive', ...)` 只看 `ctx.ongoing.ownerId === ctx.playerId`，因此真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0' })` 后，即使 P0 控制 borrowed `steampunk_aggromotive` 且自己在该基地有随从，`getPlayerEffectivePowerOnBase(..., '0')` 仍只会得到 `2`，不会吃到应有的 `+5`。现已在 [`ongoingModifiers.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ongoingModifiers.test.ts>) 新增 focused gate `borrowed steampunk_aggromotive 应按控制者而不是真实 owner 给基地总力量 +5`，首轮直接红灯坐实 `2 !== 7`；随后最小修复为：`steampunk_aggromotive` 改按 `ctx.ongoing.metadata?.sourceControllerId ?? ctx.ongoing.ownerId` 识别 ongoing 控制者，并在 [`ongoingModifiers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingModifiers.ts>) 放宽 `BasePowerModifierContext.ongoing` 类型，允许 modifier consumer 读取 metadata，而不扩到其它 Steampunks sibling 或全局 ongoing schema 重构。验证：focused `ongoingModifiers.test.ts --testNamePattern "borrowed steampunk_aggromotive|蒸汽机车|aggromotive"` => 首轮 failed 后 `1 file passed, 5 passed`；邻近 [`steampunk-aggromotive-fix.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/steampunk-aggromotive-fix.test.ts) 整文件 => `1 file passed, 8 passed`；`npx eslint src/games/smashup/abilities/ongoing_modifiers.ts src/games/smashup/domain/ongoingModifiers.ts src/games/smashup/__tests__/ongoingModifiers.test.ts` => `0 errors`；`git diff --check -- src/games/smashup/abilities/ongoing_modifiers.ts src/games/smashup/domain/ongoingModifiers.ts src/games/smashup/__tests__/ongoingModifiers.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Aggromotive` 的 base-total-power modifier controller seam，不外推所有 Steampunks、所有 base power modifier、所有 ongoing consumer，或整个 owner/source 主线已完成。
  - `cthulhu_complete_the_ritual / borrowed ongoing runtime base-reset still keyed by ownerId` 又命中一条新的 deeper seam，而不是回扫 2026-05-22 已闭合的 turn-start metadata 格。2026-05-22 收掉的只是 [`registerTrigger('cthulhu_complete_the_ritual','onTurnStart',...)`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:293>) 缺 `playerContext:'sourceController'`，保证 queued trigger 只在控制者回合开始入队；但进入 runtime 后，[`cthulhuCompleteTheRitualTrigger()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:624>) 仍按 `a.ownerId === ctx.playerId` 查找仪式本体，因此真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0' })` 后，P0 回合开始虽然能正确入队，但 `maybeResolveReactionQueue(...)` 只会返回 `TRIGGER_CONSUMED`，不会真的清场、把卡送回牌库底并换基地。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:578>) 新增 focused gate `queued onTurnStart trigger 处理 borrowed cthulhu_complete_the_ritual 时，仍应按控制者回合开始清场并换基地`，首轮直接坐实 resolved events 只有 `TRIGGER_CONSUMED`。随后最小修复为：`cthulhuCompleteTheRitualTrigger()` 优先按 `ctx.sourceCardUid` 精确定位 queued source，fallback 再按 `ctx.sourceControllerId ?? ctx.playerId` 识别控制者对应的仪式实例；不扩到其它 Cthulhu sibling，也不把整组 base-reset helper 做大重构。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后纳入与 `cthulhu_altar / cthulhu_furthering_the_cause / zombie_overrun / difference_engine` 的同组聚焦验证 `1 file passed, 5 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 33 passed`；既有 metadata gate `reactionQueueEventPlayerContext.test.ts -t "Cthulhu Complete the Ritual|Furthering the Cause|Cthulhu Altar|Zombie Overrun"` => `1 file passed, 6 passed`；既有 direct behavior gate `newOngoingAbilities.test.ts -t "complete_the_ritual|完成仪式"` => `1 file passed, 2 passed`；`git diff --check -- cthulhu.ts + reactionQueueSourceRuntimeContext.test.ts` 通过（仅 LF/CRLF warning）。该格只闭合 borrowed `Complete the Ritual` 的 runtime base-reset controller seam，不外推所有 Cthulhu、所有 `onTurnStart` trigger、所有 base reset / base replace 链，或整个 owner/source 主线已完成。
  - `cthulhu_furthering_the_cause / borrowed ongoing runtime opponent+vp recipient still keyed by ownerId` 又命中一条新的 deeper seam，而不是回扫已有的 queued metadata gate。当前 [`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:284>) 已给 `cthulhu_furthering_the_cause` 的 `onTurnEnd` trigger 注册 `playerContext:'sourceController'`，现有 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts:6216>) 也已锁住只有控制者回合结束才入队；但进入 runtime 后，[`cthulhuFurtheringTheCauseTrigger()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:876>) 仍按 `ongoing.ownerId` 判“这一回合有没有对手随从被消灭”，并把 `VP_AWARDED.playerId` 发给 true owner。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:518>) 新增 focused gate `queued onTurnEnd trigger 处理 borrowed cthulhu_furthering_the_cause 时，仍应按控制权判定对手并把 VP 给控制者`，首轮直接坐实 resolved events 只有 `TRIGGER_CONSUMED`、没有 `VP_AWARDED`。随后最小修复为：`cthulhuFurtheringTheCauseTrigger()` 统一改按 `const controllerId = ctx.sourceControllerId ?? ongoing.ownerId` 判“对手”与 VP 归属；不扩到其它 Cthulhu sibling，也不重构 destroy ledger 或 base ongoing 全局 owner/controller 字段。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 32 passed`；既有 metadata gate `reactionQueueEventPlayerContext.test.ts -t "cthulhu_furthering_the_cause|Furthering the Cause|深化目标"` => `1 file passed, 2 passed`；既有行为 gate `newOngoingAbilities.test.ts -t "cthulhu_furthering_the_cause|Furthering the Cause|深化目标"` => `1 file passed, 6 passed`；ESLint `cthulhu.ts + reactionQueueSourceRuntimeContext.test.ts` => `0 errors`。该格只闭合 borrowed `Furthering the Cause` 的 runtime opponent/recipient controller seam，不外推所有 Cthulhu、所有 `onTurnEnd` trigger、所有 VP 结算分支，或整个 owner/source 主线已完成。
  - `zombie_overrun / borrowed ongoing runtime self-destruct still keyed by ownerId` 又命中一条新的 deeper seam，而不是回扫已有的 queued metadata gate。当前 [`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts:133>) 已给 `zombie_overrun` / `zombie_overrun_pod` 的 `onTurnStart` trigger 注册 `playerContext:'sourceController'`，现有 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts:5414>) 也已锁住只有控制者回合开始才入队；但进入 runtime 后，[`zombieOverrunSelfDestruct()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts:964>) 仍按 `overrun.ownerId !== ctx.playerId` 判“是不是你的回合开始”，因此真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0' })` 后，P0 回合开始时 borrowed `zombie_overrun` 只会 `TRIGGER_CONSUMED`，不会真的自毁。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:454>) 新增 focused gate `queued onTurnStart trigger 处理 borrowed zombie_overrun 时，仍应按控制者回合自毁并把 detached card 送回真实 owner discard`，首轮直接坐实 resolved events 只有 `TRIGGER_CONSUMED`、没有 `ONGOING_DETACHED`。随后最小修复为：`zombieOverrunSelfDestruct()` 统一改按 `const controllerId = ctx.sourceControllerId ?? overrun.ownerId` 判断“是不是这张 ongoing 的控制者回合开始”；`ONGOING_DETACHED.ownerId` 继续保留真实 owner，不扩到 Restriction、其它 Zombies sibling 或 base ongoing 全局 owner/controller 字段。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 31 passed`；既有 metadata gate `reactionQueueEventPlayerContext.test.ts -t "zombie_overrun|Zombie Overrun|泛滥横行"` => `1 file passed, 2 passed`；既有行为 gate `zombieInteractionChain.test.ts -t "zombie_overrun|泛滥横行|overrun"` => `1 file passed, 2 passed`；ESLint `zombies.ts + reactionQueueSourceRuntimeContext.test.ts` => `0 errors`。该格只闭合 borrowed `Zombie Overrun` 的 runtime self-destruct controller seam，不外推所有 Zombies、所有 `onTurnStart` trigger、所有 base ongoing self-destruct，或整个 owner/source 主线已完成。
  - `cthulhu_altar / borrowed ongoing runtime eligibility+callback still keyed by ownerId` 又命中一条新的 deeper seam，而不是回扫 2026-05-22 已闭合的 collector eligibility 格。2026-05-22 收掉的只是“对手打到祭坛基地时不应先排空 trigger”这一层：通过 `sourceScope:'triggerBase' + canTrigger` 把非 owner 打随从的空 queue 挡在 collector 前；但进入 borrowed 场景后，[`isCthulhuAltarEligibleForMinionPlayed()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:827>) 与 [`cthulhuAltarTrigger()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts:840>) 仍按 `ongoing.ownerId` 判“你在这里打了随从”，因此真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0' })` 后，P0 在该基地打出随从时根本不会为 borrowed `cthulhu_altar` 入队。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:390>) 新增 focused gate `queued onMinionPlayed trigger 处理 borrowed cthulhu_altar 时，仍应按控制者判定并把额外行动给控制者`，首轮直接坐实 `collectTriggers(onMinionPlayed)` 返回 `undefined`。随后最小修复为：`isCthulhuAltarEligibleForMinionPlayed()` 与 `cthulhuAltarTrigger()` 都统一改按 `const controllerId = ctx.sourceControllerId ?? ongoing.ownerId` 判控制者；注册再补 `playerContext:'sourceController'`，把“你在这里打随从”这条语义固定到控制者上下文，而不是真实 owner。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 30 passed`；既有 collector gate `reactionQueueEventPlayerContext.test.ts -t "cthulhu_altar|祭坛|altar"` => `1 file passed, 1 passed`；既有 direct behavior gate `newOngoingAbilities.test.ts -t "cthulhu_altar|祭坛|altar"` => `1 file passed, 2 passed`；ESLint `cthulhu.ts + reactionQueueSourceRuntimeContext.test.ts` => `0 errors`。该格只闭合 borrowed `Cthulhu Altar` 的 runtime eligibility/callback controller seam，不外推所有 Cthulhu、所有 `onMinionPlayed` trigger、所有 contextual extra action，或整个 owner/source 主线已完成。
  - `steampunk_difference_engine / borrowed ongoing runtime callback still keyed by ownerId` 又命中一条新的 deeper seam，而不是回扫 2026-05-22 已闭合的 turn-boundary metadata 格。2026-05-22 收掉的只是 [`registerTrigger('steampunk_difference_engine','onTurnEnd',...)`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts:1058>) 缺 `playerContext:'sourceController'`，保证 queued trigger 只在控制者回合结束入队；但进入 runtime 后，[`steampunkDifferenceEngineTrigger()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts:420>) 仍按 `ongoing.ownerId` 判断“你”是谁、看哪一方在该基地有随从、以及把抽牌发给谁。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:308>) 新增 focused gate `queued onTurnEnd trigger 处理 borrowed steampunk_difference_engine 时，仍应按控制者判定并让控制者抽牌`，用真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0', targetBaseIndex:0 }) -> collectTriggers(onTurnEnd) -> maybeResolveReactionQueue` 链首轮坐实：queued metadata 已经是 `ownerPlayerId/sourceControllerId='0'`，但 callback 仍只返回 `TRIGGER_CONSUMED`，不会给 P0 产出 `CARDS_DRAWN`。随后最小修复为：`steampunkDifferenceEngineTrigger()` 统一改按 `const controllerId = ctx.sourceControllerId ?? ongoing.ownerId` 判断控制者、检查该控制者在基地上的随从、读取该控制者牌库并写 `CARDS_DRAWN.playerId=controllerId`；不扩到 Steampunks 其它 onTurnEnd sibling，也不重构 base ongoing 全局 owner/controller 字段。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 29 passed`；既有 metadata gate `reactionQueueEventPlayerContext.test.ts -t "Difference Engine"` => `1 file passed, 1 passed`；既有行为 gate `expansionOngoing.test.ts -t "steampunk_difference_engine|差分机|Difference Engine"` => `1 file passed, 3 passed`；ESLint `steampunks.ts + reactionQueueSourceRuntimeContext.test.ts` => `0 errors`。该格只闭合 borrowed `Difference Engine` 的 runtime controller/owner consumer seam，不外推所有 Steampunks、所有 onTurnEnd trigger、所有 base ongoing runtime callback，或整个 owner/source 主线已完成。
  - `bear_cavalry_high_ground_pod / borrowed ongoing persistent controller + callback/prompt/detach owner` 继续沿 `sourceController/eventPlayer` 与 borrowed ongoing owner/source 主线命中一条新的 deeper seam，而不是回扫 2026-05-24 已闭合的 metadata 格。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 在 borrowed `High Ground POD` 上同时存在三层错位：1) [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 的 `locateSources(...)` 对基地 ongoing 只读 `ownerId`，queue metadata 会把 persistent controller 洗回 true owner；2) `bearCavalryHighGroundPodTrigger()` 用 `ongoing.ownerId` 判“是不是对手随从移入”并创建 prompt player，导致 borrowed ongoing 根本不触发或把选择权交错人；3) `bearCavalryHighGroundPodPromptProgram.onResolve()` 把 `ONGOING_DETACHED.ownerId` 直接写成 `playerId`，会把被借打的 POD 错送进行动玩家弃牌堆。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:231>) 新增 focused gate `queued onMinionMoved trigger 处理 borrowed bear_cavalry_high_ground_pod 时，仍应把 prompt 交给控制者并把 detached card 送回真实 owner discard`，用真实 `reduce(ONGOING_ATTACHED{ ownerId:'1', sourcePlayerId:'0' }) -> collectTriggers(onMinionMoved) -> maybeResolveReactionQueue -> SYS_INTERACTION_RESPOND(destroy)` 链首轮坐实 prompt 不会给 P0。随后最小修复为：`reduce.ts` 在 base ongoing attach 时持久化 `metadata.sourcePlayerId/sourceControllerId`；`ongoingEffects.ts` 的 `locateSources(...)` 对基地 ongoing 优先读该 metadata 作为 controller；`bear_cavalry.ts` 的 High Ground POD callback 改按 `ctx.sourceControllerId ?? ongoing.ownerId` 判断控制者/己方随从、prompt player 与 destroyer，并把 prompt context 里的 `ongoingOwnerId` 与 `playerId` 分离，`onResolve()` 的 `ONGOING_DETACHED.ownerId` 改回真实 owner。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 28 passed`；既有 metadata gate `reactionQueueEventPlayerContext.test.ts -t "High Ground POD|sourceController queued onMinionMoved trigger 应把 High Ground POD 归给制高点拥有者而不是移入随从玩家"` => `1 file passed, 1 passed`；既有行为 gate `newOngoingAbilities.test.ts -t "bear_cavalry_high_ground|高地"` => `1 file passed, 2 passed`；ESLint `bear_cavalry.ts + reduce.ts + ongoingEffects.ts + reactionQueueSourceRuntimeContext.test.ts` => `0 errors, existing warnings only`。该格只闭合 borrowed `High Ground POD` 的 persistent controller / callback / prompt / detach-owner 这条 shared seam，不外推所有 Bear Cavalry、所有基地 ongoing、所有 borrowed ongoing controller 持久化，或整个 owner/source 主线已完成。
  - `fairies.ts / buildTransferAttachedActionEvents(...) -> ONGOING_ATTACHED.sourcePlayerId` 又命中一条新的 shared 真红灯，而不是回扫旧的 mermaids/tornados/world_champs 同型 case。[`fairies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/fairies.ts>) 的 `buildTransferAttachedActionEvents(...)` 会被 `fairies_tinx` 与 `fairies_ladybug/fairies_leaf_armor` 共用，用来把一张附着行动从旧宿主 detach 后转到新宿主；旧实现只保留 `ownerId`，不会在 borrowed 场景补 `sourcePlayerId`。现已在 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts:10505>) 新增 focused gate `fairies_tinx 把 borrowed 附着行动移到自己身上时，仍应保留 sourcePlayerId`，首轮直接坐实 emitted `su:ongoing_attached.payload` 只有 `ownerId='1'`、缺 `sourcePlayerId='0'`；随后只给 helper 增加 `sourcePlayerId` 参数，并仅在 `attached.ownerId !== sourcePlayerId` 时给 `SU_EVENTS.ONGOING_ATTACHED.payload` 补 `sourcePlayerId`，caller 只在 `createTransferSelfAbilityProgram(...).onResolve()` 与 `fairiesTinxPromptProgram.onResolve()` 传入 `context.playerId`。验证：focused `newFactionAbilities.test.ts -t "fairies_tinx 把 borrowed 附着行动移到自己身上时，仍应保留 sourcePlayerId|fairies_ladybug 会让附着随从不能被消灭"` => `1 file passed, 2 passed`。该格只闭合 Fairies attached-action transfer 的 `sourcePlayerId` provenance，不外推整个 Fairies、所有 attached-action transfer/move caller、所有 `ONGOING_ATTACHED` producer 或整个 owner/source 主线已完成。
  - `mermaids_shipwreck_cove / borrowed move-base-ongoing -> ONGOING_ATTACHED.sourcePlayerId` 又命中一条新的 shared 真红灯，而不是回扫旧的 queued owner seam。[`mermaids.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts>) 的 `buildMoveOngoingEvents(...)` 旧实现只会把移动后的 ongoing 重新附着为 `ownerId`，不会在 borrowed 场景补 `sourcePlayerId`；因此 queued `afterScoring` 真链里，当 `sourceControllerId='0'` 移动一张 `ownerId='1'` 的 borrowed `mermaids_shipwreck_cove` 时，emitted `su:ongoing_attached` 只有 `ownerId='1'`，缺 `sourcePlayerId='0'`，会把后续 `processAffectTriggers` consumer 的真实行动玩家 provenance 洗回 true owner。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts:147>) 新增 focused gate `queued afterScoring trigger 移动 borrowed mermaids_shipwreck_cove 时，仍应保留真正移动玩家的 sourcePlayerId`，首轮直接红灯；随后只给 [`buildMoveOngoingEvents(...)`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts:245>) 增加 `sourcePlayerId` 参数，并仅在 `ownerId !== sourcePlayerId` 时给 `SU_EVENTS.ONGOING_ATTACHED.payload` 补 `sourcePlayerId`，caller 只在 [`mermaidsOngoingMovePromptProgram.onResolve()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts:678>) 传入 `context.playerId`。验证：focused `reactionQueueSourceRuntimeContext.test.ts -t "queued afterScoring trigger 移动 borrowed mermaids_shipwreck_cove 时，仍应保留真正移动玩家的 sourcePlayerId|queued afterScoring trigger 移动被他人拥有的 mermaids_shipwreck_cove 时，仍应保留其真实 owner"` => `1 file passed, 2 passed`；邻近 `newFactionAbilities.test.ts -t "mermaids_becalmed_shores 天赋会把这张持续行动移到另一个基地|mermaids_shipwreck_cove 在计分后可把这张持续行动移到另一个基地"` => `1 file passed, 2 passed`；shared `reactionQueueEventPlayerContext.test.ts -t "processAffectTriggers 处理 borrowed ONGOING_ATTACHED 时，Brownie 应继续把真正打牌玩家视为事件玩家"` => `1 file passed, 1 passed`。该格只闭合 borrowed ongoing move 的 `sourcePlayerId` provenance，不外推整个 Mermaids、所有 ongoing move caller、所有 `ONGOING_ATTACHED` producer 或整个 owner/source 主线已完成。
  - `processDestroyTriggers / ordering non-destroy sibling advance` 命中一条新的 shared 真红灯，而 `frameId/sourceEventId` 这格只是 dedicated green evidence。[`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts:1460>) 新增 `stamps queued onMinionDestroyed reactions with explicit frame ids` 首轮转绿，证明 destroy frame/source 当前稳定；但 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts:1494>) 新增 `also advances prior non-destroy events before collecting later onMinionDestroyed` 后，首轮直接坐实 `TITAN_PLAYED(destroy-titan-a -> base 1)` 不会被推进到 destroy collector 现场。现已在 [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts:1276>) 最小修复为：按原 `filteredEvents` 顺序遍历，先把所有非 `MINION_DESTROYED` sibling `reduce` 进 `ms.core`，只对 dedup 后那批 destroy key 继续走原本的 destroy/post-process 路径。验证：focused `reactionQueueOrdering.test.ts -t "processDestroyTriggers"` => 首轮 `1 failed, 1 passed`，修复后 `1 file passed, 2 passed`；邻近 `reactionQueueBaseAbilities.test.ts -t "processDestroyTriggers"` => `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 64 passed`；`npx eslint src/games/smashup/domain/reducer.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors, 23 existing warnings`。该格只闭合 destroy ordering 的 non-destroy sibling advance，不外推所有 `onMinionDestroyed` consumer、所有 destroy replacement/save 分支或整个 post-process family 已完成。
  - `processReturnToHandTriggers / ordering frame+sibling advance` 这轮补成了 dedicated green evidence，而不是新的 shared 真红灯。[`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts:1661>) 新增 `stamps queued onCardReturnedToHand reactions with explicit frame ids` 与 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts:1693>) 新增 `also advances prior non-return events before collecting later onCardReturnedToHand` 两条 gate 后，首轮都直接转绿；这说明 [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 当前已经稳定写入 `frameId/sourceEventId`，并会先推进前置 non-return sibling 再收集后续 `onCardReturnedToHand`。验证：focused `reactionQueueOrdering.test.ts -t "processReturnToHandTriggers"` => `1 file passed, 2 passed`；邻近 `reactionQueueBaseAbilities.test.ts -t "processReturnToHandTriggers"` => `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 62 passed`；`npx eslint src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`。该格只说明 return-to-hand ordering 当前已有 dedicated green evidence，不外推整个 `onCardReturnedToHand` family 或 post-process family 已完成。
  - `effect-contract queued interaction / option.value.extraDeckUidsForShuffle` 又命中一条新的 shared 真红灯。[`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `queueDeckMinionSearch()` 会把 `extraDeckUidsForShuffle` 直接塞进 deck-search option value，后续 [`buildDeckMinionSearchEvents()`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 还会把它并回 `deckReordered(...)` 的 shuffle 集合；但 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 card-array 白名单此前不认这个 key。新 focused gate 首轮直接坐实 writes 只有 `cardInstance:deck-minion-a` 与 `base:1`，漏掉 `gelf-self-bottomed`。现已只把 card-array 白名单收紧到 `cardUids|topUids|bottomUids|removedActionUids|pickedToHandUids|playedHandUids|extraDeckUidsForShuffle`，不扩大到 generic `*Uids`、`remainingDeckUids`、`drawnUids` 或其它非 prompt key。验证：focused `option.value.extraDeckUidsForShuffle 数组应映射到多张 cardInstance footprint，而不是只记当前牌库候选` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 56 passed`；`npx eslint src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `extraDeckUidsForShuffle` 的 multi-card footprint，不外推所有 deck-search prompt、所有 shuffle sibling 或整个 queued interaction family 已完成。
  - `effect-contract queued interaction / continuationContext.playedHandUids` 又命中一条新的 shared 真红灯。[`ninjas.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ninjas.ts>) 的 `ninjaDisguisePlayPromptProgram` 会把第一次已打出的手牌 UID 列表写进 `playedHandUids`，并在第二次手牌选择时继续用它过滤 hand options；但 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 card-array 白名单此前不认这个 key。新 focused gate 首轮直接坐实 writes 只有 `cardInstance:hand-minion-b` 与 `base:0`，漏掉 `hand-minion-a`。现已只把 card-array 白名单收紧到 `cardUids|topUids|bottomUids|removedActionUids|pickedToHandUids|playedHandUids`，不扩大到 generic `*Uids`、`excludedCardUids`、`allowedCardUids` 或其它 continuation key。验证：focused `continuationContext.playedHandUids 数组应映射到多张 cardInstance footprint，而不是只记当前第二次手牌候选` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 55 passed`；`npx eslint src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `playedHandUids` 的 multi-card footprint，不外推所有 hand-chaining prompt、所有 continuation UID 数组键或整个 queued interaction family 已完成。
  - `effect-contract queued interaction / continuationContext.usedCardUids` 这轮先补了一条 dedicated green evidence，而不是继续硬猜新红灯。[`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate `continuationContext.usedCardUids 数组应映射到多张 cardInstance footprint，而不是只记当前弃牌堆候选`，夹具直接把 `usedCardUids:['used-a','used-b']` 放进 `continuationContext`；首轮直接转绿，说明 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 现有 `/(cardUids|topUids|bottomUids|removedActionUids|pickedToHandUids|playedHandUids)$/i` 已覆盖 `usedCardUids`，它不是 still-open gap。验证：focused `continuationContext.usedCardUids 数组应映射到多张 cardInstance footprint，而不是只记当前弃牌堆候选` => `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 55 passed`；`npx eslint src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只说明 `usedCardUids` 当前已有 dedicated green evidence，不外推所有 continuation UID 数组键、所有 discard-order prompt 或整个 queued interaction family 已完成。
  - `effect-contract queued interaction / runtimePrompt.continuation.context.pickedToHandUids` 又命中一条新的 shared 真红灯。[`wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `wizard_portal_order` 会把前一步已经拿进手牌的牌 UID 列表写进 `runtimePrompt.continuation.context.pickedToHandUids`，后续排序 prompt 还会拿它参与 live snapshot 过滤；但 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 旧实现只认 `cardUids|topUids|bottomUids|removedActionUids`。新 focused gate 首轮直接坐实 writes 只有 `cardInstance:order-card-a`，漏掉 `picked-a/b`。现已只把 card-array 白名单收紧到 `cardUids|topUids|bottomUids|removedActionUids|pickedToHandUids`，不扩大到 generic `*Uids`、`trackedAll`、`allTopCards` 或其它 continuation key。验证：focused `runtimePrompt.continuation.context.pickedToHandUids 数组应映射到多张 cardInstance footprint，而不是只记当前排序候选` => 首轮 failed 后 `1 file passed, 1 passed`；同组 5 条 continuation-array gate => `1 file passed, 5 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 55 passed`；`npx eslint src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `runtimePrompt.continuation.context.pickedToHandUids` 的 multi-card footprint，不外推所有 continuation UID 数组键、所有 Wizards prompt 或整个 queued interaction family 已完成。
  - `effect-contract queued interaction / continuationContext.destroyedUids` 又命中一条新的 shared 真红灯。[`werewolves.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/werewolves.ts>) 的 `werewolf_let_the_dog_out_targets` 会把本轮已经摧毁过的随从 UID 列表写进 `continuationContext.destroyedUids`，用于后续候选过滤；但 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 旧实现只认 `movedUids|selectedMinionUids`，首轮 focused gate 直接坐实 writes 只有 `minion:fresh-target` 与 `base:1`，漏掉 `destroyed-a/b`。现已只把 minion-array 白名单收紧到 `movedUids|selectedMinionUids|destroyedUids`，不扩大到 generic `*Uids`、`excludedMinionUids`、`pickedToHandUids` 或其它 continuation key。验证：focused `continuationContext.destroyedUids 数组应映射到多张 minion footprint，而不是只记当前选中的摧毁目标` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 52 passed`；`npx eslint src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `continuationContext.destroyedUids` 的 multi-minion footprint，不外推所有 continuation UID 数组键、所有 Werewolves prompt 或整个 queued interaction family 已完成。
  - `queued base collector snapshots / baseAbilityQueue.afterScoring.rankings` 命中一条新的 shared 真红灯，而且证明同一家族不只存在于 `ongoingEffects/reactionSession`。旧 [`baseAbilityQueue.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilityQueue.ts:218>) 在 `collectBaseAbilityTriggers()` 写 `TriggerInstance.rankings` 时直接透传引用，而 [`baseAbilityQueue.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilityQueue.ts:94>) 的 queued base executor 也把 `ctx.rankings` 直接塞进 runtime `BaseAbilityContext`；因此 queued `afterScoring` base ability 同样会吃到 queue 之后的 live mutation。现已在 [`reactionQueueBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts:1343>) 新增 focused gate `queued afterScoring base ability 真链不应让 rankings 快照被 live mutation 污染`，首轮直接坐实 `base-rankings:0:5:2 -> base-rankings:9:99:7`；随后只在 `baseAbilityQueue.ts` 的 capture/replay 两个点把 `rankings` 改成 `structuredClone(...)` 深拷贝，不改任何基地能力业务逻辑、排序协议、owner 解析或 extended base timing。验证：focused `reactionQueueBaseAbilities.test.ts --testNamePattern "queued afterScoring base ability 真链不应让 rankings 快照被 live mutation 污染"` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueBaseAbilities.test.ts` => `1 file passed, 30 passed`；`npx eslint src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts src/games/smashup/domain/baseAbilityQueue.ts` => `0 errors`；`git diff --check -- src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts src/games/smashup/domain/baseAbilityQueue.ts` => passed（仅 LF/CRLF warning）。该格只闭合 queued `afterScoring` base ability 的 `rankings` snapshot 污染，不外推所有 base ability timing、所有 queued base collector、所有 afterScoring consumer 或 yuanhou 四派系整体完成。
  - `queued runtime snapshot objects / affectEvent + affectBatchTargets + duel/duelWinner` 继续沿同一个 `TriggerContext -> TriggerInstance -> executeQueuedTrigger` family 命中三条新的 shared 真红灯。旧 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:543>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:790>) 对 `duel`、`duelChallenger/Challenged/Winner/Loser` 都直接透传 live 引用，[`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:563>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:828>) 对 `affectEvent` 也是裸引用，而 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:564>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:829>) 对 `affectBatchTargets` 也仍是同一份 array/object 引用。因此 queue 之后只要 live event、batch target 或 duel runtime object 被原地改写，queued executor 就会读到污染后的来源事件、批次目标或 duel 快照。现已在 [`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:997>)、[`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:1099>)、[`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:1199>) 新增三条 focused gate，首轮分别直接坐实 `affect:queued_affect_event_runtime:queued -> affect:mutated_affect_event_reason:mutated_affect_event_nested`、`batch-snapshot:ally-1:0 -> batch-snapshot:mutated-target:9`、`duel-snapshot:duel_source_snapshot:queued -> duel-snapshot:mutated_duel_source:mutated_duel_winner_nested`。随后只在 queue capture / replay 两个点把 `affectEvent`、`affectBatchTargets`、`duel`、`duelChallenger/Challenged/Winner/Loser` 都改成 `structuredClone(...)` 深拷贝，不改业务 ability、duel 规则、affect 聚合、reaction ordering 或 TriggerInstance schema。验证：focused `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "queued onMinionAffected 真链不应让 affectEvent 快照被 live mutation 污染|queued onMinionAffected 真链不应让 affectBatchTargets 快照被 live mutation 污染|queued onDuelResolved 真链不应让 duel 与 duelWinner 快照被 live mutation 污染"` => `1 file passed, 3 passed`；整文件 `reactionQueueControllerRuntimeContext.test.ts` => `1 file passed, 15 passed`；`npx eslint src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => `0 errors, 15 existing warnings`；`git diff --check -- src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => passed（仅 LF/CRLF warning）。该格只闭合 `affectEvent`、`affectBatchTargets`、`duel*` 的 queued snapshot 污染，不外推所有 object/array payload、所有 queued runtime 字段或 yuanhou 四派系整体完成。
  - `queued runtime snapshot arrays / rankings + inspectionCards + inspectionTargetPlayerIds` 命中三条新的 shared 真红灯。旧 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:565>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:797>) 对 `rankings` 都是直接透传引用，[`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:574>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:837>) 对 `inspectionCards` 也一样，而 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:576>) / [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:839>) 对 `inspectionTargetPlayerIds` 也还是直接透传；因此 queue 之后只要 live array 或其中对象被原地改写，queued executor 就会读到被污染后的赢家排序、inspected card 快照或目标玩家集合。现已在 [`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:660>)、[`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:742>)、[`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:825>) 新增三条 focused gate，首轮分别直接坐实 `rankings:0:5:4 -> rankings:1:99:1`、`inspect:peek-a:card_a -> inspect:peek-mutated:card_mutated`、`targets:1,2 -> targets:9,2`；随后只在 queue capture / replay 两个点把 `rankings`、`inspectionCards`、`inspectionTargetPlayerIds` 都改成 `structuredClone(...)` 深拷贝，不改其它 payload、业务 ability、reaction ordering 或 TriggerInstance schema。验证：focused `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "queued onDeckInspected 真链不应让 inspectionTargetPlayerIds 快照被 live mutation 污染|queued onDeckInspected 真链不应让 inspectionCards 快照被 live mutation 污染|queued afterScoring 真链不应让 rankings 快照被 live mutation 污染|queued onMinionPlayed 真链不应让 triggerMinion.metadata 的嵌套对象被 live mutation 污染"` => `1 file passed, 4 passed`；整文件 `reactionQueueControllerRuntimeContext.test.ts` => `1 file passed, 12 passed`；`npx eslint src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => `0 errors, 15 existing warnings`；`git diff --check -- src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => passed（仅 LF/CRLF warning）。该格只闭合 `rankings`、`inspectionCards` 与 `inspectionTargetPlayerIds` 的 queued snapshot 污染，不外推所有 array/object payload、所有 queued runtime 字段或 yuanhou 四派系整体完成。
  - `queued onMinionPlayed / lkiMinion.metadata nested snapshot` 命中一条新的 shared 真红灯。旧 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts:591>) 在 `createTriggerInstance()` 捕获 `lkiMinion.metadata` 时只做顶层展开，[`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts:821>) 在 queued executor 恢复 `triggerMinion.metadata` 时也只做顶层展开；因此一旦 queue 之后 live minion 的 `metadata.nested.*` 被原地改写，queued runtime 会读到被污染后的值。现已在 [`reactionQueueControllerRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts:588>) 新增 focused gate，首轮红灯直接坐实 `messageKey` 从期望的 `nested:queued` 变成 `nested:mutated`；随后只在 queue capture / replay 两个点把 `lkiMinion.metadata` 改成 `structuredClone(...)` 深拷贝，不改其它 LKI 字段、duel/affect runtime、业务 ability 或 trigger 排序。验证：focused `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "queued onMinionPlayed 真链不应让 triggerMinion.metadata 的嵌套对象被 live mutation 污染"` => 首轮 failed 后 `1 file passed, 1 passed`；整文件 `reactionQueueControllerRuntimeContext.test.ts` => `1 file passed, 9 passed`；`npx eslint src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => `0 errors, 15 existing warnings`；`git diff --check -- src/games/smashup/__tests__/reactionQueueControllerRuntimeContext.test.ts src/games/smashup/domain/ongoingEffects.ts src/games/smashup/domain/reactionSession.ts` => passed（仅 LF/CRLF warning）。该格只闭合 `triggerMinion.metadata` 的 nested snapshot 污染，不外推所有 LKI 深拷贝、所有 metadata 载荷、所有 queued runtime 字段或 yuanhou 四派系整体完成。
  - `queued executor Titans runtime gates` 这轮先做了两条 dedicated green evidence，而不是继续硬猜新红灯：`pirates_the_kraken.afterScoring` 在 live base 只剩 P0 控制者时，queued trigger 仍会靠 `triggerBaseControllersAtTrigger=['1']` 给 P1 弹 `titan_pirates_the_kraken_play_replacement`；`explorers_very_large_boulder.onMinionMoved` 也仍会把 `moveFromBaseIndex=0/moveToBaseIndex=1` 传到 runtime executor，并给 P1 弹 `titan_explorers_very_large_boulder_move`。验证：`reactionQueueControllerRuntimeContext.test.ts` 新增 2 条 focused gate 均首轮转绿，整文件现为 `1 file passed, 7 passed`；ESLint `reactionQueueControllerRuntimeContext.test.ts` 0 errors；`git diff --check` 通过。该格只说明 Titans 这两个 runtime 字段当前不是新的 queued seam，不外推所有 afterScoring/onMinionMoved、所有 Titans consumer 或整个 runtime inventory 已完成；下一步继续离开这两格，回到其它 still-open `TriggerContext -> TriggerInstance -> queued executor` 非对称找最小真红灯。
  - `queued reaction frame / same-frame runtime shared-state holder` 命中一条新的 shared 真红灯。先新增 `samurai_bushi` dedicated gate，证明 `triggerMinionPower` 在 queued `onMinionDiscardedFromBase` 真链上已能从 queue payload 传到 executor 并真实发出 `VP_AWARDED`，这格只是把开放怀疑降为 green evidence；随后新增 `queued onTurnStart 双 Sprout 共享唯一候选时，仍不应重复打出同一张 deck UID`，在真实 `smashup_reaction_choose -> 先后选择两个 mandatory trigger` 链上首轮坐实同一张 `wl-1` 被分别打到 `base_a/base_b` 两次。根因不是 `Sprout` 卡面逻辑本体，而是 queued reaction frame 缺少 direct `fireTriggers()` 那种整帧复用的 `triggerSharedState` holder，导致 `killerPlantSproutTrigger()` 的 `getSproutSharedDecks(ctx)` 在第二个 trigger 执行时重新拿到空 holder。现已在 [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts>) 增加按 `frameId ?? trigger.id` 复用的模块级 `queuedTriggerRuntimeSharedState`，`executeQueuedTrigger()` 为同一 reaction frame 内的 queued triggers 传入同一份 `triggerSharedState`，并在该 frame 排空时清理；不改 [`killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>) 的 `Sprout` 搜索/skip/reshuffle/self-destroy 语义，不把 `Map` 塞进可序列化 state，也不外推其它 timing family。验证：focused `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "queued onTurnStart 双 Sprout 共享唯一候选时，仍不应重复打出同一张 deck UID|queued onMinionDiscardedFromBase 真链仍应把 Bushi 的 triggerMinionPower 传到 executor|queued onMinionAffected ongoing trigger 的 canTrigger 与 executor 都应继续拿到 affectBatchTargets"` => `1 file passed, 3 passed`；整文件 `reactionQueueControllerRuntimeContext.test.ts` => `1 file passed, 5 passed`；邻近 `reactionQueueEventPlayerContext.test.ts --testNamePattern "Sprout|sourceController queued onTurnStart trigger 仍应只在拥有者回合开始把 Sprout"` => `1 file passed, 2 passed`；ESLint `reactionSession.ts + reactionQueueControllerRuntimeContext.test.ts` => `0 errors, 8 existing warnings`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合“同一 queued reaction frame 缺少 runtime shared holder”这条 seam，不外推所有 `onTurnStart`、所有 `triggerSharedState` consumer、所有 queued runtime context 或 yuanhou 四派系整体完成。
  - `processAffectTriggers / queued onMinionAffected / affectBatchTargets runtime contract` 命中一条新的 shared 真红灯。旧 [`ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 的 `TriggerContext` 与 [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 的 `processAffectTriggers()` 都已生成 `affectBatchTargets`，但 [`types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>) 的 `TriggerInstance`、`createTriggerInstance()` 与 [`reactionSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionSession.ts>) 的 queued executor 恢复链此前都没带这组批次目标快照；因此 `canTrigger()` 收集阶段能看到 batch，queued runtime executor 却拿不到。现已最小修复为：只给 `TriggerInstance` 增加 `affectBatchTargets`，并在 `ongoingEffects.ts` 写入、`reactionSession.ts` 恢复到 runtime `ctx`，不改 `processAffectTriggers()` 聚合规则、`shouldSkipTriggerInstance()` 的 Diva/Aramis 筛选逻辑、`reactionResources.ts` footprint 或其它 timing family。验证：focused `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "queued onMinionAffected ongoing trigger 的 canTrigger 与 executor 都应继续拿到 affectBatchTargets"` 首轮红灯坐实 `trigger.affectBatchTargets===undefined`，修复后 => `1 file passed, 1 passed`；整文件 `reactionQueueControllerRuntimeContext.test.ts` => `1 file passed, 3 passed`；邻近 `reactionQueueEventPlayerContext.test.ts --testNamePattern "sourceController queued onMinionAffected trigger"` => `1 file passed, 5 passed`；ESLint `types.ts + ongoingEffects.ts + reactionSession.ts + reactionQueueControllerRuntimeContext.test.ts` => `0 errors, 17 existing warnings`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `onMinionAffected` queued runtime 的 `affectBatchTargets` 合同，不外推所有 affect family、所有 queued runtime context、所有 `TriggerInstance` 字段或 yuanhou 四派系整体完成。
  - `ACTION_PLAYED side branch / extended-base queued runtime context / actionTarget* provenance` 命中一条新的 shared 真红灯。旧 [`baseAbilityQueue.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilityQueue.ts>) 的 `collectExtendedBaseAbilityTriggers(...)` 虽然 queued executor 已支持 `ctx.actionTargetBaseIndex/actionTargetType/actionTargetMinionUid`，但收集入口本身完全不接这三个字段，也不会把它们写进 `canTrigger/deriveFootprint` 的 optionContext 或最终 `TriggerInstance`；因此 extended base 的 `onActionPlayed` 在 queue 阶段看不到真实目标上下文。现已最小修复为：只给 `collectExtendedBaseAbilityTriggers(...)` 增加 `actionTargetBaseIndex/actionTargetType/actionTargetMinionUid` 参数透传，并在 [`index.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/index.ts>) 的 `ACTION_PLAYED` 分支把这三项真实传入 extended-base collector，不改其它 timing family、普通 base collector 或 executor 逻辑。验证：focused `reactionQueueBaseAbilities.test.ts --testNamePattern "extended queued onActionPlayed base ability 的 canTrigger/deriveFootprint 应看到 action target 上下文"` 首轮红灯后 => `1 file passed, 1 passed`；整文件 `reactionQueueBaseAbilities.test.ts` => `1 file passed, 29 passed`；ESLint `baseAbilityQueue.ts + index.ts + reactionQueueBaseAbilities.test.ts` => `0 errors, 25 existing warnings`；`git diff --check -- src/games/smashup/domain/baseAbilityQueue.ts src/games/smashup/domain/index.ts src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts` => 通过（仅 LF/CRLF warning）。该格只闭合 extended-base `onActionPlayed` 的 queued runtime action-target context，不外推所有 extended base timing、所有 `onActionPlayed` consumer 或整个 post-process family。
  - `ACTION_PLAYED side branch / mandatory extended-base onActionPlayed / queued ordering sibling` 命中一条新的 shared 真红灯。旧 [`index.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/index.ts>) 的 `postProcessSystemEvents()` 在处理 `ACTION_PLAYED` 时，只会为目标基地收集普通 `collectBaseAbilityTriggers(...)` 和 ongoing `collectTriggers(..., 'onActionPlayed', ...)`，完全漏掉 `collectExtendedBaseAbilityTriggers(...)`；因此同一基地若同时命中 mandatory ongoing 与 mandatory extended base `onActionPlayed`，queue 里只会留下 ongoing。现已最小修复为：只在 `ACTION_PLAYED` 目标基地分支追加 `collectExtendedBaseAbilityTriggers({ timing:'onActionPlayed', ownerPlayerId, baseIndex, reason:'action-played', frameId, sourceEventId })`，并沿用现有 `tempCore/tempMatchState` reduce 顺序，不改 `ACTION_PLAYED` event 构造、普通 base collector 或其它 timing family。验证：focused `reactionQueueBaseAbilities.test.ts --testNamePattern "ACTION_PLAYED 命中 mandatory ongoing 与 mandatory extended base onActionPlayed 时，应统一进入 reaction ordering"` 首轮红灯后 => `1 file passed, 1 passed`；整文件 `reactionQueueBaseAbilities.test.ts` => `1 file passed, 28 passed`；ESLint `index.ts + reactionQueueBaseAbilities.test.ts` => `0 errors, 25 existing warnings`；`git diff --check -- src/games/smashup/domain/index.ts src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts` => 通过（仅 LF/CRLF warning）。该格只闭合 `ACTION_PLAYED` post-process 的 extended-base queued ordering sibling，不外推所有 `onActionPlayed` consumer、所有 extended base timing、整个 post-process family，或 yuanhou 四派系整体完成。
  - `borrowed ONGOING_ATTACHED -> onMinionAffected source provenance` 继续沿 affect producer 命中一条新的 shared 真红灯。旧 [`affect.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/affect.ts>) 在处理 `SU_EVENTS.ONGOING_ATTACHED` 时，把 `sourcePlayerId/sourceControllerId` 都错误写成 `payload.ownerId`，完全忽略了更完整的 `payload.sourcePlayerId`；这会把借来的持续行动附着到随从时的真实打牌玩家洗回卡牌拥有者，并直接暴露在 `trickster_brownie` 这类 `onMinionAffected` consumer 上。现已最小修复为：`sourcePlayerId = payload.sourcePlayerId ?? payload.ownerId`，并同步写入 affect record 的 `sourceControllerId`；同时新增 focused gate `processAffectTriggers 处理 borrowed ONGOING_ATTACHED 时，Brownie 应继续把真正打牌玩家视为事件玩家` 与 direct gate `ONGOING_ATTACHED affect record 在 borrowed attach 场景下应保留 sourcePlayerId/sourceControllerId='0'`。验证：focused `reactionQueueEventPlayerContext.test.ts --testNamePattern "Brownie"` => `1 file passed, 3 passed`；direct `ongoingEffects.test.ts --testNamePattern "MINION_RETURNED、MINION_CONTROL_CHANGED、CARD_SUPPRESSED 被正确分类为随从 affect"` => `1 file passed, 1 passed`；ESLint `affect.ts + reactionQueueEventPlayerContext.test.ts + ongoingEffects.test.ts` => `0 errors, 1 existing warning`。该格只闭合 `ONGOING_ATTACHED` provenance 和 Brownie consumer，不外推所有 `onMinionAffected` consumer、全部 affect family，或整条 shared seam 已完成。
  - `effect-contract queued interaction / runtimePrompt.continuation.context.selectedMinionUids -> multi-minion footprint` 继续沿 `effect-contract queued interaction` 命中一条新的 shared footprint gap。生产侧 [`ninjas.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ninjas.ts>) 的 `ninja_disguise` 真链会把已选中、后续要回手的基地随从 UID 列表写进 `runtimePrompt.continuation.context.selectedMinionUids`；但旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 只会把 `movedUids` 识别成 `minion[]`，不会把 `selectedMinionUids[]` 写进冲突资源集合，结果 footprint 只记当前手牌候选 `cardUid` 与 `baseIndex`。现已最小收口为：只把 minion-array 白名单从 `movedUids` 扩到 `movedUids|selectedMinionUids`，不扩大到 generic `selected*Uids`、`selectedTargetUids`、`playedHandUids` 或其它 runtime continuation key；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate，锁住 `runtimePrompt.continuation.context.selectedMinionUids=['return-minion-a','return-minion-b']` 会与当前手牌候选一起写入 `minion` footprint。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "runtimePrompt\\.continuation\\.context\\.selectedMinionUids 数组应映射到多张 minion footprint，而不是只记当前手牌随从"` 首轮红灯后 => `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` 在 `--max-old-space-size=8192` 下 => `1 file passed, 51 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check -- src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => 通过（仅 LF/CRLF warning）。该格只闭合 `runtimePrompt.continuation.context.selectedMinionUids` 的 multi-minion footprint，不外推所有 selected UID 数组键、所有 runtime continuation prompt 或整个 queued interaction family。
  - `effect-contract queued interaction / runtimePrompt.continuation.context.removedActionUids -> multi-card footprint` 继续沿 `effect-contract queued interaction` 命中一条新的 shared footprint gap。生产侧 [`miskatonic.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/miskatonic.ts>) 的 `miskatonic_those_meddling_kids_select` 会把已处理过的行动 UID 列表写进 `runtimePrompt.continuation.context.removedActionUids`；但旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 只会把 `cardUids/topUids/bottomUids` 映射成多张 `cardInstance`，不会把 `removedActionUids[]` 写进冲突资源集合，结果 footprint 只记当前 option 自带的 `cardUid` 与 `baseIndex`。现已最小收口为：只把数组白名单从 `cardUids|topUids|bottomUids` 扩到 `cardUids|topUids|bottomUids|removedActionUids`，不扩大到 generic `*Uids`、`allowedActionUids`、`inspectedUids` 或其它 runtime continuation key；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate，锁住 `runtimePrompt.continuation.context.removedActionUids=['removed-action-a','removed-action-b']` 会与当前 `cardUid='current-action'` 一起写入 `cardInstance` footprint。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "runtimePrompt\\.continuation\\.context\\.removedActionUids 数组应映射到多张 cardInstance footprint，而不是只记当前选中的行动卡"` => `1 file passed, 1 passed`；整文件 `reactionQueueOrdering.test.ts` 在 `--max-old-space-size=8192` 下 => `1 file passed, 50 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check -- src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` => 通过（仅 LF/CRLF warning）。该格只闭合 `runtimePrompt.continuation.context.removedActionUids` 的 multi-card footprint，不外推所有 action UID 数组键、所有 runtime continuation prompt 或整个 queued interaction family。
  - `effect-contract queued interaction / continuationContext cardUids[] -> multi-card footprint` 继续沿 `effect-contract queued interaction` 命中一条新的 shared footprint gap。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 在 `addGenericResourcesFromValue(...)` 遇到 `cardUids: string[]` 这类 primitive UID 数组时，只会把数组项递归回自身，而 primitive string 不会再被识别成资源；因此像 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 里 `titan_ninjas_invisible_ninja_ongoing` 这种 `continuationContext={ titanUid, cardUids:[...] }` 的 prompt，footprint 只会保留当前 option 自带的 `cardUid`，不会把同一批已揭示候选牌全部写成 `cardInstance` 资源。现已最小收口为：只在 `addGenericResourcesFromValue(...)` 为 `cardUids` 数组增加 structured `cardInstance` 推导，不扩大到其它 plural key 或 generic array 语义；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate，锁住 `titan_ninjas_invisible_ninja_ongoing` 的 `continuationContext.cardUids=['peek-a','peek-b']` 会同时写入 `cardInstance:peek-a` 与 `cardInstance:peek-b`。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "moon_zero_three_resolve_footprint|continuationContext\\.targetPlayerId|continuationContext\\.cardUids|runtimePrompt\\.continuation\\.context|pid-only|Star Spawn|Cellular Bonding|Mark of Sleep|Invisible Ninja"` => `1 file passed, 7 passed`；整文件 `reactionQueueOrdering.test.ts` 在 `--max-old-space-size=8192` 下 => `1 file passed, 47 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`。该格只闭合 `continuationContext.cardUids` 这类已揭示卡 UID 数组的 multi-card footprint，不外推所有 plural UID key、所有 reveal/peek prompt 或整个 queued interaction family。
  - `effect-contract queued interaction / continuationContext targetPlayerId -> playerControl footprint` 继续沿 `effect-contract queued interaction` 命中一条新的 shared footprint gap。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `deriveFootprintFromInteraction()` 虽然会把 `continuationContext` 喂给 generic write 路径，但 structured player write 只认 option value 里的 `playerId/targetPlayerId/pid`，不会消费 `continuationContext` 或 `runtimePrompt.continuation.context` 里的目标玩家键；因此像 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 里 `titan_super_spies_moon_zero_three_resolve` 这类 button-only prompt，虽然真实目标玩家在 `continuationContext.targetPlayerId`，footprint 仍只会记 `cardInstance:peeked-card-1`，不会写入 `playerControl:1`。现已最小收口为：在 `reactionResources.ts` 新增 `addStructuredPlayerWritesFromValue(...)`，继续保留 option value 的 `playerId/targetPlayerId/pid -> playerControl`，并额外把 `continuationContext` 与 `runtimePrompt?.continuation?.context` 里的 `playerId/targetPlayerId/pid` 也映射成 structured `playerControl` footprint，不扩大到 `sourcePlayerId/controllerId/ownerPlayerId` 或其它 generic key。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "moon_zero_three_resolve_footprint|continuationContext\\.targetPlayerId|runtimePrompt\\.continuation\\.context|pid-only|Star Spawn|Cellular Bonding|Mark of Sleep"` => `1 file passed, 6 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 46 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 button-only interaction 在 `continuationContext.targetPlayerId` / `runtimePrompt.continuation.context.targetPlayerId` 上的 structured player-target footprint，不外推所有 player-target prompt、所有 structured player key 或整个 queued interaction family。
  - `shared-contract: runtime prompt response core-vs-events / cowboys_dynamite_surprise_seen zone transfer` 这轮命中一条新的 scoped 真红灯，不是 inspection context 本体回退。旧 [`cowboys.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cowboys.ts>) 的 `cowboys_dynamite_surprise_seen` 在 resolve 时一边直接手改 `matchState.core.players[owner].hand/deck/discard`，一边返回 `MINION_DESTROYED` 事件；而 [`systems.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/systems.ts>) 对 runtime prompt 的共享合同是“有 emitted domain events 时保留 `result.state.sys`，但用这些 events 重建 core”，所以手改的 zone 迁移会被覆盖，表现成 inspection prompt 能弹、目标随从能炸掉，但 `dyn-1` 仍留在原 hand/deck。现已最小收口为：只改 `cowboys_dynamite_surprise_seen`，把源卡离开 hand/deck 改成标准 `CARDS_DISCARDED` / `CARDS_MILLED` 事件，再拼接既有 `buildValidatedDestroyEvents(...)`，不扩大到其它 inspection family 或 shared runtime executor。验证：[`reactionQueueInspectionRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueInspectionRuntimeContext.test.ts>) => `1 file passed, 2 passed`；[`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 里三条 `cowboys_dynamite_surprise(_pod)` direct gate => `1 file passed, 3 passed`；上一轮留下的 7-file shared inventory（含 `reactionQueueActionPlayedConstruction/Duel/ExternalDiscard/ControllerRuntimeContext/InspectionRuntimeContext/SourceRuntimeContext/reactionSessionResponseActionTargetContext`）复跑 => `7 files passed, 37 passed`。这格只闭合 `Dynamite Surprise Seen` 在 reveal hand / reveal deck 两种 inspection 来源下的源卡 zone transfer，不外推整个 inspection family、所有 runtime prompt caller，或整个 queued-trigger-runtime-context 已全量完成。
  - `effect-contract queued interaction / ability runtime prompt continuation context -> structured footprint` 继续沿 `effect-contract queued interaction` 命中一条新的 shared footprint gap。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `deriveFootprintFromInteraction()` 只认 `interaction.data.continuationContext`，不认 ability runtime prompt 真实挂载的 `interaction.data.runtimePrompt.continuation.context`；因此像 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 里 `trickster_block_the_path` 这类 option 只有 `value:{ factionId }`、真实目标上下文藏在 runtime continuation 里的 prompt，仍会退回 `interaction:trickster_block_the_path:unstructured-options`。现已最小收口为：只在 `deriveFootprintFromInteraction()` 额外把 `runtimePrompt?.continuation?.context` 喂给 `addGenericResourcesFromValue(..., 'write')`，不扩大到 generic mode-only / factionId-only option 或其它 runtime surface；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 focused gate，锁住 `trickster_block_the_path` 不再 fallback，且会写入 `cardInstance:block-path-a` 与 `base:0`。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "runtimePrompt\\.continuation\\.context|trickster_block_the_path|pid-only|Cellular Bonding|Star Spawn|Mark of Sleep"` => `1 file passed, 5 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 45 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 ability runtime prompt continuation context 的 structured-footprint seam，不外推所有 mode-only / factionId-only / branchId-only prompt 或整个 queued interaction family。
  - `externalActionPlay / helper default handSizeAfterPlay / recover-then-replay ghost threshold` 这轮重新回到 `externalActionPlay` family，命中的是 helper 默认合同，而不是再补 caller inventory。旧 [`externalActionPlay.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/externalActionPlay.ts>) 只有 caller 显式传入时才会写 `AbilityContext.handSizeAfterPlay`；一旦是“先回手再立刻重打”的外部行动链但 caller 漏传，Ghosts 这类按“打出后手牌数”结算的 onPlay 就会回退到 `player.hand.length - 1`，把已经 reduce 到出牌后现场的手牌再次少算 1。现已最小收口为：helper 默认把 `handSizeAfterPlay` 对齐到 `simCore.players[playerId]?.hand.length ?? 0`，显式 override 仍优先；并在 [`abilityRegistry.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/abilityRegistry.test.ts>) 新增 focused gate，直接锁住“从 discard 回手再额外打出 `ghost_seance` 且未显式传 `handSizeAfterPlay` 时，不应因少算 1 张手牌而误抽牌”。验证：focused `abilityRegistry.test.ts --testNamePattern "未提供 handSizeAfterPlay"` => `1 file passed, 1 passed`；邻近 `query6Abilities.test.ts -t "wizard_mass_enchantment"` => `1 file passed, 5 passed`；`expansionOngoing.test.ts -t "steampunk_mechanic|steampunk_change_of_venue"` => `1 file passed, 16 passed`；`smashup.smoke.test.ts -t "奶油泡芙美人天赋会弃 1 张牌，额外打出弃牌堆标准战术，并改放牌库底"` => `1 file passed, 1 passed`。该格只闭合 external action helper 的默认 post-play hand-size 合同，不外推整个 `externalActionPlay` family、所有 replay caller 或所有基于 hand threshold 的 onPlay 已全量完成。
  - `effect-contract queued interaction / pid-only player-target option -> playerControl footprint` 继续沿 `effect-contract queued interaction` 命中上一格 `targetPlayerId + madnessUid` 的 player-target sibling。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 interaction option footprint 只认 `playerId/targetPlayerId`，不认真实生产 prompt 使用的 `pid`；因此 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 里 `trickster_mark_of_sleep` 这类 `value:{ pid }` 的 player-target 交互，会继续退回 `interaction:...:unstructured-options`。现已最小收口为：只在 `deriveFootprintFromOption()` 层把 `pid` 映射成 `playerControl`，不扩大到 generic context/state 字段；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 `Mark of Sleep` 与 `pid-only` 两条 focused gate，锁住不再 fallback，且会写入 `playerControl:1`。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "Mark of Sleep|pid-only 结构化 player-target option"` => `1 file passed, 2 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 44 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 pid-only player-target prompt 的 structured-option footprint，不外推所有 player-target prompt 或整个 queued interaction family。
  - `effect-contract queued interaction / targetPlayerId + madnessUid structured option -> player/card footprint` 继续沿 `effect-contract queued interaction` 命中上一格 `actionUid-only` 的直接 sibling。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `deriveFootprintFromOption()` 既不认 `targetPlayerId`，也不认 `madnessUid`；因此像 [`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 的 `cthulhu_star_spawn` 这类 player-target prompt，虽然 option 已带 `targetPlayerId + madnessUid` 两个结构化键，仍会退回 `interaction:...:unstructured-options`。现已最小收口为：只在 footprint 推导层把 `targetPlayerId` 映射成 `playerControl`，`madnessUid` 映射成 `cardInstance`，不扩大到 `sourcePlayerId/fromPlayerId/toPlayerId` 或其它非 prompt target 键；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增 `Star Spawn` 与 `targetPlayerId + madnessUid` 两条 focused gate，锁住不再 fallback，且会写入 `playerControl:1` 与 `cardInstance:madness-1`。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "Star Spawn|targetPlayerId \\+ madnessUid"` => `1 file passed, 2 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 42 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 player-target madness prompt 的 structured-option footprint，不外推所有 player-target prompt 或整个 queued interaction family。
  - `effect-contract queued interaction / actionUid-only structured option -> cardInstance footprint` 继续沿长期 JSON 点名的 `effect-contract queued interaction` 命中一条新的 shared footprint gap。旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `deriveFootprintFromOption()` 只识别 `cardUid/minionUid/titanUid/baseDefId` 等结构化键，不识别真实生产 prompt 使用的 `actionUid`；因此像 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 里 `shapeshifters_cellular_bonding_choose` 这类 attached-action 选择 prompt，会被误降成 `interaction:...:unstructured-options`。现已最小收口为：仅在 footprint 推导层把 `actionUid` 映射成 `cardInstance` 资源，不扩大到 generic mode-only option 或其它无结构化目标 prompt；并在 [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>) 新增两条 focused gate，锁住 `Cellular Bonding` 结构化 option 不再 fallback，且 `actionUid-only` prompt 会写入 `cardInstance:bonded-action-1`。验证：focused `reactionQueueOrdering.test.ts --testNamePattern "Cellular Bonding|actionUid-only 结构化交互 option"` => `1 file passed, 2 passed`；整文件 `reactionQueueOrdering.test.ts` => `1 file passed, 40 passed`；ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` => `0 errors`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 attached-action prompt 的 `actionUid-only` structured-option footprint，不外推所有 generic/button prompt、所有 interaction continuationContext 或整个 queued interaction family。
  - `processReturnToHandTriggers / MINION_RETURNED / extended-base onCardReturnedToHand` 继续沿长期 JSON 点名的 `processReturnToHandTriggers / extended-base / post-process side branch` 命中一条新的 shared 真红灯。旧 [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 的 `processReturnToHandTriggers()` 已经会为 `MINION_RETURNED / CARD_TRANSFERRED / BURIED_CARD_RETURNED_TO_HAND` 收集 ongoing `onCardReturnedToHand` trigger，但没有像 `processAffectTriggers()` 那样同步调用 `collectExtendedBaseAbilityTriggers(...)`；因此同一基地若同时命中 mandatory ongoing 与 mandatory extended base `onCardReturnedToHand`，queue 里只会留下 ongoing，extended base 会静默漏排。现已先在 [`reactionQueueBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts>) 新增 focused gate，首轮红灯坐实 `sourceDefId='base_b'` 根本不入队；随后只在 reducer 的 `MINION_RETURNED / CARD_TRANSFERRED / BURIED_CARD_RETURNED_TO_HAND` 三条有 base 现场的 carrier 分支补 `collectExtendedBaseAbilityTriggers(...)`，把 `baseIndex / triggerMinionUid / triggerMinionDefId / controllerId / frameId / sourceEventId / reason` 一并透传，不改 `CARD_RECOVERED_FROM_DISCARD`、attached-source fallback 或其它 post-process collector。验证：focused `reactionQueueBaseAbilities.test.ts --testNamePattern "processReturnToHandTriggers 命中 mandatory ongoing 与 mandatory extended base onCardReturnedToHand 时，应统一进入 reaction ordering"` => 首轮红灯后 `1 file passed, 1 passed`；整文件 `reactionQueueBaseAbilities.test.ts` => `1 file passed, 26 passed`；整文件 `reactionQueueOnCardReturnedToHand.test.ts` => `1 file passed, 15 passed`；ESLint `reducer.ts + reactionQueueBaseAbilities.test.ts` => `0 errors, 23 existing warnings`。该格只闭合 return-to-hand post-process 的 extended-base queued ordering sibling，不外推所有 `onCardReturnedToHand` consumer、所有 extended base timing 或整个 post-process family。
  - `bury.ts uncoverBuriedCard / borrowed buried minion -> MINION_PLAYED.ownerId` 继续沿 handbuilt `MINION_PLAYED(fromBuried)` producer 命中新的 buried engine 真红灯。旧 [`bury.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/bury.ts>) 的普通 buried minion 翻开分支发 `MINION_PLAYED(playerId/cardUid/defId/baseIndex/fromBuried)` 时没有传 `ownerId`；因此 P0 控制但 P1 拥有的 buried minion 在 P0 回合开始翻开打到基地后，会落地成 P0 owner。现已最小收口为：`uncoverBuriedCard` 的 minion 分支写入 `ownerId: buried.trueOwnerId`，不改 buried prompt、action 分支、ongoing target、base trigger queue 或通用 reducer。新增 [`buryEngine.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/buryEngine.test.ts>) gate，首轮红灯坐实 `MINION_PLAYED.payload.ownerId === undefined`；修复后 focused => `1 file passed, 1 passed`，整文件 `buryEngine.test.ts` => `1 file passed, 13 passed`，ESLint `bury.ts + buryEngine.test.ts` => `0 errors, 16 existing warnings`。该格只闭合 buried minion uncover 的 borrowed owner provenance，不外推 Ancient Egyptians/Bury 全量、buried action/ongoing 全分支、所有 fromBuried producer 或整个 `MINION_PLAYED` family。
  - `zombie_they_keep_coming / zombie_lord / borrowed discard minion -> MINION_PLAYED.ownerId` 继续沿 handbuilt `MINION_PLAYED(fromDiscard)` producer 命中新的 Zombies 真红灯。旧 [`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 的 They Keep Coming 与 Zombie Lord discard replay 分支都从当前玩家 live discard 找到 source minion，但手工 `MINION_PLAYED(fromDiscard:true)` 没有传 `ownerId`；因此 P0 从自己弃牌堆打出 `owner='1'` 的 borrowed 随从后，落地随从 owner 被洗成 P0。现已最小收口为：两条分支都写入 `ownerId: discardCard.owner`，不改弃牌堆合法性复核、base targeting、空基地约束、额外出牌额度或通用 reducer。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) 两条 gate，首轮红灯坐实二者落地 owner 都是 `0`；修复后 focused => `1 file passed, 2 passed`，整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 30 passed`，ESLint `zombies.ts + zombieWizardAbilities.test.ts` => `0 errors`。该格只闭合 `zombie_they_keep_coming` 与 `zombie_lord` 的 discard minion replay owner provenance，不外推 Zombies 全量、所有 fromDiscard minion replay caller 或整个 `MINION_PLAYED` producer family。
  - `ghost_the_dead_rise / borrowed discard minion -> MINION_PLAYED.ownerId` 继续沿 handbuilt `MINION_PLAYED(fromDiscard)` producer 命中新的 Ghosts 真红灯。旧 [`ghosts.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ghosts.ts>) 的 The Dead Rise 在弃三张后构造可从 discard 打出的低力量随从候选时只保留 `cardUid/defId/power`，base resolve 与单基地直落分支发 `MINION_PLAYED(fromDiscard:true)` 时没有携带 source card 的真实 owner；因此 P0 从自己弃牌堆打出 `owner='1'` 的 borrowed 低力量随从后，落地随从 owner 会被洗成 P0。现已最小收口为：`GhostDeadRiseCandidate`、play choice value 与 base context 都携带 `ownerId: card.owner`，两条 `MINION_PLAYED` 分支写入 `payload.ownerId`，不改弃三张、候选力量阈值、skip 或普通从弃牌堆打出语义。新增 [`ghostsAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ghostsAbilities.test.ts>) gate `从弃牌堆打出 borrowed 低力量随从时，应保留真实 owner`，首轮红灯坐实 borrowed minion 落地 owner 错成当前玩家；修复后 focused => `1 file passed, 1 passed`，整文件 `ghostsAbilities.test.ts` => `1 file passed, 10 passed`，ESLint `ghosts.ts + ghostsAbilities.test.ts` => `0 errors, 1 existing warning`（`SU_EVENTS` 既有/当前 unused import）。该格只闭合 `ghost_the_dead_rise` 的 discard minion replay owner provenance，不外推 Ghosts 全量、所有 fromDiscard minion replay caller 或整个 `MINION_PLAYED` producer family。
  - `vampire_crack_of_dusk / borrowed discard minion -> MINION_PLAYED.ownerId` 继续沿 handbuilt `MINION_PLAYED(fromDiscard)` producer 命中新的 Vampires 真红灯。旧 [`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 Crack of Dusk 与 POD base resolve 分支只发 `MINION_PLAYED(playerId=context.playerId, fromDiscard:true)`，没有从 live discard card 读取真实 `owner`；因此 P0 从自己弃牌堆打出 `owner='1'` 的 borrowed 低力量随从后，落地随从 owner 被洗成 P0。现已最小收口为：两个 base resolve 分支都从 `state.core.players[context.playerId].discard` 定位所选卡，写入 `ownerId: cardInDiscard.owner`，其他出牌/加指示物语义不变。新增 [`vampiresPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/vampiresPod.test.ts>) gate `破晓从弃牌堆打出 borrowed 低力量随从时，应保留真实 owner`，首轮红灯坐实 `minion.owner === '0'`；修复后 focused => `1 file passed, 1 passed`，整文件 `vampiresPod.test.ts` => `1 file passed, 13 passed`，ESLint `vampires.ts + vampiresPod.test.ts` => `0 errors, 43 existing warnings`。该格只闭合 Crack of Dusk / Crack of Dusk POD 的 discard minion replay owner provenance，不外推 Vampires/POD 全量、所有 fromDiscard minion replay caller 或整个 `MINION_PLAYED` producer family。
  - `ninjas_invisible_ninja / remaining revealed borrowed card -> owner deck split` 已在上一格基础上继续命中第二条同卡 sibling 真红灯。旧 `titan_ninjas_invisible_ninja_ongoing` handler 在玩家从揭示的两张牌中抽走一张后，会把 `remainingShown` 全量和当前玩家剩余 deck 一起洗成 `DECK_REORDERED.playerId=当前玩家`；因此若未选中的揭示牌是 `owner='1'` 的 borrowed card，它会继续留在 P0 deck。现已最小收口为：handler 将 `remainingShown` 按 owner 分流，先对 borrowed remaining 生成 owner-scoped `DECK_REORDERED(playerId=ownerId, sourcePlayerId=playerId)`，再只用 source-owned remaining 重排当前玩家 deck。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) gate `ninjas_invisible_ninja 选择抽走一张牌后，应把未选中的 borrowed 揭示牌分流回真实拥有者牌库`，首轮红灯坐实 `P0.deck=['p0-tail','borrowed-peek']`；修复后 focused `未选中的 borrowed 揭示牌` => `1 file passed, 2 passed`，邻近 `ninjas_invisible_ninja|Invisible Ninja` => `1 file passed, 7 passed`，ESLint `titans.ts + newFactionAbilities.test.ts` => `0 errors, 9 existing warnings`。该格只闭合 Invisible Ninja remaining revealed borrowed card 分流，不外推 Titans/Ninjas 全量、所有 reveal/order caller、所有 draw-one-then-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `ninjas_invisible_ninja / peek 前 discard reshuffle -> borrowed card owner deck split` 已从 Titans/DECK_REORDERED producer sibling 坐实为一条真实红灯并收口。旧 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `buildInvisibleNinjaPeekResult()` 在牌库不足两张且需要把当前玩家 discard 洗入 peek snapshot 时，会把 `player.discard` 全量洗成当前玩家 `DECK_REORDERED.playerId`；因此 `P0.discard` 中 `owner='1'` 的 borrowed card 会被错误放进 P0 deck，并出现在 Invisible Ninja 的可抽候选中。现已最小收口为：只在该 peek 构造里按 `card.owner` 分流 discard，当前玩家 peek snapshot 只保留 source-owned 或无有效 owner 的牌；可识别 borrowed card 通过 owner-scoped `DECK_REORDERED(playerId=ownerId, sourcePlayerId=sourcePlayerId)` 追加回真实 owner deck。新增/收紧 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) gate `ninjas_invisible_ninja 抽牌前重洗弃牌堆时，应把 borrowed 牌分流回真实拥有者牌库`，首轮红灯坐实单个 `DECK_REORDERED.playerId='0'` 错含 `borrowed-peek`；修复后 focused `borrowed 牌分流回真实拥有者牌库` => `1 file passed, 1 passed`，邻近 `ninjas_invisible_ninja|Invisible Ninja` => `1 file passed, 6 passed`，ESLint `titans.ts + newFactionAbilities.test.ts` => `0 errors, 9 existing warnings`，`diff --check` 仅 LF/CRLF warning。该格只闭合 Invisible Ninja peek 前 discard reshuffle 的 borrowed owner 分流，不外推 Titans/Ninjas 全量、所有 peek/reveal caller、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `wizard_neophyte / borrowed source-deck top action -> current player hand or true-owner extra action attach` 已从 Wizards owner/source provenance sibling 开放候选降级为 dedicated green evidence。源码复核确认 [`wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `wizardNeophyteProgram` 处理的是当前玩家自己的 deck 顶牌；选择 `to_hand` 时应通过 `CARDS_DRAWN.playerId=context.playerId` 进入当前玩家 hand，选择 `play_extra` 时则通过 `resolveWizardExternalActionPlay(...)` 保留 `sourceCard.owner` 到 `ACTION_PLAYED.ownerId` 与 `ONGOING_ATTACHED.ownerId`。新增 [`wizard-neophyte-ongoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts>) 两条 focused gate，分别锁住 `borrowed-summon(owner='1')` 由 P0 deck 进入 P0 hand 且 P1 deck 不动，以及 `borrowed-overrun(owner='1')` 由 P0 deck 移除、以 `ownerId='1'` 记录 `ACTION_PLAYED/ONGOING_ATTACHED` 并附着到基地。验证：focused `borrowed` => `1 file passed, 2 passed, 3 skipped`；整文件 `wizard-neophyte-ongoing.test.ts` => `1 file passed, 5 passed`；ESLint => `0 errors`。该格不改 runtime，只证明 `wizard_neophyte` 当前的 current source-deck/current-hand 与 play-extra true-owner provenance 成立，不外推 Wizards 全量、`wizard_scry`、`wizard_mass_enchantment`、所有 external action caller、所有 topdeck reveal/peek caller 或所有 borrowed-card zone producer 已完成。
  - `wizard_scry / borrowed source-deck action -> current player hand + current deck shuffle` 已从 Wizards owner/source provenance sibling 开放候选降级为 dedicated green evidence。源码复核确认 [`wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `wizardScryPromptProgram` 语义是“从当前玩家自己的 deck 中选择一张行动放入当前玩家 hand，并重洗当前玩家 deck”；即使该 source deck 内存在 `owner='1'` 的 borrowed action，效果文本的落点仍是当前玩家手牌，不应机械套 `CARD_TO_DECK_TOP/BOTTOM` 那类 owner deck split。新增 [`query6Abilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/query6Abilities.test.ts>) focused gate，P0 牌库含 `borrowed-action(owner='1')`、P1 牌库含 `p1-tail`；P0 打出 `wizard_scry` 后选择该 borrowed action，断言 `CARDS_DRAWN.playerId='0'`、`DECK_REORDERED.playerId='0'` 且无 `sourcePlayerId`，最终 `borrowed-action` 进入 P0 hand，P0 deck 只剩 `own-minion/own-action`，P1 deck 保持 `['p1-tail']`。验证：focused `wizard_scry` => `1 file passed, 5 passed`；整文件 `query6Abilities.test.ts` => `1 file passed, 34 passed`。该格不改 runtime，只证明 `wizard_scry` 属于 source-deck/source-hand 语义，不外推 Wizards 全量、`wizard_neophyte`、所有 deck search/draw 或所有 borrowed-card zone producer 已完成。
  - `tricksters_big_funny_giant / queued onMinionPlayed -> eventPlayer discard prompt` 已从 queued trigger registration/source-context 开放候选降级为 dedicated green evidence。它和 Cthulhu Altar / Gold Strike 同属“默认 eventPlayer 但语义正确”的分支：Big Funny Giant 的 `onMinionPlayed` trigger 在对手把随从打到泰坦所在基地时触发，真正要弃手牌的是打出随从的事件玩家，而不是泰坦控制者。新增 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) focused gate，把 `sourceControllerId=P0`、`eventPlayer=P1` 拆开，并让 P1 持有两张可弃手牌；断言 queue 保持 `playerContext='eventPlayer'`，resolve 后 prompt 的 `playerId='1'`，候选只含 P1 手牌，不含 P0 手牌。验证：focused `Big Funny Giant` => `1 file passed, 3 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 119 passed`；ESLint `reactionQueueEventPlayerContext.test.ts` => `0 errors`。`smashup.smoke.test.ts --testNamePattern "Big Funny Giant|tricksters_big_funny_giant"` 当前整文件 skipped，不作为本格证据。该格不改 runtime，只证明 Big Funny Giant 不应机械套 sourceController；不外推 Tricksters 全量、所有 Titan、所有 onMinionPlayed trigger 或所有 eventPlayer 分支已完成。
  - `samurai_way_of_the_warrior / samurai_way_of_the_warrior_pod / global queued onMinionDiscardedFromBase -> metadata drawPlayerId` 已从 Samurai sibling 开放候选降级为 dedicated green evidence。它不同于 Samurai Chan/Bushi/Shogun/Final Haiku/Honor the Fallen：注册是 `global:true`，callback `samuraiWayOfTheWarriorTrigger()` 不读取 `ctx.sourceControllerId` 或 `ctx.playerId` 决定抽牌者，而是读取离场随从 LKI metadata 中的 `samuraiWayOfTheWarriorDrawPlayerId`。新增 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 两条 focused gate，把 `drawPlayerId=P0`、`eventPlayer=P1`、`source card owner=P2` 三者拆开；普通版与 POD 版都确认 queue 仍为 `playerContext='eventPlayer'`，但真实 resolve 只给 metadata 里的 P0 抽两张，不给事件玩家 P1 或 source owner P2 抽。验证：focused `Way of the Warrior` => `1 file passed, 2 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 118 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_way_of_the_warrior|Way of the Warrior"` => `1 file passed, 7 passed`；`baseScoring.test.ts --testNamePattern "Way of the Warrior|samurai_way_of_the_warrior|samurai_way_of_the_warrior_pod"` => `1 file passed, 2 passed`；ESLint `reactionQueueEventPlayerContext.test.ts` => `0 errors`。该格不改 runtime，只证明 Way of the Warrior 不应机械套用 sourceController sibling 修复；不外推 Samurai 全量、所有 global trigger、所有 metadata-driven trigger 或所有 borrowed-action 场景已完成。
  - `samurai_honor_the_fallen / samurai_honor_the_fallen_pod / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai base-ongoing draw seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Honor the Fallen 与 Honor the Fallen POD 两组 per-instance trigger 已有 `sourceScope:'triggerBase'`，能限定同基地；但未显式写 `playerContext:'sourceController'`，而 callback `samuraiHonorTheFallenTrigger()` 用 `ctx.sourceControllerId` 抽牌。因此 P1 拥有的 Honor the Fallen 在基地上，P1 随从离场但以 P0 作为事件玩家触发时，queue metadata 中 `sourceControllerId='1'` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_honor_the_fallen` 与 `samurai_honor_the_fallen_pod` 两组离场 trigger 补 `playerContext:'sourceController'`，不改 `sourceScope:'triggerBase'`、抽牌回调、计分或 Way of the Warrior。验证：focused `Honor the Fallen 归给基地行动拥有者` 首轮红灯后 `1 file passed, 1 passed`；focused `Honor the Fallen POD 归给基地行动拥有者` `1 file passed, 1 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 116 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_honor_the_fallen|Honor the Fallen"` => `1 file passed, 1 passed`；`baseScoring.test.ts --testNamePattern "Honor the Fallen|samurai_honor_the_fallen|samurai_honor_the_fallen_pod"` => `1 file passed, 2 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Honor the Fallen/Honor the Fallen POD queued owner metadata，不外推 Samurai 全量、Way of the Warrior sibling、所有 base ongoing 离场 trigger、所有计分离场路径或整个 sourceController/eventPlayer family。
  - `samurai_final_haiku / samurai_final_haiku_pod / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai attached-action temp-power seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Final Haiku 与 Final Haiku POD 两组 per-instance trigger 注册未显式写 `playerContext:'sourceController'`，而 callback `samuraiFinalHaikuTrigger()` 用 `ctx.sourceControllerId` 给该玩家其他随从 +2 临时力量；因此 P1 拥有的 Final Haiku 附着在 P1 宿主上，但以 P0 作为事件玩家触发宿主离场时，queue metadata 中 `sourceControllerId='1'` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_final_haiku` 与 `samurai_final_haiku_pod` 两组离场 trigger 补 `playerContext:'sourceController'`，不改 attached host/LKI fallback、临时力量、计分或 Honor the Fallen/Way of the Warrior。验证：focused `Final Haiku 归给附着行动拥有者` 首轮红灯后 `1 file passed, 1 passed`；focused `Final Haiku POD 归给附着行动拥有者` `1 file passed, 1 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 114 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_final_haiku|samurai_final_haiku_pod"` => `1 file passed, 3 passed`；`baseScoring.test.ts --testNamePattern "Final Haiku|samurai_final_haiku|samurai_final_haiku_pod"` => `1 file passed, 2 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Final Haiku/Final Haiku POD queued owner metadata，不外推 Samurai 全量、Honor the Fallen/Way of the Warrior sibling、所有 attached-action 离场 trigger、所有计分离场路径或整个 sourceController/eventPlayer family。
  - `samurai_shogun / samurai_shogun_pod / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai other-friendly add-counter seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Shogun 与 Shogun POD 两组 per-instance trigger 注册未显式写 `playerContext:'sourceController'`，而 callback `samuraiShogunTrigger()` 用 `ctx.sourceControllerId` 判定离场随从是否为己方，并给仍在场的 Shogun 自身加 1 个力量指示物；因此 P1 控制 Shogun/Shogun POD 与另一名 P1 随从，但以 P0 作为事件玩家触发另一名随从离场时，queue metadata 中 `sourceControllerId='1'` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_shogun` 与 `samurai_shogun_pod` 两组离场 trigger 补 `playerContext:'sourceController'`，不改同阵营判定、加标记、计分、LKI 或 Final Haiku。验证：focused `Shogun 归给自身控制者` 首轮红灯后 `1 file passed, 1 passed`；focused `Shogun POD 归给自身控制者` `1 file passed, 1 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 112 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_shogun|samurai_shogun_pod"` => `1 file passed, 2 passed`；`baseScoring.test.ts --testNamePattern "Shogun|samurai_shogun|samurai_shogun_pod"` => `1 file passed, 2 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Shogun/Shogun POD queued owner metadata，不外推 Samurai 全量、Final Haiku sibling、所有 other-friendly 离场 trigger、所有计分离场路径或整个 sourceController/eventPlayer family。
  - `samurai_bushi / samurai_bushi_pod / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai self-source VP seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Bushi 与 Bushi POD 两组 per-instance trigger 注册未显式写 `playerContext:'sourceController'`，而 callback `samuraiBushiTrigger()` 用 `ctx.sourceControllerId` 奖励 VP，并依赖 `triggerMinionPower` / LKI 力量快照；因此 P1 控制 5 力 Bushi/Bushi POD 但以 P0 作为事件玩家触发离场时，queue metadata 中 `sourceControllerId='1'` 与 `triggerMinionPower=5` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_bushi` 与 `samurai_bushi_pod` 两组离场 trigger 补 `playerContext:'sourceController'`，不改力量阈值、VP 奖励、LKI、计分或 Shogun/Final Haiku。验证：focused `Bushi 归给自身控制者`、`Bushi POD 归给自身控制者` 均首轮红灯后转绿；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 110 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_bushi|samurai_bushi_pod"` => `1 file passed, 2 passed`；`baseScoring.test.ts --testNamePattern "Bushi|samurai_bushi|samurai_bushi_pod"` => `1 file passed, 1 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Bushi/Bushi POD queued owner metadata，不外推 Samurai 全量、Shogun/Final Haiku sibling、所有 self-source 离场 trigger 或所有计分离场路径。
  - `samurai_samurai_chan_pod / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai POD self-source seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Samurai Chan POD 两个 per-instance trigger 注册未显式写 `playerContext:'sourceController'`，callback 复用 `samuraiChanTrigger()` 并用 `ctx.sourceControllerId` 抽牌；因此 P1 控制 `samurai-chan-pod-a` 但以 P0 作为事件玩家触发离场时，queue metadata 中 `sourceControllerId='1'` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_samurai_chan_pod` 两个离场 trigger 补 `playerContext:'sourceController'`，不改普通版、Bushi/Shogun/Final Haiku 或抽牌回调。验证：focused `Samurai Chan POD 归给自身控制者` 首轮红灯后 `1 file passed, 1 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 108 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_samurai_chan|samurai_samurai_chan_pod"` => `1 file passed, 2 passed`；`baseScoring.test.ts --testNamePattern "Samurai Chan|samurai_samurai_chan|samurai_samurai_chan_pod"` => `1 file passed, 2 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Samurai Chan POD queued owner metadata，不外推 Samurai 全量、Bushi/Shogun/Final Haiku sibling、所有 self-source 离场 trigger 或所有计分离场路径。
  - `samurai_samurai_chan / queued onMinionDestroyed/onMinionDiscardedFromBase -> ownerPlayerId/playerContext` 继续沿 queued trigger registration 默认 eventPlayer family 命中新的 Samurai self-source seam。旧 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 Samurai Chan 两个 per-instance trigger 注册未显式写 `playerContext:'sourceController'`，而 callback `samuraiChanTrigger()` 用 `ctx.sourceControllerId` 抽牌；因此 P1 控制 `samurai-chan-a` 但以 P0 作为事件玩家触发离场时，queue metadata 中 `sourceControllerId='1'` 已正确，`ownerPlayerId/playerContext` 却落成 P0 / `eventPlayer`。现已最小收口为：只给 `samurai_samurai_chan` 两个离场 trigger 补 `playerContext:'sourceController'`，不改 POD、Bushi/Shogun/Final Haiku 或抽牌回调。验证：focused `Samurai Chan 归给自身控制者` 首轮红灯后 `1 file passed, 2 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 107 passed`；`newFactionAbilities.test.ts --testNamePattern "samurai_samurai_chan"` => `1 file passed, 2 passed`；`baseScoring.test.ts --testNamePattern "Samurai Chan|samurai_samurai_chan"` => `1 file passed, 2 passed`；ESLint `samurai.ts + reactionQueueEventPlayerContext.test.ts` => `0 errors, 12 existing any warnings`。该格只闭合 Samurai Chan queued owner metadata，不外推 Samurai 全量、POD 版、Bushi/Shogun/Final Haiku sibling、所有 self-source 离场 trigger 或所有计分离场路径。
  - `steampunk_change_of_venue_choose_minion / choose_base -> ACTION_PLAYED.ownerId` 继续沿 handbuilt action-play producer provenance family 补齐一条 same-owner direct gate。旧 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 的 Change of Venue 首段已经把 `selected.ownerId` 写进 `replayOwnerId`，但二段 `steampunk_change_of_venue_choose_minion` / `steampunk_change_of_venue_choose_base` 调用 `buildActionPlayedEvent(...)` 时没有传 `ownerId`；因此即使当前候选生成只允许 `ownerId === playerId` 的 ongoing，`ACTION_PLAYED.payload.ownerId` 仍缺失。现已最小收口为：两个二段 target prompt 与非 ongoing replay fallback 都把已有 `context.replayOwnerId` / `selected.ownerId` 传给 `buildActionPlayedEvent(...)`。收紧 [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 现有 minion/base target-context 两条 gate，首轮红灯坐实两条 `ACTION_PLAYED.payload.ownerId === undefined`，修复后锁住 `ownerId='0'`，且原 `targetBaseIndex/targetType/targetMinionUid` 仍保留。验证：focused 两条 => 首轮红灯后 `1 file passed, 2 passed`；Steampunks replay 邻近 => `1 file passed, 7 passed`；整文件 `expansionOngoing.test.ts` => `1 file passed, 74 passed`；ESLint `steampunks.ts + expansionOngoing.test.ts` => `0 errors`。该格只闭合 Change of Venue replay 的 `ACTION_PLAYED.ownerId` 字段完整性，当前不证明 borrowed owner 分离；不外推所有 Steampunks、所有 Change of Venue 分支、所有 replay caller 或所有 action-play producer 已完成。
  - `vampire_mad_monster_party_pod / vampire_buffet_pod triggered play -> ACTION_PLAYED.ownerId` 继续沿 handbuilt action-play producer / borrowed owner provenance family 命中新的 Vampires POD seam。旧 [`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 两个 `onMinionDestroyed` trigger 都从 destroyer 手牌找到可打出的 POD action，但 prompt context 只保存 `cardUid/defId`，`vampire_mad_monster_party_pod_play` 与 `vampire_buffet_pod_play` 最终调用 `buildActionPlayedEvent(...)` 时没有传真实 owner；因此 P0 从手牌额外打出 `owner='1'` 的 borrowed `vampire_mad_monster_party_pod` 或 `vampire_buffet_pod` 时，`ACTION_PLAYED.payload.ownerId` 仍缺失。现已最小收口为：两个 prompt context 在创建时快照 `ownerId: card.owner`，resolve 时传入 `buildActionPlayedEvent(...)`。收紧 [`reactionQueueDestroyerId.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueDestroyerId.test.ts>) 现有 target-base / untargeted 两条 gate，首轮红灯坐实两条 `ACTION_PLAYED.payload.ownerId === undefined`，修复后锁住 `ownerId='1'`，同时继续保留 Mad Monster Party 的 `targetBaseIndex/targetType` 与 Buffet 的 untargeted 语义。验证：focused 两条 => 首轮红灯后 `1 file passed, 2 passed`；整文件 `reactionQueueDestroyerId.test.ts` => `1 file passed, 4 passed`；Vampire POD 邻近 `vampiresPod.test.ts + vampireBuffetE2E.test.ts` 同 pattern => `1 file passed, 1 passed, 1 file skipped`；ESLint `vampires.ts + reactionQueueDestroyerId.test.ts` => `0 errors, 43 existing any warnings`。该格只闭合这两个 Vampires POD triggered extra action play 的 `ACTION_PLAYED.ownerId` provenance，不外推 Vampires 全量、所有 POD、所有 onMinionDestroyed trigger、所有 action-play producer 或所有 borrowed owner/source seam 已完成。
  - `ghosts_creampuff_man / external discard borrowed standard action -> ACTION_PLAYED.ownerId` 继续沿 handbuilt action-play producer / borrowed owner provenance family 命中新的 Titan/Ghosts seam。旧 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `resolveCreampuffActionPlay()` 已从 P0 discard 找到 `actionCard` 并读取 `ownerId = actionCard.owner`，同批 `CARD_TO_DECK_BOTTOM` 也会把 borrowed action 沉回真实 owner 牌库底；但 `buildActionPlayedEvent(...)` 没有传 `ownerId`，导致 P0 从弃牌堆额外打出 `owner='1'` 的 `borrowed-bananas` 时，`ACTION_PLAYED.payload.ownerId` 仍缺失。现已最小收口为：Cream Puff Man external discard action play 调用 `buildActionPlayedEvent(...)` 时传入 `ownerId`。收紧 [`reactionQueueActionPlayedExternalDiscard.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueActionPlayedExternalDiscard.test.ts>) existing gate `Cream Puff Man 从弃牌堆额外打出被他人拥有的标准行动后，仍应沉回其拥有者牌库底`，首轮红灯坐实 `ACTION_PLAYED.payload.ownerId === undefined`，修复后锁住 `ownerId='1'` 与既有 `CARD_TO_DECK_BOTTOM.ownerId='1'/sourcePlayerId='0'`。验证：focused 首轮红灯后 => `1 file passed, 1 passed`；整文件 `reactionQueueActionPlayedExternalDiscard.test.ts` => `1 file passed, 3 passed`；`smashup.smoke.test.ts --testNamePattern "Cream Puff|奶油泡芙|creampuff|ghosts_creampuff_man"` => `1 file passed, 3 passed`；`commandsValidation.test.ts` 同 pattern 全跳过，不作为证据。该格只闭合 Cream Puff Man 从弃牌堆额外打出 borrowed standard action 的 `ACTION_PLAYED.ownerId` provenance，不外推所有 Titans/Ghosts、所有 external discard replay、所有 Cream Puff target 分支或所有 action-play producer 已完成。
  - `miskatonic_librarian_pod_play_madness / borrowed Madness extra play -> ACTION_PLAYED.ownerId` 继续沿 handbuilt action-play producer / borrowed owner provenance family 命中新的 Miskatonic seam。旧 [`miskatonic.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/miskatonic.ts>) 的 Librarian POD 额外打出 Madness 分支已经统一改用 `buildActionPlayedEvent(...)`，但只传 `playerId/cardUid/defId/isExtraAction/timestamp`，没有把所选手牌 `card.owner` 传入 `ownerId`；因此 P0 手牌中 `owner='1'` 的 borrowed `special_madness` 被额外打出时，最终会进入 P1 discard，但同批 `ACTION_PLAYED.payload.ownerId` 缺失。现已最小收口为：该分支调用 `buildActionPlayedEvent(...)` 时传 `ownerId: card.owner`，不改 Madness onPlay、候选生成或其它 Miskatonic 分支。新增/保留 [`cthulhuExpansionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/cthulhuExpansionAbilities.test.ts>) focused gate `额外打出被他人拥有的疯狂卡时，应保留 ACTION_PLAYED owner 并进入拥有者弃牌堆`，首轮红灯坐实 `ACTION_PLAYED.payload.ownerId === undefined`，修复后锁住 `ownerId='1'`、P0 hand/discard 不含 `borrowed-madness`、P1 discard 追加 `borrowed-madness`。验证：focused `miskatonic_librarian_pod.*被他人拥有` 首轮红灯后 => `1 file passed, 1 passed`；邻近 `miskatonic_librarian_pod|miskatonic_those_meddling_kids|cthulhu_it_begins_again` => `1 file passed, 13 passed`；整文件 `cthulhuExpansionAbilities.test.ts` => `1 file passed, 36 passed`。该格只闭合 Librarian POD 从手牌额外打出 borrowed Madness 的 `ACTION_PLAYED.ownerId` provenance，不外推 Miskatonic 全量、Madness 全量、所有 extra action caller 或所有 action-play side branch 已完成。
  - `bear_cavalry_high_ground_pod / queued onMinionMoved trigger ownerPlayerId -> sourceController` 继续沿 `sourceController/eventPlayer` queued trigger family 命中新的 Bear Cavalry seam。旧 [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>) 注册 `registerTrigger('bear_cavalry_high_ground_pod','onMinionMoved',...)` 时未显式写 `playerContext:'sourceController'`，因此当 P1 随从移入带有 P0 `High Ground POD` 的基地时，queued trigger 的 `sourceControllerId` 虽为 P0，但 `ownerPlayerId/playerContext` 仍落成事件玩家 P1 / `eventPlayer`。现已最小收口为：只给该 trigger 注册补 `playerContext:'sourceController'`，不改 prompt resolver 或自动 destroy 分支。新增 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) focused gate `sourceController queued onMinionMoved trigger 应把 High Ground POD 归给制高点拥有者而不是移入随从玩家`，首轮红灯坐实 `ownerPlayerId='1', playerContext='eventPlayer'`，修复后锁住 `sourceCardUid='high-ground-pod-a'`、`sourceControllerId='0'`、`ownerPlayerId='0'`、`eventPlayerId='1'`、`playerContext='sourceController'`。验证：focused `reactionQueueEventPlayerContext.test.ts --testNamePattern "High Ground POD|sourceController queued onMinionMoved trigger 不应把 eventPlayer 误当成泰坦控制者"` => `1 file passed, 2 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 103 passed`；`feedback-high-ground-destroyer.test.ts` => `1 file passed, 2 passed`；`newOngoingAbilities.test.ts --testNamePattern "bear_cavalry_high_ground"` => `1 file passed, 2 passed`；ESLint 0 errors / 65 existing warnings。该格只闭合 High Ground POD 的 queued trigger owner metadata，不外推所有 Bear Cavalry、所有 `onMinionMoved` trigger 或所有 sourceController/eventPlayer family 已完成。
  - `steampunk_mechanic / borrowed discard ongoing replay -> action-play owner + source hand` 继续沿 handbuilt producer / replay action family 命中新的 Steampunks residual。旧 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 的 Mechanic 先用 `CARD_RECOVERED_FROM_DISCARD` 取回弃牌堆行动；但 shared reducer 现在按真实 owner 回手，导致 P0 从自己 discard 选择 `owner='1'` 的 borrowed `steampunk_escape_hatch` 后进入 P1 hand，下一步 `steampunk_mechanic_target` 被 `inHand(P0)` 拦成空事件。同时目标分支的 `ACTION_PLAYED` 未携带真实 owner。现已最小收口为：Mechanic 这条“从你的弃牌堆打出”改用同玩家 `CARD_TRANSFERRED(fromPlayerId=toPlayerId=playerId)`，保留卡实例 owner 但让 source card 进入行动玩家手牌；`buildActionPlayedEvent(...)` 同步传 `ownerId: context.replayOwnerId`。新增/收紧 [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) gate `steampunk_mechanic_target: 重打被他人拥有的 ongoing 时仍应保留 owner 并带 sourcePlayerId`，首轮红灯坐实 `resolved.events=[]`，修复后锁住 `ACTION_PLAYED.ownerId='1'`、`ONGOING_ATTACHED.ownerId='1'`、`sourcePlayerId='0'`，且最终 P0 hand 不再含 `dis-borrowed`、基地 ongoing 保留真实 owner。验证：focused => `1 file passed, 1 passed`；Steampunks replay 邻近 4 条 => `1 file passed, 4 passed`；整文件 `expansionOngoing.test.ts` => `1 file passed, 74 passed`；ESLint `steampunks.ts + expansionOngoing.test.ts` => 0 errors。该格只闭合 `steampunk_mechanic` 从弃牌堆重打 borrowed ongoing 的 action-play owner/source hand seam，不外推 Change of Venue、所有 `CARD_RECOVERED_FROM_DISCARD` caller、所有 `CARD_TRANSFERRED` caller 或 Steampunks 全量。
  - `base_innsmouth_base_choose_card / borrowed discard card -> owner deck bottom` 继续沿 `CARD_TO_DECK_BOTTOM(owner/source provenance)` family 命中新的 expansion base 真缝。旧 [`baseAbilities_expansion.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities_expansion.ts>) 的 `base_innsmouth_base_choose_player` 第二步把目标玩家弃牌堆候选写成 `ownerId=targetPlayerId`；因此 `P0.discard` 中 `owner='1'` 的 borrowed card 被 Innsmouth Base 选择后会错误沉入 P0 deck bottom。现已最小收口为：候选携带真实 `c.owner` 与 `sourcePlayerId=targetPlayerId`，`base_innsmouth_base_choose_card` handler 按真实 owner 调用 `buildValidatedCardToDeckBottomEvents(...)`。新增 [`expansionBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionBaseAbilities.test.ts>) focused gate `base_innsmouth_base_choose_card: 选择 borrowed 弃牌堆卡时应放到其拥有者牌库底`，锁住 borrowed card 从 P0 discard 移除、不进入 P0 deck，并进入 P1 deck bottom。验证：focused `expansionBaseAbilities.test.ts --testNamePattern "base_innsmouth_base_choose_card: 选择 borrowed|queued reaction 选择温室"` => `1 file passed, 2 passed`；整文件 `expansionBaseAbilities.test.ts` => `1 file passed, 51 passed`；`git diff --check` 仅 LF/CRLF warning。该格只闭合 Innsmouth Base 第二步 discard card -> owner deck bottom，不外推所有 expansion base ability、所有 discard-to-bottom caller 或所有 `CARD_TO_DECK_BOTTOM` producer 已完成。
  - `super_spies_moon_zero_three / borrowed inspected deck top -> owner deck bottom` 继续沿 inspect/peek continuation owner provenance 命中新的 Titan seam。旧 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `titan_super_spies_moon_zero_three_choose_player` 只把 `targetPlayerId/cardUid/defId` 写进 continuation，`titan_super_spies_moon_zero_three_resolve` 在选择“放到牌库底”时直接把 `ownerId` 写成 `targetPlayerId`；因此 P1 deck 顶若是 `owner='0'` 的 borrowed card，会被错误留在 P1 deck 底，而不是回到真实 owner P0 deck 底。现已最小收口为：choose-player 阶段把 `peek.card.owner` 写入 continuation，resolve 阶段按 `ownerId` 发 `CARD_TO_DECK_BOTTOM`，borrowed 场景补 `sourcePlayerId=targetPlayerId`。新增 [`smashup.smoke.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/smashup.smoke.test.ts>) focused gate `三号空间站天赋查看 borrowed 牌库顶并放到底部时，应进入其拥有者牌库底`，首轮红灯坐实 `borrowed-moon-top` 未进入 P0 deck，修复后锁住 P0 deck `['p0-deck-a','borrowed-moon-top']`、P1 deck `['moon-target-next']`。验证：`smashup.smoke.test.ts --testNamePattern "三号空间站天赋"` => 首轮红灯后 `1 file passed, 2 passed`；`yuanhouFactionAbilities.test.ts --testNamePattern "super_spies_moon_zero_three|三号空间站"` => `1 file passed, 7 passed`。这格只闭合 Moon Zero Three talent inspect -> bottom 的 borrowed owner continuation，不外推整个 Super Spies、所有 Titan、所有 inspectDeck/peekDeckTop caller 或所有 `CARD_TO_DECK_BOTTOM` producer 已完成。
  - `cthulhu_servitor / borrowed discard action -> owner deck top` 继续沿 `DECK_REORDERED(sourcePlayerId)` family 收掉一条新的 Cthulhu sibling seam，不是 Servitor 自毁 owner 修复的复读。旧 [`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 的 `cthulhuServitorPromptProgram.onResolve` 从当前玩家 discard 选择行动后，直接写 `DECK_REORDERED.playerId=playerId` 与 `[actionCard.uid, ...player.deck]`；因此 `P0.discard` 中 `owner='1'` 的 borrowed action 会被错误放进 P0 deck 顶。现已最小收口为：所选行动仍从当前玩家 discard live 复核，但 `DECK_REORDERED.playerId` 与 deck snapshot 改按 `actionCard.owner`，borrowed 场景补 `sourcePlayerId=playerId`。新增 [`talentAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/talentAbilities.test.ts>) focused gate `选择被他人拥有的弃牌行动时，仍应放回其拥有者牌库顶而不是当前玩家牌库顶`，用真实 `runCommand(USE_TALENT -> RESPOND)` 锁住 `borrowed-action` 进入 P1 deck 顶且不留在 P0 discard/P0 deck。验证：`talentAbilities.test.ts --testNamePattern "cthulhu_servitor.*选择被他人拥有|cthulhu_servitor（仆人 talent）"` => `1 file passed, 6 passed`；`cthulhuExpansionAbilities.test.ts --testNamePattern "cthulhu_it_begins_again|选择被他人拥有的弃牌行动"` => `1 file passed, 6 passed`；整文件 `talentAbilities.test.ts` => `1 file passed, 22 passed`；ESLint `npx eslint src/games/smashup/abilities/cthulhu.ts src/games/smashup/__tests__/talentAbilities.test.ts` => `0 errors, 7 warnings`（既有 unused warnings）。这格只闭合 Servitor talent 后续 discard action -> owner deck top，不外推整个 Cthulhu、所有 Servitor 分支、所有 discard action topdeck/shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `base_drakkar / Vikings empty-deck discard reshuffle borrowed card -> owner deck split` 继续沿 `DECK_REORDERED(sourcePlayerId)` family 收掉一条新的 Vikings/base 交叉 seam。旧 [`vikings.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vikings.ts>) 的 `getTopDeckCardWithReshuffle()` / `prepareTopDeckCards()` 在目标玩家 deck 为空时会直接把该玩家 discard 全量洗成 `DECK_REORDERED.playerId=targetPlayerId`；因此 `P1.discard` 中 `owner='0'` 的 borrowed card 会被错误洗进 P1 deck，并可能被 `base_drakkar` / `vikings_shield_maiden` 揭示后转移。现已最小收口为：新增本地 `prepareTopDeckForVikingPeek(...)`，空 deck 重洗时按真实 `card.owner` 分流 borrowed cards 到 owner-scoped deck，目标玩家 snapshot 只保留目标自有或无有效 owner fallback 卡；`resolveRevealAndStealTopCard(...)` 与 `prepareTopDeckCards(...)` 复用该 helper。新增 [`newBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newBaseAbilities.test.ts>) focused gate `base_drakkar 目标弃牌堆重洗时应分流 borrowed 卡，只揭示目标玩家自有牌`，锁住 `borrowed-d2(owner='0')` 回到 P0 deck，`own-d3(owner='1')` 被 Drakkar 揭示/转移到 P0 hand。验证：focused `newBaseAbilities.test.ts --testNamePattern "base_drakkar.*borrowed|base_drakkar 会在目标牌库为空"` => `1 file passed, 2 passed`；`base_drakkar` 子集 => `1 file passed, 5 passed`；Vikings peek 邻近 `newFactionAbilities.test.ts --testNamePattern "vikings_shield_maiden|vikings_cast_the_runes|vikings_raiding_party"` => `1 file passed, 9 passed`；Vikings 子集 `vikings_` => `1 file passed, 14 passed`；`base_drakkar|base_longhouse` 子集 => `1 file passed, 7 passed`；ESLint `npx eslint src/games/smashup/abilities/vikings.ts src/games/smashup/__tests__/newBaseAbilities.test.ts` => `0 errors, 22 warnings`（既有 `any` warnings）。这格只闭合 Vikings peek/prepare helper 的 empty-deck reshuffle borrowed 分流，不外推所有 `peekDeckTop` caller、所有 `DECK_REORDERED` producer、所有 Vikings 或所有 base reveal/steal 能力已完成。
  - `princesses_sleeping_beauty / borrowed discard fallback -> owner deck shuffle` 继续沿 `onMinionDiscardedFromBase` 与 owner/source provenance 交叉面补掉一条新的 fallback 真缝。旧 [`princesses.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/princesses.ts>) 的 `princessesSleepingBeautyOnDiscarded()` 在没有 `ctx.triggerMinion` LKI、只能从 discard fallback 定位时，会把“卡在哪个玩家 discard”当作洗回牌库的 owner；因此 `borrowed-sleep-a(owner='1')` 位于 `P0.discard` 时会被错误留在 P0 区域。现已最小收口为：fallback 从 discard card 实例读取 `card.owner` 作为真实 owner，并把 discard 所在玩家作为 `sourcePlayerId` 传入 `shuffleCardIntoDeck()` / `buildValidatedCardToDeckBottomEvents()` / `DECK_REORDERED`。新增 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) focused gate `eventPlayer queued onMinionDiscardedFromBase trigger 从弃牌堆 fallback 定位 borrowed Sleeping Beauty 时仍应洗回真实拥有者牌库`，锁住 `borrowed-sleep-a` 从 `P0.discard` 移除、不进入 `P0.deck`，并进入 `P1.deck=['owner-deck-a','borrowed-sleep-a']`。验证：focused `reactionQueueEventPlayerContext.test.ts --testNamePattern "borrowed Sleeping Beauty"` => `1 file passed, 1 passed`；整文件 `reactionQueueEventPlayerContext.test.ts` => `1 file passed, 102 passed`；Princesses 邻近 `newFactionAbilities.test.ts --testNamePattern "princesses_sleeping_beauty|princesses_direct_to_dvd_sequel|princesses_woodland_helpers|princesses_marie_degraw"` => `1 file passed, 7 passed`。这格只闭合 Sleeping Beauty 的 discard fallback / borrowed owner 分支，不外推整个 Princesses、所有 Sleeping Beauty 场景、所有 `onMinionDiscardedFromBase` trigger 或所有 deck shuffle producer 已完成。
  - `vikings_raiding_party / refreshed revealed action -> owner discard` 继续处理 Cast the Runes 扩大验证时暴露的 Vikings residual。旧红点不是生产逻辑仍坏，而是 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 的断言仍期待 P1-owned `top-action` 被 P0 通过 Raiding Party 额外打出后进入 P0 discard；这与当前已收口的 `ACTION_PLAYED.ownerId` 合同冲突。现已把该测试改为同时锁住两件事：刷新后的候选只剩当前 deck top snapshot 中的 `top-action/top-extra`，且额外打出的 P1-owned `top-action` 进入 P1 discard、不会进入 P0 discard。随后补 dedicated green gate 覆盖 targetPlayerId 与真实 owner 分离的 borrowed action：`borrowed-action(owner='0')` 从 P1 deck 被 P0 额外打出后进入 P0 discard、不会进入 P1 discard。验证：`newFactionAbilities.test.ts --testNamePattern "vikings_raiding_party"` => `1 file passed, 4 passed`。此格只闭合 Raiding Party 这条旧断言漂移、refreshed revealed action -> owner discard，以及 borrowed owner != targetPlayerId 的相邻落点证据；不外推 Vikings 全量、所有外部打出 caller 或所有 `CARD_TRANSFERRED -> PLAY_ACTION -> ACTION_PLAYED` 链已完成。
  - `vikings_cast_the_runes / borrowed revealed card -> owner deck top` 继续沿 revealed/order -> deck top owner provenance family 命中新的 Vikings 对象级真缝。旧 [`vikings.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vikings.ts>) 的 `VikingCastRunesOrderPromptContext.revealedCards` 只保存 `uid/defId`，排序 resolve 时把 `[topCard,...rest,...liveRemainingDeck]` 统一写成 `DECK_REORDERED.playerId=targetPlayerId`；因此 P1 deck 顶被揭示出的 `owner='0'` borrowed card 会错误留在 P1 deck。现已最小收口为：Cast the Runes 揭示快照携带 `owner`，排序回顶时把非 target owner 的 revealed cards 按真实 owner 分流为 owner-scoped `DECK_REORDERED(sourcePlayerId=targetPlayerId)`，target deck 只保留 target-owned 或无有效 owner 的 revealed cards 与 live remaining deck。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) focused gate，首轮红灯坐实 `P1.deck=['own-top','borrowed-top','rest-1']`，修复后锁住 `borrowed-top` 进入 P0 deck。验证：focused `newFactionAbilities.test.ts --testNamePattern "vikings_cast_the_runes_order 排序 borrowed"` 首轮红灯后转绿 => `1 file passed, 1 passed`；`vikings_cast_the_runes` 子集 => `1 file passed, 2 passed`。扩大到 `vikings_raiding_party` 时命中旧断言红点，该红点断言 P1-owned 牌被 P0 额外打出后应进 P0 discard，和当前 owner provenance 口径冲突，未纳入本格完成证据。此格只闭合 Cast the Runes 的 borrowed revealed card -> owner deck top，不外推 Vikings 全量、Raiding Party 或所有 reveal/order caller 已完成。
  - `innsmouth_new_acolytes / borrowed discard minion -> owner deck shuffle` 继续沿 discard -> deck shuffle owner provenance family 命中新的 Innsmouth 对象级真缝。旧 [`innsmouth.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/innsmouth.ts>) 的 `innsmouthNewAcolytes()` 按每位玩家扫描 discard 中所有 minion，然后直接把这些随从与该玩家 deck 合并为 `DECK_REORDERED.playerId=pid`；因此 `P0.discard` 中 `owner='1'` 的 `borrowed-minion` 会错误进入 P0 deck。现已最小收口为：仍按 source player 的 discard 扫描本次要洗回的随从，但按真实 `card.owner` 分组到 owner deck，borrowed 场景补 `sourcePlayerId: pid`，并用 `workingDecks` 避免同一 owner 接收多源卡时后续重排覆盖前一批。新增 [`cthulhuExpansionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/cthulhuExpansionAbilities.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['p0-deck-a','borrowed-minion','own-minion']`，修复后锁住 `borrowed-minion` 从 P0 discard 移除并进入 P1 deck。验证：focused `cthulhuExpansionAbilities.test.ts --testNamePattern "被他人拥有的弃牌随从"` 首轮红灯后转绿 => `1 file passed, 2 passed`；`innsmouth_new_acolytes|被他人拥有的弃牌随从` 邻近子集 => `1 file passed, 5 passed`；整文件 `cthulhuExpansionAbilities.test.ts` => `1 file passed, 35 passed`。这格只闭合 New Acolytes 的 borrowed discard minion -> owner deck shuffle，不外推 Innsmouth 全量、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `vampire_the_count_pod_talent / timed permanent power modifier` 继续收掉同文件整跑暴露的 runtime side-state 真缝。旧 [`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 The Count POD talent 把 `timedPowerModifiers` 写进 `matchState.core`，同时返回 `PERMANENT_POWER_ADDED` 事件；shared ability runtime 在产出 events 时会回退 `result.state.core` 来避免 domain events 双吃，导致这份计时 side-state 被丢弃。现已增量扩展 [`types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>) / [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>)：`PermanentPowerAddedEvent.payload.expiresOnTurnNumber` 由 reducer 写入 `timedPowerModifiers`；The Count POD talent 与 Wolf Pact POD minion -1 都改为返回带 expiry 的 `PERMANENT_POWER_ADDED` 事件。验证：focused `vampiresPod.test.ts --testNamePattern "talent 的 -1 应持续到自己下回合开始"` => `1 file passed, 1 passed`；整文件 `vampiresPod.test.ts` => `1 file passed, 12 passed`。这格只闭合 Vampires POD 两个“直到你的下回合开始”的 permanent modifier expiry 合同，不外推所有 timed effect、所有 runtime prompt side-state 或所有 `PERMANENT_POWER_ADDED` producer 已完成。
  - `vampire_wolf_pact_pod_action / borrowed discard card -> owner deck shuffle` 继续沿 `DECK_REORDERED(sourcePlayerId)` family 命中新的 Vampires POD 对象级真缝。源码复核确认 [`vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 `vampireWolfPactPodActionPromptProgram.onResolve` 旧实现从当前玩家 discard 选牌后，直接把 `player.deck + card.uid` 洗成 `DECK_REORDERED.playerId=playerId`；因此 `P0.discard` 中 `owner='1'` 的 `borrowed-discard` 会错误进入 P0 deck。现已最小收口为：source 仍从行动玩家 live discard 读取，但按 `card.owner` 分组到真实 owner deck，borrowed 场景补 `sourcePlayerId: playerId`。新增 [`vampiresPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/vampiresPod.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['p0-deck-a','borrowed-discard']`，修复后锁住 `borrowed-discard` 从 P0 discard 移除并进入 P1 deck。验证：focused `vampiresPod.test.ts --testNamePattern "战术面选择被他人拥有的弃牌"` => 首轮红灯后 `1 file passed, 1 passed`；邻近 `vampiresPod.test.ts --testNamePattern "Wolf Pact POD|狼之契约|战术面|随从面"` => `1 file passed, 3 passed`；整文件 `vampiresPod.test.ts` => `1 file passed, 12 passed`。这格只闭合 Wolf Pact POD 战术面的 borrowed discard card -> owner deck shuffle，不外推 Vampires/POD 全量、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `killer_plants_killer_kudzu / borrowed discard minion -> owner deck shuffle` 继续沿 `DECK_REORDERED(sourcePlayerId)` family 命中新的 Titan 对象级真缝。源码复核确认 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `titan_killer_plants_killer_kudzu_recycle` handler 旧实现从当前玩家 discard 多选随从后，直接把 `player.deck + selectedFromDiscard` 洗成 `DECK_REORDERED.playerId=playerId`；因此 `P0.discard` 中 `owner='1'` 的 `borrowed-sprout` 会错误进入 P0 deck。现已最小收口为：所选 discard minions 按真实 owner 分组，每个 owner 生成 owner-scoped `DECK_REORDERED`，borrowed 组补 `sourcePlayerId: playerId`。新增 [`reactionQueueTitanRemovedFromPlay.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueTitanRemovedFromPlay.test.ts>) focused gate，真实走 `TITAN_REMOVED_FROM_PLAY -> recycle` 链，首轮红灯坐实 `P0.deck=['p0-deck-a','borrowed-sprout']`，修复后锁住 `borrowed-sprout` 从 P0 discard 移除并进入 P1 deck。验证：focused `reactionQueueTitanRemovedFromPlay.test.ts --testNamePattern "回收被他人拥有的弃牌随从"` => 首轮红灯后 `1 file passed, 1 passed`；整文件 `reactionQueueTitanRemovedFromPlay.test.ts` => `1 file passed, 2 passed`。这格只闭合 Killer Kudzu recycle 的 borrowed discard minion -> owner deck shuffle，不外推 Killer Plants 全量、所有 Titan recycle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `samurai_honor_the_ancestors / borrowed discard minion -> owner deck shuffle` 继续沿“从当前玩家弃牌堆洗回牌库时仍把 borrowed card 绑定当前玩家”的 `DECK_REORDERED(sourcePlayerId)` family 命中一条新的对象级真缝。源码复核确认 [`samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 `buildHonorAncestorsEvents()` 旧实现把当前玩家 discard 前 N 张随从直接拼进 `player.deck` 并发 `DECK_REORDERED.playerId=playerId`；因此 `P0.discard` 中 `owner='1'` 的 `borrowed-ronin` 会错误进入 P0 deck。现已最小收口为：加 1 个力量指示物不变；所选 discard minions 按真实 owner 分组，每个 owner 生成 owner-scoped `DECK_REORDERED`，borrowed 组补 `sourcePlayerId: playerId`。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['p0-deck-a','borrowed-ronin']`，修复后锁住 `borrowed-ronin` 从 P0 discard 移除并进入 P1 deck。验证：focused `newFactionAbilities.test.ts --testNamePattern "洗回被他人拥有的弃牌随从"` => `1 file passed, 1 passed`；邻近 `newFactionAbilities.test.ts --testNamePattern "samurai_honor_the_ancestors"` => `1 file passed, 2 passed`。额外整文件 `newFactionAbilities.test.ts` 当前仍有 8 个 unrelated 失败，不能作为本轮 Samurai 完成证据。这格只闭合 Honor the Ancestors 的 borrowed discard minion -> owner deck shuffle，不外推 Samurai 全量、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `cowboys_gold_in_them_thar_hills / borrowed revealed remaining card -> owner deck top` 继续沿“揭示后未选中的 borrowed card 回牌库时仍留在当前玩家 deck”的 reveal/order family 命中一条新的对象级真缝，和 Athena/Portal 同族但不是复读。源码复核确认 [`cowboys.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cowboys.ts>) 的 `buildGoldDrawAndDeckEvents()` 旧实现把 `remainingCards + restOfDeck` 全部写成 `DECK_REORDERED.playerId=playerId`；因此 `P0.deck` 顶部三张中未被选中的 `owner='1'` borrowed card 会错误留在 P0 deck。现已最小收口为：所选牌仍由 `CARDS_DRAWN` 进入行动玩家手牌；未选中的 remaining cards 按真实 owner 分流，borrowed 牌先生成 owner-scoped `DECK_REORDERED(sourcePlayerId=playerId)`，再重排当前玩家剩余牌库。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['top-c','borrowed-top-a','rest-1']`，修复后锁住 `borrowed-top-a` 进入 P1 deck 顶。验证：focused `newFactionAbilities.test.ts --testNamePattern "未选中的 borrowed 揭示牌"` 首轮红灯后转绿 => `1 file passed, 1 passed`；邻近 `cowboys_gold_in_them_thar_hills` 子集 => `1 file passed, 6 passed`。这格只闭合 Gold in Them Thar Hills 的未选 borrowed revealed card -> owner deck top，不外推 Cowboys 全量、所有 reveal/order caller 或所有 `DECK_REORDERED` producer 已完成。
  - `robot_microbot_reclaimer / borrowed discard microbot -> owner deck shuffle` 继续沿“从当前玩家弃牌堆洗回牌库时仍把 borrowed card 绑定当前玩家”的 `DECK_REORDERED(sourcePlayerId)` family 命中一条新的对象级真缝。源码复核确认 [`robots.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/robots.ts>) 的 Reclaimer resolver 从当前玩家 discard 多选 Microbot 后，旧实现把 `player.deck + selected` 一次性洗成 `DECK_REORDERED.playerId=playerId`；因此 `P0.discard` 中 `owner='1'` 的 `borrowed-microbot` 会错误进入 `P0.deck`。现已最小收口为：按所选 live discard card 的真实 `card.owner` 分组，为每个 owner 生成 owner-scoped `DECK_REORDERED`，borrowed 组补 `sourcePlayerId: playerId`。新增 [`robotAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/robotAbilities.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['p0-deck-a','borrowed-microbot']`，修复后锁住该牌从 P0 discard 移除并进入 P1 deck。验证：focused `robotAbilities.test.ts --testNamePattern "选择被他人拥有的弃牌堆微型机"` 首轮红灯后转绿 => `1 file passed, 1 passed`；邻近 `robot_microbot_reclaimer` 子集 => `1 file passed, 10 passed`；整文件 `robotAbilities.test.ts` => `1 file passed, 12 passed`。这格只闭合 Reclaimer 的 single-source borrowed discard Microbot -> owner deck shuffle，不外推 Robots 全量、所有 discard-shuffle caller、或多 source 同 owner 的批量洗牌效果已完成。
  - `elder_thing_begin_the_summoning / borrowed discard minion -> owner deck top` 这轮继续沿“从当前玩家弃牌堆回顶时仍把 borrowed card 绑定当前玩家”的 `CARD_TO_DECK_TOP` producer family 命中一条新的对象级真缝，不是此前 `elder_thing_begin_the_summoning_pod` 的复读。源码复核确认 [`elder_things.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/elder_things.ts>) 普通版 prompt resolver 仍旧拼 `DECK_REORDERED.playerId=context.playerId`，因此 `P0.discard` 中 `owner='1'` 的 `borrowed-minion` 会错误进入 `P0.deck`。现已最小收口为：resolve 时从 live source discard 复核所选随从，按 `inDiscard.owner` 发 `CARD_TO_DECK_TOP.ownerId`，并带 `sourcePlayerId: context.playerId`。新增 [`elderThingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingAbilities.test.ts>) focused gate，首轮红灯坐实 `P0.deck=['borrowed-minion','p0-deck-a']`，修复后锁住 `borrowed-minion` 从 P0 discard 移除并进入 P1 deck 顶。验证：focused `elderThingAbilities.test.ts --testNamePattern "elder_thing_begin_the_summoning 选择被他人拥有的弃牌随从"` 首轮红灯后转绿 => `1 file passed, 1 passed`；邻近 `elder_thing_begin_the_summoning` 子集 => `1 file passed, 6 passed`；整文件 `elderThingAbilities.test.ts` => `1 file passed, 27 passed`。这格只闭合普通 `Begin the Summoning` 的 borrowed discard minion -> owner deck top，不外推 Elder Things 全量、POD 版以外其它 sibling、或所有 `CARD_TO_DECK_TOP/DECK_REORDERED` producer 已完成。
  - `CARDS_DISCARDED / borrowed hand card -> owner discard` 这轮继续沿“从 source hand 弃牌时仍把 owner 洗成 source player”的 shared reducer family 命中一条新的真缝，不是 `CARD_RECOVERED_FROM_DISCARD` 的复读。源码复核已确认 [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `CARDS_DISCARDED` 旧实现只按 `payload.playerId` 从该玩家 hand 取牌，再把 `discardedFromHand` 全部追加到同一玩家 discard；因此 `P0.hand` 中的 `owner='1'` borrowed card 若被 discard 效果弃掉，会错误进入 `P0.discard`。现已最小收口为：source hand 仍按 `playerId` 移除，但逐张按 `card.owner` 追加到真实 owner discard，owner 不存在时才回退 source player。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) focused gate，锁住 `borrowed-hand` 从 `P0.hand` 移除并进入 `P1.discard`，P0 discard 只接收自己拥有的 `own-hand`。验证：focused `zombieWizardAbilities.test.ts --testNamePattern "弃掉被他人拥有的手牌"` 首轮红灯后转绿 => `1 file passed, 1 passed`；`CARDS_DISCARDED|CARD_RECOVERED_FROM_DISCARD|HAND_SHUFFLED_INTO_DECK|wizard_winds_of_change|zombie_grave_digger|zombie_grave_robbing|zombie_not_enough_bullets` 子集 => `1 file passed, 15 passed`；整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 28 passed`。这格只闭合 shared `CARDS_DISCARDED` 的 borrowed hand card -> owner discard landing，不外推所有 discard producer、所有 hand-discard prompt 或整个 discard/recover family 已完成。
  - `CARD_RECOVERED_FROM_DISCARD / borrowed discard card -> owner hand` 这轮继续沿“从 source discard 回手时仍把 owner 洗成 source player”的 shared reducer family 命中一条新的真缝，不是 `HAND_SHUFFLED_INTO_DECK` 的复读。源码复核已确认 [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `CARD_RECOVERED_FROM_DISCARD` 旧实现只按 `payload.playerId` 从该玩家 discard 取牌，再把 `recovered` 全部追加到同一玩家 hand；因此 `P0.discard` 中的 `owner='1'` borrowed card 若被 Grave Digger/Grave Robbing/同类效果取回，会错误进入 `P0.hand`。现已最小收口为：source discard 仍按 `playerId` 移除，但逐张按 `card.owner` 追加到真实 owner hand，owner 不存在时才回退 source player。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) focused gate，锁住 `borrowed-discard` 从 `P0.discard` 移除并进入 `P1.hand`，P0 hand 只接收自己拥有的 `own-discard`。验证：focused `zombieWizardAbilities.test.ts --testNamePattern "取回被他人拥有的弃牌"` 首轮红灯后转绿 => `1 file passed, 1 passed`；`CARD_RECOVERED_FROM_DISCARD|zombie_grave_digger|zombie_grave_robbing|zombie_not_enough_bullets` 子集 => `1 file passed, 9 passed`；整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 27 passed`。这格只闭合 shared `CARD_RECOVERED_FROM_DISCARD` 的 borrowed discard card -> owner hand landing，不外推所有 discard-recover producer、所有 returned-to-hand trigger consumer 或整个 Zombies/Mythic/Steampunks family 已完成。
  - `HAND_SHUFFLED_INTO_DECK / borrowed hand card -> owner deck` 这轮继续沿“从 source player 手牌/牌库移入牌库时仍把 owner 洗成 source player”的 shared reducer family 命中一条新的真缝，不是 `CARDS_MILLED` 的复读。源码复核已确认 [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `HAND_SHUFFLED_INTO_DECK` 旧实现会把 `player.hand + player.deck` 按 `newDeckUids` 排序后整体写回 `playerId` 的 deck；因此 `P0.hand` 中的 `owner='1'` borrowed card 若被 `Winds of Change`/同类效果洗入牌库，会错误进入 `P0.deck`。现已最小收口为：source player 仍按 `newDeckUids` 移除被洗入牌库的手牌，但 source deck 只保留真实 owner 为 source player 的牌；owner 不同的 card 追加到其真实 owner deck，owner 不存在时才回退 source player。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) focused gate，锁住 `borrowed-hand` 从 `P0.hand` 移除并进入 `P1.deck`，P0 deck 只保留 `p0-deck-a/own-hand`。验证：focused `zombieWizardAbilities.test.ts --testNamePattern "被他人拥有的手牌洗入牌库"` 首轮红灯后转绿 => `1 file passed, 1 passed`；`HAND_SHUFFLED_INTO_DECK|wizard_winds_of_change` 子集 => `1 file passed, 5 passed`；整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 26 passed`。这格只闭合 shared `HAND_SHUFFLED_INTO_DECK` 的 borrowed hand/deck card -> owner deck landing，不外推所有 hand-shuffle producer、所有 draw-after-shuffle caller 或整个 Wizards/Miskatonic family 已完成。
  - `zombie_mall_crawl / borrowed deck card -> owner discard via CARDS_MILLED` 这轮继续沿“borrowed deck card 被移入弃牌堆时仍绑定当前玩家”的 still-open family 命中一条新的 shared reducer 真缝，不是 `zombie_lend_a_hand` discard shuffle 的复读。源码复核已确认 [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `CARDS_MILLED` 旧实现只按 `payload.playerId` 从该玩家 deck 移除并追加到同一玩家 discard；因此 [`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 的 `zombie_mall_crawl` 如果从 P0 牌库磨掉 `owner='1'` 的 `borrowed-walker`，会错误进入 P0 discard。现已最小收口为：`CARDS_MILLED` 仍只从 `playerId` 的 source deck 移除，但逐张按 `card.owner` 追加到真实 owner discard，owner 不存在时才回退 source player。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) focused gate，锁住 `borrowed-walker` 从 `P0.deck` 移除并进入 `P1.discard`，P0 discard 只接收自己拥有的 `own-walker`。验证：focused `zombieWizardAbilities.test.ts --testNamePattern "zombie_mall_crawl: 磨掉被他人拥有的牌库牌"` 首轮红灯后转绿 => `1 file passed, 1 passed`；`zombieWizardAbilities.test.ts --testNamePattern "zombie_mall_crawl"` => `1 file passed, 3 passed`；整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 25 passed`。这格只闭合 `zombie_mall_crawl` 触发的 borrowed deck card -> owner discard 与 shared `CARDS_MILLED` owner landing，不外推所有 milling caller、所有 deck-search caller 或整个 Zombies 已完成。
  - `zombie_lend_a_hand / borrowed discard card -> owner deck shuffle` 这轮继续沿“从当前玩家弃牌堆洗回牌库时仍把 borrowed card 绑定当前玩家”的 still-open family 命中一条新的对象级真缝，不是 `mythic_greeks_favor_of_poseidon` 的复读，而是同一 shared `DECK_REORDERED(sourcePlayerId)` 合同的 Zombies sibling。源码复核已确认 [`zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 的 `zombieLendAHandPromptProgram` 会从 `P0.discard` 多选任意卡，但 resolve 时把 `selectedCards + player.deck` 一次性洗成 `DECK_REORDERED.playerId=playerId`；因此 `P0.discard` 中的 `owner='1'` borrowed card 会被错误洗进 `P0.deck`。现已最小收口为：resolve 仍只信 live `selectedCards`，但按 `card.owner` 分组，为每个真实 owner 生成 owner-scoped `DECK_REORDERED`，borrowed 场景补 `sourcePlayerId: playerId`。新增 [`zombieWizardAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieWizardAbilities.test.ts>) focused gate，锁住 `borrowed-discard` 从 `P0.discard` 移除并进入 `P1.deck`，P0 deck 不混入该牌。验证：focused `zombieWizardAbilities.test.ts --testNamePattern "zombie_lend_a_hand"` => `1 file passed, 2 passed`；整文件 `zombieWizardAbilities.test.ts` => `1 file passed, 24 passed`。这格只闭合 `zombie_lend_a_hand` 的 borrowed discard card -> owner deck shuffle 分支，不外推整个 Zombies、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `mythic_greeks_favor_of_poseidon / borrowed discard card -> owner deck shuffle` 这轮继续沿“从当前玩家弃牌堆洗回牌库时仍把 borrowed card 绑定当前玩家”的 still-open family 命中一条新的对象级真缝，不是 `Favor of Athena` 的 reveal/order 复读。源码复核已确认 [`mythic_greeks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mythic_greeks.ts>) 的 `favorOfPoseidon()` 会从 `P0.discard` 生成候选，但 `poseidonPromptProgram` 结算时把所有所选牌直接拼进 `DECK_REORDERED.playerId=context.playerId`；因此 `P0.discard` 中的 `owner='1'` borrowed card 会被错误洗进 `P0.deck`。现已最小收口为：Poseidon 候选携带 `ownerId`，resolve 时从当前玩家 discard 读取 selected live card，按 `card.owner` 分组，为每个真实 owner 生成 owner-scoped `DECK_REORDERED`，borrowed 场景补 `sourcePlayerId: context.playerId`。新增 [`shayuFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/shayuFactionAbilities.test.ts>) focused gate，锁住 `borrowed-discard` 从 `P0.discard` 移除并进入 `P1.deck` 顶，P0 deck 不混入该牌。验证：focused `shayuFactionAbilities.test.ts --testNamePattern "波塞冬的恩惠"` => `1 file passed, 2 passed`；整文件 `shayuFactionAbilities.test.ts` => `1 file passed, 20 passed`。这格只闭合 `mythic_greeks_favor_of_poseidon` 的 borrowed discard card -> owner deck shuffle 分支，不外推整个 Mythic Greeks、所有 discard-shuffle caller 或所有 `DECK_REORDERED` producer 已完成。
  - `mythic_greeks_favor_of_athena / revealed borrowed deck card -> owner deck top` 这轮继续沿“揭示/排序后把 borrowed 牌放回牌库顶时仍绑定当前玩家”的 still-open family 命中一条新的对象级真红灯，不是 `mythic_greeks_favor_of_dionysus` 的 borrowed self-to-deck-top 复读。源码复核已确认 [`mythic_greeks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mythic_greeks.ts>) 的 `AthenaRevealedCard` 与 `AthenaOrderContext.remaining/ordered` 旧实现只保留 `uid/defId/type`，排序结算时 `athenaOrderPromptProgram` 又把 `CARD_TO_DECK_TOP.ownerId` 写成 `context.playerId`；因此当 P0 牌库前 5 张里包含 `owner='1'` 的 `borrowed-revealed`，选走一张行动后再排序剩余牌时，旧链会把 borrowed 牌错误留在 P0.deck。现已最小收口为：Athena reveal/order context 全程保留 `card.owner`，排序回顶时按 `allCards[index].owner` 写 `ownerId`，borrowed 场景补 `sourcePlayerId: context.playerId`；同时在 [`shayuFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/shayuFactionAbilities.test.ts>) 补 focused gate，锁住 `borrowed-revealed` 从 `P0.deck` 移除并进入 `P1.deck` 顶。验证：focused `shayuFactionAbilities.test.ts --testNamePattern "雅典娜的恩惠"` => `1 file passed, 2 passed`；整文件 `shayuFactionAbilities.test.ts` => `1 file passed, 19 passed`。这格只闭合 `mythic_greeks_favor_of_athena` 的 revealed borrowed deck card -> owner deck top 分支，不外推整个 Mythic Greeks、所有 reveal/order caller 或所有 `CARD_TO_DECK_TOP` producer 已完成。
  - `giant_ant_who_wants_to_live_forever_pod / search deck borrowed card -> owner deck top` 这轮继续沿“borrowed deck card 被放回牌库顶时仍绑定当前玩家”的 still-open family 命中一条新的对象级真红灯，不是同卡 destroy 分支对 borrowed minion owner 归属的复读。源码复核已确认 [`giant_ants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/giant_ants.ts>) 的 `giantAntWhoWantsToLiveForeverPodSearchPromptProgram` 会在 prompt 候选里列出当前玩家牌库内所有牌，但 resolver 又用 `card.owner !== context.playerId` 直接空收口；因此当 `P0.deck` 内存在 `owner='1'` 的 borrowed `borrowed-deck-card` 时，玩家能选到它，但结算后它仍留在 `P0.deck`。现已最小收口为：search resolver 按 live `card.owner` 写 `CARD_TO_DECK_TOP.ownerId`，并在 borrowed 场景补 `sourcePlayerId: context.playerId`；同时在 [`giantAntsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/giantAntsPod.test.ts>) 补 focused gate，锁住 `borrowed-deck-card` 从 `P0.deck` 移除并进入 `P1.deck` 顶。验证：focused `giantAntsPod.test.ts --testNamePattern "search 分支选择被他人拥有的牌库牌时，仍应进入其拥有者牌库顶|can still search when you have no minion to destroy|destroy 分支点中被我控制但归对手拥有的随从时，仍应进入其拥有者弃牌堆"` => `1 file passed, 3 passed`；整文件 `giantAntsPod.test.ts` => `1 file passed, 8 passed`。这格只闭合 `giant_ant_who_wants_to_live_forever_pod` 的 search-deck borrowed card -> owner deck top 分支，不外推整个 Giant Ants、所有 deck-search caller 或所有 `CARD_TO_DECK_TOP` producer 已完成。
  - `elder_thing_elder_thing / borrowed self-to-deck-bottom + failed_destroy -> owner provenance` 这轮继续沿“借来的随从自己回牌库底时仍把 owner 洗成当前玩家”的 still-open family 命中一条新的对象级真红灯，不是 `elder_thing_begin_the_summoning_pod` 的 borrowed discard topdeck 回顶复读，也不是 `elder_thing_elder_thing_pod` 的 shared `MINION_PLAYED` owner seam 回扫。源码复核已确认 [`elder_things.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/elder_things.ts>) 的 `elderThingChoicePromptProgram` 与 `elderThingDestroyFirst/SecondPromptProgram` 在 direct `deckbottom` 与 `failed_destroy` 三个回底分支里，旧实现都把 `buildValidatedCardToDeckBottomEvents(... ownerId: context.playerId ...)` 写成当前玩家；因此当 `elder_thing_elder_thing(controller='0', owner='1')` 被 P0 打到基地后，无论是直接选择“将本随从放到牌库底”，还是 destroy 链第一目标出现后其余 live 候选全部离场触发 `failed_destroy`，旧链都会把它错误沉进 `P0.deck`。现已最小收口为：三个分支统一按 `findMinionOnBases(state.core, context.cardUid)?.minion.owner ?? context.playerId` 读取 live source minion 的真实 owner，再写入 `CARD_TO_DECK_BOTTOM.ownerId`；同时在 [`elderThingsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingsPod.test.ts>) 补两条 focused gate，分别锁住 direct `deckbottom` 与“第一目标 prompt 后其余候选已离场”的 `failed_destroy` 回底分支。验证：`elderThingsPod.test.ts --testNamePattern "destroy option disabled and you can put it to deck bottom \\(FAQ\\)|failed_destroy 仍应把 borrowed 远古之物放回其拥有者牌库底"` => `1 file passed, 2 passed`。这格只闭合 `elder_thing_elder_thing` 在 borrowed self-to-deck-bottom / failed_destroy 场景下的 owner provenance，不外推整个 Elder Things、所有 self-bottom caller，或所有 `CARD_TO_DECK_BOTTOM` producer 已完成。
  - `wizard_mass_enchantment / borrowed ongoing attach -> owner provenance` 这轮继续沿“借来的 ongoing 在真实附着后仍把 owner 洗成当前玩家”的 still-open family 命中一条新的对象级真红灯，不是 `trickster_hideout_pod` 的 ongoing swap seam 复读，也不是 `elder_thing_begin_the_summoning_pod` 的 discard topdeck 回顶缝回扫。源码复核已确认 [`wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `resolveWizardExternalActionPlay()` 在 `playMode === 'ongoing-base' / 'ongoing-minion'` 两个分支里，旧实现都会把 `ONGOING_ATTACHED.ownerId` 直接写成 `context.playerId`；因此当 `wizard_mass_enchantment` 从对手牌库顶真实外部打出一张 `owner='1'` 的 borrowed `ninja_smoke_bomb / steampunk_escape_hatch` 时，旧链会把这张 ongoing 错附着成 `ownerId='0'`。现已在 [`query6Abilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/query6Abilities.test.ts>) 的两条现有 `wizard_mass_enchantment` gate 上补 owner provenance 断言：首轮红灯坐实 `su:ongoing_attached.payload.ownerId === '0'`。最小修复只收口在 `resolveWizardExternalActionPlay()`：让 `ongoing-base/minion` 两个分支都改按 `sourceCard.owner` 写 `ownerId`，不改其它 Wizards topdeck/portal/reveal 路径。验证：focused `query6Abilities.test.ts --testNamePattern "wizard_mass_enchantment: 打出附着到随从的行动时应保留目标上下文并触发 base_enchanted_glade|wizard_mass_enchantment: 打出无 onPlay 的基地 ongoing 时应保留目标上下文，并让 base_enchanted_glade 以 base-target 语义入队但不抽牌"` => `1 file passed, 2 passed`；邻近 `query6Abilities.test.ts --testNamePattern "wizard_mass_enchantment"` => `1 file passed, 5 passed`。这格只闭合 `wizard_mass_enchantment` 在 borrowed ongoing attach 场景下的 owner provenance，不外推整个 Wizards、所有 external replay caller，或全部 ongoing attach producer 已完成。
  - `elder_thing_begin_the_summoning_pod / borrowed discard minion -> owner deck top` 这轮继续沿“borrowed discard 自己回牌库顶时仍把 owner 写成当前玩家”的 still-open family 命中一条新的对象级真红灯，不是 `penguins_emperor_penguin_talent` 的 borrowed hand topdeck 复读，也不是 `trickster_hideout_pod` 的 ongoing swap seam 回扫。源码复核已确认 [`elder_things.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/elder_things.ts>) 的 `elderThingBeginTheSummoningPodPromptProgram` 旧实现会在把所选弃牌堆随从放到牌库顶时直接发 `CARD_TO_DECK_TOP.ownerId = context.playerId`；因此当 `P0` 选择自己 discard 中一张 `owner='1'` 的 borrowed `borrowed-minion` 时，会被错误放进 `P0.deck`。现已在 [`elderThingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingAbilities.test.ts>) 新增 focused gate `elder_thing_begin_the_summoning_pod 选择被他人拥有的弃牌随从时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶`，首轮红灯坐实 `P0.deck === ['borrowed-minion','p0-deck-a']`。最小修复只收口在该 POD prompt handler：改按 `inDiscard.owner` 写 `ownerId`，并显式补 `sourcePlayerId: context.playerId`，确保 borrowed source 会从当前玩家 discard 移走后，真正进入其拥有者牌库顶。验证：`elderThingAbilities.test.ts --testNamePattern "elder_thing_begin_the_summoning"` => `1 file passed, 5 passed`。这格只闭合 `elder_thing_begin_the_summoning_pod` 在 borrowed discard minion 场景下的 owner provenance，不外推整个 Elder Things、所有 discard-topdeck caller，或全部 `CARD_TO_DECK_TOP` producer 已完成。
  - `trickster_hideout_pod / borrowed ongoing swap -> owner provenance` 这轮继续沿“borrowed ongoing 被移动/换位后仍把 owner 洗成当前玩家”的 still-open family 命中一条新的对象级真红灯，不是 `mermaids_shipwreck_cove` 的 queued move owner 复读，也不是 `ghost_make_contact_pod` 的 detach seam 回扫。源码复核已确认 [`tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 的 `tricksterHideoutPodSwapPromptProgram` 旧实现会在把选中的基地持续战术换进基地时直接写 `{ ownerId: playerId }`；因此当 `P0` 从自己手牌换入一张 `owner='1'` 的 borrowed `trickster_flame_trap_pod` 时，旧状态会把基地上的新 ongoing 错写成 `ownerId='0'`。现已在 [`ongoingTalent.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ongoingTalent.test.ts>) 新增 focused gate `从手牌交换被他人拥有的持续战术时，换进基地的 ongoing 仍应保留真实 ownerId`，首轮红灯坐实 `base[0].ongoingActions[0].ownerId === '0'`。最小修复只收口在该 handler：先从所选 `hand/deck` 实例读取 `selectedCard.owner`，若实例已不在原区则直接返回空事件；随后将换进基地的 ongoing 改为 `ownerId: selectedCard.owner`，不改其它 destroy/reorder 路径。验证：`ongoingTalent.test.ts --testNamePattern "trickster_hideout_pod（藏身处 POD ongoing talent）"` => `1 file passed, 4 passed`。这格只闭合 `trickster_hideout_pod` 在 borrowed ongoing swap 场景下的 owner provenance，不外推整个 Tricksters、所有 ongoing swap/talent，或全部 ongoing move/detach family 已完成。
  - `princesses_direct_to_dvd_sequel / borrowed discard minion -> owner deck shuffle` 这轮继续沿 `topdeck/shuffle owner provenance` family 命中一条新的对象级真红灯，不是 `Woodland Helpers` 的 borrowed action 回底复读，也不是 `Sleeping Beauty` 的 destroy-self shuffle 分支回扫。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) focused gate `princesses_direct_to_dvd_sequel 选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库` 后，首轮先坐实旧 [`princesses.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/princesses.ts>) 的 `resolvePrincessesDirectToDvdSequelSelection(...)` 仍把 `player.discard` 里的所选卡直接 `random.shuffle([...player.deck, card])`，再发 `DECK_REORDERED.playerId=当前玩家`，导致 `owner='1'` 的 borrowed `borrowed-discard` 被错误洗进 `P0.deck` 并误参与当前玩家摸牌。最小修复仍限制在 `princesses.ts` 本地：改为读取 `card.owner` 作为真实 owner，用 `buildValidatedCardToDeckBottomEvents(... ownerId=card.owner, sourcePlayerId=playerId, expectedLocation:'discard')` 把 borrowed source 从当前玩家 discard 移走，再发 `DECK_REORDERED.playerId=ownerId`；若 `ownerId===playerId`，保留原“洗入自己的 deck 后摸其顶牌”语义，若 `ownerId!==playerId`，摸牌则回到 `buildStandardDrawEvents(state.core, playerId, 1, ...)` 按行动玩家自己的 deck 处理。验证：focused `newFactionAbilities.test.ts --testNamePattern "princesses_direct_to_dvd_sequel 会把弃牌堆随从洗回牌库并抽 1|princesses_direct_to_dvd_sequel 选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库"` => `1 file passed, 2 passed`；邻近 `newFactionAbilities.test.ts --testNamePattern "princesses_direct_to_dvd_sequel 会把弃牌堆随从洗回牌库并抽 1|princesses_direct_to_dvd_sequel 选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库|princesses_sleeping_beauty 被消灭时会洗回牌库而不是进弃牌堆"` => `1 file passed, 3 passed`。这格只闭合 `princesses_direct_to_dvd_sequel` 在 borrowed discard minion 场景下的 owner provenance，并给 `princesses_sleeping_beauty` 补了一个邻近 green evidence；不外推整个 Princesses、所有 shuffle caller，或全部 `DECK_REORDERED` producer 已完成。
  - `vikings_huscarl / local toDeckTop caller family / borrowed hand -> owner deck top` 这轮继续沿 `borrowed hand/discard -> owner deck top/shuffle` family 命中一条新的对象级真红灯，不是 `Time Raider / Repeater Perfect / Emperor Penguin / Cthulhu` 的复读。新增 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) focused gate `vikings_huscarl 选择被他人拥有的手牌时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶` 后，首轮先坐实旧 [`vikings.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vikings.ts>) 的本地 helper `toDeckTop(playerId, ...)` 仍把 `CARD_TO_DECK_TOP.payload.ownerId` 写死成当前玩家，导致 `owner='1'` 的 borrowed `borrowed-1` 从 P0 手牌发动 `vikings_huscarl` 后错误进入 `P0.deck`。最小修复没有动 shared reducer，而是只在 `vikings.ts` 本地收口这组 hand-topdeck caller：1) `buildHandCardOptions(...)` 透传 `ownerId`，让 prompt value 不再丢失真实 owner；2) `toDeckTop(...)` 扩成可显式传入 `ownerId`，且只在 `ownerId !== playerId` 时补 `sourcePlayerId: playerId`；3) `vikings_huscarl / vikings_raider / vikings_berserk / base_longhouse` 这四条 caller 都改按所选或所打出卡牌的真实 owner 回顶。验证：focused `newFactionAbilities.test.ts --testNamePattern "vikings_huscarl 天赋会把手牌放到牌库顶并给自身 \+2 力量|vikings_huscarl 选择被他人拥有的手牌时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶"` => `1 file passed, 2 passed`；邻近 `newFactionAbilities.test.ts --testNamePattern "vikings_huscarl 天赋会把手牌放到牌库顶并给自身 \+2 力量|vikings_huscarl 选择被他人拥有的手牌时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶|vikings_raider 会把揭示的低力量随从作为额外随从直接打出|vikings_berserk_pod 会把手牌压到牌库顶并给你的随从 \+4 力量"` => `1 file passed, 3 passed`；`newBaseAbilities.test.ts --testNamePattern "base_longhouse 改为主动基地能力：使用后会把手牌置于牌库顶并给此基地己方随从 \+2 力量|base_longhouse_pod reuses the active base ability to topdeck and buff"` => `1 file passed, 2 passed`。这格只闭合 `vikings_huscarl` 在 borrowed hand 场景下的 owner provenance，并给同文件 `vikings_raider / vikings_berserk / base_longhouse` 的 hand-topdeck sibling 补邻近 green evidence；不外推整个 Vikings、所有 `CARD_TO_DECK_TOP` caller，或其它 `shuffleCardIntoDeck / DECK_REORDERED` family 已完成。
  - `cthulhu_recruit_by_force / cthulhu_it_begins_again / borrowed discard -> owner deck top/shuffle` 这轮已从候选坐实成两条 sibling 真红灯，并顺带补了一条更窄的 shared `DECK_REORDERED(sourcePlayerId)` 合同。旧 [`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 先把 `Recruit by Force` 的所选弃牌随从直接拼进 `DECK_REORDERED.playerId=当前玩家` 的牌库顶，导致 `owner='1'` 的 borrowed minion 错进 `P0.deck`；`It Begins Again` 也把 `random.shuffle([...player.deck, ...actionsFromDiscard])` 直接写回当前玩家 `DECK_REORDERED`，导致 borrowed action 错进 `P0.deck`。最小修复分两层：1) `Recruit by Force` 改为逐张 `CARD_TO_DECK_TOP(ownerId=card.owner, sourcePlayerId=currentPlayer)`，保持多选前序卡仍在更顶端；2) `types.ts/reduce.ts` 给 `DeckReorderedEvent` 增量补 `sourcePlayerId?: PlayerId`，只在 `sourcePlayerId !== playerId` 时允许从 source player 的 hand/deck/discard 取被引用牌 UID、并把最终 `deckUids` 写回真实 owner 的 deck，再让 `It Begins Again` 按 `card.owner` 分组生成 owner-scoped `DECK_REORDERED`。验证：`cthulhuExpansionAbilities.test.ts` 新增两条 focused gates `选择被他人拥有的弃牌随从时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶` 与 `选择被他人拥有的弃牌行动时，仍应洗回其拥有者牌库而不是当前玩家牌库` 均已从红灯转绿，整文件复跑 `34 passed`，`git diff --check` 通过（仅 LF/CRLF warning）。这格只闭合 `cthulhu_recruit_by_force / cthulhu_it_begins_again` 的 borrowed discard -> owner deck top/shuffle，以及一条更窄的 shared `DECK_REORDERED(sourcePlayerId)` source/owner 合同；不外推所有 shuffle caller、所有 `DECK_REORDERED` producer，或整个 `Cthulhu` family 已完成。
  - `frankenstein_angry_mob / borrowed hand -> owner deck bottom / runtime prompt double-apply` 这轮新命中的不是 `Time Walk` 的复读，而是一条同时跨 `CARD_TO_DECK_BOTTOM` 与 shared runtime prompt 的真红灯。旧 [`frankenstein.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/frankenstein.ts>) 在 `frankensteinAngryMobChooseCardPromptProgram` 里把所选手牌默认按当前玩家处理，导致 `owner='1'` 的 borrowed `borrowed-hand` 首轮先被错误塞进 `P0.deck`；把 `sourceCard.owner + sourcePlayerId` 接进 [`abilityHelpers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/abilityHelpers.ts>) 后，又继续暴露出两层 shared seam：1) [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `CARD_TO_DECK_BOTTOM` 在 `sourcePlayerId !== ownerId` 时只追加 owner deck、不先去重 owner 三区，使同 uid 事件一旦被重复吃到就会在 owner deck 重复落底；2) 更底层的 [`systems.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/systems.ts>) 对 ability runtime prompt 仍沿用“拿 `result.state` 直接前推、再把 `result.events` 丢进后续 reduce”的路径，因此像 Angry Mob 这种 `onResolve` 同时返回 `events + nextProgram`，且 `nextProgram` 依赖 `withSimulatedMatchState(state, events)` 的链，会把同一批 `CARD_TO_DECK_BOTTOM + POWER_COUNTER_ADDED` 双吃成 `P1.deck=['p1-deck-a','borrowed-hand','borrowed-hand']` 与 `monster1.powerCounters===2`。最小修复是两步：`reduce.ts` 让 `CARD_TO_DECK_BOTTOM` 在 `sourcePlayerId != ownerId` 时先从 owner hand/discard/deck 去重，再统一 append 一次；`systems.ts` 对 ability runtime prompt 且 `emittedEvents.length>0` 的分支，把 `result.state.core` 退回 `coreBeforeHandler`，只保留交互/sys 侧的模拟态，避免同一批 domain events 被重复 reduce。验证：focused `newFactionAbilities.test.ts --testNamePattern "愤怒的民众：若所选手牌已离开手牌，不应凭旧交互再塞回牌库|愤怒的民众：选择被他人拥有的手牌时，仍应进入其拥有者牌库底而不是当前玩家牌库底"` => `1 file passed, 2 passed`；邻近 `elderThingsPod.test.ts --testNamePattern "decliner does not force the caster to play from discard"` => `1 file passed, 1 passed`。这格只闭合 `frankenstein_angry_mob` 的 borrowed hand -> owner deck bottom 分支，以及“ability runtime prompt 返回 events + nextProgram 时不得双吃 domain events”这条由 Angry Mob 命中的 shared seam；不外推所有 prompt runtime、所有 `withSimulatedMatchState(...)` caller 或全部 `CARD_TO_DECK_BOTTOM` caller 已全量收口。
  - `withSimulatedMatchState(...) + events + nextProgram` 邻近 caller 这轮没有继续空转：已新增三条 dedicated green gate，证明上面这条 shared runtime seam 不只蒙中 `Angry Mob` 本体。1) [`frankensteinFaq.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/frankensteinFaq.test.ts>) 新增 `Blitzed: removing one counter should not double-apply before the follow-up remove prompt`，锁定第一次移除后 `ally-1.powerCounters===1`、交互仍停在 `frankenstein_blitzed_remove`，不会因 runtime 双吃把 1 次移除变成 2 次；2) [`elderThingsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingsPod.test.ts>) 新增 `playing one discard minion and continuing should not double-play it before the next may-play decision`，锁定 `d1` 只会被真实打到基地一次、弃牌堆只剩 `d2/a1`，并继续回到下一轮 `elder_thing_spreading_horror_pod_may_play`；3) [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 `尸体商店：第一次分配指示物后不应双加，并应继续停留在下一轮分配 prompt`，锁定第一次分配后 `monster.powerCounters===1` 且交互仍停在 `frankenstein_body_shop_distribute`。验证：focused `frankensteinFaq.test.ts --testNamePattern "Blitzed: may remove 0 counters and still destroy a power 0 minion|Blitzed: removing one counter should not double-apply before the follow-up remove prompt"` => `1 file passed, 2 passed`；focused `elderThingsPod.test.ts --testNamePattern "decliner does not force the caster to play from discard|playing one discard minion and continuing should not double-play it before the next may-play decision"` => `1 file passed, 2 passed`；focused `newFactionAbilities.test.ts --testNamePattern "尸体商店\\+雄蜂：选择防止消灭时，应先结算雄蜂且不进入指示物分配|尸体商店\\+雄蜂：选择不防止消灭时，应在确认消灭后再进入指示物分配|尸体商店：第一次分配指示物后不应双加，并应继续停留在下一轮分配 prompt"` => `1 file passed, 3 passed`。这格只把 `Blitzed`、`Body Shop` 与 `Spreading Horror POD` 降成邻近 green evidence，不外推整个 `frankenstein.ts` / `elder_things.ts` 或所有 `withSimulatedMatchState(...) + events + nextProgram` caller 已全量收口。
  - `withSimulatedMatchState(...) + events + nextProgram` 的显式 caller inventory 这轮继续补齐到 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>)，说明上轮还漏了 2 个同族 continuation，并非 runtime 又回红。静态搜索当前 abilities 下命中 6 个 callsite，分布在 `frankenstein.ts(3)`、`elder_things.ts(1)`、`steampunks.ts(2)`；其中 Steampunks 的两个 callsite 现已各补 dedicated green gate：1) [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增 `steampunk_mechanic_target: 恢复一张 ongoing 后不应双重回手，并应继续停留在选基地 prompt`，锁定 `dis-1` 只会从 discard 真实回到 hand 一次，discard 清空，且交互列表中出现 `steampunk_mechanic_target`；2) 同文件新增 `steampunk_change_of_venue: 取回 ongoing 后不应双重离场/回手，并应继续停留在重打目标 prompt`，锁定 `ongoing-1` 只会从 base 离场一次并回到 hand 一次，base 上 ongoing 清空，且交互列表中出现 `steampunk_change_of_venue_choose_base`。验证：focused `expansionOngoing.test.ts --testNamePattern "steampunk_mechanic_target: 恢复一张 ongoing 后不应双重回手，并应继续停留在选基地 prompt|steampunk_mechanic_target: 若待附着的 ongoing 已不在手牌则不再附着到基地|steampunk_change_of_venue: 取回 ongoing 后不应双重离场/回手，并应继续停留在重打目标 prompt|steampunk_change_of_venue_choose_base: 若待重打的 ongoing 已不在手牌则不再附着"` => `1 file passed, 4 passed`。这格只把 `steampunk_mechanic` 与 `steampunk_change_of_venue` 降成同族 green evidence，并据此说明“当前显式 `withSimulatedMatchState` caller inventory 都已有 dedicated gate”；不外推其它未用该 helper 的 continuation 模式已全量收口。
  - `processReturnToHandTriggers / MINION_RETURNED / attached source leaves play` 这轮补的是 dedicated green gate，不是新的 runtime 红灯。当前 [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 在 `MINION_RETURNED` 分支已经会按宿主 LKI 上的 `attachedActions` 定向 `collectTriggers(..., 'onCardReturnedToHand', ...)`；但之前只有 `CARD_TRANSFERRED 让宿主从场上回手时，已随宿主离场的附着 source 也应按显式 source provenance 入队` 这条邻近 gate，缺少对 `MINION_RETURNED` sibling 的明确锁。现已在 [`reactionQueueOnCardReturnedToHand.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOnCardReturnedToHand.test.ts>) 新增 `MINION_RETURNED 让宿主从场上回手时，已随宿主离场的附着 source 也应按显式 source provenance 入队`，确认 queued trigger 会继续保留 `sourceCardUid='attached-source'`、`sourceControllerId='0'`、`ownerPlayerId='0'`、`eventPlayerId='1'` 与 `triggerMinionUid='return-host'`。验证：focused `reactionQueueOnCardReturnedToHand.test.ts --testNamePattern "MINION_RETURNED 让宿主从场上回手时，已随宿主离场的附着 source 也应按显式 source provenance 入队|CARD_TRANSFERRED 让宿主从场上回手时，已随宿主离场的附着 source 也应按显式 source provenance 入队"` => `1 file passed, 2 passed`。这格只证明 `MINION_RETURNED` sibling 现在也有 dedicated gate，不外推整个 `onCardReturnedToHand` family、全部 attached-action 回手来源或所有 return-to-hand consumer 已全量收口。
  - `externalActionPlay / steampunks replay final resolve` 这轮打到一条真实 caller 红灯并顺手补了 sibling final-resolve gates。先补的新 gate [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) `steampunk_mechanic_target: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文` 首轮不是语义断言失败，而是直接炸出 `ReferenceError: appendResolvedActionAbility is not defined`，堆栈定位在 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) `steampunk_mechanic_target` 的最终落地分支。最小修复只补了 `appendResolvedActionAbility` 导入；修复后 focused `expansionOngoing.test.ts --testNamePattern "steampunk_mechanic_target: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文|steampunk_mechanic_target: 恢复一张 ongoing 后不应双重回手，并应继续停留在选基地 prompt|steampunk_mechanic_target: 若待附着的 ongoing 已不在手牌则不再附着到基地"` => `1 file passed, 3 passed`，新增 gate 明确锁住 `ACTION_PLAYED.payload.targetBaseIndex=0,targetType='base'` 与 `ONGOING_ATTACHED.targetBaseIndex=0`。同轮又补 `steampunk_change_of_venue_choose_base: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文`，验证 `expansionOngoing.test.ts --testNamePattern "steampunk_change_of_venue_choose_base: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文|steampunk_change_of_venue: 取回 ongoing 后不应双重离场/回手，并应继续停留在重打目标 prompt|steampunk_change_of_venue_choose_base: 若待重打的 ongoing 已不在手牌则不再附着"` => `1 file passed, 3 passed`。这格只闭合 `steampunk_mechanic` 的缺 import 真缝，并给 `steampunk_change_of_venue` 补 final-resolve sibling gate；不外推整个 `externalActionPlay`、所有 Steampunks replay 或全部 ongoing replay caller 已完成。
  - `externalActionPlay / steampunk_change_of_venue / attached-on-minion ongoing replay gap` 这轮继续沿 Steampunks caller 自身深挖，又命中一条和 `mechanic` 缺 import 不同的真红灯。新增 [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) focused gate `steampunk_change_of_venue: 应允许取回附着在随从上的 ongoing，并继续进入重打随从目标 prompt` 后，首轮坐实旧 [`steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 的 `buildChangeOfVenueOngoingOptions()` 与 `findOngoingBaseIndexByUid()` 都只扫 `base.ongoingActions`，完全漏掉 `minion.attachedActions`，导致像 `ninja_smoke_bomb` 这种附着在随从上的 ongoing 既不会出现在候选里，也无法在 handler 内找回所属基地。最小修复只把这两个 helper 扩成同时扫描 `base.ongoingActions` 与 `minion.attachedActions`，不改 replay helper、不改 detach/recover 事件链；随后新增的 `steampunk_change_of_venue_choose_minion: 重打无 onPlay 的随从附着 ongoing 时仍应保留 ACTION_PLAYED 目标随从上下文` 进一步锁住最终落地会继续保留 `ACTION_PLAYED.payload.targetBaseIndex=0,targetType='minion',targetMinionUid='target-1'`，并产出 `ONGOING_ATTACHED.targetType='minion'` 与同一目标 UID。验证：focused `expansionOngoing.test.ts --testNamePattern "steampunk_change_of_venue: 应允许取回附着在随从上的 ongoing，并继续进入重打随从目标 prompt|steampunk_change_of_venue_choose_minion: 重打无 onPlay 的随从附着 ongoing 时仍应保留 ACTION_PLAYED 目标随从上下文|steampunk_change_of_venue: 取回 ongoing 后不应双重离场/回手，并应继续停留在重打目标 prompt|steampunk_change_of_venue_choose_base: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文|steampunk_change_of_venue_choose_base: 若待重打的 ongoing 已不在手牌则不再附着"` => `1 file passed, 5 passed`。这格只闭合 `Change of Venue` 对随从附着 ongoing 的候选/定位与 minion-target final-resolve，不外推整个 `Steampunks`、所有 replay caller 或整个 `externalActionPlay` family 已完成。
  - `wizard_mass_enchantment / base-target ongoing sibling` 这轮已明确判定为测试断言漂移，不是新的 shared seam 红灯。接手前新增的 `query6Abilities.test.ts` sibling 用例把 `base_enchanted_glade` 误写成“基地 target 的 ongoing 也应抽牌”；但 [`baseAbilities_expansion.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities_expansion.ts>) 现行实现只在 `actionTargetType==='minion'` 时 `buildStandardDrawEvents(...)`。因此正确合同是：`wizard_mass_enchantment` 把 `steampunk_escape_hatch` 打到基地后，`ACTION_PLAYED.payload.targetBaseIndex=0,targetType='base'` 与 `ONGOING_ATTACHED.targetBaseIndex=0` 仍要保留，`base_enchanted_glade` 也仍会以 `actionTargetType='base'` 入队/消费，但不会产出 `CARDS_DRAWN`。现已把 focused gate 改成断言 `TRIGGER_QUEUED -> TRIGGER_CONSUMED` 与“不抽牌”，复跑 `query6Abilities.test.ts --testNamePattern "wizard_mass_enchantment: 打出无 onPlay 的基地 ongoing 时应保留目标上下文，并让 base_enchanted_glade 以 base-target 语义入队但不抽牌|wizard_mass_enchantment: 打出附着到随从的行动时应保留目标上下文并触发 base_enchanted_glade"` => `1 file passed, 2 passed`。这格只证明新增 sibling 用例此前把规则写错了，不外推整个 `externalActionPlay` 或 `Wizards` family 已完成；下一步继续留在 `externalActionPlay / processAffectTriggers` 主线找真正 still-open caller。
  - `time_travelers_time_walk / borrowed action self-to-deck-bottom / owner provenance` 这轮继续沿 `ACTION_PLAYED` consumer sibling 收到一条新的对象级真红灯，不是 `Favor of Dionysus` 的 deck-top 复读，也不是 `mermaids_shipwreck_cove` 的 ongoing move seam。源码复核已确认 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `timeTravelersTimeWalk()` 旧实现先把 `buildValidatedCardToDeckBottomEvents(... ownerId: ctx.playerId ...)` 写死成当前玩家；因此当 `P0` 打出一张 `owner='1'` 的 borrowed `time_travelers_time_walk` 时，首轮红灯先坐实 `walk-a` 被错误沉进 `P0.deck`。把 `ownerId` 改成真实 owner 后，又继续暴露出更细的 helper 假设：`buildValidatedCardToDeckBottomEvents(... expectedLocation:'any')` 默认只在 `ownerId` 自己的手牌/牌库/弃牌里查 source card，于是 borrowed `Time Walk` 会连 `CARD_TO_DECK_BOTTOM` 事件都不再生成，表现成 `walk-a` 既不在 `P0.deck` 也不在 `P1.deck`。最终最小修复是让 `yuanhou.ts` 本地新增 `findCardOwnerAcrossPlayerZones(...)`，先按 `cardUid/defId` 从全体玩家 `hand/discard/deck` 读取 source action 的真实 `card.owner`，再让 `timeTravelersTimeWalk()` 不再走默认假设 owner-zone 的 helper，而是直接发一条 `CARD_TO_DECK_BOTTOM(ownerId=真实 owner)`。验证：focused `yuanhouFactionAbilities.test.ts --testNamePattern "被他人拥有的时间漫步结算后，仍应进入其拥有者牌库底而不是当前玩家牌库底"` => `1 file passed, 1 passed`；邻近 `yuanhouFactionAbilities.test.ts --testNamePattern "时间旅行者：时间漫步授予本回合额外随从和行动、抽两张，并将本牌放牌库底|被他人拥有的时间漫步结算后，仍应进入其拥有者牌库底而不是当前玩家牌库底"` => `1 file passed, 2 passed`。这格只闭合 `time_travelers_time_walk` 在 borrowed self-to-deck-bottom 场景下的 owner provenance，不外推整个 `Time Travelers`、所有 self-to-deck-bottom action，或 shared `buildValidatedCardToDeckBottomEvents` 已全量适配 borrowed source。
  - `penguins_emperor_penguin_talent / borrowed hand minion -> owner deck top / sourcePlayerId symmetry` 这轮又命中一条新的 owner/source provenance 真红灯，不是 `Time Walk` / `Favor of Dionysus` 的复读。新增 [`smashup.smoke.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/smashup.smoke.test.ts>) focused gate `企鹅帝皇天赋选择被他人拥有的 borrowed 手牌随从时，仍应洗回其拥有者牌库` 后，首轮先坐实旧 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `titan_penguins_emperor_penguin_talent` 把 `CARD_TO_DECK_TOP.payload.ownerId` 与 `DECK_REORDERED.payload.playerId` 都写死成当前玩家；把 producer 改成真实 `card.owner` 后，又继续要求 shared [`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `CARD_TO_DECK_TOP` 像 `CARD_TO_DECK_BOTTOM` 一样消费 `sourcePlayerId`，先从 `sourcePlayerId ?? ownerId` 的手牌/牌库/弃牌移除，再把牌真正放回 `ownerId` 的牌库顶。验证：focused `smashup.smoke.test.ts --testNamePattern "企鹅帝皇天赋选择被他人拥有的 borrowed 手牌随从时，仍应洗回其拥有者牌库"` => `1 file passed, 1 passed`；邻近 `smashup.smoke.test.ts --testNamePattern "企鹅帝皇天赋会把手中的低战力随从洗回牌库，并为泰坦增加 1 枚力量指示物|企鹅帝皇天赋选择被他人拥有的 borrowed 手牌随从时，仍应洗回其拥有者牌库"` => `1 file passed, 2 passed`。这格只闭合 `penguins_emperor_penguin_talent` 的 borrowed hand/discard minion 回顶到拥有者牌库，以及 shared `CARD_TO_DECK_TOP` 对 `sourcePlayerId` 的对称支持；不外推所有 `CARD_TO_DECK_TOP` caller、所有 `DECK_REORDERED` producer，或整个 Penguins family 已完成。
  - `time_travelers_time_raider / time_travelers_repeater_perfect / borrowed discard -> owner deck bottom/top` 这轮继续沿同一个 owner/source provenance family 又命中两条 sibling 真红灯，不是 `Time Walk` / `Emperor Penguin` 的复读。新增 [`yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts>) focused gates `时间掠夺者选择被他人拥有的弃牌时，仍应进入其拥有者牌库底而不是当前玩家牌库底` 与 `往复时间者选择被他人拥有的弃牌行动时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶` 后，首轮分别坐实旧 [`yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 仍把 `buildValidatedCardToDeckBottomEvents(... ownerId: ctx.playerId ...)` 与 `cardToDeckTop(card, playerId, ...)` 写成当前玩家。最小修复是两步：1) `Time Raider` 自动分支与 handler 都改按 `card.owner` 写 `ownerId`，并显式补 `sourcePlayerId: playerId`；2) `cardToDeckTop()` 扩成支持 `sourcePlayerId`，让 `Repeater Perfect` 自动分支与 handler 都按 `card.owner` 回顶、按 `playerId` 标记来源。验证：focused `yuanhouFactionAbilities.test.ts --testNamePattern "时间掠夺者选择被他人拥有的弃牌时，仍应进入其拥有者牌库底而不是当前玩家牌库底|往复时间者选择被他人拥有的弃牌行动时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶"` => `1 file passed, 2 passed`；邻近 `yuanhouFactionAbilities.test.ts --testNamePattern "时间掠夺者天赋允许选择弃牌堆任意一张放到牌库底|时间掠夺者弃牌堆只有一张牌时应自动放到牌库底且不弹 prompt|时间掠夺者选择被他人拥有的弃牌时，仍应进入其拥有者牌库底而不是当前玩家牌库底|往复时间者进场允许选择弃牌堆行动放到牌库顶|往复时间者弃牌堆只有一张行动时应自动放到牌库顶且不弹 prompt|往复时间者选择被他人拥有的弃牌行动时，仍应进入其拥有者牌库顶而不是当前玩家牌库顶"` => `1 file passed, 6 passed`。这格只闭合 `Time Raider / Repeater Perfect` 在 borrowed discard 场景下的 owner provenance，不外推全部 `deck top/bottom` caller、全部 discard consumer，或整个 Time Travelers family 已完成。
  - `mermaids_shipwreck_cove / borrowed ongoing move / owner provenance` 这轮继续沿“owner 被写死成 controller/current player”的 still-open family 收到一条新的对象级真红灯，不是 `shipwreck_cove` 旧 response owner 测试或 `ghost_make_contact_pod` detach seam 的回扫。源码复核已确认 [`mermaids.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts>) 的 `mermaidsShipwreckCoveAfterScoring()` 旧实现把 move prompt continuation 的 `ownerId` 直接写成 `ctx.sourceControllerId`；因此当 queued afterScoring 触发移动一张 `owner='1'`、但当前由 `sourceControllerId='0'` 结算的 borrowed `mermaids_shipwreck_cove` 时，旧实现会在 `buildMoveOngoingEvents(...)` 中把这张牌的 detach/reattach owner 洗成 `0`。现已在 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts>) 新增 focused gate `queued afterScoring trigger 移动被他人拥有的 mermaids_shipwreck_cove 时，仍应保留其真实 owner`，首轮红灯坐实移动后 `base[1].ongoingActions['ship-1'].ownerId === '0'`。最小修复是让 `mermaids.ts` 本地新增 `findOngoingOwnerOnBases(...)`，在 `mermaidsShipwreckCoveAfterScoring()` 与共用同一路径的 `mermaidsBecalmedShoresTalent()` 创建 continuation 前，都先按 `cardUid/defId` 从场上 ongoingActions 读取真实 `ownerId`。验证：focused `reactionQueueSourceRuntimeContext.test.ts --testNamePattern "移动被他人拥有的 mermaids_shipwreck_cove 时，仍应保留其真实 owner"` => `1 file passed, 1 passed`；整文件 `reactionQueueSourceRuntimeContext.test.ts` => `1 file passed, 26 passed`；邻近 `newFactionAbilities.test.ts --testNamePattern "mermaids_becalmed_shores 天赋会把这张持续行动移到另一个基地|mermaids_shipwreck_cove 在计分后可把这张持续行动移到另一个基地"` => `1 file passed, 2 passed`。这格只闭合 `mermaids_shipwreck_cove` 的 borrowed ongoing move owner provenance，并顺带保住同路径 `mermaids_becalmed_shores` talent 入口，不外推整个 `Mermaids`、所有 ongoing move effect，或所有 queued afterScoring trigger 已完成。
  - `ghost_make_contact_pod / borrowed ongoing self-detach / owner provenance` 这轮继续沿“owner 被写死成 `ctx.playerId`”的 still-open family 收到一条新的对象级真红灯，不是 `Cream Puff Man / Secret Agent / Favor of Dionysus` 那组 `ACTION_PLAYED` consumer 的回扫。源码复核已确认 [`ghosts.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ghosts.ts>) 的 `ghostMakeContactPod()` 在“手牌仍有其他卡则自毁”分支里，把 `ONGOING_DETACHED.payload.ownerId` 直接写成 `ctx.playerId`；因此当 `P0` 打出一张 `owner='1'` 的 borrowed `ghost_make_contact_pod` 且因手里还有别的牌而立即自毁时，旧实现会把这张行动错误送进 `P0.discard`。现已在 [`ghostsAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ghostsAbilities.test.ts>) 新增 focused gate `被他人拥有时自毁仍应进入其拥有者弃牌堆，而不是当前玩家弃牌堆`，首轮红灯坐实 `P0.discard === ['p0-discard-a','a1']`。最小修复是让 `ghosts.ts` 本地新增 `findCardOwnerAcrossPlayerZones(...)`，在创建 detach 事件前按 `cardUid/defId` 扫描全体玩家 `hand/discard/deck` 读取 source action 的真实 `card.owner`，再把该 `ownerId` 写入 `ONGOING_DETACHED`。验证：focused `ghostsAbilities.test.ts --testNamePattern "被他人拥有时自毁仍应进入其拥有者弃牌堆"` => `1 file passed, 1 passed`；整文件 `ghostsAbilities.test.ts` => `1 file passed, 9 passed`；这格只闭合 `ghost_make_contact_pod` 在 borrowed ongoing-action 自毁场景下的 detach-owner provenance，不外推整个 `Ghosts`、所有 `ONGOING_DETACHED` producer，或全部 borrowed-action family 已完成。
  - `ghosts_creampuff_man / borrowed discard action self-to-deck-bottom / owner provenance` 这轮继续沿“borrowed action 自己回底时仍把 owner 写成当前玩家”的 still-open family 收到一条新的对象级真红灯，不是前面已闭合的 Cream Puff Man `base-target/minion-target` 目标上下文分支复读。源码复核已确认 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `resolveCreampuffActionPlay(...)` 在额外打出弃牌行动并结算完后，仍把 `CARD_TO_DECK_BOTTOM.payload.ownerId` 直接写成 `playerId`；因此当 `P0` 从自己的 discard 额外打出一张 `owner='1'` 的 borrowed `cyborg_apes_going_bananas` 时，旧实现会把该行动错误沉进 `P0.deck`。现已在 [`reactionQueueActionPlayedExternalDiscard.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueActionPlayedExternalDiscard.test.ts>) 新增 focused gate `Cream Puff Man 从弃牌堆额外打出被他人拥有的标准行动后，仍应沉回其拥有者牌库底`，首轮红灯坐实 `CARD_TO_DECK_BOTTOM.payload.ownerId === '0'`。最小修复只收口在 `resolveCreampuffActionPlay(...)`：改按 `actionCard.owner` 写 `ownerId`，并仅在 `ownerId !== playerId` 时补 `sourcePlayerId: playerId`。验证：focused `reactionQueueActionPlayedExternalDiscard.test.ts --testNamePattern "Cream Puff Man 从弃牌堆额外打出被他人拥有的标准行动后，仍应沉回其拥有者牌库底"` => `1 file passed, 1 passed`；整文件 `reactionQueueActionPlayedExternalDiscard.test.ts` => `1 file passed, 3 passed`；`git diff --check -- src/games/smashup/abilities/titans.ts src/games/smashup/__tests__/reactionQueueActionPlayedExternalDiscard.test.ts` 通过（仅 LF/CRLF warning）。这格只闭合 `ghosts_creampuff_man` 在 borrowed discard standard action 场景下的 self-to-deck-bottom owner provenance，不外推整个 `Ghosts` Titans、所有 external discard replay，或全部 borrowed-action family 已完成。
  - `itty_critters_rainboroc / borrowed discard minion -> owner deck shuffle` 这轮继续沿“borrowed discard 自己洗回牌库时仍把 `DECK_REORDERED.playerId` 写成当前玩家”的 still-open family 收到一条新的对象级真红灯，不是前面已闭合的 `Direct to DVD Sequel / Cthulhu / Cream Puff Man` 复读。源码复核已确认 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 的 `titan_itty_critters_rainboroc_choose_discard` 旧实现直接发 `DECK_REORDERED.playerId=playerId` 且 `deckUids=random.shuffle([...player.deck, card])`；因此当 `P0` 选择自己 discard 中一张 `owner='1'` 的 borrowed `rain-borrowed-minion` 时，旧实现会把它错误洗进 `P0.deck`。现已在 [`smashup.smoke.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/smashup.smoke.test.ts>) 新增 focused gate `彩虹鸟天赋选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库`，首轮红灯坐实 `DECK_REORDERED.payload.playerId === '0'`。最小修复只收口在该 handler：改按 `card.owner` 写 `playerId`，牌库快照改基于真实 owner deck，且仅在 `ownerId !== playerId` 时补 `sourcePlayerId: playerId`。验证：focused `smashup.smoke.test.ts --testNamePattern "彩虹鸟天赋选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库"` => `1 file passed, 1 passed`；邻近 `smashup.smoke.test.ts --testNamePattern "彩虹鸟天赋会把低战力随从从弃牌堆洗回牌库，并可继续移动到其他基地|彩虹鸟天赋选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库"` => `1 file passed, 2 passed`。这格只闭合 `itty_critters_rainboroc` 在 borrowed discard minion 场景下的 owner-scoped shuffle provenance，不外推整个 `Itty Critters` Titans、所有 `DECK_REORDERED` producer，或全部 borrowed-discard family 已完成。
  - `mythic_greeks_favor_of_dionysus / borrowed action self-to-deck-top / owner provenance` 这轮继续沿 `ACTION_PLAYED` consumer family 收到一条新的对象级真红灯，不是 `Jason/Odysseus` 那组 `onActionPlayed` queue runtime 的回扫，也不是整个 `mythic_greeks` 派系重审。源码复核已确认 [`mythic_greeks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mythic_greeks.ts>) 的 `dionysusTopPromptProgram` 旧实现把“是否将此卡放到牌库顶”的 `CARD_TO_DECK_TOP.payload.ownerId` 直接写成 `context.playerId`；因此当 `P0` 真实打出一张 `owner='1'` 的 borrowed `Favor of Dionysus` 并选择回顶时，旧实现会把 `borrowed-dionysus` 错送进 `P0.deck`，而不是其真实拥有者 `P1.deck`。现已在 [`shayuFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/shayuFactionAbilities.test.ts>) 新增 focused gate `神话希腊：被他人拥有的狄俄尼索斯恩惠选择回顶时，仍应进入其拥有者牌库而不是当前玩家牌库`，首轮红灯坐实 `P0.deck === ['borrowed-dionysus','p0-deck-1']`。最小修复是让 `favorOfDionysus()` 在创建 prompt 时先按 `cardUid/defId` 扫描全体玩家 `hand/discard/deck` 取 source card 的真实 `owner`，并把该 `ownerId` 透传给 `DionysusTopContext`；`dionysusTopPromptProgram` resolve 时再按 `context.ownerId` 写 `CARD_TO_DECK_TOP`。验证：exact focused `... shayuFactionAbilities.test.ts --testNamePattern "被他人拥有的狄俄尼索斯恩惠选择回顶时"` => `1 file passed, 1 passed`；整文件 `shayuFactionAbilities.test.ts` => `1 file passed, 18 passed`；`git diff --check -- src/games/smashup/abilities/mythic_greeks.ts src/games/smashup/__tests__/shayuFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。这格只闭合 `mythic_greeks_favor_of_dionysus` 在 borrowed action 场景下的 self-to-deck-top owner provenance，不外推整个 `mythic_greeks`、所有 self-top/bottom action，或全部 `ACTION_PLAYED` consumer 已完成。
  - `princesses_woodland_helpers / borrowed ACTION_PLAYED owner provenance / sourceController consumer` 这轮新命中的是一条被 `shared ACTION_PLAYED discard-owner provenance` 修复新暴露出来的 consumer 真缝，不是回头复读 2026-05-22 那条“别人打行动时不应误触发”的 eventPlayer 负向 gate。源码复核已确认 [`princesses.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/princesses.ts:208>) 的 `princessesWoodlandHelpers()` 旧实现只按 `helperControllerId` 去自己的 discard 里找刚打出的行动，并把 prompt context 的 `ownerId` 也硬写成 `helperControllerId`。在当前树里，`ACTION_PLAYED` 已经改成让 borrowed standard action 进入其真实拥有者 discard，因此当 P0 打出一张 `owner='1'` 的 borrowed action 时，Woodland Helpers 会直接找不到这张牌，连 prompt 都起不来。现已在 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts:10296>) 新增 focused gate `princesses_woodland_helpers 在你打出被他人拥有的行动后，仍应给你创建回收选择并把该行动放回其拥有者牌库底`；最小修复是让 `princessesWoodlandHelpers()` 改为按 `cardUid` 在全体玩家 discard 中定位真实 discard owner，并把该 owner 透传给 `buildValidatedCardToDeckBottomEvents(...)`。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "princesses_woodland_helpers"` => `1 file passed, 3 passed`；邻近 queue 负向回归 `... reactionQueueEventPlayerContext.test.ts --testNamePattern "Woodland Helpers"` => `1 file passed, 1 passed`；`git diff --check -- src/games/smashup/abilities/princesses.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。这格只闭合 Woodland Helpers 在 borrowed action 真实进入 owner discard 后的 source lookup / owner propagation consumer seam，不外推整个 Princesses、所有 action-to-deck consumer，或全部 `ACTION_PLAYED` consumer 已完成。
  - `cthulhu_servitor / self-destroy / borrowed-minion owner provenance` 这轮新命中的是对象级真红灯，不是 `cthulhu_furthering_the_cause` 或 `shared destroy ledger` 的回扫。源码复核已确认 [`cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 的 `cthulhuServitorProgram` 旧实现把自毁 `destroyMinion(...)` 的 `ownerId` 直接写成 `ctx.playerId`；因此当 `cthulhu_servitor(controller='0', owner='1')` 被 P0 使用 talent 自毁时，会被错误送进控制者 P0 的弃牌堆。现已在 [`cthulhu.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/abilities/cthulhu.test.ts>) 新增 focused gate `被他人控制时自毁仍应进入自己拥有者的弃牌堆`，首轮红灯坐实 `MINION_DESTROYED.payload.ownerId === '0'`；最小修复是让 `cthulhuServitorProgram` 先按 `findMinionOnBases(ctx.state, ctx.cardUid)` 取 source minion，再把自毁 owner 改成 `sourceMinion?.owner ?? ctx.playerId`。验证：focused `1 passed`；`cthulhu_servitor（仆人 talent）` 子集 `5 passed`；`git diff --check -- src/games/smashup/abilities/cthulhu.ts src/games/smashup/__tests__/abilities/cthulhu.test.ts` 通过（仅 LF/CRLF warning）。这格只闭合 `cthulhu_servitor` 自毁在 borrowed-minion 场景下的 owner/controller 语义，不外推整个 Cthulhu、所有 self-destroy ability，或全部 destroy producer 已完成。
  - `ninjas_invisible_ninja / onMinionDestroyed + onCardReturnedToHand / destroy-return provenance + queue eligibility` 这轮命中一条新的对象级真红灯，不是旧 recover evidence 重刷，而且已经显式作废 2026-05-21 那段“当前 consumer 不形成活跃泄漏”的旧口径。源码复核已确认 [`titans.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/titans.ts>) 旧实现一边把 destroy 分支写成按 `triggerMinion.owner` 判定“another player's minion”，另一边又把 returned 分支错误写成 `returnedOwnMinion = !!ctx.triggerMinionUid`，导致任何带 `triggerMinionUid` 的 destroy 事件都可能误落到“回手”分支；之前那条 borrowed-enemy destroy 正例之所以绿，是被这个 returned fallback 蒙对。现已把 destroy 判定统一收敛为 `destroyedControllerId = ctx.triggerMinion?.controller ?? ctx.controllerId ?? ctx.playerId`，returned 判定收紧到 `ctx.timing === 'onCardReturnedToHand'`，并新增 `canTriggerInvisibleNinjaTriggered()` 挂到 `onMinionDestroyed/onCardReturnedToHand` 两个注册点，把“自己消灭己方随从”与“他人随从回到他人手牌”挡在 queue 入口外。验证：focused `ninjas_invisible_ninja|Invisible Ninja` 子集 `2 files passed, 8 passed`；`git diff --check -- src/games/smashup/abilities/titans.ts src/games/smashup/__tests__/newFactionAbilities.test.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts` 通过（仅 LF/CRLF warning）。这格只闭合 `ninjas_invisible_ninja` 的 destroy/return provenance 与 queue 入口过滤，不外推整个 Titans / Ninjas / onCardReturnedToHand family 已完成。
  - `shared destroy ledger / MINION_DESTROYED controller provenance payload gap` 这轮又收掉一条新的 shared seam，不是单卡回扫。上一拍把 `base_sakura_garden / cthulhu_furthering_the_cause / vampire_the_count / vampire_opportunist / robot_microbot_archive` 都改成按 destroyed minion `controller` 判定后，`reduce.ts:2112` 继续暴露出一条更底层的合同缝：`MINION_DESTROYED` reducer 在写 `turnDestroyedMinions` 时只会读 live `minion.controller`，一旦 destroy 事件来自 queued/延迟路径，live minion 已经不在基地上，就会直接崩在 `controller: minion.controller`。这不是单条 Time Box / Sprout / Choking Vines / Ninja Assassination / Dunwich Horror 的玩法红灯，而是 destroy 事件载荷本身没把 `controller provenance` 带下来。现已给 [`types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>) 的 `MinionDestroyedEvent.payload` 增量补 `controllerId?: PlayerId`，并让 [`abilityHelpers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/abilityHelpers.ts>) 的 `destroyMinion()/buildValidatedDestroyEvents()` 在 live witness 仍在时把 `controllerId` 一起写入 destroy 事件；[`reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 写 ledger 时改为优先吃 `payload.controllerId ?? minion?.controller ?? ownerId`。同时补齐这批已坐实会丢 live minion 的 destroy producer：`killer_plant_sprout / killer_plant_sprout_pod / killer_plant_choking_vines / elder_thing_dunwich_horror / ninja_assassination`。验证：focused `Time Box|Sprout|Choking Vines|Ninja Assassination|Dunwich Horror` 子集 `7 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `101 passed`；`shayuFactionAbilities.test.ts` 整文件 `17 passed`。这格只闭合 destroy ledger 在 delayed/queued destroy 路径下的 controller provenance payload gap，不外推所有手写 `MINION_DESTROYED` producer 或整个 destroy/discard family 已全量完成。
  - `robot_microbot_archive / onMinionDestroyed / owner-controller provenance` 这轮命中一条新的对象级真红灯，不是旧 evidence 重刷。源码复核已确认 `reducer.ts` 在 destroy 链里把 `ctx.playerId` 传成被消灭随从的 `ownerId`，控制权则单独放在 `ctx.controllerId` / `triggerMinion.controller`；但 [`abilities/robots.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/robots.ts>) 的 `robot_microbot_archive` 旧实现却把 `trigCtx.playerId` 同时当成 Archive 收益方与“你的 Microbot”判定来源。现已在 [`baseFactionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseFactionOngoing.test.ts>) 新增 focused 红灯 `被我方控制但归对手拥有的微型机被消灭时，Archive 仍应按控制权给我方抽牌`：P0 控制 Archive 与 stolen `robot_microbot_guard(controller=0, owner=1)`，触发 `onMinionDestroyed(playerId=1, controllerId=0)` 后，旧实现 `drawEventsP0=0` 直接失败。最小修复是把收益方统一收敛为 `destroyedControllerId = triggerMinion.controller ?? ctx.controllerId ?? ctx.playerId`，并按该 controller 统计 Archive、抽牌与写 `CARDS_DRAWN.playerId`。验证：focused archive 子集 `6 passed`；邻近真实命令链 [`specialInteractionChain.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/specialInteractionChain.test.ts>) 的 `微型机被消灭时 archive 拥有者抽1张牌` 继续 `1 passed`。这格只闭合 `robot_microbot_archive` 在 stolen microbot owner/controller 分离下的 destroy provenance，不外推整个 Robots / onMinionDestroyed family 已完成。
  - `abilityBehaviorAudit / legacy-allowlist drift + generated POD alias visibility` 这轮继续沿 audit 残项收掉一格“审计器自身漂移”，不是新增业务 runtime 修复。当前 `abilityBehaviorAudit.test.ts` 仅剩 3 个红点：`zombies.ts` 新出现 2 处 `registerAbility(...)`、`yuanhou.ts` 仍有大量 `registerInteractionHandler(...)`，以及 `pirate_first_mate_pod / alien_scout_pod / ancient_egyptians_mummy_pod` 三张 POD 卡的 afterScoring 语义被 `hasSpecialSemanticsRegistration()` 误判缺失。源码复核后确认：前两者是 allowlist 落后于当前迁移基线；后三者的 runtime 其实已靠 `registerPodOngoingAliases()` 继承基础版 `afterScoring` trigger，只是 `getRegisteredOngoingEffectIds()` 为避免重复审计会隐藏 generated POD alias，导致 helper 只查显式注册时看不到。现已在 [`abilityBehaviorAudit.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/abilityBehaviorAudit.test.ts>) 补 `zombies.ts / yuanhou.ts` 到遗留 allowlist，并在 [`helpers/auditUtils.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/helpers/auditUtils.ts>) 让 `checkOngoingRegistration(defId.endsWith('_pod'))` 在本卡无显式 ongoing 注册时回退到基础版 defId。验证：`abilityBehaviorAudit.test.ts --config vitest.config.audit.ts` => `26 passed`；`git diff --check -- abilityBehaviorAudit.test.ts helpers/auditUtils.ts` 通过（仅 LF/CRLF warning）。本格只收掉 audit helper / allowlist 漂移，不外推 zombies / yuanhou 全量迁移完成，也不把 generated POD alias 当成显式业务实现新增。
  - `POD alias / queued runtime context / dedicated runtime-gate backfill` 本轮没有新增 shared/runtime 修复，而是把 3 条此前只挂在 `abilityBehaviorAudit` 红项里的 POD alias 差集降成可复查绿格：新增 [`reactionQueueSourceRuntimeContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts>) gates `queued afterScoring per-instance trigger 在对手计分时仍应把 pirate_first_mate_pod 的后续选择权交给控制者`、`queued afterScoring per-instance trigger 在对手计分时仍应把 alien_scout_pod 的选择权交给控制者`，以及 [`reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) gate `sourceController queued afterScoring trigger 仍应把 Mummy POD 的埋葬 prompt 交给木乃伊控制者`。这 3 条都直接转绿，验证：focused `pirate_first_mate_pod|pirate_first_mate 的后续选择权交给控制者` => `2 passed`；双文件过滤 `pirate_first_mate_pod|alien_scout_pod|Mummy POD` => `2 files passed, 3 passed`；`git diff --check -- reactionQueueSourceRuntimeContext.test.ts reactionQueueEventPlayerContext.test.ts` 通过。结论是当前缺口更像“alias 缺 dedicated gate”，不是新的 queue/runtime 开口；不外推 Pirates / Aliens / Ancient Egyptians 全量完成。
  - `effect-contract queued interaction / baseDefId-only option footprint / fallback not global-conflict` 新收掉一条 shared interaction-footprint 真缝：`base_the_nexus_choose` / `time_travelers_time_is_fleeting_choose` 这类真实 prompt 的 option 只有 `{ baseDefId }`，旧 `deriveFootprintFromInteraction()` 只认 `baseIndex/minionUid/...` 结构键，结果把这类 prompt 降成 `fallbackReason=interaction:...:unstructured-options` 且 `reads/writes=[]`；而 `resourceFootprintsConflict()` 又把“fallback 且空 footprint”视为与所有 trigger 冲突，导致本来和 `CARDS_DRAWN` 之类互不冲突的 mandatory queue 也会被错误抬成 `smashup_reaction_choose`。现已把补洞限制在 interaction footprint 层：`deriveFootprintFromOption()` 遇到 `option.value.baseDefId` 且无 `baseIndex` 时，显式写入 `{ kind:'baseDeck' }`，不扩大到 event/generic value 层。新增 [`reactionQueueBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts>) focused gate `baseDefId-only queued interaction 不应因 footprint fallback 被错误抬成 reaction ordering`，首轮红灯坐实当前 prompt 落到 `smashup_reaction_choose`；修复后 focused `1 passed`、整文件 `25 passed`。邻近 targeted 复跑 [`yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts) 的 `The Nexus / Time Is Fleeting` base-replacement 子集 `10 passed`。本格只闭合 `baseDefId-only discard-base choice` 的 interaction footprint 推导，不外推其它 top-level afterScoring multi-special exposure；同轮原先顺手挂起的 `Time Is Fleeting + Wormhole` residual 已在 2026-05-23 复核后降级为测试断言漂移：首个 `smashup_reaction_choose` 实际是 nested `Jumper` frame，不是 outer `score-after` 窗口，不能再混记为本格回退。
  - `base_ninja_dojo / afterScoring / tied-champion whole-trigger optionality` 新收掉一条 base afterScoring runtime 真缝：`Ninja Dojo` 文本是“在这个基地计分后，冠军可以消灭任意一个随从”。这里的“可以”属于每位平局冠军各自的“可不消灭”选择，不是当前 reaction owner 对整条 tied-champion 连锁的一次性总开关。旧注册把 [`baseAbilities.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities.ts>) 中 `base_ninja_dojo.afterScoring` 记成 `mandatory:false`，导致 reaction queue 会先弹 `smashup_reaction_choose`，允许当前响应者整体跳过所有冠军的消灭资格。现已改为“整条 trigger mandatory；每位冠军自己的 prompt 内再各自 skip”。新增 [`reactionQueueBaseOptionalClockwise.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueBaseOptionalClockwise.test.ts>) focused gate `base_ninja_dojo 平局冠军时不应先把整条 tied-champion 链暴露成 reaction_choose`，修复后确认 queue trigger `mandatory=true`，且 `maybeResolveReactionQueue(...)` 直接进入 `sourceId='base_ninja_dojo'` 的真实交互，由首位冠军先选择，下一位冠军交互进入 queue。邻近验证：`reactionQueueBaseOptionalClockwise.test.ts` 整文件 `7 passed`；`baseAbilityIntegrationE2E.test.ts -t "基地达标 → Interaction 冠军消灭随从"` => `1 passed`；`baseAbilitiesPrompt.test.ts -t "base_ninja_dojo"` => `3 passed`；`git diff --check` 通过（仅 LF/CRLF warning）。该格只闭合 `Ninja Dojo` 的 tied-champion whole-trigger optionality，不外推所有 afterScoring prompt chain、所有 tied champion 语义或整个基础版基地 family 已完成。
  - `wizard-academy-scout-afterscore.test.ts / 历史 skip 债务 / object-level afterScoring chain` 已从过时夹具改成现代 focused gate，不再让这条旧 repro 继续污染开放候选面。重写后的 [`wizard-academy-scout-afterscore.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-academy-scout-afterscore.test.ts>) 直接走当前主流程：`ADVANCE_PHASE` 后先进入 `base_wizard_academy`，解决基地牌库顶选择后，`alien_scout_return` 仍继续打开；在 Scout prompt 出现前断言基地尚未被清场/替换且 `scout-0` 仍在场，随后选择回手，最终 `scout-0` 仅回手 1 次且基地替换为 `base_secret_garden`。验证：`wizard-academy-scout-afterscore.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/wizard-academy-scout-afterscore.test.ts` 通过（仅 LF/CRLF warning）。这格只收掉历史 skip 债务并补 dedicated green evidence，不代表发现了新的 shared runtime 红灯，也不外推整个 afterScoring family 已完成。
  - `mothership-scout-afterscore-bug.test.ts / 历史 skip 债务 / multi-afterScoring chain` 也已从过时 engineConfig 夹具改成现代 focused gate，不再把“母舰 + 两侦察兵 + 大副”的旧 repro 留在开放候选面。重写后的 [`mothership-scout-afterscore-bug.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/mothership-scout-afterscore-bug.test.ts>) 直接走当前主流程，并兼容 `smashup_reaction_choose` 或 direct prompt 两种现行形态：先解决 `base_the_mothership` 的收回随从选择，再依次解决两个 `alien_scout_return` 与 `pirate_first_mate_choose_base`；在每一段后都断言基地仍未提前清场，直到最后一个 First Mate prompt 解决后才完成 `base_secret_garden` 替换。验证：`mothership-scout-afterscore-bug.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/mothership-scout-afterscore-bug.test.ts` 通过（仅 LF/CRLF warning）。这格只收掉对象级多段 afterScoring 历史 skip 债务并补 dedicated green evidence，不外推整个 afterScoring family 或所有旧 skip 已完成。
  - `miskatonic-scout-afterscore.test.ts / 历史 skip 债务 / 规则口径漂移` 已改成“当前实现并不存在这条基地 afterScoring 链”的 focused gate，不再把过时假设继续挂成开放候选。重写后的 [`miskatonic-scout-afterscore.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/miskatonic-scout-afterscore.test.ts>) 直接证明：`base_miskatonic_university_base` 当前只在 `onMinionPlayed` 生效；当 `ADVANCE_PHASE` 触发基地计分时，不会先进入 `base_miskatonic_university_base` prompt，而是直接继续 `alien_scout_return`（或经 `smashup_reaction_choose` 选入 Scout prompt），且最终只保留 1 张原有 `special_madness`，没有额外触发密大基地的疯狂牌效果。验证：`miskatonic-scout-afterscore.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/miskatonic-scout-afterscore.test.ts` 通过（仅 LF/CRLF warning）。这格只说明旧 skip 是历史规则口径漂移，不代表修了新的 runtime seam，也不外推整个 Miskatonic 或 afterScoring family 已完成。
  - `test-alien-scout-afterscore.test.ts / 历史 skip 债务 / 单卡真实计分流` 已改成最窄 focused gate，不再让“侦察兵 afterScoring 是否还活着”继续挂在过时 `GameTestRunner('smashup', setupFn)` 夹具上。重写后的 [`test-alien-scout-afterscore.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/test-alien-scout-afterscore.test.ts>) 直接验证：`ADVANCE_PHASE` 进入基地计分后，会继续打开 `alien_scout_return`（或经 `smashup_reaction_choose` 选入该 prompt）；选择 `yes` 后 `scout-0` 只回手 1 次，基地随后替换为 `base_faceless_city`。验证：`test-alien-scout-afterscore.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/test-alien-scout-afterscore.test.ts` 通过（仅 LF/CRLF warning）。这格只补单卡真实计分流 green evidence，不外推所有外星人 afterScoring、所有 queued ordering 或整个 afterScoring family 已完成。
  - `igor-ondestroy-idempotency.test.ts / 历史 skip 债务 / House of Nine Lives pendingSave 两阶段 gate` 已从过时 `base_nine_lives` 夹具改成现代 focused gate，不再把这条旧 skip 挂成“九命之屋可能让 Igor 提前 onDestroy”的开放怀疑。重写后的 [`igor-ondestroy-idempotency.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/igor-ondestroy-idempotency.test.ts>) 直接验证：当 `frankenstein_igor` 在 `base_house_of_nine_lives` 以外的基地被消灭时，Phase 1 只出现 `base_nine_lives_intercept`、不会出现 `frankenstein_igor` prompt，且 `MINION_DESTROYED` 被压住；随后通过真实 handler 选择“不拯救”，恢复 `reason='九命之屋：玩家选择不拯救'` 的 destroy 事件，再次走 `processDestroyTriggers(...)` 时才出现且只出现 1 个 `frankenstein_igor` prompt。验证：`igor-ondestroy-idempotency.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/igor-ondestroy-idempotency.test.ts` 通过（仅 LF/CRLF warning）。这格只把 `Igor + House of Nine Lives` 的历史 skip 债务降为 dedicated green evidence，不外推所有 onDestroy、所有 pendingSave replacement 或整个 Frankenstein family 已完成。
  - `newFactionAbilities.test.ts / 历史 skip 债务 / vampire_opportunist 真实 Big Gulp destroy 链` 已确认不是 runtime 红灯，而是旧 skip 自己没把 `vampire_big_gulp` 的目标选择 prompt 走完。更新后的 [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 现用当前主流程验证：P0 先真实打出 `vampire_big_gulp`，再响应 `sourceId='vampire_big_gulp'` prompt 选择敌方 `e1`；结算后断言 `vampire_opportunist` 产生 `POWER_COUNTER_ADDED(reason='vampire_opportunist')`，宿主 `m0` 的 `powerCounters===1`，且 `e1` 已离场。验证：`newFactionAbilities.test.ts --testNamePattern "投机主义"` => `3 passed`；`git diff --check -- src/games/smashup/__tests__/newFactionAbilities.test.ts` 通过（仅 LF/CRLF warning）。这格只把 `vampire_opportunist` 的历史 skip 债务降为 dedicated green evidence，不外推所有 Vampires、所有 destroy trigger 或整个 `newFactionAbilities` 文件已完成。
  - `wizard-archmage-zombie-interaction.test.ts / 历史 skip 债务 / They Keep Coming 真实 prompt 链` 已确认不是 runtime 红灯，而是旧 `GameTestRunner + autoRespond` 壳已经落后于当前 `zombie_they_keep_coming` 的 merged-value 交互协议。重写后的 [`wizard-archmage-zombie-interaction.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-archmage-zombie-interaction.test.ts>) 现按真实主流程验证：P0 打出 `zombie_they_keep_coming` 后先进入 `sourceId='zombie_they_keep_coming'`、`targetType='discard_minion'` 的弃牌堆随从选择；随后用一次 `RESPOND { optionId, mergedValue:{ cardUid, defId, baseIndex } }` 把 `wizard_archmage` 从弃牌堆真实打到基地，最终断言 `archmage-1` 离开 discard 并进入基地、`actionsPlayed===1`、`actionLimit===2`，且本步结算中恰有 1 条 `LIMIT_MODIFIED(reason='wizard_archmage')`。验证：`wizard-archmage-zombie-interaction.test.ts` => `1 passed`；`git diff --check -- src/games/smashup/__tests__/wizard-archmage-zombie-interaction.test.ts` 通过（仅 LF/CRLF warning）。这格只把 `They Keep Coming -> Archmage` 的历史 skip 债务降为 dedicated green evidence，不外推整个 Zombies/Wizards、所有弃牌堆打出路径或全部 onMinionPlayed family 已完成。
  - `interactionChainE2E.test.ts / 历史 skip 债务 / alien_probe 多对手二段交互链` 已确认不是 sourceId/runtime 红灯，而是旧用例本身还停留在早期错误语义“查看牌库顶并放顶/底”。当前 [`alien-probe-bug.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/alien-probe-bug.test.ts>) 已证明 `alien_probe` 真实主流程是“先选对手，再从其手牌中选一张随从并令其弃掉”；本轮把 [`interactionChainE2E.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/interactionChainE2E.test.ts>) 中对应旧 skip 改成同口径 focused gate：P0 打出 `alien_probe` 后先进入 `sourceId='alien_probe_choose_target'`、`targetType='player'` 的选对手 prompt；选中 P1 后进入 `sourceId='alien_probe'`，并只允许点 P1 手牌中的随从 `p1-m1`，禁用行动卡 `p1-a1`；最终断言 `p1-m1` 从 P1 手牌进入弃牌堆，`p1-a1` 仍留在手牌，P2 手牌不受影响。验证：`interactionChainE2E.test.ts -t "选对手 → 选其手牌中的随从 → 该对手弃掉该随从"` => `1 passed`。这格只把 `alien_probe` 在交互链总表里的历史 skip 债务降为 dedicated green evidence，不外推所有 inspect/discard prompt、所有对手手牌可视化或整个 Aliens family 已完成。
  - `wizard-archmage-debug.test.ts / 纯 debug 壳 / 重复 Archmage discard-play 合同` 已不再保留。源码复核后确认它不是独立玩法链，而是旧 `GameTestRunner` 调试壳，语义与现有 [`wizard-archmage-discard-play.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-archmage-discard-play.test.ts>) 的“直接 fromDiscard 打出大法师获得额外行动”以及刚新增的 [`wizard-archmage-zombie-interaction.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-archmage-zombie-interaction.test.ts>) 的“通过 They Keep Coming 真实 discard-play prompt 打出大法师”完全重叠，只会把已闭合对象继续伪装成开放 skip。现已删除 [`wizard-archmage-debug.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-archmage-debug.test.ts>)，并复跑 `wizard-archmage-discard-play.test.ts + wizard-archmage-zombie-interaction.test.ts` => `2 files passed, 3 passed`。当前 SmashUp 测试内剩余 skip 已降到 4 条：`elder-thing-multi-select-integration`、`ninja-hidden-ninja-interaction-bug`、`steampunk-aggromotive-bug`、`vampireBuffetE2E`。
  - `ninja-hidden-ninja-interaction-bug.test.ts / 旧 Me First 调试壳 / 重复 Hidden Ninja 交互创建合同` 也已不再保留。复核后确认这条 skip 不是独立 seam，而是更早的 `systems:[] + 手写 responseWindow` 壳；它想锁的合同“P0 在 Me First 窗口打出 Hidden Ninja 后应创建 `sourceId='ninja_hidden_ninja'` 的 hand 选择交互”，现已被两条更贴近当前生产合同的绿测覆盖：[`ninja-hidden-ninja-interaction-bug-repro.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ninja-hidden-ninja-interaction-bug-repro.test.ts>) 直接复现真实 `scoreBases + meFirst` 场景并确认 `specialLimitUsed` 与交互创建；[`ninja-hidden-ninja-no-minions.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ninja-hidden-ninja-no-minions.test.ts>) 则继续锁“无随从自动收口 / 有随从保持窗口打开”的边界。本轮已删除 [`ninja-hidden-ninja-interaction-bug.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ninja-hidden-ninja-interaction-bug.test.ts>)，并复跑上述两文件 => `2 files passed, 3 passed`。当前 SmashUp 测试内剩余 skip 已降到 3 条：`elder-thing-multi-select-integration`、`steampunk-aggromotive-bug`、`vampireBuffetE2E`。
  - `steampunk-aggromotive-bug.test.ts / 旧错误模型壳 / 重复 Aggromotive 基地总力量合同` 现也已不再保留。复核后确认这条 skip 不是待修 runtime，而是仍停留在“第一个随从吃 +5、移除后加成会跳到第二个随从”的早期错误模型；而当前生产实现已经在 [`ongoing_modifiers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ongoing_modifiers.ts>) 明确按 `registerBasePowerModifier('steampunk_aggromotive')` 给拥有者在该基地的总力量 +5，且现有 [`steampunk-aggromotive-fix.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/steampunk-aggromotive-fix.test.ts>) 与 [`ongoingModifiers.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/ongoingModifiers.test.ts>) 已完整锁住“总力量而非单个随从”“移除后不跳加成”“叠加/异玩家/无随从/POD”这些边界。本轮已删除 [`steampunk-aggromotive-bug.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/steampunk-aggromotive-bug.test.ts>)，并复跑 `steampunk-aggromotive-fix.test.ts + ongoingModifiers.test.ts` 的蒸汽机车子集 => `2 files passed, 10 passed`。当前 SmashUp 测试内剩余 skip 已降到 2 条：`elder-thing-multi-select-integration`、`vampireBuffetE2E`。
  - `elder-thing-multi-select-integration.test.ts / 未适配集成壳 / 重复 Elder Thing 两段选择合同` 也已不再保留。复核后确认这条 skip 连 `GameTestRunner` / `SmashUpCore` / `PLAYER_IDS` 都未接线完成，不是“还缺一次修复”的真红灯；而它想证明的 Elder Thing 链路已经被 [`elder-thing-multi-select.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elder-thing-multi-select.test.ts>)、[`choice-audit-fixes.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/choice-audit-fixes.test.ts>)、[`newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 与 [`elderThingsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingsPod.test.ts>) 承接：分别锁多选交互结构、第一步/第二步 destroy prompt、基础版 onPlay 交互，以及 FAQ / POD 边界。本轮已删除 [`elder-thing-multi-select-integration.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elder-thing-multi-select-integration.test.ts>)，并复跑 Elder Thing 邻近子集 => `2 files passed, 10 passed`（另有 `elderThingsPod.test.ts` 在该过滤下整文件跳过，不构成失败）。当前 SmashUp 测试内剩余 skip 已降到 1 条：`vampireBuffetE2E`。
  - `vampireBuffetE2E.test.ts / 历史窗口假设漂移 / Buffet + Champions 旧响应窗口径` 现已改成当前真实窗口合同并转绿，不再保留最后一条 `describe.skip`。复核后确认旧文件的两个核心假设都已经漂移：其一把 `Buffet` 当成“在同一条测试里继续证明 afterScoring 放指示物”的一体链，但当前权威状态里 `BASE_SCORED` 已先发生，更稳的 bridge 应是“真实打出 -> 离手入弃 -> 写入 pendingAfterScoringSpecials”；其二把 `giant_ant_we_are_the_champions` 继续当成响应窗里会先写 `SPECIAL_AFTER_SCORING_ARMED`，而当前合同已是“在 afterScoring 响应窗口中立即起交互”。本轮已把 [`vampireBuffetE2E.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/vampireBuffetE2E.test.ts>) 从 `describe.skip` 转成 2 条当前 gate，并复跑整文件 => `1 file passed, 2 passed`。当前 `rg -n "\\b(it|test|describe)\\.skip\\(" src/games/smashup/__tests__ -S` 已无输出，说明 SmashUp 测试目录内历史 `skip` 债务当前已清零。
  - `base_sheep_shrine / onBaseRevealed / whole-trigger optionality` 新收掉一条 extended-base runtime 真缝：`Sheep Shrine` 文本是“这张基地入场后，每位玩家可以移动一个他们的随从到这”，旧注册却把整条 `onBaseRevealed` trigger 记成 `mandatory:false`，导致当前反应玩家可以在 `smashup_reaction_choose` 里一次跳过所有玩家的移动资格。现已把 [`baseAbilities_expansion.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities_expansion.ts>) 的 `base_sheep_shrine` 改为“整体 mandatory、每位玩家自己的 prompt 内再各自 skip”。新增 [`reactionQueueOnBaseRevealed.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOnBaseRevealed.test.ts>) focused gate `base_sheep_shrine 不应把整条多玩家移动链暴露成当前玩家可整体跳过的 optional trigger`，首轮红灯坐实 queued trigger 的 `mandatory=false`；修复后确认 `mandatory=true`，并且 `maybeResolveReactionQueue(...)` 直接打开 `sourceId='base_sheep_shrine'` 的真实交互，当前玩家只处理自己的可选移动，下一位玩家的交互进入 queue。验证：focused `1 passed`；`reactionQueueOnBaseRevealed.test.ts` 整文件 `6 passed`；`expansionBaseAbilities.test.ts -t "base_sheep_shrine|base_the_pasture"` => `2 passed`；`npx eslint src/games/smashup/domain/baseAbilities_expansion.ts src/games/smashup/__tests__/reactionQueueOnBaseRevealed.test.ts` => 0 errors。该格只闭合 `Sheep Shrine` 的 whole-trigger optionality，不外推所有 `onBaseRevealed`、所有 multi-player queue chain 或整个 extended-base family 已完成。
  - `fireTriggers direct caller inventory / duel + destroy replacement exceptions` 已把生产侧 `fireTriggers(...)` 直执行 caller 面补成 dedicated static gate。新增 `reactionQueueFireTriggersCallerContract.test.ts`，递归扫描 `src/games/smashup` 生产源码，排除定义文件 `domain/ongoingEffects.ts` 后，要求当前唯一合法直执行入口仍只有 `domain/duel.ts:onDuelResolved`、`domain/duel.ts:onDuelStarted` 与 `domain/reducer.ts:onMinionDestroyed` replacement；其中 destroy 入口必须保留 `phase:'replacement'`。验证：新文件 `1 file passed, 1 passed`；Pecos Bill duel smoke 三条 `3 passed`；pendingSave replacement 邻近三文件过滤 `6 passed / 9 skipped`。本格不改 runtime，只把 direct `fireTriggers` 例外清单固化，不外推所有 direct fire caller 语义或所有 duel/replacement 语义已全量完成。
  - `direct trigger caller / fireTriggerForSource inventory / immediate start-turn minion exception` 已把“生产代码不得新增未审计的 direct trigger caller”补成 dedicated static gate。新增 `reactionQueueDirectTriggerCallerContract.test.ts`，递归扫描 `src/games/smashup` 生产源码，排除定义文件 `domain/ongoingEffects.ts` 后，要求唯一 `fireTriggerForSource(...)` 生产调用点只存在于 `domain/index.ts` 的 `processImmediateStartTurnMinionTriggers()`，且该例外必须继续用 `playedEvent.payload.defId` + `'onTurnStart'`，并通过 `skipImmediateStartTurnMinionTriggers: true` 防止递归重复处理。验证：新文件 `1 file passed, 1 passed`；邻近 `ongoingEffects.test.ts -t "fireTriggerForSource"` => `3 passed`；`reactionQueueEventPlayerContext.test.ts -t "Water Lily POD|Sprout POD|Choking Vines|Entangled POD|Zombie Overrun POD"` => `5 passed`。本格不改 runtime，只把 direct caller 例外收窄成可复查 inventory gate，不外推所有 start-turn/onTurnStart 语义已全量完成。
  - `destroy pendingSave replacement / PREVENT_DESTROY_SOURCE_IDS / interactive replacement green contract` 已把“当前仍依赖 sourceId 白名单的交互式 prevent-destroy replacement”补成 dedicated static/neighbor gate。新增 `reactionQueueDestroyPendingSaveContract.test.ts`，锁定 `base_nine_lives_intercept`、`giant_ant_drone_prevent_destroy`、`pirate_buccaneer_move` 三个会创建 pending-save 交互的 replacement sourceId 必须继续进入 `processDestroyTriggers()` 白名单，并且 SmashUp 本地 `rule/ENGINE_GUIDE.md` 与 runtime 白名单一致。验证：新文件 `1 file passed, 2 passed`；邻近 `madMonsterPartyPreventedDestroy.test.ts` / `giantAntsPod.test.ts` / `buccaneer-pod-limit.test.ts` 过滤 `prevent|pendingSave|雄蜂|Buccaneer|buccaneer|pirate_buccaneer_move|giant_ant_drone_prevent_destroy` => `3 files passed, 6 passed, 9 skipped`。本格不改 runtime，也不解决 `docs/ai-rules/engine-systems.md` 与 SmashUp 本地 guide 对“是否应白名单”的规范冲突；它只把当前实际合同变成可复查 green gate，防止新增/改名交互式 replacement 时无声漏掉 pending-save。
  - `destroy pendingSave replacement / global-doc contract alignment / scoped whitelist exception` 已继续把 pendingSave 的规范冲突收口成可复查静态合同：[`docs/ai-rules/engine-systems.md`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/docs/ai-rules/engine-systems.md>) 不再笼统写“不得按 sourceId 过滤拯救交互”，而是显式改成“通用原则 + SmashUp 当前 runtime 例外”两层口径，明确当前 `processDestroyTriggers()` 仍依赖 `PREVENT_DESTROY_SOURCE_IDS`，并点名 `base_nine_lives_intercept / giant_ant_drone_prevent_destroy / pirate_buccaneer_move` 三个已锁 sourceId；同时把 [`reactionQueueDestroyPendingSaveContract.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueDestroyPendingSaveContract.test.ts>) 扩成第 3 条静态门禁，要求项目总规范必须显式承认这条 SmashUp 白名单合同，避免以后再被总规范误导去删 `PREVENT_DESTROY_SOURCE_IDS`。验证：`reactionQueueDestroyPendingSaveContract.test.ts` => `1 file passed, 3 passed`；`git diff --check -- docs/ai-rules/engine-systems.md src/games/smashup/__tests__/reactionQueueDestroyPendingSaveContract.test.ts` 通过（仅 LF/CRLF warning）；`.omx+` 长期状态 JSON 解析通过。该格不改 runtime，只收口“全局规范 vs 本地 guide vs 静态 gate”三者的合同分歧，不外推 pendingSave 架构已重写完成。
  - `ACTION_PLAYED side branch / construction inventory static gate` 已把“生产玩法代码不得绕过 `buildActionPlayedEvent()` 手写 `ACTION_PLAYED` 事件对象”补成 dedicated green gate。新增 `reactionQueueActionPlayedConstruction.test.ts` 递归扫描 `src/games/smashup` 生产源码，排除唯一工厂 `domain/actionPlayEvent.ts` 与非玩法元数据 `tutorial.ts`，直接禁止 `type: SU_EVENTS.ACTION_PLAYED` / `type: SU_EVENT_TYPES.ACTION_PLAYED` 裸构造重新出现。验证：新文件 `1 passed`；邻近 `reactionQueueBaseAbilities.test.ts -t "ACTION_PLAYED|onActionPlayed|base_enchanted_glade"` `3 passed`。本格不改 runtime，只把前面 Duel / buried / Cream Puff / Miskatonic 等 side branch 收口沉淀成可复查 inventory gate，不外推所有 `ACTION_PLAYED` caller 语义都已全量完成。
  - `filterProtectedReturnEvents / filterProtectedDeckBottomEvents / source-batched Shielding protection / green sibling gates` 已把 return/deck-placement sibling 候选降成 dedicated green evidence：两个 wrapper 当前都复用 `filterProtectedAffectEvents()`，新增 focused gates 直接证明当前置不同来源事件先移除 `Shielding` 后，后续异源 `MINION_RETURNED`、`CARD_TO_DECK_BOTTOM` 与 `CARD_TO_DECK_TOP` 不会继续被 stale core 保护吞掉。验证：`yuanhouFactionAbilities.test.ts -t "护盾被前置不同来源事件移除后，不应继续保护随后回手的宿主|护盾被前置不同来源事件移除后，不应继续保护随后放到牌库底的宿主|护盾被前置不同来源事件移除后，不应继续保护随后放到牌库顶的宿主"` => `1 file passed, 3 passed`；邻近 `护盾持续保护|护盾被前置不同来源事件移除|克莱德2.0|灵长公园阻止这里随从身上的行动` => `13 passed`；更宽 `护盾|Shielding|细胞结合复制护盾` => `9 passed`；eslint `0 errors, 1 warning`（既有 unused warning）。本格不改 runtime，只证明前面 source-batched protection 修复没有遗漏 return/deck-top/bottom sibling，不外推 protection family 或 yuanhou 全量完成。
  - `externalActionPlay / ongoing-without-onPlay executor tolerance / wizard_mass_enchantment minion-target ACTION_PLAYED context` 新收掉一条 shared carrier 真缝：旧 `appendResolvedActionAbility()` 会对所有外部打出的行动强制 `requireOnPlay(...)`，与正常 `PLAY_ACTION` 对“ongoing 且无 onPlay executor”的容忍语义不一致；当 `wizard_mass_enchantment` 从牌库顶外部打出 `ninja_smoke_bomb` 这类附着型 ongoing 时，会直接抛 `SmashUp ability 缺少声明: ninja_smoke_bomb::onPlay (externalActionPlay.appendResolvedActionAbility)`。现已改为先 `resolveSpecial(defId) ?? resolveOnPlay(defId)`，并对 `subtype === 'ongoing'` 且无 executor 的外部打出路径比照正常出牌静默跳过，不再报错；非 ongoing 仍保留原有严格性。新增 `query6Abilities.test.ts` focused gate `wizard_mass_enchantment: 打出附着到随从的行动时应保留目标上下文并触发 base_enchanted_glade`，修复后确认 `ACTION_PLAYED.payload.targetBaseIndex=0`、`targetType='minion'`、`targetMinionUid='host-0'`，且 `base_enchanted_glade` 真实抽到 `draw-a`。验证：`query6Abilities.test.ts -t "wizard_mass_enchantment"` => `1 file passed, 4 passed`；邻近 `expansionOngoing.test.ts -t "steampunk_mechanic|steampunk_change_of_venue"` => `1 file passed, 9 passed`；`npx eslint src/games/smashup/domain/externalActionPlay.ts src/games/smashup/__tests__/query6Abilities.test.ts` => `0 errors, 13 warnings`。该格只闭合 external action play 对无 `onPlay` ongoing 的容忍语义，以及 `wizard_mass_enchantment -> minion target attached action -> base_enchanted_glade` 这条真链，不外推所有 external replay、所有 Wizards 或整个 external action play family 已完成。
  - `filterProtectedMoveEvents / filterProtectedDestroyEvents / source-batched Shielding protection / stale core` 新收掉 move/destroy sibling 的批内现场漂移真缝：上一格已让 `filterProtectedAffectEvents()` 按来源推进 `workingCore`，但 move/destroy 两个保护过滤器仍用入口旧 `core` 判断。当前置不同来源事件已让 `Shielding` 离场时，后续对手来源的 `MINION_MOVED` / `MINION_DESTROYED` 仍会被旧现场错误拦掉。现已让 `filterProtectedMoveEvents()` 与 `filterProtectedDestroyEvents()` 同样按来源 key 延迟推进：同一来源批次保留原现场，不同来源切换时先 reduce 前一批输出事件，再判断后续保护。验证：新增两个 focused 用例首轮红灯后转绿，`yuanhouFactionAbilities.test.ts -t "护盾被前置不同来源事件移除后，不应继续保护随后移动的宿主|护盾被前置不同来源事件移除后，不应继续保护随后被消灭的宿主"` => `2 passed`；Shielding/Clyde/Primate Park 邻近 `-t "护盾持续保护|护盾被前置不同来源事件移除|克莱德2.0|灵长公园阻止这里随从身上的行动"` => `10 passed`；更宽 Shielding 子集 `-t "护盾|Shielding|细胞结合复制护盾"` => `6 passed`；`npx eslint src/games/smashup/domain/reducer.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` => 0 errors / 24 warnings。该格不外推所有 protection family 或 yuanhou 全量完成。
  - `filterProtectedAffectEvents / source-batched Shielding protection / stale core` 新收掉一条 attached action protection 的批内现场漂移真缝：旧过滤器对整批事件一直用传入的 `core` 判断保护，导致前置不同来源已让 `Shielding` 离场时，后续对手来源仍会借旧现场错误保护同宿主其他行动。现已让 `filterProtectedAffectEvents()` 维护本地 `workingCore`，按影响来源分组延迟推进：同一来源批次内 `Shielding` 仍保护整批影响，不同来源切换时先 reduce 已输出事件，再判断后续保护。验证：focused `yuanhouFactionAbilities.test.ts -t "护盾被前置不同来源事件移除"` => `1 passed`；Shielding/Clyde/Primate Park 邻近 `-t "护盾持续保护|护盾被前置不同来源事件移除|克莱德2.0|灵长公园阻止这里随从身上的行动"` => `8 passed`；更宽 Shielding 子集 `-t "护盾|Shielding|细胞结合复制护盾"` => `4 passed`；`npx eslint src/games/smashup/domain/reducer.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` => 0 errors / 24 warnings。该格不外推所有 protection family 或 yuanhou 全量完成。
  - `processClydeDetachChoices / same-batch moved host / stale context` 新收掉一条 Clyde 2.0 replacement choice 的同批事件现场漂移真缝：旧 detector 直接用传入 `state.core` 查 attached action 的宿主与 Clyde，若同一批事件先把宿主移动到另一基地、后续才处理 `ONGOING_DETACHED`，仍会按旧基地现场错误弹出 Clyde 回收 prompt。现已让 `processClydeDetachChoices()` 对非拦截事件按顺序 reduce 本地 `matchState.core`，再用推进后的现场判断 detached action 是否仍满足 same-base/Clyde 条件；无合格 Clyde 时保留原 `ONGOING_DETACHED` 继续正常结算。验证：新增 focused 用例首轮红灯后转绿；`yuanhouFactionAbilities.test.ts -t "克莱德2.0处理同批离场事件"` => `1 passed`；`-t "克莱德2.0|灵长公园阻止这里随从身上的行动"` => `6 passed`；`npx eslint src/games/smashup/domain/reducer.ts src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts` => 0 errors / 24 warnings。整文件 `yuanhouFactionAbilities.test.ts` 当前仍有 9 个既有红项，不能作为本格失败证据，也不能外推 yuanhou 全量完成。
  - `processReturnToHandTriggers / CARD_RECOVERED_FROM_DISCARD / multi-card returned frame` 新收掉一条上一轮 evidence 已标为“表达能力边界”的真缝：同批从弃牌堆回手多张随从时，旧分支只按第一张 recovered minion 收集 `onCardReturnedToHand` trigger，导致第二张随从没有独立 `triggerMinionUid/sourceEventId/frameId`。现已让 `CARD_RECOVERED_FROM_DISCARD` 对 `payload.cardUids` 逐张从旧弃牌堆快照还原卡牌，并为每张卡独立构造 `card-returned-to-hand:...:<cardUid>:...` frame/sourceEventId。验证：`reactionQueueOnCardReturnedToHand.test.ts -t "CARD_RECOVERED_FROM_DISCARD 同批回手多张随从"` focused `1 passed`；整文件 `14 passed`；邻近 `newFactionAbilities.test.ts` Invisible Ninja recover 子集 `1 passed`；`zombieWizardAbilities.test.ts` recover/not-enough-bullets 子集 `5 passed`；`reactionQueueEventPlayerContext.test.ts` returned-to-hand 相关子集 `3 passed`。
  - `ACTION_PLAYED side branch / Miskatonic Librarian POD Madness untargeted gate` 已把生产中最后一个直接手写 `SU_EVENTS.ACTION_PLAYED` 的玩法分支降成绿色合同门禁：`miskatonic_librarian_pod_play_madness` 只打 `special_madness`，当前 Madness 是无目标 extra action，不需要二段目标 prompt。实现已改为统一 `buildActionPlayedEvent(...)`，并在现有 Madness 用例里锁定 `targetBaseIndex/targetType/targetMinionUid` 均不存在。验证：focused `madnessAbilities.test.ts` `1 passed`；`reactionQueueBaseAbilities.test.ts -t "ACTION_PLAYED|onActionPlayed|base_enchanted_glade"` `3 passed`。
  - `ACTION_PLAYED side branch / Cream Puff Man external discard target context` 已从单一 `base-target` 补成 `base-target + minion-target` 两格 dedicated gate：`ghosts_creampuff_man` 从弃牌堆额外打出标准行动时，旧实现先前只按无目标行动校验候选，导致 `cyborg_apes_going_bananas` 这类 `playNeedsBase` 行动被静默排除；而需要随从目标的标准行动即使能被放行，也必须继续保留 `targetMinionUid`，否则 `base_enchanted_glade.onActionPlayed` 看不到“行动打到这里的随从”这条后处理链。现已让 Cream Puff Man 对 `none/base/minion` 三类标准行动分别生成候选；需要目标的行动先进入 `titan_ghosts_creampuff_man_action_target` 二段 prompt，最终统一用 `buildActionPlayedEvent(...)` 保留 `targetBaseIndex/targetType/targetMinionUid`，并继续执行行动能力与 `CARD_TO_DECK_BOTTOM`。除原有 `Going Bananas` base-target 分支外，本轮再补 `alien_beam_up` minion-target gate：`ACTION_PLAYED.payload` 需保留 `targetBaseIndex=0`、`targetType='minion'`、`targetMinionUid='enemy-target'`，真实产生 `MINION_RETURNED(reason='alien_beam_up')` 后，再经 `postProcessSystemEvents(...)` 触发 `base_enchanted_glade` 抽到 `draw-a`。首轮失败已确认不是 runtime 红灯，只是测试自己漏走了 `postProcessSystemEvents(...)`，因此此前只看到回手、没看到基地抽牌。验证：`reactionQueueActionPlayedExternalDiscard.test.ts -t "Cream Puff Man 从弃牌堆额外打出需要随从目标的标准行动时，应保留目标上下文并触发 base_enchanted_glade"` `1 passed`；`reactionQueueActionPlayedExternalDiscard.test.ts` 整文件 `2 passed`；`git diff --check -- src/games/smashup/__tests__/reactionQueueActionPlayedExternalDiscard.test.ts` 通过。该格仍只闭合 Cream Puff Man external discard play 的 `base-target + minion-target` 标准行动分支，不外推所有 external discard replay、所有 Titan special 或整个 `ACTION_PLAYED` family 已完成。
  - `explicit effectContract source-base read ordering` 新收掉普通 base ability / extended base ability 与普通 queued trigger 不对称的真缝：显式 `effectContract` 写入 queued base trigger 的 `derivedFootprint` 后，ordering 会直接使用这份 footprint，跳过 probe 默认 source reads；同帧另一条 trigger 替换该 source base 时，系统看不到 `base:baseIndex` 读写冲突。现已让 `collectBaseAbilityTriggers(...)` 与 `collectExtendedBaseAbilityTriggers(...)` 克隆显式 `effectContract` 时保留原 reads/writes，并补 `{ kind:'base', index: baseIndex }` 读取。验证：focused `source base 读取` 红灯后 `2 passed`；`reactionQueueBaseAbilities.test.ts` 整文件 `24 passed`；`reactionQueueOrdering.test.ts` 显式 contract/source 子集 `2 passed`。
  - `explicit effectContract source-card read ordering` 新收掉一条显式 contract 与 runtime probe 的非对称真缝：普通 probe 推导会自动补触发源本体读取，但显式 `effectContract` 写入 `TriggerInstance.derivedFootprint` 后，`reactionOrdering.resolveTriggerFootprint()` 会直接使用这份 footprint，跳过 probe 默认 source reads；同帧另一条 trigger 移除该 source card 时，ordering 看不到冲突并可能自动收口。现已让 `collectTriggers()` 克隆显式 `effectContract` 时保留 `sourceInstance(uid)`、`cardInstance(uid)` 与 `playerControl(sourcePlayerId)` 读取。验证：focused `trigger 的显式 effectContract 仍应保留 source card` 首轮红灯后 `1 passed`；`reactionQueueOrdering.test.ts` 整文件 `38 passed`；`ongoingEffects.test.ts` 整文件 `39 passed`。
  - `ACTION_PLAYED side branch / triggered Vampire POD target context` 新收掉一条触发打出行动的 target metadata 真缝：`vampire_mad_monster_party_pod` 文本要求“在你消灭一个随从后打出，选择其所在基地”，但 runtime prompt 旧实现发出的 `ACTION_PLAYED` 只有 `playerId/cardUid/defId/isExtraAction`，没有保留被消灭随从所在 `baseIndex`，会让依赖行动目标基地的 `onActionPlayed` collector 看不到目标。现已让该 prompt 的 `buildActionPlayedEvent(...)` 写入 `targetBaseIndex: context.baseIndex`；同时给 `vampire_buffet_pod` 补相反绿色门禁，确认它“抽两张牌，消灭后打出”不应伪造基地目标。验证：focused `vampire_mad_monster_party_pod triggered play preserves` 首轮红灯后 `1 passed`；`reactionQueueDestroyerId.test.ts` 整文件 `4 passed`；`vampiresPod.test.ts` 整文件 `11 passed`；`reactionQueueBaseAbilities.test.ts -t "onActionPlayed|ACTION_PLAYED|base_enchanted_glade"` `3 passed`。
  - `reactionSession / response-window play_action target context` 已把 `ReactionChoiceValue.play_action` 不携带 `targetMinionUid` 的候选疑点降成 dedicated green/data-contract gate：当前 `smashup_reaction_choose` 只枚举 response-window action，数据层没有任何可响应行动同时要求打出时随从目标；普通 `playNeedsMinion` 行动不会进入响应窗口。新增 `reactionSessionResponseActionTargetContext.test.ts` 锁定“response-window action 不得要求 `targetMinionUid`，直到 reaction choice 合同显式扩展”。验证：新文件 `1 passed`；邻近 `response-window-skip.test.ts` 两条 play_action/activePlayer gate `2 passed`。`abilityBehaviorAudit.test.ts` 整文件仍有既有迁移/POD 红项，不作为本格失败证据。
  - `queued-trigger-runtime-context / reaction-trigger-choice / buried ongoing ACTION_PLAYED target context` 又收掉一条 post-process side branch 真缝：`bury.ts` 的 `executeUncoveredAction()` 旧实现先手写无目标 `ACTION_PLAYED`，再解析 buried ongoing 的附着基地/随从目标，导致 `base_enchanted_glade.onActionPlayed` 这类依赖 target metadata 的基地能力看不到目标。现已让翻开 ongoing 先解析目标，再统一用 `buildActionPlayedEvent(...)` 写入 `targetBaseIndex/targetType/targetMinionUid`；多目标二段 prompt 路径继续在目标选择后发事件。验证：focused `buried ongoing action should preserve target context` 首轮红灯后 `1 passed`；`buryEngine.test.ts` 整文件 `11 passed`；`reactionQueueBaseAbilities.test.ts` 整文件 `22 passed`；`smashup.smoke.test.ts -t "buried|埋葬|sphinx"` `7 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / duel ACTION_PLAYED target context` 新收掉一条 post-process side branch 真缝：决斗中打出行动会进入 `ACTION_PLAYED` 后处理，但 `duel.ts` 旧手写事件没有携带 `targetBaseIndex/targetType/targetMinionUid`，导致像 `base_enchanted_glade.onActionPlayed` 这类依赖行动目标基地/随从的 queued base ability 不入队。现已让决斗标准行动把 `duel.baseIndex` 写入 `ACTION_PLAYED`，决斗持续行动把玩家选择的基地/随从目标写入事件。验证：focused `决斗中打出附着到随从的行动` 首轮红灯后 `1 passed`；`reactionQueueActionPlayedDuel.test.ts + reactionQueueBaseAbilities.test.ts` `23 passed`；`smashup.smoke.test.ts -t "决斗|duel|Duel|cowboys_sheriff|pecos_bill"` `7 passed`。
  - `processReturnToHandTriggers / MINION_RETURNED / attached source leaves play` 新收掉一条 `world_champs_bewitched` sibling 真缝：`CARD_TRANSFERRED` 宿主回手已能为附着行动补 explicit source，但 `MINION_RETURNED` 宿主回手分支只排普通 `onCardReturnedToHand`，没有把宿主 LKI 上的 attached action source 带入 queue，导致 `world_champs_bewitched` 不入队。现已让 `MINION_RETURNED` 分支先从事件前现场提取 returned minion LKI，再按每张 attached action 以 `sourceDefIds:[attachedAction.defId]` 定向收集。验证：focused `MINION_RETURNED 真链回手宿主` 首轮红灯后 `1 passed`；`reactionQueueOnCardReturnedToHand.test.ts` 整文件 `13 passed`；`reactionQueueSourceRuntimeContext.test.ts -t "world_champs_bewitched|MINION_RETURNED 真链回手宿主"` `2 passed`；`newFactionAbilities.test.ts -t "world_champs_bewitched"` `4 passed`；`reactionQueueSourceRuntimeContext.test.ts` 整文件 `23 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected sourceController ownership` 继续收掉一条 Titans owner/context 真缝：`frankenstein_the_bride` 的抽牌语义按被加指示物随从的控制者结算，但旧注册没写 `playerContext`，queued owner 会落到加指示物事件玩家。现已给 `frankenstein_the_bride.onMinionAffected` 补 `canTrigger: canTriggerTheBrideOnPowerCounterChanged` 与 `playerContext:'sourceController'`。验证：focused `The Bride 的抽牌|The Bride 的起始` 首轮红灯后 `2 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `96 passed`；`smashup.smoke.test.ts -t "The Bride|新娘|身体改造"` `3 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected collector eligibility` 继续收掉一条 Tricksters 空 trigger 真缝：`trickster_brownie` 只应在 Brownie 本体被对手影响且对手有手牌时入队；旧注册没有 collector 级 `canTrigger`，同基地其它随从被影响时也会先排 `trickster_brownie` trigger，callback 才回空。现已给注册补 `canTrigger: canTriggerTricksterBrownieAffected` 与 `playerContext:'sourceController'`。验证：focused `Brownie 本体` 首轮红灯后 `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `95 passed`；`baseFactionOngoing.test.ts -t "trickster_brownie|Brownie|布朗尼"` `13 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected collector eligibility` 新收掉一条 Vampires POD 空 trigger 真缝：`vampire_dinner_date_pod` 只应在被附着宿主有效力量变为 0 时入队并消灭宿主；旧注册没有 collector 级 `canTrigger`，宿主仍大于 0 时也会先排 mandatory queued trigger，callback 才回空，且 owner 默认落到事件玩家。现已给注册补 `canTrigger: canTriggerVampireDinnerDatePod` 与 `playerContext:'sourceController'`。验证：focused `Dinner Date POD` 首轮红灯后 `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `94 passed`；`vampiresPod.test.ts` 整文件 `11 passed`。
  - `expansionBaseAbilities.test.ts / queued reaction interaction location` 新收掉一条测试口径漂移：`base_innsmouth_base_choose_player` 与 `base_greenhouse` 的 queued reaction 用例旧断言只看 `state.sys.interaction.queue[0]`，但当前 `queueInteraction` 合同是没有 current 时新交互直接成为 current。现已改用 `getInteractionsFromMS(...)` 同时检查 current + queue；验证：`expansionBaseAbilities.test.ts` 整文件 `50 passed`。这格不改 runtime，只消掉错误红态。
  - `processMoveTriggers / queued extended base ability / The Pasture first-move count` 新收掉一条 queued frame 计数真缝：`base_the_pasture` 旧实现按“移动前现场”判断 `moveCount === 0` 为首次移动，但当前 `processMoveTriggers()` 已先 reduce 本次 `MINION_MOVED`，再收集 queued extended base trigger；queued executor 消费时 `minionsMovedToBaseThisTurn[player][base]` 已包含本次移动。现已让 `base_the_pasture` 识别 `sourceEventId` 以 `minion-moved:` 开头的 queued move frame，queued frame 下 `moveCount === 1` 仍视为首次移动，direct caller 仍按 `moveCount === 0` 判定。验证：focused `The Pasture` 本轮复跑 `1 passed`；`reactionQueueBaseAbilities.test.ts` 整文件 `22 passed`；`expansionBaseAbilities.test.ts -t "base_the_pasture"` `1 passed`；`expansionBaseAbilities.test.ts` 整文件修正旧位置断言后 `50 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / buried action -> onActionPlayed base ability parity` 新收掉一条 post-process side branch 真缝：普通 `ACTION_PLAYED` 已走 `postProcessSystemEvents() -> collectBaseAbilityTriggers(...) + collectTriggers(...)`，但 buried action 翻开路径在 `bury.ts` 里额外直接 `triggerBaseAbility(onActionPlayed)`，绕过 base ability `canTrigger/ownerPlayerId/ordering` queue 合同。现已移除 `executeUncoveredAction()` 与 `handleUncoverOngoingPickTargetMinion()` 中的直接基地能力执行，让 buried action 也只通过 `ACTION_PLAYED` 后处理统一入队。验证：focused `uncovered buried action should respect queued base onActionPlayed canTrigger` 首轮红灯后 `1 passed`；`buryEngine.test.ts` 整文件 => `10 passed`；`reactionQueueBaseAbilities.test.ts` 整文件 => `21 passed`；埋葬/Sphinx smoke 子集 => `7 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / baseAbilityQueue options context / frame provenance` 新收掉一条 shared contract 真缝：`collectBaseAbilityTriggers(...)` 与 `collectExtendedBaseAbilityTriggers(...)` 已把 `frameId/sourceEventId` 写入 queued `TriggerInstance`，executor 阶段也能拿到这些字段，但 options 阶段的 `canTrigger/deriveFootprint` context 没拿到同一份 runtime provenance；普通 collector 还漏传 `destroyerId/controllerId/reason` 给 options context。现已补齐普通 options context 的 `destroyerId/controllerId/reason/frameId/sourceEventId`，以及 extended options context 的 `frameId/sourceEventId`。验证：focused `canTrigger/deriveFootprint 应看到 frameId/sourceEventId` 首轮红灯后 `2 passed`；`reactionQueueBaseAbilities.test.ts` 整文件 => `21 passed`；`reactionQueueOnBaseRevealed.test.ts` => `5 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / fireTriggerForSource source selection / canTrigger eligibility` 同族继续收掉单 source 直接入口真缝：`fireTriggerForSource(...)` 的本地 source selection 也未纳入 `canTrigger`，导致显式 sourceDefId 直接触发时仍会选中不合格的第一个同名 source。现已让该入口的 per-instance 与 non-perInstance source 列表也复用 `canTriggerWithSource(...)`。验证：focused `fireTriggerForSource non-perInstance source selection` 首轮红灯后 `1 passed`；`ongoingEffects.test.ts` 整文件 => `39 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `93 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / direct fireTriggers source selection / canTrigger eligibility` 同族继续收掉 direct fire path 真缝：`fireTriggers(...)` 的非 `perInstance` source selection 仍只按 `isTriggerSourceEligible` 与 turn-boundary owner 过滤，没纳入 `canTrigger`，导致直接触发路径会选中不合格的第一个同名 source。现已让 direct fire path 的 per-instance 与 non-perInstance source 列表也复用 `canTriggerWithSource(...)`。验证：focused `fireTriggers non-perInstance source selection` 首轮红灯后 `1 passed`；`ongoingEffects.test.ts` 整文件 => `38 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `93 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / non-perInstance source selection / canTrigger eligibility` 新收掉一条 shared collector 真缝：非 `perInstance` trigger 存在多个同名 source 时，旧 `selectSpecificSourceLocation(...)` 会先选第一个 source，再在后续 `canTrigger` 复核阶段丢弃；若第一个 source 不合格、后面的同名 source 合格，整条 trigger 会被误过滤。现已把 `entry.canTrigger(buildTriggerEligibilityContext(...))` 纳入 non-perInstance source selection 的 `isEligible` 谓词，让 source selection 阶段跳过不合格 source。验证：focused `non-perInstance source selection` => `1 passed`；`ongoingEffects.test.ts` 整文件 => `37 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `93 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉 `kaiju_gorgodzolla` 异基地空 trigger 真缝：Gorgodzolla 只应在其所在基地有随从打出后加 1 个指示物；旧注册没有 collector 级 `canTrigger`，Titan 在基地 0 时，基地 1 打随从仍会排出 `kaiju_gorgodzolla` queued trigger，callback 才回空。现已在注册侧复用 `getGorgodzollaOnBase(ctx.state, ctx.sourceControllerId ?? ctx.playerId, ctx.baseIndex)` 做入队前过滤。验证：focused `Gorgodzolla 所在基地|Gorgodzolla 在对手打随从` 红灯后 `2 passed`；邻近 `smashup.smoke.test.ts -t "哥佐拉"` => `4 passed`；合并 onMinionPlayed collector 组 => `10 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `92 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉 `wizard_archmage` 空 trigger 真缝：Archmage 只应在“打出的大法师本体”进入基地后给予当回合额外行动；旧注册没有 collector 级 `canTrigger`，场上已有大法师时普通随从进场也会先排出 `wizard_archmage` queued trigger，callback 才回空。现已在 `wizard_archmage.onMinionPlayed` 注册侧增加 `canTrigger: triggerMinionDefId === 'wizard_archmage'`。验证：focused `Archmage` 首轮红灯后 `1 passed`；邻近 `baseFactionOngoing.test.ts -t "wizard_archmage|大法师"` => `4 passed`；合并 `owner/手牌|controller/力量|Brownie POD|Archmage` => `8 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `91 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉 `trickster_brownie_pod` 空 trigger 真缝：Brownie POD 只应在其他玩家把随从打到另一基地、Brownie POD 本回合未触发且其控制者仍有可抽牌来源时入队；旧注册没有 collector 级 `canTrigger`，P0 自己在另一基地打随从、P1 在同基地打随从、已触发或无牌可抽时都可能先排出无效果 trigger。现已抽出 `canTriggerTricksterBrowniePod()` 并挂到 `trickster_brownie_pod.onMinionPlayed`。验证：focused `Brownie POD` 首轮红灯后 `1 passed`；合并 `owner/手牌|controller/力量|Brownie POD` => `7 passed`；邻近 `baseFactionOngoing.test.ts -t "Brownie POD|brownie_pod|布朗尼 POD"` => `4 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `90 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一组 Tricksters ongoing/minion 空 trigger 真缝：`trickster_flame_trap`、`trickster_flame_trap_pod`、`trickster_pay_the_piper`、`trickster_pay_the_piper_pod` 只应在其他玩家把随从打到同基地时入队，其中 Pay the Piper 还要求该行动玩家仍有可弃手牌；`trickster_leprechaun` / POD 只应在其他玩家打出的随从战力低于 Leprechaun 且 POD 未在本回合触发时入队。上一轮尝试把 `canTrigger` 错挂到 `trickster_brownie` / `trickster_brownie_pod` / `trickster_gremlin_pod` 注册块，导致 `flame_trap`、`flame_trap_pod`、`pay_the_piper_pod` 仍红。现已把三个错位 `canTrigger` 移回对应 `onMinionPlayed` 注册，并保留 `leprechaun` / `pay_the_piper` 已转绿的过滤。验证：focused `owner/手牌|controller/力量` => `6 passed`；邻近 `baseFactionOngoing.test.ts -t "trickster_flame_trap|trickster_pay_the_piper|trickster_leprechaun"` => `5 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `89 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 Tricksters titan 空 trigger 真缝：`tricksters_big_funny_giant` 语义是“其他玩家把随从打到 Big Funny Giant 所在基地后，若该玩家还有可弃手牌，则弃 1 张剩余手牌”，旧注册没有 collector 级 `canTrigger`，控制者自己打随从时也会先排出 `tricksters_big_funny_giant` queued trigger，callback 才回空。现已抽出 `findEligibleBigFunnyGiantMinionPlay()` 并让 callback 与注册侧 `canTrigger` 复用同一 eligibility，同时过滤对手无剩余可弃手牌场景。验证：focused `Big Funny Giant` 首轮红灯后 `2 passed`；Big Funny Giant smoke `3 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 Itty Critters titan 空 trigger 真缝：`itty_critters_rainboroc` 语义是“每回合第一次你在这里打出战力 2 或更低的随从后，Rainboroc 获得 1 枚力量指示物”，旧注册没有 collector 级 `canTrigger`，对手把低战力随从打到 Rainboroc 所在基地时仍会排出 `itty_critters_rainboroc` queued trigger，callback 才因 titan controller / minion controller 不匹配回空。现已抽出 `findEligibleRainborocTitanForMinionPlayed()` 并让 callback 与注册侧 `canTrigger` 复用同一 eligibility。验证：focused `Rainboroc` 首轮红灯后 `1 passed`；Rainboroc onMinionPlayed smoke `1 passed`；Rainboroc afterScoring source context 邻近 `1 passed`。同轮 `skeletons_returned_one` 的“非 Returned One 本体被打出”候选补成 direct green gate，未复现红口。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 Frankenstein attached-action 空 trigger 真缝：`frankenstein_german_engineering` 语义是“你在该基地打出随从后，给该随从 +1 指示物”，旧注册缺 `sourceScope:'triggerBase'` 与 collector 级 `canTrigger`，导致对手打到挂有 German Engineering 的基地时也会先排一个无效果 queued trigger，callback/DSL `when` 才回空。现已抽出 `isFrankensteinGermanEngineeringEligible()` 并让 DSL `when` 与注册侧 `canTrigger` 复用同一 eligibility，collector 只在本次事件玩家就是 German Engineering owner 且打到同基地时入队。验证：focused `German Engineering` 首轮红灯后 `1 passed`；`newFactionAbilities.test.ts -t "german_engineering|German Engineering|德国工程"` `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `80 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 Cthulhu ongoing action 空 trigger 真缝：`cthulhu_altar` 语义是“你在该基地打出随从后，可额外打出 1 个行动”，旧注册没有 `sourceScope:'triggerBase'` 与 `canTrigger`，对手打到祭坛基地时仍会排出 `cthulhu_altar` queued trigger，callback 才因 `ongoing.ownerId !== ctx.playerId` 回空。现已抽出 `isCthulhuAltarEligibleForMinionPlayed()` 并让 callback 与注册侧复用，collector 只在同基地、祭坛 owner 等于本次打随从玩家、POD 版未在本回合用过时入队。验证：focused `Cthulhu Altar` 首轮红灯后 `1 passed`；`newOngoingAbilities.test.ts -t "cthulhu_altar"` `2 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `79 passed`。`ongoingE2E.test.ts -t "cthulhu_altar"` 本轮全跳过，不作为证据。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 World Champs attached-action 空 trigger 真缝：`world_champs_smart_set_up` 语义是“每回合第一次有随从打到附着基地后抽 1 张”，旧注册没有 `canTrigger`，第二次及以后有随从打到同一基地时仍会排 `world_champs_smart_set_up` trigger，执行时才因 `minionsPlayedPerBase` 回空。现已把同一 eligibility 抽成 `canTriggerWorldChampsSmartSetUp()` 并复用于 callback 与注册侧 `canTrigger`，让非首次打到该基地时入队前过滤。验证：focused `Smart Set-Up` 先红后绿 `1 passed`；既有正向 `world_champs_smart_set_up` `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `78 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionPlayed collector eligibility` 又收掉一条 Cowboys ongoing action 空 trigger 真缝：`cowboys_gold_strike` 语义是“你在该基地打出随从后抽 1 张”，旧注册只靠 callback 内部判断 `ctx.playerId === ctx.sourceControllerId`，导致对手在该基地打随从时 collector 仍会先排一个 mandatory trigger，执行时才回空。现已在 `cowboys_gold_strike.onMinionPlayed` 注册侧补 `canTrigger: ctx => ctx.sourceControllerId === ctx.playerId`，让对手打随从时不再入队；自己打随从仍正常入队并抽牌。验证：focused `Gold Strike|cowboys_gold_strike` `2 files passed, 3 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `77 passed`。同轮 `newFactionAbilities.test.ts` 整文件有 2 条 Giant Ants 旧红灯，未作为本格收口证据。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance source isolation` 新收掉一条 Sharks sibling 真缝：`sharks_hammerhead` 与附着行动 `sharks_chum` 同时在同一基地作为 mandatory `onMinionDestroyed` source 时，旧 `sharksDestroyedCounterTrigger(ctx)` 会在每个 queued source 执行时扫描整座基地，导致每个 source 都重复给全部 Hammerhead/Chum 宿主加 counter。现已让该 trigger 在有 `ctx.sourceCardUid` 时只结算当前 source：Hammerhead 只给自身 +1，Chum 只给其宿主 +1；无 `sourceCardUid` 的 direct fallback 仍保留旧扫描语义。验证：focused `queued onMinionDestroyed 多个 Sharks counter source 同时在场时，每个 source 只能结算自身一次` `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `71 passed`；`shayuFactionAbilities.test.ts` 整文件 `17 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance source isolation` 又收掉一条 Vampires POD sibling 真缝：`vampire_the_count_pod` 旧注册未标 `perInstance` 且 callback 扫全场 Count POD，导致多个不同控制者 Count POD 同时在场时只会入队一个 source，且 owner 默认落到事件玩家。现已给该 trigger 补 `perInstance:true, playerContext:'sourceController'`，并让有 `ctx.sourceCardUid/sourceControllerId` 的 queued 路径只为当前 source controller 创建一次 `vampire_the_count_pod_add_counter` prompt；无 source provenance 的 direct fallback 保留旧扫描语义。验证：focused `The Count POD` `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `72 passed`；`vampiresPod.test.ts` 整文件 `11 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / global onMinionDestroyed source selection` 继续收掉三张全局 Vampire POD 的 destroyer source 真缝：`vampire_buffet_pod`、`vampire_mad_monster_party_pod`、`vampire_fledgling_vampire_pod` 旧 global source selection 会在 P0/P1 都有同名 POD source 时先选 P0 source，即使真正 destroyer 是 P1，导致 owner/sourceController 错落。现已让 `selectGlobalTriggerSourceLocation(...)` 接受 `isEligible` 过滤，并在 global collector/fallback 中用 trigger `canTrigger(buildTriggerEligibilityContext(...))` 预筛；三张 Vampire POD 注册为 `playerContext:'sourceController'` 并要求 `sourceControllerId === destroyerId`。验证：focused `全局 Vampire POD` `3 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `75 passed`；`vampiresPod.test.ts` 整文件 `11 passed`；`ongoingEffects.test.ts` 整文件 `37 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance ongoing source isolation` 又收掉一条 Sharks sibling 真缝：`sharks_blood_in_the_water` 虽然注册成 `perInstance` source，但旧 `sharksBloodInTheWaterTrigger(ctx)` 真正执行时仍扫描整座基地的所有同名 ongoing action；因此 queue 中只结算 `blood-p0` 这一条 source 时，也会顺手给同基地另一位玩家的 `blood-p1` 一起发 extra minion quota。现已让该 trigger 在带 `ctx.sourceCardUid` 的 queued 路径下只结算当前 source 对应的 ongoing action；没有 `sourceCardUid` 的 direct fallback 仍保留旧扫描语义。验证：focused `queued onMinionDestroyed 的单个 sharks_blood_in_the_water source 不应重复结算同基地其他玩家的同名行动` 首轮红灯实际给出 `['0','1']` 两个 `LIMIT_MODIFIED.playerId`，修复后只剩 `['0']`；与 `Hammerhead/Chum/Mako` 同组 focused 子集 `3 passed`。这格当前只闭合 `Blood in the Water` 在 queued per-instance source 下不再越权结算同基地其他玩家的同名 ongoing action；首轮落账时 `reactionQueueEventPlayerContext.test.ts` 与 `shayuFactionAbilities.test.ts` 整文件确实还被独立的 `reduce.ts:2112 minion.controller` 共享红口污染，但该 shared seam 已在本轮后续 `MINION_DESTROYED controller provenance payload gap` 一格中收口，整文件现已分别回到 `101 passed` 与 `17 passed`。
  - `queued-trigger-runtime-context / reaction-trigger-choice / global onMinionDestroyed source selection` 又收掉同型 Sharks sibling 真缝：`sharks_mako` 也是“你消灭随从后”从手牌触发的 global source，旧注册未写 `playerContext/canTrigger`，P0/P1 都有 Mako 时会把 P1 destroyer 的 trigger 错选成 P0 的 hand source。现已给 `sharks_mako` 注册补 `playerContext:'sourceController'` 与 `canTrigger: sourceIsDestroyer`，复用 global source eligibility 过滤。验证：focused `Sharks Mako` 首轮红灯为 `['mako-p0','0','0']`，修复后 `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 `76 passed`；`shayuFactionAbilities.test.ts` 整文件 `17 passed`。
  - `registerTrigger(..., timing) -> production entry static diff` 已完成一轮复核：生产目录下当前所有已注册 timing 都至少有 `collectTriggers()` 或 `fireTriggers()` 入口。`onDuelStarted` 没有 queued collector 是合法特例，因为 `startDuel()` 直接 `fireTriggers(core, 'onDuelStarted', ...)` 并把 `matchState` prompt 应用回去；复跑 Pecos Bill 三条 duel smoke 用例 `1 file passed, 3 passed`。这说明 `onTitanRemovedFromPlay` 之后暂未发现新的“注册 timing 完全无人消费”缺口，下一格应转去“有入口但 player provenance / stale core / side branch 不对称”的 still-object/shared family。
  - `post-process family / TITAN_REMOVED_FROM_PLAY -> onTitanRemovedFromPlay / Killer Kudzu` 新收掉一条生产收集缺口：`registerTrigger(..., 'onTitanRemovedFromPlay')` 已存在，但 `postProcessSystemEvents()` 旧的 titan 后处理循环只处理 `TITAN_PLAYED / TITAN_MOVED`，不会对 `TITAN_REMOVED_FROM_PLAY` 调 `collectTriggers()`，导致 `killer_plants_killer_kudzu` 离场后的可选回收/抽牌选择在生产真链上完全不入队。现已在 `domain/index.ts` 增加 `_processedTitanRemovedEvents` 去重并对 `TITAN_REMOVED_FROM_PLAY` 收集 `onTitanRemovedFromPlay`，同时给 `killer_plants_killer_kudzu.onTitanRemovedFromPlay` 补 `playerContext:'sourceController'`，确保当前事件玩家不是泰坦拥有者时，后续 prompt 仍归泰坦控制者/拥有者。验证：新增 `reactionQueueTitanRemovedFromPlay.test.ts` focused `1 passed`；邻近 `reactionQueueEventPlayerContext.test.ts -t "Killer Kudzu"` `1 passed`；`smashup.smoke.test.ts` 泰坦 clash/移动邻近 `4 passed`；eslint 0 errors（28 warnings 为既有 any/未用类警告）。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / scoring-frame live validation + Jumper-like recover` 新收掉一条会让普通基地计分后 optional recover 卡死的真缝：`scoreOneBase()` 会先创建 `onMinionDiscardedFromBase` optional frame，随后又 push `afterScoring` mandatory frame；旧 `smashup_reaction_choose` 的 live validation 每次取 active frame，导致当前 Jumper prompt 用后来的 afterScoring frame 校验，响应被判“无效的选择”。现已让 `reactionSession.ts` 的 `optionsGenerator` 按创建 prompt 时的 `session.frameId` 取 session；同时 `Jumper / Copycat copied Jumper` 在计分清场延迟前若仍在基地上，会从基地直接回 owner hand，真正进 discard 时仍走 `CARD_RECOVERED_FROM_DISCARD`；`Temple of Goju` 会放底的最高力量随从不再提前排 recover trigger。聚焦验证：`yuanhouFactionAbilities.test.ts` 6 条 Jumper/Copycat/Temple 分支全绿，`baseScoring.test.ts -t "Sleeping Beauty"` 1 条邻近全绿。
  - `scoreOneBase / onMinionDiscardedFromBase / attached-action + discard self-trigger direct gates` 已补 dedicated green gate：Samurai scoring-discard sibling 覆盖 `Samurai Chan / Samurai-Chan POD / Bushi / Bushi POD / Shogun / Shogun POD / Final Haiku / Final Haiku POD / Way of the Warrior / Way of the Warrior POD / Honor the Fallen / Honor the Fallen POD / Sakura Garden`；World Champs self-source sibling `world_champs_samurai_chan` 已确认自身计分弃牌后给控制者抽 1 张；非 Samurai attached-action sibling `vikings_viking_funeral` 已确认在宿主计分弃牌后给 1VP 并移出该宿主；`world_champs_bewitched` 已确认在普通宿主计分弃牌后给行动拥有者打开转移附着 prompt，候选包含其它基地随从且排除原宿主；discard-zone self-trigger sibling `trickster_gremlin_pod` 已确认在 owner/controller 分离的计分弃牌入口下给拥有者抽牌；`giant_ant_worker_pod` 已确认在计分弃牌入口下打开 replay prompt，候选基地不含原计分基地；`frankenstein_igor` 已确认在计分弃牌入口下把放置指示物选择权交给控制者，候选不含 Igor 自身；`giant_ants_death_on_six_legs` 已确认在己方随从计分弃牌后给场上泰坦加 1 个指示物；`shapeshifters_doppelganger` 已确认在普通基地计分弃牌入口下给离场随从控制者/事件玩家打开 deck-minion search prompt，候选只来自其牌库快照。新增 `baseScoring.test.ts -t "Doppelganger|Way of the Warrior POD|Honor the Fallen POD|Shogun POD|Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi"`，`19 passed`；并复验既有 `Samurai-Chan POD|武士 POD` => `3 passed`，补齐 evidence 落账缺口。
  - `scoreOneBase / onMinionDiscardedFromBase / Samurai Honor the Fallen + Sakura Garden` 已补 dedicated green gate：`samurai_honor_the_fallen` 与 `base_sakura_garden` 都依赖离场随从 LKI，理论风险是计分清场/后续 frame 交错时丢掉 `ctx.triggerMinion`。新增 `baseScoring.test.ts` 真入口覆盖普通基地计分后 `Honor the Fallen` 抽到 `honor-draw-a`、`Sakura Garden` 抽到 `sakura-draw-a`，focused 均 `1 passed`。这两条候选降级为已验证邻近，不再作为当前红口。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / discard-zone self-trigger` 新收掉 `trickster_gremlin_pod` 一条真红灯：旧注册没有声明 `global/globalZones:['discard']`，导致 direct queued collector 在离场随从已进 discard、场上没有 live source 时完全找不到来源，`queued.payload.triggers[0].sourceDefId` 为 `undefined`。现已对齐 `Sleeping Beauty / Doppelganger` 同族合同，给 `trickster_gremlin_pod.onMinionDiscardedFromBase` 补 `global: true, globalZones:['discard']`，并保留 callback 按 `ctx.triggerMinion.owner` 给离场随从拥有者抽牌。同轮 `onDestroyAbilities.test.ts` 整文件又暴露 `processDestroyTriggers()` 在链式 destroy 后续 discard 分支裸读 `minion.controller` 的崩溃，现已改成 `minion?.controller ?? ownerId` 并把 `controllerId` 传给 collector；`reactionQueueEventPlayerContext.test.ts` 整文件 `70 passed`，`onDestroyAbilities.test.ts` 整文件 `14 passed`。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / attached-action LKI` 又补齐了一条新的 stale-context 真缝：`reactionSession.ts` 旧实现虽然会把 `TriggerInstance.lkiMinion.attachedActionDefIds` 带进队列，但 queued trigger 真正执行时重建出来的 `triggerMinion.attachedActions` 仍是空数组；于是像 `vikings_viking_funeral` 这种在 `onMinionDiscardedFromBase` 回调里需要确认“这张 attached action 当时确实贴在这只宿主上”的效果，一旦宿主已经离场，就会回头查当前基地 live state 而不是用 LKI，最终直接回空。现已在 `reactionSession.ts` 把 queued executor 重建的 `triggerMinion.attachedActions` 补成基于 `attachedActionDefIds + current trigger.sourceCardUid/sourceDefId` 的最小 LKI 占位，再把 `vikingsVikingFuneralTrigger()` 改成优先吃 `ctx.triggerMinion.attachedActions`，不再要求宿主在执行时仍留在当前基地。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / attached-action host LKI fallback` 又补齐了一条同型 stale-context 真缝：`samuraiFinalHaikuTrigger()` 旧实现虽然语义上只想确认“Final Haiku 当时确实附着在这只离场宿主上”，但实际仍强依赖 `findAttachedActionHost(ctx.state, ctx.sourceCardUid, ctx.sourceBaseIndex)` 回头查 live host；一旦 queued discard trigger 执行时宿主已经进 discard，就会因为 live base 上再也找不到宿主而直接回空。现已把 `samurai.ts` 收紧成两级判定：优先读取 queued executor 恢复出来的 `ctx.triggerMinion.attachedActions` 判断当前 source 是否当时贴在这只宿主上，只有这份 LKI 缺失时才 fallback 到 `findAttachedActionHost(...)`；同轮新增 `newFactionAbilities.test.ts` focused regression，直接覆盖宿主进 discard 后 `samurai_final_haiku` 仍会给其他己方随从 `+2` 力量。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / triggerMinion controller from LKI` 又补齐了一条 sibling stale-context 真缝：`frankenstein_igor` 旧实现不是 attached-action host 问题，而是 queued discard callback 仍回头查 `ctx.state.bases[ctx.baseIndex].minions.find(ctx.triggerMinionUid)` 里的 live base 来拿 `controller`；一旦宿主已经因为本次 discard 离场，这个 lookup 就会回空，导致本应交给离场随从控制者的目标选择交互直接消失。现已把 `frankenstein.ts` 收紧成“优先读取 queued executor 恢复出来的 `ctx.triggerMinion?.controller`，只有 direct caller 没带 `triggerMinion` 时才 fallback 到 live base lookup”，并在 `newFactionAbilities.test.ts` 新增 focused regression，直接覆盖宿主进 discard 后 `frankenstein_igor` 仍会给控制者创建目标选择交互。
  - `queued-trigger-runtime-context / onMinionDiscardedFromBase / triggerMinion counters-controller from LKI` 又补齐了一条 sibling stale-context 真缝：`giant_ant_worker_pod` 旧实现直接写死“destroy / scoring discard 两种时机都发生在 reducer 移除随从前，此时仍可从基地读取到真实 counters”，于是 queued discard trigger 真正执行时若宿主已经进 discard，就会因为 live base 上再也找不到这只随从而直接回空；结果本应给控制者的“从弃牌堆把它打到另一基地”交互彻底消失。现已把 `giant_ants.ts` 收紧成“优先读取 queued executor 恢复出来的 `ctx.triggerMinion` 来判断 `controller/powerCounters`，只有 direct caller 没带 `triggerMinion` 时才 fallback 到 live base lookup”，并在 `newFactionAbilities.test.ts` 新增 focused regression，直接覆盖宿主进 discard 后 `giant_ant_worker_pod` 仍会弹出 replay prompt。
  - `queued-trigger-runtime-context / reaction-trigger-choice` 又补齐一组 buried family 的 optional/per-instance owner seam：`skeletons_lord_of_bones` 与 `skeletons_gravestones` 的 `onBuriedCardUncovered` callback 本来就都按 `ctx.sourceControllerId ?? ctx.playerId` 构造后续交互，但注册侧旧实现没显式写 `playerContext:'sourceController'`，导致对手翻开埋葬牌时 queued trigger owner 仍会错落到事件玩家。现已在 `skeletons.ts` 给两条 trigger 都补上 `playerContext:'sourceController'`，并用 `reactionQueueSourceRuntimeContext.test.ts` 的两条新 focused regression + `newFactionAbilities.test.ts` 邻近对象级行为矩阵共同坐实这不是 afterScoring 旧格复读。
  - `queued-trigger-runtime-context / reaction-trigger-choice / buried trigger sourceController owner` 又补齐 `skeletons_gravetender` 这条 still-object-level family：两条 trigger 旧注册只有 `perInstance:true`，默认 owner 会落到事件玩家；但 callback 实际按 Gravetender 控制者抽牌并写 once-per-turn metadata。现已给 `onCardBuried` 与 `onBuriedCardUncovered` 都补 `playerContext:'sourceController'`，并新增 focused gate 覆盖“P1 翻开 P0 埋葬牌时，queued owner 仍是 P0，结算后 P0 抽牌且 P1 不抽牌”。
  - `queued-trigger-runtime-context / reaction-trigger-choice / ancient_egyptians_pharaoh uncover owner` 又补齐同族一格：`ancient_egyptians_pharaoh.onBuriedCardUncovered` 旧注册只有 `perInstance:true`，但 callback 直接按 `ctx.sourceControllerId` 给 Pharaoh 控制者抽牌。新增 focused gate 首次红灯为 `ownerPlayerId='1'`，现已给该 trigger 补 `playerContext:'sourceController'`，修复后 P1 翻牌也会让 P0 Pharaoh 控制者抽到 `pharaoh-draw-a`。
  - `pirate_buccaneer` replacement/live-minion owner 分支已补 dedicated green gate：三基地场景中即使 `owner='1' / controller='0'`，且触发上下文 `playerId='1'` 模拟事件 owner，`pirate_buccaneer_move` prompt 仍真实交给当前控制者 `0`。focused 与同组 Buccaneer 矩阵均通过，说明这里当前是证据缺口，不是需要批量补 `playerContext` 的同型 runtime 红灯。
  - `base_star_portal.onCardBuried` 也已补 dedicated green gate：跨玩家埋牌场景中 `buriedCardControllerId='1'` 会让埋葬者 P1 抽牌，即使 direct trigger context 的 `playerId='0'`。这证明它当前应保持 buried-card-controller/event provenance，不应被批量改成 `sourceController` 语义。
  - `processReturnToHandTriggers()` 的 `CARD_TRANSFERRED -> attached source leaves play` 真链已补齐：当宿主随从从场上回手、其附着行动因此一起离场时，旧 `collectTriggers()` 会在 after-transfer `advancedCore` 里找不到已掉进 discard 的 per-instance source，导致像 `world_champs_bewitched` 这类 `onCardReturnedToHand` trigger 根本不入队。现已在 `ongoingEffects.ts` 增加“显式 `sourceCardUid/sourceControllerId/sourceBaseIndex + sourceDefIds`”的 per-instance fallback，并在 `reducer.ts` 的 attached-action 回手分支只按该 attached action 的 `defId` 定向收集，避免 unrelated trigger 误吃同一 fallback。
  - `base_the_nexus` 的 empty-deck replacement 现在也有 direct gate，且已明确和 `Time Is Fleeting` 不是同一语义：前者会用“所选 replacement base 进场 + 其余弃牌堆与旧基地重建牌库”，后者的 empty-deck 单候选场景则不应把旧基地回灌进 deck。
  - `scoreOneBase()` 的 deferred replacement stale-reshuffle seam 已补齐：当 `Time Is Fleeting / The Nexus` 这类 afterScoring replacement choice 已把基地从 `baseDiscard` 真实移到 `baseDeck` 顶时，`replaceDeferredPostScoringReplacementBase()` 现在会同步删除旧的 `base_deck_empty_reshuffle_discard` deferred 事件，避免刚计分的旧基地被错误回灌进 `baseDeck`。
  - `replaceDeferredPostScoringReplacementBase()` 现已继续补齐同族的 deferredActions 合同：当同一 afterScoring 响应窗把 replacement base 从 `base_secret_garden` 改写为 `base_faceless_city` 时，frame 里已缓存的 `moveMinionToReplacementBase / playMinionOnReplacementBase / playTitanOnReplacementBase` 的 `targetBaseDefId` 也会一起改写，不再让延迟动作继续追旧 replacement base。
  - `queued-trigger-runtime-context` 的 `duel*` 上下文已从 collector eligibility 一路补到 `TriggerInstance -> reactionSession executor`：`onDuelResolved` 现在不再在入队或执行恢复时丢失 `duelSourceId / duelOutcome / duelWinner / duelLoser / duelTie`。
  - `processMoveTriggers()` 的 target-base extended `onMinionMoved` 已从现场直执行改为 queued ordering，并由 `reactionQueueBaseAbilities.test.ts` focused 红灯转绿。
  - `processDestroyTriggers()` 的 confirmed destroy phase2 extended `onMinionDestroyed` 也已从现场直执行改为 queued ordering，并保留 `base_the_field_of_honor / base_crypt` batch once gate；destroy 邻近 focused 与 batch 回归均已转绿。
  - `processAffectTriggers()` 的 extended base `onMinionAffected` 已从“完全漏排 queue”补成与 ongoing 同步入队；同轮还纠正了 focused regression 里误写的 `POWER_COUNTERS_ADDED` 测试夹具，使 `reactionQueueBaseAbilities.test.ts` 整文件回到 `19 passed`。
  - `processDestroyTriggers()` 的 `onMinionDiscardedFromBase` 现已从“先 reduce 到 discard 后再按 `sourceDefIds:[minionDefId]` 收集”改成“只用 discard 判定是否真的入弃牌堆，真正 queue 收集仍按 phase2 live/LKI 现场执行”；focused 与 `samurai_way_of_the_warrior` 邻近都已转绿。
  - `scoreOneBase()` 的 `onBaseRevealed` 现已从“`BASE_REPLACED` 后仍按旧 `core` 收集”改成“先把 `BASE_CLEARED / BASE_DECK_SHUFFLED / BASE_REPLACED` reduce 到 `revealCore`，再按新基地现场收集 reveal queue”；`reactionQueueOnBaseRevealed.test.ts` focused 红灯与 `baseScoring.test.ts` 邻近都已转绿。
  - `world_champs_bewitched_transfer` 这轮又收掉一条新的 borrowed attach provenance 真缝：[`world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `worldChampsBewitchedTransferPromptProgram.onResolve` 在把 `world_champs_bewitched` 从 discard 重新附着到别的随从时，旧 payload 只写 `ownerId`，不会在 borrowed 场景补 `sourcePlayerId`；因此 `Brownie` 这类 `onMinionAffected` consumer 会把真实行动玩家 provenance 洗回 true owner。现已只在该 handler 的 `SU_EVENTS.ONGOING_ATTACHED` payload 上补 `...(context.ownerId !== context.playerId ? { sourcePlayerId: context.playerId } : {})`。验证：focused `newFactionAbilities.test.ts -t "world_champs_bewitched 离场转移 borrowed 持续行动到 Brownie 身上时也应保留 sourcePlayerId"` => `1 file passed, 1 passed`；shared `reactionQueueEventPlayerContext.test.ts -t "processAffectTriggers 处理 borrowed ONGOING_ATTACHED 时，Brownie 应继续把真正打牌玩家视为事件玩家"` => `1 file passed, 1 passed`。同轮复跑 `world_champs_bewitched` 子集时，旧失败 `宿主经 CARD_TRANSFERRED 从场上回手时也应通过 reducer 真链创建转移交互` 已确认只是测试断言仍在找 `returned-own`，实际夹具宿主 UID 是 `host-1`；收紧断言后子集整体 `5 passed`。这格只闭合 `world_champs_bewitched_transfer / borrowed attach -> ONGOING_ATTACHED.sourcePlayerId`，并把该 `CARD_TRANSFERRED` 旧失败降成测试漂移，不外推整个 World Champs 或所有 `onCardReturnedToHand` consumer。
- 当前继续方向：只再找 `post-process family / queued collector / side branch` 的同型残口，例如“ongoing/base 已入队、extended 或 sibling 分支仍直执行”、“先丢 live/LKI source 再 collect”，或“事件已经进入下一现场，但 collector 仍按旧 core/旧 source 收集”的 stale-context 旁路。

## Current Status

- [x] 已继续收掉一条新的 `borrowed ONGOING_ATTACHED provenance / world_champs_bewitched_transfer` 真缝：`world_champs_bewitched` 的计分弃牌、宿主回手等 sibling 先前已有绿证据，但 `worldChampsBewitchedTransferPromptProgram.onResolve` 在把 borrowed `Bewitched` 从 discard 重新附着到新宿主时，旧 `ONGOING_ATTACHED` 仍只带 `ownerId`，不会补 `sourcePlayerId`。新增 focused gate `world_champs_bewitched 离场转移 borrowed 持续行动到 Brownie 身上时也应保留 sourcePlayerId` 首轮直接红灯；现已在 `src/games/smashup/abilities/world_champs.ts` 只补 `...(context.ownerId !== context.playerId ? { sourcePlayerId: context.playerId } : {})`，复跑 focused 与 shared Brownie consumer gate 均转绿。随后复跑 `world_champs_bewitched` 子集时，唯一残余失败也已判明只是测试断言漂移：用例在找 `triggerMinionUid === 'returned-own'`，而当前夹具真实回手宿主是 `host-1`；收紧断言后 `world_champs_bewitched` 子集整体 `5 passed`。这格只闭合 `world_champs_bewitched_transfer / borrowed attach -> ONGOING_ATTACHED.sourcePlayerId`，并把 `CARD_TRANSFERRED` 那条旧失败降成测试漂移，不外推整个 World Champs、所有 `onCardReturnedToHand` consumer 或整个 owner/source 主线。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance source isolation` 真缝：`sharks_hammerhead` 与 `sharks_chum` 同基地同时监听随从被摧毁时，旧 Sharks counter trigger 每次 queued source 执行都会扫描整座基地并重复结算所有 source。现已在 `src/games/smashup/abilities/sharks.ts` 按 `ctx.sourceCardUid` 优先只结算当前 source，并保留无 source uid 的 direct fallback。验证：`reactionQueueEventPlayerContext.test.ts -t "queued onMinionDestroyed 多个 Sharks counter source 同时在场时，每个 source 只能结算自身一次"` => `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `71 passed`；`shayuFactionAbilities.test.ts` 整文件 => `17 passed`。这格只证明 Sharks Hammerhead/Chum 多 source per-instance counter 不再重复结算，不外推整个 Sharks 或全部 destroy family。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance source isolation` 真缝：`vampire_the_count_pod` 旧实现既没有 `perInstance`，又在 callback 里扫描所有 Count POD；多控制者同场时 queue 只会选中一个 source，并把 `ownerPlayerId` 错落到事件玩家。现已在 `src/games/smashup/abilities/vampires.ts` 给该 trigger 补 `perInstance:true` 与 `playerContext:'sourceController'`，queued 路径按当前 `ctx.sourceControllerId` 只创建一次 `vampire_the_count_pod_add_counter` prompt，direct caller 无 source provenance 时仍保留旧扫描 fallback。验证：`reactionQueueEventPlayerContext.test.ts -t "The Count POD"` => `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `72 passed`；`vampiresPod.test.ts -t "The Count POD"` => `2 passed`；`vampiresPod.test.ts` 整文件 => `11 passed`。这格只证明 Count POD 多 source / 多控制者 queued owner 已修，不外推所有 Vampires、所有 optional destroy trigger 或整条 onMinionDestroyed family 已完成。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / global onMinionDestroyed source selection` 真缝：`vampire_buffet_pod`、`vampire_mad_monster_party_pod`、`vampire_fledgling_vampire_pod` 都是“你消灭随从后”才允许从手牌/弃牌堆触发的全局 POD source；旧 `selectGlobalTriggerSourceLocation(...)` 只按 defId 找第一个同名 global source，P0/P1 都有同名牌时会把 P1 destroyer 的触发错归到 P0 source。现已在 `src/games/smashup/domain/ongoingEffects.ts` 给 global source selection 增加 `isEligible` 过滤，并让 collector/fallback 复用 trigger `canTrigger` 的 eligibility；`src/games/smashup/abilities/vampires.ts` 给三张 POD 补 `playerContext:'sourceController'` 与 `canTrigger: sourceIsDestroyer`。验证：`reactionQueueEventPlayerContext.test.ts -t "全局 Vampire POD"` => `3 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `75 passed`；`vampiresPod.test.ts` 整文件 => `11 passed`；`ongoingEffects.test.ts` 整文件 => `37 passed`。这格只证明三张 Vampire POD 的 destroyer source selection 已按 destroyer 本人收紧，不外推所有 global trigger 或全部 destroy family。
- [x] 已继续收掉同型 `global onMinionDestroyed source selection` 真缝：`sharks_mako` 的语义同样是“你消灭一个随从后，可作为额外随从打到那里”，但旧注册只有 `global:true, globalZones:['hand']`，P0/P1 都有 Mako 时会把 P1 destroyer 的 queued trigger 错选成 P0 hand source。新增 focused gate `queued onMinionDestroyed 全局 Sharks Mako 应选择 destroyer 自己的 source` 首轮红灯为 `[sourceCardUid, ownerPlayerId, sourceControllerId] === ['mako-p0','0','0']`；现已在 `src/games/smashup/abilities/sharks.ts` 给 `sharks_mako` 补 `playerContext:'sourceController'` 与 `canTrigger: sourceIsDestroyer`，修复后同一场景选中 `['mako-p1','1','1']`。验证：focused `Sharks Mako` => `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `76 passed`；`shayuFactionAbilities.test.ts` 整文件 => `17 passed`。这格只证明 Mako hand global source 按 destroyer 本人收紧，不外推整个 Sharks 或全部 global destroy trigger。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionDestroyed per-instance ongoing source isolation` 真缝：`sharks_blood_in_the_water` 虽然注册成 `perInstance` source，但旧 `sharksBloodInTheWaterTrigger(ctx)` 真正执行时仍扫描整座基地的所有同名 ongoing action，导致 queue 里只结算 `blood-p0` 这一条 source 时，也会顺手给同基地另一位玩家的 `blood-p1` 一起发 `grantExtraMinion`。现已在 `src/games/smashup/abilities/sharks.ts` 让该 trigger 在带 `ctx.sourceCardUid` 的 queued 路径下只结算当前 source 对应的 ongoing action；没有 `sourceCardUid` 的 direct fallback 仍保留旧扫描语义。focused 红灯 `queued onMinionDestroyed 的单个 sharks_blood_in_the_water source 不应重复结算同基地其他玩家的同名行动` 首轮实际给出了 `['0','1']` 两个 `LIMIT_MODIFIED.playerId`，修复后只剩 `['0']`。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 -t "queued onMinionDestroyed 的单个 sharks_blood_in_the_water source 不应重复结算同基地其他玩家的同名行动|queued onMinionDestroyed 多个 Sharks counter source 同时在场时，每个 source 只能结算自身一次|queued onMinionDestroyed 全局 Sharks Mako 应选择 destroyer 自己的 source"` => `1 file passed, 3 passed`。这格只证明 `Blood in the Water` 在 queued per-instance source 下不再越权结算同基地其他玩家的同名 ongoing action，不外推整个 Sharks、全部 `onMinionDestroyed` ongoing；首轮落账时 `reactionQueueEventPlayerContext/shayuFactionAbilities` 整文件确实都被独立的 `reduce.ts:2112 minion.controller` 共享红口污染，但该 shared seam 已在下一格 `MINION_DESTROYED controller provenance payload gap` 收口，当前整文件基线已分别回到 `101 passed` 与 `17 passed`。
- [x] 已继续收掉一条新的 `shared destroy ledger / MINION_DESTROYED controller provenance payload gap` 真缝：上一拍把 `base_sakura_garden / cthulhu_furthering_the_cause / vampire_the_count / vampire_opportunist / robot_microbot_archive` 都改成按 destroyed minion `controller` 判定后，`reduce.ts:2112` 又暴露出更底层的 shared seam。旧 reducer 在 `MINION_DESTROYED` 归约阶段写 `turnDestroyedMinions` 时只会读 live `minion.controller`；一旦 destroy 事件来自 queued/延迟路径，live minion 已经不在基地上，就会直接崩在 `controller: minion.controller`。现已在 `src/games/smashup/domain/types.ts` 给 `MinionDestroyedEvent.payload` 增量补 `controllerId?: PlayerId`，并让 `src/games/smashup/domain/abilityHelpers.ts` 的 `destroyMinion()/buildValidatedDestroyEvents()` 在 live witness 仍在时把 controller provenance 一起写入 destroy 事件；`src/games/smashup/domain/reduce.ts` 写 ledger 时改为优先吃 `payload.controllerId ?? minion?.controller ?? ownerId`。同时补齐这批已坐实会丢 live minion 的 destroy producer：`killer_plant_sprout / killer_plant_sprout_pod / killer_plant_choking_vines / elder_thing_dunwich_horror / ninja_assassination`。验证：`reactionQueueEventPlayerContext.test.ts -t "Time Box|Sprout|Choking Vines|Ninja Assassination|Dunwich Horror"` => `1 file passed, 7 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `1 file passed, 101 passed`；`shayuFactionAbilities.test.ts` 整文件 => `1 file passed, 17 passed`。这格只闭合 destroy ledger 在 delayed/queued destroy 路径下的 controller provenance payload gap，不外推所有手写 `MINION_DESTROYED` producer 或整个 destroy/discard family 已全量完成。
- [x] 已继续收掉一条新的 `post-process family / TITAN_REMOVED_FROM_PLAY -> onTitanRemovedFromPlay` 真缝：注册层已有 `killer_plants_killer_kudzu.onTitanRemovedFromPlay`，但生产 `postProcessSystemEvents()` 旧循环没有把 `TITAN_REMOVED_FROM_PLAY` 纳入 titan trigger 收集，导致 Killer Kudzu 离场后的可选回收/抽牌 prompt 不会出现。现已在 `src/games/smashup/domain/index.ts` 对 `TITAN_REMOVED_FROM_PLAY` 调 `collectTriggers(titanCore, 'onTitanRemovedFromPlay', ...)`，并传递 `sourceCardUid/sourceControllerId/sourceBaseIndex/frameId/sourceEventId`；`src/games/smashup/abilities/titans.ts` 给 `killer_plants_killer_kudzu.onTitanRemovedFromPlay` 补 `playerContext:'sourceController'`。验证：`reactionQueueTitanRemovedFromPlay.test.ts` focused `1 passed`；`reactionQueueEventPlayerContext.test.ts -t "Killer Kudzu"` `1 passed`；`smashup.smoke.test.ts -t "第二个泰坦进入标准基地|丛林之灵输掉 titan clash|交互解决产生的泰坦移动进入已有其他泰坦|pecos_bill 若在决斗中进入已有泰坦"` `4 passed`；eslint 0 errors，28 warnings 为既有 `any` / unused 类警告；diff check 已通过。这格是真修复注册 timing 未被生产 collector 消费，不外推所有 titan removed / titan clash / post-process family 已全量完成。
- [x] 已完成 `registerTrigger(..., timing)` 与生产入口静态差集复核：所有已注册 timing 当前都有生产 `collectTriggers()` 或 `fireTriggers()` 入口；`onDuelStarted` 经确认是 `duel.ts/startDuel()` 的即时 `fireTriggers` 合法特例，并已复跑 Pecos Bill 三条 duel smoke 用例 `1 file passed, 3 passed`。因此下一格不再继续找“完全无人消费 timing”，而应筛“入口存在但 side branch / player provenance / stale core 仍不对称”的更小候选。
- [x] 已继续收掉 `trickster_gremlin_pod.onMinionDiscardedFromBase` 的 discard-zone self-trigger 入队红灯：新增/现有 focused gate `queued onMinionDiscardedFromBase trigger 仍应把 Gremlin POD 的抽牌归给离场随从拥有者` 首轮失败为 `queued?.payload?.triggers?.[0]?.sourceDefId === undefined`，根因是这类“离场后在弃牌堆触发自己”的来源没有声明 `global: true, globalZones:['discard']`。现已在 `src/games/smashup/abilities/tricksters.ts` 补齐该注册合同，修复后 P1 控制但 P0 拥有的 `Gremlin POD` 在被弃置进 P0 discard 后会入队，executor 通过 LKI `triggerMinion.owner='0'` 给 P0 抽到 `gremlin-draw-a`，P1 不抽牌。同轮 `onDestroyAbilities.test.ts` 整文件暴露 `processDestroyTriggers()` 链式 destroy 后续 discard 分支裸读 `minion.controller`，已在 `src/games/smashup/domain/reducer.ts` 改为 `minion?.controller ?? ownerId` 并传入 `controllerId`，避免没有 live minion 时崩溃。验证：`reactionQueueEventPlayerContext.test.ts -t "Gremlin POD"` => `1 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `70 passed`；`onDestroyAbilities.test.ts -t "trickster_gremlin_pod"` => `1 passed`；`onDestroyAbilities.test.ts` 整文件 => `14 passed`。这格是真修复 `onMinionDiscardedFromBase` 的 self-trigger source 定位与链式 destroy 兜底，不外推所有 Tricksters、所有 discard self-trigger 或整个 destroy/discard family 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / onMinionDiscardedFromBase / attached-action LKI` 真缝：旧 `TriggerInstance` 虽然会在入队时记录 `lkiMinion.attachedActionDefIds`，但 `reactionSession.ts` 真正执行 queued trigger 时重建出的 `triggerMinion.attachedActions` 仍为空；于是像 `vikings_viking_funeral` 这种需要确认“当前 trigger source 当时确实贴在这只离场宿主上”的回调，在宿主进 discard 后会因为回头查 live base 而直接回空。现已只做两处最小修复：1) `src/games/smashup/domain/reactionSession.ts` 在 queued executor 恢复 `triggerMinion` 时，把 `attachedActionDefIds` 重建成最小 attached-action LKI，占位保留当前 `trigger.sourceCardUid/sourceDefId`；2) `src/games/smashup/abilities/vikings.ts` 的 `vikingsVikingFuneralTrigger()` 改成优先吃 `ctx.triggerMinion.attachedActions`，不再要求宿主仍存在于当前基地。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueOnMinionDiscardedFromBase.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "attached-action LKI"` => `1 file passed, 1 passed`；`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "vikings_viking_funeral"` => `1 file passed, 1 passed`；`git diff --check -- src/games/smashup/domain/reactionSession.ts src/games/smashup/abilities/vikings.ts src/games/smashup/__tests__/reactionQueueOnMinionDiscardedFromBase.test.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` => passed（仅 LF/CRLF warning）。这格说明当前不是围着旧 `goal` 文案打转，而是 `onMinionDiscardedFromBase` 的 queued attached-action stale-context seam 又真实收掉一条；但不外推整个 Vikings、所有 attached-action discard trigger，或整条 destroy/discard family 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / onMinionDiscardedFromBase / attached-action host LKI fallback` 真缝：`src/games/smashup/abilities/samurai.ts` 里的 `samuraiFinalHaikuTrigger()` 旧实现虽然只是在核对“当前 queued trigger 的 source 当时是否贴在这只离场宿主上”，但实际仍硬依赖 `findAttachedActionHost(ctx.state, ctx.sourceCardUid, ctx.sourceBaseIndex)` 回头读 live host；一旦宿主已经进 discard，这条回调就会直接回空。现已只做一处最小修复：让 `samuraiFinalHaikuTrigger()` 优先检查 `ctx.triggerMinion.attachedActions` 是否含当前 `sourceCardUid`，只有 queued executor 恢复不到这份 attached-action LKI 时才 fallback 到 `findAttachedActionHost(...)`。同时在 `src/games/smashup/__tests__/newFactionAbilities.test.ts` 新增 focused regression `samurai_final_haiku 在宿主进入弃牌堆后仍会通过 queued discard trigger 给其他己方随从 +2 力量`，并与已转绿的 `vikings_viking_funeral` 同组复跑。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_final_haiku 在宿主进入弃牌堆后仍会通过 queued discard trigger 给其他己方随从 \\+2 力量|vikings_viking_funeral"` => `1 file passed, 2 passed`；`git diff --check -- src/games/smashup/domain/reactionSession.ts src/games/smashup/abilities/vikings.ts src/games/smashup/abilities/samurai.ts src/games/smashup/__tests__/reactionQueueOnMinionDiscardedFromBase.test.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` => passed（仅 LF/CRLF warning）。这格说明当前不是把同一条 LKI seam 机械复读，而是又在 `onMinionDiscardedFromBase` 家族里真实收掉了一条“callback 仍回头查 live host”的 sibling 残口；但不外推整个 Samurai、所有 attached-action discard trigger，或整条 destroy/discard family 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / onMinionDiscardedFromBase / triggerMinion controller from LKI` 真缝：`src/games/smashup/abilities/frankenstein.ts` 里的 `frankenstein_igor` 旧实现虽然想把后续目标选择交给那只离场随从的控制者，但实际仍硬依赖 `ctx.state.bases[ctx.baseIndex].minions.find(ctx.triggerMinionUid)` 回头读 live base；queued discard trigger 执行时宿主已进 discard，这个 lookup 就会回空，导致交互直接消失。现已只做一处最小修复：让 `frankenstein_igor` 优先读取 queued executor 恢复出来的 `ctx.triggerMinion?.controller`，只有 direct caller 没带 `triggerMinion` 时才 fallback 到 live base lookup。同时在 `src/games/smashup/__tests__/newFactionAbilities.test.ts` 新增 focused regression `frankenstein_igor 在宿主进入弃牌堆后仍会通过 queued discard trigger 给控制者创建目标选择交互`，并复跑既有同步入口小节 `newOngoingAbilities.test.ts` 的 `frankenstein_igor: 基地结算弃置触发`。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "frankenstein_igor 在宿主进入弃牌堆后仍会通过 queued discard trigger 给控制者创建目标选择交互"` => `1 file passed, 1 passed`；`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newOngoingAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "frankenstein_igor: 基地结算弃置触发"` => `1 file passed, 6 passed`；`git diff --check -- src/games/smashup/domain/reactionSession.ts src/games/smashup/abilities/vikings.ts src/games/smashup/abilities/samurai.ts src/games/smashup/abilities/frankenstein.ts src/games/smashup/__tests__/reactionQueueOnMinionDiscardedFromBase.test.ts src/games/smashup/__tests__/newFactionAbilities.test.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts task_plan.md progress.md evidence/smashup/smashup-in-progress-effect-atom-audit-2026-05-15.md` => passed（仅 LF/CRLF warning）。这格说明当前不是又把 attached-action host seam 重复扩样，而是在 `onMinionDiscardedFromBase` 家族里继续收掉了一条“callback 仍回头查 live base 才拿 controller”的 sibling stale-context 残口；但不外推整个 Frankenstein、所有 discard trigger，或全部 destroy/discard family 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / onMinionDiscardedFromBase / triggerMinion counters-controller from LKI` 真缝：`src/games/smashup/abilities/giant_ants.ts` 里的 `giant_ant_worker_pod` 旧实现把“destroy / scoring discard 两种时机都发生在 reducer 移除随从前，此时仍可从基地读取到真实 counters”写死在 callback 里；但 queued discard trigger 真正执行时宿主已经进 discard，这个 live base lookup 就会回空，导致本应给控制者的 `giant_ant_worker_pod_replay` 交互直接消失。现已只做一处最小修复：让 `giantAntWorkerPodReplayTrigger()` 优先读取 queued executor 恢复出来的 `ctx.triggerMinion` 来判断 `controller/powerCounters`，只有 direct caller 没带 `triggerMinion` 时才 fallback 到 live base lookup。同时在 `src/games/smashup/__tests__/newFactionAbilities.test.ts` 新增 focused regression `giant_ant_worker_pod 在宿主进入弃牌堆后仍会通过 queued discard trigger 提示从弃牌堆打到另一基地`，先红后绿锁定 prompt `sourceId='giant_ant_worker_pod_replay'`。验证：`node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "giant_ant_worker_pod 在宿主进入弃牌堆后仍会通过 queued discard trigger 提示从弃牌堆打到另一基地"` => `1 file passed, 1 passed`；`git diff --check -- src/games/smashup/abilities/giant_ants.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` => passed（仅 LF/CRLF warning）。这格说明当前不是继续在 `Igor / Final Haiku / Viking Funeral` 上原地扩样，而是在同一家族里又真实收掉了一条“callback 假设 live base 还在、却没用 queued triggerMinion witness”的 sibling stale-context 残口；但不外推整个 Giant Ants、所有 replay-from-discard trigger，或全部 destroy/discard family 已审完。
- [x] 已继续收掉一组新的 `queued-trigger-runtime-context / reaction-trigger-choice / onBuriedCardUncovered optional/perInstance owner` 真缝：`src/games/smashup/abilities/skeletons.ts` 里 `registerTrigger('skeletons_lord_of_bones', 'onBuriedCardUncovered', ...)` 与 `registerTrigger('skeletons_gravestones', 'onBuriedCardUncovered', ...)` 的 callback 本来就都按 `ctx.sourceControllerId ?? ctx.playerId` 把后续交互交还给来源控制者，但注册侧旧实现没显式写 `playerContext:'sourceController'`，导致当对手翻开埋葬牌时 queued trigger 的 `ownerPlayerId` 仍可能错落到事件玩家。现已先在 `reactionQueueSourceRuntimeContext.test.ts` 新增两条 focused regression 坐实 owner 错位，再只做两处最小修复：给这两条 trigger 都补上 `playerContext:'sourceController'`。验证：`node --max-old-space-size=8192 .\\node_modules\\vitest\\vitest.mjs run src/games/smashup/__tests__/reactionQueueSourceRuntimeContext.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1 -t "queued afterScoring trigger 在对手计分时仍应把 skeletons_gravestones 的选择权交给控制者|queued onBuriedCardUncovered per-instance trigger 在对手翻开埋葬牌时仍应把 skeletons_lord_of_bones 的选择权交给控制者|queued onBuriedCardUncovered per-instance trigger 在对手翻开埋葬牌时仍应把 skeletons_gravestones 的选择权交给控制者"` => `1 file passed, 3 passed`；`node --max-old-space-size=8192 .\\node_modules\\vitest\\vitest.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1 -t "skeletons_lord_of_bones 天赋可挖掘这里任意埋葬牌而不只限自己|skeletons_gravestones 计分后可把自己埋葬到另一个基地|skeletons_gravetender 每回合仅首次埋葬/挖掘触发抽牌"` => `1 file passed, 3 passed`。这格说明当前不是回头扫旧 `Spy / waiting overlay` objective，而是继续沿 `optional/per-instance trigger owner` 家族真实前进；但不外推整个 `skeletons`、所有 buried family，或所有 queued optional owner contract 已审完。
- [x] 已继续收掉一条新的 `processReturnToHandTriggers / queued-trigger-runtime-context / attached source leaves play` 真缝：`world_champs_bewitched` 的 owner 语义此前已经在 `reactionQueueSourceRuntimeContext.test.ts` 转绿，但 reducer 真链仍卡在 `CARD_TRANSFERRED` 场景下“不排 `TRIGGER_QUEUED`”。根因不是 callback 再次写错，而是宿主随从回手后，附着的 `world_champs_bewitched` 已随 `detachCardUidFromBases(...)` 落进 discard，旧 `collectTriggers(advancedCore, 'onCardReturnedToHand', ...)` 因 locate 不到 source 而直接漏排。现已只做两处最小修复：1) `ongoingEffects.ts` 在 `allowedSourceDefIds` 已收窄、且 caller 明确提供 `sourceCardUid/sourceControllerId/sourceBaseIndex` 时，允许 per-instance trigger 用这组显式 provenance 作为 fallback source 入队；2) `reducer.ts` 的 attached-action 回手分支按 `sourceDefIds:[attachedAction.defId]` 定向收集，避免同一 fallback 污染其它 sourceDefId。验证：`node --max-old-space-size=8192 .\\node_modules\\vitest\\vitest.mjs run src/games/smashup/__tests__/reactionQueueOnCardReturnedToHand.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1 -t "CARD_TRANSFERRED 让宿主从场上回手时，已随宿主离场的附着 source 也应按显式 source provenance 入队"` => `1 passed`；`node --max-old-space-size=8192 .\\node_modules\\vitest\\vitest.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1 -t "world_champs_bewitched 宿主经 CARD_TRANSFERRED 从场上回手时也应通过 reducer 真链创建转移交互"` => `1 passed`；邻近 owner seam `reactionQueueSourceRuntimeContext.test.ts -t "queued onCardReturnedToHand per-instance trigger 在对手回手宿主时仍应把 world_champs_bewitched 的转移权交给行动拥有者"` => `1 passed`。这格说明当前不是围着旧 goal 文案空转，而是 `onCardReturnedToHand + CARD_TRANSFERRED + attached action host leaves play` 这条 shared seam 又真实收掉一格；但不外推整个 `world_champs`、所有 attached-action return trigger，或所有 post-process family 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / skeletons_gravetender sourceController buried trigger` 真缝：显式 `playerContext` dedicated queue diff 清零后，继续筛“未显式 `playerContext` 但 callback 依赖 source controller”的 still-object-level family，命中 `skeletons_gravetender`。旧注册 `onCardBuried / onBuriedCardUncovered` 都只有 `perInstance:true`，当对手翻开己方埋葬牌时 queued owner 会默认跟事件玩家走；但 `skeletonsGravetenderTriggered()` 实际应给 Gravetender 控制者抽牌。现已给两条 trigger 都补 `playerContext:'sourceController'`，并在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused gate，直接锁住 `ownerPlayerId='0' / eventPlayerId='1' / P0 抽到 grave-draw-a / P1 不抽牌`。验证：focused buried skeletons 三条 `3 passed`；`reactionQueueSourceRuntimeContext.test.ts` 整文件 `21 passed`；Skeletons 邻近对象级矩阵 `6 passed`。这格说明当前确实有新红灯转绿，不是回旧 waiting overlay 打转；但不外推所有未显式 `playerContext` trigger 都已清零。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / ancient_egyptians_pharaoh uncover sourceController draw` 真缝：沿 Gravetender 后续候选继续筛，命中 `ancient_egyptians_pharaoh.onBuriedCardUncovered`。旧注册只有 `perInstance:true`，而 `ancientEgyptiansPharaohOnUncover()` 直接按 `ctx.sourceControllerId` 给 Pharaoh 控制者抽 1 张；focused 首次红灯坐实 `ownerPlayerId` 错落到事件玩家 `1`。现已给该 trigger 补 `playerContext:'sourceController'`，修复后 focused `1 passed`，`reactionQueueSourceRuntimeContext.test.ts` 整文件 `22 passed`，Pharaoh 邻近 `reactionQueueEventPlayerContext.test.ts -t "Pharaoh|法老"` 与 `smashup.smoke.test.ts -t "法老"` 均 `1 passed`，`newBaseAbilities.test.ts -t "base_star_portal"` `2 passed`。这格不外推 Ancient Egyptians 全部 trigger、`base_star_portal.onCardBuried` 或 `pirate_buccaneer` 已审完。
- [x] 已把 `pirate_buccaneer` replacement/live-minion owner 分支从开放候选降成 dedicated green evidence：新增 `newOngoingAbilities.test.ts` focused gate `owner 与 controller 分离时，三基地选择 prompt 应交给当前控制者`，用 `owner='1' / controller='0' / playerId='1'` 直接锁住 prompt `playerId==='0'`。验证：focused `1 passed`；同组 `pirate_buccaneer 触发器` 矩阵 `9 passed`。这格未发现 runtime 红灯，当前实现已经优先从 live minion controller 发 prompt；不外推所有 replacement trigger，但后续不应再把 `pirate_buccaneer` 当作当前 owner/context 开放残口。
- [x] 已把 `base_star_portal.onCardBuried` 从开放候选降成 dedicated green evidence：新增 `newBaseAbilities.test.ts` focused gate `base_star_portal 在其他玩家埋牌到这里时让埋葬者抽牌`，用 P1 埋牌到 Star Portal、direct trigger context `playerId='0'`、`buriedCardControllerId='1'` 直接锁住抽牌事件归 P1。验证：focused `1 passed`；同组 `base_star_portal` 矩阵 `3 passed`。这格未发现 runtime 红灯，当前语义应保持 buried-card-controller/event provenance，不外推所有 base buried triggers。
- [x] 已继续收掉一组新的 `queued-trigger-runtime-context / reaction-trigger-choice / afterScoring per-instance owner` 真缝：`src/games/smashup/abilities/yuanhou.ts` 的 `cyborgApesFlyingMonkeyAfterScoring()` 与 `shapeshiftersCellularBondingFlyingMonkey()` 回调本来就分别按 `action.ownerId / bondingAction.ownerId` 把 `cyborg_apes_flying_monkey_move` 交还给行动拥有者，但注册 `registerTrigger('cyborg_apes_flying_monkey', 'afterScoring', ...)` 与 `registerTrigger('shapeshifters_cellular_bonding', 'afterScoring', ...)` 的旧实现都只写了 `perInstance + sourceScope:'triggerBase'`，没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增两条 focused regression，首次红灯都直接坐实该 owner 错位；随后只做两处最小修复，在 `yuanhou.ts` 给两条 trigger 都补上 `playerContext:'sourceController'`。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => `2 passed`；邻近 `yuanhouFactionAbilities.test.ts -t "电子猿：飞猴在计分后按玩家选择另一基地并可跳过自动移动|电子猿：飞猴真实计分后移动宿主并摧毁本行动进入弃牌"` => `2 passed`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective，也不外推整个 `yuanhou` 或所有 afterScoring per-instance trigger 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / afterScoring per-instance owner` 真缝：`src/games/smashup/abilities/aliens.ts` 的 `alienScoutAfterScoringPerInstance()` 回调本来就按 `scout.controller` 把 `alien_scout_return` 交还给侦察兵控制者，但注册 `registerTrigger('alien_scout', 'afterScoring', ...)` 的旧实现只写了 `perInstance + sourceScope:'triggerBase'`，没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused regression `queued afterScoring per-instance trigger 在对手计分时仍应把 alien_scout 的选择权交给控制者`，首次红灯直接坐实该 owner 错位；随后只做一处最小修复，在 `aliens.ts` 给该 trigger 补上 `playerContext:'sourceController'`。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => `1 passed`；邻近 `afterScoring-rescoring.test.ts -t "base_great_library 与 alien_scout 同时进入 afterScoring 时，先结算抽牌也不会触发命令异常"` => `1 passed`；`alien-scout-pod-afterscore.test.ts` => `4 passed`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective，也不外推整个 `aliens` 或所有 afterScoring per-instance trigger 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / afterScoring per-instance owner` 真缝：`src/games/smashup/abilities/innsmouth.ts` 的 `innsmouthReturnToTheSeaAfterScoring()` 回调本来就按 `armedEntry.playerId` 执行 `innsmouth_return_to_the_sea` 后续交互，但注册 `registerTrigger('innsmouth_return_to_the_sea', 'afterScoring', ...)` 的旧实现只写了 `perInstance + sourceScope:'triggerBase'`，没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused regression `queued afterScoring per-instance trigger 在对手计分时仍应把 innsmouth_return_to_the_sea 的选择权交给控制者`，首次红灯直接坐实该 owner 错位；随后只做一处最小修复，在 `innsmouth.ts` 给该 trigger 补上 `playerContext:'sourceController'`。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => `1 passed`；邻近 `afterscoring-response-window-execution.test.ts -t "重返深海"` => `2 passed`；`afterscoring-card-registration.test.ts -t "innsmouth_return_to_the_sea 应该走统一 afterScoring trigger"` => `1 passed`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective，也不外推整个 `innsmouth` 或所有 afterScoring per-instance trigger 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / afterScoring per-instance owner` 真缝：`src/games/smashup/abilities/giant_ants.ts` 的 `giantAntWeAreTheChampionsAfterScoring()` 回调本来就按 `armedEntry.playerId` 创建 `giant_ant_we_are_the_champions_choose_snapshot_source` 交互，但注册 `registerTrigger('giant_ant_we_are_the_champions', 'afterScoring', ...)` 与 POD 版的旧实现都只写了 `perInstance + sourceScope:'triggerBase'`，没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused regression `queued afterScoring per-instance trigger 在对手计分时仍应把 giant_ant_we_are_the_champions 的确认权交给控制者`，首次红灯直接坐实该 owner 错位；随后只做两处最小修复，在 `giant_ants.ts` 给基础版和 POD 版 trigger 都补上 `playerContext:'sourceController'`。验证：focused `reactionQueueSourceRuntimeContext.test.ts` => `1 passed`；邻近 `afterScoring-rescoring.test.ts -t "afterScoring 通过统一反应入口打出我们乃最强后，不会重新给同一基地计分"` => `1 passed`；`afterscoring-response-window-execution.test.ts -t "我们乃最强"` => `2 passed`；`afterscoring-card-registration.test.ts -t "giant_ant_we_are_the_champions 应该走统一 afterScoring trigger"` => `1 passed`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective，也不外推整个 `giant_ants` 或所有 afterScoring per-instance trigger 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / beforeScoring per-instance owner` 真缝：`src/games/smashup/abilities/cthulhu.ts` 的 `cthulhuChosenBeforeScoringPerInstance()` 回调本来就按 `chosen.controller` 创建 `cthulhu_chosen_confirm` 确认交互，但注册 `registerTrigger('cthulhu_chosen', 'beforeScoring', ...)` 的旧实现只写了 `perInstance: true`，没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused regression `queued beforeScoring per-instance trigger 在对手计分时仍应把 cthulhu_chosen 的确认权交给控制者`，首次红灯直接坐实该 owner 错位；随后只做一处最小修复，在 `cthulhu.ts` 给该 trigger 补上 `playerContext:'sourceController'`。验证：`reactionQueueSourceRuntimeContext.test.ts + newOngoingAbilities.test.ts + cthulhu-chosen-display-mode.test.ts` 聚焦矩阵 => `2 files passed, 7 passed, 1 file skipped`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective，也不外推整个 `cthulhu` 或所有 per-instance beforeScoring trigger 已审完。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / beforeScoring optional trigger owner` 真缝：`src/games/smashup/abilities/pirates.ts` 的 `pirateKingBeforeScoring()` 回调本来就按每个 `king.controller` 创建 prompt，但注册 `registerTrigger('pirate_king', 'beforeScoring', ...)` 的旧实现没显式写 `playerContext:'sourceController'`，导致当 `eventPlayer='1' / sourceController='0'` 时，queued trigger 的 `ownerPlayerId` 错落到 `1`。现已在 `reactionQueueSourceRuntimeContext.test.ts` 新增 focused regression `queued beforeScoring trigger 在对手计分时仍应把 pirate_king 的选择权交给控制者`，首次红灯直接坐实该 owner 错位；随后只做一处最小修复，在 `pirates.ts` 给该 trigger 补上 `playerContext:'sourceController'`。验证：`reactionQueueSourceRuntimeContext.test.ts + newOngoingAbilities.test.ts` 聚焦矩阵 => `2 files passed, 6 passed`，且测试口径已纠偏为真实 mandatory beforeScoring 直接进入 `pirate_king_move`，不再误断言先经过 `smashup_reaction_choose`。这格说明当前仍在沿长期 JSON 的 `optional/per-instance trigger owner` 主线前进，不是回扫旧 overlay objective。
- [x] 已继续收掉一条新的 `scoreOneBase / deferred replacement target sync` 真缝：`replaceDeferredPostScoringReplacementBase()` 旧实现虽然会改 deferred `BASE_REPLACED.newBaseDefId`，但不会同步改写 scoring frame 里已经缓存的 `deferredActions.targetBaseDefId`。这会让 `Tortuga / Greenhouse / Titan replacement-base` 这类“先缓存待补发动作，再在同一 afterScoring 窗里改写 replacement base”的链路继续追旧目标基地。现已在 `afterscoring-window-skip-base-clear.test.ts` 新增 focused regression `改写 deferred replacement base 时，也应同步改写待补发 deferredActions 的 targetBaseDefId`，首次红灯坐实 `moveMinionToReplacementBase.targetBaseDefId` 仍停在 `base_secret_garden`；随后在 `scoringSession.ts` 收紧 `replaceDeferredPostScoringReplacementBase(...)`，同步改写 `moveMinionToReplacementBase / playMinionOnReplacementBase / playTitanOnReplacementBase` 三类 deferred action 的 `targetBaseDefId`。验证：focused `afterscoring-window-skip-base-clear.test.ts -t "改写 deferred replacement base 时，也应同步改写待补发 deferredActions 的 targetBaseDefId"` => `1 passed`；整文件 `afterscoring-window-skip-base-clear.test.ts` => `16 passed`；邻近 `yuanhouFactionAbilities.test.ts -t "时间旅行者基地：枢纽真实计分后若基地牌库已空且选择弃牌堆基地替换，应让所选基地替换并用其余弃牌堆与旧基地重建牌库|时间旅行者：时间流逝真实计分后若基地牌库已空且只剩一个合法基地弃牌堆候选，仍应使用该候选替换而不是退回旧 reshuffle 池"` => `2 passed`。这格说明当前不是围着旧 goal 文案空转，而是又真实关掉了一条 `BASE_REPLACED` 与 deferredActions replacement target 没同步改写的 scoring seam，不外推整个 afterScoring deferred family 已完成。
- [x] 已继续收掉一条新的 `scoreOneBase / deferred replacement` 真缝：`Time Is Fleeting` 在 afterScoring 响应窗里通过 `BASE_DECK_REORDERED(reason='time_travelers_time_is_fleeting')` 把 `base_faceless_city` 从 `baseDiscard` 放到 `baseDeck` 顶后，旧 deferred `BASE_REPLACED` 虽能改成新基地，但 `scoreOneBase()` 初次排队时留下的 `BASE_DECK_SHUFFLED(reason='base_deck_empty_reshuffle_discard')` 仍会残留，最终把刚计分的 `base_monkey_lab` 又塞回 `baseDeck`。现已在 `scoringSession.ts` 收紧 `replaceDeferredPostScoringReplacementBase(...)`：当所选 replacement base 已经真实进入当前 `state.core.baseDeck` 时，同步删除那条 stale 的 empty-deck reshuffle deferred event。验证：focused `yuanhouFactionAbilities.test.ts -t "时间旅行者：时间流逝真实计分后若基地牌库已空且只剩一个合法基地弃牌堆候选，仍应使用该候选替换而不是退回旧 reshuffle 池"` => `1 passed`；邻近 `yuanhouFactionAbilities.test.ts + baseScoring.test.ts + reactionQueueOnBaseRevealed.test.ts -t "时间流逝|scoreOneBase 在 BASE_REPLACED 后应按新基地而不是旧基地排 onBaseRevealed 队列"` => `2 files passed, 8 passed`、`1 file skipped`。这格说明当前又真实关掉了一条 `deferred BASE_DECK_SHUFFLED` 与 afterScoring replacement choice 没同步失效的 scoring seam，不外推整个 scoring/deferred family 已完成。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context` 真缝：`TriggerContext` 已有整套 `duel / duelSourceId / duelOutcome / duelChallenger / duelChallenged / duelWinner / duelLoser / duelTie`，但 `TriggerInstance`、`createTriggerInstance()` 与 `reactionSession` executor 恢复链之前都没带这组 duel 上下文。现已在 `reactionQueueControllerRuntimeContext.test.ts` 新增 focused regression `queued onDuelResolved ongoing trigger 的 canTrigger 与 executor 都应继续拿到 duel 上下文`，首次红灯坐实 `trigger.duelSourceId===undefined`；随后在 `types.ts / ongoingEffects.ts / reactionSession.ts` 最小补齐透传，并复跑 focused `2 passed` 与邻近 queued runtime matrix `reactionQueueControllerRuntimeContext.test.ts + reactionQueueSourceRuntimeContext.test.ts + reactionQueueInspectionRuntimeContext.test.ts + reactionQueueEventPlayerContext.test.ts` => `4 files passed, 6 passed`。这格属于“collector eligibility 看得到、queued instance / executor 恢复时丢字段”的 shared seam 修复，不外推整个 duel family 已全量完成。
- [x] 已继续收掉一条新的 `post-process family` 真缝：`processAffectTriggers()` 旧实现只把 ongoing `onMinionAffected` 排进 queue，extended base `onMinionAffected` 完全漏排，导致同时命中 mandatory ongoing 与 mandatory extended base 时不会进入统一 reaction ordering。现已在 `reactionQueueBaseAbilities.test.ts` 新增 focused regression `processAffectTriggers 命中 mandatory ongoing 与 mandatory extended base onMinionAffected 时，应统一进入 reaction ordering`，首次失败又顺手暴露出测试夹具误写 `SU_EVENTS.POWER_COUNTERS_ADDED` 的假红；修正为真实 carrier `POWER_COUNTER_ADDED` 后，配合 `reducer.ts` 中新增的 `collectExtendedBaseAbilityTriggers(...)` affect 分支，focused 复跑 `1 passed`、整文件复跑 `19 passed`。这格说明当前不是死循环，而是 `onMinionAffected extended-base queued-ordering` 又真实收掉一条 shared seam；但不外推整条 affect family 已全部审完。
- [x] 已继续收掉一条新的 `destroy -> discard` shared seam：`processDestroyTriggers()` 旧实现虽然能判定 `MINION_DESTROYED` 最终是否真的进 owner discard，但随后是用 `discardCore` 调 `collectTriggers('onMinionDiscardedFromBase', ..., { sourceDefIds:[minionDefId] })`。这会同时造成两类断层：一是像 `samurai_way_of_the_warrior / base_sakura_garden` 这类非 `triggerMinionDefId` 来源被硬过滤掉；二是连“被弃那张随从自己”的 source 也因为已经离场而在 `discardCore` 里找不到 live/LKI 来源。现已在 `reactionQueueOnMinionDiscardedFromBase.test.ts` 新增 focused regression `processDestroyTriggers 不应把 onMinionDiscardedFromBase 错过滤成只剩 triggerMinionDefId 来源`，首次红灯坐实两类来源都缺失；随后在 `reducer.ts` 改成仅用 `discardCore` 判定 didEnterOwnerDiscard，而真正 queue 收集回到 `phase2Core/phase2State` 的 live/LKI 现场，并移除那条错误的 `sourceDefIds` 过滤。验证：focused `1 passed`；邻近 `reactionQueueOnMinionDiscardedFromBase.test.ts + reactionQueueBaseAbilities.test.ts` => `2 files passed, 21 passed`；真实 consumer 邻近 `newFactionAbilities.test.ts` 的 `samurai_way_of_the_warrior 在 destroy -> processDestroyTriggers 真链进入弃牌堆时也会通过 queued onMinionDiscardedFromBase 抽牌` 与原有两条 discard/base-scoring 邻近同组复跑 `3 passed`。这格说明当前不是围着旧 waiting overlay symptom 原地踏步，而是又关掉一条 `destroy -> discarded-from-base` 的 shared collector/live-source 合同裂缝；但不外推整条 discard family 已经全量收口。
- [x] 已继续收掉一条新的 `scoreOneBase -> BASE_REPLACED -> onBaseRevealed` stale-core seam：旧 `index.ts` 在构造 `BASE_REPLACED` 后，仍把旧 `core` 传给 `collectExtendedBaseAbilityTriggers(...)`，导致新基地 reveal 反应会被旧基地上下文吞掉。现已在 `reactionQueueOnBaseRevealed.test.ts` 新增 focused regression `scoreOneBase 在 BASE_REPLACED 后应按新基地而不是旧基地排 onBaseRevealed 队列`，首次红灯坐实根本没有 reveal queued event；随后在 `index.ts` 改成先把 `BASE_CLEARED / BASE_DECK_SHUFFLED / BASE_REPLACED` reduce 到 `revealCore`，再按这个新基地现场收集 `onBaseRevealed`。验证：focused `1 passed`；邻近 `reactionQueueOnBaseRevealed.test.ts + baseScoring.test.ts` => `2 files passed, 24 passed`。这格说明当前不是只在 post-process collector 上兜圈，而是又关掉一条 `scoreOneBase` 延迟补发链上的 stale-context collector 真缝；但不外推整个 scoring/deferred family 已经全量收口。
- [x] 已继续收掉一条新的 live seam：`simple-choice` 的 `option.value drift`。旧实现里 `onlineAiRecovery.ts` 的 runtime candidate fingerprint 与 `server.ts` 的 simple-choice fallback fingerprint 都只锁 `id/disabled`，没把 `value` 并进去，导致同 `interactionId/sourceId/title` 下真实控制值变化（例如 `{kind:'pass'}` 与 `{done:true}`）仍会被旧 tracker 当成同一 incident。现已把 `value` 并入两层 fingerprint，并补齐 `attemptKey drift / buildOnlineAiRecoveryFingerprint drift / runtime discard old tracker` 三条 direct gate；邻近 simple-choice 矩阵复跑 `9 passed`。这格属于 shared transport 真修复，不外推其它 visible-interaction kind 已全量完成。
- [x] 已继续把 hidden `simple-choice` 的 `title / option.value / slider` 从“visible 同族外推”补成独立 direct gate：`onlineAiRecovery-gameover.test.ts` 新增三条 hidden `attemptKey` 漂移门禁，`server.test.ts` 新增 hidden runtime `option.value / slider` tracker drift 两条门禁；聚焦复跑分别为 `1 file passed, 3 passed` 与 `1 file passed, 3 passed`。这格只说明 hidden `simple-choice` 当前已具备与 visible 同粒度的 field-level 证据，不外推整个 hidden-interaction family 或更宽泛 `new hidden prompt` 漂移用例已全部稳定。
- [x] 已把那条 still-red 的旧 hidden prompt drift 用例降回测试夹具错误，而不是继续把它记成主线开放 residual：`server.test.ts` 里的 `online AI watchdog 在 hidden-interaction 候选 fingerprint 漂移到新的 hidden prompt 时，应丢弃旧 tracker` 旧 mock 给的是 only-`skip` prompt，却错误断言第一次执行必须是 `{ optionId:'confirm' }`；现已改成 `{ optionId:'skip' }` 并与 hidden drift 邻近矩阵同组复跑 `1 file passed, 4 passed`。这格说明“看起来像又卡住”的旧红点已被纠偏，不新增实现语义。
- [x] 已继续把 hidden `simple-choice` 的 `interactionId / title` 从“runtime gate 已绿，所以 isolated fingerprint 大概只是测法问题”纠偏成一条真实 caller seam：`server.ts` 的 `buildOnlineAiRecoveryFingerprint()` 旧实现对 `hidden-interaction` 仍只优先读 shared `interaction.current`，shared state 本来拿不到 owner-only prompt 时就会直接回退旧 `candidate.fingerprintHint ?? progressMarker`，因此 standalone fingerprint tests 会卡成 progressMarker，而 runtime drift gate 还能靠 seat-view candidate hint 继续转绿。现已改成在 hidden 分支缺 shared current 时回退读取 `applyPlayerView(match, candidate.playerId)` 的 `interaction.current`，并复跑 `buildOnlineAiRecoveryFingerprint 在 hidden simple-choice 的 sourceId/title/option signature 相同但 interactionId 漂移时，也必须变化|buildOnlineAiRecoveryFingerprint 在 hidden simple-choice 的 sourceId/interactionId/options 相同但 title 漂移时，也必须变化|online AI watchdog 在 hidden-interaction 的 sourceId/title/option signature 相同但 interactionId 漂移时，也必须丢弃旧 tracker|online AI watchdog 在 hidden-interaction 的 sourceId/interactionId/options 相同但 title 漂移时，也必须丢弃旧 tracker` => `1 file passed, 4 passed`。这格说明 hidden fingerprint builder 与 runtime seat-view caller 已重新对齐，不再把“runtime 绿 / fingerprint 红”的分裂继续误判成死循环。
- [x] 已继续收掉一条新的 live seam：`compare-roll-choice` 的 `confirmValue drift`。旧实现里 `onlineAiRecovery.ts` 的 `buildInteractionRecoveryFingerprintHint(...)` 与 `server.ts` 的 compare-roll fallback fingerprint 都没带 `confirmValue`，导致同 `interactionId/sourceId` 下确认载荷变化时，watchdog 第二拍仍沿旧 tracker 继续，直接落成 `visible-interaction:recover-interaction:no_progress`。现已把 `confirmValue` 并入两层 fingerprint，并补齐 `attemptKey drift / buildOnlineAiRecoveryFingerprint drift / runtime discard old tracker` 三条 direct gate；邻近 compare-roll 矩阵复跑 `7 passed`。这格属于 shared transport 真修复，不外推其它 visible-interaction kind 已全量完成。
- [x] 已继续补齐 `response-loop` 的 `windowType` 对称 direct gate：新增 `buildOnlineAiRecoveryFingerprint 在 response-loop 的 id/source/queue 相同但 windowType 漂移时，也必须变化` 与 `online AI watchdog 在 response-loop 强制关窗后若 response-window 只变了 windowType，也不应继续沿旧 tracker 强制关窗`，聚焦复跑 `2 passed`。这格只说明 `response-loop` 的 caller/handoff provenance 里还存在一条此前没显式锁住的对称证据，不外推 shared transport 主线全量完成。
- [x] 已继续收掉一条新的 shared transport 真残口：`response-loop` 在先被 `stale/missing-private-overlay` 挡住、随后 `SYS_RESPONSE_WINDOW_FORCE_CLOSE` 又没有推进时，旧实现会先把 `response-loop` 在 revalidate 阶段静默退回 `response-window`，再把最终失败吞成粗粒度 `no_progress`。现已在 `revalidateRecoveryCandidate()` 里把同一 hard-close incident 的 `response-loop` 语义保住，并让 blocked fallback 也能上报 `private_overlay_stale / private_overlay_missing`；聚焦复跑 `tryRecoverOnlineAiWithLegalAction 遇到 response-loop tracker 时，即使旧 candidate 仍是 response-window，也不应误走 emergency overlay fallback|online AI watchdog 在 response-loop 遇到 stale-private-overlay 时，不应再触发 overlay resync，而应直接强制关窗|online AI watchdog 在 response-loop 遇到 missing-private-overlay 时，不应再触发 overlay resync，而应直接强制关窗|online AI watchdog 在 response-loop 遇到 stale-private-overlay 且 force-close 无推进时，应上报 private_overlay_stale 而不是 no_progress|online AI watchdog 在 response-loop 遇到 missing-private-overlay 且 force-close 无推进时，应上报 private_overlay_missing 而不是 no_progress` => `7 passed`。这格只证明 `response-loop` 的这条 fallback provenance 已补齐，不外推整个 response-loop / response-window / hidden-interaction family 已全量完成。
- [x] 已继续核对 `goal` / `task_plan.md` / 长期状态的锚点分裂，并把“为什么像死循环”进一步拆成“真残口”和“已证伪风险”：新补直测 `online AI watchdog 在 legal-only failure revalidate 时若 stalled surface 已切到新 phase，不应沿旧 tracker 直接上报失败` 已直接转绿，说明 `revalidateRecoveryCandidate()` 这层当前不是新的 phase-switch 吞错入口，后续不再把它当成开放 residual 反复回扫。
- [x] 已确认造成“像死循环”的直接原因：活跃 `goal`、当前 `task_plan.md` 与长期状态 JSON 曾同时指向不同任务层级。
- [x] 已确认真正最新的 operational target 在长期状态 JSON，而不是活跃 `goal` 的旧 objective。
- [x] 已补 `MatchRoom` 的 seat recovery helper：`buildOnlineAiIdleSeatRecoveryKey()` / `buildOnlineAiSubmitBlockedRecoveryKey()` 现在都显式把 `buildAiProgressMarker(sharedState)` 纳入 recoveryKey，`resolveOnlineAiSeatRecoveryAttempt()` 也改成纯 helper，避免同 key 冷却拒绝时把下一次重试窗口继续往后推。
- [x] 已继续把 `legal-only` family 的 incident key 从“动作名/blockedKey 级”补到也锁住 `progressMarker / resolution.attemptKey`：`active-turn-legal-only` 现在即使同一 AI 与同一 `factionSelect` phase，也会随 `buildAiProgressMarker(sharedState)` 漂移换 fingerprint/attemptKey；`resolveOnlineAiRecoveryCandidate()` 的 `seat-legal-only` 直接候选也改为优先吃 `resolution.attemptKey`，blocked 候选则附带 `progressMarker`，不再把同一 `actionId` 或同一 `blockedKey` 的新现场折回旧 key。聚焦复跑 `onlineAiRecovery-gameover.test.ts` 3 passed、`server.test.ts` 3 passed。
- [x] 已继续把 `runOnlineAiRecoveryTick()` 的 `legal-only` 顶层 continuity 从“只补正向 drift gate”收紧到也能区分新 stalled surface：先补 `active-turn-legal-only progressMarker 漂移但仍是同一 stalled surface` 的对称直测，再新增负向红灯 `active-turn-legal-only stalled surface 已切到新 phase 时，应重建新 tracker`，确认旧实现会把 `factionSelect` 新 surface 误吞进旧 `targetingRoll` tracker；现已新增 `buildOnlineAiRecoveryContinuitySurfaceFingerprint()`，按 `active-turn-legal-only / seat-legal-only` 的真实 fingerprint 结构比较 continuity surface，并把 6-case 邻近矩阵复跑到 `6 passed`。
- [x] 已继续把 `hasOnlineAiRecoveryResolved()` 的 sibling-family direct gate 再补两格对称证据：`active-turn -> seat-legal-only` 与 `legalActionOnly -> active-turn` 现都已并入现有矩阵，聚焦复跑 `11 passed`，说明这里是 direct gate 缺证而不是新的 runtime residual。
- [x] 已继续把 `onlineAiRecovery.ts` 的 `response-window queue signature` caller provenance 从“只在 blockedKey / overlay resync caller 层存在”补到更内层的 `fingerprintHint / attemptKey`：`src/engine/transport/__tests__/onlineAiRecovery-gameover.test.ts` 先补红灯，确认自动 `response-window` 在同 `id/type/source/currentResponder` 但 `responderQueue` 漂移时旧 `fingerprintHint` 不变；随后在 `src/engine/transport/onlineAiRecovery.ts` 把 `queueSignature` 并入自动 `RESPONSE_PASS` 与手动 `SYS_RESPONSE_WINDOW_FORCE_CLOSE` 的 `fingerprintHint / attemptKey`，聚焦复跑 `手动强制结束应直接强制关闭响应窗口 / 手动 queue drift / 自动 provenance / 自动 queue drift` 共 `4 passed`。这次补的是 `response-window` 更内层 caller provenance，不外推整个 `onlineAiRecovery` family 已完成。
- [x] 已继续把 `response-window queue signature drift` 从 blockedKey 供源补成 overlay resync caller：`src/engine/transport/__tests__/server.test.ts` 新增 `online AI watchdog 在 response-window queue signature 漂移时，应允许再次触发 overlay resync，而不是被旧冷却一并吞掉`，与既有 `id/source/windowType/responder drift` 同组复跑 `5 passed`。这次把 `responseWindow.responderQueue` 的语义补接到 shared transport caller provenance，不外推整条 `response-window` family 已完成。
- [x] 已继续把 `response-window windowType drift` 从实现旁证补成两层 direct gate：`src/engine/ai/__tests__/onlineDecisionView.test.ts` 新增 `private-required response-window 即使 id/source/responder/queue 相同，只要 windowType 漂移，blockedKey 也必须变化`，并与既有 `id/source/responder/queue` 邻近矩阵同组复跑 `5 passed`；`src/engine/transport/__tests__/server.test.ts` 新增 `online AI watchdog 在 response-window windowType 漂移时，应允许再次触发 overlay resync，而不是被旧冷却一并吞掉`，并与既有 `id/source/responder drift` 同组复跑 `4 passed`。这次把 `windowType` 从 blockedKey 供源一直接到 overlay resync caller，不外推整条 `response-window` family 已完成。
- [x] 已继续把 `onlineDecisionView` 上游 `private-required response-window` 的 `blockedKey` provenance 从“只锁 `responder / queue signature`”补到也锁 `window id / sourceId`：`src/engine/ai/__tests__/onlineDecisionView.test.ts` 新增 `private-required response-window 即使 type/source/responder/queue 相同，只要 window id 漂移，blockedKey 也必须变化` 与 `private-required response-window 即使 id/type/responder/queue 相同，只要 source 漂移，blockedKey 也必须变化`，并与既有 `responder / queue` 两条同组复跑 `4 passed`。这次补的是 `engine/ai` 上游 blockedKey 供源，不外推 `server.ts` 的 overlay resync / response-loop caller 全量完成。
- [x] 已把 `overlay resync cooldown` 的 `blockedKey 相同但 progressMarker 漂移` 这格闭成 helper 级直测：`src/engine/transport/server.ts` 的 `maybeTriggerOnlineAiOverlayResync()` 现按 `${matchID}:${playerId}:${blockedKey}:${buildAiProgressMarker(match.state)}` 去重；`server.test.ts` 里的同名用例已改成直接调用 helper，确认第二次相同 incident 仍被冷却吞掉、但 progressMarker 漂移后会重新放行 resync。聚焦复跑 `overlay resync cooldown / distinct blockedKey / same blockedKey + progressMarker drift / response-window id drift` 共 `4 passed`，`git diff --check -- src/engine/transport/server.ts src/engine/transport/__tests__/server.test.ts` 仅剩 LF/CRLF warning。旧 `35` 次 broadcast 是完整 watchdog tick 的测试噪音，不是 runtime 仍在循环广播。
- [x] 已重新核实 `server.ts` 邻近稳定点：`response-loop / response-window / active-turn` 的 8-case 聚焦矩阵当前再次复跑为 `8 passed`，说明本 worktree 已回到上一个已知稳定绿态，不应继续把“恢复绿态”误判成仍未完成。
- [x] 已继续把 `hasOnlineAiRecoveryResolved()` 的 `response-loop` gate 补到 other-AI responder 对称分支：当前不只锁“当前 AI / 空 responder / human / 窗口关闭”，也显式锁住“切到别的 AI 响应者即算 resolved”。
- [x] 已继续把 `hasOnlineAiRecoveryResolved()` 的 hidden/visible sibling-family 对称性补齐：当前 `hidden-interaction` 与 `visible-interaction` 都已显式锁住切到彼此时仍不算 resolved，不再只靠各自切到 `response-window / active-turn` 的邻近分支外推。
- [x] 已继续把 `hasOnlineAiRecoveryResolved()` 的 hidden/visible sibling-family 补到 `response-loop`：当前 `hidden-interaction` 与 `visible-interaction` 也都已显式锁住切到 `response-loop` 时仍不算 resolved，不再只靠 `response-window` 或 `active-turn` 邻近分支外推。
- [x] 已继续把 `resolveOnlineAiRecoveryCandidate()` 的 `response-loop tracker` 退回条件补到 human-restored 分支：即使 tracker 已在 `response-loop` key 上，只要 `responderQueue` 又重新带回 human，candidate 也会退回 `response-window`，不会继续 hard-close。
- [x] 已继续把 `resolveOnlineAiRecoveryCandidate()` 的 `response-loop existing tracker` 前置条件补到旧 responder 漂移一格：即使 tracker 已在 `response-loop` key 上，只要当前 responder 已变，candidate 也会退回 `response-window`，不会继续 hard-close。
- [x] 已继续把 `resolveOnlineAiRecoveryCandidate()` 的 `response-window -> response-loop` 升级前置条件补到 `responder` 级 caller provenance：除了旧 `window/source` 漂移外，当前也有最小直测锁住 `tracker key` 若仍指向旧 responder，不会把现场 `response-window` 误升级成 `response-loop`。
- [x] 已继续把 `response-loop` 的 caller provenance 从 `window id` 收紧到 `sourceId / currentResponder` 级 direct gate：`buildOnlineAiRecoveryFingerprint` 现在不再只靠 `response-window` 的 overlay-resync 邻近用例外推 `response-loop` 同族漂移，`server.test.ts` 新增 `response-loop sourceId drift` 与 `response-loop responder drift` 两条最小直测，并与既有 `response-window/response-loop window id drift` 同组复跑 `4 passed`。
- [x] 已确认 `response-loop -> active-turn-legal-only` handoff 继续保持绿态。
- [x] 已补齐真正缺口：`response-loop -> seat-legal-only` 不是 candidate producer 不存在，而是 sequence-level handoff 在强关窗后把同一 AI 的新 `seat-legal-only` tracker 误当成旧 `response-loop` 漂移直接丢弃。现已收紧为只对 `active-turn / active-turn-legal-only / seat-legal-only` 续接新 tracker，而 `visible/hidden-interaction` 这类新 incident 仍断开旧链；新增直测 `online AI watchdog 在 response-loop 强制关窗后若已切到 seat-legal-only，应交给新的 legal-only tracker 继续推进`，并与 `active-turn-legal-only / new hidden-interaction` 邻近矩阵同组复跑 `3 passed`。
- [x] 已继续补一格 `response-loop same-incident continuity`：旧 `runOnlineAiRecoverySequence()` 只允许 `response-loop` 强关窗后续接 `response-window / active-turn / legal-only`，若 `resolveOnlineAiRecoveryCandidate()` 已明确返回“同一 incident 仍是 `response-loop`”，sequence 反而会把 tracker 直接删掉，下一 tick 又退回 `response-window`。现已新增直测 `online AI watchdog 在 response-loop 同一 incident 仍然成立时，不应在 sequence 内把 tracker 丢回 response-window` 先红后绿，并把 `response-loop` 自身纳入 follow-up tracker 白名单；与 `existing-tracker / active-turn handoff / new response-window / new visible / new hidden` 邻近矩阵同组复跑 `6 passed`。
- [x] 已继续补一格 live prompt kind provenance：`multistep-choice` 旧 fingerprint 过粗，只看 `kind + interactionId + sourceId`；现已补到 `dtType/max-min steps/allowed-completed dice set` 级别，并已从 direct gate 升到 runtime drift gate。
- [x] 已继续补 `dt:token-response / dt:bonus-dice` 两条 live prompt seam：watchdog forced command 已从 generic `SYS_INTERACTION_CANCEL` 收紧到正确的 `SKIP_TOKEN_RESPONSE / SKIP_BONUS_DICE_REROLL`，且 fingerprint 已带 `pendingDamage.currentDamage/tokenUsageTotals` 与 `pendingBonusDiceSettlement.rerollCount/dice` 级语义；四条聚焦直测已 `4 passed`，相邻矩阵复跑 `5 passed`。
- [x] 已补 `simple-choice / dt:card-interaction / response-window / response-loop / pending-damage` 的 interaction semantic fingerprint 与 direct gate。
- [x] 已把 manual recovery 的 `response-window` 诊断 provenance 对齐到自动 watchdog 粒度：`resolveManualForceEndAiPhase()` 的 `fingerprintHint` 现已带 `windowId`。
- [x] 已盘点 custom event-stream consumer 覆盖，确认 `src/games/dicethrone/hooks/useAnimationEffects.ts` 是当前 worktree 尚未单独锁住 rollback-clear 的剩余 consumer，并已补独立 rollback 门禁转绿。
- [x] 已继续补一条 shared consumer seam：`src/games/summonerwars/ui/useGameEvents.ts` 的 rollback-clear 合同现已由独立测试锁定，确认旧 `UNIT_ATTACKED` 还原后不重播，只有 rollback 后新增攻击事件才重新进入 `diceResult` UI。
- [x] 已继续补一条 transport helper seam：`pendingInteractionId` 触发 hidden seat inspection 的入口条件，以及 `resolveForceSkippableHiddenAiInteraction()` 的 `skip` / `__cancel__` 合同现已由独立测试锁定。
- [x] 已继续补一格 `response-loop` caller provenance：`resolveOnlineAiRecoveryCandidate()` 与 `runOnlineAiRecoverySequence()` 在把 `response-window` 升级成 `response-loop` 时，`fingerprintHint` 现已带上 `windowId/sourceId`，不再让 feedback/blockerFingerprint 把新 incident 折回旧窗口。
- [x] 已继续补一格 `response-loop -> hidden-interaction` handoff：`response-loop` 强关窗后若现场切到新的 `hidden-interaction`，旧 tracker 不会继续吞掉新私有交互；新直测 `online AI watchdog 在 response-loop 强制关窗后若已切到新的 hidden-interaction incident，不应继续沿旧 tracker 吞掉新私有交互` 已 `passed`，并与 visible / response-window / active-turn 邻近矩阵同组复跑 `4 passed`。
- [x] 已继续补一格 `active-turn-legal-only` failure provenance：strict + emergency playerView 后若仍是 `private-required` blocked，feedback 的 `blockerFingerprint` 现已保留 `blockedKey`，不再只剩粗粒度 `active-turn-legal-only:${playerId}:${phase}`。
- [x] 已补 `active-turn-legal-only` 的 `legal_action_unavailable` feedback direct gate：AI active 的 `targetingRoll` 下，无论 `legalActions` 为空还是合法动作执行失败、且本分支都不允许 fallback `ADVANCE_PHASE`，`stateSnapshot/actionLog.blockerFingerprint` 都会保留 `active-turn-legal-only:1:targetingRoll`，不再只靠 reason 级断言旁证。
- [x] 已补 `factionSelect` 的 `legal-action-recovered` provenance direct gate：`factionSelect` 不仅会走 legal-action recovery 而不是 fallback `ADVANCE_PHASE`，其 recovered feedback 现在也同时锁住 `reason=active-turn-legal-only:legal-action:select-faction:select-faction:robots` 与 `stateSnapshot/actionLog.blockerFingerprint` 含 `active-turn-legal-only:2:factionSelect`。
- [x] 已补 `seat-legal-only` 的 `legal-action-recovered` provenance direct gate：human active 的 off-turn `defensiveRoll` / `targetingRoll` 两个公开分支现在不只锁住 `resolved`，还同时锁住 `reason=seat-legal-only:legal-action:advance-phase:legal-advance`，以及 `stateSnapshot/actionLog.blockerFingerprint` 分别含 `seat-legal-only:1:defensiveRoll:advance-phase:legal-advance` 与 `seat-legal-only:1:targetingRoll:advance-phase:legal-advance`。
- [x] 已继续补一格 `seat-legal-only` failure provenance：strict + emergency playerView 后若仍是 `private-required` blocked，feedback 的 `blockerFingerprint` 现已保留 `blockedKey`，不再只剩粗粒度 `seat-legal-only:${playerId}:${phase}`。
- [x] 已补 `display-only-bonus` 的 runtime feedback direct gate：human `main1` 遗留 AI `pendingBonusDiceSettlement(displayOnly)` 且 `SKIP_BONUS_DICE_REROLL` 失败时，`stateSnapshot/actionLog.blockerFingerprint` 会保留 `display-only-bonus:${playerId}:${phase}:${settlementId}`，不再只靠 candidate 级测试旁证。
- [x] 已补 `response-window` 的 blocked feedback provenance：emergency playerView 后若仍 blocked，且 watchdog 继续 fallback `RESPONSE_PASS` 最终若失败，`stateSnapshot/actionLog.blockerFingerprint` 现也会继承 `blockedKey`，并把 `stale-private-overlay / missing-private-overlay` 直接上报成 `private_overlay_stale / private_overlay_missing`，不再被 revalidate 回粗粒度 `response-window` 或泛化 `no_progress`。
- [x] 已补 `visible-interaction` 的 blocked feedback direct gate：emergency playerView 后若仍 blocked，且 fallback force command 以 `command_failed` 失败时，`stateSnapshot/actionLog.blockerFingerprint` 也会保留旧 `blockedKey`，不再只靠 `currentCandidate.fingerprintHint` 读代码旁证。
- [x] 已补 `visible-interaction` diagnostics gate 的双出口一致性：交互选项/可选性诊断用例现在同时锁住 `stateSnapshot.actionLog.blockerFingerprint`，确认 `dt-test-visible-choice` 不只写进 snapshot，也写进 actionLog。
- [x] 已补 `hidden-interaction` 的 blocked feedback direct gate：emergency playerView 后若仍 blocked，且 fallback force command 以 `command_failed` 失败时，`stateSnapshot/actionLog.blockerFingerprint` 也会保留旧 `blockedKey`，不再只靠 `currentCandidate.fingerprintHint` 读代码旁证。
- [x] 已补 `active-turn` 的 blocked feedback direct gate：emergency playerView 后若仍 blocked，且 fallback force command 以 `command_failed` 失败时，`stateSnapshot/actionLog.blockerFingerprint` 也会保留旧 `blockedKey`，不再只靠 `currentCandidate.fingerprintHint` 读代码旁证。
- [x] 已补 `shared-visible missing-visible-state` 的 feedback direct gate：visible prompt 丢失 live visible state 且 fallback force command 以 `command_failed` 失败时，`stateSnapshot/actionLog.blockerFingerprint` 也会保留旧 `blockedKey`，不再只靠 helper 返回值旁证。
- [x] 已补 `advance_guard_blocked` 的 feedback direct gate：watchdog fallback 到 `ADVANCE_PHASE` 前若发现当前已不是 AI 回合、最终以 `advance_guard_blocked` 失败，`stateSnapshot/actionLog.blockerFingerprint` 也会保留 `legal-action-only:1:main2`，不再只靠 reason 级断言旁证。
- [x] 已纠正 `active-turn legal-action-recovered` 的 direct gate 测试口径：`blockerFingerprint` 真实应保留恢复开始前的 progress marker，而不是最终 `progressMarker`；`online AI watchdog 完成 legal action 恢复后也应写入系统反馈` 与 `online AI watchdog 在 active-turn 的多步 legal action 链中，应只在最终交还回合后上报 resolved` 聚焦复跑已 `2 passed`。
- [x] 已补 `active-turn` 的 sibling-family resolved direct gate：`hasOnlineAiRecoveryResolved()` 现在也有最小直测锁定，`active-turn` 只要同一 AI 还切在 `visible-interaction / response-window / hidden-interaction / response-loop` 之一，就仍不算 resolved。
- [x] 已补 `response-window / response-loop` 到 `hidden-interaction` 的 sibling-family resolved direct gate：此前只锁了切到 `visible-interaction / active-turn` 不算 resolved，现已新增两条最小直测，确认窗口关闭后若同一 AI 已切到 `hidden-interaction`，也必须继续视为 unresolved；与既有 visible/active-turn 邻近矩阵同组复跑 `6 passed`。
- [x] 已补 `compare-roll-choice` 的 confirm 收口 seam：`compare-roll-choice + confirmValue + 无 options` 时 watchdog 现在走 `SYS_INTERACTION_CONFIRM`，不再把本应 `CONFIRM -> RESOLVED` 的 compare-roll 收口误吞成 generic cancel。
- [x] 已补 `compare-roll-choice` 的 runtime drift gate：`compare-roll-choice` 的 sourceId 漂移现在会丢弃旧 tracker，不会把旧 tracker 带进新 compare frame。
- [x] 已补 `compare-roll-choice` 的单选 forced-command seam：单一 enabled option 的 compare-roll 现在走 `SYS_INTERACTION_RESPOND`，不再误吞成 generic cancel。
- [x] 已继续收掉 `scoreOneBase -> onMinionDiscardedFromBase -> reaction frame live validation` 的真实 shared bug：Jumper / Copycat copied Jumper 在普通基地计分弃牌后的 optional recover prompt 不再被后续 `afterScoring` active frame 误校验成“无效的选择”；`time_travelers_jumper` 与 `shapeshifters_copycat` copied Jumper 也已区分“真实进 discard 回手”和“计分清场延迟前仍在基地上从基地回 owner hand”。验证：`yuanhouFactionAbilities.test.ts -t "跳跃者在普通基地计分弃牌后仍可通过 optional reaction 回手|Copycat 复制 Jumper 后在普通基地计分弃牌时仍可代理 optional recover 回手|跳跃者被真实摧毁进入弃牌堆后仍通过 optional reaction 回手|跳跃者在 Tar Pits 首次被真实摧毁并改放牌库底时，不应触发 optional recover reaction|跳跃者在 Temple of Goju 计分后被放牌库底时，不应触发 optional recover reaction|Copycat 复制 Jumper 后被真实摧毁时，应代理其 optional recover trigger 回手"` => `1 file passed, 6 passed`；`baseScoring.test.ts -t "Sleeping Beauty"` => `1 file passed, 1 passed`。这格不外推整个 reaction runtime 或所有 `onMinionDiscardedFromBase` family 已完成。
- [x] 已把 `samurai_honor_the_fallen` 从同族开放候选降为 dedicated green evidence：新增 `scoreOneBase 会让 Honor the Fallen 在基地计分弃牌后按离场随从 LKI 抽牌`，确认普通基地计分弃牌路径会产生 `CARDS_DRAWN.playerId='0'` 且抽到 `honor-draw-a`。验证：`baseScoring.test.ts -t "Honor the Fallen"` => `1 file passed, 1 passed`。
- [x] 已把 `base_sakura_garden` 也从同族开放候选降为 dedicated green evidence：新增 `scoreOneBase 会让 Sakura Garden 在基地计分弃牌后按离场随从 LKI 抽牌`，确认普通基地计分弃牌路径会产生 `CARDS_DRAWN.playerId='0'` 且抽到 `sakura-draw-a`。验证：`baseScoring.test.ts -t "Sakura Garden"` => `1 file passed, 1 passed`。
- [x] 已把 `samurai_samurai_chan` 与 `samurai_bushi` 的 self-source scoring discard 分支也降为 dedicated green evidence：新增 `scoreOneBase 会让 Samurai Chan 自身在基地计分弃牌后按 self-source LKI 抽牌` 与 `scoreOneBase 会让 Bushi 自身在基地计分弃牌后按离场力量 LKI 得分`。验证：`baseScoring.test.ts -t "Samurai Chan|Bushi"` => `1 file passed, 2 passed`。
- [x] 已把 `samurai_shogun` 的 other-friendly scoring discard 分支降为 dedicated green evidence：新增 `scoreOneBase 会让 Shogun 在其他己方随从计分弃牌后获得 1 个力量指示物`，确认基地 0 的己方随从计分弃牌后，基地 1 仍在场的 `samurai_shogun` 会产生 `POWER_COUNTER_ADDED.reason='samurai_shogun'`。验证：focused `1 passed`；同组 `Honor the Fallen|Sakura Garden|Samurai Chan|Shogun|Bushi` => `1 file passed, 5 passed`。
- [x] 已把 `samurai_final_haiku` 的 attached-action scoring discard 分支降为 dedicated green evidence：新增 `scoreOneBase 会让 Final Haiku 在宿主计分弃牌后给其他己方随从 +2 临时力量`，确认宿主计分弃牌后，另一基地己方随从收到 `TEMP_POWER_ADDED.reason='samurai_final_haiku'`，敌方随从不收到。验证：focused `1 passed`；同组 `Honor the Fallen|Sakura Garden|Samurai Chan|Shogun|Final Haiku|Bushi` => `1 file passed, 6 passed`。
- [x] 已把非 Samurai attached-action sibling `vikings_viking_funeral` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Viking Funeral 在宿主计分弃牌后奖励 1VP 并移出该宿主`，确认宿主计分弃牌后产生 `VP_AWARDED.reason='vikings_viking_funeral'` 与 `CARD_REMOVED_FROM_GAME.cardUid='funeral-host-a'`。验证：focused `1 passed`；同组 `Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 7 passed`。
- [x] 已把 discard-zone self-trigger sibling `trickster_gremlin_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Gremlin POD 在被他人控制但归自己拥有时计分弃牌后给拥有者抽牌`，确认 P1 控制但 P0 拥有的 Gremlin POD 计分弃牌后抽牌归 P0，P1 不抽牌。验证：focused `1 passed`；同组 `Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 8 passed`。
- [x] 已把 `giant_ant_worker_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Worker POD 在计分弃牌后提示从弃牌堆打到另一基地`，确认 Worker POD 因计分弃牌时会打开 `giant_ant_worker_pod_replay` prompt，交互归控制者 P0，候选基地包含基地 1 且不包含原计分基地 0。验证：focused `1 passed`；同组 `Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 9 passed`。
- [x] 已把 `frankenstein_igor` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Igor 在计分弃牌后给控制者创建放置指示物目标选择`，确认 Igor 因计分弃牌时会打开 `frankenstein_igor` prompt，交互归控制者 P0，候选包含同基地其它己方随从与其它基地己方随从，且不包含 Igor 自身。验证：focused `1 passed`；同组 `Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 10 passed`。
- [x] 已把 `giant_ants_death_on_six_legs` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Death on Six Legs 在己方随从计分弃牌后获得 1 个力量指示物`，确认 P0 控制的随从计分弃牌时，P0 控制的场上 `Death on Six Legs` 泰坦获得 1 个力量指示物。验证：focused `1 passed`；同组 `Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 11 passed`。
- [x] 已把 `world_champs_bewitched` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Bewitched 在宿主计分弃牌后提示行动拥有者转移附着`，确认普通宿主带 `world_champs_bewitched` 计分弃牌时会打开 `world_champs_bewitched_transfer` prompt，交互归行动拥有者 P0，候选包含其它基地随从 `bewitched-target-a` 且不包含原宿主 `bewitched-host-a`，runtime continuation 保留 `sourceCardUid/sourceDefId/ownerId/triggerMinionUid`。验证：focused `1 passed`；同组 `Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 12 passed`。
- [x] 已把 `samurai_way_of_the_warrior` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Way of the Warrior 标记的随从在计分弃牌后给施放者抽两张`，确认带 `samuraiWayOfTheWarriorDraw*` metadata 的随从在计分弃牌后会给施放者 P0 抽到 `warrior-draw-a/warrior-draw-b`；夹具保留已打出的 `samurai_way_of_the_warrior` 在 P0 discard，避免伪造无来源 metadata。验证：focused `1 passed`；同组 `Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 13 passed`。
- [x] 已把 `world_champs_samurai_chan` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 World Champs Samurai Chan 自身在基地计分弃牌后抽 1 张牌`，确认 `world_champs_samurai_chan` 自身在普通基地计分弃牌后产生 `CARDS_DRAWN.playerId='0'` 且抽到 `wc-chan-draw-a`。验证：focused `1 passed`；同组 `World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 14 passed`。
- [x] 已把 `samurai_final_haiku_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Final Haiku POD 在宿主计分弃牌后给其他己方随从 +2 临时力量`，确认 POD 宿主计分弃牌后，另一基地己方 POD 随从收到 `TEMP_POWER_ADDED.reason='samurai_final_haiku'`，敌方随从不收到。验证：focused `1 passed`；同组 `Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 15 passed`。
- [x] 已把 `samurai_shogun_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Shogun POD 在其他己方 POD 随从计分弃牌后获得 1 个力量指示物`，确认基地 0 的己方 POD 随从计分弃牌后，基地 1 仍在场的 `samurai_shogun_pod` 会产生 `POWER_COUNTER_ADDED.reason='samurai_shogun'`。验证：focused `1 passed`；同组 `Shogun POD|Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 16 passed`。
- [x] 已把 `samurai_honor_the_fallen_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Honor the Fallen POD 在基地计分弃牌后按离场随从 LKI 抽牌`，确认挂有 `samurai_honor_the_fallen_pod` 的基地上，己方 POD 随从计分弃牌后会给控制者抽到 `honor-pod-draw-a`。验证：focused `1 passed`；同组 `Honor the Fallen POD|Shogun POD|Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 17 passed`。
- [x] 已把 `samurai_way_of_the_warrior_pod` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Way of the Warrior POD 标记的随从在计分弃牌后给施放者抽两张`，确认带 POD 版 draw metadata 的随从计分弃牌后会给施放者 P0 抽到 `warrior-pod-draw-a/warrior-pod-draw-b`；夹具保留已打出的 `samurai_way_of_the_warrior_pod` 在 P0 discard，避免伪造无来源 metadata。验证：focused `1 passed`；同组 `Way of the Warrior POD|Honor the Fallen POD|Shogun POD|Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 18 passed`。
- [x] 已把 `shapeshifters_doppelganger` 的 scoreOneBase 入口降为 dedicated green evidence：新增 `scoreOneBase 会让 Doppelganger 在基地计分弃牌后提示从牌库打随从到原基地`，确认普通基地计分弃牌时会打开 `shapeshifters_doppelganger_search` prompt，交互归 P0，`allowedCardUids` 只含 `dopp-candidate-a/dopp-candidate-b` 且带 skip 选项。验证：focused `1 passed`；同组 `Doppelganger|Way of the Warrior POD|Honor the Fallen POD|Shogun POD|Final Haiku POD|World Champs Samurai Chan|Way of the Warrior|Bewitched|Death on Six Legs|Igor|Worker POD|Gremlin POD|Final Haiku|Viking Funeral|Shogun|Honor the Fallen|Sakura Garden|Samurai Chan|Bushi` => `1 file passed, 19 passed`。
- [x] 已补齐 `samurai_samurai_chan_pod` / `samurai_bushi_pod` 的 evidence 落账：这两格此前已有 `baseScoring.test.ts` 真实 scoreOneBase 用例，但 evidence 只写了基础版 self-source。复跑 `baseScoring.test.ts -t "Samurai-Chan POD|武士 POD"` => `1 file passed, 3 passed`，确认 `Samurai-Chan POD` 计分弃牌后抽牌、`Bushi POD` 计分弃牌瞬间有效战力与临时力量均能触发额外 1VP。
- [x] 已完成当前 `onMinionDiscardedFromBase` 注册点的 scoreOneBase/邻近真实入口覆盖审计：24 个注册点已分别落到 `baseScoring.test.ts` direct gate、`yuanhouFactionAbilities.test.ts` 普通基地计分 recover gate、或 `reactionQueueEventPlayerContext.test.ts` eventPlayer gate；本轮补齐 `Doppelganger` 与 `Samurai-Chan POD / Bushi POD` evidence 缺口后，这条支线不再作为下一格优先残口。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected collector eligibility` 真缝：`vampire_dinner_date_pod` 语义是“被附着随从有效力量变为 0 时将其消灭”，旧注册缺 collector 级 `canTrigger`，宿主仍大于 0 时也会排出无效果 mandatory trigger，且 queued owner 默认落到事件玩家。现已在 `src/games/smashup/abilities/vampires.ts` 抽出 `canTriggerVampireDinnerDatePod(ctx)` 并挂到 `vampire_dinner_date_pod.onMinionAffected`，同时补 `playerContext:'sourceController'`。新增 `reactionQueueEventPlayerContext.test.ts` 用例 `collectTriggers onMinionAffected 只应在 Dinner Date POD 宿主有效力量为 0 时入队`，锁定宿主有效力量为 2 时不入队、有效力量为 0 时入队，且 `sourceCardUid='dinner-date-a'`、`sourceControllerId/ownerPlayerId='0'`、`eventPlayerId='1'`。验证：focused `Dinner Date POD` 红灯后转绿；`reactionQueueEventPlayerContext.test.ts` 整文件 => `94 passed`；`vampiresPod.test.ts` 整文件 => `11 passed`。这格不外推所有 `onMinionAffected`、所有 Vampire POD 或 Dinner Date POD 真实 UI 入口。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected collector eligibility` 真缝：`trickster_brownie` 语义是 Brownie 本体被对手卡牌效果影响后，让对手弃两张牌；旧注册缺 collector 级 `canTrigger`，只要场上有 Brownie，同基地任意其它随从被影响也会排出无效果 trigger。现已在 `src/games/smashup/abilities/tricksters.ts` 抽出 `canTriggerTricksterBrownieAffected(ctx)` 并挂到 `trickster_brownie.onMinionAffected`，同时补 `playerContext:'sourceController'`。新增 `reactionQueueEventPlayerContext.test.ts` 用例 `collectTriggers onMinionAffected 只应在 Brownie 本体被对手影响且对手有手牌时入队`，首轮红灯坐实 `robot_microbot` 被影响时仍有 Brownie trigger；修复后只在 `brownie-a` 本体被 P1 影响且 P1 有手牌时入队，queued trigger owner/sourceController 为 P0、eventPlayer 为 P1。验证：focused 红灯后转绿；`reactionQueueEventPlayerContext.test.ts` 整文件 => `95 passed`；`baseFactionOngoing.test.ts -t "trickster_brownie|Brownie|布朗尼"` => `13 passed`。`baseFactionOngoing.test.ts` 整文件当前仍有既有 `trickster_hideout_pod` 红灯，本格不拿该整文件作为收口证据。
- [x] 已继续收掉一条新的 `queued-trigger-runtime-context / reaction-trigger-choice / onMinionAffected sourceController ownership` 真缝：`frankenstein_the_bride` 在己方随从新增 +1 指示物后给该随从控制者抽 1 张，但旧注册没有显式 `playerContext`，P1 给 P0 随从加指示物时 queued trigger 的 `ownerPlayerId` 会错落到事件玩家 P1。现已在 `src/games/smashup/abilities/titans.ts` 抽出 `canTriggerTheBrideOnPowerCounterChanged(ctx)` 并挂到 `frankenstein_the_bride.onMinionAffected`，同时补 `playerContext:'sourceController'`。新增 `reactionQueueEventPlayerContext.test.ts` 用例 `sourceController queued onMinionAffected trigger 仍应把 The Bride 的抽牌结算归给泰坦控制者`，首轮红灯坐实 `ownerPlayerId='1' / playerContext='eventPlayer'`；修复后 owner/sourceController 均为 P0，且 `maybeResolveReactionQueue(...)` 真实产出 `CARDS_DRAWN.playerId='0'`。验证：focused `The Bride 的抽牌|The Bride 的起始` => `2 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `96 passed`；`smashup.smoke.test.ts -t "The Bride|新娘|身体改造"` => `3 passed`。
- [x] 已完成生产侧 `onMinionAffected` 注册块复核：当前注册集中在 `frankenstein_the_bride`、`ignobles_the_hill_that_strolls`、`vampires_ancient_lord`、`trickster_brownie`、`trickster_brownie_pod`、`vampire_dinner_date_pod`、`world_champs_aramis`、`world_champs_diva`。Bride/Brownie/Dinner Date 是本轮新闭合项；Aramis/Diva/Hill/Ancient Lord 已有 dedicated sourceController queue gate；`trickster_brownie_pod.onMinionAffected` 是 no-op 注册并已有 “POD 版不沿用旧版 onMinionAffected 触发” 口径。当前不再把生产 `onMinionAffected` 注册块本身列为优先开放残口。
- [x] 已继续收掉一条新的 `processAffectTriggers / onMinionAffected post-event power projection` 真缝：`vampire_dinner_date_pod` direct collector 已能在宿主当前有效力量为 0 时入队，但真实 `processAffectTriggers()` 在 reduce 当前 power-change event 前收集 trigger，导致宿主从有效力量 1 被本次 `POWER_COUNTER_REMOVED` 降到 0 时旧 `canTrigger` 仍按事前力量 1 判断不入队。现已给 Dinner Date POD eligibility 增加本次 `POWER_COUNTER_ADDED/REMOVED`、`TEMP_POWER_ADDED`、`PERMANENT_POWER_ADDED` 的 projected power 计算。验证：focused 红灯后转绿；`reactionQueueEventPlayerContext.test.ts -t "Dinner Date POD"` => `2 passed`；`reactionQueueEventPlayerContext.test.ts` 整文件 => `97 passed`；`vampiresPod.test.ts` 整文件 => `11 passed`。
- [ ] 下一格继续沿 `ACTION_PLAYED` consumer / detached-owner provenance / ongoing-move owner provenance / queued interaction / extended base side branch / post-process 旁路 / processAffect/processReturn sibling ordering / replacement pendingSave architecture` 筛新的 still-object/shared family：本轮 `princesses_woodland_helpers`、`mythic_greeks_favor_of_dionysus`、`time_travelers_time_walk`、`ghost_make_contact_pod`、`ghosts_creampuff_man`、`itty_critters_rainboroc`、`mermaids_shipwreck_cove`、`trickster_hideout_pod`、`elder_thing_begin_the_summoning_pod` 与 `wizard_mass_enchantment` 已进一步证明当前开放残口并不只在 `ACTION_PLAYED` 本身，还包括“borrowed action 自己回顶/回底/离场/自毁时仍把 owner 写成当前玩家”“borrowed discard/minion shuffle 仍把 `DECK_REORDERED.playerId` 写成当前玩家”与“borrowed ongoing 被移动/换位/真实附着后把 owner 洗成 controller/current player”的 sibling。后续优先复核其它仍会把 self-top/bottom、discard lookup、discard shuffle owner、detach/self-destroy owner、ongoing move owner 绑在 `ctx.playerId` / `sourceControllerId` / 当前玩家区位的分支；同时不要回扫已闭合的 `Woodland Helpers`、`Favor of Dionysus`、`time_travelers_time_walk`、`ghost_make_contact_pod`、`ghosts_creampuff_man`、`itty_critters_rainboroc`、`mermaids_shipwreck_cove`、`trickster_hideout_pod`、`elder_thing_begin_the_summoning_pod`、`wizard_mass_enchantment`、`Fort Titanosaurus`、`Gorgodzolla`、`Happily Ever After`、`Secret Agent`、`Cream Puff Man external discard target-context`、`baseDefId-only discard-base choice interaction footprint`、`base_sheep_shrine`、`base_ninja_dojo`、`Time Is Fleeting + Wormhole` 或旧 transport/waiting overlay 标题。pendingSave 的 docs/sourceId 白名单规范冲突已在 2026-05-23 收口；后续若再碰 pendingSave，除非先设计并验证结构化替代判定，否则不得直接硬删 `PREVENT_DESTROY_SOURCE_IDS`。

## Errors Encountered

| 时间 | 错误 | 处置 |
| --- | --- | --- |
| 2026-05-19 | 当前 worktree 继承的 `task_plan.md` 顶部仍是旧 `shayu` 任务，和活跃 `goal` 一起把“继续”导回历史残项。 | 以本节接管当前 worktree 正式计划入口，并明确 operational target 以长期状态 JSON 为准。 |
| 2026-05-19 | `active-turn legal-action-recovered` 两条 direct gate 旧断言把 `blockerFingerprint` 误写成等于最终 `progressMarker`，造成最新 worktree 看似“又卡红”。 | 改为锁恢复开始前的 `buildAiProgressMarker(match.state)`；聚焦复跑两条相关用例，结果 `2 passed`。 |
| 2026-05-19 | `overlay resync cooldown` 的 `blockedKey 相同但 progressMarker 漂移` 用例第一次挂出 `broadcastState=35`，表面像 runtime 还在循环广播。 | 已确认实现不是根因，问题是完整 watchdog tick 把旁路广播一并计数；现改成 helper 级直测 `maybeTriggerOnlineAiOverlayResync()`，同组复跑 `4 passed`。 |

---

# Task Plan: SmashUp shayu 三派系通用入口矩阵补强与全量重审（2026-05-12）

> 当前正式计划入口。下方旧计划均为历史上下文，不作为本轮任务入口。

## Goal

把“交互入口语义审计”从一句通用原则补强成可执行的通用审计矩阵，并按新矩阵对 SmashUp shayu 三派系（sharks / tornados / mythic_greeks）39 张卡 + 6 张基地做 P0/P1 全量重审；发现问题必须修复或显式登记，旧 evidence 失效结论必须回写。

## Constraints

- 不创建/切换/删除分支或 worktree；在当前工作树既有脏改基础上推进。
- 不把抽样审计说成全量；全量必须有对象清单逐项状态。
- 通用规范只写通用矩阵，不写 shayu / 飞鲨 / 单卡特例。
- 结论按 L1/L2/L3/L4 分层；没有新增 E2E 截图时不得宣称 L3 已补齐。
- 使用 completion guard：`temp/smashup-shayu-full-audit-2026-05-12.json`。

## Acceptance Checklist

- [x] S0 读取规范与项目 skill：game-audit-workflow、add-new-faction、testing-audit、engine-systems、testing-best-practices、automated-testing、data-entry。
- [x] S1 补强 `docs/ai-rules/testing-audit.md`：交互入口语义矩阵、目标归属、数量/可选、动作链、上下文携带、自动执行 vs 玩家选择。
- [x] S2 建立 shayu 39 卡 + 6 基地对象清单，标 L0-L4 与 P0/P1 风险。
- [x] S3 对每个对象做 P0/P1 重审：描述动作链、第一入口、数据字段、UI/validator/handler/reducer 链路、上下文与可选/数量语义。
- [x] S4 修复或登记发现项；同步测试与旧 evidence 回写。
- [x] S5 运行相关验证并更新 completion guard，不满足则不得宣称完成。

## Current Status

- [x] 已确认根 `task_plan.md` 旧当前任务为七大恨 intake，已 completed；本轮在顶部切换为 shayu 全量重审计划并保留历史。
- [x] 已创建 completion guard 状态文件。
- [x] 已读取 OpenSpec 指引：本轮属于现有审计/bug 修复/证据补强，不先创建新 OpenSpec proposal。
- [x] 已补强通用规范、完成全量审计清单与验证。

## Errors Encountered

| 时间 | 错误 | 处置 |
| --- | --- | --- |
| 2026-05-12 | planning-with-files session-catchup 提示原生 Codex session 解析未实现。 | 记录为无可同步上下文，继续按当前对话与项目文件推进。 |

---

# Task Plan: 七大恨新游戏前置 intake 与可行性分析（2026-05-11）

> 当前正式计划入口。下方旧计划均为历史上下文，不作为本轮任务入口。

## Goal

基于 `D:\gongzuo\webgame\gameasset\七大恨 中文mod\七大恨规则.pdf` 与 `D:\gongzuo\webgame\gameasset\七大恨 中文mod\Images`，先完成新游戏前置 intake：把规则 PDF 转成易读 Markdown，把需要用到的图片放入项目正式资源目录并规范命名，随后分析“七大恨”接入本项目的实现可行性与风险；同时记录现有 create-new-game skill 的缺口，形成后续 skill 优化建议。

## Constraints

- 不擅自创建、切换、重建或删除分支；`create-new-game` 的正式建游戏分支要求等待用户明确授权。
- 本轮先做规则/资源/可行性前置，不直接创建完整游戏骨架。
- 主真相源：用户提供的中文规则 PDF 与中文 mod 图片目录。
- 图片正式资源必须遵循 `docs/ai-rules/asset-pipeline.md`：运行时资源落 `public/assets/i18n/zh-CN/<gameId>/...` 或过渡期等价路径，路径语义化，后续代码引用不写 `compressed/`。
- 录入中间产物、OCR/核对图、识别清单放 `temp/`，不混入正式资源树。

## Acceptance Checklist

- [x] S0 规划与规范读取：已读取 AGENTS、OpenSpec、planning-with-files、create-new-game、asset-pipeline、data-entry、temp-files-management。
- [x] S1 规则转档：将 `七大恨规则.pdf` 转为易读 Markdown，落到项目内新游戏 `rule/` 或前置文档目录，并保留转换方式与质量说明。
- [x] S2 素材盘点：列出 `Images` 下素材清单、尺寸、文件类型、疑似用途与命名依据。
- [x] S3 资源入库：把可裁定用途的正式图片复制到项目规范目录，采用语义化命名；不确定用途只登记，不强行命名。
- [x] S4 资源压缩/清单：对正式入库图片执行最小必要压缩或记录阻塞原因。
- [x] S5 可行性分析：基于规则文档与素材盘点分析核心机制、引擎映射、UI/资源复杂度、MVP 切分与风险。
- [x] S6 skill 优化建议：记录 create-new-game 对“PDF 转 MD + 素材 intake + 可行性评估”阶段的可补强点。

## Current Status

- [x] 已确认本轮不创建分支，先执行新游戏前置 intake。
- [x] 已读取项目根 AGENTS 与 OpenSpec 指引。
- [x] 已读取 planning-with-files 与 create-new-game skill。
- [x] 已读取图片资源、数据录入、临时文件管理规范。
- [x] 已完成规则转档核验、素材规范入库、压缩、manifest 校验、R2 上传、远端抽查、可行性分析与 skill 补强。

## Errors Encountered

| 时间 | 错误 | 处置 |
| --- | --- | --- |
| 2026-05-11 | planning-with-files session-catchup 提示原生 Codex session 解析未实现。 | 记录为无可同步上下文，继续按当前对话与项目文件推进。 |

---

# Task Plan: DiceThrone 新增 Treant / Ninja 两个英雄（2026-05-09）

> 当前正式计划入口。下方历史计划来自创建 worktree 时的主线文件，仅保留为历史上下文，不作为本轮任务入口。

## Goal

在独立 worktree `.worktrees/dicethrone-treant-ninja` 中，基于用户提供的两组中文图片素材新增 Dice Throne `treant` 与 `ninja` 两个英雄，完成三方图片规格对比、资源接入、静态数据与必要机制实现、审计文档、测试/E2E、截图与资源链路收口。

## Scope

- 主真相源：
  - `public/assets/i18n/zh-CN/dicethrone/images/treant`
  - `public/assets/i18n/zh-CN/dicethrone/images/ninja`
- 参考对象：成熟旧英雄与新英雄 `gunslinger`，必要时对照 `samurai` / `moon_elf` 等复合升级与 atlas 接线。
- 工作现场：`D:\gongzuo\webgame\BoardGame\.worktrees\dicethrone-treant-ninja`
- 分支状态：detached HEAD，未新建分支。

## Acceptance Checklist

- [x] S0 合同层：锁定两英雄真相源、素材清单、图片规格差异、可复用项/谨慎项、冲突待裁定项。
- [x] S0 裁图层：生成单对象可读裁图/核对图，临时图放 `temp/`，正式资源与核对中间产物分层登记。
- [x] S0 文档层：为两个英雄创建/更新真相源表、录入核对、卡牌录入核对。
- [x] S1 资源层：压缩正式资源，重建 manifest，确认 `compressed/` 和 atlas 引用合同。
- [x] S1 配置层：接入英雄注册、骰面、token、能力、卡牌、critical images、locale。
- [x] S2 机制层：实现无法直接复用的 token / 被动 / 技能 / 卡牌机制，优先复用旧英雄共享逻辑。
- [x] S2 共享契约对比：至少与 `gunslinger` 和一个成熟复合升级英雄做并排核对。
- [x] S3 验证层：补/更新现有测试文件，跑相关 Vitest、eslint/typecheck，必要时跑真实入口 E2E。
- [x] S3 截图层：若涉及 UI/卡图展示，必须实际看截图并写 evidence。
- [x] S4 审计层：在 `evidence/` 落两个英雄审计与端到端证据文档，结论按 L1-L4 分层。
- [x] S4 资源远端层：运行资源上传并抽查代表性 URL；若受环境阻塞，明确列未上传资源与影响。

## Current Status

- [x] 已创建 detached worktree：`.worktrees/dicethrone-treant-ninja`
- [x] 已确认主工作树有大量无关脏改，本轮不在主工作树继续。
- [x] 已把用户给出的 `treant` / `ninja` 图片目录复制进新 worktree。
- [x] 已完成 S0-S4：新增 treant/ninja，完成资源、配置、规则文档、审计证据、测试/E2E、R2 回查。

## Reopened Scope（2026-05-10 用户复盘）

- [x] 重新按 `dicethrone-hero-intake` 新门禁复核，不再把选角 E2E 视为全流程完成。
- [x] 建立 treant/ninja 批次矩阵：数据录入、机制、资源上传、E2E、审计逐格证明。
- [x] 逐项核对两个角色的技能、Token、卡牌是否只有 L1/L2，列出未实现项。
- [x] 修订 evidence，明确哪些是真完成、哪些是 scoped-debt。
- [x] 如果要宣称彻底完成，必须补齐 L2/L3/L4 缺口；否则不得收口。


## Restart Contract（2026-05-10 重来口径）

> 用户明确要求“新增派系是通用 skill，没有就加，给我重来”。本节覆盖上方旧 Closeout Snapshot；旧 `S0-S4 已完成` 只能视为上一轮误收口历史，不作为当前完成证明。

### 新增派系/角色通用 skill

| 项 | 状态 | 证据 |
|---|---|---|
| 项目通用 skill `.windsurf/skills/add-new-faction/SKILL.md` | passed | `PYTHONUTF8=1 python D:\codex-home\skills\.system\skill-creator\scripts\quick_validate.py .windsurf\skills\add-new-faction` -> `Skill is valid!` |
| `data-entry-workflow` 路由到通用新增派系 skill | passed | `.windsurf/skills/data-entry-workflow/SKILL.md` 已包含“通用新增派系 / 新增角色 / 新增英雄”路由 |
| DiceThrone hero intake 门禁补强 | passed | `docs/games/dicethrone/workflows/dicethrone-hero-intake.md` 已增加禁止提前收口、批次矩阵、L0-L4 与资源/E2E/审计门禁 |

### Treant / Ninja 重审批次矩阵（当前真状态）

| objectId | 数据录入 | 资源链 | 机制实现 | 审计 | E2E | 状态 |
|---|---|---|---|---|---|---|
| `treant` | passed | passed | passed | passed | passed | passed |
| `ninja` | passed | passed | passed | passed | passed | passed |

上表已经在 2026-05-10 20:16 +08 全部核销为 `passed`；本轮可以使用“完成/收口”口径，但必须同时引用 evidence、测试命令和截图路径。

### 重审缺口核销结果

以下清单是 2026-05-10 18:49 +08 重新打开时的待审/待修项，20:16 +08 后不再作为阻塞项保留；逐项实现状态、L2/L3 证据与剩余风险以 `evidence/dicethrone/dicethrone-treant-ninja-intake-audit-2026-05-10.md` 为准。

- Treant：`seedling` / `sapling` / `divine` / `life_sap` / `thorn` 已完成机制复核；生命源泉另有真实入口 E2E 截图链证明主阶段奖励骰治疗可触发、可展示、可收口。
- Ninja：`delayed_poison` / `smoke_bomb` / `ninjutsu` 已完成机制复核；忍术另有真实入口 E2E 截图链证明 beforeDamageDealt 奖励骰加伤可触发、可展示、可收口。
- 旧问题“按钮可见但 custom 被动不派发命令”已修在 `src/games/dicethrone/Board.tsx`。
- 旧问题“beforeDamageDealt token 加伤只更新 pendingDamage，不同步 pendingAttack.bonusDamage”已修在 `src/games/dicethrone/domain/reduceCombat.ts`。

## Closeout Snapshot

- 2026-05-10 20:16 +08：按通用新增派系 skill 重来后，Treant / Ninja 的数据录入、资源链、机制 L2、真实入口 E2E、审计 evidence 已全部重新核销为 passed。
- 旧 16:20 收口只证明选角/静态接入，已在 evidence 中明确标记为失效结论。
- 证据文档：`evidence/dicethrone/dicethrone-treant-ninja-intake-audit-2026-05-10.md`。
- 机制 E2E 命令：`PW_PORT=6473 / PW_GAME_SERVER_PORT=20300 / PW_API_SERVER_PORT=21300 / PW_WORKERS=1 npm run test:e2e:ci -- e2e/dicethrone/dicethrone-treant-ninja-mechanics.e2e.ts` -> 4 passed。
- 关键截图：
  - `test-results/evidence-screenshots/dicethrone/dicethrone-treant-ninja-mechanics.e2e/树精生命源泉应在主阶段触发奖励骰治疗并收口/03-life-sap-after-close.png`
  - `test-results/evidence-screenshots/dicethrone/dicethrone-treant-ninja-mechanics.e2e/忍者忍术应在伤害前掷骰加伤并回到可收口状态/02-ninjutsu-bonus-die-overlay.png`
  - `test-results/evidence-screenshots/dicethrone/dicethrone-treant-ninja-mechanics.e2e/忍者忍术应在伤害前掷骰加伤并回到可收口状态/03-ninjutsu-after-bonus-closeout.png`
  - `test-results/evidence-screenshots/dicethrone/dicethrone-treant-ninja-mechanics.e2e/树精木苗树灵两个主阶段按钮应短文案展示并真实结算/01-sapling-short-buttons-before-use.png`
  - `test-results/evidence-screenshots/dicethrone/dicethrone-treant-ninja-mechanics.e2e/忍者忍术6点应弹出分支选择并能施加慢性中毒/02-ninjutsu-6-choice-modal.png`


## Errors Encountered

| 时间 | 错误 | 处置 |
| --- | --- | --- |
| 2026-05-09 | 首次复制素材时用 `Copy-Item -LiteralPath ...\*`，PowerShell 将 `*` 当字面量导致找不到路径。 | 改用 `Copy-Item -Path ...\*` 后复制成功。 |

---

# Task Plan: 线上反馈持续修复（2026-05-03）

> 来源：线上反馈源（生产 API + 生产 Mongo）
> 说明：本节是当前正式计划入口；下方旧任务计划仅保留为历史记录，不再作为本轮任务入口。

## Goal
> 持续清空当前线上 `open` 反馈，默认以**人类反馈优先**为主线推进；系统自动反馈只作为补现场、补根因或止血支线处理。对仍在持续刷新的 watchdog，可并行止血，但不得再覆盖人类反馈的主优先级。

## Priority Rule

- [x] 已按 2026-05-05 新口径更新本任务优先级
  - 默认顺序：`人类反馈 > 系统自动反馈`
  - `watchdog` / `unsatisfiable-interaction-auto-skipped` / `force-end-turn-*` 仅在两类情况下提前处理：
    - 为某条人类反馈补现场或补根因；
    - 正在持续制造新故障、刷屏或资源风险，需要并行止血。
  - 后续汇报必须区分“人类反馈主线”与“系统反馈止血支线”，不得再混成单一优先级口径。

## Current Snapshot

- [x] 2026-05-10 命令执行异常全链路已完成本地修复与聚焦验证
  - 后端 batch 失败不再固定折叠为 `command_failed`，会透传领域错误码或 `pipeline_error: <message>`
  - 前端不再静默 `command_failed`，非 `stale_state` 的 batch rejection 会进入错误展示路径
  - 已补证据：`evidence/transport-command-error-full-chain-fix-2026-05-10.md`
  - 已通过聚焦 transport / MatchRoom helper 测试与 `npm run typecheck`
  - `长舟` 已按用户澄清重新定位为 SmashUp `base_drakkar`（德拉卡尔号 / Drakkar），不是 SummonerWars；根因是 2026-05-08 引入的运行时 `effectContract` 漏 `playLimits` / `discardState` / `opensInteraction` 后误拦截合法基地能力
  - 已补 `PLAY_MINION -> base_drakkar` 真实触发链回归，聚焦 `base_drakkar` 测试 4 passed
- [x] 审计流程已按“执行层级不够深”的复盘结论升级
  - 已更新 `docs/ai-rules/testing-audit.md`，新增“深度审计流程（强制）”
  - 已把对象清单、完整链路、真实入口、共享根因扩审、旧结论失效回写，改成统一深审门禁
  - 已明确把 `D37` 与 `D40` 标为本轮漏审复盘中的高风险专项
- [x] 生产反馈真源已恢复可读
  - 2026-05-03 生产 `Mongo` 因根盘打满 + `FTDC diagnostic.data` 异常重启，导致 `/admin/feedback` 返回 `500`
  - 已截断 `boardgame-game-server` 的 `13G` Docker 日志，根盘从 `100%` 降到 `68%`
  - 已确认 `boardgame-mongodb` 恢复为正常启动，`GET /admin/feedback?status=open` 恢复可读
- [x] 当前线上盘面已快照到本地
  - `temp/feedback-online/current-open-20260503.json`
  - `temp/feedback-online/current-in-progress-20260503.json`
- [x] `splendor` watchdog 本地止血补丁已完成并通过最小回归
  - `src/engine/transport/onlineAiRecovery.ts` / `src/engine/transport/server.ts`
  - 已验证：`splendor` 不再生成/执行裸 `ADVANCE_PHASE` recovery，manifest 明确禁用 AI 时 watchdog 会忽略残留 AI seat metadata
- [x] `dicethrone` 当前 watchdog / defensiveRoll 主链已完成本地聚焦验证
  - 已通过：`basic-commands-coverage`、`response-window-interaction-lock`、`flow.test.ts` 中 `targetingRoll / defensive / displayOnly / bonus` 相关聚焦用例
- [x] `smashup` 当前 `visible-interaction` / `scoreBases` 主链已完成本地聚焦验证
  - 已通过：transport `visible-interaction / recover-interaction` 相关回归 + `scoreBases-auto-continue`
- [x] `69f7ac9d...` 对应的 `smashup_reaction_choose` 重复 special 候选已完成本地最小修复验证
  - 已定位线上快照特征：同一 prompt 中重复出现 `activate_special:titan:titan_2_wizards_arcane_protector:3`
  - 已在 `reactionSession` 增加按 `option.id / reaction value` 去重，并补 `scoreBases-auto-continue` 三条聚焦回归通过
  - 已补最小兼容修复：`src/games/smashup/abilities/innsmouth.ts` / `e2e/src/games/smashup/abilities/innsmouth.ts` 缺失 `registerInteractionHandler` import，修复后 transport 聚焦套件可再次编译
- [x] `smashup` watchdog transport 闭环证明已补齐
  - 已新增并跑通：`src/engine/transport/__tests__/server.test.ts` 中 “`smashup` 持久化 stale reaction choice 走 watchdog 恢复时，不应落成 `blocker_persisted`”
  - 2026-05-04 已再次复跑通过：`stale reaction choice` / `visible-interaction action` / `follow-up advance` 三条 watchdog 聚焦用例
- [x] `splendor` 线上 orphan watchdog 已完成生产止血
  - 先确认 `/internal/rooms` 已为空但 `boardgame-game-server` 单进程仍持续对 `Nh_5xVWO0km` 执行 `ADVANCE_PHASE -> unknownCommand`
  - 已执行最小生产操作：重启 `boardgame-game-server`
  - 复核：`69f6c4bc9ec13b96d710e10d` 停在 `occurrenceCount = 417` / `lastOccurredAt = 2026-05-03T17:40:12.626Z`，重启后 1 分钟日志不再出现该 `matchID`
- [x] `69f5be8c9ec13b96d710baa4` 已完成线上状态回写
  - 2026-05-04 生产 Mongo 直查先确认该条仍为 `open`，且现场仍对应 human `main1` 残留 AI 枪手 `displayOnly` 奖励骰孤儿态
  - 已按现有 transport/watchdog 修复证据执行最小回写：`matched=1`、`modified=1`
  - 回写后复核：`temp/feedback-online/post-69f5be-resolved-summary-20260504.json` 显示该条已为 `resolved`，当前 `openTotal = 20`，`dicethrone|feedback-modal` 从 `7` 降到 `6`
- [x] `69f7ac9d9ec13b96d710fded` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条对应 `smashup_reaction_choose` 中重复的 `arcane protector` special 候选；本地 runtime + watchdog 聚焦回归已通过
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
  - 回写后复核：当前 `openTotal = 19`，`smashup|online-ai-watchdog` 从 `4` 降到 `3`
- [x] `69f4acdf9ec13b96d7109f30` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条用户反馈“头晕目眩无法使用”；现场权威态显示 Barbarian 在 `main2` 手里持有 `card-dizzy`，但攻击后响应链未被用户正常使用
  - 本地已有 `card-dizzy` 的领域回归与真实 E2E 证据：攻击结算后 `afterAttackResolved` 响应窗真实出现，`card-dizzy` 可打出并对目标施加 `Concussion`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] `69f5c17f9ec13b96d710bb03` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条属于 `smashup_reaction_choose` 的 `scoreBases` / `visible-interaction:recover-interaction:blocker_persisted` 聚合项
  - 本地已有 transport 闭环补测，证明持久化 stale reaction choice 走 watchdog 恢复时会先按当前 live 语义收口，不再落成 `blocker_persisted`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] `69f423585cacc4e6b5cdbdbf` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条是 `69f5c17f...` 的更早同类 `scoreBases` / `smashup_reaction_choose` 聚合项
  - 2026-05-04 按同一 transport/runtime 证据链通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 新一轮回写后盘面已降到 `openTotal = 16`
  - 聚类更新为：`dicethrone|feedback-modal = 5`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`smashup|online-ai-watchdog = 1`、`splendor|online-ai-watchdog = 1`
- [x] `69f479c69ec13b96d71099e3` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条是最后 1 条 `smashup|online-ai-watchdog open`，根因不是 `scoreBases` stale reaction，而是 `endTurn` mandatory 顺序交互收口后，watchdog 没把 SmashUp `endTurn` 纳入 follow-up `ADVANCE_PHASE` fallback
  - 已补本地 transport 修复：`src/engine/transport/server.ts` 允许 SmashUp `endTurn` 在 legal action 耗尽后继续 fallback `ADVANCE_PHASE`
  - 已补并跑通聚焦回归：`watchdog falls back to first trigger respond for smashup onTurnEnd mandatory reaction ordering`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 15`
  - 聚类更新为：`dicethrone|feedback-modal = 5`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`splendor|online-ai-watchdog = 1`
- [x] `69f21b05ab54eadcc2bb2b9e` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条现场不是泛化 AI 发呆，而是 DiceThrone 枪手 `targetingRoll -> Loaded token -> bonus die` 收口链脱节：末尾事件已走到 `BONUS_DICE_REROLL_REQUESTED`，但系统最终落成 `sys.phase=targetingRoll`、`flowHalted=true`、`interaction.queue=[]`
  - 根因簇与已回写 `69f5be8c...` 的 `displayOnly / pendingBonusDiceSettlement / hidden response` 修复链一致，也共享 `69f04210...` 的 `targetingRoll` 推进缺口
  - 已复跑并通过本地聚焦回归：`src/games/dicethrone/__tests__/flow.test.ts` 4 条 `targetingRoll` 用例、`src/engine/transport/__tests__/server.test.ts` 5 条 `displayOnly / hidden interaction / watchdog` 用例
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 14`
  - 聚类更新为：`dicethrone|feedback-modal = 4`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`splendor|online-ai-watchdog = 1`
- [x] `69f2a81c5cacc4e6b5cdb4e5` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条生产快照并非卡死终态，而是已经完整收口到 `main2`：末尾事件顺序为 `TOKEN_RESPONSE_REQUESTED -> TOKEN_USED -> TOKEN_RESPONSE_CLOSED -> ATTACK_RESOLVED -> SYS_PHASE_CHANGED(defensiveRoll -> main2)`
  - 终态同时满足：`flowHalted=false`、`interaction.queue=[]`、`pendingAttack=null`
  - 该条与 DiceThrone `pendingInteractionId / hidden response / token response` 修复簇一致，按已修未回写处理
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 13`
  - 聚类更新为：`dicethrone|feedback-modal = 3`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`splendor|online-ai-watchdog = 1`
- [x] `69f31c695cacc4e6b5cdb992` 已按“本地已修即 resolved”口径完成线上状态回写
  - 项目现有专项审计已直接点名同一时间戳、同一反馈原文“再来点这张卡自己整个回合都用不了”
  - 根因是 4 人 `targetingRoll` 自动目标窗口里攻击修正卡误死绑 `pendingAttack.defenderId`
  - 2026-05-04 已复跑并通过聚焦回归：`攻击修正卡可在 defenderId 写回前直接结算到自动目标`、`Loaded token 的奖励骰特写应命中自动目标`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 12`
  - 聚类更新为：`dicethrone|feedback-modal = 2`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`splendor|online-ai-watchdog = 1`
- [x] `69f18ca4ab54eadcc2bb2322` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上现场仍处于 `defensiveRoll`，且底层骰子数据存在；问题位点对齐到共享骰面可见性修复簇 `69cba605...`
  - 已复跑共享 fallback 单测通过；fresh E2E 尝试因测试 runtime 启动失败未进入业务断言
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 新一轮回写后盘面已降到 `openTotal = 11`
  - 聚类更新为：`dicethrone|feedback-modal = 1`、`dicethrone|online-ai-watchdog = 2`、`smashup|feedback-modal = 7`、`splendor|online-ai-watchdog = 1`
- [x] `69f1978dab54eadcc2bb24b0` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条缺少 `stateSnapshot` / `errorContext`，按明确推断并入同日 DiceThrone 全局 HUD 加载失败簇 `69f1f938...` / `69f1f943...`
  - 已重跑同簇本地验证：`chatSelectionLogic.test.ts` 14 通过，`npm run build` 成功
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 10`
  - 聚类更新为：`smashup|feedback-modal = 7`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - `dicethrone|feedback-modal` 已清零
- [x] `69f27faaab54eadcc2bb2c77` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`蒸汽朋克卡牌差分机可以无限抽牌`
  - 根因不是 `Difference Engine` 自身递归，而是 `endTurn` 恢复态再次重复 `collectTriggers('onTurnEnd')`，把同一帧 `turn-end:1:9:0` trigger 重新入队
  - 已补本地修复：`src/games/smashup/domain/index.ts` 为 `from === 'endTurn'` 的恢复态加闸，避免收口后再次重排同一组 `onTurnEnd` trigger
  - 已复跑并通过：`turnCycle.test.ts` 中新增最小复现 + `expansionOngoing.test.ts` 中 `steampunk_difference_engine` 聚焦回归
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 9`
  - 聚类更新为：`smashup|feedback-modal = 6`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch9.json`
- [x] `69f27a5dab54eadcc2bb2c75` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`因为忍者侍从打出的随从无法触发打出效果`
  - 根因不是 `ninja_acolyte_play` 没产出 `MINION_PLAYED`，而是 `afterEvents` 轮里产出的 `MINION_PLAYED` 在 `postProcessSystemEvents()` 触发 `onPlay` 前还没先 reduce 进临时 `core`，导致 `cowboys_gunfighter` 看不到自己已在场上，决斗交互直接短路
  - 已补本地修复：`src/games/smashup/domain/index.ts` 先把该 `MINION_PLAYED` 临时 reduce 到 `tempCore`，再触发 `fireMinionPlayedTriggers()`
  - 已复跑并通过：`baseFactionOngoing.test.ts` 新增最小回归 + `newFactionAbilities.test.ts` 枪手原始 `onPlay` 聚焦回归
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 8`
  - 聚类更新为：`smashup|feedback-modal = 5`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch10.json`
- [x] `69f385d75cacc4e6b5cdbd4a` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`大杀四方  小妖精的泰坦效果没有触发  效果是触发有或者的效果时  一回合一次能两个效果全部触发   但我只能选择一个触发`
  - 当前仓库已有与该反馈直接同构的精确回归：`fairies_puck 在丛林之灵在场时会先执行已选分支，再给剩余分支与跳过`
  - 本轮已复跑并通过：`newFactionAbilities.test.ts` 的 `Puck + Spirit of the Forest` 聚焦回归，以及 `commandsValidation.test.ts` 的 Titan 额度守门回归
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 7`
  - 聚类更新为：`smashup|feedback-modal = 4`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch11.json`
- [x] `69f544f99ec13b96d710ae00` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`为什么出现了选择反应，然后选择轮回者又没效果，然后之前还有选择名人堂和大法师结算顺序，有什么意义`
  - 线上当前权威态已显示《轮回者》最终确实埋进《名人堂》下方，且链路已收口；仓库现有 E2E 证据也明确说明《轮回者》打出后先进入 `smashup_reaction_choose` 再收口是当前真实语义
  - 关于《名人堂 + 大法师》的另一半诉求，仓库已有 `archmageE2E` 精确回归证明应自动收口，不弹无意义排序交互
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 6`
  - 聚类更新为：`smashup|feedback-modal = 3`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch12.json`
- [x] `69f387a35cacc4e6b5cdbd4c` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`按效果我应该加2战力  而不是减2`
  - 线上当前权威态显示：`fairies_tinx` 当前控制者是 `0`，其身上的《雏菊花环 / Daisy Chain》拥有者是 `2`
  - 当前仓库中英文本地化文案与 `ongoing_modifiers.ts` 现有实现都明确要求：`ownerId === controller` 才是 `+2`，否则就是 `-2`
  - 本条不是“实现把正负号写反了”，而是用户把附着牌拥有者与当前随从控制者的关系看反了；本轮无需改代码
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 5`
  - 聚类更新为：`smashup|feedback-modal = 2`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch13.json`
- [x] `69f01fd49b68d90ee983669d` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`没法选择打出斯芬克斯`
  - 线上当前权威态不是“系统没给可选目标”，而是已经进入 `titan_sphinx_start_turn` 真实交互；当前候选位点在基地下方埋葬牌区域，不是单独一个 “Sphinx” 按钮
  - 本轮已复跑并通过：`src/games/smashup/__tests__/smashup.smoke.test.ts` 中 `狮身人面像会在你的回合开始时创建回收埋葬牌并进场的交互|狮身人面像在其所在基地计分后会创建回收该基地埋葬牌的交互`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 4`
  - 聚类更新为：`smashup|feedback-modal = 1`、`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch14.json`
- [x] `69f5469a9ec13b96d710ae26` 已按“本地已修即 resolved”口径完成线上状态回写
  - 线上反馈原文：`着魔没效果，目标随从没有附加行动卡`
  - 线上 action log 已直接记录多次《着魔》真实附着：`附加持续战术： 着魔 -> c24 / c6`
  - 当前终态看不到宿主身上仍挂着《着魔》，是因为链路已经继续推进到宿主与《着魔》都离场后的更后拍，不等于前面没有附着成功
  - 本轮已复跑并通过：`src/games/smashup/__tests__/newFactionAbilities.test.ts` 中 `world_champs_bewitched 离场转移交互可把持续行动从弃牌堆重新附着`
  - 2026-05-04 通过生产 Mongo 回写：`matched=1`、`modified=1`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 3`
  - 聚类更新为：`dicethrone|online-ai-watchdog = 2`、`splendor|online-ai-watchdog = 1`
  - `smashup|feedback-modal` 已清零
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch15.json`
- [x] `69f471da9ec13b96d7109902`、`69f73be49ec13b96d710f1c2` 已按“本地已修即 resolved”口径完成线上状态回写
  - 两条都是同一类 DiceThrone watchdog 系统单：`force-end-turn-failed active-turn-legal-only:follow-up-advance:legal_action_unavailable`
  - 线上当前只剩 watchdog 聚合摘要，已无可继续复核的真实残局；当前 `occurrenceCount` 分别停在 `2563` 与 `2`
  - 本轮 fresh transport 聚焦回归已通过：
    - `DiceThrone 非战斗阶段遗留 displayOnly 奖励骰时，应直接代 AI 收口而不是放任残留`
    - `dicethrone: human main1 遗留 AI displayOnly pendingBonusDiceSettlement 时，watchdog 应直接替 AI 确认收口`
    - `online AI watchdog 在 pendingInteractionId 锁住 response window 时，应优先执行 hidden interaction 收口`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 1`
  - 聚类更新为：`splendor|online-ai-watchdog = 1`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch16.json`
- [x] `69f6c4bc9ec13b96d710e10d` 已按“本地已修即 resolved”口径完成线上状态回写
  - 该条是本轮最早优先止血的 Splendor watchdog 聚合项：`force-end-turn-failed active-turn:follow-up-advance:command_failed`
  - 当前本地修复已明确覆盖：Splendor 不再生成裸 `ADVANCE_PHASE` fallback，且 manifest `localAi=false` 时 watchdog 会忽略残留 AI seat metadata
  - 本轮 fresh 聚焦回归已通过：
    - `Splendor 即使残留了 AI seat metadata，也不得生成裸 ADVANCE_PHASE fallback`
    - `online AI watchdog 对 manifest 明确禁用 AI 的 splendor 应忽略残留 seatControllers`
- [x] 2026-05-04 最新盘面已降到 `openTotal = 0`
  - `inProgressTotal = 0`
  - 聚类已清空：`{}`
  - 最新快照已落盘：`temp/feedback-online/current-open-20260504-after-batch17.json`
- [x] 当前 `open` 反馈 20 条全部完成分类
- [x] 当前仍在刷新的 watchdog 问题完成止血
- [x] 用户反馈逐条修复、验证、留证并回写状态

## Phases

- [x] **Phase 0: 恢复线上反馈源**
  - [x] 读取生产环境入口与反馈规则
  - [x] 通过 SSH / 生产容器确认反馈源异常根因
  - [x] 恢复 `Mongo` 与 `/admin/feedback` 可读性
- [x] **Phase 1: 线上 open 盘面收敛**
  - [x] 拉取 `open / in_progress` 最新快照
  - [x] 生成去重后的问题簇与优先级
  - [x] 把“重复 watchdog 聚合项 / 真正用户反馈”拆开处理
- [ ] **Phase 2: 生产止血**
  - [x] 本地修复 `splendor` watchdog `command_failed` 死循环，避免再生成裸 `ADVANCE_PHASE`
  - [x] 本地验证 `dicethrone` watchdog `legal_action_unavailable` / 防御窗口链路主路径
  - [x] 本地验证 `smashup` watchdog `visible-interaction` 主路径
  - [x] 补齐 `smashup` transport 闭环测试，证明持久化 stale `smashup_reaction_choose` 不会再落成 `blocker_persisted`
  - [x] 为 `69f7ac9d...` 补 `reaction option` 去重与 stale special 正规化回归，锁定 `smashup_reaction_choose` 重复 special 候选不再原样外露
  - [x] 通过重启 `boardgame-game-server` 清掉生产 orphan room，确认 `splendor` 聚合项停止新增
  - [x] 评估并执行最小风险热补发布路径：在远端源码仓库同步 `engine/transport` 修复与最小依赖，借 `Node 24` 容器编出 `temp/prod-bundles/game/server.mjs`
  - [x] 将热补 bundle 覆盖到生产 `boardgame-game-server:/app/server.mjs` 并重启复核，确认 `/health` 正常且 `cWGQSaUXt1B` 不再继续刷日志
  - [x] 当前任务口径下已完成止血与反馈清盘；正式镜像发布路径保留为后续非阻塞事项
- [x] **Phase 3: 用户反馈逐条修复**
  - [x] Dice Throne `feedback-modal`
  - [x] Smash Up 2 条 `feedback-modal`
  - [x] 与 watchdog 重复描述的用户反馈合并验证，避免重复劳动
- [x] **Phase 4: 验证、证据、回写**
  - [x] 每个已修项补对应测试 / E2E / 证据文档
  - [x] 线上反馈状态回写为 `resolved` / `closed`
  - [x] 复查是否还有新增 `open` 项在继续产生

## Priority Queue

1. 当前 open / in_progress 已清零
   - 最新快照：`temp/feedback-online/post-20260504-resolved-batch-17-summary.json`
2. 若后续需要继续推进
   - 可把 Splendor 热补进一步收敛到正式镜像发布路径，但这不是本轮 `resolved=本地已修好` 口径的阻塞项

## Constraints

- 当前工作区已存在大量未提交改动，默认视为既有工作基线；修复线上反馈时不得回滚或覆盖这些改动。
- `C:\Users\zhuagenbao\.codex\.omx\ralph-loop.local.md` 当前被另一条长期任务占用；本任务改用仓库计划文件 + 独立 JSON state 持续推进，不抢占现有 loop。
- 当前工作区包含大量并行 dirty 改动；任何生产发布前都必须先确认不会把未验证的无关改动一并带上生产。

# Task Plan: Smash Up Oops 四派系接入与玩法实施

## Addendum（2026-04-07）：Android 本地素材包图片加载故障

### Goal
> 修复 App 端“素材包已下载但进入游戏后图片仍全部加载中”的问题，确保前端能在未走大厅包管理 hook 的情况下接住已安装游戏包，并且不会把 Android `/_capacitor_file_/...` 本地路径误套进开发态图片 fetch/blob workaround。

### Phase

- [x] **Phase A: 链路排查与根因确认**
  - [x] 复核原生安装目录、前端 asset override 注入点、MatchRoom 关键图片加载链路
  - [x] 确认启动期 hydration 会跳过“未预注册 fallbackState 的已安装包”
  - [x] 确认 `OptimizedImage` 会把 `/_capacitor_file_/...` 本地包路径误走开发态 `fetch -> blob` workaround

- [x] **Phase B: 修复与回归**
  - [x] 修复 `hydrateInstalledNativeGamePackages()` 对已安装包的兜底 hydration
  - [x] 收窄 `OptimizedImage` 的 blob-fetch workaround，只保留开发态 public `/assets/...`
  - [x] 补定向测试并完成 eslint / vitest 校验

## Goal
> 分两阶段完成 Smash Up `Oops, You Did It Again` 四个派系（埃及、牛仔、武士、维京人）的完整交付：先完成图片 intake、可复刻工作流与静态接入；再按 `Ancient Egyptians → Vikings → Cowboys → Samurai` 的顺序逐派系实施正式玩法、补齐 UI、新交互类型 E2E、统一审计与证据留档。

## Phases

- [x] **Phase 1: 发现与设计（intake）**
  - [x] 阅读 AGENTS、OpenSpec、资产/录入/测试/审计规范
  - [x] 创建独立 worktree 与任务分支
  - [x] 盘点现有 Smash Up 图片接入链路、脚本、数据结构与目标素材
  - [x] 创建 OpenSpec proposal/tasks/design/spec delta

- [x] **Phase 2: 资产处理与录入（intake）**
  - [x] 锁定权威来源与图片清单，建立 Markdown 核对契约
  - [x] 完成图片压缩、图集/切片配置与资源落盘
  - [x] 完成 i18n / 静态数据 / atlas / faction metadata 的同步录入
  - [x] 沉淀“给一批图片即可录入”的复刻工作流文档

- [x] **Phase 3: 审计与验证（intake）**
  - [x] 对照描述、资源路径、加载链路做 intake 审计
  - [x] 运行相关 Vitest / 审计脚本
  - [x] 编写并运行相关 E2E，用截图留证
  - [x] 汇总 evidence、结果与残留风险

- [x] **Phase 4: 玩法提案与实施设计（gameplay）**
  - [x] 创建 `add-smashup-oops-faction-gameplay` OpenSpec 变更
  - [x] 明确用户要求的实施顺序：逐派系实现，全部完成后统一审计与 E2E
  - [x] 将 bury UI 与新交互类型纳入正式 scope
  - [x] 运行 `openspec validate add-smashup-oops-faction-gameplay --strict --no-interactive`
  - [x] 等待用户确认 proposal 后进入 `Ancient Egyptians`

- [x] **Phase 5: Ancient Egyptians**
  - [x] 补齐 card defs 元数据与 `abilityTags`
  - [x] 实现埋葬、翻开、替代去向与相关 base/action/minion ability
  - [x] 补齐 owner-visible bury UI 与对手隐藏占位
  - [x] 补领域测试与统一 E2E 证据收口

- [x] **Phase 6: Vikings**
  - [x] 按官方规则书 / Fandom 口径修正 defs、locale 与 ability metadata
  - [x] 实现 deck-top / discard / steal / extra-action 联动与相关基地能力
  - [x] 补领域测试并完成增量门禁验证
  - [x] 统一 E2E 与更严格语义收口已在四派系统一审计阶段完成

- [x] **Phase 7: Cowboys**
  - [x] 实现官方 duel 内核、move / destroy / ongoing draw 与相关 metadata
  - [x] 补决斗/目标选择最小交互断言
  - [x] 补完整 duel 浏览器 E2E 与证据收口

- [x] **Phase 8: Samurai**
  - [x] 按官方规则书 / Fandom 口径修正 defs、locale 与 ability metadata
  - [x] 实现 honor / duel / destroy / temporary-buff / ongoing draw 与相关基地能力
  - [x] Samurai 专项浏览器 E2E、临时触发精细语义与更严格审计已在统一审计阶段完成

- [x] **Phase 9: 统一审计与收尾**
  - [x] 四派系完成后再统一做 gameplay 审计
  - [x] 运行相关 Vitest / typecheck / OpenSpec 校验
  - [x] 运行覆盖新交互类型的 E2E 并留证
  - [x] 汇总最终 evidence、残留风险与后续扩展点

## Technical Decisions
| Decision | Rationale | Status |
| :--- | :--- | :--- |
| 使用独立 worktree `feat/smashup-base-faction-assets` | 根工作区已有并行任务与规划文件，隔离当前任务避免串改 | Approved |
| 使用 OpenSpec + planning-with-files 双轨记录 | 本次既要落地实现，也要沉淀可复刻流程和验收证据 | Approved |
| 以用户提供图片作为当前任务的直接权威来源 | 符合数据录入规范第 3 优先级，可直接用于资源与索引录入 | Approved |
| Smash Up 规则文本与审计必须走 Wiki 爬虫 | 项目专用强制规范，不能只凭图片或记忆录入 | Approved |
| 本轮 scope 以 intake/静态接入为准 | 用户要求整条资源接入链路，但 OpenSpec 已收束为图片、atlas、静态数据、文档、测试、E2E；不在本 change 内补完四派系完整 gameplay ability | Approved |
| `aiji.png` 按 `7x7`、`aiji_base.png` 按 `2x4` row-major 切片 | 已通过直接看图确认 48 张卡 + 1 尾格、8 张基地；后续 atlas/index 以此为唯一切片基准 | Approved |
| 武士基地 defId 使用 canonical 英文名，图面英文差异写入证据文档 | 图面为 `Kyuden Konbini / Sakura Shigemi`，TTS / Wiki canonical 为 `Shogun's Palace / Sakura Garden`；运行时名称与来源说明必须分离 | Approved |
| 先完整录入 locale 文本，再最小化卡牌结构标签 | 为避免把“未实现玩法”误录成“已实现 ability”，本轮卡牌 defs 仅承载图片、数量、力量、所属派系与最小结构，详细文本放入 locale | Approved |
| gameplay 以独立 OpenSpec change 推进，而不与 intake 混写 | intake 已完成并可单独验收；玩法补完涉及新交互类型、UI 与审计范围，必须单独建模 | Approved |
| gameplay 实施顺序固定为 `Ancient Egyptians → Vikings → Cowboys → Samurai` | 先打通 bury 主链路与 UI，再做 duel / movement / replacement，更容易收敛和审计 | Approved |
| bury UI 必须纳入 Ancient Egyptians 第一波范围 | 用户已指出吸血鬼 pod 时 bury 体系只有领域逻辑，没有正式 UI；若继续只做逻辑会重复留下未完成实现 | Approved |

## Critical Errors / Blockers
| Error | Impact | Resolution |
| :--- | :--- | :--- |
| 根工作区 `task_plan.md/findings.md/progress.md` 已服务其他任务 | 不能在原工作区继续维护本次计划 | 新建独立 worktree 承载本任务 |

## Addendum（2026-04-22）：lane-S2R SmashUp 卡牌效果/文本偏差反馈修复

### Goal
> 核对并最小修复 7 条线上 human open 反馈：世界冠军/美人鱼效果、436-1337工厂计分、疯狂山脉抽牌、缅怀先祖、天守阁决斗、武士进弃牌堆加攻击力链路；补测试、运行验证，并产出 vidence/smashup/2026-04-22 逐条证据。

### Phase
- [x] Phase A: 读取规范、锁权威基线与现有实现
- [x] Phase B: 最小修复反馈相关实现与文本
- [x] Phase C: 补现有测试文件中的回归用例并运行验证
- [x] Phase D: 写 evidence/smashup/2026-04-22 逐条结论与最终汇报

### 2026-04-30 复核结论
- 本 Addendum 实际已完成，原未勾选属于 planning 回填遗漏，不再代表“仍未做完”。
- 对应证据并非只落在单一 `evidence/smashup/2026-04-22/*` 路径，而是分布在：
  - `evidence/feedback-closeout/smashup-human-open14-closeout-2026-04-22.md`
  - `evidence/smashup/smashup-feedback-69e61a97-world-champs-card-index-fix-2026-04-25.md`
  - `evidence/smashup/smashup-10th-anniversary-factions-audit-20260419.md`
  - `evidence/smashup/smashup-10th-anniversary-final-closeout-20260419.md`
- 其中 `69e61a97` 旧关闭结论曾在 2026-04-25 被判定失效，但同日已按“世界冠军 cards7 图集索引错位”根因重新修复并补齐新证据；截至 2026-04-30，lane-S2R 范围内 7 条反馈已具备重新收口依据。

### Scope Control
- 只改 SmashUp 反馈相关文件和 evidence。
- 不触碰当前工作区已有的非本轮改动；已发现 src/games/smashup/domain/index.ts 与 src/games/smashup/__tests__/smashup.smoke.test.ts 存在他人改动，本轮除非必要不修改。

## Addendum（2026-04-22）：SmashUp 10 周年三派系审计复审

### Goal
> 持续验证 `mermaids / skeletons / world_champs` 三派系在当前主线上的实现稳定性，并补齐审计维度（D1-D49）与横幅统一样式证据，确保“实施中”文案与样式收敛后无回归。

### Phase
- [x] 复跑三派系能力与审计门禁（newFactionAbilities + 4 个 audit suite）
- [x] 复跑三派系统一斜向横幅 E2E 并更新截图证据
- [x] 删除中英文 locale 里的 `faction_implementation_in_progress_hint`，只保留“实施中”主文案
- [x] 在 `evidence/smashup/smashup-10th-anniversary-factions-audit-20260419.md` 补齐 D1-D49 维度
- [x] 按“配置直通 / 新机制 / 新 UI-E2E”补齐主回归文件三派系能力覆盖缺口（静态比对为 0）
- [x] 回写通用 workflow：新增 `targetType: 'generic'` 双登记门禁（实现 + 审计理由）避免后续派系重复踩坑
- [x] 2026-04-24 再次复跑并同步最新口径：`newFactionAbilities = 168 passed / 1 skipped`、4 审计套件全绿、`smashup.e2e.ts = 3 passed`、横幅截图时间更新为 `2026-04-24 09:08`
- [x] 2026-04-24 追加静态覆盖复核：`registerAbility` 对照 `newFactionAbilities.test.ts`，三派系总计 `40` 条能力、缺口 `0`
- [x] 2026-04-24 复跑 OpenSpec + R2 回查：`openspec validate add-smashup-oops-faction-gameplay` 通过，`wangling.webp / wangling_base.webp` HEAD 均为 `200`
- [x] 2026-04-24 强化通用工作流：更新 `.windsurf/skills/data-entry-workflow/SKILL.md` 与 `docs/games/smashup/workflows/smashup-faction-implementation.md`，新增“长期任务连续执行”强制规则
- [x] 2026-04-24 同步两条 watchdog 反馈审计文档复核补记（`69db57c`、`69daa51e`），与主线 E2E `3 passed` 口径对齐
- [x] 2026-04-24 同步 Android 内置 SmashUp locale：删除 `faction_implementation_in_progress_hint`，并复跑 `assets:upload`（上传 `0` / 跳过 `530` / 失败 `0`）
- [x] 2026-04-25 完成两条 watchdog 反馈定向 E2E 复测：`69db57c` 1 条、`69daa51e` 2 条，均通过并回写证据截图路径
- [x] 2026-04-25 修订 `mermaids_toll_bay` 审计口径：旧“触发窗口标记”结论失效，按卡面语义统一为“即时抽牌”；`newFactionAbilities` 为 `170 passed / 1 skipped`，并复跑 4 审计套件 + i18n + `smashup.e2e.ts` 全绿
- [x] 2026-04-25 补跑 `smashup.smoke.test.ts`（`121 passed`）确认三派系修复未引入主流程烟测回归
- [x] 2026-04-25 追加全量 SmashUp 回归（`146 files passed / 9 skipped`，`1962 passed / 19 skipped`）与 R2 二次 HEAD 复核（`wangling.webp` / `wangling_base.webp` 均 `200`）
- [x] 2026-04-25 修复“巨石阵附着天赋二次发动”回归：`USE_TALENT(ongoingCardUid)` 补巨石阵双才能例外，复跑 `talentAbilities(22 passed)`、`smashup-gameplay.e2e(7 passed)`、`smashup.e2e(3 passed)`、`newFactionAbilities(174 passed/1 skipped)`、`smoke(121 passed)`、4 审计套件（`36 passed`）与 `i18n:check` 全绿
- [x] 2026-04-25 去重 `talentAbilities` 重复新增 case 并全链路复跑：`talentAbilities(20 passed)`、`newFactionAbilities(179 passed/1 skipped)`、`smoke(122 passed)`、`smashup-gameplay.e2e(7 passed)`、`smashup.e2e(3 passed)`、4 审计套件（`36 passed`）与 `i18n:check` 全绿
- [x] 2026-04-25 补齐数据录入基操脚本：`scrape-wiki-with-descriptions.mjs` 纳入 `skeletons/mermaids/world_champs`，`final-wiki-code-comparison.mjs` 补单双引号与弯直引号归一化并声明“仅校验 name/count”；复核 `skeletons` 抓取 `12/20`、对比 `1 正确/0 问题`、脚本 `eslint` 全绿
- [x] 2026-04-29 补《快如闪电 / 女主角 / 阿拉密斯》联合反应窗 L3，并回写旧“女主角实现正确”结论失效：根因确认为 `smashup_reaction_choose` 双 reduce + `Aramis` 触发范围缺口，补齐 `finalState / triggerQueue / reaction session / 真实入口 E2E` 审计维度
- [x] 2026-04-29 补《人鱼女王 / 安静的海岸》L3：把 `Mermaids` 的“模式选择 / 场上持续牌天赋迁移”从 L2 扩到浏览器级真实入口，并同步回写累计对象证据口径
- [x] 2026-04-29 补《塞壬的歌声 / 他们出来了》L3：把 `Mermaids` 的“来源基地过滤 + 逐段移动”与 `Skeletons` 的“选基地后多张挖掘”补到浏览器级真实入口，并显式修掉一次 E2E 场景误用不存在 card def 的低级错误
- [x] 2026-04-29 补《墓园》L3：把 `Skeletons` 的“场上持续牌天赋 -> 挖掘 -> 可选 +1 指示物”从 L2 扩到浏览器级真实入口，并同步回写累计对象证据口径
- [x] 2026-04-29 补《骸骨之王》L3：把 `Skeletons` 的“场上 minion 天赋 -> 挖掘这里任意埋葬牌 -> 先经 reaction session 再进 +1 后续交互”从 L2 扩到浏览器级真实入口，并同步回写累计对象证据口径
- [x] 2026-04-29 回写长期任务 / 派系重审 workflow 门禁：把“批量派系重审批次清单”“E2E 场景 defId 预检”“L0-L4 分层验收”“reaction session 抽样门禁”补进 `.windsurf/skills/data-entry-workflow/SKILL.md`、`docs/games/smashup/workflows/smashup-faction-implementation.md`、`docs/ai-rules/testing-audit.md`
- [x] 2026-04-30 收口《墓地爆发》L3，并修复 `scoreBases` 交互事件在 reduce 前被提前计分的时序缺口；定向 E2E `1 passed`，回归 Vitest `2 passed`
- [x] 2026-04-30 补《塞壬 / 诱惑者 / 无人岛》L3，并修复 `BaseZone` 分数徽章绕过 `getPlayerEffectivePowerOnBase(...)` 的 UI 口径缺口；3 条定向 E2E、`ongoingModifiers` 聚焦回归 `6 passed`、`typecheck` 全绿
- [x] 2026-04-30 补《武士 陈》正路径 L3，并收口 `World Champs` 最后一个对象级冻结点；定向 E2E `1 passed`，聚焦 Vitest `2 passed`

### Current Remaining Batch（强制继续，未清空前不得按“收口”停下）
- [x] 明确枚举 `World Champs / 世界冠军` 剩余未到发布级门禁的对象/链路，补到对象级 L3 或明确降级理由
- [x] 明确枚举 `Skeletons / 骷髅` 剩余未到发布级门禁的对象/链路，补到对象级 L3 或明确降级理由
- [x] 对三派系当前已补对象做一轮“卡图口径 vs UI真实出口 vs reaction session”交叉抽检，防止再出现“领域对 / UI错”型漏审
- [x] 回写总审计文档里所有仍写着泛化“已完成专项审计与回归验证”的旧高层口径，避免旧结论继续误导
- [x] 只有当上面 4 项全部勾完，且总审计文档的“仍有残余范围”被逐条消解或显式冻结，才允许进入最终收口汇报


## Addendum（2026-04-22）：线上 Dicethrone critical 反馈收口补强（69c3c83e / 69cba605）

### Goal
> 对 `69c3c83e`（黑屏）与 `69cba605`（骰面不可见）做当前代码基线复核；对仍存在前端兜底缺口的骰面链路做最小修复并补回归证据。

### Phase
- [x] Phase A: 复核反馈上下文与当前实现入口
- [x] Phase B: 最小修复 `Dice3D` 无 sprite 可见性兜底
- [x] Phase C: 补现有测试断言并运行验证
- [x] Phase D: 产出 evidence 文档并回填 planning 文件

### Scope Control
- 仅修改 `src/games/dicethrone/ui/Dice3D.tsx` 与对应现有测试文件。
- 黑屏链路仅做兼容修复有效性复核，不引入额外架构改动。

## Addendum（2026-04-26）：SmashUp 三派系审计续跑（_pod alias + 横幅复核）

### Goal
> 继续执行三派系审计批次：修复 `_pod` alias 审计误报，对齐 Mermaid 新语义断言，并复核统一斜向“实施中”横幅链路是否持续稳定。

### Phase
- [x] 修复 `interactionCompletenessAudit` 的 `_pod` alias 孤儿误报
- [x] 对齐 `Mermaids` 争议用例语义并复跑 `newFactionAbilities`
- [x] 复跑四项审计套件 + i18n 门禁
- [x] 复测横幅 E2E 并完成截图核图
- [x] 继续补齐 `World Champs` 关键链路 L3（`斗志奖杯`、`鼠、鸟与香肠`）并回写专项证据
- [x] 收敛 `smashup.e2e.ts` 中“3 人房座位状态”join 超时稳定性（`3 人房`用例增加 `test.setTimeout(120000)`，复跑 `smashup.e2e.ts` 全绿）
- [x] 收敛全量 `src/games/smashup` 回归失败簇（afterScoring/onDestroy/validation 共 14 条，已收敛为 0）
- [x] 修复 `bear_cavalry_bear_necessities` 交互 stale 目标兜底，并对齐新旧测试语义（“随从或行动卡”）
- [x] 收敛横幅 E2E 的服务就绪抖动：`ensureGameServerAvailable` 改为 45s 轮询，避免误判 skip
- [x] 2026-04-29 补《沉船湾 / 轮回者 / 诡异。可怕。 / 墓碑》L3，并回写两类场景错误：`轮回者` 的旧“直接无交互”假设失效；`沉船湾 / 墓碑` 的旧在线场景未满足计分阈值，根因属于 E2E 注入错误而非实现错误

## 2026-05-05 Follow-up
- [x] 复核当前线上人类 open 反馈并锁定主故障为房间加入失败
- [x] 确认生产 game-server 仍跑旧 join 协议（join 强制要求 playerID）
- [x] 使用生产部署脚本更新 latest 镜像并完成生产 create/claim-seat/join 复测
- [x] 将 69f86b739ec13b96d71107d4 / 69f86c159ec13b96d7110804 按证据链回写为 resolved，并同步 status-board
- [x] 锁定 Android `AppUpdate` 缺插件对应的正式原生壳版本：`0.5.0`（以及更早壳）；首个确认带 `AppUpdatePlugin` 的正式包为 `0.5.1.apk`
- [ ] 视发布窗口决定是否将 Android AppUpdate 缺插件兜底补丁随下一次正式发布带上生产

## Addendum（2026-05-05）：SmashUp 并列计分口径修复
- 用户给出的当前产品口径：`大杀四方战斗力相等时，应取第二位/更低位分，不取并列名次的高位分`。
- 已定位根因：`src/games/smashup/domain/index.ts` 的 `buildBaseRankings()` 之前按“并列沿用当前 rankSlot”发分，导致并列第一仍拿第一位分、并列第二仍拿第二位分。
- 已落修复：改为按并列组占据的最低名次发分（例如并列第一拿第二位分，并列第二拿第三位分）。
- 一致性补充：同步修正 `src/games/smashup/ai.ts` 的基地 VP 估值逻辑，避免 AI 仍按旧口径评估。
- 已补测试：`src/games/smashup/__tests__/baseScoring.test.ts`
  - `scoreOneBase 在并列第一时给并列玩家第二位分`
  - `scoreOneBase 在并列第二时给并列玩家第三位分`
- 已验证：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --maxWorkers 1`
  - `npm run typecheck`

## Addendum（2026-05-05 23:35 +08）：人类反馈优先续跑

### Goal
> 按“人类反馈优先”新口径，先收敛 SmashUp 剩余 3 条人工反馈：并列计分、熊泰坦额外随从、多人观战异常。

### Phase
- [x] 把 `人类反馈 > 系统自动反馈` 回写到 `.windsurf/skills/feedback-closeout/SKILL.md` 与本计划
- [x] `69f96a734590ce09779a7205` 并列计分：确认本地已修并复跑定向回归
- [x] `69f9623c4590ce09779a715f` 熊的泰坦不能用额外随从打出：完成共享修复与回归
- [x] `69f961ca4590ce09779a715a` 多人观战有 bug 看不了其他人：完成多视角修复、真实 E2E 与收口截图

### Notes
- `69f9623c4590ce09779a715f` 的共享根因已确认不是熊专属逻辑，而是 `smashup_immediate_extra_minion` 候选只枚举手牌随从，没有纳入 `playAsKinds=['minion']` 的 `setaside` 泰坦。
- `69f961ca4590ce09779a715a` 的真实根因已收敛到 `SmashUpBoard` 的二元视角模型：旧实现只能在“自己 / 第一个对手”之间切换，多人局无法点谁看谁。
- 本轮新增本地收口证据：
  - `evidence/smashup/smashup-feedback-69f96a734590ce09779a7205-tied-base-scoring-local-closeout-2026-05-05.md`
  - `evidence/smashup/smashup-feedback-69f9623c4590ce09779a715f-extra-minion-titan-local-closeout-2026-05-05.md`
  - `evidence/smashup/smashup-feedback-69f961ca4590ce09779a715a-multi-opponent-view-local-closeout-2026-05-05.md`
- 本地状态板当前还是旧 `remote-human-unresolved-20260421-163730.json` 衍生快照，这 3 条新人工反馈尚未进入板子；在拿到最新 human summary 或正式远端写入口前，不伪造状态板条目。

## Addendum（2026-05-06 07:42 +08）：SmashUp 三条人工反馈正式状态回写

- [x] 核对 HTTP 反馈接口当前不可作为正式写入口：`GET /feedback/open?...` 返回 `404`
- [x] 通过生产 `feedbacks` 集合直连确认 3 条目标反馈回写前均为 `open`
- [x] 已把 `69f96a734590ce09779a7205 / 69f9623c4590ce09779a715f / 69f961ca4590ce09779a715a` 正式回写为 `resolved`
- [x] 已把本地 `temp/feedback-closeout/status-board.json` 同步补入并校验通过
- [x] 线上人类未收口反馈最终已清零；最后两条 `69fa23e04590ce09779a7c52 / 69fa0bd74590ce09779a7bd6` 已在后续批次完成正式回写

## Addendum（2026-05-06 08:10 +08）：SmashUp 最后两条人工反馈回写与人类未收口清零

- [x] 继续沿用 `人类反馈 > 系统自动反馈` 口径处理最后两条 `smashup|feedback-modal`
- [x] `69fa23e04590ce09779a7c52` 已按“已修未回写”回写为 `resolved`
- [x] `69fa0bd74590ce09779a7bd6` 已按“非 bug / 规则符合”回写为 `closed`
- [x] 本地 `status-board.json` 已与这两条最终状态对齐，并通过 `feedback-status: ok`
- [x] 已通过生产 `feedbacks` 复核：`reporterType=user && status in [open,in_progress]` 当前 `count=0`

### Notes

- 正式证据文档：
  - `evidence/feedback-closeout/smashup-human-final-two-writeback-2026-05-06.md`
- 关键快照：
  - `temp/feedback-closeout/query-feedback-69fa23e0-69fa0bd7-before-writeback-20260506.raw.txt`
  - `temp/feedback-closeout/update-feedback-status-20260506-smashup-human-remaining-two.raw.txt`
  - `temp/feedback-closeout/query-feedback-69fa23e0-69fa0bd7-after-writeback-20260506.raw.txt`
  - `temp/feedback-closeout/query-human-open-inprogress-after-final-writeback-20260506.raw.txt`

## Addendum（2026-05-07 00:20 +08）：SmashUp 新人工反馈 `69faac614590ce09779a7d8f` 宗教圆环发不了效果

- [x] 重新核对线上真源，确认当前人类反馈新增 1 条 `smashup|feedback-modal`
- [x] 锁定目标反馈：`69faac614590ce09779a7d8f`，原文 `宗教圆环发不了效果`
- [x] 结合生产快照与用户截图定位到前端根因，不是领域校验失败
  - 新补 E2E 首轮直接卡在点击 `[data-ongoing-uid="oa-sacred-circle"]`
  - Playwright 明确报错为透明 `absolute inset-0 z-60` 层拦截点击
- [x] 已做最小修复
  - `src/games/smashup/ui/BaseZone.tsx`
  - `e2e/src/games/smashup/ui/BaseZone.tsx`
  - 桌面端基地 ongoing 放大镜包裹层改为 `pointer-events-none`
- [x] 已补最小 UI 复现
  - `e2e/smashup/smashup-base-minion-selection.e2e.ts`
  - 场景覆盖：点击《宗教圆环》 -> 进入已用态 -> 选择手牌《本地人》 -> 成功打到巫师学院
- [x] 已完成本地 E2E 收口并补证据
  - `evidence/smashup/smashup-feedback-69faac614590ce09779a7d8f-sacred-circle-click-fix-e2e-2026-05-07.md`
- [x] 已按 2026-05-07 新口径补充 workflow：反馈只要完成修复验证，就应立刻回写远端正式状态，不再默认停在本地 resolved
- [x] 已完成远端反馈状态回写与生产复核
  - `temp/feedback-closeout/query-feedback-69faac61-before-writeback-20260507.raw.txt`
  - `temp/feedback-closeout/update-feedback-status-20260507-69faac61-to-resolved.raw.txt`
  - `temp/feedback-closeout/query-feedback-69faac61-after-writeback-20260507.raw.txt`
  - `temp/feedback-closeout/query-human-open-inprogress-after-20260507.raw.txt`
  - 线上 `reporterType=user && status in [open,in_progress]` 当前 `count=0`
- [x] 全量线上反馈已清零
  - `temp/feedback-closeout/update-feedback-status-20260507-final-watchdog-batch.raw.txt`
  - `temp/feedback-closeout/query-all-open-inprogress-after-final-watchdog-batch-20260507.raw.txt`
  - `temp/feedback-closeout/query-all-open-inprogress-current-20260507.raw.txt`
  - 截至 `2026-05-07 21:25 +08`，生产真源 `open/in_progress = 0`
  - 本轮最后 `21` 条 watchdog 系统单已完成正式回写：`resolved = 9`、`closed = 12`
  - 当前可以正式宣称“线上人类反馈已清零，系统反馈也已清零，所有反馈都已修好”

## Addendum（2026-05-07 21:25 +08）：最后 21 条 watchdog 系统反馈正式清零

- [x] 生产真源回写前盘面核对完成
  - 回写前真实待清批次是 `21` 条，另有 `69fb3fde... / 69fc6298...` 已在本轮更早一拍单独回写
  - 这 `21` 条全部来自 `reporterType=system`、`source=online-ai-watchdog`
- [x] 判定口径已落地
  - `force-end-turn-failed ...` 与 `unsatisfiable-interaction-auto-skipped empty-options` 按 `resolved`
  - `force-end-turn-success ...` 按 `closed`
- [x] 最后一批生产正式回写完成
  - 回写时间：`2026-05-07 21:08:22 +08`
  - 回写结果：`resolved.matchedCount=9 / modifiedCount=9`，`closed.matchedCount=12 / modifiedCount=12`
- [x] 本地状态板已同步补入并准备校验
  - `temp/feedback-closeout/status-board.json`
- [x] 最终复核已确认线上全量清零
  - `temp/feedback-closeout/query-all-open-inprogress-current-20260507.raw.txt`
  - 截至 `2026-05-07 21:25 +08`：`totalOpenOrInProgress=0`、`humanOpen=0`

## Addendum（2026-05-07 21:52 +08）：`69fc6298` 短暂重开后再次清零

- [x] `69fc62984a37805e1526f6d9` 在生产真源短暂回到 `open`
  - fresh 生产直查结果：`totalOpenOrInProgress=1`、`humanOpen=0`
- [x] 复核同局 `bSJjqanl8rO` 的日志后确认这是同一系统聚合项的再刷
  - watchdog 已继续把局面从 `scoreBases -> draw -> playCards` 推进收口
  - 这条仍按失败类系统单回写 `resolved`
- [x] 生产再次回写成功
  - `matchedCount=1 / modifiedCount=1`
  - 目标：`69fc62984a37805e1526f6d9`
- [x] 最新复核再次确认全量清零
  - `totalOpenOrInProgress=0`
  - `humanOpen=0`
  - 当前最终口径仍是“所有反馈已清零”

## Addendum（2026-05-07 22:00 +08）：fresh 生产直查仍为全量清零

- [x] 最新生产直查结果
  - `ts=2026-05-07T14:00:21.653Z`
  - `totalOpenOrInProgress=0`
  - `humanOpen=0`
- [x] 当前最终口径再次确认不变
  - 线上人类反馈已清零
  - 系统 watchdog 反馈已清零
  - 所有反馈已清零

## Addendum（2026-05-09 23:58 +08）：新一批人工反馈继续处理

- [x] 生产 Mongo 重新拉取人工 open/in_progress
  - 截至 `2026-05-09 20:40:30 +08`：8 条人工未收口。
  - 本地状态板：`temp/feedback-closeout/status-board.json` 已补入新批次。
- [x] 优先修复 3 条 SmashUp critical 扩展基地反馈
  - `69feca4bf0a61f28ba015d7e`：印斯茅斯弃牌区为空时无法发动/跳过。
  - `69fecbb9f0a61f28ba015d9e`：印斯茅斯效果触发不了。
  - `69fec94df0a61f28ba015d49`：温室无法执行。
  - 根因：queued reaction 执行器 effect contract 缺少 `controllerState`，运行时读取 `state.players.*` 时抛错。
- [x] 已补修复与验证
  - `src/games/smashup/domain/baseAbilities_expansion.ts`
  - `src/games/smashup/__tests__/expansionBaseAbilities.test.ts`
  - 证据：`evidence/smashup/smashup-feedback-20260509-expansion-base-effect-contract.md`
- [x] 已回写 3 条生产反馈为 `resolved`
  - `69fec94df0a61f28ba015d49` 本轮脚本实际 `matched=1 / modified=1`
  - `69feca4bf0a61f28ba015d7e`、`69fecbb9f0a61f28ba015d9e` 回写前已是 `resolved`
- [x] 已修复 `69feac13f0a61f28ba015c93` 巫师空牌库抽牌/揭示反馈
  - `wizard_neophyte` 空牌库走 `peekDeckTop`，POD 学徒可先洗弃牌堆再揭示。
  - `wizard_enchantress`、`wizard_mystic_studies`、`wizard_sacrifice` 改走 `buildStandardDrawEvents`，避免空牌库时只记录抽牌但最终手牌未增加。
  - 验证：`factionAbilities.test.ts -t "69feac13"` 3 passed；整文件 46 passed；eslint 0 errors。
  - 证据：`evidence/smashup/smashup-wizard-neophyte-empty-deck-feedback-2026-05-09.md`
- [x] 已回写 `69feac13f0a61f28ba015c93` 生产反馈为 `resolved` 并复查剩余未收口数量
- [x] 已修复并回写 `69feede0f0a61f28ba0163df` 泰坦场下询问反馈
  - 根因：`werewolves_great_wolf_spirit` 的 `onTurnStart` 被错误登记为 `global`，场下 setaside 泰坦也会被 `collectTriggers()` 放入 reaction queue。
  - 修复：移除巨狼之灵 `global` 触发注册，删除重复注册块，同步 `e2e/src` 镜像。
  - 验证：`turnCycle -t 线上反馈 69feede0` 1 passed；`smashup.smoke -t Great Wolf Spirit creates a start-of-turn move interaction` 1 passed；eslint 0 errors。
  - 证据：`evidence/smashup/smashup-great-wolf-spirit-setaside-feedback-2026-05-09.md`
  - 生产回写：`matched=1 / modified=1`
- [x] 最新生产剩余人工/反馈弹窗队列已重新拉取并同步状态板
  - 截至 `2026-05-10 02:55 +08`：`remainingHumanOrModalOpenInProgress.count = 5`
  - 新增两条：`69ff7291f0a61f28ba0189b9` 实验工坊有bug；`69ff720cf0a61f28ba01897d` 非常多bug，海盗的bug很多。
- [x] 继续处理剩余 5 条：Cardia 教程、SmashUp AI/卡住、实验工坊、海盗反馈等。
- [x] 已修复并回写 `69ff7291f0a61f28ba0189b9` 实验工坊反馈
  - 根因：实验工坊/同类基地把“本回合该基地已打出随从次数”放在 queued trigger 执行期读取，并声明 `playLimits`，与大法师写 `playLimits` 误判为强制触发排序冲突。
  - 修复：基地能力支持 `canTrigger` 入队前预筛；实验工坊/集会场/名人堂不再在 queued 执行期读取出牌计数字段，避免残留 `triggerQueue` 或弹无意义排序窗口。
  - 验证：`archmageE2E` 聚焦 `69ff7291` 1 passed，整文件 9 passed；`newBaseAbilities` 实验工坊/集会场 7 passed；`expansionBaseAbilities` 名人堂 1 passed；eslint 0 errors。
  - 证据：`evidence/smashup/smashup-laboratorium-archmage-feedback-2026-05-09.md`
  - 生产 Mongo 回写：`matchedCount=1 / modifiedCount=1`；回写后剩余人工/反馈弹窗 open/in_progress 为 4 条。
- [x] 已补充 `69ff7291f0a61f28ba0189b9` 旧生产持久化队列兼容复核
  - 发现：生产快照中的 `base_laboratorium` trigger 已持久化旧 `effectContract.reads`，需要证明旧局也能恢复。
  - 补充：`reactionOrdering` 物化排序 contract 时兼容旧版实验工坊/集会场首随从基地触发；新增旧队列回归。
  - 验证：生产快照只读灌入 `maybeResolveReactionQueue` 后 `triggerQueueLength=0 / currentInteractionSourceId=null / archmagePowerCounters=1 / actionLimit=2`；`newBaseAbilities` 59 passed；`reactionQueueOrdering` 18 passed。
  - 证据已修订：`evidence/smashup/smashup-laboratorium-archmage-feedback-2026-05-09.md`
- [x] 已回写 `69ff720cf0a61f28ba01897d` 海盗泛反馈为同根因 `resolved`
  - 现场：用户描述泛称海盗 bug，但快照实际为 `robot_hoverbot` 打到 `base_laboratorium` 后残留旧实验工坊 trigger。
  - 验证：生产快照只读灌入 `maybeResolveReactionQueue` 后 `triggerQueueLength=0 / currentInteractionSourceId=null / hoverbotPowerCounters=1 / consumedEvents=1`。
  - 证据：`evidence/smashup/smashup-laboratorium-archmage-feedback-2026-05-09.md`
  - 生产 Mongo 回写：`matchedCount=1 / modifiedCount=1`；fresh 后剩余人工/反馈弹窗 open/in_progress 为 3 条。

## Addendum（2026-05-10 05:36 +08）：5/10 本批人工反馈清零

- [x] 剩余 3 条人工/反馈弹窗 open 已全部收口并回写生产 Mongo。
  - `69ff0e90f0a61f28ba016a4d` Cardia 教程反馈：`resolved`，证据 `evidence/cardia/cardia-tutorial-full-flow-e2e-test.md`
  - `69ff0cd0f0a61f28ba0169e9` SmashUp AI 出牌阶段卡死：`resolved`，回写产物 `temp/feedback-closeout/update-feedback-status-20260510-69ff0cd0-ai-playcards-stalled-to-resolved.raw.txt`，`matched=1 / modified=1`
  - `69ff0310f0a61f28ba0167d6` SmashUp 天选之人确认交互卡住：`resolved`，回写产物 `temp/feedback-closeout/update-feedback-status-20260510-69ff0310-cthulhu-chosen-confirm-to-resolved.raw.txt`，`matched=1 / modified=1`
- [x] 已补齐 69ff0310 浏览器 UI 证据链。
  - E2E：`npm run test:e2e:ci:file -- e2e/smashup/smashup-cthulhu.e2e.ts "线上反馈 69ff0310：旧天选之人确认交互应显示按钮弹层并可关闭"` -> `1 passed`
  - 截图 1：`D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\smashup\smashup-cthulhu.e2e\线上反馈-69ff0310：旧天选之人确认交互应显示按钮弹层并可关闭\69ff0310-chosen-confirm-button-overlay.png`
  - 截图 2：`D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\smashup\smashup-cthulhu.e2e\线上反馈-69ff0310：旧天选之人确认交互应显示按钮弹层并可关闭\69ff0310-chosen-confirm-after-no.png`
- [x] 已补充 69ff0cd0 最新回归验证。
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseAbilitiesPrompt.test.ts --configLoader native --maxWorkers 1 -t "69ff0cd0|base_the_mothership"` -> `6 passed`
- [x] 本地状态板已更新并通过校验。
  - `node scripts/verify/verify-feedback-status.mjs temp/feedback-closeout/status-board.json` -> `feedback-status: ok`
- [x] fresh 生产真源清零核对完成。
  - 查询产物：`temp/feedback-closeout/query-open-human-final-20260510.raw.txt`
  - 截至 `2026-05-10 05:35 +08`，生产 Mongo 人工/feedback-modal `open/in_progress`：`count=0`

## Addendum（2026-05-23 11:47 +08）：`vampire_the_count` / `vampire_opportunist` owner-controller provenance 真红灯转绿

- [x] 命中新的一条 destroy provenance 真红灯，不是 green evidence
  - 规则语义是 “After another player's minion is destroyed”，这里的“another player”必须按被消灭随从的 `controller` 判定，不能按 `owner`。
  - destroy 链当前在 reducer 侧给 `onMinionDestroyed` 的 `ctx.playerId` 仍是被消灭随从 `ownerId`；若 consumer 直接把它当“被消灭随从所属玩家”，在 stolen minion（`controller != owner`）场景就会误判。
- [x] 已在 Vampires 对象级 consumer 收口最小修复
  - [`src/games/smashup/abilities/vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>) 的 `vampire_the_count` 与 `vampire_opportunist` 都改为统一读取 `destroyedControllerId = ctx.triggerMinion?.controller ?? ctx.controllerId ?? ctx.playerId`。
  - `vampire_the_count` 现按 `m.controller !== destroyedControllerId` 判定是否是“其他玩家的随从被消灭”。
  - `vampire_opportunist` 现按 `m.controller === destroyedControllerId` 排除己方宿主，不再把 owner 当成敌我归属。
- [x] 已补 focused 红灯并转绿
  - [`src/games/smashup/__tests__/newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增：
  - `投机主义：被我方控制但归对手拥有的随从被消灭时，仍应按控制权判定为其他玩家随从并触发`
  - `吸血鬼伯爵：被他人控制但归己方拥有的随从被消灭时，不应按拥有权误判为己方随从`
  - 首轮红灯都坐实为 `POWER_COUNTER_ADDED` 缺失；当前已转绿。
- [x] 已做最小验证
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "投机主义：被我方控制但归对手拥有的随从被消灭时，仍应按控制权判定为其他玩家随从并触发|吸血鬼伯爵：被他人控制但归己方拥有的随从被消灭时，不应按拥有权误判为己方随从|投机主义：对手随从在 Big Gulp 真实 destroy 链中被消灭后才给附着随从\\+1|投机主义：己方随从被消灭时不应触发|吸血鬼伯爵：己方随从被消灭时不应触发|雄蜂\\+吸血鬼伯爵：pendingSave 时 onMinionDestroyed 触发器的副作用事件被抑制|雄蜂\\+投机主义：pendingSave 时 onMinionDestroyed 触发器的副作用事件被抑制|雄蜂跳过后（drone_skip），吸血鬼伯爵正常获得 \\+1 指示物"` -> `1 file passed, 8 passed`
  - `git diff --check -- src/games/smashup/abilities/vampires.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` -> 无 diff --check 错误，仅 LF/CRLF warning
- [x] 当前边界已写死
  - 这格只闭合 `vampire_the_count` / `vampire_opportunist` 在 stolen minion owner/controller 分离下的 destroy provenance。
  - 不外推整个 `Vampires`、整个 `onMinionDestroyed` family，或其它仍直接读取 `ctx.playerId` 的 destroy/discard consumer 已全量完成。

## Addendum（2026-05-23 12:02 +08）：`base_sakura_garden` destroy ledger controller 结构缝收口

- [x] 命中新的 runtime 结构缝
  - `base_sakura_garden` 的“本回合第一次有你的随从被消灭时抽 1”语义按 `controller` 判定。
  - 但旧实现一边按 `ctx.triggerMinion.controller` 给抽牌玩家，一边又只看 `turnDestroyedMinions[].owner` 做 once-per-turn 去重；同一玩家若先失去 stolen minion（`controller=0, owner=1`）再失去自有 minion（`controller=0, owner=0`），旧逻辑会误判成两次首次。
- [x] 已做最小 runtime 修复
  - [`src/games/smashup/domain/types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>)：`turnDestroyedMinions` 增量补 `controller?: string`
  - [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>)：`MINION_DESTROYED` 写入 destroy record 时同步记录 `controller: minion.controller`
  - [`src/games/smashup/abilities/samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>)：`samuraiSakuraGardenTrigger` 改为优先按 `record.controller ?? record.owner` 去重
- [x] 已补 focused gate
  - [`src/games/smashup/__tests__/newBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newBaseAbilities.test.ts>) 新增：
  - `base_sakura_garden 在同一玩家先失去 stolen minion 再失去自有随从时，不应因 owner 不同而重复抽牌`
  - 同时同步更新 reducer ledger 精确断言：
  - [`src/games/smashup/__tests__/newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>)
  - [`src/games/smashup/__tests__/feedback-high-ground-destroyer.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/feedback-high-ground-destroyer.test.ts>)
- [x] 已做最小验证
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newBaseAbilities.test.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts src/games/smashup/__tests__/feedback-high-ground-destroyer.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "base_sakura_garden 在本回合第一次有你的随从被消灭时让你抓一张牌|base_sakura_garden 同回合第二次有同一玩家的随从被消灭时不应再次抽牌|base_sakura_garden 在同一玩家先失去 stolen minion 再失去自有随从时，不应因 owner 不同而重复抽牌|reducer: MINION_DESTROYED 会追踪到 turnDestroyedMinions|自动执行消灭分支时 destroyerId 仍为制高点拥有者"` -> `3 files passed, 5 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newBaseAbilities.test.ts src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "base_sakura_garden|Sakura Garden"` -> `2 files passed, 8 passed`
  - `git diff --check -- src/games/smashup/domain/types.ts src/games/smashup/domain/reduce.ts src/games/smashup/abilities/samurai.ts src/games/smashup/__tests__/newBaseAbilities.test.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts src/games/smashup/__tests__/feedback-high-ground-destroyer.test.ts` -> 无 diff --check 错误，仅 LF/CRLF warning
- [x] 当前边界已写死
  - 这格只闭合 `base_sakura_garden` 在 stolen-minion controller/owner 分离场景下的 once-per-turn 去重语义。
  - 不外推所有 `turnDestroyedMinions` consumer、整个 `Samurai` family，或其它 destroy ledger 已全量完成。

## Addendum（2026-05-23 12:08 +08）：`cthulhu_furthering_the_cause` another-player controller 语义收口

- [x] 命中新的一条 `turnDestroyedMinions` consumer 真红灯
  - 卡文是 `At the end of each turn, if another player's minion was destroyed here, gain 1 VP.`
  - 这里的 `another player` 也应按 destroyed minion `controller` 判定，不应按 `owner` 判定。
  - 旧实现 `cthulhuFurtheringTheCauseTrigger()` 只检查 `turnDestroyedMinions.owner !== ongoing.ownerId`；若被消灭的是 stolen minion（`controller=1, owner=0`），会把“对手控制、自己拥有”的随从误判成不是 another player。
- [x] 已做最小修复
  - [`src/games/smashup/abilities/cthulhu.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cthulhu.ts>) 改为按 `d.controller ?? d.owner` 比较 `ongoing.ownerId`
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增：
  - `被他人控制但归自己拥有的随从本回合在此被消灭时，仍应按控制权判定为 another player 并获得 1 VP`
  - 首轮红灯已坐实：`VP_AWARDED` 缺失
- [x] 已补 queued 邻近 gate
  - [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增：
  - `sourceController queued onTurnEnd trigger 遇到被他人控制但归自己拥有的 destroyed minion 时，Furthering the Cause 仍应按控制权给拥有者 1 VP`
- [x] 已做最小验证
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newOngoingAbilities.test.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "cthulhu_furthering_the_cause|Furthering the Cause"` -> `2 files passed, 8 passed`
  - `git diff --check -- src/games/smashup/abilities/cthulhu.ts src/games/smashup/__tests__/newOngoingAbilities.test.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts` -> 无 diff --check 错误，仅 LF/CRLF warning
- [x] 当前边界已写死
  - 这格只闭合 `cthulhu_furthering_the_cause` 在 stolen-minion controller/owner 分离场景下的 `another player's minion` 判定。
  - 不外推整个 `Cthulhu`、全部 `onTurnEnd` ongoing，或整个 `turnDestroyedMinions` family 已完成。

## Addendum（2026-05-23 13:35 +08）：`world_champs_diva` copied destroy owner/controller 语义收口

- [x] 命中新的一条对象级真红灯
  - 这次不是前面 `shared destroy ledger` 那格回扫，而是 `world_champs_diva` 自己在复制 destroy 事件时把 owner/controller 重新写坏了。
  - 旧 [`src/games/smashup/abilities/world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `buildDivaMirroredEvent()` 在 `SU_EVENTS.MINION_DESTROYED` 分支把 `ownerId` 直接写成 `divaControllerId`。
  - 因此 Diva 一旦是被借走的随从（`controller='0', owner='1'`），复制 destroy 效果后会错误进入控制者 `P0` 的弃牌堆，而不是拥有者 `P1` 的弃牌堆。
- [x] 已做最小 runtime 修复
  - `buildDivaMirroredEvent()` 新增显式参数 `divaOwnerId`
  - 复制 destroy 事件改为写 `ownerId: divaOwnerId`
  - 同时补 `controllerId: divaControllerId`，避免后续 destroy consumer 再丢 controller provenance
  - `worldChampsDivaOnMinionAffected()` 传入 `sourceMinion.owner`
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增：
  - `world_champs_diva 在被他人控制时复制 destroy 效果后，仍应进入自己拥有者的弃牌堆`
  - 夹具直接构造 `diva-1(controller='0', owner='1')` 复制 `vampire_big_gulp` 风格的 destroy effect
  - 首轮红灯已坐实：`world_champs_diva_copy_destroyed.payload.ownerId === '0'`，而不是预期的 `'1'`
- [x] 已做最小验证
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_diva 在被他人控制时复制 destroy 效果后，仍应进入自己拥有者的弃牌堆"` -> `1 file passed, 1 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_diva|World Champs Diva|world_champs_fast_as_lightning 打到阿拉密斯后应进入包含女主角与阿拉密斯的反应窗|world_champs_fast_as_lightning 依次选择女主角与阿拉密斯后应正确收口并保留额外行动"` -> `1 file passed, 4 passed`
  - `git diff --check -- src/games/smashup/abilities/world_champs.ts src/games/smashup/__tests__/newFactionAbilities.test.ts` -> 无 diff --check 错误，仅 LF/CRLF warning
- [x] 当前边界已写死
  - 这格只闭合 `world_champs_diva` 复制 destroy 效果时的 owner/controller 语义。
  - 不外推整个 `World Champs`、所有 copied event，或 `MINION_RETURNED`/其它 mirrored branch 已一并完成。

## Addendum（2026-05-23 13:49 +08）：`giant_ant_who_wants_to_live_forever_pod` borrowed-minion destroy owner 语义收口

- [x] 命中新的一条对象级真红灯
  - 这次不是前面 `shared destroy ledger` 或 `world_champs_diva` 的回扫，而是 `giant_ant_who_wants_to_live_forever_pod` 自己在 destroy 分支里把 owner/controller 又混掉了。
  - 旧 [`src/games/smashup/abilities/giant_ants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/giant_ants.ts>) 中 `giant_ant_who_wants_to_live_forever_pod_destroy` 先按 `collectOwnMinions(ctx.state, ctx.playerId)` 用 `controller` 收集“己方随从”，但真正发 `destroyMinion(...)` 时把 `ownerId` 也硬写成 `context.playerId`。
  - 因此如果你控制了一个归对手拥有的 borrowed minion，这张牌会把它当成你的牌去销毁，并送进错误弃牌堆。
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/giantAntsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/giantAntsPod.test.ts>) 新增：
  - `destroy 分支点中被我控制但归对手拥有的随从时，仍应进入其拥有者弃牌堆`
  - 夹具直接构造 `borrowed-1(controller='0', owner='1')`，P0 打出 `giant_ant_who_wants_to_live_forever_pod` 后选择消灭该随从
  - 首轮红灯已坐实：`destroyEvent.payload.ownerId === '0'`，而不是预期的 `'1'`
- [x] 已做最小 runtime 修复
  - `destroyMinion(...)` 的 `ownerId` 参数改为所选随从真实 `minion.owner`
  - `destroyerId` 仍保留 `context.playerId`
- [x] 已做最小验证
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/giantAntsPod.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "destroy 分支点中被我控制但归对手拥有的随从时，仍应进入其拥有者弃牌堆"` -> `1 file passed, 1 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/giantAntsPod.test.ts src/games/smashup/__tests__/vampiresPod.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Who Wants to Live Forever|WWTLF POD \\+ Drone skip should still count as having destroyed a minion this turn|destroy 分支点中被我控制但归对手拥有的随从时，仍应进入其拥有者弃牌堆"` -> `2 files passed, 3 passed`
- [x] 当前边界已写死
  - 这格只闭合 `giant_ant_who_wants_to_live_forever_pod` destroy 分支在 borrowed minion 场景下的 owner/controller 语义。
  - 不外推整个 `Giant Ants`、所有 self-destroy action，或全部 borrowed-minion consumer 已完成。

## Addendum（2026-05-23 14:32 +08）：`MINION_PLAYED` shared owner provenance 收口，并闭合 `elder_thing_elder_thing_pod` borrowed play-bottom

- [x] 命中新的一条 shared producer 真红灯
  - 这次不是 `elder_thing_elder_thing_pod` 单卡自己把 owner 写坏，而是共享 `PLAY_MINION -> MINION_PLAYED -> reduce(MINION_PLAYED)` 合同从入口就把 borrowed minion 的 owner 洗成了当前打牌玩家。
  - 旧 [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 在构造 `MINION_PLAYED` 时只写 `playerId/cardUid/defId/baseIndex/power`，不带原卡 owner。
  - 旧 [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 在处理 `MINION_PLAYED` 时又把落地后的 `MinionOnBase.owner` 硬写成 `playerId`。
  - 因此 `elder_thing_elder_thing_pod(owner='1')` 由 P0 真实打到基地后，live minion 已经变成 `controller='0', owner='0'`；后续 `elder_things.ts` 再按 live source minion 取 owner，也只能继续拿到错值。
- [x] 已做最小 shared 修复
  - [`src/games/smashup/domain/types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>)：给 `MinionPlayedEvent.payload` 增量补 `ownerId?: PlayerId`
  - [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>)：真实 `PLAY_MINION` 构造 `MINION_PLAYED` 时写入 `ownerId: card.owner`
  - [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>)：落地 `MinionOnBase` 时改为 `owner: ownerId ?? playerId`
  - [`src/games/smashup/abilities/elder_things.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/elder_things.ts>)：`elder_thing_elder_thing_pod` 的 `bottom / forced / fallback` 三个沉底分支统一按 live source minion `owner` 发 `CARD_TO_DECK_BOTTOM`
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/elderThingsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingsPod.test.ts>) 新增：
  - `被他人控制时选择沉底，仍应进入自己拥有者的牌库底`
  - 先断言 `PLAY_MINION` 后 live pod 已是 `controller='0', owner='1'`，再断言 `CARD_TO_DECK_BOTTOM.payload.ownerId === '1'`，且最终只进入 P1 牌库底
  - 首轮红灯已坐实：旧实现下 `CARD_TO_DECK_BOTTOM.payload.ownerId === '0'`
- [x] 已做最小验证
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/elderThingsPod.test.ts -t "被他人控制时选择沉底，仍应进入自己拥有者的牌库底"` -> `1 file passed, 1 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/elderThingsPod.test.ts` -> `1 file passed, 15 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/talentAbilities.test.ts -t "被他人控制时自毁仍应进入自己拥有者的弃牌堆|cthulhu_servitor（仆人 talent）"` -> `1 file passed, 5 passed`
- [x] 当前边界已写死
  - 这格只闭合 `MINION_PLAYED` 在 borrowed-minion 真打出路径上的 owner provenance，以及 `elder_thing_elder_thing_pod` 借来后沉底分支。
  - 不外推所有 borrowed action、所有 `ACTION_PLAYED/ONGOING_ATTACHED` producer，或整个 Elder Things / SmashUp play-event family 已完成。

## Addendum（2026-05-23 14:41 +08）：`ONGOING_ATTACHED` shared owner/source provenance 收口

- [x] 命中新的一条 shared producer 真红灯
  - 这次不是某张 ongoing 单卡自己坏，而是 `PLAY_ACTION(ongoing) -> ONGOING_ATTACHED -> reduce(ONGOING_ATTACHED)` 也存在和旧 `MINION_PLAYED` 对称的 provenance 缝。
  - 旧 [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 在 ongoing 行动真实打出时把 `ONGOING_ATTACHED.payload.ownerId` 直接写成 `command.playerId`
  - 旧 [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 又把 `ownerId` 同时拿来做“附着后卡真正归谁”和“从谁的牌区移走这张卡”；因此 borrowed ongoing 没法同时满足 owner 保真和 source-zone 移除
- [x] 已做最小 shared 修复
  - [`src/games/smashup/domain/types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>)：给 `OngoingAttachedEvent.payload` 增量补 `sourcePlayerId?: PlayerId`
  - [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>)：真实 `PLAY_ACTION` 打出 ongoing 时写 `ownerId: card.owner`、`sourcePlayerId: command.playerId`
  - [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>)：处理 `ONGOING_ATTACHED` 时改为按 `sourcePlayerId ?? ownerId` 从源牌区移除，但附着后的 `ownerId` 继续保留真实 owner
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts>) 新增：
  - `直接打出被他人拥有的 ongoing 行动时，附着后仍应保留 owner 且从当前玩家手牌移除`
  - 夹具让 P0 手牌直接持有 `makeCard('overrun','zombie_overrun','action','1')`，真实 `PLAY_ACTION` 到基地
  - 首轮红灯已坐实：`ONGOING_ATTACHED.payload.ownerId === '0'`，而不是预期的 `'1'`
- [x] 已做最小验证
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts -t "直接打出被他人拥有的 ongoing 行动时，附着后仍应保留 owner 且从当前玩家手牌移除"` -> `1 file passed, 1 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts` -> `1 file passed, 3 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/architecture-duplicate-processing.test.ts -t "D42: ONGOING_ATTACHED 重新附着同一 uid 时应先清理旧挂载位置"` -> `1 file passed, 1 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/smashup.smoke.test.ts -t "翻开埋葬的远古诅咒在跨基地存在唯一合法目标时应保持目标上下文|翻开埋葬的远古诅咒在存在多个合法目标时，选择目标后应继续进入确认交互"` -> `1 file passed, 1 passed`
- [x] 当前边界已写死
  - 这格只闭合真实 ongoing 打出路径上的 `ownerId/sourcePlayerId` 双 provenance。
  - 不外推所有 `ACTION_PLAYED` consumer、所有 buried/uncover producer，或整个 action-play family 已完成。

## Addendum（2026-05-23 15:07 +08）：`ACTION_PLAYED` shared discard-owner provenance 收口

- [x] 命中新的一条 shared producer 真红灯
  - 这次不是 `wizard_summon` 单卡特例，而是标准 `PLAY_ACTION -> ACTION_PLAYED -> reduce(ACTION_PLAYED)` 也存在与前两格对称的 provenance 缝。
  - 旧 [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>) 构造 `ACTION_PLAYED` 时不带 action card 真 owner。
  - 旧 [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 在 standard/special action 结算后，总是把已打出的行动放回 `state.players[playerId].discard`；因此 borrowed standard action 会错误进入当前打牌玩家弃牌堆。
- [x] 已做最小 shared 修复
  - [`src/games/smashup/domain/types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>)：给 `ActionPlayedEvent.payload` 增量补 `ownerId?: PlayerId`
  - [`src/games/smashup/domain/actionPlayEvent.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/actionPlayEvent.ts>)：`buildActionPlayedEvent(...)` 新增透传 `ownerId`
  - [`src/games/smashup/domain/reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>)：真实 `PLAY_ACTION` 构造 `ACTION_PLAYED` 时写 `ownerId: card.owner`
  - [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>)：处理 `ACTION_PLAYED` 时继续按 `playerId` 从源手牌/源弃牌移除，但 standard/special action 的落点 discard 改为 `ownerId ?? movedCard.owner ?? playerId` 对应的拥有者弃牌堆
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/actionAndTalent.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/actionAndTalent.test.ts>) 新增：
  - `直接打出被他人拥有的标准行动时，应从当前玩家手牌移除并进入其拥有者弃牌堆`
  - 夹具让 P0 手牌直接持有 `makeCard('summon','wizard_summon','action','1')`，真实 `PLAY_ACTION`
  - 首轮红灯已坐实：`finalCore.players['0'].discard` 仍错误包含 `summon`
- [x] 已做最小验证
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/actionAndTalent.test.ts -t "直接打出被他人拥有的标准行动时，应从当前玩家手牌移除并进入其拥有者弃牌堆"` -> `1 file passed, 1 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/actionAndTalent.test.ts` -> `1 file passed, 6 passed`
  - `node node_modules/vitest/vitest.mjs run --configLoader native --maxWorkers 1 src/games/smashup/__tests__/wizard-neophyte-ongoing.test.ts` -> `1 file passed, 3 passed`
- [x] 当前边界已写死
  - 这格只闭合标准 action 真打出后的 discard-owner provenance。
  - 不外推所有 `ACTION_PLAYED` consumer、所有 buried/fromDiscard source，或整个 action-play family 已完成。

## Addendum（2026-05-23 21:40 +08）：`wizard_portal` borrowed topdeck owner/source provenance 收口

- [x] 命中新的一条对象级真红灯
  - 这次不是 shared `CARD_TO_DECK_TOP` reducer 再出问题，而是 [`src/games/smashup/abilities/wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `wizard_portal` 仍把“剩余揭示牌放回牌库顶”硬写成 `ownerId: context.playerId`
  - 命中的是真实 borrowed action 在 `P0.deck` 顶部被 `wizard_portal` 揭示、排序并放回顶的场景；旧实现会把它错误留在 `P0.deck`
  - 更窄的合同是：既要把这张牌回到真实 `ownerId` 的 deck top，又要在 borrowed 场景带上 `sourcePlayerId=context.playerId`，否则 reducer 无法从当前玩家牌库移走 source card
- [x] 已做最小对象级修复
  - 同文件新增 `buildWizardPortalReturnToDeckTopEvent(...)`
  - 该 helper 从 `state.players[sourcePlayerId].deck` 按 `cardUid/defId` 取 live source card 的真实 `owner`
  - 仅当 `ownerId !== sourcePlayerId` 时补 `sourcePlayerId`
  - `wizard_portal` 的 3 个回顶分支已统一接入：
  - `wizardPortalProgram` 单剩余牌自动回顶
  - `wizardPortalPickPromptProgram` 选随从后只剩 1 张非随从自动回顶
  - `wizardPortalOrderPromptProgram` 排序收口时逐张回顶
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/query6Abilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/query6Abilities.test.ts>) 新增：
  - `wizard_portal_order: borrowed action 放回牌库顶时应回到拥有者牌库并带 sourcePlayerId`
  - 夹具：`P0.hand=['a1:wizard_portal']`，`P0.deck=['borrowed-a(owner=1)','own-a1','own-a2']`，`P1.deck=['p1-tail']`
  - 首轮红灯已坐实：最终 `CARD_TO_DECK_TOP.payload` 对 `borrowed-a` 仍是 `ownerId:'0'`，且 borrowed 卡会错误留在 `P0.deck`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/query6Abilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "wizard_portal: 顶部5张全是行动卡时不抽牌但创建排序 Prompt|wizard_portal_order: 牌库顶被插入新牌后不应继续保留旧揭示排序候选|wizard_portal_order: borrowed action 放回牌库顶时应回到拥有者牌库并带 sourcePlayerId"` -> `1 file passed, 3 passed`
- [x] 当前边界已写死
  - 这格只闭合 `wizard_portal` 在 borrowed deck-top 场景下的 `ownerId/sourcePlayerId` provenance。
  - 不外推整个 `Wizards`、所有 `CARD_TO_DECK_TOP` caller，或 `wizard_scry / wizard_neophyte / resolveWizardExternalActionPlay()` sibling 已完成。

## Addendum（2026-05-23 22:05 +08）：`steampunk` replay ongoing attach owner/source provenance 收口

- [x] 命中新的一条对象级真红灯
  - 这次不是 shared `PLAY_ACTION -> ONGOING_ATTACHED` producer 回退，而是 [`src/games/smashup/abilities/steampunks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/steampunks.ts>) 里 `steampunk_mechanic / steampunk_change_of_venue` 三个手工拼的 replay attach 旁路仍写 `ownerId: playerId`
  - 命中的是真实把 borrowed ongoing 从当前玩家 discard 取回到手牌，再附着到基地/随从的分支；旧实现会把 attached action 错挂成当前玩家拥有
  - 这条 seam 同样要求补 `sourcePlayerId=playerId`，否则 reducer 无法从当前玩家手牌移走 replay source card
- [x] 已做最小对象级修复
  - 同文件新增 `buildReplayOngoingAttachedEvent(...)`
  - replay prompt context 继续透传 `replayCardUid/replayDefId`，并新增复用已有字段 `replayOwnerId`
  - `steampunkMechanicPromptProgram` 从 discard 选牌时把 `liveCard.owner` 记入 `replayOwnerId`
  - `steampunkChangeOfVenuePromptProgram` 取回 ongoing 时把 `selected.ownerId` 记入 `replayOwnerId`
  - `steampunk_mechanic_target`、`steampunk_change_of_venue_choose_minion`、`steampunk_change_of_venue_choose_base` 三个手工 `ONGOING_ATTACHED` 出口统一改走 helper，`ownerId !== sourcePlayerId` 时自动补 `sourcePlayerId`
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>) 新增：
  - `steampunk_mechanic_target: 重打被他人拥有的 ongoing 时仍应保留 owner 并带 sourcePlayerId`
  - 夹具：`P0.discard=['dis-borrowed(owner=1):steampunk_escape_hatch']`，再真实走 `mechanic -> recover -> choose base`
  - 首轮红灯已坐实：`ONGOING_ATTACHED.payload.ownerId === '0'`，且缺少 `sourcePlayerId`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/expansionOngoing.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "steampunk_mechanic_target: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文|steampunk_mechanic_target: 重打被他人拥有的 ongoing 时仍应保留 owner 并带 sourcePlayerId|steampunk_change_of_venue_choose_minion: 重打无 onPlay 的随从附着 ongoing 时仍应保留 ACTION_PLAYED 目标随从上下文|steampunk_change_of_venue_choose_base: 重打无 onPlay 的 ongoing 时仍应保留 ACTION_PLAYED 目标基地上下文"` -> `1 file passed, 4 passed`
- [x] 当前边界已写死
  - 这格只闭合 `steampunk_mechanic / steampunk_change_of_venue` replay ongoing attach 的 `ownerId/sourcePlayerId` provenance。
  - 不外推整个 `Steampunks`、所有 replay caller，或所有手工 `ONGOING_ATTACHED` 旁路已完成。

## Addendum（2026-05-23 23:15 +08）：`princesses_marie_degraw` borrowed deck-top -> owner deck bottom 收口

- [x] 命中新的一条对象级真红灯
  - 这次不是 `sleeping_beauty` 那个更广的 shuffle helper，也不是 `direct_to_dvd_sequel / woodland_helpers` 复读，而是 [`src/games/smashup/abilities/princesses.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/princesses.ts>) 里 `princesses_marie_degraw` 看到牌库顶行动后，把该牌放到底的分支仍写 `ownerId: ctx.playerId`
  - 若 `P0.deck` 顶部是 `owner='1'` 的 borrowed action，旧实现会把它错误留在 `P0.deck` 底，而不是送回 `P1.deck` 底
- [x] 已做最小对象级修复
  - 只改 `princesses_marie_degraw` 这一个 talent，不扩到整个 `Princesses` helper：
  - `buildValidatedCardToDeckBottomEvents(...)` 现在改按 `peeked.card.owner` 写 `ownerId`
  - 仅在 `peeked.card.owner !== ctx.playerId` 时补 `sourcePlayerId: ctx.playerId`
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增：
  - `princesses_marie_degraw 看到被他人拥有的牌库顶行动时，仍应放到其拥有者牌库底而不是当前玩家牌库底`
  - 夹具：`P0` 场上有 `marie-1`，`P0.deck=['borrowed-top(owner=1)','p0-deck-1']`，`P1.deck=['p1-deck-1']`
  - 首轮红灯已坐实：`borrowed-top` 错误留在 `P0.deck`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "princesses_marie_degraw 看到被他人拥有的牌库顶行动时，仍应放到其拥有者牌库底而不是当前玩家牌库底|princesses_direct_to_dvd_sequel 会把弃牌堆随从洗回牌库并抽 1|princesses_direct_to_dvd_sequel 选择被他人拥有的弃牌随从时，仍应洗回其拥有者牌库而不是当前玩家牌库|princesses_snow_white 会把另一个基地上的仆从移动到这里"` -> `1 file passed, 4 passed`
- [x] 当前边界已写死
  - 这格只闭合 `princesses_marie_degraw` 在 borrowed deck-top -> owner deck bottom 场景下的 `ownerId/sourcePlayerId` provenance。
  - 不外推整个 `Princesses`、所有 peek/topdeck/bottom caller，或 `sleeping_beauty` 那条更广 helper 已完成。

## Addendum（2026-05-24 09:46 +08）：`bury_uncover` borrowed buried ongoing attach owner provenance 收口

- [x] 命中新的一条手工 `ONGOING_ATTACHED` 旁路红灯
  - 这次不是 shared `PLAY_ACTION(ongoing) -> ONGOING_ATTACHED` producer 回退，也不是 `steampunk` replay attach 复读，而是 [`src/games/smashup/domain/bury.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/bury.ts>) 的 buried uncover 手工旁路仍写 `ownerId: playerId`
  - 当 `controllerId='0' / trueOwnerId='1'` 的 borrowed buried ongoing 被 P0 在回合开始翻开并附着时，旧链会把 attached action 错挂成 P0 拥有
- [x] 已做最小对象级修复
  - `executeUncoveredAction()` 的 base-target 与 minion-target 两条 ongoing attach 分支改用 `buried.trueOwnerId`
  - `handleUncoverOngoingPickTargetMinion()` 的二段目标选择分支同样按 live buried card 的 `trueOwnerId` 写 `ONGOING_ATTACHED.ownerId`
  - 同步给 `buildActionPlayedEvent(... fromBuried)` 传入 `ownerId: buried.trueOwnerId`，保持 action-play event payload 与真实 owner 一致
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/buryEngine.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/buryEngine.test.ts>) 新增：
  - `uncovering a borrowed buried ongoing action should preserve true owner when attaching`
  - 首轮红灯已坐实：`ONGOING_ATTACHED.payload.ownerId === '0'`，而不是预期的 `'1'`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/buryEngine.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "borrowed buried ongoing"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/buryEngine.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 12 passed`
- [x] 当前边界已写死
  - 这格只闭合 `bury_uncover_start_turn / bury_uncover_ongoing_target` 在 borrowed buried ongoing 场景下的 attached owner provenance。
  - 不外推所有 buried action、所有二段目标 prompt、所有手工 `ONGOING_ATTACHED` 旁路或整个 buried engine 已完成。

## Addendum（2026-05-24 10:04 +08）：`time_travelers_its_astounding` discard ongoing attach owner/source provenance 收口

- [x] 命中新的一条弃牌堆额外行动手工旁路红灯
  - 这次不是 shared `PLAY_ACTION(ongoing) -> ONGOING_ATTACHED` producer 回退，也不是 buried/steampunk replay attach 复读，而是 [`src/games/smashup/abilities/yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `buildDiscardActionPlayEvents(...)` 在 `time_travelers_its_astounding` 从弃牌堆额外打出 ongoing action 时仍写 `ONGOING_ATTACHED.ownerId: playerId`
  - 当 P0 从自己 discard 额外打出 `owner='1'` 的 borrowed `cyborg_apes_cyberevolution` 并附着到随从时，旧链会把 attached action 错挂成 P0 拥有
  - 首轮 focused 红灯已坐实：`attachedActions[0].ownerId === '0'`，预期为 `'1'`
- [x] 已做最小对象级修复
  - `buildDiscardActionPlayEvents(...)` 调用 `buildActionPlayedEvent(...)` 时传入 `ownerId: card.owner`
  - ongoing 分支的手工 `ONGOING_ATTACHED` 改为 `ownerId: card.owner`
  - borrowed 场景补 `sourcePlayerId: playerId`，让 reducer 从 source discard/hand 移除，同时附着后的 action 保留真实 owner
- [x] 同步补 shared reducer 相邻修复
  - 邻近 `令人震惊` 子集暴露 `ACTION_PLAYED(fromDiscard)` same-owner standard action 会把 source card 从 discard 移除后又基于旧 `targetOwner.discard` 追加，导致弃牌堆重复保留被打出的行动
  - [`src/games/smashup/domain/reduce.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reduce.ts>) 的 `ACTION_PLAYED` 现在在 `resolvedOwnerId === playerId` 时以 `discardWithoutSource` 作为 target discard 基底；borrowed owner 仍以真实 owner discard 为基底
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts>) 新增：
  - `时间旅行者：令人震惊从弃牌堆打出 borrowed 持续行动时，附着后仍应保留真实 owner`
  - 夹具：`P0.discard=['borrowed-evo(owner=1):cyborg_apes_cyberevolution']`，P0 打出 `Its Astounding` 后选择 `host-a`
  - 修复后断言 `host-a.attachedActions[0].ownerId === '1'`，P0 discard 只剩 `astounding-a`，P1 discard 不接收该 ongoing
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "borrowed 持续行动"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/yuanhouFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "令人震惊"` -> 修复相邻 duplicate 后 `1 file passed, 7 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/actionAndTalent.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 6 passed`
- [x] 当前边界已写死
  - 这格只闭合 `time_travelers_its_astounding` 从弃牌堆额外打出 borrowed ongoing action 的 attached owner/source provenance，以及 `ACTION_PLAYED(fromDiscard)` same-owner discard 基底重复问题。
  - 不外推所有 discard-action extra play、所有 `Its Astounding` 分支、所有手工 `ONGOING_ATTACHED` 旁路或整个 Time Travelers / yuanhou 全量完成。

## Addendum（2026-05-24 10:28 +08）：`duel` borrowed ongoing attach owner/source provenance 收口

- [x] 命中一条决斗行动旁路红灯
  - 这次不是 shared `PLAY_ACTION(ongoing) -> ONGOING_ATTACHED` reducer 回退，而是 [`src/games/smashup/domain/duel.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/duel.ts>) 在决斗中打出行动时手工拼 `ACTION_PLAYED` / `ONGOING_ATTACHED`。
  - 旧 `playOngoingActionAsDuelCard()` 没读取手牌实例真实 `card.owner`，因此 P0 在决斗中打出 `owner='1'` 的 borrowed ongoing action 时，会把 attached action 错写成 P0 拥有。
  - 同路径的标准行动 `playActionAsDuelCard()` 也需要把 `ACTION_PLAYED.ownerId` 写成手牌实例真实 owner，避免决斗旁路继续丢失 action owner provenance。
- [x] 已做最小 shared side-branch 修复
  - `playActionAsDuelCard()` 改用 `buildActionPlayedEvent(...)`，并传入 `ownerId: card.owner` 与 `targetBaseIndex: duel.baseIndex`。
  - `playOngoingActionAsDuelCard()` 从 `state.core.players[playerId].hand` 读取 source card，`ACTION_PLAYED.ownerId` 与 `ONGOING_ATTACHED.ownerId` 都改成真实 `ownerId`。
  - borrowed 场景向 `ONGOING_ATTACHED` 补 `sourcePlayerId: playerId`，让 reducer 从当前玩家手牌移除 source card，同时附着后的 ongoing 保留真实 owner。
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/reactionQueueActionPlayedDuel.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueActionPlayedDuel.test.ts>) 新增：
  - `决斗中打出被他人拥有的 ongoing 行动时，应保留真实 owner 并附着到目标上`
  - 夹具：P0 手牌持有 `borrowed-duel-action(owner=1):cyborg_apes_juiced_up`，在决斗中选择该行动并附着到 `challenger-1`。
  - 修复后断言 `ACTION_PLAYED.ownerId === '1'`、`ONGOING_ATTACHED.ownerId === '1'`、最终宿主 `attachedActions` 保留 `ownerId='1'`，且 source card 已从 P0 hand 移除。
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueActionPlayedDuel.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "被他人拥有的 ongoing"` -> `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueActionPlayedDuel.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "附着到随从"` -> `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueActionPlayedDuel.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 2 passed`
- [x] 当前边界已写死
  - 这格只闭合决斗中从手牌打出 borrowed ongoing action 并附着目标的 `ACTION_PLAYED ownerId` 与 `ONGOING_ATTACHED owner/source` 旁路。
  - 不外推所有 duel、所有 `ACTION_PLAYED` caller、所有 `ONGOING_ATTACHED` caller、所有 extra action/replay caller 或整个 action-play family 已完成。

## Addendum（2026-05-24 10:47 +08）：`wizard_mass_enchantment` external action `ACTION_PLAYED.ownerId` provenance 收口

- [x] 命中一条 Wizards external action side-branch 红灯
  - 这次不是 `wizard_mass_enchantment` 的 ongoing attach owner 旧格复读；当前 attach 已经使用 `sourceCard.owner`。
  - 真缝是同批 `ACTION_PLAYED` 仍没有 `ownerId`：[`src/games/smashup/abilities/wizards.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/wizards.ts>) 的 `resolveWizardExternalActionPlay()` 手工调用 `buildActionPlayedEvent(...)` 时未传 source card owner。
  - 因此 P0 从 P1 deck 通过 `Mass Enchantment` 打出 `owner='1'` 的 borrowed ongoing action 时，最终附着 owner 是对的，但 action-play event provenance 仍不可审计。
- [x] 已做最小对象级修复
  - `resolveWizardExternalActionPlay()` 的 `buildActionPlayedEvent(...)` 增量传入 `ownerId: sourceCard.owner`。
  - 不改变候选生成、目标选择、`CARD_TRANSFERRED`、ongoing attach、`appendResolvedActionAbility(...)` 或 `wizard_portal` sibling。
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/query6Abilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/query6Abilities.test.ts>) 现有两条真实链追加 owner provenance 断言：
  - `wizard_mass_enchantment: 打出附着到随从的行动时应保留目标上下文并触发 base_enchanted_glade`
  - `wizard_mass_enchantment: 打出无 onPlay 的基地 ongoing 时应保留目标上下文，并让 base_enchanted_glade 以 base-target 语义入队但不抽牌`
  - 首轮红灯坐实两条 `ACTION_PLAYED.payload` 都缺 `ownerId:'1'`，修复后两条转绿。
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/query6Abilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "wizard_mass_enchantment: 打出附着到随从的行动时应保留目标上下文并触发 base_enchanted_glade|wizard_mass_enchantment: 打出无 onPlay 的基地 ongoing 时应保留目标上下文"` -> 首轮红灯后 `1 file passed, 2 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/query6Abilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 33 passed`
- [x] 当前边界已写死
  - 这格只闭合 `wizard_mass_enchantment` external action play 的 `ACTION_PLAYED.ownerId` provenance，与已存在的 target context / ongoing attach owner evidence 对齐。
  - 不外推整个 Wizards、所有 external action caller、所有 `ACTION_PLAYED` consumer，或所有 topdeck / replay / discard play side branch 已完成。

## Addendum（2026-05-24 15:05 +08）：`vikings_viking_funeral` queued trigger owner metadata 收口

- [x] 命中新的一条 queued trigger registration 红灯
  - 这次不是 `Viking Funeral` 的 VP 奖励或移出宿主语义失败，而是 [`src/games/smashup/abilities/vikings.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vikings.ts>) 的 `registerTrigger('vikings_viking_funeral', 'onMinionDestroyed'|'onMinionDiscardedFromBase', ...)` 未显式写 `playerContext:'sourceController'`
  - 当 P1 控制的宿主身上附着 P0 拥有的 `Viking Funeral`，并由 P1 作为事件玩家离场时，旧 queue metadata 中 `sourceControllerId === '0'`，但 `ownerPlayerId === '1'` 且 `playerContext === 'eventPlayer'`
- [x] 已做最小修复
  - 只给 `vikings_viking_funeral` 的 `onMinionDestroyed` 与 `onMinionDiscardedFromBase` 两个注册补 `playerContext:'sourceController'`
  - 不改 `vikingsVikingFuneralTrigger()` 回调、VP 奖励、宿主移出游戏、LKI 或计分逻辑
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增：
  - `sourceController queued onMinionDiscardedFromBase trigger 应把 Viking Funeral 归给附着行动拥有者而不是宿主控制者`
  - 首轮红灯坐实：`ownerPlayerId === '1'`、`playerContext === 'eventPlayer'`，预期为 `ownerPlayerId === '0'`、`playerContext === 'sourceController'`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Viking Funeral"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 104 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "vikings_viking_funeral"` -> `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Viking Funeral"` -> `1 file passed, 1 passed`
  - `npx eslint src/games/smashup/abilities/vikings.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts` -> `0 errors, 22 existing any warnings`
- [x] 当前边界已写死
  - 这格只闭合 `vikings_viking_funeral` 在 queued `onMinionDestroyed/onMinionDiscardedFromBase` 路径下的 trigger owner metadata。
  - 不外推 Vikings 全量、所有附着行动 trigger、所有 `onMinionDiscardedFromBase` trigger、所有 sourceController/eventPlayer family 或所有计分离场路径已完成。

## Addendum（2026-05-24 15:12 +08）：`world_champs_samurai_chan` queued trigger owner metadata 收口

- [x] 命中新的一条 queued trigger registration 红灯
  - 这次不是 `World Champs Samurai Chan` 的抽牌效果失败，而是 [`src/games/smashup/abilities/world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `registerTrigger('world_champs_samurai_chan', 'onMinionDestroyed'|'onMinionDiscardedFromBase', ...)` 未显式写 `playerContext:'sourceController'`
  - 当 P1 控制的 `World Champs Samurai Chan` 以 P0 作为事件玩家离场时，旧 queue metadata 中 `sourceControllerId === '1'`，但 `ownerPlayerId === '0'` 且 `playerContext === 'eventPlayer'`
- [x] 已做最小修复
  - 只给 `world_champs_samurai_chan` 的 `onMinionDestroyed` 与 `onMinionDiscardedFromBase` 两个注册补 `playerContext:'sourceController'`
  - 不改 `worldChampsSamuraiChanTrigger()` 抽牌回调、onPlay、计分、LKI 或其它 World Champs trigger
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增：
  - `sourceController queued onMinionDiscardedFromBase trigger 应把 World Champs Samurai Chan 归给自身控制者而不是事件玩家`
  - 首轮红灯坐实：`ownerPlayerId === '0'`、`playerContext === 'eventPlayer'`，预期为 `ownerPlayerId === '1'`、`playerContext === 'sourceController'`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "World Champs Samurai Chan"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 105 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_samurai_chan"` -> `1 file passed, 2 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "World Champs Samurai Chan"` -> `1 file passed, 1 passed`
  - `npx eslint src/games/smashup/abilities/world_champs.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这格只闭合 `world_champs_samurai_chan` 在 queued `onMinionDestroyed/onMinionDiscardedFromBase` 路径下的 trigger owner metadata。
  - 不外推 World Champs 全量、所有自身离场抽牌 trigger、所有 Samurai/Samurai Chan sibling、所有 sourceController/eventPlayer family 或所有计分离场路径已完成。

## Addendum（2026-05-24 15:18 +08）：`world_champs_smart_set_up` queued trigger owner metadata 收口

- [x] 命中新的一条 queued trigger registration 红灯
  - 这次不是 `Smart Set-Up` 抽牌效果失败，也不是负向 `canTrigger` 分支漂移，而是 [`src/games/smashup/abilities/world_champs.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/world_champs.ts>) 的 `registerTrigger('world_champs_smart_set_up', 'onMinionPlayed', ...)` 未显式写 `playerContext:'sourceController'`
  - 当 P1 的宿主随从上附着 P0 拥有的 `Smart Set-Up`，且 P1 第一次把随从打到该基地时，旧 queue metadata 中 `sourceControllerId === '0'`，但 `ownerPlayerId === '1'` 且 `playerContext === 'eventPlayer'`
- [x] 已做最小修复
  - 只给 `world_champs_smart_set_up.onMinionPlayed` 注册补 `playerContext:'sourceController'`
  - 保留既有 `sourceScope:'triggerBase'` 与 `canTriggerWorldChampsSmartSetUp`，不改抽牌回调、首次打到基地判断或 attached host 判断
- [x] 已补 focused red-green gate
  - [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增：
  - `sourceController queued onMinionPlayed trigger 应把 Smart Set-Up 归给附着行动拥有者而不是打出随从玩家`
  - 首轮红灯坐实：`ownerPlayerId === '1'`、`playerContext === 'eventPlayer'`，预期为 `ownerPlayerId === '0'`、`playerContext === 'sourceController'`
- [x] 已做最小验证
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Smart Set-Up"` -> 首轮红灯后 `1 file passed, 2 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 106 passed`
  - `node --max-old-space-size=4096 scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "world_champs_smart_set_up|world_champs_samurai_chan"` -> `1 file passed, 3 passed`
  - `npx eslint src/games/smashup/abilities/world_champs.ts src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这格只闭合 `world_champs_smart_set_up` 在 queued `onMinionPlayed` 路径下的 trigger owner metadata。
  - 不外推 World Champs 全量、所有 attached action onMinionPlayed trigger、所有 `sourceScope:'triggerBase'` trigger、所有 canTrigger 分支或整个 sourceController/eventPlayer family 已完成。

## Addendum（2026-05-24 18:56 +08）：`robot_hoverbot / bear_cavalry_commission` borrowed minion play owner provenance 收口

- [x] 命中两条 handbuilt `MINION_PLAYED` owner 红灯
  - `robot_hoverbot` 从当前玩家 deck 顶额外打出 minion 时，prompt value/base context 未携带 source card `owner`。
  - `bear_cavalry_commission` 从当前玩家 hand 额外打出 minion 时，hand option/continuation context 未携带 source card `owner`。
  - 两条旧链在 `owner='1'` 的 borrowed source card 上都会把落地随从 owner 洗成行动玩家 P0。
- [x] 已做最小修复
  - [`robots.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/robots.ts>)：`RobotHoverbotChoice` / `RobotHoverbotBaseContext` 增加 `ownerId`，play option、live option、单基地直落和二段 base resolve 都写入 `MINION_PLAYED.payload.ownerId`。
  - [`bear_cavalry.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/bear_cavalry.ts>)：Commission hand-minion option 增加 `ownerId: c.owner`，单基地直落与二段 choose-base resolve 都写入 `MINION_PLAYED.payload.ownerId`。
  - 不改 Hoverbot reveal/skip、Commission 额外随从额度、移动后续链、targeting 或通用 reducer。
- [x] 已补 focused red-green gate
  - [`robot-hoverbot-chain.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/robot-hoverbot-chain.test.ts>)：`从牌库顶打出 borrowed 随从时，应保留真实 owner`，首轮红灯 `owner='0'`，修复后 `owner='1'`。
  - [`expansionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionAbilities.test.ts>)：`选择手牌 borrowed 随从打出时，应保留真实 owner`，首轮红灯 `owner='0'`，修复后 `owner='1'`。
- [x] 已做最小验证
  - `robot-hoverbot-chain.test.ts --testNamePattern "borrowed 随从"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `expansionAbilities.test.ts --testNamePattern "borrowed 随从"` -> 首轮红灯后 `1 file passed, 1 passed`
  - 整文件 `robot-hoverbot-chain.test.ts` -> `1 file passed, 4 passed`
  - 整文件 `expansionAbilities.test.ts` -> `1 file passed, 32 passed`
  - ESLint `robots.ts + robot-hoverbot-chain.test.ts + bear_cavalry.ts + expansionAbilities.test.ts` -> `0 errors, existing warnings`
- [x] 当前边界已写死
  - 这格只闭合 Hoverbot deck-top borrowed minion 与 Commission hand borrowed minion 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Robots 全量、Bear Cavalry 全量、所有 fromDeck/fromHand minion extra play 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 19:12 +08）：`alien_terraform` borrowed hand minion play owner provenance 收口

- [x] 命中一条 handbuilt `MINION_PLAYED(fromHand)` owner 红灯
  - `alien_terraform` 在替换基地后的第三步允许行动玩家额外打出一个手牌随从。
  - 旧 `alien_terraform_play_minion` resolve 虽然从 live hand 取到了 `selectedCard`，但 `MINION_PLAYED.payload` 未携带 `selectedCard.owner`，会把 borrowed hand minion 的落地 owner 洗成当前玩家。
- [x] 已做最小修复
  - [`aliens.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/aliens.ts>)：`alien_terraform_play_minion` 的 `MINION_PLAYED.payload` 增加 `ownerId: selectedCard.owner` 与 `reason:'alien_terraform'`。
  - 不改基地替换、baseDeck shuffle、skip、Titan as minion 分支、额外随从额度或通用 reducer。
- [x] 已补 focused red-green gate
  - [`alienAuditFixes.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/alienAuditFixes.test.ts>)：`alien_terraform: 第三步额外打出借来的手牌随从时应保留真实 owner`
  - 首轮红灯：事件 payload 缺 `ownerId:'1'`，最终落地随从 `controller='0'` 但 `owner='0'`；修复后保持 `owner='1'`。
- [x] 已做最小验证
  - `alienAuditFixes.test.ts --config vitest.config.audit.ts --testNamePattern "alien_terraform: 第三步额外打出借来的手牌随从时应保留真实 owner"` -> 首轮红灯后 `1 file passed, 1 passed`
  - 整文件 `alienAuditFixes.test.ts --config vitest.config.audit.ts` -> `1 file passed, 15 passed`
  - ESLint `aliens.ts + alienAuditFixes.test.ts` -> `0 errors, 2 existing warnings`
- [x] 当前边界已写死
  - 这格只闭合 Terraform 第三步 hand borrowed minion 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Aliens 全量、Terraform Titan 分支、所有 fromHand minion extra play 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 19:19 +08）：`mermaids_ultimate_song_hand` borrowed hand minion play owner provenance 收口

- [x] 命中一条 forced handbuilt `MINION_PLAYED(fromHand)` owner 红灯
  - `mermaids_ultimate_song` 先由施放者选择目标基地，再逐个强制其他玩家从 hand 额外打出力量 3 或以下的随从。
  - 旧 `mermaids_ultimate_song_hand` prompt value 只带 `cardUid/defId/power`，resolve 未从 forced player live hand 读取 source card owner，导致 borrowed hand minion 落地 owner 被洗成 forced player。
- [x] 已做最小修复
  - [`mermaids.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mermaids.ts>)：`HandMinionChoice` / `buildHandMinionOptions(...)` 携带 `ownerId`，resolve 再用 live hand 复核 `selectedCard` 并写入 `MINION_PLAYED.payload.ownerId`。
  - 同一事件补 `reason:'mermaids_ultimate_song'`，不改 base prompt、forced player 顺序、power≤3 过滤、skip-on-play、施放者额外额度或通用 reducer。
- [x] 已补 focused red-green gate
  - [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>)：`mermaids_ultimate_song 强制对手打出 borrowed 小随从时应保留真实 owner`
  - 首轮红灯：事件 payload 缺 `ownerId:'0'`，最终落地随从 `controller='1'` 但 `owner='1'`；修复后保持 `owner='0'`。
- [x] 已做最小验证
  - focused `newFactionAbilities.test.ts --testNamePattern "mermaids_ultimate_song 强制对手打出 borrowed 小随从时应保留真实 owner"` -> 首轮红灯后 `1 file passed, 1 passed`
  - scoped neighbor `newFactionAbilities.test.ts --testNamePattern "mermaids_ultimate_song"` -> `1 file passed, 2 passed`
  - ESLint `mermaids.ts + newFactionAbilities.test.ts` -> `0 errors, 19 existing warnings`
  - 整文件 `newFactionAbilities.test.ts` 当前仍有 7 个 unrelated failures；本格不把整文件失败收口成 Mermaids 语义失败，也不擅自修其它业务线。
- [x] 当前边界已写死
  - 这格只闭合 Ultimate Song hand forced borrowed minion 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Mermaids 全量、所有 Ultimate Song 多玩家/无候选分支、所有 forced extra play、所有 fromHand minion extra play 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 19:27 +08）：`killer_plants` deck-search borrowed minion play owner provenance 收口

- [x] 命中一条 shared deck-search `MINION_PLAYED(fromDeck)` owner 红灯
  - `killer_plant_venus_man_trap` 与 `killer_plant_sprout` / POD 共享 `buildKillerPlantDeckSearchResolutionEvents(...)`。
  - 旧 resolver 只按所选 UID 检查还在 deck 中，随后发 `MINION_PLAYED`，未写 source card owner；borrowed deck minion 会落地成当前玩家 owner。
- [x] 已做最小修复
  - [`killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>)：resolver 从 live deck snapshot 按 `uid/defId/type` 定位 `selectedCard`，并把 `selectedCard.owner` 写入 `MINION_PLAYED.payload.ownerId`。
  - 同一事件补 `fromDeck:true` 与 `reason:abilitySourceId`，不改候选过滤、skip、deck reshuffle、Sprout 自毁或 Venus talent 入口。
- [x] 已补 focused red-green gate
  - [`expansionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionOngoing.test.ts>)：`牌库检索打出 borrowed 随从时应保留真实 owner`
  - 首轮红灯：payload 缺 `ownerId:'1'`、`fromDeck:true`、`reason:'killer_plant_venus_man_trap'`；修复后转绿。
- [x] 已做最小验证
  - focused `expansionOngoing.test.ts --testNamePattern "牌库检索打出 borrowed 随从时应保留真实 owner"` -> 首轮红灯后 `1 file passed, 1 passed`
  - scoped neighbor `expansionOngoing.test.ts --testNamePattern "killer_plant_venus_man_trap"` -> `1 file passed, 5 passed`
  - 整文件 `expansionOngoing.test.ts` -> `1 file passed, 75 passed`
  - ESLint `killer_plants.ts + expansionOngoing.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这格只闭合 Killer Plants deck-search minion play 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Killer Plants 全量、Budding/Blossom、所有 deck-search caller、所有 fromDeck minion extra play 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 19:39 +08）：`base_greenhouse` replacement-base borrowed deck minion owner provenance 收口

- [x] 命中一条 replacement-base / pending `MINION_PLAYED(fromDeck)` owner 红灯
  - `base_greenhouse` afterScoring 让赢家从 deck 搜索一个 minion，并把它打出到替换后的基地。
  - 旧 handler 只检查所选 UID 仍在当前玩家 deck 中，pending action 与直接 `MINION_PLAYED` 都未携带 source card `owner`；`buildPendingPostScoringActionEvents(...)` 补发时也只按 `playerId` 落 owner。
  - 因此 P0 deck 中真实 `owner='1'` 的 borrowed minion 经 Greenhouse 直接打出或 deferred replacement 补发后都会落地成 P0 owner。
- [x] 已做最小修复
  - [`baseAbilities_expansion.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities_expansion.ts>)：`base_greenhouse` resolve 改为从 live deck 按 `uid/defId/type` 取 `selectedCard`，并把 `selectedCard.owner` 写入 pending action 和直接 `MINION_PLAYED.payload.ownerId`。
  - [`types.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/types.ts>)：`PendingPostScoringAction.playMinionOnReplacementBase` 增量支持 `ownerId?: PlayerId`。
  - [`scoringSession.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/scoringSession.ts>)：`buildPendingPostScoringActionEvents(...)` 补发前定位 live deck card，`MINION_PLAYED.payload.ownerId` 使用 `action.ownerId ?? cardStillInDeck.owner`。
  - 不改 replacement base target sync、skip、deck search 候选、normal limit、base clear/replaced deferred flow 或通用 reducer。
- [x] 已补 focused red-green gate
  - [`expansionBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/expansionBaseAbilities.test.ts>)：
  - `base_greenhouse: 直接打出 borrowed 牌库随从时应保留真实 owner`
  - `base_greenhouse: replacement deferred 打出 borrowed 牌库随从时应保留真实 owner`
  - 首轮红灯：直接分支 `MINION_PLAYED.payload` 缺 `ownerId:'1'`；deferred 分支 `playMinionOnReplacementBase` 也缺 `ownerId:'1'`。
- [x] 已做最小验证
  - focused `expansionBaseAbilities.test.ts --testNamePattern "base_greenhouse: .*borrowed"` -> 首轮红灯后 `1 file passed, 2 passed`
  - scoped neighbor `expansionBaseAbilities.test.ts --testNamePattern "base_greenhouse|queued reaction 选择温室"` -> `1 file passed, 7 passed`
  - 整文件 `expansionBaseAbilities.test.ts` -> `1 file passed, 53 passed`
  - sibling deferred gate `afterscoring-window-skip-base-clear.test.ts --testNamePattern "base_greenhouse|playMinionOnReplacementBase|deferred replacement"` -> `1 file passed, 5 passed`
  - ESLint `baseAbilities_expansion.ts + scoringSession.ts + types.ts + expansionBaseAbilities.test.ts` -> `0 errors, 2 existing warnings`（`types.ts` 既有 unused 参数 warning）
- [x] 当前边界已写死
  - 这格只闭合 `base_greenhouse` direct/deferred replacement-base deck minion play 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推所有基地能力、所有 afterScoring deferred action、所有 replacement-base sibling、所有 fromDeck minion extra play 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 19:57 +08）：`giant_ant_worker_pod` borrowed discard replay owner provenance 收口

- [x] 命中一条 handbuilt `MINION_PLAYED(fromDiscard)` owner 红灯
  - `giant_ant_worker_pod` 在无指示物并离场进入 discard 后，可选择把自己从 discard 打到另一个基地。
  - 旧 `giantAntWorkerPodReplayPromptProgram` 已从当前玩家 live discard 找到 `cardInDiscard`，但发 `MINION_PLAYED(fromDiscard:true)` 时未写 `ownerId`。
  - 因此 P0 控制、P1 拥有的 borrowed Worker POD 进入 P0 discard 后被重打，会落地成 P0 owner。
- [x] 已做最小修复
  - [`giant_ants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/giant_ants.ts>)：`giant_ant_worker_pod_replay` 的 `MINION_PLAYED.payload` 增加 `ownerId: cardInDiscard.owner`。
  - 不改 trigger 入队条件、无指示物检查、目标基地选择、`discardPlaySourceId`、额度或通用 reducer。
- [x] 已补 focused red-green gate
  - [`giantAntsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/giantAntsPod.test.ts>)：`从弃牌堆重打 borrowed Worker POD 时应保留真实 owner`。
  - 首轮红灯：`MINION_PLAYED.payload` 缺 `ownerId:'1'`，落地随从 owner 会默认成当前玩家。
- [x] 已做最小验证
  - focused `giantAntsPod.test.ts --testNamePattern "borrowed Worker POD"` -> 首轮红灯后 `1 file passed, 1 passed`
  - `newFactionAbilities.test.ts --testNamePattern "giant_ant_worker_pod"` -> `1 file passed, 1 passed`
  - `baseScoring.test.ts --testNamePattern "Worker POD"` -> `1 file passed, 1 passed`
  - 整文件 `giantAntsPod.test.ts` -> `1 file passed, 9 passed`
  - ESLint `giant_ants.ts + giantAntsPod.test.ts` -> `0 errors, 7 existing any warnings`
- [x] 当前边界已写死
  - 这格只闭合 `giant_ant_worker_pod` discard replay 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Giant Ants 全量、所有 Worker POD scoring/destroy sibling、所有 fromDiscard replay、所有 onMinionDiscardedFromBase trigger 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 20:16 +08）：`elder_thing_spreading_horror_pod` borrowed discard small-minion play owner provenance 收口

- [x] 命中一条 handbuilt `MINION_PLAYED(fromDiscard)` owner 红灯
  - `elder_thing_spreading_horror_pod` 在对手拒绝弃两张非疯狂卡后，允许施放者从 discard 打出一个力量 3 或以下随从。
  - 旧 choose-minion resolve 只消费 prompt value 的 `cardUid/defId`，发 `MINION_PLAYED(fromDiscard:true)` 时未写 source card owner。
  - 因此 P0 discard 中真实 `owner='1'` 的 borrowed 小随从会落地成 P0 owner。
- [x] 已做最小修复
  - [`elder_things.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/elder_things.ts>)：`elder_thing_spreading_horror_pod_choose_minion` resolve 从 live discard 按 `uid/defId/type` 定位 `selectedCard`，并把 `selectedCard.owner` 写入 `MINION_PLAYED.payload.ownerId`。
  - 不改对手拒绝流程、may-play 次数、base 选择、`discardPlaySourceId`、额度或通用 reducer。
- [x] 已补 focused red-green gate
  - [`elderThingsPod.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/elderThingsPod.test.ts>)：`从弃牌堆打出 borrowed 小随从时应保留真实 owner`。
  - 首轮先暴露夹具错误：`robot_microbot` 在该候选链缺稳定小随从定义，被 power 过滤；改用同文件现有候选链证明过的 `elder_thing_byakhee_pod` 后，真红灯坐实 `MINION_PLAYED.payload.ownerId === undefined`。
- [x] 已做最小验证
  - focused `elderThingsPod.test.ts --testNamePattern "从弃牌堆打出 borrowed 小随从时应保留真实 owner"` -> 真红灯后 `1 file passed, 1 passed`
  - scoped neighbor `elderThingsPod.test.ts --testNamePattern "Spreading Horror POD|spreading_horror_pod|散播恐怖"` -> `1 file passed, 3 passed`
  - 整文件 `elderThingsPod.test.ts` -> `1 file passed, 18 passed`
  - ESLint `elder_things.ts + elderThingsPod.test.ts` -> `0 errors, 4 existing warnings`
- [x] 当前边界已写死
  - 这格只闭合 `elder_thing_spreading_horror_pod` discard small-minion replay 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Elder Things 全量、普通 Spreading Horror、所有 POD discard replay、所有 fromDiscard minion replay caller 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 20:22 +08）：`ninja_disguise` borrowed hand minion play owner provenance 收口

- [x] 命中一条 handbuilt `MINION_PLAYED(fromHand)` owner 红灯
  - `ninja_disguise` 返回己方随从后，从当前玩家 hand 打出同数量随从到同一基地。
  - 旧 hand option 只带 `cardUid/defId/power`，play resolve 直接发 `MINION_PLAYED`，未携带 source hand card owner。
  - 因此 P0 hand 中真实 `owner='1'` 的 borrowed minion 会落地成 P0 owner。
- [x] 已做最小修复
  - [`ninjas.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/ninjas.ts>)：`ninja_disguise_choose_play` resolve 从 live hand 按 `uid/defId/type` 定位 `selectedCard`，并把 `selectedCard.owner` 写入 `MINION_PLAYED.payload.ownerId`。
  - 不改可返回随从筛选、返回事件、最多打出数量、第二张手牌选择、额外额度或通用 reducer。
- [x] 已补 focused red-green gate
  - [`query6Abilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/query6Abilities.test.ts>)：`ninja_disguise: 打出 borrowed 手牌随从时应保留真实 owner`。
  - 首轮红灯：`MINION_PLAYED.payload.ownerId === undefined`。
- [x] 已做最小验证
  - focused `query6Abilities.test.ts --testNamePattern "ninja_disguise: 打出 borrowed 手牌随从时应保留真实 owner"` -> 真红灯后 `1 file passed, 1 passed`
  - scoped neighbor `query6Abilities.test.ts --testNamePattern "ninja_disguise"` -> `1 file passed, 4 passed`
  - 整文件 `query6Abilities.test.ts` -> `1 file passed, 35 passed`
  - ESLint `ninjas.ts + query6Abilities.test.ts` -> `0 errors, 21 existing warnings`
- [x] 当前边界已写死
  - 这格只闭合 `ninja_disguise` hand borrowed minion play 的 `MINION_PLAYED.ownerId` provenance。
  - 不外推 Ninjas 全量、Disguise 两张回手/二段打出所有分支、所有 fromHand minion play caller 或整个 `MINION_PLAYED` producer family 已完成。

## Addendum（2026-05-24 20:52 +08）：`mythic_greeks_favor_of_athena` discard reshuffle borrowed owner split 收口

- [x] 命中一条 `DECK_REORDERED(sourcePlayerId)` sibling 真红灯
  - `mythic_greeks_favor_of_athena` 在牌库不足 5 张时会把当前玩家 discard 洗入 reveal snapshot。
  - 旧 `buildAthenaRevealEvents(...)` 直接把 `random.shuffle([...player.discard])` 全量并入当前玩家 `deckSim`，随后发 `DECK_REORDERED.playerId=playerId`。
  - 因此 `P0.discard` 中真实 `owner='1'` 的 `borrowed-discard` 会被错误洗进 `P0.deck`，并出现在 Athena 本次 reveal/order 链里。
- [x] 已做最小修复
  - [`mythic_greeks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/mythic_greeks.ts>)：`buildAthenaRevealEvents(...)` 的 discard reshuffle 分支改为先按 `card.owner` 分流。
  - source-owned 或无有效 owner 的牌继续进入当前玩家 `deckSim`；borrowed 牌为每个真实 owner 单独生成 `DECK_REORDERED { playerId: ownerId, sourcePlayerId: playerId }`。
  - 顺手把 `findCardOwnerAcrossPlayerZones(...)` 的未使用解构键去掉，消除本地 ESLint warning；不改 Athena pick/order prompt、draw、回顶顺序或其它 Mythic Greeks caller。
- [x] 已补 focused red-green gate
  - [`shayuFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/shayuFactionAbilities.test.ts>)：`神话希腊：雅典娜的恩惠牌库不足洗入弃牌堆时，应把 borrowed 弃牌分流回真实拥有者牌库`。
  - 首轮红灯：事件里只有 `DECK_REORDERED.playerId='0'`，且 `deckUids` 错含 `borrowed-discard`。
- [x] 已做最小验证
  - focused `shayuFactionAbilities.test.ts --testNamePattern "雅典娜的恩惠"` -> `1 file passed, 3 passed`
  - 整文件 `shayuFactionAbilities.test.ts` -> `1 file passed, 21 passed`
  - ESLint `mythic_greeks.ts + shayuFactionAbilities.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这格只闭合 `mythic_greeks_favor_of_athena` 在牌库不足 5 张、discard reshuffle 进入 reveal 前的 borrowed owner split。
  - 不外推 Mythic Greeks 全量、Athena 所有 pick/order 分支、所有 discard reshuffle caller 或所有 `DECK_REORDERED` producer 已完成。

## Addendum（2026-05-25 00:25 +08）：`world_champs_stoneford` borrowed source-deck action green gate

- [x] 已补一条 dedicated green evidence，而不是 runtime 修复
  - `world_champs_stoneford` 的语义是“从你的牌库选择一张行动卡加入手牌”。
  - 这里的 source zone 与 destination 都是当前玩家；即使 source deck 中存在真实 `owner!='currentPlayer'` 的 borrowed action，也不应机械套成 owner-deck split。
- [x] 已补 focused gate
  - [`newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>)：`world_champs_stoneford 选择被他人拥有的牌库行动时，仍应进入当前玩家手牌并保留真实 owner`。
  - 夹具：P0 手牌打出 `stoneford-1`，从自己牌库选择 `deck-borrowed-action(owner='1')`。
- [x] 已完成最小验证
  - focused `newFactionAbilities.test.ts --testNamePattern "world_champs_stoneford"` -> `1 file passed, 2 passed`
  - ESLint `newFactionAbilities.test.ts` -> `0 errors, 6 existing warnings`
- [x] 当前边界已写死
  - 这格只把 `world_champs_stoneford` 从 deck-search owner/source 候选降级为 dedicated green evidence：borrowed action 进入当前玩家手牌，且卡实例 `owner` 仍为真实 owner。
  - 不外推 World Champs 全量、所有 deck-search caller、所有 `CARDS_DRAWN`/deck-search family，或整个 owner/source provenance 主线已完成；下一步继续回到 `processAffectTriggers / ACTION_PLAYED side branch / effect-contract queued interaction` 查真红灯。

## Addendum（2026-05-25 01:40 +08）：`processDeckInspectionTriggers` extended-base queued ordering sibling 收口

- [x] 命中一条新的 post-process family 真红灯
  - `processDeckInspectionTriggers()` 旧实现会为 `REVEAL_HAND / REVEAL_DECK_TOP / DECK_INSPECTED` 收集 ongoing `onDeckInspected` trigger。
  - 但它没有像 `processMoveTriggers / processAffectTriggers / processReturnToHandTriggers` 那样同步扫描 `collectExtendedBaseAbilityTriggers(...)`。
  - 因此同一时机若同时命中 mandatory ongoing 与 mandatory extended base `onDeckInspected`，queue 里只会留下 ongoing，extended base 会静默漏排。
- [x] 已补 focused red-green gate
  - [`reactionQueueBaseAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueBaseAbilities.test.ts>)：`processDeckInspectionTriggers 命中 mandatory ongoing 与 mandatory extended base onDeckInspected 时，应统一进入 reaction ordering`
  - 首轮红灯：`sourceDefId='test_inspection_watcher'` 已入队，但 `sourceDefId='base_b'` 不入队。
- [x] 已做最小修复
  - [`reducer.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reducer.ts>)：`processDeckInspectionTriggers()` 在保留既有 ongoing `collectTriggers(...)` 的同时，追加按 `advancedCore.bases` 遍历收集 `collectExtendedBaseAbilityTriggers(...)`。
  - 透传 `ownerPlayerId=inspectionCausePlayerId`、`reason/frameId/sourceEventId`；不改 inspection context 结构、不改 queued executor、不扩大到其它 timing family。
- [x] 已做最小验证
  - focused `reactionQueueBaseAbilities.test.ts --testNamePattern "processDeckInspectionTriggers 命中 mandatory ongoing 与 mandatory extended base onDeckInspected 时，应统一进入 reaction ordering"` -> 真红灯后 `1 file passed, 1 passed`
  - 整文件 `reactionQueueBaseAbilities.test.ts` -> `1 file passed, 27 passed`
  - 邻近 `reactionQueueInspectionRuntimeContext.test.ts` -> `1 file passed, 2 passed`
  - ESLint `reducer.ts + reactionQueueBaseAbilities.test.ts` -> `0 errors, 23 existing warnings`
- [x] 当前边界已写死
  - 这格只闭合 `processDeckInspectionTriggers` 的 extended-base queued ordering sibling。
  - 不外推所有 `onDeckInspected` consumer、所有 inspection/reveal family、所有 extended base timing，或整个 post-process family 已完成；下一步继续离开 inspection 对象级链，回到 `ACTION_PLAYED side branch / processAffectTriggers / effect-contract queued interaction` 查下一格真红灯。

## Addendum（2026-05-25 01:46 +08）：`effect-contract queued interaction` 的 `topUids/bottomUids -> multi-card footprint` 收口

- [x] 命中一条新的 queued interaction 真红灯
  - `super_spies_for_my_eyes_only_reorder`、`super_spies_permit_to_kill_order`、`base_isis_swingin_pad_reorder` 这类排序 prompt 的 option value 会带 `targetPlayerId + topUids[] + bottomUids[]`。
  - 旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 只会把 `cardUids[]` 映射成多张 `cardInstance` footprint，`topUids[]/bottomUids[]` 会被完全忽略。
  - 因此 reorder prompt 的 footprint 只记到 `playerControl`，不会把被重排的整批牌写进冲突资源集合。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：`deck reorder 交互应从 topUids/bottomUids 推导多张 cardInstance footprint，而不是只记 targetPlayerId`
  - 首轮红灯：writes 只有 `playerControl:0`，没有 `cardInstance:deck-a/deck-b/deck-c`。
- [x] 已做最小修复
  - `reactionResources.ts` 的 `addGenericResourcesFromValue(...)` 把数组键白名单从 `cardUids` 扩到 `cardUids|topUids|bottomUids`。
  - 不扩大到其它 plural key、discard 选择、多玩家数组或 generic string array。
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts --testNamePattern "deck reorder 交互应从 topUids/bottomUids 推导多张 cardInstance footprint，而不是只记 targetPlayerId"` -> 真红灯后 `1 file passed, 1 passed`
  - 整文件 `reactionQueueOrdering.test.ts` -> `1 file passed, 48 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这格只闭合 reorder prompt 上 `topUids/bottomUids` 的 multi-card footprint。
  - 不外推所有 deck reorder prompt、所有 UID 数组键、所有 queued interaction family 或整条 reveal/peek/order 主线已完成；下一步继续离开当前已闭合的 interaction sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers` 查下一格真红灯。

## Addendum（2026-05-25 01:54 +08）：`effect-contract queued interaction` 的 `continuationContext.movedUids -> multi-minion footprint` 收口

- [x] 命中一条新的 queued interaction 真红灯
  - `pirate_full_sail_choose_minion` 会把已移动过的随从 UID 列表写进 `continuationContext.movedUids`，用于后续 prompt 排除这些己方随从。
  - 旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 已能把 `cardUids/topUids/bottomUids` 映射成多张 `cardInstance`，但不会把 `movedUids[]` 映射成 `minion` 资源。
  - 因此 `Full Sail` 这类 continuation prompt 的 footprint 只会记当前选中的 `minionUid` 与其 `baseIndex`，不会把前面已经移动过的同批随从写进冲突资源集合。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：`continuationContext.movedUids 数组应映射到多张 minion footprint，而不是只记当前选中的随从`
  - 首轮红灯：writes 只有 `minion:moved-candidate` 与 `base:1`，没有 `minion:moved-a/moved-b`。
- [x] 已做最小修复
  - `reactionResources.ts` 的 `addGenericResourcesFromValue(...)` 只为 `movedUids` 增加 `minion` 数组映射。
  - 不扩大到 generic `*Uids`、`allowedMinionUids`、`removedActionUids` 或其它 continuation key。
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts --testNamePattern "continuationContext\\.movedUids 数组应映射到多张 minion footprint，而不是只记当前选中的随从"` -> 首轮 failed，修复后 `1 file passed, 1 passed`
  - 整文件 `reactionQueueOrdering.test.ts`（`--max-old-space-size=8192`）-> `1 file passed, 49 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
  - `git diff --check -- src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` -> 通过（仅 LF/CRLF warning）
- [x] 当前边界已写死
  - 这格只闭合 `continuationContext.movedUids` 的 multi-minion footprint。
  - 不外推所有 minion UID 数组键、所有 move continuation prompt、所有 queued interaction family 或整条 Pirates move 链已完成；下一步继续离开当前已闭合的 interaction sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers` 查下一格真红灯。

## Addendum（2026-05-25 02:12 +08）：`effect-contract queued interaction` 的 `runtimePrompt.continuation.context.removedActionUids -> multi-card footprint` 收口

- [x] 命中一条新的 queued interaction 真红灯
  - `miskatonic_those_meddling_kids_select` 会把已处理过的行动 UID 列表写进 `runtimePrompt.continuation.context.removedActionUids`，用于后续 prompt 排除这些已处理行动。
  - 旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 已能把 `cardUids/topUids/bottomUids` 映射成多张 `cardInstance`，但不会把 `removedActionUids[]` 映射成 `cardInstance` 资源。
  - 因此 `Those Meddling Kids` 这类 runtime continuation prompt 的 footprint 只会记当前选中的 `cardUid` 与 `baseIndex`，不会把前面已经处理过的同批行动写进冲突资源集合。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：`runtimePrompt.continuation.context.removedActionUids 数组应映射到多张 cardInstance footprint，而不是只记当前选中的行动卡`
  - 首轮红灯：writes 只有 `cardInstance:current-action` 与 `base:0`，没有 `cardInstance:removed-action-a/removed-action-b`。
- [x] 已做最小修复
  - `reactionResources.ts` 的 `addGenericResourcesFromValue(...)` 只把数组白名单从 `cardUids|topUids|bottomUids` 扩到 `cardUids|topUids|bottomUids|removedActionUids`。
  - 不扩大到 generic `*Uids`、`allowedActionUids`、`inspectedUids` 或其它 continuation key。
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts --testNamePattern "runtimePrompt\\.continuation\\.context\\.removedActionUids 数组应映射到多张 cardInstance footprint，而不是只记当前选中的行动卡"` -> `1 file passed, 1 passed`
  - 整文件 `reactionQueueOrdering.test.ts`（`--max-old-space-size=8192`）-> `1 file passed, 50 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
  - `git diff --check -- src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` -> 通过（仅 LF/CRLF warning）
- [x] 当前边界已写死
  - 这格只闭合 `runtimePrompt.continuation.context.removedActionUids` 的 multi-card footprint。
  - 不外推所有 action UID 数组键、所有 runtime continuation prompt、所有 queued interaction family 或整条 Miskatonic action-removal 链已完成；下一步继续离开当前已闭合的 interaction sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers` 查下一格真红灯。

## Addendum（2026-05-25 02:21 +08）：`effect-contract queued interaction` 的 `runtimePrompt.continuation.context.selectedMinionUids -> multi-minion footprint` 收口

- [x] 命中一条新的 queued interaction 真红灯
  - `ninja_disguise` 真链会把已选中、后续要回手的基地随从 UID 列表写进 `runtimePrompt.continuation.context.selectedMinionUids`。
  - 旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 的 `addGenericResourcesFromValue(...)` 已能把 `movedUids` 映射成多张 `minion`，但不会把 `selectedMinionUids[]` 映射成 `minion` 资源。
  - 因此 `Ninja Disguise` 这类 runtime continuation prompt 的 footprint 只会记当前手牌候选 `cardUid` 与 `baseIndex`，不会把前面已选中、后续会被回手的同批随从写进冲突资源集合。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：`runtimePrompt.continuation.context.selectedMinionUids 数组应映射到多张 minion footprint，而不是只记当前手牌随从`
  - 首轮红灯：writes 只有 `cardInstance:hand-minion-a` 与 `base:0`，没有 `minion:return-minion-a/return-minion-b`。
- [x] 已做最小修复
  - `reactionResources.ts` 的 minion-array 白名单只从 `movedUids` 扩到 `movedUids|selectedMinionUids`。
  - 不扩大到 generic `selected*Uids`、`selectedTargetUids`、`playedHandUids` 或其它 continuation key。
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts --testNamePattern "runtimePrompt\\.continuation\\.context\\.selectedMinionUids 数组应映射到多张 minion footprint，而不是只记当前手牌随从"` -> 首轮 failed，修复后 `1 file passed, 1 passed`
  - 整文件 `reactionQueueOrdering.test.ts`（`--max-old-space-size=8192`）-> `1 file passed, 51 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
  - `git diff --check -- src/games/smashup/domain/reactionResources.ts src/games/smashup/__tests__/reactionQueueOrdering.test.ts` -> 通过（仅 LF/CRLF warning）
- [x] 当前边界已写死
  - 这格只闭合 `runtimePrompt.continuation.context.selectedMinionUids` 的 multi-minion footprint。
  - 不外推所有 selected UID 数组键、所有 runtime continuation prompt、所有 queued interaction family 或整条 Ninja Disguise play/return 链已完成；下一步必须离开当前已连续收口的 interaction sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers` 或其它 still-open shared seam 查新的真红灯。

## Addendum（2026-05-25 05:14 +08）：`reactionResources` 事件侧的 `deckUids/newDeckUids -> multi-card footprint` 收口

- [x] 命中两条新的事件侧 shared 真红灯
  - `DECK_REORDERED.payload.deckUids[]`
  - `HAND_SHUFFLED_INTO_DECK.payload.newDeckUids[]`
  - 旧 [`reactionResources.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/reactionResources.ts>) 对这两类事件只会记 `playerHand/playerDeck/playerDiscard`，不会把真实参与 reorder/shuffle 的整批 `cardInstance` 写进 footprint。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：
    - `DECK_REORDERED 事件应从 deckUids 映射到多张 cardInstance footprint，而不是只记玩家牌区`
    - `HAND_SHUFFLED_INTO_DECK 事件应从 newDeckUids 映射到多张 cardInstance footprint，而不是只记玩家牌区`
  - 首轮红灯：writes 都只有 `playerHand:0/playerDeck:0/playerDiscard:0`，没有任何 `cardInstance:*`。
- [x] 已做最小修复
  - `reactionResources.ts` 的 card-array 白名单只从 `cardUids|topUids|bottomUids|removedActionUids|pickedToHandUids|playedHandUids|extraDeckUidsForShuffle` 扩到 `cardUids|topUids|bottomUids|deckUids|newDeckUids|removedActionUids|pickedToHandUids|playedHandUids|extraDeckUidsForShuffle`。
  - 不扩大到 generic `*Uids`、`reshuffledDeckUids`、`inspectedUids`、`candidateUids` 或混合语义 key。
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts -t "DECK_REORDERED 事件应从 deckUids 映射到多张 cardInstance footprint，而不是只记玩家牌区|HAND_SHUFFLED_INTO_DECK 事件应从 newDeckUids 映射到多张 cardInstance footprint，而不是只记玩家牌区"` -> 首轮 failed，修复后 `1 file passed, 2 passed`
  - 整文件 `reactionQueueOrdering.test.ts` -> `1 file passed, 58 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这轮只闭合事件侧 `deckUids/newDeckUids` 的 multi-card footprint。
  - 不外推所有 event payload 数组键、所有 shuffle/reorder producer、所有 owner-provenance 语义，或整个 `reactionResources` family 已完成；下一步继续找下一条真实生产 key，若找不到纯 UID 真红灯，再切回 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor`。

## Addendum（2026-05-25 05:25 +08）：`reactionResources` 交互 root/continuation 的 `inspectedUids + candidateUids` 收口

- [x] 命中两条新的交互侧 shared 真红灯
  - `interaction.data.inspectedUids[]`
  - `continuationContext.remainingPlayers[].candidateUids[]`
  - 前者对应 `cyborg_apes_monkey_see_monkey_do_choose` 这类“option 只列 action，但 inspected batch 更大”的 prompt；后者对应 `base_temple_of_goju_tiebreak` 这类“当前 option 之外，还在 continuation 中藏着后续候选随从快照”的链式 prompt。
- [x] 已补 focused red-green gate
  - [`reactionQueueOrdering.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueOrdering.test.ts>)：
    - `interaction.data.inspectedUids 应映射到整批 inspected cardInstance footprint，而不是只记 option 中出现的行动牌`
    - `continuationContext.remainingPlayers[].candidateUids 应映射到后续候选随从的 minion footprint，而不是误记成 cardInstance`
  - 首轮红灯：
    - `inspectedUids` 只记到 option 中的 `inspect-action-a/b`，漏 `inspect-minion-c`
    - `candidateUids` 把 `remaining-minion-a/b` 记成了 `cardInstance:*`，不是 `minion:*`
- [x] 已做最小修复
  - `reactionResources.ts` 的 card-array 白名单增加 `inspectedUids`
  - `deriveFootprintFromInteraction(...)` 只补 inspection root 数据 `inspectedUids/inspectedCards`
  - `addGenericResourcesFromValue(...)` 只为 `candidateUids` 这种随从对象数组增加 `minion` 映射，并阻止它再被 generic `uid` 分支误记成 `cardInstance`
  - 不扩大到 generic `interaction.data.*`、`allowed*`、`remainingPlayers` 其它字段或 generic `uid` 语义重写
- [x] 已做最小验证
  - focused `reactionQueueOrdering.test.ts -t "interaction.data.inspectedUids 应映射到整批 inspected cardInstance footprint，而不是只记 option 中出现的行动牌"` -> 首轮 failed，修复后 `1 file passed, 1 passed`
  - focused `reactionQueueOrdering.test.ts -t "continuationContext.remainingPlayers\\[\\]\\.candidateUids 应映射到后续候选随从的 minion footprint，而不是误记成 cardInstance"` -> 首轮 failed，修复后 `1 file passed, 1 passed`
  - 整文件 `reactionQueueOrdering.test.ts` -> `1 file passed, 60 passed`
  - ESLint `reactionResources.ts + reactionQueueOrdering.test.ts` -> `0 errors`
- [x] 当前边界已写死
  - 这轮只闭合 `inspectedUids` 的 inspected batch footprint，以及 `candidateUids` 的 hidden follow-up minion footprint。
  - 不外推所有 interaction.data root 字段、所有 continuation 对象数组、所有 hidden snapshot 结构，或整个 `reactionResources` family 已完成；下一步继续找下一条真实生产 key，若没有，再切回 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor`。

## Latest Note

- `onDestroyAbilities.test.ts` 里 `trickster_gremlin` 的“`processDestroyTriggers` 仍吃打出前手牌”旧注释/旧断言已被 focused 复跑坐实为测试漂移，不是新的 runtime/shared 红灯。
- 当前已把这格改写成 dedicated green evidence：
  - 当前玩家打出唯一手牌 `c1` 消灭 `trickster_gremlin` 后，不再产生 `CARDS_DISCARDED(playerId='0')`
  - 当前玩家若原本还有 `c2`，则只会弃掉 `c2`，不会把已打出的 `c1` 回流成弃牌候选
- 下一步继续离开这条已转绿的 `onDestroy` 证据，回到 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor / onMinionDiscardedFromBase` 的 still-open shared seam；不得把这条 dedicated green evidence 误报成整个 destroy family 已收口。
- `bury.ts` 这轮又命中一条新的 handbuilt `ONGOING_ATTACHED` provenance 真红灯：
  - 翻开 `trueOwnerId != playerId` 的 borrowed buried ongoing 时，旧实现只保留 `ownerId`，漏掉 `sourcePlayerId`
  - focused `buryEngine.test.ts` 已先红后绿，锁住 `Brownie` 真实链会继续看到 `eventPlayerId='0'`
- 下一步继续离开这条已闭合的 bury sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor / onMinionDiscardedFromBase` 的 still-open shared seam；不得把这条单格修复误报成所有 bury/ongoing provenance 已收口。
- `wizards.ts` 这轮又命中一条新的 handbuilt `ONGOING_ATTACHED` provenance 真红灯：
  - `resolveWizardExternalActionPlay()` 在 `wizard_mass_enchantment` 借打他人拥有的 ongoing action 时，`ACTION_PLAYED.ownerId` 已正确，但两个 `ONGOING_ATTACHED` 出口都漏 `sourcePlayerId`
  - `query6Abilities.test.ts -t "wizard_mass_enchantment"` 已先红后绿；整文件 `query6Abilities.test.ts` 与 shared `reactionQueueEventPlayerContext` 的 Brownie borrowed ONGOING_ATTACHED gate 都已复跑通过
- 下一步继续离开这条已闭合的 Wizards sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor / onMinionDiscardedFromBase` 的 still-open shared seam；不得把这条单格修复误报成所有 Wizards/external action provenance 已收口。
- `tornados.ts` 这轮又命中一条新的 handbuilt `ONGOING_ATTACHED` provenance 真红灯：
  - `rippedOffTargetPromptProgram.onResolve()` 在 `tornados_ripped_off` 搬运 borrowed 附着行动时，两个 re-attach 出口都只保留 `ownerId`，不会补 `sourcePlayerId`
  - `shayuFactionAbilities.test.ts -t "龙卷风：扯走把 borrowed 附着行动移到 Brownie 身上时，应保留 sourcePlayerId 并把 queued 事件玩家留给真正行动玩家"` 已先红后绿；邻近 Tornados sibling 与 shared `reactionQueueEventPlayerContext` 的 Brownie borrowed ONGOING_ATTACHED gate 都已复跑通过
- 下一步继续离开这条已闭合的 Tornados sibling，回到 `ACTION_PLAYED side branch / processAffectTriggers / TriggerContext -> TriggerInstance -> queued executor / onMinionDiscardedFromBase` 的 still-open shared seam；不得把这条单格修复误报成所有 Tornados/attached-action transfer provenance 已收口。
- `ongoing_modifiers.ts` 这轮又命中一条新的 borrowed modifier controller 真红灯：
  - `registerPowerModifier('steampunk_steam_man', ...)` 的 base ongoing 与 attached action 分支仍只按 `ownerId` 判“本基地是否有你的行动卡”
  - `ongoingModifiers.test.ts --testNamePattern "steampunk_steam_man borrowed action controller seam|borrowed 基地 ongoing|borrowed 附着行动|steampunk_steam_man"` 已先红后绿；当前两条 focused gate 都已转成 `1 file passed, 2 passed`
- 下一步继续留在 `ongoing_modifiers.ts` 这条活跃 family，筛下一个 live state 已有 `metadata.sourceControllerId`、但 modifier/runtime consumer 仍只读 `ownerId` 的真红灯；优先检查同文件里仍显式写 `action.ownerId === ctx.minion.controller` 的 sibling，而不是回扫已收口的 Bear Cavalry / Secret Agent / overlay。
- `ongoing_modifiers.ts` 这轮继续命中一条新的 borrowed modifier controller 真红灯：
  - `fairies_daisy_chain` 仍只按 `ownerId` 判“你控制则 +2，否则 -2”
  - `ongoingModifiers.test.ts --testNamePattern "fairies_daisy_chain borrowed action controller seam|borrowed 雏菊花环|steampunk_steam_man borrowed action controller seam"` 已先红后绿；当前四条 focused gate 都已转成 `1 file passed, 4 passed`
- 下一步离开 `ongoing_modifiers.ts` 里这两条已转绿的单表达式 seam，切回更广的 modifier/runtime consumer 搜索面，继续找下一个 live state 已有 `metadata.sourceControllerId`、但 consumer 仍只读 `ownerId` 的最窄真红灯；不要回扫已收口的 `steam_man / daisy_chain`。
- protection consumer 搜索面这轮又连命中两条 borrowed controller 真红灯：
  - `dinoWildlifePreserveChecker()` 仍只按 `ownerId` 判野生保护区保护归属
  - `killerPlantDeepRootsChecker()` 仍只按 `ownerId` 判深根保护归属
  - `wildlifePreserveProtection.test.ts --testNamePattern "borrowed wildlife_preserve|wildlife_preserve: action 保护检查"` 与 `expansionOngoing.test.ts --testNamePattern "killer_plant_deep_roots: 深根保护|borrowed deep_roots"` 都已先红后绿
- 下一步优先打同型候选 `giant_ant_the_show_must_go_on`；它在源码里仍是同级 `ownerId === ctx.targetMinion.controller` 判断，最像下一条单格红灯，不要回扫已收口的 `wildlife_preserve / deep_roots / steam_man / daisy_chain`。
- protection consumer 搜索面这轮继续命中一条 borrowed controller 真红灯：
  - `registerGiantAntProtections()` 里的 `giant_ant_the_show_must_go_on(_pod)` 保护 checker 仍只按 `ownerId` 判“谁的有指示物随从受保护”
  - `newFactionAbilities.test.ts --testNamePattern "borrowed giant_ant_the_show_must_go_on"` 已先红后绿；当前两条 focused gate 都已转成 `1 file passed, 2 passed`
- 下一步离开这三条已收口的 protection checker seam，单独评估同文件更宽 pattern 暴露的 `drone_skip / 防止失败` 两条巨蚁旧红灯是不是本轮主线下一格；不要把它们和刚收口的 borrowed controller seam 混成一条问题。

## 2026-05-25 10:06 +08

- 已把 `giant_ant_drone / frankenstein_igor` 这簇旧红灯拆成两条真实实现缝并收口：
  - `giant_ant_drone_prevent_destroy` 的并发 pendingSave prompt 旧实现把 `interactionId` 固定成 `giant_ant_drone_prevent_destroy_${now}`；同帧两个被保护目标会生成同 id 交互，第一下响应后 `systems.ts` 误把“下一条同 id 当前交互”也一起 `resolveInteraction()` 弹掉，表现为第 2 个防止交互直接消失。现已把 id 收紧成 `giant_ant_drone_prevent_destroy_${targetMinionUid}_${fromBaseIndex}_${now}`，只修这条 prompt 的同帧唯一性，不外扩到其它交互 runtime。
  - `frankenstein_igor` 旧实现同时保留 `onDestroy` 与 `onMinionDiscardedFromBase`，而 `processDestroyTriggers()` 在 destroy 落地进入 owner discard 时会同时走两条链，导致 `Igor + Big Gulp`、`reason=drone_skip` 等场景双加同一个 `POWER_COUNTER_ADDED(reason='frankenstein_igor')`。现已把 `frankenstein_igor` 的 discard trigger 收紧为：若 `sourceEventId` 形如 `minion-discarded-from-base:*`，说明它来自 destroy->discard 真链，此时交给 `onDestroy` 处理，不再重复结算 discard 分支；计分弃牌/直接弃牌入口仍保留原 `onMinionDiscardedFromBase` 语义。
- 已验证：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "reason=drone_skip|防止失败（指示物耗尽）"` -> `1 file passed, 2 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/igor-big-gulp-two-igors.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 2 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/igor-two-igors-one-destroyed.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 1 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/igor-ondestroy-idempotency.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1` -> `1 file passed, 4 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/architecture-duplicate-processing.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Igor|onDestroy"` -> `1 file passed, 2 passed`
- 当前边界已写死：
  - 这格只闭合 `giant_ant_drone` 的同帧并发交互 id 冲突，以及 `frankenstein_igor` 的 destroy->discard 双触发 seam。
  - 不外推所有 Giant Ants、所有 pendingSave prompt、所有 `onDestroy + onMinionDiscardedFromBase` 自身牌，或整个 destroy/discard family 已完成。
  - 下一步优先继续筛同类“同一张牌同时声明 self-source `onDestroy` 与 `onMinionDiscardedFromBase`，且 destroy 落地会重复结算”的 sibling，而不是回扫刚转绿的 `Drone / Igor / Big Gulp`。

## 2026-05-25 10:31 +08

- 已沿同一条 destroy/discard sibling 主线继续收掉 `Samurai` 家族 4 条真实红灯，不是停在 `Drone / Igor` 原地打转：
  - `samurai_samurai_chan`
  - `samurai_bushi`
  - `samurai_shogun`
  - `samurai_honor_the_fallen`
- 红灯事实：
  - 先补 `newFactionAbilities.test.ts` focused gates，直接坐实上述 4 条牌在 `processDestroyTriggers()` 的 destroy -> owner discard 真链里会同时跑 `onMinionDestroyed` 与 `onMinionDiscardedFromBase`
  - 首轮分别表现为：
    - `Samurai Chan` 双抽
    - `Bushi` 双给 VP
    - `Shogun` 因同一名己方离场随从双加指示物
    - `Honor the Fallen` 因同一名己方离场随从双抽
- 已做最小修复：
  - 在 [`src/games/smashup/abilities/samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 提炼 `isDestroyPipelineDiscardTrigger(ctx)`
  - 只对 `samuraiChanTrigger`、`samuraiBushiTrigger`、`samuraiShogunTrigger`、`samuraiHonorTheFallenTrigger` 加守卫：
    - 若当前是 `timing === 'onMinionDiscardedFromBase'`
    - 且 `sourceEventId` 形如 `minion-discarded-from-base:*`
    - 则视为 destroy 落地的 discard 分支，直接跳过，交给 `onMinionDestroyed` 结算
  - 不扩到 `samurai_final_haiku`、`samurai_way_of_the_warrior`、`base_sakura_garden` 或其它非同型 trigger
- 已验证：
  - `newFactionAbilities.test.ts --testNamePattern "samurai_samurai_chan 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|samurai_bushi 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双给 VP|samurai_shogun 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双加指示物|samurai_honor_the_fallen 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双抽"` -> `1 file passed, 4 passed`
  - `baseScoring.test.ts --testNamePattern "Samurai Chan|Bushi"` -> `1 file passed, 3 passed`
  - `newBaseAbilities.test.ts --testNamePattern "samurai_samurai_chan|samurai_bushi|Sakura Garden"` -> `1 file passed, 2 passed`
  - `reactionQueueEventPlayerContext.test.ts --testNamePattern "Samurai Chan|Bushi"` -> `1 file passed, 5 passed`
  - `reactionQueueControllerRuntimeContext.test.ts --testNamePattern "Bushi"` -> `1 file passed, 1 passed`
- 当前边界已写死：
  - 这格只闭合 `Samurai Chan / Bushi / Shogun / Honor the Fallen` 在 destroy -> discard 真链上的重复结算
  - 不外推整个 Samurai family、所有 dual-timing trigger、`Final Haiku / Way of the Warrior`、所有 attached action 离场链，或整个 destroy/discard family 已完成
  - 下一步优先审 `samurai_final_haiku` 与 `samurai_way_of_the_warrior` 现有“会触发”测试是否也掩盖了 destroy->discard 双结算；若命中红灯，再继续按同型 focused gate 收口

## 2026-05-25 10:52 +08

- 已继续沿同一条 destroy/discard sibling 主线把 `Samurai` 这格从“普通版 4 张”扩收到了 `Way of the Warrior / Final Haiku`，并补齐了 4 条 POD focused gate，不是停在上一轮原地打转。
- 命中对象：
  - `samurai_way_of_the_warrior`
  - `samurai_way_of_the_warrior_pod`
  - `samurai_final_haiku`
  - `samurai_final_haiku_pod`
  - 以及 `samurai_samurai_chan_pod / samurai_bushi_pod / samurai_shogun_pod / samurai_honor_the_fallen_pod` 的 focused destroy 真链锁定
- 根因判断：
  - `Way of the Warrior` / `Final Haiku` 起初看上去像比前一格更深一层的新 bug，但临时 runtime 采样已证实并不是第三层问题。
  - 真正重复来源仍是同一条 seam：同一张牌同时响应
    - `onMinionDestroyed`
    - `onMinionDiscardedFromBase`
  - destroy 落地进入 owner discard 时，两条分支都吃到同一事件语义，旧实现会双结算。
- 已做修复：
  - 继续复用 [`src/games/smashup/abilities/samurai.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/samurai.ts>) 的 `isDestroyPipelineDiscardTrigger(ctx)`。
  - 在前一格 `Chan/Bushi/Shogun/Honor` 基础上，再把同一守卫扩到：
    - `samuraiFinalHaikuTrigger`
    - `samuraiWayOfTheWarriorTrigger`
  - 语义不变：`timing==='onMinionDiscardedFromBase'` 且 `sourceEventId` 形如 `minion-discarded-from-base:*` 时，discard 分支直接跳过，交给 destroy 分支结算。
  - 这轮最终 runtime 改动只留在 `samurai.ts`；中途为了定位加过的 `console.log` 已删除，没有把 debug 留在工作树里。
- 已补 focused/neighbor 验证：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_way_of_the_warrior 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|samurai_final_haiku 在 destroy -> processDestroyTriggers 真链里不应给同一批己方随从重复 \\+2 力量"` -> `1 file passed, 2 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_way_of_the_warrior_pod 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|samurai_final_haiku_pod 在 destroy -> processDestroyTriggers 真链里不应给同一批己方随从重复 \\+2 力量"` -> `1 file passed, 2 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_samurai_chan_pod 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|samurai_bushi_pod 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双给 VP|samurai_shogun_pod 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双加指示物|samurai_honor_the_fallen_pod 在 destroy -> processDestroyTriggers 真链里不应因同一名己方随从双抽"` -> `1 file passed, 4 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_way_of_the_warrior|samurai_final_haiku"` -> `1 file passed, 14 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "samurai_final_haiku|samurai_way_of_the_warrior|Final Haiku|Way of the Warrior"` -> `1 file passed, 4 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Final Haiku|Way of the Warrior"` -> `1 file passed, 4 passed`
- 当前状态：
  - 现在已把 `Samurai` 这条 destroy/discard duplicate seam 从普通版 4 张，扩收到了 `Way of the Warrior / Final Haiku` 及其 POD focused gate。
  - 这是真实进展，不是重复试同一件事；但它仍然只覆盖 `Samurai` 这一个 family 的 destroy/discard duplicate seam。
  - 下一步必须离开 `Samurai`，回到“同时声明 self-source `onDestroy + onMinionDiscardedFromBase` 但还没 focused destroy 真链 gate”的 sibling 搜索面，优先看 `giant_ant_worker_pod / princesses_sleeping_beauty / giant_ants_death_on_six_legs / vikings_viking_funeral / world_champs_bewitched / world_champs_samurai_chan / trickster_gremlin_pod`。

## 2026-05-25 11:40 +08

- 已继续沿同一条 destroy/discard sibling 主线离开 `World Champs`，把新的 live 红灯 `trickster_gremlin_pod` 收绿，不是停在上一格原地复读。
- 命中对象：
  - `trickster_gremlin_pod`
- 红灯事实：
  - focused gate `trickster_gremlin_pod 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽` 首轮直接失败：`drawEvents` 预期 `1`，实际 `2`。
  - 根因和前面的 `Samurai Chan / World Champs Samurai Chan` 同型：`trickster_gremlin_pod` 同时注册 `onMinionDestroyed + onMinionDiscardedFromBase`，destroy 真链里派生出来的 discard 第二拍又给同一宿主补了一次 draw。
- 已做最小修复：
  - 在 [`src/games/smashup/abilities/tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 提炼 `isDestroyPipelineDiscardTrigger(ctx)`。
  - 只对 `trickster_gremlin_pod` 的 `onMinionDiscardedFromBase` 分支加守卫：若 `sourceEventId` 形如 `minion-discarded-from-base:*`，则视为 destroy 落地派生的 discard 分支，直接跳过，保留 `onMinionDestroyed` 与计分清场 discard 各自应有的一次触发。
  - 不扩到其它 Tricksters sibling，也不动 shared reducer / queue。
- 已验证：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/newFactionAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "trickster_gremlin_pod 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽|princesses_sleeping_beauty 被消灭时会洗回牌库而不是进弃牌堆"` -> `1 file passed, 2 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/baseScoring.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "Gremlin POD"` -> `1 file passed, 1 passed`
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/onDestroyAbilities.test.ts --configLoader native --pool forks --no-file-parallelism --maxWorkers 1 --testNamePattern "trickster_gremlin_pod"` -> `1 file passed, 1 passed`
- 当前边界已写死：
  - 这格只闭合 `trickster_gremlin_pod` 的 destroy/discard duplicate draw seam。
  - 不外推整个 Tricksters、所有 dual-timing self-trigger、所有 `onMinionDestroyed + onMinionDiscardedFromBase` sibling，或整个 destroy/discard family 已完成。
  - 下一步优先给 `princesses_sleeping_beauty` 补 dedicated destroy 真链 duplicate gate，确认它现在是已有绿证据即可，还是还藏着同型红灯。

## 2026-05-25 11:52 +08

- 已继续把剩余 inventory 候选里的 `princesses_sleeping_beauty / base_sakura_garden` 都补成 dedicated destroy 真链 gate，并确认两者当前都是 green evidence，不是新的 live 红灯。
- 命中对象：
  - `princesses_sleeping_beauty`
  - `base_sakura_garden`
- 结论：
  - `Sleeping Beauty` 的 destroy 真链 focused gate 首轮不是“重复 2 次”，而是我先抓错了事件锚点；临时事件采样已证实真实只吐出 1 条 `su:card_to_deck_bottom`。
  - `Sakura Garden` 的 destroy 真链 focused gate 直接转绿，说明它当前靠 `turnDestroyedMinions` 去重，能挡住 destroy 后派生 discard 第二拍的重复抽牌。
  - 因此这两格都只补测试，不改 runtime 逻辑。
- 已验证：
  - `newFactionAbilities.test.ts --testNamePattern "princesses_sleeping_beauty 被消灭时会洗回牌库而不是进弃牌堆|princesses_sleeping_beauty 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双重洗回牌库|trickster_gremlin_pod 在 destroy -> processDestroyTriggers 真链进入弃牌堆时不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双抽"` -> `1 file passed, 3 passed`
  - `baseScoring.test.ts --testNamePattern "Sleeping Beauty"` -> `1 file passed, 1 passed`
  - `reactionQueueEventPlayerContext.test.ts --testNamePattern "Sleeping Beauty"` -> `1 file passed, 2 passed`
  - `newBaseAbilities.test.ts --testNamePattern "base_sakura_garden 在 destroy -> processDestroyTriggers 真链里不应同时走 onMinionDestroyed 与 onMinionDiscardedFromBase 双重抽牌|base_sakura_garden 在本回合第一次有你的随从被消灭时让你抓一张牌|base_sakura_garden 与 samurai_honor_the_fallen 同时触发时两者都会结算抓牌"` -> `1 file passed, 3 passed`
- 当前边界已写死：
  - 这格只把 `Sleeping Beauty / Sakura Garden` 从“未显式锁 destroy 真链”降成 dedicated green evidence，不外推整个 Princesses、整个 Samurai bases、或整个 destroy/discard family 已完成。
  - 当前这轮 `onMinionDestroyed + onMinionDiscardedFromBase` sibling inventory 已扫空；下一步应离开这条 family，转去找新的 shared seam 或新的 open family，而不是继续围着这份 dual-timing 清单复扫。

## 2026-05-25 12:59 +08

- 已继续沿 borrowed controller seam 收掉两格新的真进展，不是停在状态汇报。
- 命中对象：
  - `cowboys_stagecoach`
  - `vampire_summon_wolves`
- `cowboys_stagecoach`：
  - focused gate `cowboys_stagecoach 应按控制者而不是真实 owner 搬运基地上的 borrowed 持续行动` 首轮直接红灯：`PLAY_ACTION(stagecoach-1)` 后没有进入 `cowboys_stagecoach_source` prompt。
  - 最小根因在 [`src/games/smashup/abilities/cowboys.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/cowboys.ts>)：`collectStagecoachCardsOnBase()` 仍用 `action.ownerId === playerId` 过滤 base ongoing，把 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed ongoing 直接排除。
  - 已做最小修复：只把 Stagecoach 的 base ongoing 控制者判定改成 `metadata.sourceControllerId ?? ownerId`，不扩到移动落点或其它 Cowboys 分支。
  - 已验证：
    - `newFactionAbilities.test.ts --testNamePattern "cowboys_stagecoach 可把同一基地上至多两个己方随从移动到另一个基地|cowboys_stagecoach 也可搬运基地上的持续行动和埋葬牌|cowboys_stagecoach 应按控制者而不是真实 owner 搬运基地上的 borrowed 持续行动"` -> `1 file passed, 3 passed`
    - `newFactionAbilities.test.ts --testNamePattern "cowboys_stagecoach"` -> `1 file passed, 4 passed`
- `borrowed vampire_summon_wolves`：
  - 在 [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Vampire Summon Wolves 应按控制者而不是真实 owner 在控制者回合开始增加 ongoing card counter`。
  - 首轮直接红灯坐实：queued trigger owner 已正确归给控制者 P0，但 `maybeResolveReactionQueue(...)` 只吐 `TRIGGER_CONSUMED`，没有 `ONGOING_CARD_COUNTER_CHANGED`。
  - 最小根因在 [`src/games/smashup/abilities/vampires.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/vampires.ts>)：`vampire_summon_wolves` callback 本体仍只认 `oa.ownerId === playerId`，把执行期控制者又洗回真实 owner。
  - 已做最小修复：只把该 callback 的 ongoing 控制者判定改成 `metadata.sourceControllerId ?? ownerId`，不扩到其它 Vampires 分支。
  - 已验证：
    - `reactionQueueEventPlayerContext.test.ts -t "Vampire Summon Wolves|borrowed Vampire Summon Wolves"` -> `1 file passed, 2 passed`
    - `vampiresPod.test.ts -t "Summon Wolves|召唤狼群|Dinner Date"` -> `1 file passed, 2 passed`
- 当前边界：
  - 这两格只闭合 `Stagecoach` 的 borrowed ongoing source collection 与 `borrowed Vampire Summon Wolves` 的 onTurnStart callback controller seam。
  - 不外推整个 Cowboys、整个 Vampires、所有 owner-only onTurnStart callback、或整条长期任务已完成。
  - 下一步优先继续筛同类 still-open consumer：仍显式写 `ownerId === playerId`、但 live state 已持久化 `metadata.sourceControllerId` 的 ongoing counter / start-turn / score participation sibling，避免回扫已转绿的 `Stagecoach / Summon Wolves / Marking Territory / zero-power-scoring`。

## 2026-05-25 13:08 +08

- 已继续沿同一条 borrowed controller / owner-only consumer 主线再收掉两格新的真红灯，不是停在上一轮的 `Stagecoach / Summon Wolves`。
- 命中对象：
  - `werewolf_marking_territory`
  - `domain/ongoingModifiers.ts:getOngoingCardPowerContribution()`
- `borrowed werewolf_marking_territory`：
  - 在 [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Werewolf Marking Territory 应按控制者而不是真实 owner 在控制者回合开始写入 breakpoint modifier`。
  - 首轮直接红灯坐实：queued trigger metadata 已正确是 `sourceControllerId='0' / ownerPlayerId='0'`，但 executor 只吐 `TRIGGER_CONSUMED`，没有 `BREAKPOINT_MODIFIED`。
  - 最小根因在 [`src/games/smashup/abilities/werewolves.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/werewolves.ts>)：`werewolf_marking_territory` callback 本体仍用 `a.ownerId === playerId` 判断“你在这里有制造恐慌”。
  - 已做最小修复：只把该 callback 的 ongoing 控制者判定改成 `metadata.sourceControllerId ?? ownerId`，不扩到其它 Werewolves 分支。
  - 已验证：`reactionQueueEventPlayerContext.test.ts -t "Werewolf Marking Territory|borrowed Werewolf Marking Territory"` -> `1 file passed, 2 passed`
- `borrowed ongoing counter scoring`：
  - 在 [`src/games/smashup/__tests__/zero-power-scoring.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zero-power-scoring.test.ts>) 新增 focused gate `只有 borrowed ongoing 卡力量贡献（无随从）的控制者也应该参与计分`。
  - 首轮直接红灯坐实：P0 控制一张 `ownerId='1', sourceControllerId='0', powerCounters=5` 的 borrowed ongoing 时，最终 `finalCore.players['0'].vp` 仍是 `0`，没有按 5 点力量拿到第 2 名 VP。
  - 最小根因在 [`src/games/smashup/domain/ongoingModifiers.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingModifiers.ts>)：`getOngoingCardPowerContribution()` 仍只按 `oa.ownerId !== playerId` 过滤。
  - 已做最小修复：只把该 shared consumer 的 ongoing 控制者判定改成 `metadata.sourceControllerId ?? ownerId`，不扩到 minion power、titan contribution 或其它 scoring helper。
  - 已验证：`zero-power-scoring.test.ts` 整文件 -> `1 file passed, 4 passed`
- 当前边界：
  - 这两格只闭合 `borrowed Marking Territory` 的 onTurnStart callback controller seam 与 borrowed ongoing counter 对计分资格/排名的 shared consumer seam。
  - 不外推整个 Werewolves、所有 ongoing counter scorer、所有 onTurnStart callback、或整条长期任务已完成。
  - 下一步优先继续找同 family 里仍残留的 owner-only ongoing consumer，而不是回扫刚转绿的四格 `Stagecoach / Summon Wolves / Marking Territory / ongoing counter scoring`。

## 2026-05-25 13:12 +08

- 已继续沿 owner-only ongoing consumer 主线再收掉一条新的真红灯：`zombie_theyre_coming_to_get_you` 的 discard play provider。
- 命中对象：
  - `zombie_theyre_coming_to_get_you`
- 红灯证据：
  - 在 [`src/games/smashup/__tests__/zombieInteractionChain.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/zombieInteractionChain.test.ts>) 新增 focused gate `borrowed ongoing 附着基地时，控制者也应能通过 PLAY_MINION fromDiscard 打到该基地`。
  - 首轮直接坐实：`PLAY_MINION fromDiscard` 在命令校验阶段就被拒绝，错误为“该卡牌不能从弃牌堆打出到此基地”。
- 最小根因：
  - [`src/games/smashup/abilities/zombies.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/zombies.ts>) 的 `registerDiscardPlayProvider('zombie_theyre_coming_to_get_you').getPlayableCards(...)` 仍只按 `o.ownerId === playerId` 寻找“附着了此 ongoing 卡的基地”。
  - 对 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `它们为你而来` 来说，这会让控制者 P0 根本拿不到 allowedBases。
- 已做最小修复：
  - 只把该 discard play provider 的 ongoing 控制者判定改成 `metadata.sourceControllerId ?? ownerId`，不扩到出牌落点 owner 语义、其它 Zombies provider、或 shared command validator。
- 已验证：
  - `zombieInteractionChain.test.ts -t "theyre_coming|borrowed ongoing 附着基地时"` -> `1 file passed, 5 passed`
  - `zombieInteractionChain.test.ts -t "zombie_theyre_coming_to_get_you"` -> `1 file passed, 5 passed`
- 当前边界：
  - 这格只闭合 `borrowed zombie_theyre_coming_to_get_you` 的 discard play provider / allowedBases controller seam。
  - 不外推整个 Zombies、所有 fromDiscard provider、所有 borrowed ongoing consumer、或整条长期任务已完成。
  - 下一步优先继续扫仍显式写 `ownerId === playerId`、且 live state 已持久化 `metadata.sourceControllerId` 的 provider / handler / candidate builder sibling，不回扫刚转绿的 `Stagecoach / Summon Wolves / Marking Territory / ongoing counter scoring / They’re Coming to Get You`。

## 2026-05-25 13:20 +08

- 已继续沿同一条 borrowed ongoing controller 主线再收掉一条新的真红灯：`skeletons_gravestones` 的 afterScoring interaction handler。
- 红灯证据：
  - 在 [`src/games/smashup/__tests__/newFactionAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newFactionAbilities.test.ts>) 新增 focused gate `borrowed skeletons_gravestones 应按控制者而不是真实 owner 在计分后把自己埋到另一基地，并保留 true owner`。
  - 首轮直接坐实：`skeletons_gravestones_after_scoring` prompt 会正常弹给控制者 P0，但 resolve 后目标基地 `buriedCards` 里没有 `gravestones-1`。
- 最小根因：
  - [`src/games/smashup/abilities/skeletons.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/skeletons.ts>) 的 `handleSkeletonsGravestonesAfterScoring()` 仍只按 `action.ownerId === playerId` 回捞 source 卡，导致 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Gravestones` 在控制者响应时直接回空。
  - 同一 handler 还把 `buildBuryCardEvents(...).trueOwnerId` 错写成 `playerId`，会把埋葬后的 owner provenance 洗成控制者。
- 已做最小修复：
  - `skeletonsGravestonesAfterScoring()` 的 continuationContext 显式带上 `sourceCardUid`。
  - `handleSkeletonsGravestonesAfterScoring()` 改按 `sourceCardUid + (metadata.sourceControllerId ?? ownerId)` 精确回捞当前 source 卡。
  - 埋葬事件的 `trueOwnerId` 改回 `actionCard.ownerId`，不扩到其它 Skeletons bury handler。
- 已验证：
  - `newFactionAbilities.test.ts --testNamePattern "borrowed skeletons_gravestones|skeletons_gravestones 计分后可把自己埋葬到另一个基地"` -> `1 file passed, 2 passed`
  - `reactionQueueSourceRuntimeContext.test.ts + newFactionAbilities.test.ts -t "skeletons_gravestones|Gravestones|borrowed skeletons_gravestones"` -> `2 files passed, 4 passed`
- 当前边界：
  - 这格只闭合 `borrowed skeletons_gravestones` 的 afterScoring handler / bury true-owner seam。
  - 不外推整个 Skeletons、所有 bury handler、所有 afterScoring ongoing interaction、或整条长期任务已完成。
  - 下一步优先继续扫仍显式写 `ownerId === playerId`、但 live state 已持久化 `metadata.sourceControllerId` 的 provider / handler / candidate builder sibling；优先看 `Infiltrate` 基地限制例外和同类 afterScoring / bury handler，而不是回扫刚转绿的 `zombie_theyre_coming_to_get_you / skeletons_gravestones`。

## 2026-05-25 13:26 +08

- 已继续把 `Infiltrate` 在两个基地 FAQ 例外上的 borrowed controller seam 打成 focused 红灯后收绿，不是停在 Skeletons。
- 红灯证据：
  - 在 [`src/games/smashup/__tests__/baseRestrictions.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseRestrictions.test.ts>) 新增两条 focused gate：
  - `borrowed Infiltrate 由控制者控制时，控制者仍可在 Tsar’s Palace 打出 power≤2 随从`
  - `borrowed Infiltrate 由控制者控制时，控制者仍可忽略 Antarctic Base 的每回合每基地一次限制`
  - 首轮两条都直接红灯：`isOperationRestricted(...)` 都返回 `true`，说明 FAQ 例外没有认出 borrowed `Infiltrate` 的控制者。
- 最小根因：
  - [`src/games/smashup/domain/ongoingEffects.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/ongoingEffects.ts>) 在 `base_tsars_palace` 与 `base_antarctic_base` 的 FAQ 例外里，仍只按 `o.ownerId === playerId` 检查基地上是否有 `ninja_infiltrate`。
  - 对 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Infiltrate` 来说，这会把控制者 P0 继续当成“没有 Infiltrate 的人”。
- 已做最小修复：
  - 只把这两个 FAQ 例外分支统一改成按 `o.metadata?.sourceControllerId ?? o.ownerId` 判控制者。
  - 不扩到其它基地限制、其它 Ninja 行动、或 shared restriction registry。
- 已验证：
  - `baseRestrictions.test.ts --testNamePattern "borrowed Infiltrate|Tsar’s Palace|Antarctic Base"` -> 首轮 failed，修复后 `1 file passed, 3 passed`
- 当前边界：
  - 这格只闭合 borrowed `Infiltrate` 在 `Tsar's Palace / Antarctic Base` 两个 FAQ 例外上的 base restriction seam。
  - 不外推所有基地限制、所有 FAQ 例外、所有 Ninja 行动、或整条长期任务已完成。

## 2026-05-25 13:34 +08

- 已继续把 borrowed `Infiltrate` 在两个基地能力上的 owner/controller seam 打成 focused 红灯后收绿，不是停在 restriction FAQ。
- 红灯证据：
  - 在 [`src/games/smashup/__tests__/baseAbilityIntegration.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseAbilityIntegration.test.ts>) 新增 focused gate `borrowed Infiltrate 由控制者控制时，应阻止 Castle Blood 给控制者打出的随从放指示物`。
  - 在 [`src/games/smashup/__tests__/baseAbilitiesPrompt.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseAbilitiesPrompt.test.ts>) 新增 focused gate `borrowed Infiltrate 由控制者控制时，应阻止 The Hill 给控制者生成移动 Prompt`。
  - 首轮两条都直接红灯：`Castle Blood` 仍吐 `POWER_COUNTER_ADDED`，`The Hill` 仍生成 1 条交互。
- 最小根因：
  - [`src/games/smashup/domain/baseAbilities.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/domain/baseAbilities.ts>) 在 `base_castle_blood` 与 `base_the_hill` 两条能力里，仍只按 `o.ownerId === ctx.playerId` 认基地上的 `ninja_infiltrate`。
  - 对 `ownerId='1', metadata.sourceControllerId='0'` 的 borrowed `Infiltrate` 来说，这会让控制者 P0 仍被基地能力命中。
- 已做最小修复：
  - 只把这两条基地能力的 `ignored` 判定改成按 `o.metadata?.sourceControllerId ?? o.ownerId` 识别控制者。
  - 不扩到其它基地能力、其它 Ninja 行动或 shared base ability framework。
- 已验证：
  - `baseAbilityIntegration.test.ts + baseAbilitiesPrompt.test.ts -t "borrowed Infiltrate|Castle Blood|The Hill"` -> 首轮 failed，修复后 `2 files passed, 2 passed`
- 当前边界：
  - 这格只闭合 borrowed `Infiltrate` 在 `Castle Blood / The Hill` 两条基地能力上的 base-ability seam。
  - 不外推所有基地能力、所有 `ignored by Infiltrate` 分支、所有 Ninja 行动、或整条长期任务已完成。

## 2026-05-25 13:40 +08

- 已继续沿同一条 borrowed ongoing controller 主线再收掉两条新的 `Killer Plants` turn-start 真红灯，不是回扫已封账的 `Spy / Time Box / The Bride / Fairies / destroy-discard inventory`。
- 红灯证据：
  - 在 [`src/games/smashup/__tests__/newOngoingAbilities.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/newOngoingAbilities.test.ts>) 新增 focused gate `borrowed Overgrowth 应按控制者而不是真实 owner 在控制者回合开始降低临界点`。
  - 同文件新增 focused gate `borrowed Entangled 应按控制者而不是真实 owner 在控制者回合开始自毁`。
  - 首轮直接坐实：P0 控制、P1 拥有的 borrowed `killer_plant_overgrowth` 在 P0 回合开始时没有 `BREAKPOINT_MODIFIED(reason='killer_plant_overgrowth')`；borrowed `killer_plant_entangled` 在 P0 回合开始时没有 `ONGOING_DETACHED(defId='killer_plant_entangled')`。
- 最小根因：
  - [`src/games/smashup/abilities/killer_plants.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/killer_plants.ts>) 的 `killerPlantOvergrowthTrigger()` 仍只按 `a.ownerId === ctx.playerId` 认“你的回合开始”。
  - 同文件的 `killerPlantEntangledDestroyTrigger()` 仍只按 `entangled.ownerId !== ctx.playerId` 判是否自毁。
  - 两处都忽略了 live state 已持久化的 `metadata.sourceControllerId`，把 runtime 控制者洗回真实 owner。
- 已做最小修复：
  - `killerPlantOvergrowthTrigger()` 改按 `a.metadata?.sourceControllerId ?? a.ownerId` 判控制者。
  - `killerPlantEntangledDestroyTrigger()` 也统一改按 `entangled.metadata?.sourceControllerId ?? entangled.ownerId` 判控制者。
  - 不扩到 `killer_plant_choking_vines`、`entangled` protection checker、其它 Killer Plants sibling 或 shared onTurnStart framework。
- 已验证：
  - `newOngoingAbilities.test.ts -t "borrowed Overgrowth|borrowed Entangled|killer_plant_overgrowth|killer_plant_entangled"` -> 首轮 failed，修复后 `1 file passed, 11 passed`
- 当前边界：
  - 这轮只闭合 borrowed `killer_plant_overgrowth` 的 turn-start breakpoint modifier seam，以及 borrowed `killer_plant_entangled` 的 turn-start self-destruct seam。
  - 不外推整个 Killer Plants、所有 protection checker、所有 onTurnStart ongoing、或整条长期任务已完成。
  - 下一步优先继续筛仍显式写 `ownerId === playerId`、但 live state 已持久化 `metadata.sourceControllerId` 的 runtime consumer / restriction / candidate builder sibling；优先看 [`src/games/smashup/abilities/yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>)、[`src/games/smashup/abilities/sharks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/sharks.ts>)、[`src/games/smashup/abilities/tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 里仍裸读 `ownerId` 的候选，不回扫刚转绿的 `Infiltrate / skeletons_gravestones / killer_plant_overgrowth / killer_plant_entangled`。

## 2026-05-25 13:44 +08

- 已继续把 `time_travelers_stasis_field` 的 borrowed turn-start self-destruct controller seam 补成 dedicated focused gate，并完成最小修复验证。
- 已验证事实：
  - 在 [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Stasis Field 应按控制者而不是真实 owner 在控制者回合开始自毁`。
  - 该 gate 当前已通过：queued trigger 的 `ownerPlayerId/sourceControllerId` 都是 `0`，resolve 后 `stasis-borrowed` 从基地离场并进入真实拥有者 P1 的 discard。
- 最小修复：
  - [`src/games/smashup/abilities/yuanhou.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/yuanhou.ts>) 的 `timeTravelersStasisFieldTurnStart()` 旧实现仍只按 `action.ownerId === ctx.playerId` 判“你的回合开始”。
  - 现已只把这条 callback 改成按 `action.metadata?.sourceControllerId ?? action.ownerId` 判控制者，不扩到 scoring suppression、attach 链、E2E 壳或其它 Time Travelers sibling。
- 已验证：
  - `reactionQueueEventPlayerContext.test.ts -t "Stasis Field|borrowed Stasis Field"` -> `1 file passed, 2 passed`
- 当前边界：
  - 这格只把 borrowed `time_travelers_stasis_field.destroy_at_owner_turn_start` 从“已有 owner-only gate、缺 borrowed dedicated gate”提升为 dedicated green evidence。
  - 不外推整个 Time Travelers、所有 onTurnStart ongoing、或整条长期任务已完成。

## 2026-05-25 13:48 +08

- 已继续把 `sharks_week_of_sharks` 的 borrowed turn-end draw controller seam 补成 dedicated focused gate，并完成最小修复验证。
- 已验证事实：
  - 在 [`src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/reactionQueueEventPlayerContext.test.ts>) 新增 focused gate `borrowed Week of Sharks 应按控制者而不是真实 owner 在控制者回合结束抽牌`。
  - 该 gate 当前已通过：queued trigger 的 `ownerPlayerId/sourceControllerId` 都是 `0`；resolve 后 `week-draw-borrowed` 进入 P0 hand，而不是 P1 hand。
- 最小修复：
  - [`src/games/smashup/abilities/sharks.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/sharks.ts>) 的 `sharksWeekOfSharksTrigger()` 旧实现三处都绑在 `action.ownerId`：识别回合归属、判“这里有你的随从”、以及发抽牌事件的目标玩家。
  - 现已只把这条 callback 统一改成按 `action.metadata?.sourceControllerId ?? action.ownerId` 判控制者，不扩到 `Mako / Blood in the Water / Hammerhead / Chum` 或其它 Sharks sibling。
- 已验证：
  - `reactionQueueEventPlayerContext.test.ts -t "Week of Sharks|borrowed Week of Sharks"` -> `1 file passed, 2 passed`
- 当前边界：
  - 这格只把 borrowed `sharks_week_of_sharks.only_owner_turn_end_draw_one` 从“已有 owner-only gate、缺 borrowed dedicated gate”提升为 dedicated green evidence。
  - 不外推整个 Sharks、所有 onTurnEnd ongoing、或整条长期任务已完成。

## 2026-05-25 13:56 +08

- 已继续把 `trickster_flame_trap / trickster_pay_the_piper` 与其 POD 版的 borrowed onMinionPlayed controller seam 收成一组 focused green，不再停在旧的 collector eligibility 文档。
- 已验证事实：
  - 在 [`src/games/smashup/__tests__/baseFactionOngoing.test.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/__tests__/baseFactionOngoing.test.ts>) 新增 4 条 focused gate：
  - `borrowed Flame Trap 应按控制者而不是真实 owner 消灭对手打出的随从`
  - `borrowed Flame Trap POD 应按控制者而不是真实 owner 消灭对手打出的随从并标记 destroyerId`
  - `borrowed Pay the Piper 应按控制者而不是真实 owner 让对手弃牌`
  - `borrowed Pay the Piper POD 应按控制者而不是真实 owner 让对手弃牌`
  - 当前 4 条 gate 都已通过；邻近非 borrowed 子集 `trickster_flame_trap|trickster_pay_the_piper` 也保持通过。
- 最小修复：
  - [`src/games/smashup/abilities/tricksters.ts`](</D:/gongzuo/webgame/BoardGame/.worktrees/smashup-yuanhou-factions/src/games/smashup/abilities/tricksters.ts>) 的 `canTriggerTricksterBaseOngoingAgainstOtherPlayer()` 旧实现仍只按 `source.ownerId === ctx.playerId` 判“是不是其他玩家”。
  - `trickster_flame_trap / trickster_pay_the_piper / trickster_flame_trap_pod / trickster_pay_the_piper_pod` 四条 callback 也都仍只按 `ownerId` 判敌我。
  - 现已统一改成按 `metadata.sourceControllerId ?? ownerId` 判控制者；`Flame Trap POD` 的 `destroyerId` 也改为控制者，不再错误写成 true owner。
- 已验证：
  - `baseFactionOngoing.test.ts -t "borrowed Flame Trap|borrowed Pay the Piper|Flame Trap POD|Pay the Piper POD"` -> `1 file passed, 4 passed`
  - `baseFactionOngoing.test.ts -t "trickster_flame_trap|trickster_pay_the_piper"` -> `1 file passed, 6 passed`
- 当前边界：
  - 这轮只闭合 `trickster_flame_trap / trickster_pay_the_piper` 标准版与 POD 版在 borrowed onMinionPlayed 场景下的 controller seam。
  - 不外推整个 Tricksters、所有 POD、`Flame Trap POD` 的 onTurnStart breakpoint prompt、或整条长期任务已完成。
