export {
    DEFAULT_ANDROID_BACKEND_URL,
    DEFAULT_PUBLIC_BACKEND_URL,
    assertNoPublicBackendSplit,
    normalizeBackendUrl,
    resolveAndroidBackendUrl,
    resolvePublicBackendUrl,
} from './public-backend-url.js';
