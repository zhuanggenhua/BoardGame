/**
 * 召唤师战争 - 棋盘配置
 * 
 * 单一数据源：所有棋盘尺寸相关的代码都应该引用这里
 * 注意：这里的值必须与 public/game-data/summonerwars.layout.json 保持一致
 * 
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │ 坐标系说明                                                              │
 * ├─────────────────────────────────────────────────────────────────────────┤
 * │                                                                         │
 * │ 【配置坐标系】- 左下角原点（玩家0视角，用于阵营配置）                    │
 * │   - row: 从下往上数（0=最底行/我方后排，7=最顶行/对方后排）              │
 * │   - col: 从左往右数（0=最左，5=最右）                                    │
 * │                                                                         │
 * │ 【数组坐标系】- 左上角原点（二维数组索引，用于 board[row][col]）         │
 * │   - row: 从上往下数（0=最顶行，7=最底行）                                │
 * │   - col: 从左往右数（0=最左，5=最右）                                    │
 * │                                                                         │
 * │ 【转换公式】                                                            │
 * │   - 玩家0（底部）: arrayRow = BOARD_ROWS - 1 - configRow                │
 * │   - 玩家1（顶部）: arrayRow = configRow, arrayCol = BOARD_COLS - 1 - configCol │
 * │                                                                         │
 * │ 棋盘示意图（配置坐标系）：                                               │
 * │                                                                         │
 * │   col:  0   1   2   3   4   5                                           │
 * │       ┌───┬───┬───┬───┬───┬───┐                                         │
 * │  row 7│   │   │   │   │   │   │  ← 对方后排                             │
 * │       ├───┼───┼───┼───┼───┼───┤                                         │
 * │  row 6│   │   │   │   │   │   │                                         │
 * │       ├───┼───┼───┼───┼───┼───┤                                         │
 * │  ...  │   │   │   │   │   │   │                                         │
 * │       ├───┼───┼───┼───┼───┼───┤                                         │
 * │  row 1│   │   │   │   │   │   │                                         │
 * │       ├───┼───┼───┼───┼───┼───┤                                         │
 * │  row 0│   │   │   │   │   │   │  ← 我方后排                             │
 * │       └───┴───┴───┴───┴───┴───┘                                         │
 * │                                                                         │
 * └─────────────────────────────────────────────────────────────────────────┘
 */

/** 棋盘行数 */
export const BOARD_ROWS = 8;

/** 棋盘列数 */
export const BOARD_COLS = 6;
