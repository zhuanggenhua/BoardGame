## 2026-05-08 bonus dice regression follow-up
- action: 使用正确 E2E 入口复现 `samurai righteousness should resolve a valid branch against monk`，并读取失败上下文。
- rationale: 需要把摘要里的推测换成真实 Playwright 证据，确认是否仍卡在 bonus dice overlay 的收尾态。
- result: `npm run test:e2e:file -- e2e/dicethrone/dicethrone-watch-out-spotlight.e2e.ts "samurai righteousness should resolve a valid branch against monk"` 已真实跑到目标用例并在 300000ms 超时。失败点位于 `e2e/dicethrone/dicethrone-watch-out-spotlight.e2e.ts:2255` 的 `page.waitForFunction`：页面始终等不到 `[data-testid="bonus-die-overlay"]` 内出现最终稳定态（注释写的是停止滚动后出现 `.animate-pulse` glow ring）。此前两次失败分别是误用 vitest 入口、以及把 `default` 误传成 Playwright 文件过滤参数，现已排除为命令层问题。
- next: 继续读取 error-context / 截图文本上下文，确认 overlay 当时 DOM 是否存在、缺的是 pulse 态还是 overlay 本身未关闭，再决定是修 E2E 断言还是修业务收尾逻辑。
