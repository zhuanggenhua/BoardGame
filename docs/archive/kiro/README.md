# Kiro 旧自动恢复归档

旧 Kiro auto-resume、window-title monitor 和 watcher 文档已删除长正文，只保留当前仍有价值的结论：

- 旧 auto-continue / window-title monitor 方案已退役，不是当前项目入口。
- 项目日志不能代表 Kiro / AI IDE 的真实运行状态。
- 可复用经验只有 checkpoint / resume、false-active 识别和长任务健康判定。

需要当前项目 AI 规范时回到 [`.spec`](../../../.spec/AGENTS.md)。需要追溯旧方案细节时查 git 历史。
