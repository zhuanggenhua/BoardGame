# 70e94e3bf 逐行审查归档摘要

原文件是一次性 `git diff` 逐行审查输出，逐行复制了大量新增 / 删除代码和临时任务记录。它不作为当前代码审查入口、AI 规范或任务状态来源。本文保留当时可复用的结论和风险，不恢复 8000 行机器生成 diff。

## 审查对象

- 提交：`70e94e3bfedc7c7925f392ab180f35e0ad59c991`
- 原审查口径：对 diff 中新增 / 删除行逐行标注 `OK`、`注意`、`风险`。
- 原标签含义：
  - `OK`：低风险、文档或测试记录。
  - `注意`：需要核对逻辑、交互或覆盖变化。
  - `风险`：可能引起回归，需要人工确认或补测试。

## 保留结论

当时最重要的风险是 DiceThrone 4 人 / 2v2 的 `targetingRoll` 接入后，流程状态和命令校验不同步：

- 测试状态会进入 `targetingRoll`。
- 当时新增用例会在 `targetingRoll` 下继续投骰、确认骰子或锁骰。
- 命令校验没有同步允许 `ROLL_DICE`、`CONFIRM_ROLL`、`TOGGLE_DIE_LOCK` 在该阶段执行。
- 现实后果是流程用例出现 `invalid_phase`，玩家流程可能卡在目标投骰阶段。

旧文末尾记录该风险已在当时工作区修复；当前是否仍成立必须重新查源码和测试，不能直接引用本归档当作当前状态。

## 当时其它注意点

- 删除或收口 E2E 文件时，需要确认覆盖没有减少；原审查对多个旧 E2E 删除标过“注意”。
- DiceThrone helper 曾新增 API 可用性探测、重试和 setup debug 记录，目标是区分房间创建、占座、加入、服务不可用等真实失败步骤。
- 原 diff 中曾出现中文注释乱码，如“跳过”“等待特定阶段”等字符显示异常；当时审查把它标为需要核对的文本质量问题。
- 后续 progress 片段记录过 `targetingSelectionResolved` 幂等保护、4 人站位、2v2 队伍轮转、响应队列过滤等一组修复，但这些属于旧会话记录，不能接管当前目标。

## 不恢复全文的原因

- 原文主体是逐行 diff 复刻，不是稳定知识。
- 大量测试源码片段、`findings.md`、`task_plan.md` 和临时进度会污染当前检索。
- 当前需要保留的是“当时发现了什么风险、为什么重要、当前如何重新验证”，不是每一行新增/删除代码。

## 重新验证入口

若当前再次怀疑 DiceThrone 4 人 / 2v2 回归，最小验证应回到：

- 领域流程与校验：`src/games/dicethrone/domain/`
- DiceThrone 测试：`src/games/dicethrone/__tests__/`
- 在线 E2E：`e2e/dicethrone/`
- 当前任务对应的 evidence 或 OpenSpec change

最终结论必须以当前源码和当前测试结果为准。
