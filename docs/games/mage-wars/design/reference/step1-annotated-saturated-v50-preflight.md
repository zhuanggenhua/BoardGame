# Mage Wars 标注图饱和布局 v50 出图前硬回执

> 状态：`preflight-ready / open-design-artifact-only / media-generate-forbidden / human-review-not-allowed / implementation-blocked / mobile-blocked`。本轮先更新项目设计前置规范的“规则对象实体锚点守恒”，再把 v49 重构为 v50；不进入真实 Board/UI、真实运行页 E2E 或移动端适配。

## 本轮问题锁定

| 项 | 当前锁定 |
| --- | --- |
| 问题对象 | `mage-wars-step1-annotated-saturated-v49.html` 的状态 / 摘要层是否复制规则实体 |
| 真相来源 | 用户反馈、`apprentice-spellbooks.md`、`board-ui-preflight-matrix.md`、`board-coordinate-contract.md`、`step1-runtime-board-asset-input-manifest.md`、全局 `ui-design-pipeline` 的规则对象实体锚点守恒 |
| 目标入口 / 环境 | Open Design artifact 代码设计稿 `mage-wars-step1-annotated-saturated-v50.html` |
| 验收口径 | 每个可见 / 可操作规则对象只能有一个实体锚点；状态、来源、目标摘要和详情只能引用或贴附该锚点；AI 图面核验通过后才允许人工验收 |

## 规则到画面结论

| 规则 / 设计结论 | 影响主体 | v50 设计决策 |
| --- | --- | --- |
| `火球术` 是己方已计划法术中的一张真实牌 | 右下已计划槽 | 保留唯一实体卡 `spell:1700:planned-own-1`；把 `施法来源` 做成贴附状态，不再显示“已选法术”复制卡 |
| `西锁骑士` 是 B2 区域内的场上目标实体 | B2 场上卡与目标摘要 | 竞技场内保留唯一实体卡 `spell:2909:field-b2-copy-1`；目标摘要改为 `B2 / 西锁骑士` 文字引用，不再复制第二张西锁骑士缩略图 |
| 弃牌堆是公开归档区，不是当前场上对象 | 右下弃牌堆入口 | 弃牌堆顶牌换成未在场的 `风龙皮甲` 示例，避免同名同图与 A2 场上 `烈焰狱鬼` 混淆；仍作为公开归档入口 |
| 执行态法术书不是当前来源主体 | 底部法术书浏览 | 法术书继续显示其它可浏览法术、分类和分页；不重复已计划 `火球术` |
| 当前动作仍是 `已计划火球术 -> B2 西锁骑士` | 路径线、目标高亮和骰子 / token | 路径线从右下唯一来源锚点指向 B2 唯一目标锚点；攻击骰、效果骰、伤害 / 燃烧 token 仍锚在目标附近 |

## 规则对象实体锚点表

| 规则对象 | 唯一实体锚点 | 状态 / 详情引用 | 同名多副本裁定 |
| --- | --- | --- | --- |
| 火球术 | 右下 `己方已计划` 第一张计划卡，`data-rule-object-id="spell:1700:planned-own-1"` | `施法来源` 徽标贴附在该卡上；路径线从该锚点出发 | 当前稿只展示 1 张火球术；法术书浏览不再列出火球术 |
| 西锁骑士 | B2 场上卡，`data-rule-object-id="spell:2909:field-b2-copy-1"` | 目标摘要只写 `B2 / 西锁骑士 / 场上目标`，不放第二张卡图 | 当前稿只展示 1 张西锁骑士实体 |
| 烈焰狱鬼 | A2 场上卡，`data-rule-object-id="spell:2803:field-a2-copy-1"` | 无详情复制；弃牌堆不再用同图顶牌 | 当前稿只展示 1 张烈焰狱鬼实体 |
| 法术书候选 | 底部法术书浏览 5 张候选卡 | 分类 / 页码只引用集合，不与已计划卡重复 | 候选卡均不与当前已计划火球术重复 |
| 对手已计划法术 | 左上 2 张卡背 | 只显示卡背和数量，不公开正面或名称 | 隐藏信息边界不允许实体正面 |
| 弃牌堆 | 右下边缘入口，顶牌为 `风龙皮甲` 示例 | 只作为公开归档入口和数量提示 | 不与当前场上卡、当前来源或法术书可见候选重复 |

## 禁止项

- 不调用 `od media generate`，不走图片模型生图。
- 不写真实 Board/UI，不启动真实运行页，不跑实现 E2E，不做移动端。
- 不创建底部“已选法术”大卡，不把 `已选 / 当前来源 / 当前目标 / 摘要 / 详情` 画成第二个规则实体。
- 不引入 `手牌`、`hand`、`opponent-hand` 或常驻 `确认 / 执行 / 取消`。

## 人工验收状态

- 当前 v50 只能进入截图和 AI 图面核验。
- AI 图面核验通过前，仍为 `human-review-not-allowed`。
- 若 AI 图面发现同一规则对象被复制成两个实体、目标链断开、骰子 / token 缺失或出现规则外术语，本稿直接降为 `REVISE`，不得打开给用户。
