# 法师战争领域建模前置审查

> 当前快照（2026-08-04，`1825 / 1901 / 1904` 响应链完成后）：配置包共 91 张学徒法术，其中 90 张 implemented、1 张 needsCode；分类为攻击 10/10、生物 24/24、魔物 1/1、咒语 18/18、装备 14/14、结界 23/24。后文带日期的切片段落保留当时统计，当前状态以本快照和最新切片为准。

## 2026-08-04 结构化群兽法杖特殊能力切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `3710` 群兽法杖为费用 7、快速装备 / 武器、限定兽王；每回合一次支付 2 点法力，选择距离法师最多 1 格的己方活体动物生物，给予近战 +2 直到本回合结束，或投掷 2 颗攻击骰治疗。 |
| 配置 owner | `data.combatProfiles.attacks` 保存蛮力一击的快速近战 4 骰；`data.combatTraits.beastStaff` 保存稳定能力 ID、兽王限制、2 点能力费用、快速行动、0-1 格范围、近战 +2 和 2 颗治疗骰；`3710` 标记为 `requiresCodeSupport=false`。 |
| 运行时消费 | 复用 `USE_ARENA_OBJECT_ABILITY`，以 `mode=melee-bonus / heal` 区分效果；只接受附着在当前兽王法师身上的配置装备，目标必须是己方活体动物生物且距离不超过 1 格；能力按快速施法窗口或生物行动阶段消耗对应行动轨道，并记录装备本回合已用。 |
| 生命周期 | 强化通过 `ARENA_OBJECT_TEMPORARY_TRAITS_GAINED` 写入回合有效的近战 +2，跨越生物行动阶段和最终快速施法，在下一回合统一清理；`3417` 荒野呼唤同步使用同一回合有效标记。治疗通过 `SPELL_HEALING_ROLLED` 记录恰好 2 颗攻击骰，实际治疗不超过目标伤害。 |
| 边界 | 不实现 `3716` 法术绑定、装备栏、完整职业系统或 UI；不新增第二套装备能力命令。 |
| 验证证据 | 独立 OpenSpec `refactor-mage-wars-structured-beast-staff`；`config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts` 共 233 tests 通过，TypeScript、定向 ESLint 和 OpenSpec 严格校验通过。 |

## 2026-08-04 结构化元素魔杖法术绑定切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同与卡面文本锁定 `3716` 元素魔杖：可从自己的法术书绑定非史诗攻击类法术；更换绑定法术属于快速法术并支付 3 点法力。 |
| 状态 owner | 绑定关系由具体 `equipment` 对象的 `boundSpellCardId` 持有；不复用玩家计划列表，不把绑定牌写入弃牌堆。 |
| 命令 / 事件 | 首次绑定随 `CAST_SPELL` 创建装备；替换绑定复用 `USE_ARENA_OBJECT_ABILITY` 与 `ARENA_OBJECT_ABILITY_RESOLVED`，由 reducer 更新装备绑定和法力 / 快速施法标记。 |
| 资格校验 | 配置解析保留卡牌原始标签；运行时只读法术书成员关系、`spellType=攻击` 和史诗标签，拒绝相同绑定牌、非法来源、非快速施法阶段和费用不足。 |
| 边界 | `1804` 法师祸咒需要独立的非玩家生物施法者来源建模；`1825` 厄运、`1901` 法力失效和 `1904` 攻击逆转的响应链已在独立切片完成；完整卡牌实例化、装备栏限制和完整装备 UI 不在本切片。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts` 共 207 tests 通过；TypeScript 和 OpenSpec 严格校验通过。 |

## 2026-08-04 结构化偏移护腕法师防御切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `3715` 偏移护腕为费用 6、装备 / 防御、目标法师；卡面防御为 7+ / 1x。 |
| 已覆盖 | 配置包为 `3715` 录入结构化 `combatProfiles.defenses`，法师附着装备读取该 profile；法师被基础近战、竞技场对象或攻击类法术攻击时复用现有 `DEFENSE_AVAILABLE` 交互。7+ 成功时攻击未命中，失败时原攻击只恢复一次。 |
| 回合状态 | 玩家按来源 profile 记录本回合防御次数；行动准备重置时清除。移除装备后来源查询为空，不再提供防御。 |
| 规则 owner | 配置包保存阈值和次数，`spellRules.ts` 汇总法师装备来源，`execute.ts` / `spellAbilityExecutors.ts` 负责攻击窗口和恢复，`systems.ts` 保持单一防御交互 owner；不从防御展示文本读取。 |
| 边界 | `1804` 仍保持 needsCode；`1825`、`1901`、`1904`、`3700` 与 `3710` 由本文件或独立结构化切片覆盖；不修改 UI、素材、隐藏结界或完整防御 UI。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；覆盖法师 / 对象 / 攻击法术三类攻击入口、成功 / 失败、费用不重复、每回合重置和配置文本不作为规则 owner。 |

## 2026-08-04 结构化抑制斗篷来袭攻击费用切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `3705` 抑制斗篷为费用 8、装备 / 法力、目标法师；每个生物每回合首次对该法师进行近战攻击时，攻击者控制方须支付 2 点法力，反击不触发。 |
| 已覆盖 | `data.combatTraits.meleeAttackManaTax` 记录 2 点、每个攻击生物每回合一次和反击排除；附着在目标法师身上的装备提供费用来源。只有生物近战攻击法师时触发，远程、非生物和反击不触发。 |
| 支付与取消 | 费用在既有防御窗口前支付；法力不足时不投骰、不造成伤害，但通过空骰 `ARENA_OBJECT_ATTACK_DECLARED` 和 `ATTACK_MISSED` 消耗攻击行动。抑制斗篷与 `1912` 心灵安抚同时存在时合计判断，禁止部分扣费。 |
| 回合与来源 | 每个来源记录本回合已触发的攻击生物；同回合不重复，新回合恢复，多来源分别收费，移除装备后来源查询为空。防御响应恢复原攻击时不会重复收费。 |
| 规则 owner | `mage-wars.config.json` 保存静态特性，`configPackage.ts` 严格读取，`spellRules.ts` 汇总目标法师装备来源，`execute.ts` 负责原子支付和攻击取消，`reducer.ts` 保存跨防御窗口的回合事实；不从卡面中文文本读取。 |
| 验证证据 | 独立 OpenSpec `refactor-mage-wars-structured-suppression-cloak`；`config-package.test.ts`、`combat-profile.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`、完整 Mage Wars 测试 7 files / 222 tests、TypeScript、定向 ESLint 和 OpenSpec 严格校验。 |

## 2026-08-04 结构化恶魔胸甲反伤屏障切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书 page 027 / 029 与 FAQ page 005 锁定反伤屏障：成功的敌方近战攻击在全部打击完成后自动发动；至少投掷过一颗近战攻击骰，不能由全部回避或失手触发；每回合对每名攻击方一次。 |
| 已覆盖 | `data.combatTraits.damageBarrier` 记录 `diceCount=1`、内部 `aether`、`unavoidable=true`、`lethal=true` 和每回合每攻击者一次；`3700` 已标记 `requiresCodeSupport=false`。法师基础近战和场上对象近战都在既有伤害结算后触发独立屏障攻击。 |
| 伤害与响应 | 屏障复用 `DamageCalculation` 与 `DAMAGE_DEALT`；致命伤害不加入护甲修正，目标对象的庇护减骰仍复用既有目标侧规则；屏障不创建防御、反击或第二次屏障。屏障伤害可以击败原攻击者。 |
| 回合与来源 | 装备来源记录本回合攻击者键；同回合不重复，新回合恢复，移除装备后来源查询为空。 |
| 规则 owner | 配置包保存静态特性，`spellRules.ts` 查询附着法师装备，`execute.ts` 统一生成两类攻击入口的屏障事件，`reducer.ts` 保存来源使用事实；不从中文卡面文本读取。 |
| 验证证据 | 独立 OpenSpec `refactor-mage-wars-structured-damage-barrier`；配置、能力目录、来源查询、法师近战、对象近战、护甲忽略、回合限制、来源移除和攻击者击败测试均已覆盖。 |

## 2026-08-04 结构化心灵安抚切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `1912` 心灵安抚为费用 `2+2`、可见对象附属结界、目标为非法师生物；每回合该生物首次进行近战或远程攻击时，其控制者须支付 2 点法力，否则本次攻击取消，反击不触发。 |
| 已覆盖 | `1912` 由配置包 `data.semantics` 声明对象锚点和 `grants: mental-calm=2`，合法施放生成公开附属结界；对象攻击宣告阶段收集当前回合尚未触发的来源，法力足够时先支付并在防御窗口前记录触发，法力不足时发出空骰攻击宣告与 `ATTACK_MISSED`，不进入防御或伤害。 |
| 回合与多来源 | 每个附属来源独立按 2 点收费并记录攻击者 / 回合；同一来源同一回合不重复触发，新回合按回合号重新可用；反击入口显式排除，不新增第二套攻击系统。 |
| 规则 owner | `mage-wars.config.json` 保存静态特性，`spellRules.ts` 汇总附属来源，`execute.ts` 负责 `MANA_SPENT` / `MENTAL_CALM_TRIGGERED` 与攻击取消时序，`reducer.ts` 保存跨防御窗口的回合事实；不从玩家可见规则文本读取。 |
| 边界 | 不实现法师绑定、法师攻击、法术攻击、装备攻击继承、隐藏结界、展示 / 反制和完整结界 UI。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；覆盖防御窗口前支付、法力不足取消、每回合一次、新回合恢复、双来源独立收费、远程触发、反击排除和展示文本不作为 owner。OpenSpec change 已严格校验。 |

## 2026-08-04 结构化尸鬼腐化切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `1820` 尸鬼腐化为费用 `2+4`、可见附属结界、目标为活体生物；卡面效果是每个维持阶段造成 2 点直接毒素伤害，并带有尚未闭合的法师绑定+2。 |
| 已覆盖 | `1820` 由配置包 `data.semantics` 声明对象附属和 `upkeepEffects: direct-damage / 2 / 毒素`，合法施放生成公开附属结界；进入维持阶段时读取附属来源并复用直接伤害、毒素免疫和对象击败路径。移除展示文案不影响结算，来源被驱散后停止后续伤害。 |
| 规则 owner | 维持伤害由配置语义和附属来源查询驱动，不从玩家可见规则文本读取；不混入腐化状态 token 的维持伤害，也不新增第二套状态模型。 |
| 边界 | 法师绑定+2 支付 / 维持系统、`1804` 法师祸咒、`1904` 攻击逆转、`1912` 心灵安抚、隐藏结界、展示 / 反制和完整结界 UI 不在本切片。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；领域流程覆盖固定伤害、展示文案移除、毒素免疫、来源驱散后停止和既有腐化维持回归。 |

## 2026-08-04 结构化原力之握切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `1908` 原力之握为费用 `2+2`、可见附属结界、目标为生物；卡面效果是束缚目标并获得稳固，对不羁生物无效。 |
| 已覆盖 | `1908` 由配置包 `data.semantics` 声明 `visible-object-enchantment` 与 `grants: restrained`，合法施放生成公开附属结界并发出现有 `ARENA_OBJECT_RESTRAINED` 关系事件；不羁目标拒绝施放。普通移动、原力推斥和传送继续复用既有不可移动校验，结界驱散 / 离场继续复用既有来源清理。 |
| 规则 owner | 运行时从结构化配置和来源关系读取束缚，不从玩家可见规则文本读取；不新增第二套移动或状态模型。 |
| 边界 | `2224` 缠绕藤蔓保持原有魔物路径；`1904` 攻击逆转、`1912` 心灵安抚、隐藏结界、展示 / 反制和完整结界 UI 不在本切片。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；领域流程覆盖不羁拒绝、移除展示文案后仍生效、移动 / 推斥 / 传送拒绝和驱散后束缚清理。 |

> 状态：`foundation-model-ready / foundation-implementation-reviewed / arena-object-action-slice-started / object-armor-damage-pipeline-covered / object-attack-profile-explicit / object-attack-effect-die-status-covered / daze-stun-conjuration-immunity-covered / daze-attack-miss-covered / daze-action-end-removal-covered / daze-defense-penalty-covered / stun-defense-disabled-covered / restrained-defense-penalty-covered / stun-basic-action-lock-covered / stun-attack-spell-lock-covered / stun-quick-nonattack-cast-exception-covered / stun-action-end-removal-covered / spell-action-speed-config-covered / spell-action-speed-all-apprentice-covered / stun-guard-removal-covered / guard-interception-covered / guard-melee-removal-covered / guard-object-command-covered / guard-counterstrike-choice-covered / object-defense-window-covered / object-defense-cooldown-covered / object-attack-pierce-armor-covered / object-triple-strike-covered / object-regeneration-covered / rot-status-token-covered / rot-upkeep-direct-damage-covered / burn-upkeep-tick-covered / sleep-damage-daze-replacement-covered / weak-cripple-status-token-covered / weak-attack-dice-effect-covered / cripple-move-lock-covered / cripple-escape-check-covered / healing-spell-slice-covered / life-drain-direct-damage-covered / intermittent-jet-burn-removal-covered / nonliving-bonus-damage-covered / generic-attack-spell-slice-covered / dazzling-flash-area-attack-covered / jet-stream-push-covered / force-push-incantation-covered / teleport-incantation-covered / sleep-incantation-covered / plain-creature-summon-covered / config-creature-rot-attack-covered / flying-bonus-damage-covered / chain-lightning-covered / apprentice-attack-spells-covered / deepwood-shadow-elusive-legendary-covered`。本文件完成 `add-mage-wars-foundation` 的 3.3 门禁：领域对象模型、术语到事件映射、决策点清单和引擎能力缺口清单。2026-07-29 已补 foundation 实现复核；2026-08-01 已补场上对象行动首段实现复核，并把场上对象护甲接入通用伤害管线；对象攻击命令已改为显式选择攻击 profile，避免自动替玩家挑攻击条；对象攻击条上的效果骰状态 token 已接入同一 `STATUS_TOKEN_PLACED` 路径，当前覆盖燃烧、眩晕、昏迷、腐化、虚弱、残废，并对毒素状态执行魔物 / 非活体免疫过滤；眩晕 / 昏迷不会落到魔物，携带眩晕的法师基础近战或生物对象攻击会在攻击前掷效果骰，6 或更低产生 `ATTACK_MISSED`，不投攻击骰也不造成伤害；带眩晕的场上对象防御掷骰已按配置包每枚眩晕 -2 修正，并允许防御方作为反应方执行防御掷骰命令；带昏迷的场上对象不能使用防御 profile，裁定来自配置包瘫痪规则；残废生物视为被束缚，防御掷骰额外 -2，该修正只在存在残废时应用一次；行动阶段结束时移除当前行动方法师和己方生物的全部眩晕与昏迷，并为当前行动方携带残废的生物进行 7+ 逃逸检定，成功时移除残废；昏迷法师不能普通移动、守卫、基础近战攻击或施放攻击类法术，且 91 张学徒法术均已配置 `spellActionSpeed`，昏迷法师可用行动施放非攻击快速法术、不能施放标准法术；快速施法阶段会拒绝非快速法术；昏迷生物不能普通移动或攻击，获得昏迷时会移除守卫；同区敌方守卫生物会拦截生物近战目标选择，且残废/束缚、昏迷和小型守卫不提供区域保护；守卫成为近战攻击目标后，即使攻击因眩晕失手也会移除守卫标记，且不影响守卫对象行动标记；己方 ready 生物可执行守卫命令，消耗自身行动标记而不是法师行动标记；守卫被近战攻击选中后的自愿反击机会已进入防御方交互，可放弃，也可按守卫快速近战攻击结算且不消耗守卫对象行动标记、不递归暴露反击机会；对象攻击目标具有可用防御 profile 且攻击不是无法回避时，会在攻击骰前进入防御方交互，可放弃，选择防御时按防御骰成功回避或失败继续结算，无法回避攻击不暴露防御选择；虚弱已按规则让法师基础近战和场上对象非法术攻击每枚 -1 攻击骰、最低 1 颗，法术攻击不受影响；残废已按规则在移动行动校验中阻止生物执行普通移动；对象攻击条中的 `穿刺+N` 已按规则抵消目标护甲，不作为额外伤害；对象攻击条中的三连击已按一次行动内多段独立伤害结算接入；维持阶段已按规则接入活体对象重生，多个重生来源只取最高，并支持高地独角兽同区友方活体重生光环；维持阶段腐化已按规则让法师和活体场上对象每层受到 1 点直接伤害，非活体对象跳过，腐化 token 保留；维持阶段燃烧已按规则为每枚燃烧 token 投 1 颗攻击骰，非空白结果造成直接伤害，空白结果移除对应燃烧 token；沉睡已按规则在目标受到实际伤害后替换为同层眩晕，0 伤害不触发替换；`次级治疗`、`群体治疗` 与 `单体治疗` 已按配置放行进入施法校验、掷骰、治疗事件、状态 reducer 和行动日志链路；`生命汲取` 已按卡面实现活体目标校验、5 骰直接伤害、不吃对象护甲、按实际伤害治疗施法者和非活体拒绝；`间歇喷泉` 已按卡面实现“燃烧目标取消攻击并移除所有燃烧”路径；`圣光之柱` 已按卡面实现对非活体生物 +2 伤害与眩晕 / 昏迷效果骰；`烈焰爆弹`、`雷导术`、`闪电箭矢` 已按卡面接入普通攻击骰与效果骰状态；`眩目闪光` 已按卡面实现同区区域攻击、排除施法者、命中目标区所有对象、非活体 +2 伤害与效果骰眩晕 / 昏迷；`气流` 已按卡面实现 2 骰攻击、对飞行 +2 伤害、效果骰推斥、11+ 推斥并眩晕、显式推斥目标区校验、`SPELL_PUSH_RESOLVED` 事件、行动日志与风力 FX；`原力推斥` 已按卡面实现目标生物显式相邻目标区推斥 1 格，复用 `SPELL_PUSH_RESOLVED` 与现有推斥 FX；`传送` 已按卡面实现目标生物与目标区域双目标校验、按目标生物当前区域到目标区域距离计算 X 费用、至少 3 点法力、`SPELL_TELEPORT_RESOLVED` 事件、行动日志与传送 FX；`连锁闪电` 已按卡面实现目标链显式输入、每跳来源切换、目标不重复、距离上一受伤目标 `0-1`、攻击骰逐跳递减和效果骰逐跳减 1。完整完成证据见 `docs/games/mage-wars/foundation-completion-self-audit.md`。

> 2026-08-02 补充状态：`damage-type-adjustment-covered / damage-type-immunity-covered / elemental-combat-creature-config-covered / charge-trait-covered / highland-unicorn-regeneration-charge-covered / charge-on-incantation-covered / thunderift-falcon-flying-swift-covered / darkfenne-bat-flying-rot-covered / mana-drain-attack-covered / asyran-cleric-healing-light-covered / grey-angel-redemption-sacrifice-covered / call-of-the-wild-melee-animal-covered / bloodstrike-vampiric-pierce-covered / deepwood-shadow-elusive-legendary-covered / tanglevine-conjuration-restraint-covered / basic-armor-equipment-covered`，详见下方“伤害类型 +/- 修正切片补充”“伤害类型免疫切片补充”“元素 / 战斗字段生物配置放行切片补充”“冲锋特性切片补充”“高地独角兽重生 / 冲锋组合切片补充”“冲锋陷阵律令咒语切片补充”“雷隙猎鹰飞行 / 迅捷配置放行切片补充”“暗沼蝙蝠飞行 / 腐化攻击配置放行切片补充”“法力流失攻击切片补充”“阿希拉牧师治疗之光切片补充”、“灰衣天使救赎献祭切片补充”“荒野呼唤友方动物近战 +1 切片补充”“汲血之击吸血 / 穿刺切片补充”、“深林幽影切维尔迅捷 / 遁逸 / 传奇切片补充”与“缠绕藤蔓魔物 / 束缚切片补充”。

> 2026-08-03 最新补充状态：`steal-enchantment-covered / dispel-visible-enchantment-covered / explode-equipment-covered / dissolve-alternate-covered / passive-armor-equipment-covered / weapon-equipment-attack-covered / visible-object-enchantment-traits-covered`。当前配置包准确统计为 91 张学徒法术中 68 张 implemented、23 张 needsCode；分类为攻击 10/10、生物 24/24、魔物 1/1、咒语 18/18、装备 8/14、结界 7/24。新增放行 `1808` 公牛耐力、`1816` 身心俱疲、`1914` 巨熊力量、`1916` 体肤重生与 `1917` 犀牛兽皮：五张牌可作为已展示附属结界施放到合法对象，并持续影响有效生命、迟缓、近战骰、重生与护甲。此前 `3704` 奥秘法杖、`3706` 阿希拉法杖、`3409` 结界窃取、`3419` / `3606` 驱散、`3401` 炎爆、`3406` / `3605` 瓦解、`3703` 龙鳞锁甲、`3708` 风龙皮甲、`3709` 元素斗篷与 `3711` 巨熊皮甲已放行。当前不代表 `1813` / `1911` 神力加护、`1913` 圣佑领地、强制防御结界、隐藏结界施放、展示/反制、庇护、区域结界、法师附属结界、完整视线/墙体、完整结界 UI、`3701` 狱火长鞭远触、`3710` 群兽法杖特殊能力、防御护腕、法术绑定、反伤屏障、装备栏或职业限制已闭合。上方 2026-08-02 流水保留当时切片状态，最新统计以本段和下方“显性附属持续结界切片补充”“武器装备攻击切片补充”“结界窃取显性结界转移切片补充”“驱散显性结界切片补充”“炎爆装备爆炸切片补充”“瓦解同名候选切片补充”“被动护甲 / 元素抗性装备切片补充”为准。

> 2026-08-04 阶段性历史状态（`1910` / `1913` 前）：`structured-combat-traits-covered / structured-enchantment-defense-covered / structured-reach-attack-covered / structured-counterstrike-enchantment-covered`。当时配置包统计为 91 张学徒法术中 72 张 implemented、19 张 needsCode；该段记录保留当时的装备、结界和区域缺口，不代表当前快照。

> 2026-08-04 `1910` 鲜血贪噬已完成结构化迁移：配置包统计为 91 张学徒法术中 73 张 implemented、18 张 needsCode，结界 11/24。`1910` 作为可见附属结界授予近战吸血，治疗按实际伤害累计且多段攻击只生成一个治疗事件；远程攻击不吸血，展示文案移除后仍由配置语义生效，持续附属不在攻击后清除。`3404` 汲血之击仍保持一次性临时吸血 / 穿刺行为。

## 2026-08-04 结构化庇护切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书 page 042 与字段合同锁定 `1813` / `1911` 神力加护为可见附属结界，目标为活体生物，授予庇护 1；多个庇护不叠加，只取最高值，攻击骰最低保留 1，伤害类型免疫仍走不投骰分支。 |
| 已覆盖 | 配置包以 `semantics.grants: aegis=1` 标记两张同效牌并设为 implemented；对象攻击、普通攻击法术和连锁闪电在掷骰前读取目标对象附属来源的最高庇护值。移除或改写附属结界展示文案不影响结算。 |
| 规则 owner | `mage-wars.config.json` 保存静态特性，`spellRules.ts` 汇总目标对象附属来源，`execute.ts` 与 `spellAbilityExecutors.ts` 消费同一减骰结果；不新增第二套最低骰数或文本解析规则。 |
| 边界 | `1913` 圣佑领地已由独立区域庇护切片覆盖；当前 `DECLARE_ATTACK` 只有法师目标，没有法师攻击生物入口，因此不扩展新命令。法师绑定、隐藏结界、展示 / 反制和完整结界 UI 仍不在范围内。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；覆盖 `1813` / `1911` / `1913` 配置、区域与对象来源取最高、对象攻击、普通攻击法术、连锁闪电、展示文案移除、最低 1 骰和免疫边界。 |

## 2026-08-04 结构化区域庇护切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书 page 042 与字段合同锁定 `1913` 圣佑领地为区域结界：本区域友方活体生物获得庇护 1；区域来源不影响敌方生物、法师或非活体对象。 |
| 已覆盖 | `1913` 由配置包声明 `visible-area-enchantment`、公开 `zone` 锚点和 `grants: aegis=1`，合法施放生成公开区域锚定结界；对象攻击和攻击法术复用统一庇护查询。 |
| 规则 owner | `mage-wars.config.json` 保存区域语义，`spellAbilityExecutors.ts` 生成区域关系，`spellRules.ts` 按目标当前区域和控制者收集区域来源，与对象附属来源合并后只取最高值；不从玩家可见文本解析。 |
| 边界 | 生物离开区域后失效；不新增法师攻击生物入口，不实现墙体 / 标准 12 区、隐藏结界、展示 / 反制、法师绑定或完整区域结界 UI。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；覆盖区域锚点、友方生物、敌方与区域外生物、攻击法术、展示文案移除、区域与对象来源取最高。 |

## 2026-08-04 结构化死亡印记切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `1826` 死亡印记为费用 `2+2`、可见附属结界、目标为生物；所有生物攻击本生物时获得攻击骰+1，但每个生物每回合仅首次攻击获得。法师绑定+2 仍未闭合。 |
| 已覆盖 | `1826` 由配置包 `data.semantics` 声明 `visible-object-enchantment` 与 `grants: death-mark=1`，合法施放生成公开附属结界；对象生物攻击附着目标时按来源结界、攻击者和当前回合判定首攻加骰；同一攻击的三连击每段均使用 +1，来源只在该攻击者本回合首次攻击时记录一次。移除展示文案不影响规则。 |
| 规则 owner | 首攻资格由附属结界来源查询和结构化回合消费状态驱动，对象攻击事件记录来源与回合，reducer 回放时记录已用攻击者；不从玩家可见规则文本读取，不新增第二套多段攻击系统。 |
| 边界 | 法师绑定+2、法师攻击、攻击法术、`1904` 攻击逆转、`1912` 心灵安抚、隐藏结界、展示 / 反制和完整结界 UI 不在本切片。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；领域流程覆盖移除展示文案后仍生效、同一生物首攻 / 后续攻击、不同生物首攻和三连击三段均加骰且来源只消费一次。 |

## 2026-08-04 结构化格挡切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同锁定 `1806` 格挡为费用 `2+2`、可见附属结界、目标为实体生物；攻击可回避时必须展示并视为一次防御，攻击不可回避时销毁但不产生回避效果。 |
| 已覆盖 | `1806` 由配置包 `combatProfiles.defenses` 声明 `automatic-evade` 与 `consumesSource`，合法施放生成公开附属结界；可回避对象攻击进入防御窗口时只暴露格挡、不提供跳过，直接产生 `ATTACK_MISSED` 并销毁来源；不可回避攻击跳过防御窗口但先销毁格挡再继续伤害。移除展示文案不影响规则。 |
| 规则 owner | 附属防御 profile 从配置读取并携带来源对象身份；交互系统负责强制选项，执行器负责自动回避和不可回避销毁，来源移除复用 `ARENA_OBJECT_DEFEATED`，不掷防御骰、不新增结界清理模型。 |
| 边界 | `1904` 攻击逆转、`1912` 心灵安抚、`3715` 偏移护腕、法师防御、隐藏结界、通用反制优先级和完整结界 UI 不在本切片。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts`、`domain-flow.test.ts`；领域流程覆盖自动回避、无防御骰、强制无跳过选项、展示文案移除、来源销毁和不可回避攻击继续结算。OpenSpec change 已严格校验。 |

## 2026-08-02 伤害类型 +/- 修正切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_026.md` 明确攻击修正会改变攻击骰数量，也会修正效果骰结果，且除非目标免疫该伤害类型，攻击骰最低 1 颗；`page_028.md` 明确 `闪电+2` / `火焰-2` 等伤害修正会同时修正攻击骰与效果骰，并说明免疫会导致完全不投攻击骰或效果骰；`page_044.md` 明确“伤害类型+/-X”可累加并修正攻击骰和效果骰。 |
| 已覆盖 | 法术攻击、`连锁闪电` 每跳攻击和场上对象攻击都会从攻击 profile 解析伤害类型，并从目标特性文本中汇总匹配的 `火焰-2`、`闪电+2` 等修正；运行时用修正后的攻击骰数量掷骰，最低保留 1 颗；效果骰事件同时记录原始效果骰和修正后效果骰，状态阈值按修正后结果结算。 |
| 边界 | 本切片只负责 `火焰-2` / `闪电+2` 等 +/- 修正；伤害类型免疫已由下方独立切片覆盖，不能和 +/- 修正混成同一规则分支。 |
| 验证证据 | `domain-flow.test.ts` 覆盖 `闪电箭矢` 命中 `闪电+2` 目标时 5 骰变 7 骰、效果骰 6 修为 8 并落昏迷；`烈焰爆弹` 命中 `火焰-2` 目标时 4 骰变 2 骰、效果骰 11 修为 9 并降低燃烧层数；`火烙魔婴` 对象攻击命中 `火焰-2` 目标时 2 骰降到最低 1 骰、效果骰 9 修为 7 且不落燃烧。 |

## 2026-08-02 伤害类型免疫切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_026.md` 明确攻击修正最低 1 骰的例外是目标免疫该伤害类型；`page_028.md` 明确免疫会导致完全不受攻击影响、不投攻击骰或效果骰；`page_044.md` 明确“免疫”会让对象免疫特定伤害类型攻击、伤害、状态与效果，且特定类型法术不能将其作为目标或产生影响。 |
| 已覆盖 | 目标型攻击法术会在校验层拒绝匹配伤害类型免疫的对象目标；区域攻击法术命中免疫对象时，对该对象跳过攻击骰、效果骰、伤害和攻击附带状态，仍正常结算区域内其它非免疫对象；场上对象攻击命中免疫目标时仍消耗攻击者行动标记并记录免疫导致的 `ATTACK_MISSED`，但不投攻击骰或效果骰、不造成伤害、不放置攻击附带状态，也不误报为眩晕失手或防御回避。 |
| 仍未覆盖 | 直接伤害的伤害类型免疫、非攻击型特定伤害类型法术目标免疫、装备 / 结界动态赋予免疫、UI 骰盘 / 动作日志更细展示仍属后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖 `闪电箭矢` 被 `闪电免疫` 目标校验拒绝；`雷导术` 区域攻击跳过 `闪电免疫` 对象且继续伤害其它目标；`火烙魔婴` 对象攻击命中 `火焰免疫` 目标时消耗行动、记录免疫 miss、目标不受伤且不落燃烧。 |

## 2026-08-02 守卫反击机会切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_029.md` / `page_030.md` 明确反击发生在近战攻击后的反击步骤，是防御方自愿行为；`page_031.md` 明确守卫中生物所有快速近战攻击获得反击特性，守卫被近战攻击目标选中后在反击步骤结束移除守卫；`page_045.md` 明确瘫痪生物不能反击。 |
| 已覆盖 | 生物近战攻击选中守卫且守卫仍存活、未瘫痪、具备快速近战攻击时，运行时发出 `MW_COUNTERSTRIKE_AVAILABLE` 机会事件，并在 `MW_GUARD_REMOVED` 前进入动作记录；该机会会进入防御方 `sys.interaction` 简单选择；防御方可放弃，选择反击时按守卫快速近战攻击结算，不消耗守卫对象行动标记，且不递归暴露新的反击机会；眩晕导致攻击失手时仍会暴露该反击机会；远程攻击和瘫痪守卫不会暴露反击机会。 |
| 仍未覆盖 | 强制防御结界、反伤屏障、多个响应同时出现的优先级、横扫 / 飞行 / 遁逸等攻击特例仍需后续攻击流程切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖守卫近战被攻击时生成反击机会、机会早于守卫移除、不会自动替守卫攻击、可选择放弃、选择反击时产生防御方快速近战伤害且不消耗守卫行动、不递归反击、眩晕失手仍生成机会，以及远程攻击 / 瘫痪守卫不生成机会。 |

## 2026-08-02 对象防御响应窗口切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_026.md` 明确防御是被攻击时可选择使用的回避能力，成功后攻击完全回避且不投攻击骰；同页明确 `1x` 防御每回合仅能使用一次，使用就绪标记记录是否已用，并举例说明使用后直到下一个重置阶段不能再次使用。`page_027.md` 明确回避攻击后跳到额外打击步骤。`page_043.md` 明确重置阶段将就绪标记翻回就绪面。`page_045.md` 明确瘫痪不能防御，束缚防御掷骰 -2。 |
| 已覆盖 | 对象攻击目标具有可用防御 profile 且攻击不是无法回避时，运行时发出 `MW_DEFENSE_AVAILABLE` 并在攻击骰和伤害前进入防御方 `sys.interaction`；防御方可放弃，放弃后继续原攻击且不重复消耗攻击者行动；选择防御时掷效果骰，成功产生 `ATTACK_MISSED` 且不投攻击骰、不造成伤害，失败则继续原攻击；使用 `1x` 防御后记录本回合已用，后续攻击不再暴露该防御，直接防御命令也拒绝已冷却 profile；防御方进入下一次重置阶段后清空防御使用记录；无法回避对象攻击不暴露防御选择。 |
| 仍未覆盖 | 多个防御来源的合法性排序、强制防御结界、反伤屏障、反击与防御成功后的更细联动、横扫 / 飞行 / 遁逸等攻击特例仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖防御窗口先于攻击骰、可放弃继续、成功回避、失败继续、防御用后冷却、重置恢复、无法回避攻击跳过防御，以及眩晕 / 残废 / 昏迷对防御骰或防御禁用的既有状态事实。 |

## 2026-08-02 传送咒语切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `3410` 传送：目标生物被传送至目标区域；X 费用按目标生物当前区域到目标区域距离计算，每格 3 点法力，至少 3 点法力；本效果不计为生物的移动行动。 |
| 已覆盖 | `3410` 已由配置包标为 implemented；校验层要求目标为场上生物对象、显式目标区域、目标生物与目标区域均在法术射程内，并按目标生物当前区域到目标区域的区域距离计算 X 费用；执行层新增 `SPELL_TELEPORT_RESOLVED`，移动目标生物且不消耗目标生物行动标记，并接入行动日志与独立传送 FX。 |
| 仍未覆盖 | 视线、墙体、完整 12 区竞技场、其它传送相关反应窗口仍属后续切片。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 3410 implemented 计数；`domain-flow.test.ts` 覆盖传送成功移动目标、缺目标区域拒绝、不能以法师为目标、不能以魔物为目标、费用不匹配拒绝和目标 / 目标区域超距拒绝；`event-fx-mapper.test.ts` 覆盖传送事件映射到独立传送 FX。 |
## 2026-08-02 昏睡咒语切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `3411` 昏睡：目标为非法师活体生物，放置 1 枚沉睡状态标记；具有精神免疫特性的生物无效；X 费用按目标生物等级计算。 |
| 已覆盖 | `3411` 已由配置包标为 implemented；校验层要求目标为场上生物对象、活体、非精神免疫、在法术射程内，并按目标生物配置等级计算 X 费用：1 级 4、2 级 5、3 级 6、4 级起每级 +2；执行层复用 `STATUS_TOKEN_PLACED`，向目标对象放置 1 枚沉睡 token，并消费快速施法标记 / 已计划法术。 |
| 仍未覆盖 | 沉睡 UI 提示、精神免疫来源更复杂的装备 / 结界叠加和其它精神类咒语仍属后续切片。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 3411 implemented 计数；`domain-flow.test.ts` 覆盖昏睡成功放置沉睡 token、目标等级费用不匹配拒绝、不能以法师为目标、不能以非活体或精神免疫生物为目标。 |

## 2026-08-02 原力推斥咒语切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md`（历史卡牌字段）已锁定 `3425` / `3523` 原力推斥；当前运行空间是完整标准 4x3，墙体通行伤害仍属后续规则切片。 |
| 已覆盖 | `3425` / `3523` 已由配置包标为 implemented；校验层要求目标为生物对象、显式提供相邻目标区域且在法术射程内；执行层复用 `SPELL_PUSH_RESOLVED`，目标生物被移动到所选相邻区域，并消费快速施法标记 / 已计划法术。 |
| 仍未覆盖 | 标准 12 区墙体、穿越具有通行伤害墙体时额外支付 3 点法力，以及完整墙体伤害链仍属后续墙体切片。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 3425 / 3523 implemented 计数；`domain-flow.test.ts` 覆盖原力推斥移动目标生物、缺少目标区域拒绝、非相邻目标区域拒绝和不能以法师为目标。 |

## 2026-08-02 普通生物召唤切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2819` 丛林灰狼与 `2826` 骷髅哨兵：二者均为无额外规则正文的普通生物；骷髅哨兵为生物 / 亡灵、骷髅、士兵，黑暗 2 级，费用 8，生命 11，护甲 0，攻击条为“短剑：快速近战 4 骰；非活体；精神免疫”。 |
| 已覆盖 | `2819` / `2826` 已由配置包标为 implemented；现有生物召唤执行器按配置字段构造场上生物对象，保留名称、来源 CardID / objectId、生命、护甲、攻击条、目标区域，以及骷髅哨兵的非活体 / 精神免疫特性；召唤消耗施法者行动轨，不消耗被召唤生物未来行动标记。 |
| 仍未覆盖 | 其它生物牌中带治疗行动、装备法力流失、法力传输、元素免疫、飞行、迅捷、迟缓、重生、嗜血、遁逸、传奇、特殊激活能力等效果的完整裁定仍需逐卡切片；本切片只迁出无额外规则正文且当前 parser 已覆盖攻击条 / 特性文本的丛林灰狼与骷髅哨兵。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 2819 / 2826 implemented 计数；`domain-flow.test.ts` 覆盖丛林灰狼按配置召唤为场上对象，并覆盖骷髅哨兵按配置召唤后保留生命、护甲、来源、攻击条、非活体 / 精神免疫特性，且会被昏睡目标校验拒绝。 |

## 2026-08-02 配置生物攻击效果切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2808` 翠绿树蜥：费用 9，生命 8，护甲 3，攻击条为“剧毒噬咬：快速近战 3 骰，效果骰 `8+=腐化”，无额外规则正文。 |
| 已覆盖 | `2808` 已由配置包标为 implemented；生物召唤执行器按配置字段构造翠绿树蜥场上对象；对象攻击 profile 从配置攻击条解析 `attack-0`，在效果骰达到 8 时通过 `STATUS_TOKEN_PLACED` 给目标活体对象放置 1 枚腐化 token，并保留 `mw.object.2808.attack-0` 作为来源能力 ID。 |
| 仍未覆盖 | 结界、装备、其它咒语和未单独消费的对象特殊规则仍必须保持 `requiresCodeSupport=true`；雷隙猎鹰、暗沼蝙蝠、苦木林狐、蓝色精怪、深林幽影切维尔和缠绕藤蔓均已由独立切片覆盖各自当前字段，但不能因为基础召唤、魔物召唤或攻击骰可解析就批量放行其它牌。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 2808 implemented 计数；`domain-flow.test.ts` 覆盖翠绿树蜥按配置召唤为场上对象，并覆盖其攻击命中活体对象后按效果骰放置腐化 token。 |

## 2026-08-02 被动生物配置放行切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2800` 暗契屠魔与 `2909` 西锁骑士：二者正文区没有额外可执行规则；暗契屠魔保留生命 14、护甲 2、狱火剑快速近战 4 骰、穿刺+2、火焰-2；西锁骑士保留生命 10、护甲 3、长剑快速近战 5 骰、防御图标 `8+ / 1x`、闪电+2。 |
| 已覆盖 | `2800` / `2909` 已由配置包标为 implemented；生物召唤执行器按配置字段构造场上对象，现有战斗管线已能消费护甲、防御 profile、穿刺和伤害类型 +/- 修正，因此二者不再需要额外代码支持。 |
| 仍未覆盖 | 该切片只放行没有额外动作模型的被动生物；飞行、迅捷、迟缓、治疗行动、装备法力流失、法力传输、重生、嗜血、遁逸、传奇、墙体 / 12 区完整移动等规则仍按逐卡切片处理，不能因本切片批量放行其它生物。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 2800 / 2909 implemented 计数；`domain-flow.test.ts` 覆盖两张生物按配置召唤后保留名称、生命、护甲、来源和攻击 / 防御 / 抗性文本。 |

## 2026-08-02 蓝色精怪激活移动切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2822` 蓝色精怪：激活时可支付 1 点法力；若支付，则蓝色精怪获得迅捷直到本回合结束，并且本回合每次执行移动行动时以传送方式进入下一格区域。规则书 `规则.txt` 说明迅捷与迟缓互相抵消，传送是直接移动到另一格区域并绕过墙体与对象。 |
| 已覆盖 | `2822` 已由配置包标为 implemented；运行时新增场上对象能力命令，只允许 ready 的蓝色精怪在己方生物行动阶段支付 1 法力获得临时 `swift` / `teleportMovement`；第一次相邻移动记录为 `movementMode=teleport` 且 `actionCost=none`，第二次移动仍消耗行动标记；离开生物行动阶段时清理该临时能力。 |
| 仍未覆盖 | 当前标准 4x3 竞技场尚无完整墙体 / 阻挡对象模型，因此本切片只记录“传送式移动”的事件事实，并不放宽相邻一格校验；迟缓与迅捷抵消、墙体穿越、飞行与其它移动特性仍待后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖蓝色精怪按配置召唤后付费激活、首次传送移动不耗行动、第二次移动耗行动、行动阶段结束清理临时能力，以及非蓝色精怪 / 费用不匹配 / 已激活重复使用的拒绝路径；`ability-catalog.test.ts` 覆盖 2822 implemented 计数。 |

## 2026-08-02 迟缓移动切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `101721 法师战争 Mage Wars 规则.md` 的移动规则说明普通生物可用快速行动进行第二次移动；常见问题与勘误说明具有迅捷的生物也会受阻碍移动限制；`规则.txt` 与蓝色精怪切片已确认迅捷与迟缓互相抵消。 |
| 已覆盖 | 运行时新增迟缓特性识别：当场上对象同时拥有迟缓与临时迅捷 / 传送式移动事实时，迟缓会取消迅捷带来的免费移动，移动按普通行动成本消耗对象行动标记；若同一效果仍提供传送式移动，事件继续记录 `movementMode=teleport`。 |
| 仍未覆盖 | 当前基础行动模型仍未完整还原“移动后快速行动 / 第二次移动”的正式节奏，阻碍、飞行、遁逸、墙体和标准 12 区移动仍是后续切片；本切片只锁定迟缓对现有迅捷免费移动 owner 的抵消。 |
| 验证证据 | `domain-flow.test.ts` 覆盖带迟缓的蓝色精怪激活后移动仍记录为传送，但 `actionCost=normal` 并消耗对象行动标记；`ability-catalog.test.ts` 覆盖 2809 / 2810 / 2901 implemented 计数；`domain-flow.test.ts` 还覆盖三张慢速生物按配置召唤后保留攻击、反击、三连击、重生、虚弱 / 残废效果骰和迟缓文本。 |

## 2026-08-02 印刷迅捷移动切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2812` 苦木林狐：费用 5，生命 5，攻击条为 `噬咬：快速近战 3 骰；迅捷`，无额外规则正文；移动规则与迟缓切片已确认迅捷免费移动会被迟缓抵消。 |
| 已覆盖 | `2812` 已由配置包标为 implemented；运行时新增印刷迅捷识别，复用已有免费移动 owner：首次普通相邻移动记录 `actionCost=none` 且保留行动标记，第二次移动按普通行动成本消耗对象行动标记，行动阶段结束清理本行动免费移动标记。 |
| 仍未覆盖 | 本切片只消费无飞行、无遁逸、无特殊行动的印刷迅捷；`2820` 雷隙猎鹰的飞行 + 迅捷、`2906` 野性山猫和 `2814` 高地独角兽的冲锋均已由独立切片覆盖；`2824` 深林幽影切维尔的迅捷 + 遁逸 + 传奇和起始区阻碍边界已由独立切片覆盖，但目标区阻碍后的完整第二次移动节奏、墙体和飞行阻碍仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖苦木林狐按配置召唤、首次移动不耗行动、第二次移动耗行动、阶段结束清理 `swiftFreeMove` 标记；`ability-catalog.test.ts` 覆盖 2812 implemented 计数。 |

## 2026-08-02 冲锋特性切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书 `page_042.md` 锁定 `冲锋+X`：生物移动并至少移动 1 格区域后立即进行攻击时，下一次近战攻击获得 +X 颗攻击骰；FAQ `page_009.md` 明确冲锋只修正攻击骰，不修正效果骰。 |
| 已覆盖 | `2906` 野性山猫已由配置包标为 implemented；运行时新增通用冲锋解析和移动后快速行动窗口，普通相邻移动后允许该对象立即使用快速近战攻击，攻击骰按 `冲锋+2` 增加，效果骰保留原始结果，不被冲锋修正。 |
| 边界 | 传送式移动不触发冲锋；攻击后或行动阶段结束会清理移动后快速行动窗口；本切片只覆盖印刷 `冲锋+X`，`3407` 冲锋陷阵已由下方独立切片覆盖，飞行 / 遁逸 / 阻碍 / 墙体等移动特例仍需各自切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖野性山猫普通移动后立即快速近战，攻击骰从 2 增至 4，效果骰保持 3，攻击后清理 `movedThisAction` / `quickActionAfterMoveAvailable`；`ability-catalog.test.ts` 覆盖 `2906` implemented 计数。 |

## 2026-08-02 高地独角兽重生 / 冲锋组合切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2814` 高地独角兽：攻击条为 `特角：快速近战 3 骰；重生2；冲锋+2`，规则正文为“所有与高地独角兽位于同一格区域的友方活体生物获得重生1特性”。 |
| 已覆盖 | `2814` 已由配置包标为 implemented；现有召唤管线保留生命、护甲、攻击条与正文；维持阶段重生 owner 已能消费自身 `重生2` 与同区友方活体 `重生1` 光环，并按最高重生值结算；冲锋 owner 已能消费其普通移动后立即快速近战的 `冲锋+2` 攻击骰。 |
| 边界 | 本切片只放行高地独角兽当前印刷字段；`3407` 冲锋陷阵已由下方独立切片覆盖，飞行、遁逸、阻碍、墙体、其它光环、结界赋予重生和 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖高地独角兽与同区友方活体在维持阶段分别按 `重生2` / `重生1` 恢复且不叠加更低重生值；冲锋通用测试覆盖 `冲锋+2` 普通移动后快速近战；`ability-catalog.test.ts` 覆盖 `2814` implemented 计数。 |

## 2026-08-02 冲锋陷阵律令咒语切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3407` 冲锋陷阵：快速咒语 / 律令，费用 4，射程 `0-2`，目标为实体生物，效果为“目标获得迅捷和冲锋+1特性直到本回合结束”。 |
| 已覆盖 | `3407` 已由配置包标为 implemented；校验层要求目标为范围内实体生物对象；执行层给目标对象写入本行动/本回合临时 `swift` 与 `chargeDiceModifier=1`，普通移动后快速近战按额外 1 颗攻击骰结算，效果骰不被修正。 |
| 边界 | 本切片只覆盖冲锋陷阵赋予的临时迅捷与冲锋+1；飞行、遁逸、阻碍、墙体、其它律令咒语、临时特性的 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖冲锋陷阵施放、临时迅捷 / 冲锋获得、普通移动后快速近战攻击骰 +1、效果骰不修正和阶段结束清理；`ability-catalog.test.ts` 覆盖 `3407` implemented 计数。 |

## 2026-08-02 荒野呼唤友方动物近战 +1 切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3417` 荒野呼唤：快速咒语 / 自然，费用 4，射程 `0-0`，目标为竞技场，效果为“竞技场中所有的友方动物生物获得近战+1特性直到本回合结束”。 |
| 已覆盖 | `3417` 已由配置包标为 implemented；施放时不要求单体目标，执行层只给竞技场中友方动物生物写入临时 `meleeDiceModifier=1`；对象近战攻击会增加 1 颗攻击骰，远程攻击不受影响。 |
| 边界 | 本切片只覆盖荒野呼唤赋予的友方动物近战 +1 和阶段结束清理；其它临时战斗咒语、结界 / 装备赋予近战、阻碍、墙体和 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖友方动物获得临时近战 +1、友方非动物与敌方动物不获得、近战攻击 3 骰变 4 骰、远程攻击保持 2 骰，以及生物行动阶段结束清理 `meleeDice`；`ability-catalog.test.ts` 覆盖 `3417` implemented 计数。 |

## 2026-08-02 汲血之击吸血 / 穿刺切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3404` 汲血之击：快速咒语 / 吸血，费用 3，射程 `0-2`，目标为活体生物，效果为“本回合中目标的下一次近战攻击获得吸血和穿刺+1特性”。 |
| 已覆盖 | `3404` 已由配置包标为 implemented；校验层要求目标为范围内活体生物对象；执行层给目标对象写入临时 `vampiricNextMelee` 与 `nextMeleePierceModifier=1`；对象近战攻击会把临时穿刺计入护甲抵消，并按实际伤害治疗攻击者控制方法师。 |
| 边界 | 本切片只覆盖汲血之击赋予的下一次近战吸血 / 穿刺+1 和阶段结束清理；远程攻击不触发也不清理；其它吸血来源、装备武器、结界赋予攻击特性、响应窗口和 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖汲血之击施放、近战攻击穿刺+1 抵消护甲、按实际伤害治疗攻击者控制方法师、近战攻击后清理临时事实、远程攻击不触发且阶段结束清理；`ability-catalog.test.ts` 覆盖 `3404` implemented 计数。 |

## 2026-08-02 狼人宠物戈伦嗜血切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2804` 狼人宠物戈伦：生物 / 狼人、犬科，费用 15，生命 12，护甲 3，攻击条含 `嗜血+1` 与 `两连击`；规则书 `page_044.md` 锁定 `嗜血+X` 只在近战攻击已受伤活体目标时加骰，且多段攻击仅首次获得；卡面正文锁定与控制方法师同区时额外获得 `嗜血+1`。 |
| 已覆盖 | `2804` 已由配置包标为 implemented；生物召唤执行器按配置字段构造戈伦场上对象；对象攻击会解析基础 `嗜血+1`，同区控制方法师时追加 1 点嗜血修正；只对已受伤活体目标生效，非活体或未受伤目标不加骰；`两连击` 被解析为两段攻击，嗜血只修正第一段。 |
| 边界 | 本切片只覆盖嗜血加骰与两连击解析；嗜血强制攻击目标选择、嘲讽冲突、抑制斗篷等装备额外费用、遁逸、墙体、阻碍和 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖戈伦按配置召唤、同区控制方法师 + 已受伤活体目标时 4 骰变 6 骰、不同区只获得基础 1 骰、未受伤 / 非活体目标不加骰，以及完整行动 `两连击` 仅第一段获得嗜血加骰；`ability-catalog.test.ts` 覆盖 `2804` implemented 计数。 |

## 2026-08-02 深林幽影切维尔迅捷 / 遁逸 / 传奇切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2824` 深林幽影切维尔：生物 / 树妖，费用 14，生命 11，护甲 2，攻击条含快速近战 4 骰、防御 `8+ / 1x`、迅捷、遁逸与传奇；规则书 `page_042.md` 锁定传奇同名唯一与遁逸忽略敌方生物阻碍，`page_031.md` 锁定遁逸攻击可忽略守卫，但主动攻击守卫时守卫仍可反击。 |
| 已覆盖 | `2824` 已由配置包标为 implemented；生物召唤执行器按配置字段构造切维尔场上对象，并保留生命、护甲、防御 profile、迅捷、遁逸、传奇和攻击条；施法校验会在同名传奇对象仍在场时拒绝再次召唤，原对象离场后允许后续召唤；遁逸对象近战攻击敌方法师或非守卫对象时可忽略同区敌方守卫拦截，但如果主动攻击守卫，仍暴露守卫反击机会；遁逸对象起始区有敌方生物时仍可消费印刷迅捷的首次免费移动，非遁逸迅捷对象在同样阻碍下移动会消耗行动。 |
| 边界 | 本切片只覆盖传奇唯一、守卫拦截、守卫反击和起始区阻碍对现有迅捷免费移动 owner 的影响；目标区阻碍后的“禁止第二次移动但保留快速行动窗口”、墙体、飞行阻碍、完整 12 区移动和 UI 骰盘展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖切维尔按配置召唤后保留生命、护甲、防御 profile、迅捷、遁逸、传奇和攻击条；覆盖同名传奇在场拒绝再次召唤、离场后允许；覆盖遁逸忽略守卫拦截、主动攻击守卫仍暴露反击机会；覆盖遁逸迅捷在起始区被敌方生物阻碍时仍免费移动，而非遁逸迅捷在同样阻碍下移动消耗行动；`ability-catalog.test.ts` 覆盖 `2824` implemented 计数。 |

## 2026-08-02 缠绕藤蔓魔物 / 束缚切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2224` 缠绕藤蔓：魔物 / 植物、藤蔓，费用 5，生命 8，护甲 0，目标为实体生物；目标被束缚并获得稳固，不能以飞行或不羁生物为目标，远程攻击不能以缠绕藤蔓为目标。规则书 `page_018.md` 锁定对象型魔物附着、同名附着限制和目标移动 / 传送时魔物摧毁；`page_045.md` 锁定束缚、推斥和无法移动边界。 |
| 已覆盖 | `2224` 已由配置包标为 implemented；施放会生成锚定目标生物的魔物对象，保留生命 8、护甲 0、植物 / 藤蔓、活体、火焰+2、水流免疫和卡面规则文本；目标写入 `restrainedByObjectId`，普通移动被拒绝；因缠绕藤蔓获得稳固 / 不可移动的目标不能被 `原力推斥` 推走；飞行、不羁、法师、非生物和魔物目标被拒绝；同一目标已附着同名魔物时拒绝重复施放；远程对象攻击不能攻击缠绕藤蔓，近战攻击仍允许；目标或藤蔓离场、目标移动 / 传送时清理附着和束缚关系。 |
| 边界 | 本切片只覆盖 `2224` 对象型魔物提供的束缚 / 稳固事实；不代表结界版束缚、其它魔物、墙体通行伤害、完整 12 区推斥方向随机、飞行完整移动、UI 骰盘或行动日志细节已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 46 implemented / 45 needsCode、魔物 1/1 implemented 和 `2224` implemented；`domain-flow.test.ts` 覆盖合法施放生成 `ARENA_OBJECT_SUMMONED` 与 `ARENA_OBJECT_RESTRAINED`、目标记录 `restrainedByObjectId`、普通移动拒绝、原力推斥拒绝不可移动目标、飞行 / 不羁 / 法师 / 非生物 / 魔物目标拒绝、同名重复附着拒绝、远程攻击藤蔓拒绝和近战攻击藤蔓允许。 |

## 2026-08-02 兽性觉醒咒语 / 本回合召唤行动恢复切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3403` 兽性觉醒：费用 `X`，咒语 / 自然，等级 1，射程 `0-2`，目标为活体生物；规则文本要求选择本回合被召唤的生物，将其行动标记翻回活动面，同一生物每回合只能被本法术影响一次，X 等于目标等级。 |
| 已覆盖 | `3403` 已由配置包标为 implemented；生物 / 魔物召唤对象记录 `summonedTurnNumber`；兽性觉醒只允许指定本回合被召唤的活体生物，按目标来源卡等级校验 X 费用，合法施放发出 `ARENA_OBJECT_ROUSED`，把目标 `actionReady` 设回 `true` 并记录 `rousedBySpellTurnNumber`；同一目标同一回合重复施放被拒绝，新回合开始时清理觉醒标记。 |
| 边界 | 本切片只覆盖 `3403` 对“本回合召唤的活体生物”的行动标记恢复；不代表结界、装备、非召唤当回合行动恢复、完整 UI 骰盘、行动日志细节或其它临时行动恢复效果已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 47 implemented / 44 needsCode、咒语 12/18 implemented 和 `3403` implemented；`domain-flow.test.ts` 覆盖先召唤野性山猫再施放兽性觉醒、目标行动标记翻回活动面、X 费用按目标等级、旧召唤目标 / 非活体 / 魔物目标拒绝、同回合重复目标拒绝、法力不足拒绝和新回合清理 `rousedBySpellTurnNumber`。 |

## 2026-08-02 瓦解咒语 / 附属装备摧毁切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3605` 瓦解为新手法术书实例使用版本：费用 `X`，快速咒语 / 酸性，等级 1，射程 `0-1`，目标为法师；规则文本要求选择一个附属在目标法师上的装备对象并摧毁，X 等于该装备的施法费用。 |
| 已覆盖 | `3605` 已由配置包标为 implemented；运行时允许场上对象以 `kind=equipment` 表示已附属装备；瓦解只允许指定附属在法师上的装备对象，按该装备来源卡施法费用校验 X 费用，合法施放发出 `ARENA_OBJECT_DEFEATED` 并移除装备对象。 |
| 边界 | 本切片只覆盖“已有装备对象被瓦解摧毁”的规则；不代表装备施放、装备属性加成、装备栏限制、炎爆、驱散、结界窃取、完整装备 UI 或装备随法师移动的表现细节已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 48 implemented / 43 needsCode、咒语 13/18 implemented 和 `3605` implemented；`domain-flow.test.ts` 覆盖合法摧毁附属装备、费用不匹配、未附属装备、非装备对象、法力不足和距离外目标拒绝。 |

## 2026-08-02 基础护甲装备切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3702` 皮革手套与 `3721` 皮革长靴：二者均为快速装备，费用 2，目标为法师，文本为“法师获得护甲+1”。规则书 `page_022.md` 明确装备附属到法师并公开可检视。 |
| 已覆盖 | `3702` / `3721` 已由配置包标为 implemented；运行时只允许基础护甲装备自施放到法师，生成 `kind=equipment` 的公开附属对象，并从装备规则文本派生法师装备护甲减伤；法师移动时，附属装备对象跟随进入新区域。 |
| 边界 | 本切片只覆盖两张基础护甲装备的自施放、附属、移动跟随与护甲减伤；不代表装备栏限制、职业限定、武器攻击、防御、反伤屏障、法术绑定、完整装备 UI 或其它装备已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 在该切片完成时覆盖 50 implemented / 41 needsCode、装备 2/14 implemented 和 `3702` / `3721` implemented；`domain-flow.test.ts` 覆盖皮革手套自施放生成附属装备、装备随法师移动、装备护甲进入 `DAMAGE_DEALT` breakdown、错误目标拒绝。当时 `3703` 龙鳞锁甲仍保持未实现，后续已由 2026-08-03 被动护甲 / 元素抗性装备切片放行。 |

## 2026-08-03 被动护甲 / 元素抗性装备切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3703` 龙鳞锁甲、`3708` 风龙皮甲、`3709` 元素斗篷与 `3711` 巨熊皮甲：四者均为快速装备，目标为法师，文本分别提供护甲与 `火焰-2` / `风力-3` / `霜冻-2` / `闪电-2` 等伤害类型修正。规则书 `page_022.md` 明确装备附属到法师并公开可检视。 |
| 已覆盖 | 四张牌已由配置包标为 implemented；运行时复用公开附属装备对象和法师移动跟随；攻击法术与场上对象攻击命中装备法师时，会从附属装备文本汇总护甲和伤害类型修正：匹配伤害类型先修正攻击骰与效果骰，随后装备护甲进入 `DAMAGE_DEALT` breakdown 减伤。 |
| 边界 | 本切片只覆盖被动护甲和元素抗性文本；不代表装备栏限制、职业限定、武器攻击、防御、反伤屏障、法术绑定、完整装备 UI 或其它装备已经闭合。当时 `3704` 奥秘法杖等主动 / 武器型装备仍保持 needsCode；后续 `3704` / `3706` 已由独立武器装备攻击切片放行。 |
| 验证证据 | `ability-catalog.test.ts` 在该切片完成时覆盖 54 implemented / 37 needsCode、装备 6/14 implemented 和 `3702` / `3703` / `3708` / `3709` / `3711` / `3721` implemented；`domain-flow.test.ts` 覆盖龙鳞锁甲自施放生成附属装备、火焰攻击法术命中龙鳞锁甲法师时先按 `火焰-2` 减少攻击骰 / 效果骰再按护甲减伤、元素斗篷法师被闪电对象攻击时同样消费 `闪电-2` 与护甲+1。 |

## 2026-08-03 武器装备攻击切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3704` 奥秘法杖与 `3706` 阿希拉法杖均为快速装备 / 武器，目标为法师；奥秘法杖有快速近战 4 骰法力流失+1，以及完整行动远程 `1-1` 3 骰法力流失+1；阿希拉法杖有快速近战圣光 4 骰，对非活体生物+2，效果骰 5-10 放置眩晕、11+ 放置昏迷。 |
| 已覆盖 | `3704` / `3706` 已由配置包标为 implemented；运行时允许二者自施放生成公开附属装备对象，并新增装备攻击命令。装备攻击会从装备对象攻击条解析显式 profile，复用对象攻击伤害、法力流失、伤害类型修正、非活体加伤、状态落点、距离和守卫拦截校验，但消耗附属法师的行动标记而不是装备对象行动标记。 |
| 边界 | 本切片只覆盖“法师使用已附属武器装备攻击”。`3701` 狱火长鞭仍因远触规则未闭合保持 needsCode；`3710` 群兽法杖特殊能力、`3715` 偏移护腕防御、`3716` 元素魔杖法术绑定、反伤屏障、装备栏限制、职业限制和完整装备 UI 仍未闭合。 |
| 验证证据 | `ability-catalog.test.ts` 在该切片完成时覆盖 63 implemented / 28 needsCode、装备 8/14 implemented、`3704` / `3706` implemented 和 `3701` needs-code；后续显性附属持续结界切片已将当前 implemented 计数推进到 68 张、结界类推进到 7/24。`domain-flow.test.ts` 覆盖奥秘法杖自施放为附属装备、近战攻击触发法力流失、远程 profile 只能打 1 格目标、装备攻击消耗法师行动、阿希拉法杖对非活体+2并按效果骰放置眩晕 / 昏迷，以及 3701、被动护甲装备、未附属装备和非己方装备攻击拒绝。 |

## 2026-08-04 狱火长鞭远触攻击切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书术语“远触”规定：远触仍是近战攻击，只允许攻击同一区域的飞行生物；`apprentice-card-field-contract.md` 锁定 `3701` 狱火长鞭为快速近战火焰 4 骰，效果骰 7-10 燃烧、11+ 燃烧x2。 |
| 已覆盖 | `3701` 已由配置包标为 implemented；攻击 profile 增加结构化 `reach=true`，并从配置消费火焰伤害与两段燃烧阈值。普通近战不能攻击飞行目标，远触近战可以攻击同区飞行目标；装备攻击仍复用近战的守卫、反伤屏障、反击和行动消耗路径。 |
| 边界 | 本切片不实现除霜、法力传输、装备栏、职业限制、反伤屏障、`3710` 群兽法杖或其它装备能力；远触不扩大攻击区域距离，也不变成远程攻击。 |
| 验证证据 | `combat-profile.test.ts` 覆盖配置 profile、移除展示攻击文本后的远触和燃烧阈值、普通近战 / 远触对飞行目标的合法性；`domain-flow.test.ts` 覆盖 `3701` 自施放、同区飞行目标攻击、4 骰火焰伤害和 11+ 燃烧x2；`ability-catalog.test.ts` 覆盖 71 implemented / 20 needsCode、装备 9/14。 |

## 2026-08-03 显性附属持续结界切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `1808` 公牛耐力、`1816` 身心俱疲、`1914` 巨熊力量、`1916` 体肤重生与 `1917` 犀牛兽皮均为结界，目标为对象生物或活体生物，卡面持续效果分别是生命+4、迟缓、近战+2、重生2和护甲+2。 |
| 已覆盖 | 五张结界已由配置包标为 implemented；施放时按目标规则和施法+展示费用校验，生成 `kind=enchantment` 的公开附属对象并记录锚点。有效生命、有效护甲、近战攻击骰修正、维持阶段重生和迟缓移动成本都会读取来源卡的配置包 `data.semantics`；公开附属结界文本只保留给玩家展示，不作为这五张持续效果的执行真相源。 |
| 边界 | 本切片只覆盖“已经展示且附属在场上对象上的持续 trait 结界”。`1813` / `1911` 神力加护、`1913` 圣佑领地、`1804` 法师祸咒、`1806` 格挡等仍因庇护、区域光环、响应、强制防御或展示时机未闭合保持 needsCode；`1809` 灵蛇反射与 `1818` 原力法剑已由独立防御切片覆盖。隐藏结界、反制窗口、法师附属结界和完整结界 UI 也不在本切片。 |
| 验证证据 | 历史切片的 `ability-catalog.test.ts` 记录 68 implemented / 23 needsCode；当前测试覆盖 71 implemented / 20 needsCode、装备 9/14、结界 9/24，并覆盖 `1808` / `1809` / `1816` / `1818` / `1914` / `1916` / `1917`。`domain-flow.test.ts` 还覆盖灵蛇反射从附属对象提供防御、展示文案改写不影响 profile、原力法剑在昏迷状态下保留防御且眩晕骰值修正仍生效，以及狱火长鞭远触攻击同区飞行目标。 |

## 2026-08-03 瓦解同名候选切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3406` 瓦解为同名旧版 / 对照候选，`3605` 瓦解为新手法术书实例使用版本；两者均为费用 `X`、快速咒语 / 酸性、等级 1、射程 `0-1`、目标为法师，规则文本均要求选择附属在目标法师上的装备对象并摧毁，X 等于该装备施法费用。 |
| 已覆盖 | `3406` 与 `3605` 均由配置包标为 implemented；运行时以 `cardId` 识别两张瓦解并复用同一装备摧毁链，保留各自 `mw.spell.<CardID>` 来源能力。 |
| 边界 | 本切片只覆盖同名瓦解候选复用已验证的附属装备摧毁规则；不代表炎爆、驱散、结界窃取、其它咒语、其它装备、完整装备 UI 或自由构筑同名裁定已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 55 implemented / 36 needsCode、咒语 14/18 implemented，以及 `3406` / `3605` 均为 implemented；`domain-flow.test.ts` 参数化覆盖 `3406` 与 `3605` 合法摧毁附属装备，并覆盖费用不匹配、未附属装备、非装备对象、法力不足和距离外目标拒绝不退化。 |

## 2026-08-03 炎爆装备爆炸切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3401` 炎爆：费用 `X`，快速咒语 / 火焰，等级 2，射程 `0-1`，目标为法师；卡面要求选择附属在目标法师上的装备，X 等于该装备施法费用 + 6，摧毁装备后对目标法师进行火焰攻击。 |
| 已覆盖 | `3401` 已由配置包标为 implemented；运行时只允许指定附属在法师上的装备对象，按装备来源卡费用 + 6 校验 X 费用；合法施放先发出装备离场事件并移除装备对象，再把炎爆的 4 骰火焰攻击结算到该装备附属的法师，并按效果骰放置燃烧。 |
| 边界 | 本切片只覆盖炎爆“装备离场 + 后续火焰攻击”事实；被摧毁装备不参与本次后续攻击减伤。当前不代表驱散、结界窃取、结界、装备栏、职业限制、武器攻击、防御、反伤屏障、法术绑定、完整装备系统或 UI 已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 56 implemented / 35 needsCode、咒语 15/18 implemented 和 `3401` implemented；`domain-flow.test.ts` 覆盖炎爆合法摧毁附属装备、对装备附属法师进行 4 骰火焰攻击、效果骰放置燃烧、被摧毁装备不进入本次伤害减免，以及费用不匹配、未附属装备、非装备对象、法力不足和距离外目标拒绝。 |

## 2026-08-03 驱散显性结界切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3419` 驱散为同名旧版 / 对照候选，`3606` 驱散为新手法术书实例使用版本；两者均为费用 `X`、快速咒语 / 超魔、等级 1、射程 `0-2`、目标为显性结界，规则文本要求摧毁目标，X 等于目标结界的法力总费用（施法费用加展示费用）。 |
| 已覆盖 | `3419` 与 `3606` 均由配置包标为 implemented；运行时只允许指定已存在、已展示且附属在对象或法师上的 `kind=enchantment` 场上对象，按来源结界卡 `raw` 费用数字求和校验 X，合法施放发出结界离场事件并移除目标对象，保留各自 `mw.spell.<CardID>` 来源能力。 |
| 边界 | 本切片只覆盖“已有显性结界对象被驱散摧毁”的规则；不代表隐藏结界施放、展示时机、法术反制、结界持续效果、结界窃取、完整结界系统或 UI 已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 58 implemented / 33 needsCode、咒语 17/18 implemented，以及 `3419` / `3606` 均为 implemented；`domain-flow.test.ts` 覆盖 `3606` 合法摧毁附属显性结界、`3419` 同名候选执行层对照，以及费用不匹配、隐性结界、非结界对象、未附属结界、法力不足和距离外目标拒绝。 |

## 2026-08-03 结界窃取显性结界转移切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `3409` 结界窃取：费用 `X`，快速咒语 / 超魔，等级 3，射程 `0-2`，目标为“显性结界和新目标”；卡面要求将一个显性结界移至一个新的合法目标上，并由施法者控制该结界；X 等于目标结界施法 + 展示总费用，加上附属到新目标所需的施法 + 展示总费用。 |
| 已覆盖 | `3409` 已由配置包标为 implemented；运行时只允许指定已存在、已展示且已附属的 `kind=enchantment` 场上对象，并要求显式提供且只能提供一个新目标；新目标必须符合被窃取结界来源卡的目标规则，不能与原锚点相同，旧结界和新目标都必须在 `0-2` 范围内；合法施放发出 `ENCHANTMENT_STOLEN` 事件，移动结界对象、更新锚点，并把 owner 改为施法者。 |
| 边界 | 本切片只覆盖“已有显性结界对象迁移到新合法目标”的执行事实；当前学徒基础版没有墙体 / 视线系统，所以只实现两端目标距离校验，不代表完整视线、墙体、隐藏结界施放、展示时机、法术反制、持续结界效果或完整结界 UI 已经闭合。 |
| 验证证据 | `ability-catalog.test.ts` 在该切片完成时覆盖 59 implemented / 32 needsCode、咒语 18/18 implemented，以及 `3409` implemented；后续武器装备攻击与结界基础效果切片已将当前总计推进到 63 implemented / 28 needsCode。`domain-flow.test.ts` 覆盖合法转移显性附属结界、控制权改为施法者、锚点与区域更新、费用扣除和进弃牌，并覆盖缺新目标、多个新目标、费用不匹配、隐性结界、非结界、未附属结界、非法新目标、同一目标、法力不足、旧目标超距和新目标超距拒绝。 |

## 2026-08-02 雷隙猎鹰飞行 / 迅捷配置放行切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2820` 雷隙猎鹰：生物 / 动物、鸟类，费用 6，生命 5，护甲 0，攻击条为 `剃刀鸟喙：快速近战 3 骰；飞行；迅捷`，无额外规则正文。 |
| 已覆盖 | `2820` 已由配置包标为 implemented；生物召唤执行器按配置字段构造雷隙猎鹰场上对象；现有印刷迅捷 owner 允许首次普通相邻移动不消耗行动标记；现有飞行识别 owner 可被 `气流` 的对飞行 +2 伤害消费，残废失去飞行的规则仍由既有残废切片覆盖。 |
| 边界 | 本切片只消费雷隙猎鹰当前字段；不代表飞行移动、阻碍、墙体、完整 12 区、遁逸、其它飞行生物或 UI 骰盘展示已经闭合。 |
| 验证证据 | `domain-flow.test.ts` 覆盖雷隙猎鹰按配置召唤后保留费用 / 生命 / 护甲 / 攻击特性、首次印刷迅捷移动不耗行动，并被 `气流` 识别为飞行目标获得 +2 伤害；`ability-catalog.test.ts` 覆盖 `2820` implemented 计数。 |

## 2026-08-02 暗沼蝙蝠飞行 / 腐化攻击配置放行切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 字段合同已锁定 `2825` 暗沼蝙蝠：生物 / 动物、蝙蝠，费用 5，生命 4，护甲 0，攻击条为“致病噬咬：快速近战 2 骰，效果骰 9+=腐化；飞行”，无额外规则正文。 |
| 已覆盖 | `2825` 已由配置包标为 implemented；生物召唤执行器按配置字段构造暗沼蝙蝠场上对象；现有攻击效果骰 owner 可在 9+ 时放置腐化 token；现有飞行识别 owner 可被 `气流` 的对飞行 +2 伤害消费，残废失去飞行的规则仍由既有残废切片覆盖。 |
| 边界 | 本切片只消费暗沼蝙蝠当前字段；不代表飞行移动、阻碍、墙体、完整 12 区、遁逸、其它飞行生物或 UI 骰盘展示已经闭合。 |
| 验证证据 | `domain-flow.test.ts` 覆盖暗沼蝙蝠按配置召唤后保留费用 / 生命 / 护甲 / 攻击特性、致病噬咬 9+ 放置腐化，并被 `气流` 识别为飞行目标获得 +2 伤害；`ability-catalog.test.ts` 覆盖 `2825` implemented 计数。 |

## 2026-08-02 法力流失攻击切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则书 `page_042.md` 锁定 `法力流失+X`：攻击对敌方生物造成伤害后，目标控制者失去 X 点法力，不足 X 时失去全部；多次攻击只在首次攻击获得奖励。`page_043.md` 锁定 `法力传输+X` 是另一条额外让攻击方获得法力的规则，本切片不合并处理。 |
| 已覆盖 | `2807` 汲法水蛭已由配置包标为 implemented；场上对象攻击在造成实际伤害后解析攻击条中的 `法力流失+X`，只在 `strikeIndex=0` 时发出 `MANA_DRAINED` 事件，并按目标控制方法力池封顶扣除法力；后续武器装备攻击切片已让 `3704` 奥秘法杖复用同一法力流失 owner。 |
| 仍未覆盖 | 法力传输、攻击方获得法力事件、`3701` 狱火长鞭远触、`3710` 群兽法杖特殊能力、其它未覆盖法力流失牌、UI 骰盘 / 日志更细展示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖汲法水蛭快速攻击造成伤害后让目标控制方法力流失、目标法力不足时按现有法力封顶、三连击模拟只触发一次法力流失；武器装备攻击测试覆盖奥秘法杖近战 / 远程 profile 造成伤害后触发法力流失；`ability-catalog.test.ts` 覆盖 `2807` / `3704` implemented 计数，且 `3701` 狱火长鞭仍保持 needs-code。 |

## 2026-08-02 阿希拉牧师治疗之光切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 卡图 `cardid-2811.png` 与字段合同锁定 `2811` 阿希拉牧师：治疗之光为完整行动能力，距离 `0-1`，投 1 颗治疗骰，目标为活体生物，治疗效果等于掷骰结果。 |
| 已覆盖 | `2811` 已由配置包标为 implemented；场上对象能力命令新增治疗之光分支，只允许本方 ready 阿希拉牧师在生物行动阶段以 0 法力使用，校验目标为距离 0-1 的活体生物；执行层复用 `SPELL_HEALING_ROLLED` 治疗事件，并通过对象能力事件消耗牧师行动标记。 |
| 仍未覆盖 | 其它治疗行动、治疗 UI 骰盘、治疗与反应窗口的特殊联动仍需后续切片；本切片不把所有治疗行动泛化放行。 |
| 验证证据 | `domain-flow.test.ts` 覆盖治疗之光治疗相邻活体对象、消耗牧师行动、不消耗法力、非活体目标拒绝、超距目标拒绝和错误来源拒绝；`ability-catalog.test.ts` 覆盖 `2811` implemented 计数。该切片完成时 `3704` 奥秘法杖仍保持 needs-code，后续已由独立武器装备攻击切片放行。 |

## 2026-08-02 灰衣天使救赎献祭切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 卡图 `cardid-2907.png` 与字段合同锁定 `2907` 灰衣天使：救赎献祭为完整行动能力，投 6 颗治疗骰，治疗竞技场中任一活体生物，治疗效果等于掷骰结果，然后摧毁灰衣天使。 |
| 已覆盖 | `2907` 已由配置包标为 implemented；场上对象能力命令新增救赎献祭分支，只允许本方 ready 灰衣天使在生物行动阶段以 0 法力使用，校验目标为任一活体生物；执行层复用 `SPELL_HEALING_ROLLED` 治疗事件，并在治疗后发出 `ARENA_OBJECT_DEFEATED` 移除灰衣天使。 |
| 仍未覆盖 | 飞行移动含义、其它治疗行动、治疗 UI 骰盘、治疗与反应窗口的特殊联动仍需后续切片；本切片不把所有治疗行动泛化放行。 |
| 验证证据 | `domain-flow.test.ts` 覆盖救赎献祭治疗远距活体对象、按目标伤害封顶、摧毁灰衣天使、非活体目标拒绝和错误来源拒绝；`ability-catalog.test.ts` 覆盖 `2907` implemented 计数。该切片完成时 `3704` 奥秘法杖仍保持 needs-code，后续已由独立武器装备攻击切片放行。 |

## 2026-08-02 元素 / 战斗字段生物配置放行切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定 `2801` 火烙魔婴、`2802` 钢爪灰熊、`2803` 烈焰狱鬼与 `2813` 布洛根·血石：四张牌的当前可执行字段集中在费用、生命、护甲、攻击条、穿刺、无法回避、伤害类型修正 / 免疫和燃烧效果骰；基础规则把 `除霜` 写为火焰攻击关键词，并说明霜冻相关规则属于后续扩展。 |
| 已覆盖 | 四张牌已由配置包标为 implemented；生物召唤执行器按配置字段构造场上对象，保留名称、来源、生命、护甲和攻击 / 特性文本；现有战斗管线已能消费火焰免疫、火焰 / 霜冻 / 闪电修正、穿刺、无法回避和火焰攻击效果骰燃烧。 |
| 仍未覆盖 | `除霜` 当前只作为火焰攻击关键词保留在字段文本中，不新增霜冻状态或扩展规则；治疗行动、装备法力流失、法力传输、飞行移动含义、阻碍、遁逸、重生光环、特殊激活和其它逐卡特殊规则仍按后续切片处理。 |
| 验证证据 | `ability-catalog.test.ts` 覆盖 2801 / 2802 / 2803 / 2813 implemented 计数；`domain-flow.test.ts` 覆盖四张牌按各自学徒法师真实法术书召唤，并保留费用、生命、护甲、来源与完整攻击 / 特性文本。 |

## 2026-08-02 燃烧维持 tick 切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_044.md` 明确每个燃烧状态标记会在维持阶段投掷 1 颗攻击骰造成直接伤害，投出 `0` 时移除该燃烧标记；`mage-wars.config.json` 的 `burn-token` 记录 `upkeepRule: roll-one-attack-die-per-token-and-remove-token-on-blank`。 |
| 已覆盖 | 进入维持阶段时，法师和场上对象按燃烧层数逐枚投骰；非空白结果汇总为直接伤害；空白结果移除对应燃烧 token；未投空白的燃烧 token 保留。 |
| 仍未覆盖 | 燃烧维持直接伤害与火焰免疫的完整联动、燃烧 UI 骰面展示仍需后续切片；攻击结算中的火焰免疫已由伤害类型免疫切片覆盖。 |
| 验证证据 | `domain-flow.test.ts` 覆盖法师、场上对象、空白移除、非空白直接伤害、token 保留和行动日志；燃烧伤害通过 `DamageCalculation` 的直接伤害路径结算。 |

## 2026-08-02 无法燃烧落 token 过滤补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_044.md` 明确“无法燃烧”对象不能获得燃烧状态，但仍能受到火焰伤害；同页还明确所有虚体对象都具有无法燃烧特性。 |
| 已覆盖 | 法术攻击与场上对象攻击共用 `canMageWarsStatusTokenAffectArenaObject` 过滤燃烧落点；目标规则文本或特性文本包含“无法燃烧”或“虚体”时，不放置燃烧 token；攻击伤害仍按原路径结算。 |
| 仍未覆盖 | 直接伤害免疫、火焰免疫与燃烧状态的完整联动、燃烧 UI 骰面展示仍需后续切片；攻击结算中的火焰免疫已由伤害类型免疫切片覆盖。 |
| 验证证据 | `domain-flow.test.ts` 覆盖 `烈焰爆弹` 命中无法燃烧对象时仍造成伤害但不产生燃烧事件，以及火烙魔婴对象攻击命中无法燃烧对象时仍造成伤害但不放置燃烧 token。 |

## 2026-08-02 腐化维持伤害切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_044.md` 明确每个腐化状态标记会在维持阶段让对象受到 1 点直接伤害；`mage-wars.config.json` 的 `rot-token` 记录 `upkeepRule: deal-1-direct-damage`。 |
| 已覆盖 | 进入维持阶段时，法师和活体场上对象按腐化层数受到直接伤害；非活体场上对象跳过；腐化 token 不会因本次维持伤害自动移除；若伤害击败法师或场上对象，走既有击败事件。 |
| 仍未覆盖 | 毒素净化类能力和腐化相关 UI 提示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖法师、活体对象、非活体对象、token 保留和行动日志；腐化伤害通过 `DamageCalculation` 的直接伤害路径结算。 |

## 2026-08-02 沉睡受伤替换切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_042.md` 明确沉睡生物受到任何伤害时，移除沉睡标记并用眩晕标记代替；`mage-wars.config.json` 的 `sleep-token` 记录 `automaticReplacementRule: replace-with-daze-when-damaged`。 |
| 已覆盖 | 任何 `DAMAGE_DEALT` 事件造成实际伤害时，目标身上的全部沉睡层数会被移除，并放置同层眩晕；伤害为 0 时不触发替换。 |
| 仍未覆盖 | 沉睡 UI 提示仍需后续切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖沉睡对象受伤替换为眩晕，以及护甲抵消至 0 伤害时沉睡保留。 |

## 2026-08-02 状态 token 移除费用配置补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_029.md` 明确“移除费用”只能被法术或能力允许后支付，不能直接支付；`page_034.md` 明确女祭司“复原术”可移除燃烧、眩晕、昏迷、腐化、虚弱、残废和沉睡；`page_042.md` / `page_043.md` / `page_044.md` / `page_045.md` 分别给出残废、沉睡、腐化、昏迷、燃烧、虚弱和眩晕的移除费用规则。 |
| 已覆盖 | `mage-wars.config.json` 已为燃烧、眩晕、腐化、虚弱、残废、沉睡和昏迷写入 `statusTokenId`、固定移除费用或按目标等级计算的移除费用规则、同名状态必须整批支付的规则事实；守卫标记只保留为守卫标记，不暴露为可付费移除的状态 token。快速复原术已作为女祭司法师能力消费固定移除费用：快速施法窗口内支付目标生物同名状态全部层数费用，整批移除该类状态，并且不进入准备牌或弃牌堆。标准复原术已可在生物行动阶段选择多类状态，固定费用按同名层数计算，沉睡按目标生物配置等级计算，并消耗完整行动。 |
| 仍未覆盖 | 本切片不新增“直接支付法力移除状态”的通用命令；毒素净化类法术仍需后续能力 / 阶段切片消费这些配置数据。 |
| 验证证据 | `config-package.test.ts` 覆盖七类可移除状态 token 的费用数据、沉睡按目标等级规则、残废 7+ 逃逸事实和守卫标记非付费状态边界；`domain-flow.test.ts` 覆盖女祭司快速复原术支付 4 点法力移除目标生物 2 个燃烧、保留眩晕、消耗快速施法且不动已计划 / 弃牌区，以及标准复原术支付 9 点法力移除目标生物燃烧、昏迷和沉睡并消耗完整行动。 |

## 2026-08-01 眩晕 / 昏迷切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_014.md` / `page_043.md` 明确施法条行动图标区分快速法术和标准法术；`page_045.md` 明确眩晕生物每当攻击时掷效果骰，7+ 正常结算、6 或更低攻击失手，魔物不会受到眩晕，且瘫痪法师只能用行动施放非攻击性快速法术，不能施放标准法术或攻击类法术。 |
| 已覆盖 | 攻击法术和对象攻击的眩晕 / 昏迷 token 仍走 `STATUS_TOKEN_PLACED`；魔物不会获得眩晕或昏迷；携带眩晕的法师基础近战和生物对象攻击在 d12 结果 6 或更低时产生 `ATTACK_MISSED`，不掷攻击骰、不造成伤害、不落攻击附带状态，但会消耗本次攻击行动；带眩晕的场上对象防御掷骰会按配置包每枚眩晕 -2 修正；带昏迷的场上对象不能使用防御 profile，裁定来自配置包 `paralyzeRule`；昏迷法师不能普通移动、守卫、基础近战攻击或施放攻击类法术；配置包已为 91 张学徒法术记录卡面行动图标速度，其中快速 61 张、标准 30 张；快速施法阶段拒绝标准法术，昏迷法师可用行动施放 `次级治疗` 这类非攻击快速法术并拒绝 `群体治疗` 这类标准法术；昏迷生物不能普通移动或攻击；目标获得昏迷时自动移除守卫；行动阶段结束时移除当前行动方法师和己方生物的全部眩晕与昏迷。 |
| 仍未覆盖 | 强制防御结界、反伤屏障、响应优先级、结界、装备、其它咒语和其它逐卡特殊效果仍需继续补执行器。 |
| 验证证据 | `config-package.test.ts` 覆盖 91 张学徒法术的 `spellActionSpeed`、快速 / 标准数量和易错样本；`domain-flow.test.ts` 覆盖魔物免疫眩晕 / 昏迷、眩晕生物攻击失手、眩晕法师基础近战失手、眩晕生物防御掷骰 -2、昏迷生物防御禁用、昏迷生物不能移动 / 攻击、昏迷法师不能移动 / 守卫 / 基础近战、昏迷法师不能施放攻击类法术、昏迷法师可用行动施放非攻击快速法术、昏迷法师不能施放标准法术、标准法术不能在快速施法阶段施放、眩晕行动阶段结束清理、昏迷行动阶段结束清理、昏迷落点清除守卫和行动日志记录。 |

## 2026-08-01 残废 / 束缚切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_042.md` 明确残废是毒素状态、使生物被束缚、行动阶段结束可 7+ 逃逸、移除费用为 4、不能影响魔物；`page_045.md` 明确束缚生物不能执行移动行动，但仍可被推斥或传送，不羁生物和所有魔物不能被束缚，飞行生物被束缚期间失去飞行。 |
| 已覆盖 | 残废会阻止生物执行普通移动；残废不会落到魔物、非活体生物或不羁生物；残废的飞行生物不再被视为飞行，因此不吃 `气流` 的对飞行 +2 伤害；残废生物视为被束缚，防御掷骰额外 -2，该修正只在存在残废时应用一次；推斥 / 传送没有被残废移动锁误拦截；当前行动方生物在行动阶段结束时会掷效果骰做逃逸检定，7+ 移除残废，6 或更低保留残废，对手生物不被当前阶段结算误清。 |
| 仍未覆盖 | 束缚对象不阻碍移动、横扫 / 飞行 / 遁逸等反击特例仍未闭合。 |
| 验证证据 | `domain-flow.test.ts` 覆盖残废普通移动拒绝、不羁免疫残废、残废飞行目标被 `气流` 推斥但不触发飞行加伤、残废生物防御掷骰 -2、行动阶段结束 7+ 逃逸移除残废和 6 点失败保留残废。 |

## 2026-08-02 守卫拦截、近战移除与进行守卫动作切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `page_031.md` 明确：生物可以花费一个快速行动进行守卫；同区域内存在敌方守卫时，生物不能对不具有守卫标记的对象进行近战攻击；法术与远程攻击始终忽略守卫；受到束缚、瘫痪或小型守卫不能保护区域；守卫成为近战攻击目标后必须在攻击完成后移除守卫标记，即使攻击失败仍需移除，且攻击守卫不影响其行动标记。 |
| 已覆盖 | 己方 ready 场上生物可通过 `GUARD` 进入守卫，消耗该生物自己的行动标记，不要求也不消耗法师行动标记；场上生物使用近战攻击 profile 时，若同区存在可保护区域的敌方守卫生物，校验会拒绝攻击敌方法师或非守卫对象，并要求先攻击守卫；残废/束缚、昏迷和小型守卫不作为保护者；远程对象攻击与法术攻击不进入该守卫拦截限制。守卫对象被近战对象攻击选为目标后，会产生 `GUARD_REMOVED` 并移除守卫标记；眩晕失手仍移除；目标对象 `actionReady` 保留。 |
| 仍未覆盖 | 守卫反击与防御窗口 / 防御冷却的联动、飞行 / 遁逸 / 横扫等更细例外仍需后续攻击流程切片。 |
| 验证证据 | `domain-flow.test.ts` 覆盖 ready 场上生物执行守卫动作不消耗法师行动、同区敌方守卫拦截生物近战打法师和非守卫对象，并允许目标直接指向守卫；同时覆盖守卫被近战对象攻击后移除，以及眩晕失手仍移除守卫。 |

## 2026-07-29 实现复核层

| 项 | 当前结论 |
| --- | --- |
| 回合 / 计划 / 移动 / 守卫 / 攻击 / 胜负主链 | 已在 `domain-flow.test.ts` 和 `npx vitest run src/games/mage-wars` 中覆盖 foundation 层。 |
| 法术书 / 学徒卡牌 | 四名学徒法术书组成、91 张 S0 字段、正式 atlas/frame 和 Board 卡牌预览接线已完成。 |
| 场上对象行动 | `core.objects` 已建立；生物/魔物基础召唤、生物对象移动、残废阻止生物普通移动、显式攻击 profile、近战/远程基础距离校验、场上生物执行守卫动作、同区守卫拦截和守卫近战移除、对象攻击效果骰状态 token（燃烧 / 眩晕 / 昏迷 / 腐化 / 虚弱 / 残废）、毒素状态免疫过滤、对象护甲、对象攻击穿刺抵消护甲、三连击多段伤害、维持阶段重生、燃烧维持阶段攻击骰 tick、腐化维持阶段直接伤害、沉睡受伤替换眩晕、对象受伤 / 死亡移除和对象行动重置已进入领域层与测试。 |
| FX | `eventFxMapper.ts`、`fxSetup.ts`、`useGameEvents.ts` 已接入；`event-fx-mapper.test.ts` 和 `Board.fx.test.tsx` 覆盖基础映射。 |
| 隐藏信息 | foundation 层已证明对手已计划法术、未公开法术书内容和隐性结界使用卡背或数量表达，不公开私有牌名；完整隐藏结界展示 / 反制流程仍是后续玩法深水区。 |
| 不在本轮 | 全 322 张法术、自由构筑、四人模式、完整 AI、教程、行动日志 UI、撤回 UI 和完整隐藏结界执行器。 |

## 2026-08-04 结构化精华汲取维持费用切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | 规则页 `page_009.md` 明确维持费用必须在维持阶段支付，否则法术被摧毁；字段合同锁定 `1815` 精华汲取为目标非法师生物获得维持 `+2`。 |
| 已覆盖 | `1815` 由配置包声明可见对象附属和 `upkeepEffects: mana-cost / 2`；维持阶段从附属来源发出待处理事件，目标生物控制者可支付 2 点法力或摧毁该结界，法力不足时唯一摧毁选项自动结算。 |
| 规则 owner | 维持费用由配置语义、附属来源查询和 `UPKEEP_COST_AVAILABLE` 事件驱动；支付使用 `MANA_SPENT`，来源销毁使用既有 `ARENA_OBJECT_DEFEATED`，不从展示文本读取，也不使用攻击法力流失事件。 |
| 边界 | 不实现完整维持效果排序、法师绑定、`1804` 生物施法触发、隐藏结界、展示 / 反制或完整结界 UI。 |
| 验证证据 | `config-package.test.ts`、`ability-catalog.test.ts` 和 `domain-flow.test.ts` 覆盖配置标记、目标控制者支付、法力不足摧毁、展示文案移除和来源移除后的陈旧响应；当前统计为 91 张学徒法术 `82 implemented / 9 needsCode`，结界 `20/24`。 |

## 领域对象模型

| 规则对象 | 工程对象 | 单对象 / 资源 | 稳定身份 | 真相层 | 当前范围 |
| --- | --- | --- | --- | --- | --- |
| 法师 | `MageEntity` / `MageWarsPlayerState` | 单对象 | `playerId + mageId` | `core.players` | 正式起始法师状态已接入；扩展能力后续 |
| 法师状态板 | `MageBoardState` | 单对象面板 | `playerId` | `core.players` + 后续派生读模型 | 生命、伤害、法力、聚魔已占位 |
| 竞技场区域 | `ArenaZone` | 单对象格区 | `zoneId` | `core.arena` | 完整标准 4x3 / 12 区已接入 |
| 法术书 | `SpellbookZone` | 私有卡组 | `playerId` | `core.players` + 标准起始法术书配置 | 标准起始法术书已录入并在 Board 预览；完整卡牌执行器后续 |
| 计划法术 | `PreparedSpellRef` | 单卡实例 | `cardInstanceId` | 私有 `playerView` 过滤 | foundation 层已有准备槽 / 对手卡背 UI；完整多步骤计划交互后续 |
| 法术牌 | `SpellCardDef` + `SpellCardInstance` | 单卡 | `defId` + `instanceId` | 静态数据 + runtime 区域 | 91 张学徒 S0 字段和 atlas/frame 已完成；`次级治疗` / `群体治疗` / `单体治疗` 已有治疗执行切片；`生命汲取` 已有直接伤害 + 治疗执行切片；10 张攻击法术已接入普通攻击骰 / 区域攻击 / 状态骰 / 非活体加伤 / 对飞行加伤 / 推斥 / 连锁递减等覆盖管线；其它逐张执行器后续 |
| 生物 / 魔物 | `ArenaObject` | 单对象 | `objectId` | `core.objects` | 学徒生物 / 魔物字段和视觉素材已完成；基础召唤、移动、显式攻击 profile、近战/远程基础距离校验、场上生物执行守卫命令、同区敌方守卫对生物近战目标选择的拦截、守卫被近战攻击后移除、守卫反击机会 / 放弃 / 选择反击结算、对象防御窗口（可放弃、成功回避、失败继续、用后冷却、重置恢复、无法回避跳过）、对象攻击效果骰状态 token（燃烧 / 眩晕 / 昏迷 / 腐化 / 虚弱 / 残废）、毒素状态对魔物 / 非活体免疫过滤、对象护甲减伤、穿刺抵消对象护甲、三连击多段伤害、维持阶段重生、燃烧维持阶段攻击骰 tick、虚弱非法术攻击减骰、残废阻止生物普通移动、残废行动阶段结束 7+ 逃逸检定、眩晕防御骰 -2、昏迷行动阶段结束清理、对象伤害和死亡移除已实现；`气流` 与 `原力推斥` 已覆盖显式相邻目标区和区域移动；强制防御结界、反伤屏障、非护甲属性修正、墙体通行伤害额外费用、虚弱 / 残废移除费用和特殊行动后续 |
| 墙体 | `WallObject` | 单对象边界 | `wallId` | `core.walls` 后续扩展 | 标准空间数据待录 |
| 装备 / 结界 | `AttachmentObject` | 单对象附着 | `attachmentId` | `core.attachments` 后续扩展 | 隐藏结界必须有可见性模型 |
| 状态标记 | `TagContainer` + token view | 可堆叠状态 | `statusId` | `tags.ts` / 派生 UI | 守卫 / 伤害 / 燃烧 / 腐化 / 眩晕 / 昏迷 / 虚弱 / 残废 / 沉睡 token 资源与运行时显示已接线；燃烧维持阶段攻击骰 tick、眩晕攻击失手、眩晕防御骰 -2、眩晕行动阶段结束清理、昏迷基础行动锁、昏迷防御禁用、昏迷清守卫、昏迷行动阶段结束清理、腐化维持阶段直接伤害、沉睡受伤替换眩晕、虚弱非法术攻击减骰、残废阻止生物普通移动、残废防御骰 -2 与残废行动阶段结束 7+ 逃逸检定已进入领域结算；燃烧 / 眩晕 / 昏迷 / 腐化 / 虚弱 / 残废 / 沉睡移除费用规则已进入配置包；女祭司快速 / 标准复原术已消费固定费用、同名整批、多状态和沉睡等级费用；`昏睡` 已可放置沉睡 token，沉睡 UI 提示后续 |
| 伤害 / 护甲 / 攻击修正 | `DamageCalculation` + `Attribute` | 数值管线 | 来源事件 ID | `damageCalculation.ts` | foundation 攻击 / 伤害主链已测试；场上对象护甲、`对抗非活体生物+N` 和 `对抗飞行+N` 已作为 `additionalModifiers` 进入伤害 breakdown；完整卡牌修正后续 |
| 法术特效 | `SpellFxDescriptor` | 事件派生表现 | `eventId + spellId` | EventStream / FX 层 | 必须由合法事件驱动 |

## 真相层分离

| 层级 | 内容 | 法师战争落点 |
| --- | --- | --- |
| 正式真相层 | 法师、区域、对象位置、卡牌实例、法力、伤害、准备/行动标记、隐藏信息可见性 | `core` 与后续 `playerView` |
| 系统层 | 阶段推进、响应窗口、选择目标、计划/展示确认、撤回快照 | `sys.phase`、`sys.interaction`、`sys.responseWindow` |
| 派生读模型 | 区域可点状态、法术可施放状态、可见卡面、FX 锚点、状态徽章 | `ui/` hooks 后续生成 |
| 纯 UI 状态 | hover、展开、缩放、当前检视卡、动效局部播放进度 | React state / FX runtime |

## 术语到事件映射

| 规则术语 | 语义边界 | 状态 / 事件方向 | UI / FX 承接 |
| --- | --- | --- | --- |
| 聚魔 | 法师及有聚魔的对象获得法力 | `MANA_CHANNELED`，写入法力池 | 法力数值脉冲，不进入法术成功 FX |
| 计划法术 | 从私有法术书选择准备法术，通常最多 2 张 | `SPELL_PREPARED`，私有卡引用 | 自己可见，对手只见背面或数量 |
| 快速施法 | 消耗快速施法标记施放合法快速法术 | `QUICKCAST_USED` + `SPELL_CAST_STARTED` | 起手反馈必须绑定法师/来源 |
| 施法 | 支付费用、检查范围/目标/视线并产生法术结算 | `SPELL_CAST_STARTED` → `SPELL_RESOLVED` / `SPELL_CANCELLED` | source、target、travel/spread、impact |
| 目标移动取消 | 施法者、来源或目标在结算前移动导致取消/落空 | `SPELL_CANCELLED` / `ATTACK_MISSED` | 只播取消/失效反馈，不播成功命中 |
| 近战攻击 | 选择攻击、声明目标、守卫/防御/反击/伤害流程 | `ATTACK_DECLARED` → `DAMAGE_DEALT` | 攻击路径、骰面、命中对象反馈 |
| 守卫 | 生物进入守卫状态，可影响攻击目标选择，并在成为近战攻击目标后移除 | `GUARD_GAINED` / `GUARD_REMOVED` | 状态标记贴在对象本体 |
| 结界隐藏 / 展示 | 面朝下附着，拥有者可付展示费揭示 | `ENCHANTMENT_ATTACHED_HIDDEN` / `ENCHANTMENT_REVEALED` | 对手不可见身份；揭示时短促 reveal FX |
| 装备 | 附着到法师，改变属性或提供能力 | `EQUIPMENT_ATTACHED` | 法师板装备槽 + 属性变化提示 |
| 维持 | 支付或放弃维持费用，结算重生、燃烧、腐化等 | `ARENA_OBJECT_REGENERATED` 已覆盖重生；燃烧维持攻击骰 tick 和腐化维持直接伤害已覆盖；维持费用后续 | 短状态，不写长规则正文 |
| 法师死亡 | 法师累积伤害达到生命导致游戏结束 | `MAGE_DEFEATED` / `GAME_OVER` | 结束态读 `G.sys.gameover` |

## 玩家决策点清单

| 决策点 | 类型 | 规则阶段 | 系统承接 | 当前状态 |
| --- | --- | --- | --- | --- |
| 选择法师 / 学徒法术书 | 强制 | setup | setupOptions 或角色选择系统 | foundation 后续 |
| 计划最多 2 张法术 | 强制 / 可少选 | planning | `createSimpleChoice` / 多选 Interaction | 待实现 |
| 部署再生点准备法术 | 可选/强制按对象 | deployment | Interaction + response window | 待实现 |
| 是否使用优先快速施法 | 可选 | initiativeQuickcast | 可跳过 Interaction | 待实现 |
| 激活哪个生物行动 | 强制直到无活动生物 | creatureAction | 区域/对象直选 | 待实现 |
| 移动到哪个区域 | 可选 | creatureAction | 竞技场区域直选 | 待实现 |
| 选择并施放哪张法术 | 可选 | quickcast / action | 私有已计划法术槽直选；计划阶段从法术书选择最多 2 张 | 待实现 |
| 选择目标区域/对象/墙体 | 强制，依赖法术 | 施法结算 | 目标 descriptor，不靠 payload 形状猜 UI | 待实现 |
| 是否展示结界 | 可选或规则触发的强制展示 | 多阶段 | `ResponseWindow` / `Interaction`；强制展示不能 `pass` 或取消 | `1825` / `1901` / `1904` 响应切片已覆盖；`1804` 仍待独立生物施法者来源模型 |
| 是否支付维持 | 可选 | upkeep | SimpleChoice | 待实现 |
| 攻击目标与攻击方式 | 强制/可选 | creatureAction | 对象直选 + 攻击 descriptor | 待实现 |
| 防御/反击/守卫响应 | 条件式 | 攻击流程 | `sys.interaction` / 后续 ResponseWindow | 守卫反击机会、放弃和选择反击结算已覆盖；基础对象防御窗口、防御用后冷却和重置恢复已覆盖；`3700` 反伤屏障已由独立结构化切片覆盖；强制防御结界和多响应优先级待实现 |

## 引擎能力映射与缺口

| 能力 | 当前引擎映射 | 优先级 | 结论 |
| --- | --- | --- | --- |
| 回合阶段 | `FlowSystem` | P0 | 可直接复用；阶段顺序已占位 |
| 玩家选择 | `InteractionSystem` / `SimpleChoiceSystem` / `ResponseWindowSystem` | P0 | 可复用；必须用 descriptor 明确展示模式 |
| 隐藏信息 | `DomainCore.playerView` | P0 | 需要游戏层实现私有法术书、计划法术、隐藏结界过滤 |
| 状态 / 特性 | `tags.ts` | P0 | 可复用；状态标记不要散落布尔字段 |
| 法力 / 聚魔 | `resources.ts` 或游戏内资源字段 | P0 | 首轮可游戏内字段；若对象法力池扩展再统一资源容器 |
| 属性修正 | `attribute.ts` + `modifier.ts` | P0 | 场上对象护甲与穿刺抵消已走 `DamageCalculation.additionalModifiers`；近战/远程修正、状态加减和装备/结界属性仍需继续走原语 |
| 伤害计算 | `damageCalculation.ts` | P0 | 攻击实现必须接入，禁止手写 `DAMAGE_DEALT` |
| 法术能力 | `ability.ts` + 游戏内执行器 | P1 | 已建立 `domain/abilityCatalog.ts`，91 张学徒法术均有稳定能力 ID；当前配置包标记 90 张已实现，其中攻击法术 10 张、生物 24 张、魔物 1 张、咒语 18 张、装备 14 张、结界 23 张。已实现咒语包括 `生命汲取` / `炎爆` / `驱散` / `结界窃取` / `次级治疗` / `群体治疗` / `单体治疗` / `原力推斥` / `传送` / `昏睡` / `兽性觉醒` / `瓦解` / `冲锋陷阵` / `荒野呼唤`；武器装备攻击已覆盖 `3701` 狱火长鞭、`3704` 奥秘法杖和 `3706` 阿希拉法杖，群兽法杖 `3710` 已覆盖兽王限制、回合一次、近战 +2 / 2 骰治疗两种能力模式；来袭攻击费用已覆盖 `3705` 抑制斗篷，反伤屏障已覆盖 `3700` 恶魔胸甲；显性附属持续结界已覆盖 `1808` 公牛耐力、`1813` / `1911` 神力加护、`1816` 身心俱疲、`1914` 巨熊力量、`1916` 体肤重生与 `1917` 犀牛兽皮；区域结界已覆盖 `1913` 圣佑领地；附属防御已覆盖 `1809` 灵蛇反射与 `1818` 原力法剑；`1903` 反戈一击已覆盖可见附属授予反击及首次反击消费；`1825` 厄运、`1901` 法力失效和 `1904` 攻击逆转已覆盖强制展示、响应前支付、对应取消 / 逆转结算、来源结界摧毁与弃牌；其中 `1825` 额外覆盖法师 / 生物锚点、法力返还和准备牌返还；`结界窃取` 覆盖显性附属结界目标、新合法目标、双重总费用 X 校验、控制权转移、锚点迁移和非法目标拒绝；庇护已覆盖对象攻击、普通攻击法术和连锁闪电的最高来源减骰。剩余 1 张结界为 `1804` 法师祸咒，仍缺非玩家生物施法者来源；多响应优先级、墙体通行伤害额外费用、毒素净化类法术和逐卡特殊效果仍保持 `needs-code` 缺口 |
| 空间区域 / 墙体 | `zones.ts` + 游戏层 `ArenaZone/WallObject` | P1 | 2x3 可先游戏层；标准 12 区和墙体边界需结构化数据 |
| FX 播放 | `engine/fx` + EventStream | P0 | 需要 `SpellFxDescriptor` 映射表；命令失败只播失败/取消反馈 |
| Phaser | 后续专门 FX 层候选 | P2 | 本 foundation 不引入 |

## 基础规则语义覆盖矩阵

| 规则语义 | 基础版必要性 | 状态真相 | 命令 / 事件 / 结算路径 | UI 承接对象 | 验证证据 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 2 人正式标准竞技场 setup | 基础版必需 | `MageWarsCore.players`、`arena` | `setup()` | Board 显示法师状态与完整 4x3 区域 | `smoke.test.ts` | done-foundation |
| 回合阶段顺序 | 基础版必需 | `sys.phase` + `MAGE_WARS_PHASE_ORDER` | FlowSystem `ADVANCE_PHASE` | phase chip | `domain-flow.test.ts`、`smoke.test.ts` | done-foundation |
| 法术书与计划法术 | 基础版必需 | `mage-wars.config.json`、准备槽、私有视角 UI | 配置包法术书读取 / foundation 准备槽 / 计划显示；完整计划交互后续 | 私有法术书/计划区 | `config-package.test.ts`、`smoke.test.ts`、真实 Board E2E | done-foundation-visual / config-runtime-source |
| 逐卡法术能力缺口 | 基础版必需 | `domain/abilityCatalog.ts` + `mage-wars.config.json` | 91 个 `mw.spell.<CardID>` 能力 ID；当前配置包标记 90 张已实现，其中攻击法术 10/10、生物 24/24、魔物 1/1、咒语 18/18、装备 14/14、结界 23/24。武器装备攻击已覆盖 `3701` 狱火长鞭、`3704` 奥秘法杖和 `3706` 阿希拉法杖，`3710` 群兽法杖已覆盖兽王限制、快速能力、回合一次、近战 +2 / 2 骰治疗，`3716` 元素魔杖已覆盖首次绑定和快速施法支付 3 点法力替换绑定；来袭攻击费用已覆盖 `3705` 抑制斗篷，反伤屏障已覆盖 `3700` 恶魔胸甲；显性附属持续结界已覆盖公牛耐力、`1813` / `1911` 神力加护、身心俱疲、巨熊力量、体肤重生与犀牛兽皮；区域结界已覆盖 `1913` 圣佑领地；附属防御已覆盖 `1809` 灵蛇反射与 `1818` 原力法剑；`1903` 反戈一击已覆盖可见附属授予反击及首次反击消费；`1825` 厄运、`1901` 法力失效和 `1904` 攻击逆转已覆盖强制展示、响应前支付、对应取消 / 逆转结算、来源结界摧毁与弃牌；其中 `1825` 额外覆盖法师 / 生物锚点、法力返还和准备牌返还；`结界窃取` 覆盖显性附属结界目标、新合法目标、双重总费用 X 校验、控制权转移、锚点迁移和非法目标拒绝；庇护已覆盖对象攻击、普通攻击法术和连锁闪电的最高来源减骰。剩余 1 张结界为 `1804` 法师祸咒，仍缺非玩家生物施法者来源；多响应优先级、墙体通行伤害额外费用、毒素净化类法术和逐卡特殊效果仍未闭合 | 施法执行器 / 配置审查表 | `ability-catalog.test.ts`、`domain-flow.test.ts`、`enchantment-response.test.ts` | in_progress-partial-executors / enchantment-response-runtime-covered |
| 场上对象行动 | 基础版必需 | `core.objects[*].actionReady`、`zone.objectIds`、`core.objects[*].armor`、`attackProfileId` 命令输入、`attackOrTraitLine` 重生特性 | `MOVE_ARENA_OBJECT` / `GUARD` / `DECLARE_OBJECT_ATTACK` / `ARENA_OBJECT_MOVED` / `ARENA_OBJECT_ATTACK_DECLARED` / `GUARD_REMOVED` / `DAMAGE_DEALT` / `STATUS_TOKEN_PLACED` / `STATUS_TOKEN_REMOVED` / `ARENA_OBJECT_REGENERATED` / `ARENA_OBJECT_DEFEATED` / `ACTION_READINESS_RESET`；对象目标伤害通过护甲修正进入 `DamageCalculation`；己方 ready 生物可用 `GUARD` 进入守卫，消耗自身行动而不占用法师行动；对象攻击以 `attack-0` / `attack-1` 等显式 profile 承接近战/远程距离；攻击条效果骰可放置燃烧 / 眩晕 / 昏迷 / 腐化 / 虚弱 / 残废状态 token，且毒素状态不会落到魔物或非活体对象；眩晕对象攻击会先检定失手，当前行动阶段结束时移除当前行动方法师和己方生物的全部眩晕；昏迷对象不能普通移动或攻击，获得昏迷时移除守卫，当前行动阶段结束时移除当前行动方法师和己方生物的全部昏迷；虚弱会在法师基础近战和对象攻击掷骰前减少非法术攻击骰，最低 1 颗，法术攻击不受影响；残废对象执行普通移动行动时由校验层直接拒绝，推斥 / 传送仍走各自事件，当前行动阶段结束时为当前行动方携带残废的生物做 7+ 逃逸检定，成功时移除残废；燃烧会在维持阶段逐枚投攻击骰，非空白结果造成直接伤害，空白结果移除对应燃烧 token；腐化会在维持阶段让法师和活体场上对象每层受到 1 点直接伤害，非活体对象跳过且 token 保留；沉睡会在实际受伤后替换成同层眩晕，0 伤害不触发替换；攻击条 `穿刺+N` 抵消目标对象护甲，不增加基础伤害；三连击在一次行动内生成多段独立攻击伤害；近战对象攻击选中守卫后会移除守卫标记，即使眩晕失手也移除，且不消耗目标守卫对象行动标记；维持阶段重生按最高值移除伤害 | 当前 Board 已能显示对象；对象直选 UI 与攻击 profile UI 后续接入 | `domain-flow.test.ts` | done-domain-slice / object-armor-covered / object-attack-profile-explicit / object-attack-effect-die-status-covered / daze-attack-miss-covered / daze-action-end-removal-covered / stun-basic-action-lock-covered / stun-action-end-removal-covered / stun-guard-removal-covered / object-attack-pierce-armor-covered / object-triple-strike-covered / object-regeneration-covered / rot-status-token-covered / rot-upkeep-direct-damage-covered / burn-upkeep-tick-covered / sleep-damage-daze-replacement-covered / weak-cripple-status-token-covered / weak-attack-dice-effect-covered / cripple-move-lock-covered / cripple-escape-check-covered / guard-melee-removal-covered / guard-object-command-covered / ui-interaction-pending |
| 可见施法必须有 FX | 基础版必需 | EventStream + FX descriptor | `MW_SPELL_CAST_RESOLVED` 等事件映射 | 竞技场/对象锚点 | `event-fx-mapper.test.ts`、`Board.fx.test.tsx` | done-foundation |
| 攻击与伤害 | 基础版必需 | foundation 攻击命令和伤害事件 | `DECLARE_ATTACK` / `DAMAGE_DEALT` | 攻击目标、骰盘、受击对象 | `domain-flow.test.ts`、E2E 截图骰盘 | done-foundation |
| 守卫和常见状态 | 基础版必需 | 守卫命令、token 资源和对象标记 | `GUARD` / 守卫状态 | 对象本体标记 | `domain-flow.test.ts`、真实 Board E2E | done-foundation |
| 隐藏结界 / 隐藏信息边界 | 基础版必需 | playerView / 卡背边界 | 对手已计划法术、未公开法术书内容和隐性结界以卡背 / 数量呈现；完整隐藏结界执行器后续 | 附着区 + 卡背 / reveal FX | 真实 Board E2E、v6 历史审计 | done-foundation-boundary |
| 素材正式接线 | 基础版必需 | `public/assets/i18n/zh-CN/mage-wars/**` + atlas configs | 压缩 / manifest / `OptimizedImage` / `CardPreview` / server + Android 回查 | 正式 Board / Open Design artifact | `runtime-resource-chain-audit.md`、E2E | done-foundation |

## 2026-08-04 结界反制与攻击逆转响应窗口切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `apprentice-card-field-contract.md` 锁定 `1825` 厄运、`1901` 法力失效和 `1904` 攻击逆转的强制展示时机；施法链在法术反制阶段中断，攻击链在回避攻击阶段中断。`1804` 法师祸咒的触发条件另要求生物作为施法者。 |
| 引擎能力 | `ResponseWindowSystem` 新增可选 `requiredInteractionId`，强制交互完成前拒绝 `RESPONSE_PASS` 和取消；`createBaseSystems` 通过配置注入响应系统，普通可选响应行为不变。 |
| 续链 owner | `responseResolution.ts` 对施法 / 攻击上下文做结构化读取，并以 `resolutionFrame` 作为跨响应窗口的唯一续链 owner；交互只保存响应对象的可验证引用，不保存第二份攻击或施法主状态。 |
| 已覆盖 | `1901` 仅在对手控制的咒语 / 结界目标命中附属实体生物时触发；展示后反制原法术并摧毁来源结界。`1904` 在可回避攻击时交换来源 / 目标并保留原攻击 profile，在无法回避时只摧毁来源结界并继续原攻击。展示完成后响应窗口自动关闭。 |
| 未覆盖 | `1804` 法师祸咒仍需要独立的非玩家生物施法者来源；多响应优先级、完整隐藏结界实例化、完整卡牌实例化、法师附属结界的完整 UI 和其它逐卡效果仍未闭合。 |
| 配置裁定 | `1825`、`1901`、`1904` 已由 `requiresCodeSupport=true` 改为 `false`；仅 `1804` 仍保留 `requiresCodeSupport=true`。当前 91 张学徒法术统计为 `90 implemented / 1 needsCodeSupport`。 |
| 验证证据 | `src/games/mage-wars/__tests__/enchantment-response.test.ts` 9 项、`ability-catalog.test.ts`、完整 Mage Wars 测试共 `247 passed`，引擎响应窗口测试 4 项通过，TypeScript 和定向 ESLint 通过；响应链覆盖法师 / 生物锚点、快速 / 标准法术边界、费用、准备牌、弃牌堆、强制展示、旧交互、错误响应者、重复响应、错误来源和 frame 删除。 |

## 2026-08-04 非玩家生物施法者来源切片补充

| 项 | 当前结论 |
| --- | --- |
| 规则真相源 | `apprentice-card-field-contract.md` 的 `1804 法师祸咒` 明确要求“本生物施放并结算一个法术”后受 1 点直接伤害；规则书第 13、18 页进一步定义魔宠与再生点可以施法，并在施法过程中作为施法者。当前学徒配置没有这些来源卡，因此不能把普通生物或法师玩家 ID 猜作实际生物施法者。 |
| 来源模型 | `MageWarsSpellCasterRef` 区分 `mage` 和 `arena-object`；场上生物只有在配置存在 `spellcastingSource.abilityId` 时才是合法非玩家施法者。施法开始 / 结算事件与结界响应 frame 都携带实际施法者引用。 |
| `1804` 运行时 | `SPELL_CAST_RESOLVED` 带合法生物施法者引用时，交互系统按当前对象关系查找附属 `1804`，通过 `DamageCalculation` 生成 1 点 `damageScope=direct` 的 `DAMAGE_DEALT`；法师来源、无施法能力生物、错误控制者和已离场对象不触发。 |
| 当前缺口 | 规则有正式的魔宠 / 再生点来源，但当前 91 张学徒配置没有对应来源卡；运行时也没有对象独立计划、对象法力优先支付和魔宠行动 / 再生点部署施法入口。因此 `1804` 仍保留 `requiresCodeSupport=true`。事件层已覆盖法师来源、错误来源、反制 / 取消前不触发、施法者离场和多个 `1804` 叠加；真实魔宠 / 再生点命令入口及其完整生命周期仍待后续提案。Workshop 已命中巢穴 `2218`、乌鸦魔宠胡金 `2908`、花仙魔宠菲蕾拉 `2905`、灵婴魔宠瑟希利克斯 `3017`，但这些只是候选真相源，不等于已接入基础配置。 |
| 验证证据 | `spell-caster-source.test.ts` 5 项通过，覆盖来源解析、成功结算直接伤害、双来源叠加、成功结算前不触发、法师 / 普通生物排除和陈旧来源；完整 Mage Wars 测试 9 files / 252 tests 通过，TypeScript、全目录 ESLint 和 OpenSpec 提案校验通过。 |

> 上表是 2026-08-04 非玩家生物施法者来源切片的历史快照；其“当前缺口”描述的是该切片完成时的状态，不覆盖后续独立提案。

## 2026-08-04 魔宠与再生点独立施法接入后的当前状态

| 项 | 当前结论 |
| --- | --- |
| 首批来源卡 | `2908 乌鸦魔宠胡金` 与 `2218 巢穴` 已进入 `mage-wars.config.json`；`2905 花仙魔宠菲蕾拉`、`3017 灵婴魔宠瑟希利克斯` 仍保持字段锁定 / deferred，不加入本批运行时配置。 |
| 对象计划与支付 | `PLAN_OBJECT_SPELL` 为来源对象建立独立单张计划；对象法力先支付，法师法力补足；法师的两张计划法术槽不被占用。 |
| 施法阶段与范围 | 魔宠只在 `creatureAction` 施法，再生点只在 `deployment` 施法；施法范围从真实来源对象所在区域计算。来源对象行动消耗与再生点召唤入口分别复用既有对象行动 / 召唤链。 |
| 生命周期 | 成功施放进入弃牌堆；反制 / 取消恢复计划和已支付法力；来源在结算前被摧毁时计划随来源消失；回合结束未施放计划返还。 |
| 信息边界 | 控制者可见对象计划牌 ID；对手只看到对象旁的计划数量，不泄露牌面身份。 |
| `1804` 法师祸咒 | 仅在真实带施法来源的竞技场对象成功结算法术后触发；法师施法、普通生物、反制 / 取消和来源失效路径不触发。 |
| 验证证据 | `familiar-spellcasting.test.ts` 4 项、定向相关测试共 26 项、Mage Wars 全量 10 files / 258 tests、TypeScript、Mage Wars 定向 ESLint 和 `add-mage-wars-familiar-spellcasting` OpenSpec 严格校验通过。`2218` / `2908` frame 已进入现有正式 deck 图集，atlas manifest 增量校验通过；完整 `--full` 镜像校验因共享 manifest 的远端保留键不适用。 |

## 暂不抽共享层

- 区域/墙体模型、法术 FX descriptor、隐藏 attachment 都像候选共享能力，但本轮没有用户授权“百游戏模式”。
- 本轮只在 `mage-wars` 内记录对象模型和缺口；后续若两个以上游戏需要同类能力，再拆共享 change。
