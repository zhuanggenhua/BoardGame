# 审计 Evidence 模板

> 适用：凡是要对外宣称“已审计 / 已验证 / 已收口 / 旧结论失效”的对象，都应基于此模板落地到 `evidence/`。

## 1. 基本信息

- 对象：
- 日期：
- 作者：
- 文档类型：`audit` / `rollup` / `closeout` / `invalidation`
- 关联需求 / PR / 任务：

## 2. 审计范围

- 本轮覆盖的文件：
- 本轮覆盖的卡牌 / 能力 / 模块：
- 本轮覆盖的共享链路：
- 明确不在本轮范围内的对象：

### 2.1 对象级完成门禁（默认强制）

若标题、任务名或对外汇报中使用“审计 / 全面审计 / 深入审计 / 收口审计 / 做到底”，则本文档默认必须满足：

- 当前范围内每个对象都有独立审计行，不能只写“派系 A / 模块 B 总评”；
- 每个对象都拆出规则子句 `C1/C2/C3...`，逐项写清实现入口与证据；
- 每个对象都给出 `L0/L1/L2/L3/L4` 当前层级或阻塞原因；
- 只有“共享链路完全同构，仅配置不同”的对象，才允许复用代表链；
- 若仍有对象级 L3/L4 缺口，结论只能写 `代表性玩法已验证` 或 `仍有残余范围`，不得写 `当前发布口径已收口`。

## 3. 结论等级

从以下五项中明确选择一项，禁止写模糊结论：

- `结构审计通过`
- `代表性玩法已验证`
- `当前发布口径已收口`
- `仍有残余范围`
- `旧结论失效`

判定理由：

## 4. 权威来源

- 主真相源：
- 对照源：
- 关键规则原文 / 裁定：
- 若图片/玩家板/棋盘/卡图/图集可直接判定合同：必须补“图上实际看到什么”，不能写推测。

### 4.1 图片合同表（有图可判定时强制）

| visualRegion / slotId | 图上对象 | 运行时对象 | 允许状态 | 是否可交互 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 示例：`sky` | 左下独立被动槽 | `quiet-cultivation` | `passive` | 否 | 一致 |

## 5. 逐项结论

每个对象至少一条，禁止只写总评。推荐表头：

| 对象 | 规则子句 | 实现入口 | 共享链路 / 复用依据 | 命中维度 | 证据层级 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 示例 | `C1 打出时` `C2 选择 1 个基地并移动` | `src/...` | `shared-move-base / 与 foo 同 handler，同 skip 路径，仅筛选参数不同` | `D1 D5 D8` | `L1/L2/L3` | `代表性玩法已验证` |

补充要求：

- `规则子句` 不能只写对象摘要，必须覆盖时机、主效果、替代入口、额外触发、例外/否则、once/turn、清理。
- `共享链路 / 复用依据` 为空时，表示该对象未复用代表链；若复用，必须写清复用对象、共享链路 ID、判等依据和剩余差异。
- 任一对象缺独立行，即视为未审。

### 5.1 对象级层级矩阵（推荐强制）

| 对象 | L0 | L1 | L2 | L3 | L4 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 示例 | `passed` | `passed` | `passed` | `reused: foo` | `pending` | `仍有残余范围` | `L4 还缺 reaction session` |

## 6. 验证证据

### L1 结构证据

- 命令：
- 结果：
- 结论：

### L2 领域行为证据

- 命令：
- 结果：
- 结论：

### L3 真实玩法证据

- 真实入口：
- 操作链路：
- E2E / 手工截图 / 录像路径：
- 人工观察结论：
- 若本轮涉及图片合同：必须至少有 1 张能直接看到对应区域本体的真实截图，禁止只截外围容器或旁边 UI。
- 若本条是复用代表链：必须写清被复用对象、共享链路 ID、为什么判定为“仅配置不同”。

### L4 治理证据

- 残余范围：
- 共享根因：
- 是否需要降级旧 summary：
- 若链路涉及 reaction / deferred / finalize / afterScoring / beforeScoring：必须补 `finalState / triggerQueue / reaction session` 或等价权威状态证据。

## 7. 禁止假阳性检查

- 是否误用“选择页 / 横幅 / 静态展示 E2E”充当玩法收口：
- 是否误用“测试里出现 id / registerAbility 覆盖”充当行为完整：
- 是否误用“注入型 interaction E2E”充当真实入口玩法证据：
- 是否只证明 prompt 出现、未证明最终权威状态变化：

## 8. 共享根因与残余范围

- 共享根因项：
- 对象级局部问题：
- 未审家族 / 未覆盖交互链：
- 当前不能宣称整体收口的原因：
- 已登记的合法代表链复用：

## 9. 修订 / 失效记录

- 旧文档路径：
- 旧结论：
- 失效原因：
- 替代旧结论的新证据：
- 新增回归 / 新增真实入口验证：

## 10. 对外汇报口径

- 允许说：
- 禁止说：

### 10.1 口径速记

- 只有当对象全集、子句矩阵、L0-L4 证据、复用依据、残余范围都齐全时，才允许说 `已审计完成 / 当前发布口径已收口 / 没有死角`。
- 只做了抽样、代表链、高风险对象或部分对象时，必须降级成 `抽样审计`、`代表性玩法已验证` 或 `仍有残余范围`。
