## 1. Specification
- [x] 1.1 为 `game-ai-system` 增加 AI 语义 hints、适配边界、trace 契约 requirements
- [x] 1.2 为 `interaction-system` 增加 interaction option AI-only hints 传递 requirement

## 2. Validation
- [x] 2.1 运行 `openspec validate update-turn-based-ai-framework-semantics --strict --no-interactive`
- [x] 2.2 确认该 change 与现有 AI change 的边界说明清晰，不重复定义已有 requirement
