## 1. 数据模型与接口
- [x] 1.1 新增用户音频设置数据模型，按 `userId` 唯一存储音频偏好与可选 `bgmSelections`
- [x] 1.2 新增 `user-settings` 模块，提供 service / controller
- [x] 1.3 实现登录态读取与更新接口（`GET /auth/user-settings/audio`、`PUT /auth/user-settings/audio`）
- [x] 1.4 加入输入校验（`0~1` 音量范围）与鉴权保护
- [x] 1.5 补充后端测试，覆盖读取、更新、未登录拒绝与参数校验

## 2. 前端同步与迁移
- [x] 2.1 新增前端用户设置 API 客户端
- [x] 2.2 登录后拉取服务器设置并应用到当前会话
- [x] 2.3 服务器无设置时，把当前本地偏好迁移到服务器
- [x] 2.4 登录态远程 apply 不覆盖游客本地缓存，登出时恢复游客本地偏好
- [x] 2.5 登录态下后续设置变更同步写回服务器；未登录时继续只写本地缓存
- [x] 2.6 补充前端测试，覆盖 API 读写与登录同步路径

## 3. 验证
- [x] 3.1 已补齐对应 API 与前端单测
- [x] 3.2 本 change 归档按“实际实现完成度”判定，不以额外手工验证作为阻塞项
