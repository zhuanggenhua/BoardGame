## 1. 旧回退链路退役
- [x] 1.1 将本变更从“服务器主源 + 对象存储回退”改为“服务器唯一线上源”。
- [x] 1.2 删除或退役对象存储上传、下载、同步、备份和容量门禁脚本入口。
- [x] 1.3 后台发布 ready 状态改为检查服务器资源发布入口，不再检查对象存储配置。
- [x] 1.4 现行部署文档和 Android 发布 skill 改为服务器主源口径。

## 2. Worker 与发布脚本
- [x] 2.1 Worker 删除对象存储回退读取，源站失败返回显式错误。
- [x] 2.2 Worker 支持 CORS `OPTIONS` 预检，并返回可诊断来源头。
- [x] 2.3 OTA 发布后等待 bundle 和 `latest.json` 公开域名可读。
- [x] 2.4 OTA 发布校验 `latest.json` 正文大小、SHA-256 和 CORS 预检。
- [x] 2.5 移动素材包和原生更新发布不得生成对象存储灾备队列。

## 3. 客户端失败语义
- [x] 3.1 Android OTA 清单读取改为“存在 / 不存在 / 读取错误”三态。
- [x] 3.2 只有 `404` 能解释为清单不存在；网络、跨域、超时、非 OK、非 JSON 和 JSON 解析失败都必须显式报错。
- [x] 3.3 强制更新遇到清单读取错误时显示阻塞错误态。
- [x] 3.4 后台自动检查遇到清单读取错误时给出可追踪提示，不静默降级。

## 4. 验证
- [x] 4.1 增加 Worker 不读取对象存储兜底的测试。
- [x] 4.2 增加服务器资源等待脚本的 CORS 预检测试。
- [x] 4.3 增加 OTA 清单读取失败必须返回显式错误的测试。
- [x] 4.4 运行本变更 OpenSpec 严格校验。
- [x] 4.5 扫描现行代码、脚本、文档和活跃规范，确认不再保留对象存储回退或发布前置口径。
