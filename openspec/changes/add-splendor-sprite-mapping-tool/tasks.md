# 实现任务清单

## 1. 映射数据单一来源
- [x] 1.1 为 `splendor` 新增独立映射配置文件，承载卡牌/贵族 frame 与模型 ID 的对应关系
- [x] 1.2 调整 `src/games/splendor/sprites.ts`，改为从映射配置生成运行时索引
- [x] 1.3 更新 `sprites.test.ts`，校验映射配置的完整性、唯一性与 tier 一致性

## 2. DevTool：页面化校对/配置能力
- [x] 2.1 在 `assetslicer` 中新增 `splendor` 雪碧图映射模式入口
- [x] 2.2 支持选择图集类型（一级/二级/三级/贵族）
- [x] 2.3 左侧展示雪碧图格子，右侧展示对应卡牌/贵族模型列表
- [x] 2.4 支持在页面中为格子分配/替换模型 ID
- [x] 2.5 实时显示未映射、重复映射、缺失模型等校验结果

## 3. 导入导出与校对流程
- [x] 3.1 支持从当前映射配置加载初始状态
- [x] 3.2 支持导出映射结果（至少包含可直接回填仓库的结构化数据）
- [x] 3.3 支持保留本地草稿，避免页面刷新后丢失人工校对进度

## 4. 文档与验证
- [x] 4.1 更新 `public/assets/splendor/README.md`，说明新工具页的使用流程
- [x] 4.2 更新 `public/assets/splendor/sprite-mapping.md`，明确文档与工具/配置文件的职责关系
- [x] 4.3 补充测试与证据，验证运行时仍能正确渲染、工具页能发现映射问题
