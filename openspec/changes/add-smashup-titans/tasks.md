## 1. 领域建模

- [ ] 1.1 为大杀四方新增 `TitanCardDef`、`TitanState`、显式 `CardType='titan'`，并清理现有 `activeTitan` 临时读取点。
- [ ] 1.2 在选派系初始化后，把已选派系对应泰坦放入 `setaside` 区，而不是牌库/手牌/弃牌堆。
- [ ] 1.3 增加统一的泰坦查询与 helper：查控制者当前泰坦、查基地上的泰坦、查泰坦力量贡献、移除泰坦出场。

## 2. 命令与事件

- [ ] 2.1 增加泰坦打出/移动/离场/冲突/指示物相关事件与类型。
- [ ] 2.2 让能力层通过统一 helper 打出和移动泰坦，而不是手写裸事件。
- [ ] 2.3 在 `postProcessSystemEvents` 中统一处理 titan clash 和相关后续逻辑。

## 3. 规则整合

- [ ] 3.1 将泰坦纳入力量计算、计分资格、基地清场、基地离场等公共流程。
- [ ] 3.2 扩展目标选择与保护/限制逻辑，使“target any card”类效果可以显式处理泰坦，而“target minion/action”仍不会误伤泰坦。
- [ ] 3.3 为“可按随从/行动方式打出”的泰坦补显式出牌语义，让“选择一个随从/行动打出”可以纳入候选，但不改变 `titan` 牌种。
- [ ] 3.3 为未来 Kaiju Island 预留显式的 `allowMultipleTitans` 基地配置，不写死特殊分支。

## 4. 房间扩展开关与 setup 契约

- [ ] 4.1 为 `GameSetupField` 新增通用 `multi-select` 契约，并补创建房间 UI 渲染逻辑。
- [ ] 4.2 在创建房间弹窗中实现“多选下拉 + 已选标签 + 标签叉号取消 + 默认全选 + 允许清空”的统一交互。
- [ ] 4.3 在 `GameDetailsModal` / `setupData` / Smash Up `setup()` 中打通扩展开关传递，支持按房间配置启停 TITANS。

## 5. 首批官方泰坦接入

- [ ] 5.1 为当前仓库已实现且有官方泰坦的 10 个派系补齐泰坦卡牌定义与文案键。
- [ ] 5.2 实现这些泰坦的 summon 条件、ongoing、special、talent 与必要交互。
- [ ] 5.3 更新受泰坦影响的既有派系能力逻辑，改为调用正式泰坦查询接口。

## 6. UI 与文档

- [ ] 6.1 在大杀四方 UI 中展示 set-aside 泰坦与在场泰坦，并补充必要的交互提示。
- [ ] 6.2 按新口径摆放泰坦：己方可用泰坦放在牌库右侧；基地上的泰坦优先放在持续行动上方一排，没有持续行动时放在基地上方。
- [ ] 6.3 更新 `src/games/smashup/rule/大杀四方规则.md`，补齐项目中的泰坦通用规则与实现范围说明。
- [ ] 6.4 如新增公共契约或 helper，补充对应开发文档说明。

## 7. 验证

- [ ] 7.1 在现有 Smash Up 测试文件中补领域测试，覆盖泰坦初始化、打出、移动、clash、离场、计分清场。
- [ ] 7.2 为至少 1 个典型泰坦交互补 E2E，用 TestHarness 构造场景、运行通过并检查截图。
- [ ] 7.3 创建 `evidence/smashup/smashup-titans-e2e-test.md`，附截图分析与验证结论。
