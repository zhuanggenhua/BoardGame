## 1. Spec
- [x] 1.1 新增 `qidahen-formal-core-loop` spec delta，写清正式主循环、手牌一级入口和地区状态语义拆分要求。
- [x] 1.2 新增 `qidahen-tutorials` spec delta，写清教程章节、规则覆盖面和截图证据门槛。
- [x] 1.3 运行 `openspec validate refactor-qidahen-formal-core-loop-and-tutorial-coverage --strict --no-interactive` 并通过。

## 2. Formal Runtime
- [x] 2.1 盘点正式局当前手牌真相源，并把“全部 unknown 壳”提升到最小可追预览身份。
- [x] 2.2 继续补齐正式手牌真相映射，明确哪些牌能建立稳定规则级 `cardKind / cardDefId / armamentId`，哪些仍缺素材或规则映射。
  - 2026-07-02 已核 TTS Workshop `deckId 13 / 16 / 17` 与 `26 / 27 / 28`：前者缺逐牌 `Nickname / Description`，后者是 1x1 军备状态对象，不能当普通手牌军备牌真相源。
- [x] 2.3 把正式进行页的手牌直点入口与 badge 判定收口到领域层，不再由 `Board.tsx` 本地硬编码。
- [x] 2.4 把 `执行事件 / 升级军备 / 势力行动` 收口成规则书口径的手牌行动一级入口，不再停留在“只有少量已识别牌能直点，其余退回抽象按钮”的长期状态。
  - 2026-07-02 阻塞证据：运行时 faction atlas preview 仍是 16-frame seam（`cardAtlas.ts` 的 `buildFrames(topXs, leftYs)` + `QIDAHEN_FACTION_HAND_PREVIEW_COUNT = 16`），不是完整 10x7 逐牌合同；普通事件、军备、战术、银两仍需逐牌牌表/OCR 真相源后才能勾选。
  - 保持未完成：TTS 核验没有发现可直接作为普通 `事件 / 军备 / 战术 / 银两` 真相源的逐牌定义；不得把局部可识别牌或教程注入态当正式入口闭环。
  - 2026-07-02 已将本地候选审计沉淀到可提交文档：`docs/games/qidahen/records/qidahen-hand-card-truth-source-candidates.md`。结论只证明大明 17 张、后金 15 张、蒙古 17 张牌面可能可读，仍缺逐牌中文牌名、类型、效果与军备目标映射；本地 `tmp/` 证据只是辅助输入，不能作为长期交付物或 2.4 完成依据。
  - 2026-07-02 EasyOCR 已对 49 张安全标题裁切生成候选优先级：high 16、medium 7、low 19、unreadable 7；但 OCR 存在明显错读，只能辅助人工录入排序，不能作为正式中文牌名、牌类、效果或军备目标真相源。
  - 2026-07-02 已新增可提交人工录入矩阵：`docs/games/qidahen/records/qidahen-hand-card-manual-entry-matrix.md`。矩阵只把 OCR 候选转成 49 行待复核工作台，当前没有任何一行达到“已确认”，因此仍不能修改正式手牌规则映射。
  - 2026-07-02 已新增 high 优先级核读索引：`docs/games/qidahen/records/qidahen-hand-card-high-priority-title-review.md`。16 张 high 候选主要是人物/非普通手牌标题，不能补普通事件、军备、战术、银两映射，2.4 仍保持未完成。
  - 2026-07-02 已新增 medium 优先级核读入口：`docs/games/qidahen/records/qidahen-hand-card-medium-priority-title-review.md`。7 张 medium 候选只完成安全标题小裁切索引和本地辅助图册整理，尚无人工确认的中文牌名、牌类、效果或军备目标，不能作为 2.4 完成依据。
  - 2026-07-02 已新增 low 优先级核读入口：`docs/games/qidahen/records/qidahen-hand-card-low-priority-title-review.md`。19 张 low 候选只完成安全标题小裁切索引和本地辅助图册整理，OCR 置信度更低且错读风险更高，尚不能作为正式手牌规则真相源。
  - 2026-07-02 已新增 unreadable 候选核读入口：`docs/games/qidahen/records/qidahen-hand-card-unreadable-title-review.md`。7 张 unreadable 候选 OCR 置信度为 0，只完成安全标题小裁切索引和本地辅助图册整理，只能作为外部 OCR 或人工复核入口。
  - 2026-07-02 已重新做 TTS 结构化字段复核：`deckId 13 / 16 / 17` 的正式牌库对象仍缺逐牌 `Nickname / Description`；`deckId 26 / 27 / 28` 仍只是 1x1 军备状态对象；其他带名称对象主要是积分、辅助卡、骰子、控制标记、部队或地区标记，不能作为普通事件、军备、战术、银两逐牌规则表。
  - 2026-07-02 已新增 EasyOCR 批量牌面复核记录：`docs/games/qidahen/records/qidahen-hand-card-easyocr-batch-review.md`。49 张候选均有牌面正文 OCR 文本，43 张有标题 OCR 文本，但结果仍有明显错读，只能辅助人工录入，不能直接作为正式中文牌名、牌类、效果或军备目标真相源。
  - 2026-07-02 已新增 OCR 关键词分流记录：`docs/games/qidahen/records/qidahen-hand-card-ocr-keyword-triage.md`。25 张普通手牌关键词命中全部同时命中人物或剧本/纪年线索，说明“手牌/打出”等词会出现在非普通手牌正文里，不能作为普通事件、军备、战术或银两牌判定依据。
  - 2026-07-02 已新增非普通手牌排除候选清单：`docs/games/qidahen/records/qidahen-hand-card-nonordinary-exclusion-candidates.md`。37 张候选命中人物或剧本/纪年线索，普通牌类词独占命中为 0；该清单只能缩小人工复核范围，不能替代逐牌人工确认。
  - 2026-07-02 已新增剩余人工复核队列：`docs/games/qidahen/records/qidahen-hand-card-remaining-manual-review-queue.md`。当前把 49 张候选缩到 12 张仍需人工确认的候选，但这些候选没有普通牌类词命中，仍不能自动判为普通事件、军备、战术或银两牌。
  - 2026-07-02 已新增剩余 12 张二次文本分流：`docs/games/qidahen/records/qidahen-hand-card-remaining-review-subtriage.md`。结果为疑似人物牌文本 5 张、低信息/疑似牌背或空白 6 张、仍需人工看图 1 张；仍没有任何候选可直接确认为普通事件、军备、战术或银两牌。
  - 2026-07-02 已新增最后未分流候选安全 OCR 记录：`docs/games/qidahen/records/qidahen-hand-card-final-candidate-safe-ocr.md`。`mongol_r07_c10` 的小尺寸变体 OCR 仍没有稳定中文牌名、牌类、效果或军备目标，只能保留为人工看图或外部 OCR 候选。
  - 2026-07-03 已新增剩余候选人工复核清单：`docs/games/qidahen/records/qidahen-hand-card-human-review-checklist.md`。12 张候选已固定人工确认字段与排除字段，但没有任何候选达到普通事件、军备、战术或银两的正式规则映射门槛，2.4 仍保持未完成。
  - 2026-07-03 已新增规则 PDF 文本复核：`docs/games/qidahen/records/qidahen-hand-card-rule-pdf-review.md`。规则 PDF 只确认牌类说明和手牌行动分支，没有逐张普通事件、军备、战术、银两的牌名、效果或军备目标，不能作为 2.4 完成依据。
  - 2026-07-03 已新增外部文本来源搜索记录：`docs/games/qidahen/records/qidahen-hand-card-external-source-search.md`。本轮搜索没有找到可追溯逐牌牌表，不能补普通事件、军备、战术或银两的正式规则映射。
  - 2026-07-03 已新增结构化素材来源复查：`docs/games/qidahen/records/qidahen-hand-card-structured-source-recheck.md`。资源清单只有文件路径层级，TTS JSON 仍只有 CardID 顺序、图集键、辅助卡和军备状态对象名称，不能提供普通事件、军备、战术或银两的逐牌规则字段。
  - 2026-07-03 已新增完成依据决策矩阵：`docs/games/qidahen/records/qidahen-hand-card-truth-source-decision-matrix.md`。所有已核来源都不能单独或合并关闭 2.4；下一步只能是找到可追溯逐牌牌表、可靠 OCR 后人工确认，或完成可审计人工录入。
  - 2026-07-03 已新增正式映射反写契约：`docs/games/qidahen/records/qidahen-hand-card-formal-mapping-contract.md`。契约明确人工确认结果未来应如何落到 `cardKind / cardDefId / armamentId` 与已有 `QidahenArmamentId`；2026-07-04 之前“没有确认行可反写”的旧结论已被 atlas05 新发现取代。
  - 2026-07-03 已新增人工录入反写校验脚本：`scripts/verify/qidahen-hand-card-manual-entry.mjs`。脚本检查人工录入矩阵、复核清单和完整 CardID 人工录入矩阵中的“已确认”行是否满足字段门槛；当前无确认行时通过但明确输出“不允许反写正式手牌规则映射”。
  - 2026-07-03 已补强人工录入反写校验脚本：当当前没有任何已确认普通手牌行时，脚本会同时检查 OpenSpec `2.4 / 4.5` 仍保持未完成，防止把候选 OCR/人工录入入口误当正式入口闭环。
  - 2026-07-04 已确认 atlas05 普通手牌 49 张，并新增 `src/games/qidahen/domain/ordinaryHandCardIdentities.ts`、`resolveQidahenAtlas05OrdinaryHandCardIdentity(atlasIndex)`、`qidahen:atlas05-ordinary-hand-preview` 图集资源与正式发牌/摸牌接入；定向测试覆盖事件、军备、战术、银两、排除行，以及正式开局/后续摸牌消费 atlas05 `passed` 序列。保持未完成：事件效果全集和战术时机仍未闭环；CardID 1647 / r05c08 边缘窄条不在 TTS DeckIDs 中，只作排除证据，不进入正式映射。
  - 2026-07-04 已修复待结算/战后运行态旧交互快照残留：`PENDING_ACTION_RESOLVED / POST_BATTLE_DECISION_RESOLVED` 后会先清掉当前运行时交互，再从推进后的核心状态重建；同时保留“核心状态残留旧 selection 时优先沿当前 interaction data 续建”的既有合同。定向验证已覆盖攻城教程守城宣告到城战、旧快照续建合同、657 个七大恨相关单测、typecheck 与 OpenSpec strict validate。
  - 2026-07-04 已把 atlas05 已确认普通手牌中文牌名接入运行态手牌 `label`：正式手牌、放大预览、无障碍标签与战术牌摘要不再只显示“某方手牌 N”占位名；定向测试已覆盖初始正式手牌按 atlas05 `displayName` 顺序生成。
  - 2026-07-04 已把 atlas05 已确认普通手牌规则效果摘要接入运行态手牌 `rulesSummary`：49 张确认行现在不仅有中文牌名、牌类、`cardDefId / armamentId`，还携带人工录入矩阵反写的效果摘要；这只是规则真相源字段推进，不代表事件效果全集或战术时机完成。
  - 2026-07-04 已把规则摘要明确写有“不能在攻城、守城时使用”、明确野战限定或明确守城专用的战术牌接入待结算战斗时机门禁：这类牌在当前攻方 `PLAY_TACTIC_CARD` 窗口中会按时机被拦截，野战待结算窗口仍沿既有战术牌链路打出。该步骤只覆盖明确禁用攻城/守城、明确野战限定或明确守城专用的战术牌门禁，不代表战术时机全集、守方响应窗口或逐张战术效果完成。
  - 2026-07-03 已新增 TTS CardID 位置清单：`docs/games/qidahen/records/qidahen-hand-card-tts-cardid-position-map.md`。清单能把 `DeckIDs` 转成图集行列和重复次数，但仍没有中文牌名、牌类、效果或军备目标，只能降低人工复核成本，不能作为 2.4 完成依据。
  - 2026-07-03 已新增 TTS CardID 完整人工录入矩阵：`docs/games/qidahen/records/qidahen-hand-card-tts-cardid-full-manual-entry-matrix.md`。矩阵把 10 个牌组段展开为 143 行出现记录、99 个唯一图集位置；后续已按已有哈希交叉证据和低分辨率安全预览核读结果全部回填为“已排除”，没有任何普通事件、军备、战术或银两确认行，不能作为 2.4 完成依据。
  - 2026-07-03 已完成 TTS `DeckIDs` 与运行时图集交叉核验：`deckId 13 / 16 / 17` 的 FaceURL 哈希分别命中素材清单中的蒙古、纪年、朝鲜整版图集，不等价于 `ming-faction-deck-atlas / mongol-faction-deck-atlas / jin-faction-deck-atlas`；因此 CardID 矩阵不能直接映射到正式手牌预览帧。
  - 2026-07-03 已新增运行时图集候选交叉核验与人工录入矩阵：`docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-tts-crosswalk.md` 与 `docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-manual-entry-matrix.md`。该步骤曾把可能命中正式 faction hand preview atlas 的候选收窄为大明 9、蒙古 14、后金 5，共 28 条；后续低分辨率安全核读已全部排除为非普通手牌，且 TTS 仍缺逐牌 `Nickname / Description`，不能作为 2.4 完成依据。
  - 2026-07-03 已新增运行时图集候选安全复核入口：`docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-safe-review.md`。本地脚本只生成 28 条候选的小尺寸缩略图与标题裁切索引，避免直接读取大图；这仍只是人工/OCR 入口，不能作为 2.4 完成依据。
  - 2026-07-04 已按项目安全读图流程回到完整素材目录 `D:\gongzuo\webgame\gameasset\七大恨 中文mod\Images`，确认 `deckKey 16` 的 atlas05 不是纪年图集，而是普通手牌图集；已生成 `test-results/evidence-image-validation/qidahen-formal-handcard-2.4.json` 与 `docs/games/qidahen/records/qidahen-hand-card-atlas05-manual-entry-matrix.md`，当前已确认 TTS deckKey 16 实际使用的 49 张可正式反写普通手牌（事件、战术、军备、银两），CardID 1647 / r05c08 边缘窄条不在 TTS DeckIDs 中，只作排除证据。当前已完成 49 张确认行的正式运行时接入和定向测试；但不得把局部图集录入和 49 张接入进展当作 2.4 / 4.5 完成。
  - 2026-07-04 已按安全读图抽样复核升级 3 张 atlas05 争议牌：`王公大会`、`火炮技术`、`封贡敕书`。流程为子代理按反写门槛读取单牌裁切并返回结构化结论，主线程再读取 98KB 压缩对照图抽样复核；两边一致后才把三张从 partial/blocked 升级为 passed。`银两` 抽样仍缺可读规则效果摘要，继续保持待复核且不进入正式映射。
  - 2026-07-04 已继续按安全读图抽样复核升级 4 张 atlas05 争议牌：`人参貂皮`、`东北大军`、`蒙古大旱`、`骑兵火器`。流程为子代理按反写门槛读取 300KB 压缩对照图并返回结构化结论，主线程读取同图抽样复核；两边一致后才把四张从 blocked 升级为 passed。`步骑联合`、`铁炮阵` 仍因牌类或正文裁切不完整保持 partial，不进入正式映射。
  - 2026-07-04 已继续用主线程单卡安全复核升级 3 张 atlas05 争议牌：`红衣大炮`、`坚守不屈`、`拒马`。流程为先生成 #10/#35/#36/#37 单卡完整裁图与标题/正文切片，再只读取低体积压缩复核图；当时 `七大恨` 仍因旧裁图正文不足保持 partial，后续已通过完整 10x7 网格重裁复核。
  - 2026-07-04 已继续用低体积单卡对齐样本升级 4 张 atlas05 争议牌：`步骑联合`、`骑兵铁甲`、`西式棱堡`、`战车阵`。流程为先生成 #28/#29/#42/#43/#45/#46 contact sheet、标题合辑与正文合辑，再只把字段完整且与矩阵行列/裁图文件对齐的四张写入正式真相表；`鸟真超哈` 仍因标题不够稳保持 partial，不进入正式映射。
  - 2026-07-04 已修正 atlas05 普通手牌预览帧合同：`qidahen:atlas05-ordinary-hand-preview` 从旧的顶行+左列 preview seam 改为完整 10x7 网格，`atlasIndex` 现在按 `(row - 1) * 10 + (col - 1)` 对齐完整图集格子；同时纠正 `成立蒙八旗` 为 CardID 1607 / atlasIndex 7，并把 `玉匣出土`、`步兵铁甲`、`分进合击`、`链炮阵`、`机里耐步兵`、`鸟真超哈`、`连环火铳` 等字段完整候选写入正式真相表。当前 atlas05 验收为 `passed=49 / failed=0 / blocked=0 / partial=0`，但完整行动语义仍未闭环。
  - 2026-07-04 已按运行时完整 10x7 网格重裁 #10 `七大恨` 并读取 236KB 低体积对照图：牌名、事件牌类和完整规则效果摘要均可读，已从 partial 升级为 passed 并写入正式真相表；r05c08 / CardID 1647 仍只读到边缘窄条，且不在 TTS DeckIDs 中，只保留为排除证据。
  - 2026-07-04 已读取 161KB 完整 10x7 网格邻格诊断图复核最后 3 张 blocked：idx25 / idx44 的目标格均为银两图形，结合正式映射契约和运行时 direct action 逻辑，已按“支付或资源弃牌候选、无直接行动入口”语义升级为 passed；r05c08 / CardID 1647 的目标格仍只有边缘窄条，且不在 TTS DeckIDs 中，只保留为排除证据；随后读取 54KB row6/row7 安全诊断图确认 TTS 实际使用的 CardID 1650「乌真超哈」与 CardID 1660「诈败诱敌」均满足反写门槛。当前 atlas05 为 `passed=49 / failed=0 / blocked=0 / partial=0`，但事件效果全集、战术时机和完整行动语义仍未闭环。
  - 2026-07-04 已补 atlas05 已确认军备牌全集运行时合同：12 张普通军备牌均可从手牌本体直入“升级军备”，执行准备会按来源牌携带对应 `armamentId`，升级摘要会写明“打出军备牌：牌名”和目标军备升级结果；这只关闭军备牌一级入口的一层证据，不代表事件效果全集、战术时机、银两资源链或完整行动语义已闭环。
  - 2026-07-04 已记录 `步兵铁甲` / `骑兵铁甲` 已有结构化军备效果：升级后，对应步兵/骑兵防御等级会进入结构化战斗掷骰与损伤结算；既有回归测试已覆盖后金步兵铁甲和蒙古骑兵铁甲的掷骰摘要与战斗损伤。这是对现有窄口的证据补齐，不代表军备效果全集、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `火炮技术` 从“允许建立/训练炮兵”推进到一层真实战斗军备效果：升级后，每个炮兵在结构化战斗掷骰中额外掷 1 颗骰；小型回归测试覆盖 2 个炮兵合计掷 4 颗炮兵骰。这只关闭 `火炮技术` 军备等级本身的炮兵掷骰数窄口，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `红衣大炮` 推进到来源牌专属炮兵窄口：升级军备会把打出的军备牌 defId 记录到军备来源；当 `火炮技术` 来源包含 `红衣大炮` 时，结构化战斗中野战防守炮兵每个额外掷 1 颗骰，攻城进攻炮兵每个额外掷 2 颗骰；普通 `火炮技术` 仍只提供每炮兵 +1 骰，不被红衣大炮语义污染。这只关闭 `红衣大炮` 的来源牌专属炮兵额外掷骰窄口，不代表军备效果全集、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `骑兵火器` 从“打出军备牌并升级对应军备”推进到两层真实战斗窄口：升级后在野战骑兵阶段，己方每个骑兵部队额外掷 1 颗骰；当双方都有骑兵且军备来源牌为 `骑兵火器` 时，己方骑兵先结算，并按造成损伤压制对手骑兵反击。小型回归测试直接覆盖结构化战斗掷骰摘要 `骑兵 攻4/4/4/4=16/守-=0` 和先结算摘要 `骑兵(骑兵火器指定骑兵先掷) 攻4/4/4/4=16/守-=0`。这只关闭 `骑兵火器` 的额外掷骰与战斗掷骰层先结算底座，不代表拒马交互、其它军备效果、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `精钢马刀` 推进到来源牌专属骑兵窄口：`精钢马刀` 与 `骑兵火器` 共享 `cavalry-firearm` 军备位，但只有来源牌为 `精钢马刀` 且控制鄂尔多斯部时，结构化野战骑兵阶段才让己方每个骑兵部队额外掷 1 颗骰；未控制鄂尔多斯部时不触发，并且不继承 `骑兵火器` 的骑兵先结算语义。这只关闭 `精钢马刀` 的控制鄂尔多斯条件窄口，不代表拒马交互、其它军备效果、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `骏马育种` 从“打出军备牌并升级对应军备”推进到一层真实军备效果：升级后在骑兵/调骑移动档位上移动力 +1，步兵/调步档位不受影响；回归测试覆盖移动搜索中原本 4 点不可达、升级后骑兵档位 5 点可达的区域，同时确认步兵调度仍不可达。这只关闭 `骏马育种` 单项移动效果，不代表其它军备效果、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `机里耐步兵` 从“只记录战术牌规则摘要”推进到两层窄口：在当前攻方 `PLAY_TACTIC_CARD` 窗口打出后，若本次野战为进攻明军，则攻方每个步兵部队额外掷 1 颗骰；后续又补防守明军时步兵先结算的战斗掷骰层底座，并在 2026-07-05 推进到大明守方正式 `PLAY_TACTIC_CARD` 窄口：大明作为野战防守方打出后，会写入防守方步兵先结算修正，后续结算中防守明军步兵先掷，并按造成损伤压制攻方步兵反击。回归测试覆盖打出战术牌后再结算待结算战斗、预置防守先结算修正后结算待结算战斗，以及守方正式打出后再结算待结算战斗。这只关闭 `机里耐步兵` 的进攻明军窄口和大明守方正式打出窄口，不开放通用守方响应窗口、兵种阶段窗口或其它守方战术牌入口，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `西式棱堡` 从“打出军备牌并升级对应军备”推进到一层真实军备效果：升级后在城战中，守方每个部队投 2 颗骰；小型回归测试直接覆盖结构化城战掷骰摘要里的守方 `4/4/4/4=16`。这只关闭 `西式棱堡` 单项军备效果，不代表军备效果全集、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `连环火铳` 从“打出军备牌并升级对应军备”推进到两层真实战斗窄口：升级后在野战步兵阶段，己方每个步兵部队额外掷 1 颗骰；当双方都有步兵且军备来源牌为 `连环火铳` 时，己方步兵先结算，并按造成损伤压制对手步兵反击。小型回归测试直接覆盖结构化野战掷骰摘要 `步兵 攻4/4/4/4=16` 和先结算摘要 `步兵(连环火铳指定步兵先掷) 攻4/4/4/4=16/守-=0`。后续又补上 `拒马` / `步骑联合` 取消 `连环火铳` 先结算的战斗掷骰层底座：预置拒马取消修正后，对手步兵会恢复同步掷骰；步骑联合正式打出产生的修正也会携带取消连环火铳先结算的元数据。这只关闭 `连环火铳` 的额外掷骰、战斗掷骰层先结算与取消底座，不代表守方响应窗口、兵种阶段窗口、其它军备效果、事件效果全集、战术时机或银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `拒马` 取消 `骑兵火器` 先结算补到战斗掷骰层底座：当待结算野战已经同时带有对手 `骑兵火器` 的来源牌骑兵先结算修正和己方 `拒马` 的取消修正时，对手 `骑兵火器` 先结算修正会失效，双方骑兵按普通同步掷骰结算。2026-07-05 已把 `拒马` 补到守方野战待结算 `PLAY_TACTIC_CARD` 窄口：守方正式打出后会写入取消 `箭如雨下`、`连环火铳`、`骑兵火器` 先结算和骑兵跨阶段修正的元数据。这只关闭 `拒马` 的守方野战待结算窄口，不代表通用守方响应窗口、骑兵/步兵阶段窗口、完整战术时机或军备效果全集完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `坚守不屈` 从战斗掷骰层底座推进到守方城战 `PLAY_TACTIC_CARD` 窄口：城战待结算时只有守方可正式打出，打出后写入攻城方掷骰结果减半修正，消耗手牌并进入弃牌堆；攻方野战/城战窗口仍会拦截该守城专用战术，守方其它普通战术牌也不会被泛用放行。这只关闭 `坚守不屈` 的守方城战待结算窄口，不开放通用守方响应窗口、兵种阶段窗口或完整战术时机，2.4 / 4.5 继续保持未完成。
  - 2026-07-10 已用完整单卡重新锁定 `链炮阵` 原文：不能在攻城、守城时使用；战斗中承受损伤时一律由炮兵先承受；每个炮兵的防御等级为 4，受损炮兵立即移除。当前攻方 `PLAY_TACTIC_CARD` 窗口会在野战写入炮兵优先承伤规则，待结算与战后占领/撤回会逐个移除受损炮兵；若损伤超过炮兵数量，剩余损伤继续按非炮兵最高等级结算。回归测试同时覆盖普通规则下炮兵不承伤、链炮阵多点损伤先移除全部炮兵、剩余损伤移除最高等级步兵，以及正式打出到战后占领的完整窄口。这只关闭 `链炮阵` 当前攻方野战的完整承伤语义，不实现守方响应窗口、其它战术时机或完整战术牌全集，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `策反/内应` 从“只记录战术牌规则摘要”推进到真实炮兵转侧窄口效果；2026-07-05 又把普通 `策反` 接入同源火炮转侧窄口。当前攻方 `PLAY_TACTIC_CARD` 窗口打出后，若目标战斗有敌方炮兵，则 1 个敌方炮兵临时转为攻方参战并进入攻方炮兵掷骰侧。回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认战术修正写入 `convertEnemyTroopCount=1`、守方炮兵掷骰侧清空、攻方炮兵掷骰侧出现转来的炮兵，并确认普通 `策反` 的战报摘要保留真实牌名而不是固定写成 `策反/内应`。这只关闭两张火炮版策反的当前攻方战术窗口炮兵转侧窄口，不锁定临时炮兵军备归属，不实现守方响应窗口、目标选择、战后回归、次级部队版策反或完整战术时机，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补 atlas05 已确认银两牌支付执行合同：2 张普通银两牌仍无直接行动入口，但作为支付牌消费时会进入现有弃牌执行链并在行动记录里保留“银两资源牌”身份；这只关闭普通支付场景的一层证据，不代表特殊事件、替代支付、跨势力弃牌或完整银两资源链已闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补普通支付价值的最小语义：后金/蒙古弃 `封贡敕书` 支付普通行动时，按其规则摘要“视作 2 张银两”计入现有支付值、支付上限、自动支付和执行记录；大明弃同牌仍按 1 张普通手牌计。该步骤只作用于普通行动支付，不实现 `封贡敕书` 的事件效果、跨势力弃牌、对手弃牌堆归属或完整银两资源链，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已补银两自动支付回归证据：后金直接执行普通行动时，自动选牌路径会把 `封贡敕书` 当作 2 张银两支付，只消耗该牌而不会额外消耗第二张手牌。该步骤只锁定现有支付价值合同，不实现 `封贡敕书` 的事件效果、目标对手选择、对手动作选择或对手弃牌堆归属，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已补蒙古普通支付回归证据：蒙古手动支付普通行动时，`封贡敕书` 也按“视作 2 张银两”计入支付值，达到当前行动费用后不会继续多选第二张手牌，并在执行记录保留资源支付身份；同日又补蒙古直接执行普通行动的自动支付回归，确认自动选牌路径也只消耗 `封贡敕书` 而不会额外消耗第二张手牌。该阶段只补足后金/蒙古普通支付价值合同；蒙古目标正式行动语义已于 2026-07-10 补齐，银两资源语义也已按规则书收口，但事件效果全集仍未完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已补大明普通支付回归证据：大明弃 `封贡敕书` 支付普通行动时，仍只按 1 张普通手牌计入支付值；执行后只消耗 `封贡敕书`、保留第二张手牌，行动记录不会显示“视作 2 张银两”。该步骤只补足普通支付价值合同的大明侧执行后证据，不实现 `封贡敕书` 事件效果、完整跨势力覆盖或银两资源链闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补当前攻方战术窗口的守城专用门禁：规则摘要包含“只能于守城时使用”的战术牌不会再被攻方野战或城战 `PLAY_TACTIC_CARD` 入口误放行；现有城战门禁仍拦截野战限定、攻守城禁用和守城专用战术牌。这只关闭可由当前 `pendingTargetAction` 证明的一层时机约束，不代表守方战术窗口、兵种阶段、炮兵/次级部队等战术时机全集已闭环。
  - 2026-07-04 已补当前攻方战术窗口的触发型战术保守门禁：`偷袭与伏击` 这类“敌人增援时”战术，以及 `诈败诱敌` 这类“取消对手宣告的骑兵劫掠”战术，不再能从普通待结算战斗的 `PLAY_TACTIC_CARD` 窗口泛用打出；2026-07-05 已补 `诈败诱敌` 与 `偷袭与伏击` 具名保护性回归，确认普通攻方战术窗口会拦截且不会消耗手牌或写入战术修正。当前代码已确认围城增援结算为“不进入战斗”，且尚无守方响应、增援触发或骑兵劫掠宣告窗口；因此这只是一层防误放行门禁，不代表相关触发窗口或逐张战术效果完成。
  - 2026-07-04 已补当前攻方战术窗口的阶段型战术保守门禁；2026-07-05 已把 `箭如雨下` 从该保守门禁里单独推进到当前攻方野战待结算战斗窄口，`PLAY_TACTIC_CARD` 打出后会写入攻方步兵先结算修正。`乌真超哈` 特殊牌已单独从该保守门禁里推进到当前攻方野战待结算窄口：`PLAY_TACTIC_CARD` 打出后会写入 1 个攻方步兵提前在炮兵阶段攻击且不重复攻击的修正。当前仍无完整步兵/骑兵/炮兵阶段上下文；因此这只关闭 `箭如雨下` 与 `乌真超哈` 特殊牌的当前攻方野战窄口和一层防误放行门禁，不代表兵种阶段窗口、守方响应窗口或逐张战术效果完成。
  - 2026-07-05 已补当前攻方战术窗口的战斗承接链保守门禁：`骑马步兵` 这类需要附兵部队模型的牌、`分进合击` 这类需要未参战部队再移动/距离校验/增援进战斗入口的牌，以及次级部队版 `策反` 这类需要敌方次级部队参战触发窗口的牌，不再能从普通待结算战斗的 `PLAY_TACTIC_CARD` 窗口泛用打出。这只是一层防误放行门禁，不代表附兵兵种、次级部队参战触发、目标选择、战后回归、增援移动、距离校验、兵种阶段窗口或逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补 atlas05 已确认事件牌的正式打出记录层，并推进一层真实结算语义：普通事件牌现在可从手牌本体进入 `执行事件`，执行记录会保留“打出哪张事件牌”和人工录入的规则效果摘要；规则摘要写有“使用后移出游戏”的事件牌，打出牌本身不再进入弃牌堆，弃牌堆计数也不会因该事件牌增加；规则摘要写有“持续事件”的事件牌会写入运行态在场承载。这仍只关闭“打出记录 + 使用后移出游戏 + 持续事件在场承载”的一层合同，不实现逐张事件效果、额外成本、持续事件转移/离场、目标选择或跨势力资源链，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已补执行事件入口的特定时机保守门禁：`各个击破` 这类需要遭到攻击时打出并承接多边界撤离排序的事件，尝试从普通 `执行事件` 入口打出时会保留手牌并提示缺少承接链，不再被通用记录层误消耗；同日新增具名回归确认 `各个击破` 不会被通用入口消耗或记为已结算。同日已把 `东北大军` 从该通用拦截里拆出三层窄口：当前选中女真人真实区域时，合法区域会写入 `jala` 类型区域事件标记，非女真人区域不会放置标记；2026-07-10 已核对卡面、规则书全配件表、TTS 模组实体和本地 70 张素材，未找到甲喇专属图片；地图同步层仅对甲喇启用显式汉字 `甲喇`，禁止借用后金控制标记或其它错误素材；可先用真实地图选择命令切换到女真人区域再打出东北大军，甲喇标记会落在新选区；轮盘行动标记在新年位置时，`东北大军` 不能使用，会保留手牌、不进入弃牌堆且不放置甲喇标记。这只关闭东北大军的当前选区、真实地图选择命令承接和新年格禁用窄口。2026-07-11 已把各个击破推进到真实攻击响应：仅多来源进攻时由防守方从手牌打出，复用正式区域高亮并直接点击来源区域决定顺序，不同来源分别进行完整战斗，同一来源合并；前一来源战败后推进下一来源，前一来源获胜后其余来源以零损失进入战后来源组；大明、蒙古使用无效果。普通执行事件入口仍按牌身份拦截，防止绕过正确时机。该单牌完成不代表事件效果全集或战术时机全集完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `七大恨` 从事件记录层推进到一张真实事件效果：后金从手牌本体打出该牌时，会在当前选中的后金本土建立 2 个 3 级后金步兵，并同步地区兵力、结构化部队栈和可视棋子集合；大明、蒙古仍按规则摘要保持无效果边界。这只关闭 `七大恨` 单牌建兵合同，不代表旱灾/甲喇标记、目标选择事件、额外成本或跨势力资源链已完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `成立汉八旗 / 成立满八旗 / 成立蒙八旗` 从事件记录层推进到真实持续事件效果：后金从手牌本体打出这些牌时，会在后金控制的汉人/女真人/蒙古人区域各建立 1 个 2 级后金步兵，并把对应族属区域作为后续后金本土判定来源；这些牌按持续事件不进入弃牌堆，行动摘要保留持续事件文案，并写入运行态在场承载。这只关闭八旗持续事件合同，不代表旱灾/甲喇标记、目标选择事件、额外成本或跨势力资源链已完成。2026-07-11 已按地图真相修正 `玉匣出土`：归化城位于土默特部（`city-region-20`），打出事件牌不会凭空改变归化控制；拥有者同时控制土默特部与鄂尔多斯部时给 +1 威望，失去任一地区时持续事件转移给新占领者。2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `蒙古大旱 / 东北大旱` 从事件记录层推进到旱灾标记状态层：打出对应事件牌时，会按蒙古人/女真人族属门槛校验当前选中区域，合法区域写入正式区域事件标记并同步地图旱灾标记 token，不合法区域不会放置标记；年中土地税赋与新年补给/兵力耗损已按旱灾把有效人口视为 0，劫掠仍保留真实人口。2026-07-05 已补 `东北大旱` 女真人区域正向正式打出回归，确认可在建州写入旱灾事件标记并同步地图可见标记；同日补第二张 `蒙古大旱`（qidahen-atlas05-1637-mongol-drought-alt）同源正向回归，确认可在察哈尔写入旱灾事件标记并同步地图可见标记。这只关闭旱灾标记的状态承载、可见地图证据和两处季节结算人口语义，不代表甲喇标记、目标选择全集、其他人口读取入口或跨势力资源链已完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已补齐三张旱灾事件的牌面时段限制：两张 `蒙古大旱` 与 `东北大旱` 在轮盘行动标记位于年中或新年时均不能使用，事件牌保留在手中且不会放置旱灾标记；定向回归覆盖 3 张牌 × 2 个禁用位置共 6 组，连同正常放置回归共 10/10 通过。十张剩余事件牌已经按当前运行时代码与 443 条事件相关回归完成原子核销，最终语义矩阵更新为 `30 pass / 19 in_progress`，剩余项全部为战术牌；2.4 / 4.5 在十九张战术牌和最终截图发布完成前继续保持未完成。
  - 2026-07-04 已把 `翻山越岭` 从事件记录层推进到一次性进攻效果：从手牌打出后进入一次突袭待结算；本次进攻中的长城、山脉边界按平原边界计算战场宽度与投入压力，运行时地图边界仍保持原始长城/山脉配置。定向测试覆盖土默特部进攻宁远的长城边界和土默特部进攻大同的山脉边界。这只关闭该牌一次性进攻边界语义，不代表事件效果全集、战术时机或银两资源链已完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补战术牌打出记录的规则摘要承接：当前可从攻方战术窗口打出的战术牌，会在战报和行动记录里保留人工确认的规则摘要，避免只留下“打出战术牌”但无法追溯牌面语义。这只关闭打出记录层，不代表守方响应窗口、兵种阶段窗口、骰子修改或逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `鸟真超哈` 从战术记录层推进到一张真实战术效果：从攻方战术窗口打出后，会在当前待结算战斗上写入一次性战术修正，本次野战攻方步兵掷骰等级 +1；回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认攻方 2 级步兵按 3 级 d10 掷骰，守方仍按自身军备/等级结算。这只关闭 `鸟真超哈` 单牌修正，不代表守方响应窗口、兵种阶段窗口或其他逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `骑兵冲锋` 从战术记录层推进到一张真实战术效果：从攻方战术窗口打出后，会在当前待结算战斗上写入一次性战术修正，本次野战攻方每个骑兵部队额外掷 2 颗骰；回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认 2 个攻方骑兵部队合计掷 6 颗骑兵骰。这只关闭 `骑兵冲锋` 单牌修正，不代表守方响应窗口、兵种阶段窗口或其他逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `战车阵` 从战术记录层推进到一张真实战术效果：从攻方战术窗口打出后，会在当前待结算战斗上写入一次性战术修正，本次战斗攻方步兵防御等级 +1；回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认攻方 2 级步兵按 3 级 d10 掷骰，守方仍按自身等级结算。这只关闭 `战车阵` 单牌修正，不代表守方响应窗口、兵种阶段窗口或其他逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `巴雅喇` 从战术记录层推进到两层窄口：从攻方战术窗口打出后，会在当前待结算战斗上写入一次性战术修正，本次进攻守方所有部队防御等级 -1；战斗掷骰层底座可在待结算战斗已有防守方步兵防御等级 +1 修正时，让防守方步兵按更高等级掷骰。2026-07-05 又把甲兵防守半段从预置战斗层底座推进到守方正式打出窄口：守方可在待结算战斗中打出 `巴雅喇`，写入防守方步兵防御等级 +1，并在后续掷骰中按 3 级 d10 结算。回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认攻方半段会让守方步兵、骑兵、炮兵三类结构化部队掷骰等级均降低；也覆盖守方正式打出后再结算待结算战斗，确认防守方步兵按更高等级掷骰。这只关闭 `巴雅喇` 的锁链甲进攻半段和甲兵防守正式打出窄口，不开放通用守方响应窗口、兵种阶段窗口或其他逐张战术时机，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `步骑联合` 从战术记录层推进到一张真实同攻窄口战术效果：从攻方战术窗口打出后，会在当前待结算野战上写入一次性战术修正；当攻方步兵和骑兵同时参战时，攻方步兵、骑兵掷骰等级各 +1。回归测试覆盖 `PLAY_TACTIC_CARD` 后再 `RESOLVE_PENDING_ACTION`，确认两类结构化部队均按 3 级 d10 掷骰。这只关闭 `步骑联合` 的同攻掷骰窄口，不代表骑兵撤离、步兵阶段响应、守方窗口或其他逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `策反/内应` 从战术记录层推进到真实炮兵转侧窄口战术效果；2026-07-05 又把普通 `策反` 复用同一火炮转侧窄口，并让战报摘要显示实际牌名。两张火炮版策反从攻方战术窗口打出后，都会在当前待结算战斗上写入一次性战术修正；当守方有炮兵参战时，1 个守方炮兵会转入攻方炮兵掷骰侧。回归测试只锁定转侧结果和真实牌名摘要，不把临时炮兵是否继承攻方军备写成完成口径。这只关闭两张火炮版策反的当前攻方炮兵转侧窄口，不代表军备归属、守方响应、目标选择、战后回归、次级部队版策反或其他逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `箭如雨下` 从战斗掷骰层底座推进到当前攻方野战战术窗口窄口：从 `PLAY_TACTIC_CARD` 打出后，会在当前待结算野战上写入攻方步兵先结算修正；2026-07-11 又补齐与 `拒马` 的正式先后时序，双方均记录打出时点和整牌压制关系，后出的牌使先出的对方牌失效，同一时点发动时双方均生效。结算时有效的 `箭如雨下` 会让攻方步兵先掷，并按伤害压制守方步兵反击。回归测试覆盖打出入口、城战拦截、战斗结算和三种先后时序。这只关闭 `箭如雨下` 的当前攻方野战窄口，不代表兵种阶段窗口、通用守方响应窗口或完整战术时机完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `坚守不屈` 推进到战斗掷骰层底座：当城战待结算战斗已经带有攻击方掷骰值减半战术修正时，攻城攻击方骰值会先完成加减，再除以 2 向下取整。2026-07-05 又补到守方城战 `PLAY_TACTIC_CARD` 窄口：城战待结算时只有守方可正式打出该牌并写入攻城方掷骰减半修正；当前仍不开放通用守方响应窗口、兵种阶段窗口或完整战术时机，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `拒马` 推进到战斗掷骰层底座；2026-07-05 补到守方野战待结算 `PLAY_TACTIC_CARD` 窄口。2026-07-11 进一步补齐与 `箭如雨下` 的正式先后时序：双方均记录打出时点和整牌压制关系，后出的 `拒马` 使先出的 `箭如雨下` 失效，后出的 `箭如雨下` 使先出的 `拒马` 失效，同一时点发动时两者均生效；有效的 `拒马` 继续取消对手 `连环火铳`、`骑兵火器` 先结算和骑兵跨阶段修正。大明、蒙古、后金作为野战守方正式打出后均生成相同修正。这只关闭 `拒马` 的守方野战待结算窄口，不代表骑兵/步兵阶段时机、通用守方响应或完整战术时机完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已继续补 `拒马` 取消对手骑兵按步兵阶段掷骰的战斗掷骰层底座：当待结算野战已有对手骑兵跨到步兵阶段掷骰的修正，且己方已有 `拒马` 的来源取消修正时，对手骑兵不再混入步兵阶段掷骰。2026-07-05 新增的守方野战待结算正式打出窄口会生成这类取消元数据；当前仍不开放通用守方响应窗口或兵种阶段窗口。这只关闭一层取消底座和守方野战待结算打出入口，不代表完整战术时机或逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `打草惊蛇` 推进到战斗劫掠层底座：当待结算骑兵劫掠已经带有该牌的免伤战术修正时，劫掠骑兵不承受守方炮骑反击伤害，并按全部存活骑兵计算可劫掠人口。2026-07-05 又从防误放行门禁推进到攻方合法骑兵劫掠待结算 `PLAY_TACTIC_CARD` 窄口：只有当前待结算行动确认为攻方骑兵劫掠、目标为非城市/非朝鲜且有人口、来源有承诺骑兵时才可正式打出，随后写入骑兵劫掠免伤修正；回归测试覆盖正式打出后再结算劫掠，确认劫掠 3 人口且骑兵不因守方炮骑反击损失。这只关闭当前攻方合法骑兵劫掠待结算的正式打出窄口，不代表扎营流程、全部劫掠时机、骑兵劫掠、通用触发窗口、守方响应或完整战术时机完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-10 已补正式手牌界面与命令门禁同源：`HandZone` 不再只凭“存在攻方待结算行动”把所有攻方战术牌做成可点击，而是复用 `PLAY_TACTIC_CARD` 的同一战术资格合同，并按当前手牌所属势力识别攻方或守方。合法骑兵劫掠时 `打草惊蛇` 可从手牌本体打出；普通野战、城战、朝鲜目标、无人区域或来源没有承诺骑兵时不会显示成可打出；同时恢复 `坚守不屈`、`拒马`、`巴雅喇`、`机里耐步兵` 等既有守方正式窄口的手牌点击入口。这只关闭界面可点击性与命令验证不一致，不代表通用响应窗口或完整战术时机完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已把 `乌真超哈` 特殊牌推进到战斗掷骰层底座：当待结算野战已经带有提前炮兵阶段攻击修正时，1 个攻方步兵会在炮兵阶段攻击，并从普通步兵阶段扣除，避免同一个步兵重复攻击。2026-07-05 又补到当前攻方野战待结算 `PLAY_TACTIC_CARD` 窄口：正式打出后会写入这条提前炮兵阶段攻击修正。当前仍不开放独立炮兵阶段时机、指定目标选择或摧毁火炮技术后的攻击等级加成。这只关闭提前炮兵阶段攻击的当前攻方野战窄口，不代表完整兵种阶段窗口或逐张战术效果完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `乌真超哈` 特殊牌已覆盖上一条历史缺口：正式打出后进入实际参战步兵牌直选，可选择销毁本势力拥有的《火炮技术》数量；确认后才消耗手牌并按所毁数量扣减军备来源牌和等级，指定步兵提前在炮兵阶段攻击且不重复攻击，每毁 1 张攻击等级 +1，仍叠加其它步兵加成并正常承受战损；大明、蒙古效果相同。确认事件会使旧待结算交互失效并按新核心状态重建，连续真实流水线不会再读取确认前快照。专项 7/7、原失败流水线和 payment-selection 438/438 均通过；最终语义矩阵更新为 `32 pass / 17 in_progress`。这只关闭本牌五条原子规则，2.4 / 4.5 仍因其它十七张战术牌和总门禁未闭环而保持未完成。
  - 2026-07-04 已把 `偷袭与伏击` 推进到战斗掷骰层底座：当待结算战斗已经带有指定兵种骰子等级 -1 修正时，对应兵种会按降低后的等级掷骰。回归测试不通过 `PLAY_TACTIC_CARD` 打出该牌，而是预置该战术修正后结算待结算战斗；当前仍不开放敌人增援触发窗口、指定兵种选择、追加战术牌或正式打出入口。这只关闭指定兵种骰子等级降低的掷骰层基础，不代表增援响应、兵种选择、追加战术窗口或完整战术时机完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-04 已补事件牌通用记录层的防误判拦截：规则摘要要求“放入该对手弃牌堆”、人物目标或二择一效果选择的事件牌会在 `执行事件` 入口被拦截，保留原手牌并提示缺少承接链，不再被通用记录层写成“使用后进入当前势力弃牌堆”或“已移出游戏”。后续 `反间计`、`人参貂皮`、`封贡敕书`、`王公大会` 和 `开门迎降` 已分别拆出正式承接链；其中 `开门迎降` 旧人物掷骰窄口已由 2026-07-11 的专用效果组合、后金人物/兵牌损失和大明派系人物清除状态机覆盖。剩余拦截仍只防止其它未闭合归属链伪装成已结算，不代表事件效果全集完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已完成 `玉匣出土` 地图与持续事件语义复核：轻量裁图 `temp/qidahen-guihua-evidence/tumed_ordos_vp_wide.jpg` 显示“归化”与 VP 图标位于土默特部区域，运行时以 `city-region-20` 的真实控制者作为归化控制真相；正式打出事件牌不再把归化控制强行写给出牌者。拥有者同时控制土默特部与鄂尔多斯部（`city-region-26`）时获得 +1 有效威望，失去任一地区时持续事件会沿统一状态归约转移给新占领者。该单牌已闭环，但事件/战术全集仍未全部核销，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 剩余战术链可行性复核：`骑马步兵` / `分进合击` / `诈败诱敌` / 次级部队版 `策反` 仍不是漏写 reducer，而是分别缺附兵身份、未参战部队再移动/距离校验、对手骑兵劫掠宣告、敌方次级部队参战触发与战后回归承接链；普通 `PLAY_TACTIC_CARD` 入口继续门禁拦截，2.4 / 4.5 不勾完成。
  - 2026-07-11 已关闭 1629 号次级部队版 `策反` 的正式链路：只在野战且敌方有实际参战次级部队时开放，打牌后进入地图兵牌本体直选并只高亮合法目标；取消不耗牌，确认后锁定具体次级棋子，仅在本场战斗把该棋子计入攻方掷骰，地图原始阵营与所属势力不改变，战后自然恢复。该成果只关闭本牌链路，`骑马步兵` / `分进合击` / `诈败诱敌`、事件效果全集和完整战术时机仍未闭合，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已关闭 1620 号 `骑马步兵` 的正式链路：只有当前进攻真实承诺至少 1 个附兵时才可打出，打出后进入弃牌堆；本场战斗仅把攻方参战附兵临时按步兵阶段、等级、防御与战术结算，攻方正规骑兵、敌方附兵及地图原始附兵身份均不改变。该成果只关闭本牌链路，`分进合击` / `偷袭与伏击` / `诈败诱敌`、事件效果全集和完整战术时机仍未闭合，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已为 1620 号 `骑马步兵` 补齐直接组合证据：预先存在步兵防御等级和步兵额外骰战术修正时，正式打出本牌会让攻方参战附兵进入步兵阶段并同时获得两类修正；攻方正规骑兵和敌方附兵仍在骑兵阶段，地图原始兵牌身份不变。`payment-selection.test.ts` 438/438 与类型检查通过，最终语义矩阵更新为 `33 pass / 16 in_progress`；2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已为 1645 号 `战车阵` 补齐三势力直接行为证据：大明、后金、蒙古分别从真实势力手牌正式打出后，本场攻方步兵防御等级均 +1，守方步兵不受影响。定向回归 3/3、`payment-selection.test.ts` 440/440 与类型检查通过，最终语义矩阵更新为 `34 pass / 15 in_progress`；2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已为 1618 号 `骑兵冲锋` 补齐单牌精确时机证据：同一真实手牌在城战正式拒绝，野战打出后每个攻方骑兵额外掷 2 颗骰，2 个骑兵共掷 6 颗骰。定向回归 1/1、`payment-selection.test.ts` 440/440 与类型检查通过，最终语义矩阵更新为 `35 pass / 14 in_progress`。通用 19 张战术时机聚合用例仍失败于 `commands.test.ts:1046` 的批量野战可用断言，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 后续已解决上述通用战术时机聚合失败：诊断确认 `步骑联合` 与 `乌真超哈` 需要实际参战兵种和专用选择状态，聚合测试已按真实规则归入状态依赖牌，不放宽生产门禁。`巴雅喇` 同日补齐大明、蒙古、后金三势力攻守同效回归，攻方使守方全部兵种防御等级 -1，守方使己方步兵防御等级 +1；语义矩阵更新为 `36 pass / 13 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `鸟真超哈` 随后补齐城战禁用、野战步兵实际等级 +1 和大明/蒙古/后金三势力同效证据；相关定向回归 5/5 通过，语义矩阵更新为 `37 pass / 12 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `坚守不屈` 随后补齐守城专用、攻城骑兵先减值再除以 2 向下取整，以及大明/蒙古/后金三势力同效证据；相关定向回归 6/6 通过，语义矩阵更新为 `38 pass / 11 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `拒马` 随后补齐与 `箭如雨下` 的先后发动和同时发动合同、对手骑兵跨步兵阶段取消、`连环火铳` / `骑兵火器` 反制，以及大明/蒙古/后金三势力同效证据；battleRollArmamentEffects、commands、payment-selection 三文件 482/482 通过，类型检查、相关 ESLint 和 OpenSpec strict validate 通过，语义矩阵更新为 `39 pass / 10 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `机里耐步兵` 随后补齐攻方精确时机门禁：只有野战且守方为大明时可正式打出，攻打蒙古等非大明势力时命令拒绝且手牌、弃牌堆均不改变；进攻明军时攻方每个步兵额外掷 1 颗骰、大明作为野战守方时步兵先结算两条牌面规则均有正式打出和战斗结果证据。commands、payment-selection 两文件 468/468 通过，类型检查、相关 ESLint 和 OpenSpec strict validate 通过，语义矩阵更新为 `40 pass / 9 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 1629 号 `策反` 的最终审计已与既有正式链对齐：只有实际参战的敌方次级兵牌可被直选，确认后该具体兵牌只在当前战斗转入攻方掷骰侧，地图棋子与区域部队栈的原阵营始终不变，战后自然回归；instigateDefectionSelection、commands、Board 三文件 214/214 通过，语义矩阵更新为 `41 pass / 8 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 `分进合击` 与 `偷袭与伏击` 的最终审计已与既有正式联动链对齐：分进合击只在野战开放，候选兵牌必须未参战且能在对应兵种行动距离内到达目标，每次最多选择 2 个具体兵牌；确认增援后，守方可打出偷袭与伏击，指定敌方实际参战兵种骰子等级 -1，并可再追加一张合法守方战术牌。pincerAdvanceSelection、raidAndAmbushSelection、commands、payment-selection 四文件 478/478 通过，语义矩阵更新为 `43 pass / 6 in_progress`。2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已关闭 1632 号 `分进合击` 与 1622 号 `偷袭与伏击` 的正式链路：`分进合击` 只在野战且存在满足行动距离的未参战兵牌时开放，最多选择 2 个具体兵牌，取消不耗牌、确认后才弃牌；增援按真实来源、兵种、等级和兵牌编号进入结构化战斗，不再折算为主来源兵种。确认增援后，如守方持有 `偷袭与伏击`，原战斗暂停并把操作权交给守方；守方可点击真实手牌打出或选择不使用，打出后只能选择敌方当前实际参战兵种使骰子等级 -1，并可再追加一张合法守方战术牌或跳过，结束后恢复原待结算交互。该成果只关闭两张牌；当前仍未闭合的是事件效果全集和完整战术时机，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 历史阻塞记录（已被后续逐牌正式链覆盖）：当时 `反间计`、`开门迎降`、`王公大会`、`人参貂皮`、`封贡敕书`、`各个击破`、`东北大军`、`骑马步兵`、`分进合击`、次级部队版 `策反` 与 `诈败诱敌` 仍处于局部窄口或防误放行阶段。当前状态以本节后续日期更新为准：`开门迎降` 的人物判定/第二项未闭环描述已由 2026-07-11 专用状态机覆盖，其余具名牌也已分别记录正式链关闭情况。本条只保留实施过程，不再代表当前剩余缺口；2.4 / 4.5 仍因事件效果全集和完整战术时机未闭合而保持未完成。
  - 2026-07-11 已关闭 1660 号 `诈败诱敌` 的正式链路，并纠正此前人工录入把“骑兵劫掠”误写成“附兵劫掠”的错误：攻方宣告合法骑兵劫掠后，若守方真实手牌持有本牌，原劫掠会暂停并进入守方专用响应；守方可直接点击真实手牌打出或选择不使用。不使用会沿交互快照恢复原骑兵劫掠；打出会消耗真实手牌并进入弃牌堆，取消人口劫掠，原宣告骑兵按原出兵数量生成正常战斗骰并进入正常战斗与战后撤退规则。非守方、错误卡牌、普通战术窗口和非骑兵劫掠状态均不会放行。普通手牌覆盖矩阵当前为 `49 pass-candidate / 0 generic-armament-review / 0 pending`；2.4 / 4.5 仍因事件效果全集和最终完整时机审计保持未完成。
  - 历史口径覆盖说明：本节中早于上述 2026-07-11 关闭记录、仍把 `诈败诱敌`、`骑马步兵`、`分进合击`、`偷袭与伏击` 或次级部队版 `策反` 写成“仍缺正式链”的段落，只保留为实施过程记录，不再代表当前状态；其中“附兵劫掠/附兵直接投入战斗”是错误人工录入，正式规则统一为“骑兵劫掠/骑兵直接投入战斗”。
  - 2026-07-11 已修正并闭合 `反间计` 人物目标链：牌面要求额外弃 2 张手牌，因此支付总数为事件牌本体加两张额外手牌；只选一张额外牌时不能执行。支付完成后进入人物目标选择，目标列表排除努尔哈赤、林丹汗和阿巴凯；选择合法对手人物后将其移出游戏，事件牌本体移出游戏，两张额外支付牌进入弃牌堆；无合法人物目标时三张牌均不消耗。完整 `payment-selection.test.ts` 426/426、相关 ESLint 和类型检查已通过。该单牌完成不代表事件效果全集或战术时机全集完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `王公大会` 从纯人物二择一阻塞里拆出后金/大明使用无效果窄口：非蒙古势力可从 `执行事件` 打出该牌并进入当前势力弃牌堆，摘要明确无效果；蒙古使用已进入打出蒙古人物/回收蒙古人物二择一，并可结算具体蒙古人物。该窄口不代表其它人物牌堆链或事件效果全集；同段历史上对 `开门迎降` 的人物判定/第二项未闭环描述已由 2026-07-11 正式状态机覆盖，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `人参貂皮` 从纯对手弃牌堆归属阻塞里拆出两层窄口：非后金势力可从 `执行事件` 打出该牌并进入当前势力弃牌堆，摘要明确无效果；后金使用会进入专用目标对手给牌链，先由后金指定一个有手牌的对手，再由该对手选择给出 1 张手牌，结算后事件牌进入该对手弃牌堆。该窄口不实现其它跨势力归属链，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `封贡敕书` 从纯对手弃牌堆归属阻塞里拆出额外弃牌、目标对手与对手动作选择窄口：当前势力打出时必须额外弃 1 张手牌，随后先指定一个对手，再由该对手选择 `赐印招安` 或 `驱虎吞狼`，选择后事件牌离开打出者手牌并进入该对手弃牌堆，额外费用牌进入打出者弃牌堆。
  - 2026-07-05 已继续补上 `封贡敕书` 目标为大明时的既有正式链承接窄口：大明选择 `赐印招安` 会复用现有赐印招安结算，大明选择 `驱虎吞狼` 会进入既有驱虎吞狼同意链并保留调度来源；同日新增目标蒙古保护性回归，当时蒙古选择 `赐印招安` 仍停在通用未承接摘要，避免误套只产出大明部队的旧链。该历史缺口已由 2026-07-10 的执行势力参数化补齐；事件效果全集仍未闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-10 已把 `封贡敕书` 目标蒙古从保护性占位推进到正式行动承接：蒙古选择 `赐印招安` 时复用既有目标选择并按实际执行势力把相邻敌军转为蒙古部队；蒙古选择 `驱虎吞狼` 时保留蒙古为指挥势力进入既有同意与调度链，提示、快照重建和结算摘要不再硬编码大明。回归同时覆盖目标大明原有两条正式链，2.4 / 4.5 继续保持未完成。
  - 2026-07-05 已把 `七大恨` 的大明/蒙古无效果边界补成显式窄口：非后金势力可从 `执行事件` 打出该牌，摘要明确无效果，牌按“使用后移出游戏”不进入弃牌堆，也不建立部队；后金本土建兵窄口保持不变。该窄口不代表事件效果全集、战术时机或银两资源链闭环，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已关闭 1621 号 `开门迎降` 的正式链路并纠正旧人物掷骰误实现：出牌者可选择只执行第一项、只执行第二项或两项依次执行；第一项由后金玩家任意选择弃掉的后金在场人物，再按剩余人物每张强制选择 2 个具体后金部队损失，兵力不足时弃尽全部可用部队；第二项由大明玩家选择一个派系并弃掉该派系全部在场人物；后金、大明使用效果相同，事件牌使用后移出游戏。回归覆盖三种效果组合、零/部分/全部人物、兵力不足、具体野外/城内/围城兵牌、跨席位操作权、错误席位拒绝和最终移出游戏。该成果只关闭本牌正式链，事件效果全集和完整战术时机仍未闭合，2.4 / 4.5 继续保持未完成。
  - 2026-07-11 已关闭原 7 张通用军备逐牌复核：现有参数化回归逐一遍历 12 张已确认军备牌，证明每张具体牌都能从真实手牌直点进入升级军备、完成支付、升级其 `armamentId` 对应军备并记录实际 `cardDefId` 来源；步兵铁甲、骑兵铁甲、骏马育种、西式棱堡另有最终战斗或移动结果断言。普通手牌覆盖矩阵更新为 `49 pass-candidate / 0 generic-armament-review / 0 pending`；该成果关闭军备逐牌复核，但不替代最终事件效果/战术时机全集审计，2.4 / 4.5 继续保持未完成。
  - 2026-07-03 已新增运行时图集候选小图 OCR 尝试记录：`docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-ocr-attempt.md`。EasyOCR 路径编码问题已定位并绕过，但随后受内存/显存不足影响，未稳定完成 28 条候选识别；该记录只能作为阻塞证据，不能作为 2.4 完成依据。
  - 2026-07-03 已新增运行时图集候选与既有 OCR 线索交叉表：`docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-existing-ocr-crosswalk.md`。28 条候选全部命中既有 OCR/核读线索，但 24 条带非普通手牌线索，2 条只有普通关键词仍缺人工确认，2 条低信息；不能作为 2.4 完成依据。
  - 2026-07-03 已新增运行时图集候选最小人工确认队列：`docs/games/qidahen/records/qidahen-hand-card-runtime-atlas-priority-review.md`。该步骤曾把可优先人工核读的候选从 28 条收窄为 4 条：后金 CardID 1508、大明 CardID 202、蒙古 CardID 1300、蒙古 CardID 1302；后续单张低分辨率安全预览已确认这些行均为人物牌或纪年/剧本类牌，不能反写正式手牌规则映射。
  - 2026-07-03 已对上述 4 条最小队列做单张低分辨率安全预览核读：后金 1508 为皇太极人物牌，大明 202 为孙元化人物牌，蒙古 1300 / 1302 为纪年或剧本类牌；4 条均应排除，不能补普通事件、军备、战术或银两映射。
  - 2026-07-03 已继续核读混合线索第一批 4 条：大明 CardID 206 为袁崇焕人物牌，蒙古 CardID 1303 / 1304 / 1305 均为纪年卡；这些行也应排除，说明“打出”关键词仍会出现在非普通手牌正文里。
  - 2026-07-03 已继续核读混合线索第二批 4 条：蒙古 CardID 1306 / 1307 / 1308 / 1309 均为纪年卡；这些行也应排除，不能补普通事件、军备、战术或银两映射。
  - 2026-07-03 已继续核读混合线索第三批 5 条：蒙古 CardID 1310 / 1320 / 1330 / 1340 / 1350 均为纪年卡。至此 13 条“普通词与非普通线索混合”运行时图集候选已全部排除，剩余 11 条转为“偏非普通手牌”队列继续复核。
  - 2026-07-03 已继续核读偏非普通手牌第一批 6 条：后金 CardID 1500 / 1503 / 1505 / 1507 与大明 CardID 200 / 201 均为人物牌或人物效果相关非普通牌；这些行也应排除，不能补普通事件、军备、战术或银两映射。
  - 2026-07-03 已继续核读偏非普通手牌第二批 5 条：大明 CardID 203 / 204 / 205 / 207 / 220 均为人物牌。至此 28 条运行时 faction hand preview atlas 候选均已核读并排除为非普通手牌，不能作为 `2.4` 完成依据。
  - 2026-07-03 已继续核读剩余 12 张人工复核候选标题预览：5 张为人物牌标题，6 张为牌背/空白，1 张为不可读/非普通候选；没有任何候选可补普通事件、军备、战术或银两映射。
  - 2026-07-03 已继续核读主矩阵最后 11 张待复核候选标题预览并回填：阿敏、额亦都、皇太极、范文程、努尔哈赤、王化贞、高第、萨囊彻辰均为人物牌标题，蒙古低置信候选为纪年/剧本类牌；至此 49 张 OCR 候选主矩阵全部排除为非普通手牌、牌背/空白或不可读来源，仍不能关闭普通事件、军备、战术或银两真相源缺口。
  - 2026-07-03 已继续回填 TTS CardID 完整人工录入矩阵 143 行：28 条运行时图集候选已安全核读排除，111 条纪年/朝鲜或非正式手牌图集由哈希证据排除，4 条 1x1 `CardCustom` 小牌组对象无牌名、说明、牌类或效果字段；TTS CardID 链也没有普通事件、军备、战术或银两确认行。
  - 2026-07-03 已完成文档一致性收口：早期 OCR/候选清单中的“待复核 / 待人工复核”字段视为当时归档状态，当前裁决以主矩阵、TTS 完整矩阵、运行时图集矩阵和完成依据决策矩阵为准；三条矩阵均无 pending/confirmed 普通手牌行。
  - 2026-07-03 已新增本地素材逐牌来源穷尽复查：`docs/games/qidahen/records/qidahen-hand-card-local-asset-source-exhaustion.md`。素材目录只发现 62 个 JPG、9 个 PNG、1 个 TTS JSON 和 1 个规则 PDF，没有额外文本牌表、结构化表格或压缩包；71 张图片文件名没有普通手牌牌类关键词命中；运行时 manifest 的 30 个 `cards/` 键也没有普通手牌牌类关键词命中，本地素材目录没有第四条可追溯文本来源可关闭 `2.4`。
  - 2026-07-03 已把疑似单卡素材 OCR 小批量试跑沉淀到 `docs/games/qidahen/records/qidahen-hand-card-single-card-ocr-probe.md` 并回写总览。33 张候选经 PIL + EasyOCR 试跑后，普通牌类关键词独占命中为 0，混合普通/非普通命中为 0；4 张命中人物或下野等非普通线索，23 张只有低信息 OCR 文本，6 张没有 OCR 文本。后续按需求交接式安全读图流程验收全部 33 张候选，得到 30 张非普通手牌/非手牌素材排除和 3 张纯色底块阻塞；这说明安全读图链路可用，但本批没有任何 `passed` 普通事件、军备、战术或银两牌，仍未产生可反写逐牌真相源，不能关闭 `2.4`。
  - 2026-07-03 已新增图片验收产物校验脚本：`scripts/verify/qidahen-hand-card-image-validation.mjs`。脚本检查 `test-results/evidence-image-validation/qidahen-formal-handcard-2.4.json` 的统计、逐项状态、子代理与主线程抽样对照记录，并在 `passed=0` 时强制要求 `2.4 / 4.5` 保持未完成；存在 passed 行时仍要求人工录入反写校验和后续定向测试共同证明可改正式映射。
  - 2026-07-03 已追加定向仓库/素材源复扫，只覆盖七大恨源码、七大恨工作流文档、图片验收产物、验证脚本和本地素材根目录，排除宽范围 `temp/` 噪音；未发现新的表格、压缩包或隐藏文本牌表，普通手牌关键词命中仍集中在既有审计/规则/教程/代码字段名和 TTS 空字段，不能补逐牌真相源。
  - 2026-07-03 已追加公开文本来源复查：`docs/games/qidahen/records/qidahen-hand-card-external-source-search.md`。新增中英文搜索词仍只返回无关页面或搜索噪音，没有找到普通事件、军备、战术、银两的可追溯逐牌牌表。
  - 2026-07-03 已追加精确词公开搜索复查：`"七大恨" "事件牌"`、`"七大恨" "军备牌"`、`"七大恨" "战术牌"`、`"七大恨" "银两牌"`、`"七大恨" "牌表"`、`"Seven Grievances" "event card" board game`、`"Seven Grievances" "armament" card`、`"七大恨" "大汗令箭"`、`"七大恨" "火炮技术"` 均未命中可追溯逐牌牌表；返回仍是百科、单词、歌曲、知乎/无关页面或搜索噪音，不能补逐牌真相源。
  - 2026-07-10 已完成 `蒙古大旱 / 东北大旱` 的人口消费者扩审：新增共享有效人口规则，税赋、补给耗损、中立守军、守城收入、高第人口调度及自动候选/排序统一把旱灾区人口视为 0；真实人口存储、人口增长、骑兵劫掠和战后劫掠继续读取原值。定向回归覆盖旱灾中立区不生成守军、旱灾城市守城不收入人口、旱灾区不提供人口调度，以及旱灾区仍可按真实人口劫掠。该人口语义已闭合，但事件效果全集和完整战术时机仍未完成，2.4 / 4.5 继续保持未完成。
  - 2026-07-10 已完成银两规则消费扩审：规则书明确银两牌无特殊效果、只能弃作资源；atlas05 两张银两均无直接行动入口，可进入统一手动/自动支付链并在执行记录保留资源身份；`封贡敕书` 在后金/蒙古弃作资源时由共享支付值统一按 2 点计算，大明按 1 点，手动支付、直接执行自动支付和执行后消耗均已有回归覆盖。银两不再列为 2.4 的剩余缺口；`封贡敕书` 目标蒙古和完整跨势力动作仍归事件效果缺口，事件效果全集与完整战术时机未完成，因此 2.4 / 4.5 继续保持未完成。
  - 2026-07-11 最终普通手牌语义审计已完成：`temp/qidahen-completion-implementation/final-card-semantic-audit.json` 为 `49 pass / 0 in_progress`，逐牌原子子句没有未检查项；`temp/qidahen-completion-implementation/final-card-evidence-index.json` 为 49/49 张，每张均有运行时文件、测试文件和可回查直接测试标题。《打草惊蛇》《箭如雨下》《链炮阵》最后三张已补齐正式时机与结果证据，其中《链炮阵》按固定防御等级 4、十二面骰和炮兵优先承伤结算。最终门禁已通过：七大恨单测 36 个文件 944/944、手牌验收 49/49、类型检查、相关 ESLint 与 OpenSpec strict 校验均通过；教程 16/16、移动端 1/1 通过，服务器公开相册 64/64 张逐图真实加载并保持根任务列表入口。2.4 已完成。
- [x] 2.5 拆分 `selectedRegionId` 的混合职责，至少把默认聚焦、已锁来源、当前目标和展示锚点变成独立语义，不再互相反写。
- [x] 2.6 确认正式棋盘始终按当前座位展示私有手牌与可操作对象，当前行动势力切换时不得串到其他玩家视角。

## 3. Tutorial
- [x] 3.1 重构教程章节边界，让目录中的玩家主章节直接覆盖“开局与完整回合、进攻与野战、攻城与围城、轮盘分支与发展、年中与新年、朝鲜与地图特例”。
- [x] 3.2 基础章必须从正式开局出发，真实示范：手牌上限、轮盘推进、一次手牌行动、一次轮盘行动，并明确横幅只是提示、真实点击落在轮盘/手牌/地图/按钮本体。
- [x] 3.3 补齐当前缺失的关键规则面章节或真实步骤：中立入侵、水路移动、战术牌、守城宣告或骑兵城战减值、朝鲜朝贡或朝鲜耗损。
  - 2026-07-03 自审纠偏：`qidahen-tutorial-coverage-matrix.md` 当时仍明确记录中立入侵、水路移动、战术牌打出时机缺真实入口；后续已补中立入侵、水路移动、骑兵城战减值和战术牌真实链，`3.3` 现在可恢复完成状态。
  - 2026-07-03 已补战术牌真实入口：`src/games/qidahen/tutorial.ts` 的 `tactic-window` 改为手牌区真实步骤，监听 `TACTIC_CARD_PLAYED`；`tutorialSetup.ts` 注入大明教程战术牌；`domain/commands.ts` / `commandEventBuilders.ts` / `resolvedEventReducers.ts` 新增 `PLAY_TACTIC_CARD` 到 `TACTIC_CARD_PLAYED` 的正式命令链；`battleInteractionBuilders.ts` 允许待结算战斗窗口先打战术牌再结算。`tutorialFlow.test.ts` 已断言教程不再用 `NEXT` 跳过，而是 dispatch `PLAY_TACTIC_CARD`，并确认手牌移除、弃牌数增加、摘要写入“打出战术牌”；`commands.test.ts` 已断言只有攻方能在待结算战斗中打出自己的战术牌。
  - 2026-07-03 已新增 `cavalry-plunder` 隐藏教程链：复用真实待结算战斗按钮 `cavalry-plunder-defender`，通过 `RESOLVE_PENDING_ACTION` 执行骑兵劫掠并写入结算摘要。
  - 2026-07-03 已新增 `cavalry-evasion` 隐藏教程链：复用真实待结算战斗按钮 `cavalry-evasion:city-region-19`，通过 `RESOLVE_PENDING_ACTION` 执行守方骑兵避战并写入结算摘要。
  - 2026-07-03 已新增 `neutral-invasion` 隐藏教程链：复用真实待结算战斗按钮，通过 `RESOLVE_PENDING_ACTION` 结算大明调度进攻土默特部时按人口生成中立守军，并确认攻方未突破时区域仍保持中立控制。
  - 2026-07-03 已新增并扩展 `water-dispatch` 隐藏教程链：复用真实轮盘进攻/调度候选，大明骑兵调度从皮岛选择东江，候选与待结算均锁定 `海岸/水路`、耗 2、战场宽度 2、最多投入 2 个部队；同一玩家视角还会显示 `皮岛 → 东江 → 登莱` 的纯水路续航候选，并断言 `city-region-25 / 锦州` 这类陆路后续目标不会进入候选。
  - 2026-07-03 已通过 `siege-and-occupation` 真实守城/城战链补到骑兵城战减值摘要证据：教程 setup 注入攻守双方结构化骑兵，守方真实点击 `守城避战` 后进入城战待结算，再由真实待结算按钮结算城战；摘要出现 `战斗掷骰（城战）`、`骑步` 与 `raw->value` 形式的减值结果。
  - 2026-07-03 已用完整七大恨关闭链路 E2E 复核：`node scripts/infra/run-e2e-single.mjs ci e2e/qidahen/qidahen-closeout.e2e.ts` 15/15 通过；`attack-and-battle` 真实点击教程战术牌后进入战斗承伤，`siege-and-occupation` 真实守城宣告、山海关城战待结算和围城选择均通过。
  - 2026-07-03 已补强 `korea-and-special-map-rules` E2E 核心状态断言：朝鲜牌库/弃牌堆为 9/3，`xian-xing`、`city-region-18`、`city-region-29` 人口均为 0；新年维护后结算摘要同时断言 `朝鲜耗损` 与 `非朝鲜区域`，证明朝鲜区不放人口且不计入普通控制区统计。
  - 2026-07-03 已补强 `year-and-characters` E2E 核心状态断言：年中摘要包含土地税赋、战败标记与非朝鲜区域统计，三方战败标记结算后归零；新年维护后摘要包含维护、兵力耗损、本年纪年卡、威望 +1 与非朝鲜区域统计，核心状态确认年份推进、纪年卡区刷新、蒙古支付手牌获得威望、大明新年人物出场。
  - 2026-07-03 已补强 `siege-and-occupation` E2E 核心状态断言：山海关城战后选择围城时，山海关仍由后金控制且城内人口状态保留；围城结算后仍保留后金控制，`siegeState` 记录大明为外围围城方并保留围城兵力。该证据收口“围城不立刻改控制权”和山海关控制联动。
  - 2026-07-04 已追加 `siege-and-occupation` 同章占领对照 E2E：从真实守城宣告和城战突破进入战后选择后点击 `占领该区`，断言摘要包含 `战后占领 / 山海关`，并确认山海关控制权改为大明、城内状态与围城状态清空；“攻下所有城市后占领成功”的教程同章对照缺口已关闭。
  - 2026-07-04 已追加 `wheel-shared-cost` 到隐藏续章的真实连续性 E2E：主章节 finish 后点击完成按钮会导航到 `/play/qidahen/tutorial/wheel-reclaim`，显示 `开垦不是回合外奖励` 的 overview，并生成 `15g-轮盘第7步-主章节续到开垦教程.png`；轮盘分支主章节与隐藏续章连续性缺口已关闭。
- [x] 3.4 更新中英文教程文案，移除作者旁白腔、编排解释和“这一步做什么”式常驻说明，只保留规则动作、对象、成本与结果。

## 4. Verification
- [x] 4.1 已补第一批定向单测，覆盖正式手牌最小身份、手牌入口合同与 `Board.tsx` 不再硬编码手牌规则。
- [x] 4.2 继续补齐七大恨定向单测，覆盖地区状态拆分和教程 manifest/步骤合同。
- [x] 4.3 回跑 `qidahen` 定向 E2E，刷新关键教程截图与正式流程截图。
- [x] 4.4 回写运行态真相源文档，确认 `qidahen-primary-interaction-audit.md` 与 `qidahen-tutorial-coverage-matrix.md` 已同步到最新实现，而不是停留在计划态。
- [x] 4.5 只有在正式局一级入口与教程规则覆盖都闭环后，才允许把《七大恨》上调到“新游戏接近收工”的口径；否则必须继续明确保留缺口。

2026-07-11 最终语义审计达到 49/49 张、0 个待核原子子句；完整代码门禁、16/16 教程 E2E、1/1 移动端 E2E、64 张最终截图服务器发布与逐图公开加载回查均已通过。正式局一级入口与教程规则覆盖闭环，游戏清单已移除“实施中”状态，4.5 完成。

七大恨已关闭三层窄口并补控制权边界：大明/蒙古使用无效果并移出游戏；后金可在实际控制的后金本土建立 2 个 3 级步兵；运行态中每有 1 张由后金持有的汉八旗/满八旗/蒙八旗持续事件，七大恨额外建立 1 个 3 级步兵，最多共 5 个。他势力持有的八旗、无关持续事件和单独存在的军备等级不增加数量；八旗军备等级只负责对应族属区域的后金本土判定，且仍必须搭配后金实际控制该区域才可建兵。其余附兵、增援与完整事件效果已由最终 49/49 语义审计和完整门禁统一闭环。

2026-07-10 甲喇地图表达口径更新：卡面、规则书、TTS 模组实体与本地 70 张素材均未提供甲喇专属图片；用户已明确批准以汉字 `甲喇` 作为东北大军所放甲喇标记的正式地图表达。运行态通过甲喇专用显式文字配置生成该标记，地图固定显示汉字 `甲喇`，不再等待专属图片；这一决定只适用于甲喇，不恢复其它缺图标记的通用文字降级，也不允许借用后金控制标记或其它派系素材。此前“不生成甲喇文字 token”的记录由本条覆盖；该口径已纳入最终 49/49 语义审计与 2.4 / 4.5 完成证据。

## 5. 附加能力矩阵事后纠偏
- [x] 5.1 `action-log`：实施本轮。`src/games/qidahen/game.ts` 已接入共享行动记录系统，并用七大恨领域记录生成玩家可读行动文本。
- [x] 5.2 `undo-system`：实施本轮。`src/games/qidahen/game.ts` 已接入共享撤回系统，`src/games/qidahen/Board.tsx` 已在局内壳层提供撤回上下文。
- [x] 5.3 `game-ai-system`：实施本轮。`src/games/qidahen/manifest.ts` 开启本地 AI 能力，`src/games/qidahen/game.ts` 已注册七大恨 AI 运行时。
- [x] 5.4 `tutorial-engine`：实施本轮。七大恨教程已接入共享教程系统，最终 closeout 记录已覆盖 16/16 教程 E2E。
- [x] 5.5 `debug-config`：实施本轮，2026-08-19 补漏。七大恨现已把区域蒙版编辑器和运行时预览挂进局内共享调试面板；没有注册到大厅工具栏目。本项不宣称已实现任意改牌、改资源或跳阶段等底层作弊命令，后者需要单独接 `cheat-system` / `createCheatSystem` 后才能交付。
