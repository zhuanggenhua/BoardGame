## 1. Specification And Architecture
- [x] 1.1 确认 `GameConfigPackage` 命名、能力边界和官方数据格式默认值。
- [x] 1.2 定义配置包 schema：游戏元数据、派系、卡牌/单位/事件/建筑、牌组构成、起始部署、素材引用、能力绑定。
- [x] 1.3 定义能力绑定合同：`abilityId + params`、实现状态、参数校验和未实现报错。
- [x] 1.4 定义玩家配置修正提案的数据结构与状态流转。

## 2. Shared Runtime
- [x] 2.1 新建共享配置包加载与校验 Module。
- [x] 2.2 实现严格 JSON 加载 adapter。
- [x] 2.3 实现配置包到运行时强类型对象的物化接口。
- [x] 2.4 增加校验测试，覆盖缺字段、重复 ID、非法能力 ID、非法参数和素材引用错误。

## 3. Review And Feedback Flow
- [x] 3.1 实现配置表格只读审查模型，确保展示数据来自配置包物化结果。
- [x] 3.2 实现字段级修正提案提交：保存对象 ID、字段路径、当前值、建议值、说明、证据和上下文。
- [ ] 3.3 接入 AI 首轮审查，输出建议接受、建议拒绝、需更多证据、需人工确认或需代码支持。（本轮已预留状态模型，实际 AI 审查动作待后续接入）
- [ ] 3.4 增加后台/管理视图或等价入口查看、筛选和处理配置修正提案。（按当前指令先隐藏，不作为本轮提交阻塞）
- [x] 3.5 重构旧游戏审查表 adapter 流程：字段清单先从规则真相源和运行时消费链反推，UI 列、字段值、适用性和编辑解析统一消费字段清单。

## 4. Legacy Compatibility Policy
- [x] 4.1 明确旧游戏默认不强制迁移，保留当前配置方案。
- [x] 4.2 定义旧游戏 opt-in adapter 的准入条件：明确需要统一表格审查、结构化修正提案或迁移到配置包数据源。
- [x] 4.3 提供一个低风险 adapter 模板或文档，供旧游戏后续单独接入时复用。

## 5. New Game Defaults
- [x] 5.1 更新新游戏 OpenSpec 模板或项目规则：新游戏静态配置默认使用配置包系统。
- [x] 5.2 编写开发者文档，说明严格 JSON 官方源、导入导出边界和玩家提案审核流程。
- [x] 5.3 对配置包能力运行 `openspec validate add-game-config-package-system --strict --no-interactive`。
